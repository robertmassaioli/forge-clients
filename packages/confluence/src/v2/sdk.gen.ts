// @ts-nocheck
import { ForgeApiError } from "@forge-clients/core";
import type { BoundClient } from "@forge-clients/core";
import type * as Types from "./types.gen.js";

// Generated by @forge-clients/generator — DO NOT EDIT
// Source: Confluence Cloud REST API v2
/* eslint-disable */
export * from './types.gen.js';

/**
 * Get Admin Key
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAdminKey(client: BoundClient): Promise<Types.AdminKeyResponse> {
    const path = '/admin-key';
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AdminKeyResponse>;
}

export interface EnableAdminKeyParams {
    body?: Record<string, unknown>;
}

/**
 * Enable Admin Key
 * @forge-scopes-asApp read:jira-work
 */
export async function enableAdminKey(client: BoundClient, params: EnableAdminKeyParams): Promise<Types.AdminKeyResponse> {
    const path = '/admin-key';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AdminKeyResponse>;
}

/**
 * Disable Admin Key
 * @forge-scopes-asApp read:jira-work
 */
export async function disableAdminKey(client: BoundClient): Promise<void> {
    const path = '/admin-key';
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAttachmentsParams {
    sort?: Types.AttachmentSortOrder;
    cursor?: string;
    status?: Array<"current" | "archived" | "trashed">;
    mediaType?: string;
    filename?: string;
    limit?: number;
}

/**
 * Get attachments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachments(client: BoundClient, params: GetAttachmentsParams): Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = '/attachments';
    const queryParams = {
      sort: params.sort,
      cursor: params.cursor,
      status: params.status,
      mediaType: params.mediaType,
      filename: params.filename,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetAttachmentByIdParams {
    path: { id: string };
    version?: number;
    includeLabels?: boolean;
    includeProperties?: boolean;
    includeOperations?: boolean;
    includeVersions?: boolean;
    includeVersion?: boolean;
    includeCollaborators?: boolean;
}

/**
 * Get attachment by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentById(client: BoundClient, params: GetAttachmentByIdParams): Promise<Types.AttachmentSingle & { _links?: { base?: string } }> {
    const path = `/attachments/${params.path.id}`;
    const queryParams = {
      version: params.version,
      includeLabels: params.includeLabels,
      includeProperties: params.includeProperties,
      includeOperations: params.includeOperations,
      includeVersions: params.includeVersions,
      includeVersion: params.includeVersion,
      includeCollaborators: params.includeCollaborators,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AttachmentSingle & { _links?: { base?: string } }>;
}

export interface DeleteAttachmentParams {
    path: { id: string };
    purge?: boolean;
}

/**
 * Delete attachment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteAttachment(client: BoundClient, params: DeleteAttachmentParams): Promise<void> {
    const path = `/attachments/${params.path.id}`;
    const queryParams = {
      purge: params.purge,
    };
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAttachmentLabelsParams {
    path: { id: string };
    prefix?: "my" | "team" | "global" | "system";
    sort?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Get labels for attachment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentLabels(client: BoundClient, params: GetAttachmentLabelsParams): Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }> {
    const path = `/attachments/${params.path.id}/labels`;
    const queryParams = {
      prefix: params.prefix,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }>;
}

export interface GetAttachmentOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for attachment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentOperations(client: BoundClient, params: GetAttachmentOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/attachments/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetAttachmentContentPropertiesParams {
    path: { attachmentId: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for attachment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentContentProperties(client: BoundClient, params: GetAttachmentContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/attachments/{attachment-id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateAttachmentPropertyParams {
    path: { attachmentId: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for attachment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createAttachmentProperty(client: BoundClient, params: CreateAttachmentPropertyParams): Promise<Types.ContentProperty> {
    const path = `/attachments/{attachment-id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetAttachmentContentPropertiesByIdParams {
    path: { attachmentId: string; propertyId: string };
}

/**
 * Get content property for attachment by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentContentPropertiesById(client: BoundClient, params: GetAttachmentContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/attachments/{attachment-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateAttachmentPropertyByIdParams {
    path: { attachmentId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for attachment by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateAttachmentPropertyById(client: BoundClient, params: UpdateAttachmentPropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/attachments/{attachment-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteAttachmentPropertyByIdParams {
    path: { attachmentId: string; propertyId: string };
}

/**
 * Delete content property for attachment by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteAttachmentPropertyById(client: BoundClient, params: DeleteAttachmentPropertyByIdParams): Promise<void> {
    const path = `/attachments/{attachment-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAttachmentVersionsParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: Types.VersionSortOrder;
}

/**
 * Get attachment versions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentVersions(client: BoundClient, params: GetAttachmentVersionsParams): Promise<{ results?: Array<Types.AttachmentVersion>; _links?: Types.MultiEntityLinks }> {
    const path = `/attachments/${params.path.id}/versions`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.AttachmentVersion>; _links?: Types.MultiEntityLinks }>;
}

export interface GetAttachmentVersionDetailsParams {
    path: { attachmentId: string; versionNumber: string };
}

/**
 * Get version details for attachment version
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentVersionDetails(client: BoundClient, params: GetAttachmentVersionDetailsParams): Promise<Types.DetailedVersion> {
    const path = `/attachments/{attachment-id}/versions/{version-number}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DetailedVersion>;
}

export interface GetAttachmentCommentsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
    sort?: Types.CommentSortOrder;
    version?: number;
}

/**
 * Get attachment comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentComments(client: BoundClient, params: GetAttachmentCommentsParams): Promise<{ results?: Array<Types.AttachmentCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/attachments/${params.path.id}/footer-comments`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
      version: params.version,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.AttachmentCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetBlogPostsParams {
    id?: Array<number>;
    spaceId?: Array<number>;
    sort?: Types.BlogPostSortOrder;
    status?: Array<"current" | "deleted" | "trashed">;
    title?: string;
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
}

/**
 * Get blog posts
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPosts(client: BoundClient, params: GetBlogPostsParams): Promise<{ results?: Array<Types.BlogPostBulk>; _links?: Types.MultiEntityLinks }> {
    const path = '/blogposts';
    const queryParams = {
      id: params.id,
      spaceId: params.spaceId,
      sort: params.sort,
      status: params.status,
      title: params.title,
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.BlogPostBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateBlogPostParams {
    private?: boolean;
    body?: Record<string, unknown>;
}

/**
 * Create blog post
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createBlogPost(client: BoundClient, params: CreateBlogPostParams): Promise<Types.BlogPostSingle & { _links?: { base?: string } }> {
    const path = '/blogposts';
    const queryParams = {
      private: params.private,
    };
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BlogPostSingle & { _links?: { base?: string } }>;
}

export interface GetBlogPostByIdParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentationSingle;
    getDraft?: boolean;
    status?: Array<"current" | "trashed" | "deleted" | "historical" | "draft">;
    version?: number;
    includeLabels?: boolean;
    includeProperties?: boolean;
    includeOperations?: boolean;
    includeLikes?: boolean;
    includeVersions?: boolean;
    includeVersion?: boolean;
    includeFavoritedByCurrentUserStatus?: boolean;
    includeWebresources?: boolean;
    includeCollaborators?: boolean;
}

/**
 * Get blog post by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostById(client: BoundClient, params: GetBlogPostByIdParams): Promise<Types.BlogPostSingle & { _links?: { base?: string } }> {
    const path = `/blogposts/${params.path.id}`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      getDraft: params.getDraft,
      status: params.status,
      version: params.version,
      includeLabels: params.includeLabels,
      includeProperties: params.includeProperties,
      includeOperations: params.includeOperations,
      includeLikes: params.includeLikes,
      includeVersions: params.includeVersions,
      includeVersion: params.includeVersion,
      includeFavoritedByCurrentUserStatus: params.includeFavoritedByCurrentUserStatus,
      includeWebresources: params.includeWebresources,
      includeCollaborators: params.includeCollaborators,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BlogPostSingle & { _links?: { base?: string } }>;
}

export interface UpdateBlogPostParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update blog post
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateBlogPost(client: BoundClient, params: UpdateBlogPostParams): Promise<Types.BlogPostSingle & { _links?: { base?: string } }> {
    const path = `/blogposts/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BlogPostSingle & { _links?: { base?: string } }>;
}

export interface DeleteBlogPostParams {
    path: { id: string };
    purge?: boolean;
    draft?: boolean;
}

/**
 * Delete blog post
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteBlogPost(client: BoundClient, params: DeleteBlogPostParams): Promise<void> {
    const path = `/blogposts/${params.path.id}`;
    const queryParams = {
      purge: params.purge,
      draft: params.draft,
    };
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBlogpostAttachmentsParams {
    path: { id: string };
    sort?: Types.AttachmentSortOrder;
    cursor?: string;
    status?: Array<"current" | "archived" | "trashed">;
    mediaType?: string;
    filename?: string;
    limit?: number;
}

/**
 * Get attachments for blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogpostAttachments(client: BoundClient, params: GetBlogpostAttachmentsParams): Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/${params.path.id}/attachments`;
    const queryParams = {
      sort: params.sort,
      cursor: params.cursor,
      status: params.status,
      mediaType: params.mediaType,
      filename: params.filename,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetCustomContentByTypeInBlogPostParams {
    path: { id: string };
    type: string;
    sort?: Types.CustomContentSortOrder;
    cursor?: string;
    limit?: number;
    bodyFormat?: Types.CustomContentBodyRepresentation;
}

/**
 * Get custom content by type in blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentByTypeInBlogPost(client: BoundClient, params: GetCustomContentByTypeInBlogPostParams): Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/${params.path.id}/custom-content`;
    const queryParams = {
      type: params.type,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
      bodyFormat: params.bodyFormat,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetBlogPostLabelsParams {
    path: { id: string };
    prefix?: "my" | "team" | "global" | "system";
    sort?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Get labels for blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostLabels(client: BoundClient, params: GetBlogPostLabelsParams): Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/${params.path.id}/labels`;
    const queryParams = {
      prefix: params.prefix,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }>;
}

export interface GetBlogPostLikeCountParams {
    path: { id: string };
}

/**
 * Get like count for blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostLikeCount(client: BoundClient, params: GetBlogPostLikeCountParams): Promise<{ count?: number }> {
    const path = `/blogposts/${params.path.id}/likes/count`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ count?: number }>;
}

export interface GetBlogPostLikeUsersParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
}

/**
 * Get account IDs of likes for blog post
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getBlogPostLikeUsers(client: BoundClient, params: GetBlogPostLikeUsersParams): Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/${params.path.id}/likes/users`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }>;
}

export interface GetBlogpostContentPropertiesParams {
    path: { blogpostId: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogpostContentProperties(client: BoundClient, params: GetBlogpostContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/{blogpost-id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateBlogpostPropertyParams {
    path: { blogpostId: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for blog post
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createBlogpostProperty(client: BoundClient, params: CreateBlogpostPropertyParams): Promise<Types.ContentProperty> {
    const path = `/blogposts/{blogpost-id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetBlogpostContentPropertiesByIdParams {
    path: { blogpostId: string; propertyId: string };
}

/**
 * Get content property for blog post by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogpostContentPropertiesById(client: BoundClient, params: GetBlogpostContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/blogposts/{blogpost-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateBlogpostPropertyByIdParams {
    path: { blogpostId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for blog post by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateBlogpostPropertyById(client: BoundClient, params: UpdateBlogpostPropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/blogposts/{blogpost-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteBlogpostPropertyByIdParams {
    path: { blogpostId: string; propertyId: string };
}

/**
 * Delete content property for blogpost by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteBlogpostPropertyById(client: BoundClient, params: DeleteBlogpostPropertyByIdParams): Promise<void> {
    const path = `/blogposts/{blogpost-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBlogPostOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostOperations(client: BoundClient, params: GetBlogPostOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/blogposts/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetBlogPostVersionsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
    sort?: Types.VersionSortOrder;
}

/**
 * Get blog post versions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostVersions(client: BoundClient, params: GetBlogPostVersionsParams): Promise<{ results?: Array<Types.BlogPostVersion>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/${params.path.id}/versions`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.BlogPostVersion>; _links?: Types.MultiEntityLinks }>;
}

export interface GetBlogPostVersionDetailsParams {
    path: { blogpostId: string; versionNumber: string };
}

/**
 * Get version details for blog post version
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostVersionDetails(client: BoundClient, params: GetBlogPostVersionDetailsParams): Promise<Types.DetailedVersion> {
    const path = `/blogposts/{blogpost-id}/versions/{version-number}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DetailedVersion>;
}

export interface ConvertContentIdsToContentTypesParams {
    body?: Record<string, unknown>;
}

/**
 * Convert content ids to content types
 * @forge-scopes-asApp read:jira-work
 */
export async function convertContentIdsToContentTypes(client: BoundClient, params: ConvertContentIdsToContentTypesParams): Promise<Types.ContentIdToContentTypeResponse> {
    const path = '/content/convert-ids-to-types';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentIdToContentTypeResponse>;
}

export interface GetCustomContentByTypeParams {
    type: string;
    id?: Array<number>;
    spaceId?: Array<number>;
    sort?: Types.CustomContentSortOrder;
    cursor?: string;
    limit?: number;
    bodyFormat?: Types.CustomContentBodyRepresentation;
}

/**
 * Get custom content by type
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentByType(client: BoundClient, params: GetCustomContentByTypeParams): Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = '/custom-content';
    const queryParams = {
      type: params.type,
      id: params.id,
      spaceId: params.spaceId,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
      bodyFormat: params.bodyFormat,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateCustomContentParams {
    body?: Record<string, unknown>;
}

/**
 * Create custom content
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCustomContent(client: BoundClient, params: CreateCustomContentParams): Promise<Types.CustomContentSingle & { _links?: { base?: string } }> {
    const path = '/custom-content';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomContentSingle & { _links?: { base?: string } }>;
}

export interface GetCustomContentByIdParams {
    path: { id: string };
    bodyFormat?: Types.CustomContentBodyRepresentationSingle;
    version?: number;
    includeLabels?: boolean;
    includeProperties?: boolean;
    includeOperations?: boolean;
    includeVersions?: boolean;
    includeVersion?: boolean;
    includeCollaborators?: boolean;
}

/**
 * Get custom content by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentById(client: BoundClient, params: GetCustomContentByIdParams): Promise<Types.CustomContentSingle & { _links?: { base?: string } }> {
    const path = `/custom-content/${params.path.id}`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      version: params.version,
      includeLabels: params.includeLabels,
      includeProperties: params.includeProperties,
      includeOperations: params.includeOperations,
      includeVersions: params.includeVersions,
      includeVersion: params.includeVersion,
      includeCollaborators: params.includeCollaborators,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomContentSingle & { _links?: { base?: string } }>;
}

export interface UpdateCustomContentParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update custom content
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCustomContent(client: BoundClient, params: UpdateCustomContentParams): Promise<Types.CustomContentSingle & { _links?: { base?: string } }> {
    const path = `/custom-content/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomContentSingle & { _links?: { base?: string } }>;
}

export interface DeleteCustomContentParams {
    path: { id: string };
    purge?: boolean;
}

/**
 * Delete custom content
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteCustomContent(client: BoundClient, params: DeleteCustomContentParams): Promise<void> {
    const path = `/custom-content/${params.path.id}`;
    const queryParams = {
      purge: params.purge,
    };
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetCustomContentAttachmentsParams {
    path: { id: string };
    sort?: Types.AttachmentSortOrder;
    cursor?: string;
    status?: Array<"current" | "archived" | "trashed">;
    mediaType?: string;
    filename?: string;
    limit?: number;
}

/**
 * Get attachments for custom content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentAttachments(client: BoundClient, params: GetCustomContentAttachmentsParams): Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/custom-content/${params.path.id}/attachments`;
    const queryParams = {
      sort: params.sort,
      cursor: params.cursor,
      status: params.status,
      mediaType: params.mediaType,
      filename: params.filename,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetCustomContentCommentsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
    sort?: Types.CommentSortOrder;
}

/**
 * Get custom content comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentComments(client: BoundClient, params: GetCustomContentCommentsParams): Promise<{ results?: Array<Types.CustomContentCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/custom-content/${params.path.id}/footer-comments`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CustomContentCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetCustomContentLabelsParams {
    path: { id: string };
    prefix?: "my" | "team" | "global" | "system";
    sort?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Get labels for custom content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentLabels(client: BoundClient, params: GetCustomContentLabelsParams): Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }> {
    const path = `/custom-content/${params.path.id}/labels`;
    const queryParams = {
      prefix: params.prefix,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }>;
}

export interface GetCustomContentOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for custom content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentOperations(client: BoundClient, params: GetCustomContentOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/custom-content/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetCustomContentContentPropertiesParams {
    path: { customContentId: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for custom content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentContentProperties(client: BoundClient, params: GetCustomContentContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/custom-content/{custom-content-id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateCustomContentPropertyParams {
    path: { customContentId: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for custom content
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCustomContentProperty(client: BoundClient, params: CreateCustomContentPropertyParams): Promise<Types.ContentProperty> {
    const path = `/custom-content/{custom-content-id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetCustomContentContentPropertiesByIdParams {
    path: { customContentId: string; propertyId: string };
}

/**
 * Get content property for custom content by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentContentPropertiesById(client: BoundClient, params: GetCustomContentContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/custom-content/{custom-content-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateCustomContentPropertyByIdParams {
    path: { customContentId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for custom content by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCustomContentPropertyById(client: BoundClient, params: UpdateCustomContentPropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/custom-content/{custom-content-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteCustomContentPropertyByIdParams {
    path: { customContentId: string; propertyId: string };
}

/**
 * Delete content property for custom content by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteCustomContentPropertyById(client: BoundClient, params: DeleteCustomContentPropertyByIdParams): Promise<void> {
    const path = `/custom-content/{custom-content-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetLabelsParams {
    labelId?: Array<number>;
    prefix?: Array<string>;
    cursor?: string;
    sort?: string;
    limit?: number;
}

/**
 * Get labels
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLabels(client: BoundClient, params: GetLabelsParams): Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }> {
    const path = '/labels';
    const queryParams = {
      labelId: params.labelId,
      prefix: params.prefix,
      cursor: params.cursor,
      sort: params.sort,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }>;
}

export interface GetLabelAttachmentsParams {
    path: { id: string };
    sort?: Types.AttachmentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get attachments for label
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLabelAttachments(client: BoundClient, params: GetLabelAttachmentsParams): Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/labels/${params.path.id}/attachments`;
    const queryParams = {
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetLabelBlogPostsParams {
    path: { id: string };
    spaceId?: Array<number>;
    bodyFormat?: Types.PrimaryBodyRepresentation;
    sort?: Types.BlogPostSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get blog posts for label
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLabelBlogPosts(client: BoundClient, params: GetLabelBlogPostsParams): Promise<{ results?: Array<Types.BlogPostBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/labels/${params.path.id}/blogposts`;
    const queryParams = {
      spaceId: params.spaceId,
      bodyFormat: params.bodyFormat,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.BlogPostBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetLabelPagesParams {
    path: { id: string };
    spaceId?: Array<number>;
    bodyFormat?: Types.PrimaryBodyRepresentation;
    sort?: Types.PageSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get pages for label
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLabelPages(client: BoundClient, params: GetLabelPagesParams): Promise<{ results?: Array<Types.PageBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/labels/${params.path.id}/pages`;
    const queryParams = {
      spaceId: params.spaceId,
      bodyFormat: params.bodyFormat,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.PageBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPagesParams {
    id?: Array<number>;
    spaceId?: Array<number>;
    sort?: Types.PageSortOrder;
    status?: Array<"current" | "archived" | "deleted" | "trashed">;
    title?: string;
    bodyFormat?: Types.PrimaryBodyRepresentation;
    subtype?: "live" | "page";
    cursor?: string;
    limit?: number;
}

/**
 * Get pages
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPages(client: BoundClient, params: GetPagesParams): Promise<{ results?: Array<Types.PageBulk>; _links?: Types.MultiEntityLinks }> {
    const path = '/pages';
    const queryParams = {
      id: params.id,
      spaceId: params.spaceId,
      sort: params.sort,
      status: params.status,
      title: params.title,
      bodyFormat: params.bodyFormat,
      subtype: params.subtype,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.PageBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface CreatePageParams {
    embedded?: boolean;
    private?: boolean;
    rootLevel?: boolean;
    body?: Record<string, unknown>;
}

/**
 * Create page
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPage(client: BoundClient, params: CreatePageParams): Promise<Types.PageSingle & { _links?: { base?: string } }> {
    const path = '/pages';
    const queryParams = {
      embedded: params.embedded,
      private: params.private,
      rootLevel: params.rootLevel,
    };
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageSingle & { _links?: { base?: string } }>;
}

export interface GetPageByIdParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentationSingle;
    getDraft?: boolean;
    status?: Array<"current" | "archived" | "trashed" | "deleted" | "historical" | "draft">;
    version?: number;
    includeLabels?: boolean;
    includeProperties?: boolean;
    includeOperations?: boolean;
    includeLikes?: boolean;
    includeVersions?: boolean;
    includeVersion?: boolean;
    includeFavoritedByCurrentUserStatus?: boolean;
    includeWebresources?: boolean;
    includeCollaborators?: boolean;
    includeDirectChildren?: boolean;
}

/**
 * Get page by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageById(client: BoundClient, params: GetPageByIdParams): Promise<Types.PageSingle & { _links?: { base?: string } }> {
    const path = `/pages/${params.path.id}`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      getDraft: params.getDraft,
      status: params.status,
      version: params.version,
      includeLabels: params.includeLabels,
      includeProperties: params.includeProperties,
      includeOperations: params.includeOperations,
      includeLikes: params.includeLikes,
      includeVersions: params.includeVersions,
      includeVersion: params.includeVersion,
      includeFavoritedByCurrentUserStatus: params.includeFavoritedByCurrentUserStatus,
      includeWebresources: params.includeWebresources,
      includeCollaborators: params.includeCollaborators,
      includeDirectChildren: params.includeDirectChildren,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageSingle & { _links?: { base?: string } }>;
}

export interface UpdatePageParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update page
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePage(client: BoundClient, params: UpdatePageParams): Promise<Types.PageSingle & { _links?: { base?: string } }> {
    const path = `/pages/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageSingle & { _links?: { base?: string } }>;
}

export interface DeletePageParams {
    path: { id: string };
    purge?: boolean;
    draft?: boolean;
}

/**
 * Delete page
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePage(client: BoundClient, params: DeletePageParams): Promise<void> {
    const path = `/pages/${params.path.id}`;
    const queryParams = {
      purge: params.purge,
      draft: params.draft,
    };
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPageAttachmentsParams {
    path: { id: string };
    sort?: Types.AttachmentSortOrder;
    cursor?: string;
    status?: Array<"current" | "archived" | "trashed">;
    mediaType?: string;
    filename?: string;
    limit?: number;
}

/**
 * Get attachments for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageAttachments(client: BoundClient, params: GetPageAttachmentsParams): Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/attachments`;
    const queryParams = {
      sort: params.sort,
      cursor: params.cursor,
      status: params.status,
      mediaType: params.mediaType,
      filename: params.filename,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.AttachmentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetCustomContentByTypeInPageParams {
    path: { id: string };
    type: string;
    sort?: Types.CustomContentSortOrder;
    cursor?: string;
    limit?: number;
    bodyFormat?: Types.CustomContentBodyRepresentation;
}

/**
 * Get custom content by type in page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentByTypeInPage(client: BoundClient, params: GetCustomContentByTypeInPageParams): Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/custom-content`;
    const queryParams = {
      type: params.type,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
      bodyFormat: params.bodyFormat,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageLabelsParams {
    path: { id: string };
    prefix?: "my" | "team" | "global" | "system";
    sort?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Get labels for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageLabels(client: BoundClient, params: GetPageLabelsParams): Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/labels`;
    const queryParams = {
      prefix: params.prefix,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageLikeCountParams {
    path: { id: string };
}

/**
 * Get like count for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageLikeCount(client: BoundClient, params: GetPageLikeCountParams): Promise<{ count?: number }> {
    const path = `/pages/${params.path.id}/likes/count`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ count?: number }>;
}

export interface GetPageLikeUsersParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
}

/**
 * Get account IDs of likes for page
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getPageLikeUsers(client: BoundClient, params: GetPageLikeUsersParams): Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/likes/users`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageOperations(client: BoundClient, params: GetPageOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/pages/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetPageContentPropertiesParams {
    path: { pageId: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageContentProperties(client: BoundClient, params: GetPageContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/{page-id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreatePagePropertyParams {
    path: { pageId: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for page
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPageProperty(client: BoundClient, params: CreatePagePropertyParams): Promise<Types.ContentProperty> {
    const path = `/pages/{page-id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetPageContentPropertiesByIdParams {
    path: { pageId: string; propertyId: string };
}

/**
 * Get content property for page by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageContentPropertiesById(client: BoundClient, params: GetPageContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/pages/{page-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdatePagePropertyByIdParams {
    path: { pageId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for page by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePagePropertyById(client: BoundClient, params: UpdatePagePropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/pages/{page-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeletePagePropertyByIdParams {
    path: { pageId: string; propertyId: string };
}

/**
 * Delete content property for page by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePagePropertyById(client: BoundClient, params: DeletePagePropertyByIdParams): Promise<void> {
    const path = `/pages/{page-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface PostRedactPageParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Redact Content in a Confluence Page
 * @forge-scopes-asApp read:jira-work
 */
export async function postRedactPage(client: BoundClient, params: PostRedactPageParams): Promise<Types.RedactionResponse> {
    const path = `/pages/${params.path.id}/redact`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RedactionResponse>;
}

export interface PostRedactBlogParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Redact Content in a Confluence Blog Post
 * @forge-scopes-asApp read:jira-work
 */
export async function postRedactBlog(client: BoundClient, params: PostRedactBlogParams): Promise<Types.RedactionResponse> {
    const path = `/blogposts/${params.path.id}/redact`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RedactionResponse>;
}

export interface UpdatePageTitleParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update page title
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePageTitle(client: BoundClient, params: UpdatePageTitleParams): Promise<Types.PageSingle & { _links?: { base?: string } }> {
    const path = `/pages/${params.path.id}/title`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageSingle & { _links?: { base?: string } }>;
}

export interface GetPageVersionsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
    sort?: Types.VersionSortOrder;
}

/**
 * Get page versions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageVersions(client: BoundClient, params: GetPageVersionsParams): Promise<{ results?: Array<Types.PageVersion>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/versions`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.PageVersion>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateWhiteboardParams {
    private?: boolean;
    body?: Record<string, unknown>;
}

/**
 * Create whiteboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createWhiteboard(client: BoundClient, params: CreateWhiteboardParams): Promise<Types.WhiteboardSingle & { _links?: { base?: string } }> {
    const path = '/whiteboards';
    const queryParams = {
      private: params.private,
    };
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WhiteboardSingle & { _links?: { base?: string } }>;
}

export interface GetWhiteboardByIdParams {
    path: { id: string };
    includeCollaborators?: boolean;
    includeDirectChildren?: boolean;
    includeOperations?: boolean;
    includeProperties?: boolean;
}

/**
 * Get whiteboard by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardById(client: BoundClient, params: GetWhiteboardByIdParams): Promise<Types.WhiteboardSingle & { _links?: { base?: string } }> {
    const path = `/whiteboards/${params.path.id}`;
    const queryParams = {
      includeCollaborators: params.includeCollaborators,
      includeDirectChildren: params.includeDirectChildren,
      includeOperations: params.includeOperations,
      includeProperties: params.includeProperties,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WhiteboardSingle & { _links?: { base?: string } }>;
}

export interface DeleteWhiteboardParams {
    path: { id: string };
}

/**
 * Delete whiteboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWhiteboard(client: BoundClient, params: DeleteWhiteboardParams): Promise<void> {
    const path = `/whiteboards/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWhiteboardContentPropertiesParams {
    path: { id: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for whiteboard
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardContentProperties(client: BoundClient, params: GetWhiteboardContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/whiteboards/${params.path.id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateWhiteboardPropertyParams {
    path: { id: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for whiteboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createWhiteboardProperty(client: BoundClient, params: CreateWhiteboardPropertyParams): Promise<Types.ContentProperty> {
    const path = `/whiteboards/${params.path.id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetWhiteboardContentPropertiesByIdParams {
    path: { whiteboardId: string; propertyId: string };
}

/**
 * Get content property for whiteboard by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardContentPropertiesById(client: BoundClient, params: GetWhiteboardContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/whiteboards/{whiteboard-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateWhiteboardPropertyByIdParams {
    path: { whiteboardId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for whiteboard by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateWhiteboardPropertyById(client: BoundClient, params: UpdateWhiteboardPropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/whiteboards/{whiteboard-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteWhiteboardPropertyByIdParams {
    path: { whiteboardId: string; propertyId: string };
}

/**
 * Delete content property for whiteboard by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWhiteboardPropertyById(client: BoundClient, params: DeleteWhiteboardPropertyByIdParams): Promise<void> {
    const path = `/whiteboards/{whiteboard-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWhiteboardOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for a whiteboard
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardOperations(client: BoundClient, params: GetWhiteboardOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/whiteboards/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetWhiteboardDirectChildrenParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: string;
}

/**
 * Get direct children of a whiteboard
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardDirectChildren(client: BoundClient, params: GetWhiteboardDirectChildrenParams): Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/whiteboards/${params.path.id}/direct-children`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetWhiteboardDescendantsParams {
    path: { id: string };
    limit?: number;
    depth?: number;
    cursor?: string;
}

/**
 * Get descendants of a whiteboard
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardDescendants(client: BoundClient, params: GetWhiteboardDescendantsParams): Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/whiteboards/${params.path.id}/descendants`;
    const queryParams = {
      limit: params.limit,
      depth: params.depth,
      cursor: params.cursor,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetWhiteboardAncestorsParams {
    path: { id: string };
    limit?: number;
}

/**
 * Get all ancestors of whiteboard
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardAncestors(client: BoundClient, params: GetWhiteboardAncestorsParams): Promise<{ results?: Array<Types.Ancestor> }> {
    const path = `/whiteboards/${params.path.id}/ancestors`;
    const queryParams = {
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Ancestor> }>;
}

export interface CreateDatabaseParams {
    private?: boolean;
    body?: Record<string, unknown>;
}

/**
 * Create database
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createDatabase(client: BoundClient, params: CreateDatabaseParams): Promise<Types.DatabaseSingle & { _links?: { base?: string } }> {
    const path = '/databases';
    const queryParams = {
      private: params.private,
    };
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DatabaseSingle & { _links?: { base?: string } }>;
}

export interface GetDatabaseByIdParams {
    path: { id: string };
    includeCollaborators?: boolean;
    includeDirectChildren?: boolean;
    includeOperations?: boolean;
    includeProperties?: boolean;
}

/**
 * Get database by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseById(client: BoundClient, params: GetDatabaseByIdParams): Promise<Types.DatabaseSingle & { _links?: { base?: string } }> {
    const path = `/databases/${params.path.id}`;
    const queryParams = {
      includeCollaborators: params.includeCollaborators,
      includeDirectChildren: params.includeDirectChildren,
      includeOperations: params.includeOperations,
      includeProperties: params.includeProperties,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DatabaseSingle & { _links?: { base?: string } }>;
}

export interface DeleteDatabaseParams {
    path: { id: string };
}

/**
 * Delete database
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDatabase(client: BoundClient, params: DeleteDatabaseParams): Promise<void> {
    const path = `/databases/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDatabaseContentPropertiesParams {
    path: { id: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for database
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseContentProperties(client: BoundClient, params: GetDatabaseContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/databases/${params.path.id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateDatabasePropertyParams {
    path: { id: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for database
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createDatabaseProperty(client: BoundClient, params: CreateDatabasePropertyParams): Promise<Types.ContentProperty> {
    const path = `/databases/${params.path.id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetDatabaseContentPropertiesByIdParams {
    path: { databaseId: string; propertyId: string };
}

/**
 * Get content property for database by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseContentPropertiesById(client: BoundClient, params: GetDatabaseContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/databases/{database-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateDatabasePropertyByIdParams {
    path: { databaseId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for database by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateDatabasePropertyById(client: BoundClient, params: UpdateDatabasePropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/databases/{database-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteDatabasePropertyByIdParams {
    path: { databaseId: string; propertyId: string };
}

/**
 * Delete content property for database by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDatabasePropertyById(client: BoundClient, params: DeleteDatabasePropertyByIdParams): Promise<void> {
    const path = `/databases/{database-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDatabaseOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for a database
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseOperations(client: BoundClient, params: GetDatabaseOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/databases/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetDatabaseDirectChildrenParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: string;
}

/**
 * Get direct children of a database
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseDirectChildren(client: BoundClient, params: GetDatabaseDirectChildrenParams): Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/databases/${params.path.id}/direct-children`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetDatabaseDescendantsParams {
    path: { id: string };
    limit?: number;
    depth?: number;
    cursor?: string;
}

/**
 * Get descendants of a database
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseDescendants(client: BoundClient, params: GetDatabaseDescendantsParams): Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/databases/${params.path.id}/descendants`;
    const queryParams = {
      limit: params.limit,
      depth: params.depth,
      cursor: params.cursor,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetDatabaseAncestorsParams {
    path: { id: string };
    limit?: number;
}

/**
 * Get all ancestors of database
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseAncestors(client: BoundClient, params: GetDatabaseAncestorsParams): Promise<{ results?: Array<Types.Ancestor> }> {
    const path = `/databases/${params.path.id}/ancestors`;
    const queryParams = {
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Ancestor> }>;
}

export interface CreateSmartLinkParams {
    body?: Record<string, unknown>;
}

/**
 * Create Smart Link in the content tree
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createSmartLink(client: BoundClient, params: CreateSmartLinkParams): Promise<Types.SmartLinkSingle & { _links?: { base?: string } }> {
    const path = '/embeds';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SmartLinkSingle & { _links?: { base?: string } }>;
}

export interface GetSmartLinkByIdParams {
    path: { id: string };
    includeCollaborators?: boolean;
    includeDirectChildren?: boolean;
    includeOperations?: boolean;
    includeProperties?: boolean;
}

/**
 * Get Smart Link in the content tree by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSmartLinkById(client: BoundClient, params: GetSmartLinkByIdParams): Promise<Types.SmartLinkSingle & { _links?: { base?: string } }> {
    const path = `/embeds/${params.path.id}`;
    const queryParams = {
      includeCollaborators: params.includeCollaborators,
      includeDirectChildren: params.includeDirectChildren,
      includeOperations: params.includeOperations,
      includeProperties: params.includeProperties,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SmartLinkSingle & { _links?: { base?: string } }>;
}

export interface DeleteSmartLinkParams {
    path: { id: string };
}

/**
 * Delete Smart Link in the content tree
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSmartLink(client: BoundClient, params: DeleteSmartLinkParams): Promise<void> {
    const path = `/embeds/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetSmartLinkContentPropertiesParams {
    path: { id: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for Smart Link in the content tree
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSmartLinkContentProperties(client: BoundClient, params: GetSmartLinkContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/embeds/${params.path.id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateSmartLinkPropertyParams {
    path: { id: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for Smart Link in the content tree
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createSmartLinkProperty(client: BoundClient, params: CreateSmartLinkPropertyParams): Promise<Types.ContentProperty> {
    const path = `/embeds/${params.path.id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetSmartLinkContentPropertiesByIdParams {
    path: { embedId: string; propertyId: string };
}

/**
 * Get content property for Smart Link in the content tree by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSmartLinkContentPropertiesById(client: BoundClient, params: GetSmartLinkContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/embeds/{embed-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateSmartLinkPropertyByIdParams {
    path: { embedId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for Smart Link in the content tree by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSmartLinkPropertyById(client: BoundClient, params: UpdateSmartLinkPropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/embeds/{embed-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteSmartLinkPropertyByIdParams {
    path: { embedId: string; propertyId: string };
}

/**
 * Delete content property for Smart Link in the content tree by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSmartLinkPropertyById(client: BoundClient, params: DeleteSmartLinkPropertyByIdParams): Promise<void> {
    const path = `/embeds/{embed-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetSmartLinkOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for a Smart Link in the content tree
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSmartLinkOperations(client: BoundClient, params: GetSmartLinkOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/embeds/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetSmartLinkDirectChildrenParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: string;
}

/**
 * Get direct children of a Smart Link
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSmartLinkDirectChildren(client: BoundClient, params: GetSmartLinkDirectChildrenParams): Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/embeds/${params.path.id}/direct-children`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetSmartLinkDescendantsParams {
    path: { id: string };
    limit?: number;
    depth?: number;
    cursor?: string;
}

/**
 * Get descendants of a smart link
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSmartLinkDescendants(client: BoundClient, params: GetSmartLinkDescendantsParams): Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/embeds/${params.path.id}/descendants`;
    const queryParams = {
      limit: params.limit,
      depth: params.depth,
      cursor: params.cursor,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetSmartLinkAncestorsParams {
    path: { id: string };
    limit?: number;
}

/**
 * Get all ancestors of Smart Link in content tree
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSmartLinkAncestors(client: BoundClient, params: GetSmartLinkAncestorsParams): Promise<{ results?: Array<Types.Ancestor> }> {
    const path = `/embeds/${params.path.id}/ancestors`;
    const queryParams = {
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Ancestor> }>;
}

export interface CreateFolderParams {
    body?: Record<string, unknown>;
}

/**
 * Create folder
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createFolder(client: BoundClient, params: CreateFolderParams): Promise<Types.FolderSingle & { _links?: { base?: string } }> {
    const path = '/folders';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FolderSingle & { _links?: { base?: string } }>;
}

export interface GetFolderByIdParams {
    path: { id: string };
    includeCollaborators?: boolean;
    includeDirectChildren?: boolean;
    includeOperations?: boolean;
    includeProperties?: boolean;
}

/**
 * Get folder by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFolderById(client: BoundClient, params: GetFolderByIdParams): Promise<Types.FolderSingle & { _links?: { base?: string } }> {
    const path = `/folders/${params.path.id}`;
    const queryParams = {
      includeCollaborators: params.includeCollaborators,
      includeDirectChildren: params.includeDirectChildren,
      includeOperations: params.includeOperations,
      includeProperties: params.includeProperties,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FolderSingle & { _links?: { base?: string } }>;
}

export interface DeleteFolderParams {
    path: { id: string };
}

/**
 * Delete folder
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFolder(client: BoundClient, params: DeleteFolderParams): Promise<void> {
    const path = `/folders/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFolderContentPropertiesParams {
    path: { id: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for folder
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFolderContentProperties(client: BoundClient, params: GetFolderContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/folders/${params.path.id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateFolderPropertyParams {
    path: { id: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for folder
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createFolderProperty(client: BoundClient, params: CreateFolderPropertyParams): Promise<Types.ContentProperty> {
    const path = `/folders/${params.path.id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetFolderContentPropertiesByIdParams {
    path: { folderId: string; propertyId: string };
}

/**
 * Get content property for folder by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFolderContentPropertiesById(client: BoundClient, params: GetFolderContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/folders/{folder-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateFolderPropertyByIdParams {
    path: { folderId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for folder by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateFolderPropertyById(client: BoundClient, params: UpdateFolderPropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/folders/{folder-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteFolderPropertyByIdParams {
    path: { folderId: string; propertyId: string };
}

/**
 * Delete content property for folder by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFolderPropertyById(client: BoundClient, params: DeleteFolderPropertyByIdParams): Promise<void> {
    const path = `/folders/{folder-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFolderOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for a folder
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFolderOperations(client: BoundClient, params: GetFolderOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/folders/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetFolderDirectChildrenParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: string;
}

/**
 * Get direct children of a folder
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFolderDirectChildren(client: BoundClient, params: GetFolderDirectChildrenParams): Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/folders/${params.path.id}/direct-children`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetFolderDescendantsParams {
    path: { id: string };
    limit?: number;
    depth?: number;
    cursor?: string;
}

/**
 * Get descendants of folder
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFolderDescendants(client: BoundClient, params: GetFolderDescendantsParams): Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/folders/${params.path.id}/descendants`;
    const queryParams = {
      limit: params.limit,
      depth: params.depth,
      cursor: params.cursor,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetFolderAncestorsParams {
    path: { id: string };
    limit?: number;
}

/**
 * Get all ancestors of folder
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFolderAncestors(client: BoundClient, params: GetFolderAncestorsParams): Promise<{ results?: Array<Types.Ancestor> }> {
    const path = `/folders/${params.path.id}/ancestors`;
    const queryParams = {
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Ancestor> }>;
}

export interface GetPageVersionDetailsParams {
    path: { pageId: string; versionNumber: string };
}

/**
 * Get version details for page version
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageVersionDetails(client: BoundClient, params: GetPageVersionDetailsParams): Promise<Types.DetailedVersion> {
    const path = `/pages/{page-id}/versions/{version-number}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DetailedVersion>;
}

export interface GetCustomContentVersionsParams {
    path: { customContentId: string };
    bodyFormat?: Types.CustomContentBodyRepresentation;
    cursor?: string;
    limit?: number;
    sort?: Types.VersionSortOrder;
}

/**
 * Get custom content versions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentVersions(client: BoundClient, params: GetCustomContentVersionsParams): Promise<{ results?: Array<Types.CustomContentVersion>; _links?: Types.MultiEntityLinks }> {
    const path = `/custom-content/{custom-content-id}/versions`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CustomContentVersion>; _links?: Types.MultiEntityLinks }>;
}

export interface GetCustomContentVersionDetailsParams {
    path: { customContentId: string; versionNumber: string };
}

/**
 * Get version details for custom content version
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentVersionDetails(client: BoundClient, params: GetCustomContentVersionDetailsParams): Promise<Types.DetailedVersion> {
    const path = `/custom-content/{custom-content-id}/versions/{version-number}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DetailedVersion>;
}

export interface GetSpacesParams {
    ids?: Array<number>;
    keys?: Array<string>;
    type?: "global" | "collaboration" | "knowledge_base" | "personal" | "system" | "onboarding" | "xflow_sample_space";
    status?: "current" | "archived";
    labels?: Array<string>;
    favoritedBy?: string;
    notFavoritedBy?: string;
    sort?: Types.SpaceSortOrder;
    descriptionFormat?: Types.SpaceDescriptionBodyRepresentation;
    includeIcon?: boolean;
    cursor?: string;
    limit?: number;
}

/**
 * Get spaces
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaces(client: BoundClient, params: GetSpacesParams): Promise<{ results?: Array<Types.SpaceBulk>; _links?: Types.MultiEntityLinks }> {
    const path = '/spaces';
    const queryParams = {
      ids: params.ids,
      keys: params.keys,
      type: params.type,
      status: params.status,
      labels: params.labels,
      favoritedBy: params.favoritedBy,
      notFavoritedBy: params.notFavoritedBy,
      sort: params.sort,
      descriptionFormat: params.descriptionFormat,
      includeIcon: params.includeIcon,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.SpaceBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateSpaceParams {
    body?: Record<string, unknown>;
}

/**
 * Create space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createSpace(client: BoundClient, params: CreateSpaceParams): Promise<Types.SpaceBulk & { _links?: { base?: string } }> {
    const path = '/spaces';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceBulk & { _links?: { base?: string } }>;
}

export interface GetSpaceByIdParams {
    path: { id: string };
    descriptionFormat?: Types.SpaceDescriptionBodyRepresentation;
    includeIcon?: boolean;
    includeOperations?: boolean;
    includeProperties?: boolean;
    includePermissions?: boolean;
    includeRoleAssignments?: boolean;
    includeLabels?: boolean;
}

/**
 * Get space by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceById(client: BoundClient, params: GetSpaceByIdParams): Promise<Types.SpaceSingle & { _links?: { base?: string } }> {
    const path = `/spaces/${params.path.id}`;
    const queryParams = {
      descriptionFormat: params.descriptionFormat,
      includeIcon: params.includeIcon,
      includeOperations: params.includeOperations,
      includeProperties: params.includeProperties,
      includePermissions: params.includePermissions,
      includeRoleAssignments: params.includeRoleAssignments,
      includeLabels: params.includeLabels,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceSingle & { _links?: { base?: string } }>;
}

export interface GetBlogPostsInSpaceParams {
    path: { id: string };
    sort?: Types.BlogPostSortOrder;
    status?: Array<"current" | "deleted" | "trashed">;
    title?: string;
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
}

/**
 * Get blog posts in space
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostsInSpace(client: BoundClient, params: GetBlogPostsInSpaceParams): Promise<{ results?: Array<Types.BlogPostBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/blogposts`;
    const queryParams = {
      sort: params.sort,
      status: params.status,
      title: params.title,
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.BlogPostBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetSpaceLabelsParams {
    path: { id: string };
    prefix?: "my" | "team";
    sort?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Get labels for space
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceLabels(client: BoundClient, params: GetSpaceLabelsParams): Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/labels`;
    const queryParams = {
      prefix: params.prefix,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }>;
}

export interface GetSpaceContentLabelsParams {
    path: { id: string };
    prefix?: "my" | "team";
    sort?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Get labels for space content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceContentLabels(client: BoundClient, params: GetSpaceContentLabelsParams): Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/content/labels`;
    const queryParams = {
      prefix: params.prefix,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Label>; _links?: Types.MultiEntityLinks }>;
}

export interface GetCustomContentByTypeInSpaceParams {
    path: { id: string };
    type: string;
    cursor?: string;
    limit?: number;
    bodyFormat?: Types.CustomContentBodyRepresentation;
}

/**
 * Get custom content by type in space
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentByTypeInSpace(client: BoundClient, params: GetCustomContentByTypeInSpaceParams): Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/custom-content`;
    const queryParams = {
      type: params.type,
      cursor: params.cursor,
      limit: params.limit,
      bodyFormat: params.bodyFormat,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CustomContentBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetSpaceOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for space
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceOperations(client: BoundClient, params: GetSpaceOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/spaces/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetPagesInSpaceParams {
    path: { id: string };
    depth?: "all" | "root";
    sort?: Types.PageSortOrder;
    status?: Array<"current" | "archived" | "deleted" | "trashed">;
    title?: string;
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
}

/**
 * Get pages in space
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPagesInSpace(client: BoundClient, params: GetPagesInSpaceParams): Promise<{ results?: Array<Types.PageBulk>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/pages`;
    const queryParams = {
      depth: params.depth,
      sort: params.sort,
      status: params.status,
      title: params.title,
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.PageBulk>; _links?: Types.MultiEntityLinks }>;
}

export interface GetSpacePropertiesParams {
    path: { spaceId: string };
    key?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Get space properties in space
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceProperties(client: BoundClient, params: GetSpacePropertiesParams): Promise<{ results?: Array<Types.SpaceProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/{space-id}/properties`;
    const queryParams = {
      key: params.key,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.SpaceProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateSpacePropertyParams {
    path: { spaceId: string };
    body?: Types.SpacePropertyCreateRequest;
}

/**
 * Create space property in space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createSpaceProperty(client: BoundClient, params: CreateSpacePropertyParams): Promise<Types.SpaceProperty> {
    const path = `/spaces/{space-id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceProperty>;
}

export interface GetSpacePropertyByIdParams {
    path: { spaceId: string; propertyId: string };
}

/**
 * Get space property by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpacePropertyById(client: BoundClient, params: GetSpacePropertyByIdParams): Promise<Types.SpaceProperty> {
    const path = `/spaces/{space-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceProperty>;
}

export interface UpdateSpacePropertyByIdParams {
    path: { spaceId: string; propertyId: string };
    body?: Types.SpacePropertyUpdateRequest;
}

/**
 * Update space property by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSpacePropertyById(client: BoundClient, params: UpdateSpacePropertyByIdParams): Promise<Types.SpaceProperty> {
    const path = `/spaces/{space-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceProperty>;
}

export interface DeleteSpacePropertyByIdParams {
    path: { spaceId: string; propertyId: string };
}

/**
 * Delete space property by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSpacePropertyById(client: BoundClient, params: DeleteSpacePropertyByIdParams): Promise<void> {
    const path = `/spaces/{space-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetSpacePermissionsAssignmentsParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
}

/**
 * Get space permissions assignments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpacePermissionsAssignments(client: BoundClient, params: GetSpacePermissionsAssignmentsParams): Promise<{ results?: Array<Types.SpacePermissionAssignment>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/permissions`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.SpacePermissionAssignment>; _links?: Types.MultiEntityLinks }>;
}

export interface GetAvailableSpacePermissionsParams {
    cursor?: string;
    limit?: number;
}

/**
 * Get available space permissions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvailableSpacePermissions(client: BoundClient, params: GetAvailableSpacePermissionsParams): Promise<{ results?: Array<Types.SpacePermission>; _links?: Types.MultiEntityLinks }> {
    const path = '/space-permissions';
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.SpacePermission>; _links?: Types.MultiEntityLinks }>;
}

export interface GetAvailableSpaceRolesParams {
    spaceId?: string;
    roleType?: string;
    principalId?: string;
    principalType?: Types.PrincipalType;
    cursor?: string;
    limit?: number;
}

/**
 * Get available space roles
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvailableSpaceRoles(client: BoundClient, params: GetAvailableSpaceRolesParams): Promise<{ results?: Array<Types.SpaceRole>; _links?: Types.MultiEntityLinks }> {
    const path = '/space-roles';
    const queryParams = {
      spaceId: params.spaceId,
      roleType: params.roleType,
      principalId: params.principalId,
      principalType: params.principalType,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.SpaceRole>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateSpaceRoleParams {
    body?: { name: string; description: string; spacePermissions: Array<string> };
}

/**
 * Create a space role
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createSpaceRole(client: BoundClient, params: CreateSpaceRoleParams): Promise<Types.SpaceRole> {
    const path = '/space-roles';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceRole>;
}

export interface GetSpaceRolesByIdParams {
    path: { id: string };
}

/**
 * Get space role by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceRolesById(client: BoundClient, params: GetSpaceRolesByIdParams): Promise<Types.SpaceRole & { _links?: { base?: string } }> {
    const path = `/space-roles/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceRole & { _links?: { base?: string } }>;
}

export interface UpdateSpaceRoleParams {
    path: { id: string };
    body?: { name: string; description: string; spacePermissions: Array<string>; anonymousReassignmentRoleId?: string; guestReassignmentRoleId?: string };
}

/**
 * Update a space role
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSpaceRole(client: BoundClient, params: UpdateSpaceRoleParams): Promise<Types.UpdateSpaceRoleResponse> {
    const path = `/space-roles/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UpdateSpaceRoleResponse>;
}

export interface DeleteSpaceRoleParams {
    path: { id: string };
}

/**
 * Delete a space role
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSpaceRole(client: BoundClient, params: DeleteSpaceRoleParams): Promise<Types.DeleteSpaceRoleResponse> {
    const path = `/space-roles/${params.path.id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DeleteSpaceRoleResponse>;
}

/**
 * Get space role mode
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceRoleMode(client: BoundClient): Promise<{ mode?: "PRE_ROLES" | "ROLES_TRANSITION" | "ROLES" }> {
    const path = '/space-role-mode';
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ mode?: "PRE_ROLES" | "ROLES_TRANSITION" | "ROLES" }>;
}

export interface GetSpaceRoleAssignmentsParams {
    path: { id: string };
    roleId?: string;
    roleType?: string;
    principalId?: string;
    principalType?: Types.PrincipalType;
    cursor?: string;
    limit?: number;
}

/**
 * Get space role assignments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceRoleAssignments(client: BoundClient, params: GetSpaceRoleAssignmentsParams): Promise<{ results?: Array<Types.SpaceRoleAssignment>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/role-assignments`;
    const queryParams = {
      roleId: params.roleId,
      roleType: params.roleType,
      principalId: params.principalId,
      principalType: params.principalType,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.SpaceRoleAssignment>; _links?: Types.MultiEntityLinks }>;
}

export interface SetSpaceRoleAssignmentsParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Set space role assignments
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setSpaceRoleAssignments(client: BoundClient, params: SetSpaceRoleAssignmentsParams): Promise<{ results?: Array<Types.SpaceRoleAssignment>; _links?: Types.MultiEntityLinks }> {
    const path = `/spaces/${params.path.id}/role-assignments`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.SpaceRoleAssignment>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageFooterCommentsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    status?: Array<"current" | "archived" | "trashed" | "deleted" | "historical" | "draft">;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get footer comments for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageFooterComments(client: BoundClient, params: GetPageFooterCommentsParams): Promise<{ results?: Array<Types.PageCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/footer-comments`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      status: params.status,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.PageCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageInlineCommentsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    status?: Array<"current" | "archived" | "trashed" | "deleted" | "historical" | "draft">;
    resolutionStatus?: Array<"resolved" | "open" | "dangling" | "reopened">;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get inline comments for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageInlineComments(client: BoundClient, params: GetPageInlineCommentsParams): Promise<{ results?: Array<Types.PageInlineCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/inline-comments`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      status: params.status,
      resolutionStatus: params.resolutionStatus,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.PageInlineCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetBlogPostFooterCommentsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    status?: Array<"current" | "deleted" | "trashed" | "historical" | "draft">;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get footer comments for blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostFooterComments(client: BoundClient, params: GetBlogPostFooterCommentsParams): Promise<{ results?: Array<Types.BlogPostCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/${params.path.id}/footer-comments`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      status: params.status,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.BlogPostCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetBlogPostInlineCommentsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    status?: Array<"current" | "deleted" | "trashed" | "historical" | "draft">;
    resolutionStatus?: Array<"resolved" | "open" | "dangling" | "reopened">;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get inline comments for blog post
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostInlineComments(client: BoundClient, params: GetBlogPostInlineCommentsParams): Promise<{ results?: Array<Types.BlogPostInlineCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/blogposts/${params.path.id}/inline-comments`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      status: params.status,
      resolutionStatus: params.resolutionStatus,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.BlogPostInlineCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetFooterCommentsParams {
    bodyFormat?: Types.PrimaryBodyRepresentation;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get footer comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFooterComments(client: BoundClient, params: GetFooterCommentsParams): Promise<{ results?: Array<Types.FooterCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = '/footer-comments';
    const queryParams = {
      bodyFormat: params.bodyFormat,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.FooterCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateFooterCommentParams {
    body?: Types.CreateFooterCommentModel;
}

/**
 * Create footer comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createFooterComment(client: BoundClient, params: CreateFooterCommentParams): Promise<Types.FooterCommentModel & { _links?: { base?: string } }> {
    const path = '/footer-comments';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FooterCommentModel & { _links?: { base?: string } }>;
}

export interface GetFooterCommentByIdParams {
    path: { commentId: string };
    bodyFormat?: Types.PrimaryBodyRepresentationSingle;
    version?: number;
    includeProperties?: boolean;
    includeOperations?: boolean;
    includeLikes?: boolean;
    includeVersions?: boolean;
    includeVersion?: boolean;
}

/**
 * Get footer comment by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFooterCommentById(client: BoundClient, params: GetFooterCommentByIdParams): Promise<Types.FooterCommentModel & { _links?: { base?: string } }> {
    const path = `/footer-comments/{comment-id}`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      version: params.version,
      includeProperties: params.includeProperties,
      includeOperations: params.includeOperations,
      includeLikes: params.includeLikes,
      includeVersions: params.includeVersions,
      includeVersion: params.includeVersion,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FooterCommentModel & { _links?: { base?: string } }>;
}

export interface UpdateFooterCommentParams {
    path: { commentId: string };
    body?: Types.UpdateFooterCommentModel & { _links?: { base?: string } };
}

/**
 * Update footer comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateFooterComment(client: BoundClient, params: UpdateFooterCommentParams): Promise<Types.FooterCommentModel> {
    const path = `/footer-comments/{comment-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FooterCommentModel>;
}

export interface DeleteFooterCommentParams {
    path: { commentId: string };
}

/**
 * Delete footer comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFooterComment(client: BoundClient, params: DeleteFooterCommentParams): Promise<void> {
    const path = `/footer-comments/{comment-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFooterCommentChildrenParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get children footer comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFooterCommentChildren(client: BoundClient, params: GetFooterCommentChildrenParams): Promise<{ results?: Array<Types.ChildrenCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/footer-comments/${params.path.id}/children`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildrenCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetFooterLikeCountParams {
    path: { id: string };
}

/**
 * Get like count for footer comment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFooterLikeCount(client: BoundClient, params: GetFooterLikeCountParams): Promise<{ count?: number }> {
    const path = `/footer-comments/${params.path.id}/likes/count`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ count?: number }>;
}

export interface GetFooterLikeUsersParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
}

/**
 * Get account IDs of likes for footer comment
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getFooterLikeUsers(client: BoundClient, params: GetFooterLikeUsersParams): Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }> {
    const path = `/footer-comments/${params.path.id}/likes/users`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }>;
}

export interface GetFooterCommentOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for footer comment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFooterCommentOperations(client: BoundClient, params: GetFooterCommentOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/footer-comments/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetFooterCommentVersionsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
    sort?: Types.VersionSortOrder;
}

/**
 * Get footer comment versions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFooterCommentVersions(client: BoundClient, params: GetFooterCommentVersionsParams): Promise<{ results?: Array<Types.CommentVersion>; _links?: Types.MultiEntityLinks }> {
    const path = `/footer-comments/${params.path.id}/versions`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CommentVersion>; _links?: Types.MultiEntityLinks }>;
}

export interface GetFooterCommentVersionDetailsParams {
    path: { id: string; versionNumber: string };
}

/**
 * Get version details for footer comment version
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFooterCommentVersionDetails(client: BoundClient, params: GetFooterCommentVersionDetailsParams): Promise<Types.DetailedVersion> {
    const path = `/footer-comments/${params.path.id}/versions/{version-number}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DetailedVersion>;
}

export interface GetInlineCommentsParams {
    bodyFormat?: Types.PrimaryBodyRepresentation;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get inline comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInlineComments(client: BoundClient, params: GetInlineCommentsParams): Promise<{ results?: Array<Types.InlineCommentModel>; _links?: Types.MultiEntityLinks }> {
    const path = '/inline-comments';
    const queryParams = {
      bodyFormat: params.bodyFormat,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.InlineCommentModel>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateInlineCommentParams {
    body?: Types.CreateInlineCommentModel;
}

/**
 * Create inline comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createInlineComment(client: BoundClient, params: CreateInlineCommentParams): Promise<Types.InlineCommentModel & { _links?: { base?: string } }> {
    const path = '/inline-comments';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.InlineCommentModel & { _links?: { base?: string } }>;
}

export interface GetInlineCommentByIdParams {
    path: { commentId: string };
    bodyFormat?: Types.PrimaryBodyRepresentationSingle;
    version?: number;
    includeProperties?: boolean;
    includeOperations?: boolean;
    includeLikes?: boolean;
    includeVersions?: boolean;
    includeVersion?: boolean;
}

/**
 * Get inline comment by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInlineCommentById(client: BoundClient, params: GetInlineCommentByIdParams): Promise<Types.InlineCommentModel & { _links?: { base?: string } }> {
    const path = `/inline-comments/{comment-id}`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      version: params.version,
      includeProperties: params.includeProperties,
      includeOperations: params.includeOperations,
      includeLikes: params.includeLikes,
      includeVersions: params.includeVersions,
      includeVersion: params.includeVersion,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.InlineCommentModel & { _links?: { base?: string } }>;
}

export interface UpdateInlineCommentParams {
    path: { commentId: string };
    body?: Types.UpdateInlineCommentModel;
}

/**
 * Update inline comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateInlineComment(client: BoundClient, params: UpdateInlineCommentParams): Promise<Types.InlineCommentModel & { _links?: { base?: string } }> {
    const path = `/inline-comments/{comment-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.InlineCommentModel & { _links?: { base?: string } }>;
}

export interface DeleteInlineCommentParams {
    path: { commentId: string };
}

/**
 * Delete inline comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteInlineComment(client: BoundClient, params: DeleteInlineCommentParams): Promise<void> {
    const path = `/inline-comments/{comment-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetInlineCommentChildrenParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    sort?: Types.CommentSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get children inline comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInlineCommentChildren(client: BoundClient, params: GetInlineCommentChildrenParams): Promise<{ results?: Array<Types.InlineCommentChildrenModel>; _links?: Types.MultiEntityLinks }> {
    const path = `/inline-comments/${params.path.id}/children`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.InlineCommentChildrenModel>; _links?: Types.MultiEntityLinks }>;
}

export interface GetInlineLikeCountParams {
    path: { id: string };
}

/**
 * Get like count for inline comment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInlineLikeCount(client: BoundClient, params: GetInlineLikeCountParams): Promise<{ count?: number }> {
    const path = `/inline-comments/${params.path.id}/likes/count`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ count?: number }>;
}

export interface GetInlineLikeUsersParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
}

/**
 * Get account IDs of likes for inline comment
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getInlineLikeUsers(client: BoundClient, params: GetInlineLikeUsersParams): Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }> {
    const path = `/inline-comments/${params.path.id}/likes/users`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Like>; _links?: Types.MultiEntityLinks }>;
}

export interface GetInlineCommentOperationsParams {
    path: { id: string };
}

/**
 * Get permitted operations for inline comment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInlineCommentOperations(client: BoundClient, params: GetInlineCommentOperationsParams): Promise<Types.PermittedOperationsResponse> {
    const path = `/inline-comments/${params.path.id}/operations`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedOperationsResponse>;
}

export interface GetInlineCommentVersionsParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    cursor?: string;
    limit?: number;
    sort?: Types.VersionSortOrder;
}

/**
 * Get inline comment versions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInlineCommentVersions(client: BoundClient, params: GetInlineCommentVersionsParams): Promise<{ results?: Array<Types.CommentVersion>; _links?: Types.MultiEntityLinks }> {
    const path = `/inline-comments/${params.path.id}/versions`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.CommentVersion>; _links?: Types.MultiEntityLinks }>;
}

export interface GetInlineCommentVersionDetailsParams {
    path: { id: string; versionNumber: string };
}

/**
 * Get version details for inline comment version
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInlineCommentVersionDetails(client: BoundClient, params: GetInlineCommentVersionDetailsParams): Promise<Types.DetailedVersion> {
    const path = `/inline-comments/${params.path.id}/versions/{version-number}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DetailedVersion>;
}

export interface GetCommentContentPropertiesParams {
    path: { commentId: string };
    key?: string;
    sort?: Types.ContentPropertySortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get content properties for comment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCommentContentProperties(client: BoundClient, params: GetCommentContentPropertiesParams): Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }> {
    const path = `/comments/{comment-id}/properties`;
    const queryParams = {
      key: params.key,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ContentProperty>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateCommentPropertyParams {
    path: { commentId: string };
    body?: Types.ContentPropertyCreateRequest;
}

/**
 * Create content property for comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCommentProperty(client: BoundClient, params: CreateCommentPropertyParams): Promise<Types.ContentProperty> {
    const path = `/comments/{comment-id}/properties`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface GetCommentContentPropertiesByIdParams {
    path: { commentId: string; propertyId: string };
}

/**
 * Get content property for comment by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCommentContentPropertiesById(client: BoundClient, params: GetCommentContentPropertiesByIdParams): Promise<Types.ContentProperty> {
    const path = `/comments/{comment-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface UpdateCommentPropertyByIdParams {
    path: { commentId: string; propertyId: string };
    body?: Types.ContentPropertyUpdateRequest;
}

/**
 * Update content property for comment by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCommentPropertyById(client: BoundClient, params: UpdateCommentPropertyByIdParams): Promise<Types.ContentProperty> {
    const path = `/comments/{comment-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentProperty>;
}

export interface DeleteCommentPropertyByIdParams {
    path: { commentId: string; propertyId: string };
}

/**
 * Delete content property for comment by id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteCommentPropertyById(client: BoundClient, params: DeleteCommentPropertyByIdParams): Promise<void> {
    const path = `/comments/{comment-id}/properties/{property-id}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetTasksParams {
    bodyFormat?: Types.PrimaryBodyRepresentation;
    includeBlankTasks?: boolean;
    status?: "complete" | "incomplete";
    taskId?: Array<number>;
    spaceId?: Array<number>;
    pageId?: Array<number>;
    blogpostId?: Array<number>;
    createdBy?: Array<string>;
    assignedTo?: Array<string>;
    completedBy?: Array<string>;
    createdAtFrom?: number;
    createdAtTo?: number;
    dueAtFrom?: number;
    dueAtTo?: number;
    completedAtFrom?: number;
    completedAtTo?: number;
    cursor?: string;
    limit?: number;
}

/**
 * Get tasks
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTasks(client: BoundClient, params: GetTasksParams): Promise<{ results?: Array<Types.Task>; _links?: Types.MultiEntityLinks }> {
    const path = '/tasks';
    const queryParams = {
      bodyFormat: params.bodyFormat,
      includeBlankTasks: params.includeBlankTasks,
      status: params.status,
      taskId: params.taskId,
      spaceId: params.spaceId,
      pageId: params.pageId,
      blogpostId: params.blogpostId,
      createdBy: params.createdBy,
      assignedTo: params.assignedTo,
      completedBy: params.completedBy,
      createdAtFrom: params.createdAtFrom,
      createdAtTo: params.createdAtTo,
      dueAtFrom: params.dueAtFrom,
      dueAtTo: params.dueAtTo,
      completedAtFrom: params.completedAtFrom,
      completedAtTo: params.completedAtTo,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Task>; _links?: Types.MultiEntityLinks }>;
}

export interface GetTaskByIdParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
}

/**
 * Get task by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTaskById(client: BoundClient, params: GetTaskByIdParams): Promise<Types.Task> {
    const path = `/tasks/${params.path.id}`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Task>;
}

export interface UpdateTaskParams {
    path: { id: string };
    bodyFormat?: Types.PrimaryBodyRepresentation;
    body?: Record<string, unknown>;
}

/**
 * Update task
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateTask(client: BoundClient, params: UpdateTaskParams): Promise<Types.Task> {
    const path = `/tasks/${params.path.id}`;
    const queryParams = {
      bodyFormat: params.bodyFormat,
    };
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Task>;
}

export interface GetChildPagesParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: string;
}

/**
 * Get child pages
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getChildPages(client: BoundClient, params: GetChildPagesParams): Promise<{ results?: Array<Types.ChildPage>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/children`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildPage>; _links?: Types.MultiEntityLinks }>;
}

export interface GetChildCustomContentParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: string;
}

/**
 * Get child custom content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getChildCustomContent(client: BoundClient, params: GetChildCustomContentParams): Promise<{ results?: Array<Types.ChildCustomContent>; _links?: Types.MultiEntityLinks }> {
    const path = `/custom-content/${params.path.id}/children`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildCustomContent>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageDirectChildrenParams {
    path: { id: string };
    cursor?: string;
    limit?: number;
    sort?: string;
}

/**
 * Get direct children of a page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageDirectChildren(client: BoundClient, params: GetPageDirectChildrenParams): Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/direct-children`;
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
      sort: params.sort,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.ChildrenResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageAncestorsParams {
    path: { id: string };
    limit?: number;
}

/**
 * Get all ancestors of page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageAncestors(client: BoundClient, params: GetPageAncestorsParams): Promise<{ results?: Array<Types.Ancestor>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/ancestors`;
    const queryParams = {
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.Ancestor>; _links?: Types.MultiEntityLinks }>;
}

export interface GetPageDescendantsParams {
    path: { id: string };
    limit?: number;
    depth?: number;
    cursor?: string;
}

/**
 * Get descendants of page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageDescendants(client: BoundClient, params: GetPageDescendantsParams): Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }> {
    const path = `/pages/${params.path.id}/descendants`;
    const queryParams = {
      limit: params.limit,
      depth: params.depth,
      cursor: params.cursor,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.DescendantsResponse>; _links?: Types.MultiEntityLinks }>;
}

export interface CreateBulkUserLookupParams {
    body?: Record<string, unknown>;
}

/**
 * Create bulk user lookup using ids
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function createBulkUserLookup(client: BoundClient, params: CreateBulkUserLookupParams): Promise<{ results?: Array<Types.User>; _links?: Types.MultiEntityLinks }> {
    const path = '/users-bulk';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.User>; _links?: Types.MultiEntityLinks }>;
}

export interface CheckAccessByEmailParams {
    body?: Record<string, unknown>;
}

/**
 * Check site access for a list of emails
 * @forge-scopes-asApp read:jira-work
 */
export async function checkAccessByEmail(client: BoundClient, params: CheckAccessByEmailParams): Promise<{ emailsWithoutAccess?: Array<string>; invalidEmails?: Array<string> }> {
    const path = '/user/access/check-access-by-email';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ emailsWithoutAccess?: Array<string>; invalidEmails?: Array<string> }>;
}

export interface InviteByEmailParams {
    body?: Record<string, unknown>;
}

/**
 * Invite a list of emails to the site
 * @forge-scopes-asApp read:jira-work
 */
export async function inviteByEmail(client: BoundClient, params: InviteByEmailParams): Promise<void> {
    const path = '/user/access/invite-by-email';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get data policy metadata for the workspace
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDataPolicyMetadata(client: BoundClient): Promise<Types.DataPolicyMetadata> {
    const path = '/data-policies/metadata';
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DataPolicyMetadata>;
}

export interface GetDataPolicySpacesParams {
    ids?: Array<number>;
    keys?: Array<string>;
    sort?: Types.SpaceSortOrder;
    cursor?: string;
    limit?: number;
}

/**
 * Get spaces with data policies
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDataPolicySpaces(client: BoundClient, params: GetDataPolicySpacesParams): Promise<{ results?: Array<Types.DataPolicySpace>; _links?: Types.MultiEntityLinks }> {
    const path = '/data-policies/spaces';
    const queryParams = {
      ids: params.ids,
      keys: params.keys,
      sort: params.sort,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<Types.DataPolicySpace>; _links?: Types.MultiEntityLinks }>;
}

/**
 * Get list of classification levels
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getClassificationLevels(client: BoundClient): Promise<Array<Types.ClassificationLevel>> {
    const path = '/classification-levels';
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ClassificationLevel>>;
}

export interface GetSpaceDefaultClassificationLevelParams {
    path: { id: string };
}

/**
 * Get space default classification level
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceDefaultClassificationLevel(client: BoundClient, params: GetSpaceDefaultClassificationLevelParams): Promise<Types.ClassificationLevel> {
    const path = `/spaces/${params.path.id}/classification-level/default`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ClassificationLevel>;
}

export interface PutSpaceDefaultClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update space default classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function putSpaceDefaultClassificationLevel(client: BoundClient, params: PutSpaceDefaultClassificationLevelParams): Promise<void> {
    const path = `/spaces/${params.path.id}/classification-level/default`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface DeleteSpaceDefaultClassificationLevelParams {
    path: { id: string };
}

/**
 * Delete space default classification level
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSpaceDefaultClassificationLevel(client: BoundClient, params: DeleteSpaceDefaultClassificationLevelParams): Promise<void> {
    const path = `/spaces/${params.path.id}/classification-level/default`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPageClassificationLevelParams {
    path: { id: string };
    status?: "current" | "draft" | "archived";
}

/**
 * Get page classification level
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPageClassificationLevel(client: BoundClient, params: GetPageClassificationLevelParams): Promise<Types.ClassificationLevel> {
    const path = `/pages/${params.path.id}/classification-level`;
    const queryParams = {
      status: params.status,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ClassificationLevel>;
}

export interface PutPageClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update page classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function putPageClassificationLevel(client: BoundClient, params: PutPageClassificationLevelParams): Promise<void> {
    const path = `/pages/${params.path.id}/classification-level`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface PostPageClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Reset page classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function postPageClassificationLevel(client: BoundClient, params: PostPageClassificationLevelParams): Promise<void> {
    const path = `/pages/${params.path.id}/classification-level/reset`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBlogPostClassificationLevelParams {
    path: { id: string };
    status?: "current" | "draft" | "archived";
}

/**
 * Get blog post classification level
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlogPostClassificationLevel(client: BoundClient, params: GetBlogPostClassificationLevelParams): Promise<Types.ClassificationLevel> {
    const path = `/blogposts/${params.path.id}/classification-level`;
    const queryParams = {
      status: params.status,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ClassificationLevel>;
}

export interface PutBlogPostClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update blog post classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function putBlogPostClassificationLevel(client: BoundClient, params: PutBlogPostClassificationLevelParams): Promise<void> {
    const path = `/blogposts/${params.path.id}/classification-level`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface PostBlogPostClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Reset blog post classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function postBlogPostClassificationLevel(client: BoundClient, params: PostBlogPostClassificationLevelParams): Promise<void> {
    const path = `/blogposts/${params.path.id}/classification-level/reset`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWhiteboardClassificationLevelParams {
    path: { id: string };
}

/**
 * Get whiteboard classification level
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWhiteboardClassificationLevel(client: BoundClient, params: GetWhiteboardClassificationLevelParams): Promise<Types.ClassificationLevel> {
    const path = `/whiteboards/${params.path.id}/classification-level`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ClassificationLevel>;
}

export interface PutWhiteboardClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update whiteboard classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function putWhiteboardClassificationLevel(client: BoundClient, params: PutWhiteboardClassificationLevelParams): Promise<void> {
    const path = `/whiteboards/${params.path.id}/classification-level`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface PostWhiteboardClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Reset whiteboard classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function postWhiteboardClassificationLevel(client: BoundClient, params: PostWhiteboardClassificationLevelParams): Promise<void> {
    const path = `/whiteboards/${params.path.id}/classification-level/reset`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDatabaseClassificationLevelParams {
    path: { id: string };
}

/**
 * Get database classification level
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDatabaseClassificationLevel(client: BoundClient, params: GetDatabaseClassificationLevelParams): Promise<Types.ClassificationLevel> {
    const path = `/databases/${params.path.id}/classification-level`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ClassificationLevel>;
}

export interface PutDatabaseClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Update database classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function putDatabaseClassificationLevel(client: BoundClient, params: PutDatabaseClassificationLevelParams): Promise<void> {
    const path = `/databases/${params.path.id}/classification-level`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface PostDatabaseClassificationLevelParams {
    path: { id: string };
    body?: Record<string, unknown>;
}

/**
 * Reset database classification level
 * @forge-scopes-asApp read:jira-work
 */
export async function postDatabaseClassificationLevel(client: BoundClient, params: PostDatabaseClassificationLevelParams): Promise<void> {
    const path = `/databases/${params.path.id}/classification-level/reset`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAttachmentThumbnailByIdParams {
    path: { id: string };
    version?: number;
    height?: number;
    width?: number;
}

/**
 * Download attachment thumbnail by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentThumbnailById(client: BoundClient, params: GetAttachmentThumbnailByIdParams): Promise<void> {
    const path = `/attachments/${params.path.id}/thumbnail/download`;
    const queryParams = {
      version: params.version,
      height: params.height,
      width: params.width,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetForgeAppPropertiesParams {
    cursor?: string;
    limit?: number;
}

/**
 * Get Forge app properties.
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getForgeAppProperties(client: BoundClient, params: GetForgeAppPropertiesParams): Promise<{ results?: Array<{ key?: string; value?: Record<string, unknown> }>; _links?: Types.MultiEntityLinks }> {
    const path = '/app/properties';
    const queryParams = {
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ results?: Array<{ key?: string; value?: Record<string, unknown> }>; _links?: Types.MultiEntityLinks }>;
}

export interface GetForgeAppPropertyParams {
    path: { propertyKey: string };
}

/**
 * Get a Forge app property by key.
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getForgeAppProperty(client: BoundClient, params: GetForgeAppPropertyParams): Promise<{ key?: string; value?: Record<string, unknown> }> {
    const path = `/app/properties/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ key?: string; value?: Record<string, unknown> }>;
}

export interface PutForgeAppPropertyParams {
    path: { propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Create or update a Forge app property.
 * @forge-scopes-asApp read:jira-work
 */
export async function putForgeAppProperty(client: BoundClient, params: PutForgeAppPropertyParams): Promise<void> {
    const path = `/app/properties/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface DeleteForgeAppPropertyParams {
    path: { propertyKey: string };
}

/**
 * Deletes a Forge app property.
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteForgeAppProperty(client: BoundClient, params: DeleteForgeAppPropertyParams): Promise<void> {
    const path = `/app/properties/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}
