/**
 * Confluence Cloud REST API v1 client (deprecated)
 *
 * v1 is maintained for backwards compatibility.
 * New integrations should use v2 (the default export from @forge-clients/confluence).
 *
 * This file and all *.gen.ts files in this directory are generated.
 * Run `pnpm generate` to regenerate from the latest cleaned spec.
 */

// Placeholder export so the package compiles before generation
export const CONFLUENCE_V1_PLACEHOLDER = 'Confluence v1 client — run pnpm generate to populate';
