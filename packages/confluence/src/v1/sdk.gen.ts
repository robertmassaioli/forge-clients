// @ts-nocheck
import { ForgeApiError } from "@forge-clients/core";
import type { ForgeAdapter, AuthContext } from "@forge-clients/core";
import type * as Types from "./types.gen.js";

// Generated by @forge-clients/generator — DO NOT EDIT
// Source: Confluence Cloud REST API v1
/* eslint-disable */
export type * from './types.gen.js';

export interface GetAuditRecordsParams {
    startDate?: string;
    endDate?: string;
    searchString?: string;
    start?: number;
    limit?: number;
}

/**
 * Get audit records
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAuditRecords(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAuditRecordsParams): Promise<Types.AuditRecordArray> {
    const path = '/wiki/rest/api/audit';
    const queryParams = {
      startDate: params.startDate,
      endDate: params.endDate,
      searchString: params.searchString,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AuditRecordArray>;
}

export interface CreateAuditRecordParams {
    body?: Types.AuditRecordCreate;
}

/**
 * Create audit record
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createAuditRecord(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateAuditRecordParams): Promise<Types.AuditRecord> {
    const path = '/wiki/rest/api/audit';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AuditRecord>;
}

export interface ExportAuditRecordsParams {
    startDate?: string;
    endDate?: string;
    searchString?: string;
    format?: "csv" | "zip";
}

/**
 * Export audit records
 * @forge-scopes-asApp read:jira-work
 */
export async function exportAuditRecords(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ExportAuditRecordsParams): Promise<void> {
    const path = '/wiki/rest/api/audit/export';
    const queryParams = {
      startDate: params.startDate,
      endDate: params.endDate,
      searchString: params.searchString,
      format: params.format,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get retention period
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRetentionPeriod(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.RetentionPeriod> {
    const path = '/wiki/rest/api/audit/retention';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RetentionPeriod>;
}

export interface SetRetentionPeriodParams {
    body?: Types.RetentionPeriod;
}

/**
 * Set retention period
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setRetentionPeriod(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetRetentionPeriodParams): Promise<Types.RetentionPeriod> {
    const path = '/wiki/rest/api/audit/retention';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RetentionPeriod>;
}

export interface GetAuditRecordsForTimePeriodParams {
    number?: number;
    units?: "NANOS" | "MICROS" | "MILLIS" | "SECONDS" | "MINUTES" | "HOURS" | "HALF_DAYS" | "DAYS" | "WEEKS" | "MONTHS" | "YEARS" | "DECADES" | "CENTURIES";
    searchString?: string;
    start?: number;
    limit?: number;
}

/**
 * Get audit records for time period
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAuditRecordsForTimePeriod(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAuditRecordsForTimePeriodParams): Promise<Types.AuditRecordArray> {
    const path = '/wiki/rest/api/audit/since';
    const queryParams = {
      number: params.number,
      units: params.units,
      searchString: params.searchString,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AuditRecordArray>;
}

export interface ArchivePagesParams {
    body?: { pages?: Array<{ id: number }> };
}

/**
 * Archive pages
 * @forge-scopes-asApp read:jira-work
 */
export async function archivePages(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ArchivePagesParams): Promise<Types.LongTask> {
    const path = '/wiki/rest/api/content/archive';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LongTask>;
}

export interface PublishLegacyDraftParams {
    path: { draftId: string };
    status?: string;
    body?: Types.ContentBlueprintDraft;
}

/**
 * Publish legacy draft
 * @forge-scopes-asApp read:jira-work
 */
export async function publishLegacyDraft(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PublishLegacyDraftParams): Promise<Types.Content> {
    const path = `/wiki/rest/api/content/blueprint/instance/${params.path.draftId}`;
    const queryParams = {
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Content>;
}

export interface PublishSharedDraftParams {
    path: { draftId: string };
    status?: string;
    body?: Types.ContentBlueprintDraft;
}

/**
 * Publish shared draft
 * @forge-scopes-asApp read:jira-work
 */
export async function publishSharedDraft(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PublishSharedDraftParams): Promise<Types.Content> {
    const path = `/wiki/rest/api/content/blueprint/instance/${params.path.draftId}`;
    const queryParams = {
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Content>;
}

export interface SearchContentByCQLParams {
    cql: string;
    cqlcontext?: string;
    cursor?: string;
    limit?: number;
}

/**
 * Search content by CQL
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchContentByCQL(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchContentByCQLParams): Promise<Types.ContentArray> {
    const path = '/wiki/rest/api/content/search';
    const queryParams = {
      cql: params.cql,
      cqlcontext: params.cqlcontext,
      cursor: params.cursor,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentArray>;
}

export interface DeletePageTreeParams {
    path: { id: string };
}

/**
 * Delete page tree
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePageTree(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeletePageTreeParams): Promise<Types.LongTask> {
    const path = `/wiki/rest/api/content/${params.path.id}/pageTree`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LongTask>;
}

export interface MovePageParams {
    path: { pageId: string; position: string; targetId: string };
}

/**
 * Move a page to a new location relative to a target page
 * @forge-scopes-asApp read:jira-work
 */
export async function movePage(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MovePageParams): Promise<{ pageId?: Types.ContentId }> {
    const path = `/wiki/rest/api/content/${params.path.pageId}/move/${params.path.position}/${params.path.targetId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ pageId?: Types.ContentId }>;
}

export interface CreateAttachmentParams {
    path: { id: string };
    status?: "current" | "draft";
    body?: Record<string, unknown>;
}

/**
 * Create attachment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createAttachment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateAttachmentParams): Promise<Types.ContentArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/child/attachment`;
    const queryParams = {
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentArray>;
}

export interface CreateOrUpdateAttachmentsParams {
    path: { id: string };
    status?: "current" | "draft";
    body?: Record<string, unknown>;
}

/**
 * Create or update attachment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createOrUpdateAttachments(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateOrUpdateAttachmentsParams): Promise<Types.ContentArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/child/attachment`;
    const queryParams = {
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentArray>;
}

export interface UpdateAttachmentPropertiesParams {
    path: { id: string; attachmentId: string };
    body?: Types.AttachmentPropertiesUpdateBody;
}

/**
 * Update attachment properties
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateAttachmentProperties(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateAttachmentPropertiesParams): Promise<Types.Content> {
    const path = `/wiki/rest/api/content/${params.path.id}/child/attachment/${params.path.attachmentId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Content>;
}

export interface UpdateAttachmentDataParams {
    path: { id: string; attachmentId: string };
    body?: Record<string, unknown>;
}

/**
 * Update attachment data
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateAttachmentData(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateAttachmentDataParams): Promise<Types.Content> {
    const path = `/wiki/rest/api/content/${params.path.id}/child/attachment/${params.path.attachmentId}/data`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Content>;
}

export interface DownloadAttatchmentParams {
    path: { id: string; attachmentId: string };
    version?: number;
    status?: Array<string>;
}

/**
 * Get URI to download attachment
 * @forge-scopes-asApp read:jira-work
 */
export async function downloadAttatchment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DownloadAttatchmentParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/child/attachment/${params.path.attachmentId}/download`;
    const queryParams = {
      version: params.version,
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetContentDescendantsParams {
    path: { id: string };
    expand?: Array<"attachment" | "comment" | "page">;
}

/**
 * Get content descendants
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getContentDescendants(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentDescendantsParams): Promise<Types.ContentChildren> {
    const path = `/wiki/rest/api/content/${params.path.id}/descendant`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentChildren>;
}

export interface GetDescendantsOfTypeParams {
    path: { id: string; type: string };
    depth?: "all" | "root" | "<any positive integer argument in the range of 1 and 100>";
    start?: number;
    limit?: number;
}

/**
 * Get content descendants by type
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getDescendantsOfType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDescendantsOfTypeParams): Promise<Types.ContentArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/descendant/${params.path.type}`;
    const queryParams = {
      depth: params.depth,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentArray>;
}

export interface GetMacroBodyByMacroIdParams {
    path: { id: string; version: string; macroId: string };
}

/**
 * Get macro body by macro ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getMacroBodyByMacroId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetMacroBodyByMacroIdParams): Promise<Types.MacroInstance> {
    const path = `/wiki/rest/api/content/${params.path.id}/history/${params.path.version}/macro/id/${params.path.macroId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.MacroInstance>;
}

export interface GetAndConvertMacroBodyByMacroIdParams {
    path: { id: string; version: string; macroId: string; to: string };
    spaceKeyContext?: string;
    embeddedContentRender?: "current" | "version-at-save";
}

/**
 * Get macro body by macro ID and convert the representation synchronously
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAndConvertMacroBodyByMacroId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAndConvertMacroBodyByMacroIdParams): Promise<Types.ContentBody> {
    const path = `/wiki/rest/api/content/${params.path.id}/history/${params.path.version}/macro/id/${params.path.macroId}/convert/${params.path.to}`;
    const queryParams = {
      spaceKeyContext: params.spaceKeyContext,
      embeddedContentRender: params.embeddedContentRender,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentBody>;
}

export interface GetAndAsyncConvertMacroBodyByMacroIdParams {
    path: { id: string; version: string; macroId: string; to: string };
    allowCache?: boolean;
    spaceKeyContext?: string;
    embeddedContentRender?: "current" | "version-at-save";
}

/**
 * Get macro body by macro ID and convert representation Asynchronously
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAndAsyncConvertMacroBodyByMacroId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAndAsyncConvertMacroBodyByMacroIdParams): Promise<Types.AsyncId> {
    const path = `/wiki/rest/api/content/${params.path.id}/history/${params.path.version}/macro/id/${params.path.macroId}/convert/async/${params.path.to}`;
    const queryParams = {
      allowCache: params.allowCache,
      spaceKeyContext: params.spaceKeyContext,
      embeddedContentRender: params.embeddedContentRender,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AsyncId>;
}

export interface AddLabelsToContentParams {
    path: { id: string };
    body?: Types.LabelCreateArray | Types.LabelCreate;
}

/**
 * Add labels to content
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addLabelsToContent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddLabelsToContentParams): Promise<Types.LabelArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/label`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LabelArray>;
}

export interface RemoveLabelFromContentUsingQueryParameterParams {
    path: { id: string };
    name: string;
}

/**
 * Remove label from content using query parameter
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeLabelFromContentUsingQueryParameter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveLabelFromContentUsingQueryParameterParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/label`;
    const queryParams = {
      name: params.name,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveLabelFromContentParams {
    path: { id: string; label: string };
}

/**
 * Remove label from content
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeLabelFromContent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveLabelFromContentParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/label/${params.path.label}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWatchesForPageParams {
    path: { id: string };
    start?: number;
    limit?: number;
}

/**
 * Get watches for page
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWatchesForPage(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWatchesForPageParams): Promise<Types.WatchArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/notification/child-created`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WatchArray>;
}

export interface GetWatchesForSpaceParams {
    path: { id: string };
    start?: number;
    limit?: number;
}

/**
 * Get watches for space
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWatchesForSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWatchesForSpaceParams): Promise<Types.SpaceWatchArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/notification/created`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceWatchArray>;
}

export interface CopyPageHierarchyParams {
    path: { id: string };
    body?: Types.CopyPageHierarchyRequest;
}

/**
 * Copy page hierarchy
 * @forge-scopes-asApp read:jira-work
 */
export async function copyPageHierarchy(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CopyPageHierarchyParams): Promise<Types.LongTask> {
    const path = `/wiki/rest/api/content/${params.path.id}/pagehierarchy/copy`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LongTask>;
}

export interface CopyPageParams {
    path: { id: string };
    body?: Types.CopyPageRequest;
}

/**
 * Copy single page
 * @forge-scopes-asApp read:jira-work
 */
export async function copyPage(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CopyPageParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/copy`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface CheckContentPermissionParams {
    path: { id: string };
    body?: Types.ContentPermissionRequest;
}

/**
 * Check content permissions
 * @forge-scopes-asApp read:jira-work
 */
export async function checkContentPermission(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CheckContentPermissionParams): Promise<Types.PermissionCheckResponse> {
    const path = `/wiki/rest/api/content/${params.path.id}/permission/check`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionCheckResponse>;
}

export interface GetRestrictionsParams {
    path: { id: string };
    expand?: Array<"restrictions.user" | "read.restrictions.user" | "update.restrictions.user" | "restrictions.group" | "read.restrictions.group" | "update.restrictions.group" | "content">;
    start?: number;
    limit?: number;
}

/**
 * Get restrictions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRestrictions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRestrictionsParams): Promise<Types.ContentRestrictionArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction`;
    const queryParams = {
      expand: params.expand,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentRestrictionArray>;
}

export interface AddRestrictionsParams {
    path: { id: string };
    expand?: Array<"restrictions.user" | "read.restrictions.user" | "update.restrictions.user" | "restrictions.group" | "read.restrictions.group" | "update.restrictions.group" | "content">;
    body?: Types.ContentRestrictionAddOrUpdateArray;
}

/**
 * Add restrictions
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addRestrictions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddRestrictionsParams): Promise<Types.ContentRestrictionArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentRestrictionArray>;
}

export interface UpdateRestrictionsParams {
    path: { id: string };
    expand?: Array<"restrictions.user" | "read.restrictions.user" | "update.restrictions.user" | "restrictions.group" | "read.restrictions.group" | "update.restrictions.group" | "content">;
    body?: Types.ContentRestrictionAddOrUpdateArray;
}

/**
 * Update restrictions
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateRestrictions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateRestrictionsParams): Promise<Types.ContentRestrictionArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentRestrictionArray>;
}

export interface DeleteRestrictionsParams {
    path: { id: string };
    expand?: Array<"restrictions.user" | "read.restrictions.user" | "update.restrictions.user" | "restrictions.group" | "read.restrictions.group" | "update.restrictions.group" | "content">;
}

/**
 * Delete restrictions
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRestrictions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRestrictionsParams): Promise<Types.ContentRestrictionArray> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentRestrictionArray>;
}

export interface GetRestrictionsByOperationParams {
    path: { id: string };
    expand?: Array<"restrictions.user" | "restrictions.group" | "content">;
}

/**
 * Get restrictions by operation
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRestrictionsByOperation(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRestrictionsByOperationParams): Promise<Record<string, { operationType?: Types.ContentRestriction; _links?: Types.GenericLinks }>> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, { operationType?: Types.ContentRestriction; _links?: Types.GenericLinks }>>;
}

export interface GetRestrictionsForOperationParams {
    path: { id: string; operationKey: string };
    expand?: Array<"restrictions.user" | "restrictions.group" | "content">;
    start?: number;
    limit?: number;
}

/**
 * Get restrictions for operation
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRestrictionsForOperation(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRestrictionsForOperationParams): Promise<Types.ContentRestriction> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation/${params.path.operationKey}`;
    const queryParams = {
      expand: params.expand,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentRestriction>;
}

export interface GetIndividualGroupRestrictionStatusByGroupIdParams {
    path: { id: string; operationKey: string; groupId: string };
}

/**
 * Get content restriction status for group
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIndividualGroupRestrictionStatusByGroupId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIndividualGroupRestrictionStatusByGroupIdParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation/${params.path.operationKey}/byGroupId/${params.path.groupId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface AddGroupToContentRestrictionByGroupIdParams {
    path: { id: string; operationKey: string; groupId: string };
}

/**
 * Add group to content restriction
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addGroupToContentRestrictionByGroupId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddGroupToContentRestrictionByGroupIdParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation/${params.path.operationKey}/byGroupId/${params.path.groupId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveGroupFromContentRestrictionParams {
    path: { id: string; operationKey: string; groupId: string };
}

/**
 * Remove group from content restriction
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeGroupFromContentRestriction(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveGroupFromContentRestrictionParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation/${params.path.operationKey}/byGroupId/${params.path.groupId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetContentRestrictionStatusForUserParams {
    path: { id: string; operationKey: string };
}

/**
 * Get content restriction status for user
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getContentRestrictionStatusForUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentRestrictionStatusForUserParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation/${params.path.operationKey}/user`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface AddUserToContentRestrictionParams {
    path: { id: string; operationKey: string };
}

/**
 * Add user to content restriction
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function addUserToContentRestriction(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddUserToContentRestrictionParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation/${params.path.operationKey}/user`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveUserFromContentRestrictionParams {
    path: { id: string; operationKey: string };
}

/**
 * Remove user from content restriction
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function removeUserFromContentRestriction(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveUserFromContentRestrictionParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/restriction/byOperation/${params.path.operationKey}/user`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetContentStateParams {
    path: { id: string };
    status?: "current" | "draft" | "archived";
}

/**
 * Get content state
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getContentState(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentStateParams): Promise<Types.ContentStateResponse> {
    const path = `/wiki/rest/api/content/${params.path.id}/state`;
    const queryParams = {
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentStateResponse>;
}

export interface SetContentStateParams {
    path: { id: string };
    status: "current" | "draft";
    body?: Types.ContentStateRestInput;
}

/**
 * Set the content state of a content and publishes a new version of the content.
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setContentState(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetContentStateParams): Promise<Types.ContentStateResponse> {
    const path = `/wiki/rest/api/content/${params.path.id}/state`;
    const queryParams = {
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentStateResponse>;
}

export interface RemoveContentStateParams {
    path: { id: string };
    status?: "current" | "draft";
}

/**
 * Removes the content state of a content and publishes a new version.
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeContentState(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveContentStateParams): Promise<Types.ContentStateResponse> {
    const path = `/wiki/rest/api/content/${params.path.id}/state`;
    const queryParams = {
      status: params.status,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentStateResponse>;
}

export interface GetAvailableContentStatesParams {
    path: { id: string };
}

/**
 * Gets available content states for content.
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvailableContentStates(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAvailableContentStatesParams): Promise<Types.AvailableContentStates> {
    const path = `/wiki/rest/api/content/${params.path.id}/state/available`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AvailableContentStates>;
}

export interface RestoreContentVersionParams {
    path: { id: string };
    expand?: Array<string>;
    body?: Types.VersionRestore;
}

/**
 * Restore content version
 * @forge-scopes-asApp read:jira-work
 */
export async function restoreContentVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RestoreContentVersionParams): Promise<Types.Version> {
    const path = `/wiki/rest/api/content/${params.path.id}/version`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Version>;
}

export interface DeleteContentVersionParams {
    path: { id: string; versionNumber: string };
}

/**
 * Delete content version
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteContentVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteContentVersionParams): Promise<void> {
    const path = `/wiki/rest/api/content/${params.path.id}/version/${params.path.versionNumber}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get Custom Content States
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomContentStates(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ContentState>> {
    const path = '/wiki/rest/api/content-states';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ContentState>>;
}

export interface AsyncConvertContentBodyRequestParams {
    path: { to: string };
    spaceKeyContext?: string;
    contentIdContext?: string;
    allowCache?: boolean;
    embeddedContentRender?: "current" | "version-at-save";
    body?: Types.ContentBodyCreate;
}

/**
 * Asynchronously convert content body
 * @forge-scopes-asApp read:jira-work
 */
export async function asyncConvertContentBodyRequest(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AsyncConvertContentBodyRequestParams): Promise<Types.AsyncId> {
    const path = `/wiki/rest/api/contentbody/convert/async/${params.path.to}`;
    const queryParams = {
      spaceKeyContext: params.spaceKeyContext,
      contentIdContext: params.contentIdContext,
      allowCache: params.allowCache,
      embeddedContentRender: params.embeddedContentRender,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AsyncId>;
}

export interface AsyncConvertContentBodyResponseParams {
    path: { id: string };
}

/**
 * Get asynchronously converted content body from the id or the current status of the task.
 * @forge-scopes-asApp read:jira-work
 */
export async function asyncConvertContentBodyResponse(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AsyncConvertContentBodyResponseParams): Promise<Types.AsyncContentBody> {
    const path = `/wiki/rest/api/contentbody/convert/async/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AsyncContentBody>;
}

export interface BulkAsyncConvertContentBodyResponseParams {
    ids: Array<string>;
}

/**
 * Get asynchronous content body conversion task result in bulk
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkAsyncConvertContentBodyResponse(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkAsyncConvertContentBodyResponseParams): Promise<Types.AsyncContentBodyArray> {
    const path = '/wiki/rest/api/contentbody/convert/async/bulk/tasks';
    const queryParams = {
      ids: params.ids,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AsyncContentBodyArray>;
}

export interface BulkAsyncConvertContentBodyRequestParams {
    body?: Types.BulkContentBodyConversionInput;
}

/**
 * Create asynchronous content body conversion tasks in bulk
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkAsyncConvertContentBodyRequest(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkAsyncConvertContentBodyRequestParams): Promise<Types.AsyncIdArray> {
    const path = '/wiki/rest/api/contentbody/convert/async/bulk/tasks';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AsyncIdArray>;
}

export interface GetAllLabelContentParams {
    name: string;
    type?: "page" | "blogpost" | "attachment" | "page_template";
    start?: number;
    limit?: number;
}

/**
 * Get label information
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllLabelContent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllLabelContentParams): Promise<Types.LabelDetails> {
    const path = '/wiki/rest/api/label';
    const queryParams = {
      name: params.name,
      type: params.type,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LabelDetails>;
}

export interface GetGroupsParams {
    start?: number;
    limit?: number;
    accessType?: "user" | "admin" | "site-admin";
}

/**
 * Get groups
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getGroups(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetGroupsParams): Promise<Types.GroupArrayWithLinks> {
    const path = '/wiki/rest/api/group';
    const queryParams = {
      start: params.start,
      limit: params.limit,
      accessType: params.accessType,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GroupArrayWithLinks>;
}

export interface CreateGroupParams {
    body?: Types.GroupName;
}

/**
 * Create new user group
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createGroup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateGroupParams): Promise<Types.Group> {
    const path = '/wiki/rest/api/group';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Group>;
}

export interface GetGroupByGroupIdParams {
    id: string;
}

/**
 * Get group
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getGroupByGroupId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetGroupByGroupIdParams): Promise<Types.Group> {
    const path = '/wiki/rest/api/group/by-id';
    const queryParams = {
      id: params.id,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Group>;
}

export interface RemoveGroupByIdParams {
    id: string;
}

/**
 * Delete user group
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeGroupById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveGroupByIdParams): Promise<void> {
    const path = '/wiki/rest/api/group/by-id';
    const queryParams = {
      id: params.id,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SearchGroupsParams {
    query: string;
    start?: number;
    limit?: number;
    shouldReturnTotalSize?: boolean;
}

/**
 * Search groups by partial query
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchGroups(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchGroupsParams): Promise<Types.GroupArrayWithLinks> {
    const path = '/wiki/rest/api/group/picker';
    const queryParams = {
      query: params.query,
      start: params.start,
      limit: params.limit,
      shouldReturnTotalSize: params.shouldReturnTotalSize,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GroupArrayWithLinks>;
}

export interface GetGroupMembersByGroupIdParams {
    path: { groupId: string };
    start?: number;
    limit?: number;
    shouldReturnTotalSize?: boolean;
    expand?: Array<"operations" | "personalSpace" | "isExternalCollaborator">;
}

/**
 * Get group members
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getGroupMembersByGroupId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetGroupMembersByGroupIdParams): Promise<Types.UserArray> {
    const path = `/wiki/rest/api/group/${params.path.groupId}/membersByGroupId`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
      shouldReturnTotalSize: params.shouldReturnTotalSize,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserArray>;
}

export interface AddUserToGroupByGroupIdParams {
    groupId: string;
    body?: Types.AccountId;
}

/**
 * Add member to group by groupId
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function addUserToGroupByGroupId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddUserToGroupByGroupIdParams): Promise<void> {
    const path = '/wiki/rest/api/group/userByGroupId';
    const queryParams = {
      groupId: params.groupId,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveMemberFromGroupByGroupIdParams {
    groupId: string;
    accountId: string;
}

/**
 * Remove member from group using group id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeMemberFromGroupByGroupId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveMemberFromGroupByGroupIdParams): Promise<void> {
    const path = '/wiki/rest/api/group/userByGroupId';
    const queryParams = {
      groupId: params.groupId,
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetTasksParams {
    key?: string;
    start?: number;
    limit?: number;
}

/**
 * Get long-running tasks
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTasks(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetTasksParams): Promise<Types.LongTaskStatusArray> {
    const path = '/wiki/rest/api/longtask';
    const queryParams = {
      key: params.key,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LongTaskStatusArray>;
}

export interface GetTaskParams {
    path: { id: string };
}

/**
 * Get long-running task
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTask(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetTaskParams): Promise<Types.LongTaskStatusWithLinks> {
    const path = `/wiki/rest/api/longtask/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LongTaskStatusWithLinks>;
}

export interface FindTargetFromSourceParams {
    path: { relationName: string; sourceType: string; sourceKey: string; targetType: string };
    sourceStatus?: string;
    targetStatus?: string;
    sourceVersion?: number;
    targetVersion?: number;
    expand?: Array<"relationData" | "source" | "target">;
    start?: number;
    limit?: number;
}

/**
 * Find target entities related to a source entity
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function findTargetFromSource(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindTargetFromSourceParams): Promise<Types.RelationArray> {
    const path = `/wiki/rest/api/relation/${params.path.relationName}/from/${params.path.sourceType}/${params.path.sourceKey}/to/${params.path.targetType}`;
    const queryParams = {
      sourceStatus: params.sourceStatus,
      targetStatus: params.targetStatus,
      sourceVersion: params.sourceVersion,
      targetVersion: params.targetVersion,
      expand: params.expand,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RelationArray>;
}

export interface GetRelationshipParams {
    path: { relationName: string; sourceType: string; sourceKey: string; targetType: string; targetKey: string };
    sourceStatus?: string;
    targetStatus?: string;
    sourceVersion?: number;
    targetVersion?: number;
    expand?: Array<"relationData" | "source" | "target">;
}

/**
 * Find relationship from source to target
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRelationship(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRelationshipParams): Promise<Types.Relation> {
    const path = `/wiki/rest/api/relation/${params.path.relationName}/from/${params.path.sourceType}/${params.path.sourceKey}/to/${params.path.targetType}/${params.path.targetKey}`;
    const queryParams = {
      sourceStatus: params.sourceStatus,
      targetStatus: params.targetStatus,
      sourceVersion: params.sourceVersion,
      targetVersion: params.targetVersion,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Relation>;
}

export interface CreateRelationshipParams {
    path: { relationName: string; sourceType: string; sourceKey: string; targetType: string; targetKey: string };
    sourceStatus?: string;
    targetStatus?: string;
    sourceVersion?: number;
    targetVersion?: number;
}

/**
 * Create relationship
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createRelationship(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateRelationshipParams): Promise<Types.Relation> {
    const path = `/wiki/rest/api/relation/${params.path.relationName}/from/${params.path.sourceType}/${params.path.sourceKey}/to/${params.path.targetType}/${params.path.targetKey}`;
    const queryParams = {
      sourceStatus: params.sourceStatus,
      targetStatus: params.targetStatus,
      sourceVersion: params.sourceVersion,
      targetVersion: params.targetVersion,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Relation>;
}

export interface DeleteRelationshipParams {
    path: { relationName: string; sourceType: string; sourceKey: string; targetType: string; targetKey: string };
    sourceStatus?: string;
    targetStatus?: string;
    sourceVersion?: number;
    targetVersion?: number;
}

/**
 * Delete relationship
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRelationship(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRelationshipParams): Promise<void> {
    const path = `/wiki/rest/api/relation/${params.path.relationName}/from/${params.path.sourceType}/${params.path.sourceKey}/to/${params.path.targetType}/${params.path.targetKey}`;
    const queryParams = {
      sourceStatus: params.sourceStatus,
      targetStatus: params.targetStatus,
      sourceVersion: params.sourceVersion,
      targetVersion: params.targetVersion,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface FindSourcesForTargetParams {
    path: { relationName: string; sourceType: string; targetType: string; targetKey: string };
    sourceStatus?: string;
    targetStatus?: string;
    sourceVersion?: number;
    targetVersion?: number;
    expand?: Array<"relationData" | "source" | "target">;
    start?: number;
    limit?: number;
}

/**
 * Find source entities related to a target entity
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function findSourcesForTarget(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindSourcesForTargetParams): Promise<Types.RelationArray> {
    const path = `/wiki/rest/api/relation/${params.path.relationName}/to/${params.path.targetType}/${params.path.targetKey}/from/${params.path.sourceType}`;
    const queryParams = {
      sourceStatus: params.sourceStatus,
      targetStatus: params.targetStatus,
      sourceVersion: params.sourceVersion,
      targetVersion: params.targetVersion,
      expand: params.expand,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RelationArray>;
}

export interface SearchByCQLParams {
    cql: string;
    cqlcontext?: string;
    cursor?: string;
    next?: boolean;
    prev?: boolean;
    limit?: number;
    start?: number;
    includeArchivedSpaces?: boolean;
    excludeCurrentSpaces?: boolean;
    excerpt?: "highlight" | "indexed" | "none" | "highlight_unescaped" | "indexed_unescaped";
    sitePermissionTypeFilter?: "all" | "externalCollaborator" | "none";
    _?: number;
    expand?: Array<string>;
}

/**
 * Search content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchByCQL(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchByCQLParams): Promise<Types.SearchPageResponseSearchResult> {
    const path = '/wiki/rest/api/search';
    const queryParams = {
      cql: params.cql,
      cqlcontext: params.cqlcontext,
      cursor: params.cursor,
      next: params.next,
      prev: params.prev,
      limit: params.limit,
      start: params.start,
      includeArchivedSpaces: params.includeArchivedSpaces,
      excludeCurrentSpaces: params.excludeCurrentSpaces,
      excerpt: params.excerpt,
      sitePermissionTypeFilter: params.sitePermissionTypeFilter,
      _: params._,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchPageResponseSearchResult>;
}

export interface SearchUserParams {
    cql: string;
    start?: number;
    limit?: number;
    expand?: Array<string>;
    sitePermissionTypeFilter?: "all" | "externalCollaborator" | "none";
}

/**
 * Search users
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function searchUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchUserParams): Promise<Types.SearchPageResponseSearchResult> {
    const path = '/wiki/rest/api/search/user';
    const queryParams = {
      cql: params.cql,
      start: params.start,
      limit: params.limit,
      expand: params.expand,
      sitePermissionTypeFilter: params.sitePermissionTypeFilter,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchPageResponseSearchResult>;
}

export interface GetLookAndFeelSettingsParams {
    spaceKey?: string;
}

/**
 * Get look and feel settings
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getLookAndFeelSettings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetLookAndFeelSettingsParams): Promise<Types.LookAndFeelSettings> {
    const path = '/wiki/rest/api/settings/lookandfeel';
    const queryParams = {
      spaceKey: params.spaceKey,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LookAndFeelSettings>;
}

export interface UpdateLookAndFeelParams {
    body?: Types.LookAndFeelSelection;
}

/**
 * Select look and feel settings
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateLookAndFeel(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateLookAndFeelParams): Promise<Types.LookAndFeelSelection> {
    const path = '/wiki/rest/api/settings/lookandfeel';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LookAndFeelSelection>;
}

export interface UpdateLookAndFeelSettingsParams {
    spaceKey?: string;
    body?: Types.LookAndFeel;
}

/**
 * Update look and feel settings
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 */
export async function updateLookAndFeelSettings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateLookAndFeelSettingsParams): Promise<Types.LookAndFeelWithLinks> {
    const path = '/wiki/rest/api/settings/lookandfeel/custom';
    const queryParams = {
      spaceKey: params.spaceKey,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LookAndFeelWithLinks>;
}

export interface ResetLookAndFeelSettingsParams {
    spaceKey?: string;
}

/**
 * Reset look and feel settings
 * @forge-scopes-asApp manage:jira-configuration
 */
export async function resetLookAndFeelSettings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ResetLookAndFeelSettingsParams): Promise<void> {
    const path = '/wiki/rest/api/settings/lookandfeel/custom';
    const queryParams = {
      spaceKey: params.spaceKey,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get system info
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSystemInfo(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.SystemInfoEntity> {
    const path = '/wiki/rest/api/settings/systemInfo';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SystemInfoEntity>;
}

export interface GetThemesParams {
    start?: number;
    limit?: number;
}

/**
 * Get themes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getThemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetThemesParams): Promise<Types.ThemeArray> {
    const path = '/wiki/rest/api/settings/theme';
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ThemeArray>;
}

/**
 * Get global theme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getGlobalTheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.Theme> {
    const path = '/wiki/rest/api/settings/theme/selected';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Theme>;
}

export interface GetThemeParams {
    path: { themeKey: string };
}

/**
 * Get theme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetThemeParams): Promise<Types.Theme> {
    const path = `/wiki/rest/api/settings/theme/${params.path.themeKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Theme>;
}

export interface CreateSpaceParams {
    body?: Types.SpaceCreate;
}

/**
 * Create space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateSpaceParams): Promise<Types.Space> {
    const path = '/wiki/rest/api/space';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Space>;
}

export interface CreatePrivateSpaceParams {
    body?: Types.SpaceCreate;
}

/**
 * Create private space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPrivateSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreatePrivateSpaceParams): Promise<Types.Space> {
    const path = '/wiki/rest/api/space/_private';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Space>;
}

export interface UpdateSpaceParams {
    path: { spaceKey: string };
    body?: Types.SpaceUpdate;
}

/**
 * Update space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateSpaceParams): Promise<Types.Space> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Space>;
}

export interface DeleteSpaceParams {
    path: { spaceKey: string };
}

/**
 * Delete space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteSpaceParams): Promise<Types.LongTask> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LongTask>;
}

export interface AddPermissionToSpaceParams {
    path: { spaceKey: string };
    body?: Types.SpacePermissionRequest;
}

/**
 * Add new permission to space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addPermissionToSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddPermissionToSpaceParams): Promise<Types.SpacePermissionV2> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/permission`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpacePermissionV2>;
}

export interface AddCustomContentPermissionsParams {
    path: { spaceKey: string };
    body?: Types.SpacePermissionCustomContent;
}

/**
 * Add new custom content permission to space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addCustomContentPermissions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddCustomContentPermissionsParams): Promise<void> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/permission/custom-content`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemovePermissionParams {
    path: { spaceKey: string; id: string };
}

/**
 * Remove a space permission
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removePermission(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemovePermissionParams): Promise<void> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/permission/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetSpaceSettingsParams {
    path: { spaceKey: string };
}

/**
 * Get space settings
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceSettings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSpaceSettingsParams): Promise<Types.SpaceSettings> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/settings`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceSettings>;
}

export interface UpdateSpaceSettingsParams {
    path: { spaceKey: string };
    body?: Types.SpaceSettingsUpdate;
}

/**
 * Update space settings
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSpaceSettings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateSpaceSettingsParams): Promise<Types.SpaceSettings> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/settings`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceSettings>;
}

export interface GetSpaceContentStatesParams {
    path: { spaceKey: string };
}

/**
 * Get space suggested content states
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceContentStates(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSpaceContentStatesParams): Promise<Array<Types.ContentState>> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/state`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ContentState>>;
}

export interface GetContentStateSettingsParams {
    path: { spaceKey: string };
}

/**
 * Get content state settings for space
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getContentStateSettings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentStateSettingsParams): Promise<Types.ContentStateSettings> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/state/settings`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentStateSettings>;
}

export interface GetContentsWithStateParams {
    path: { spaceKey: string };
    stateId: number;
    expand?: Array<string>;
    limit?: number;
    start?: number;
}

/**
 * Get content in space with given content state
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getContentsWithState(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentsWithStateParams): Promise<Types.ContentArray> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/state/content`;
    const queryParams = {
      stateId: params.stateId,
      expand: params.expand,
      limit: params.limit,
      start: params.start,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentArray>;
}

export interface GetSpaceThemeParams {
    path: { spaceKey: string };
}

/**
 * Get space theme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSpaceTheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSpaceThemeParams): Promise<Types.Theme> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/theme`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Theme>;
}

export interface SetSpaceThemeParams {
    path: { spaceKey: string };
    body?: Types.ThemeUpdate;
}

/**
 * Set space theme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setSpaceTheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetSpaceThemeParams): Promise<Types.Theme> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/theme`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Theme>;
}

export interface ResetSpaceThemeParams {
    path: { spaceKey: string };
}

/**
 * Reset space theme
 * @forge-scopes-asApp read:jira-work
 */
export async function resetSpaceTheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ResetSpaceThemeParams): Promise<void> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/theme`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWatchersForSpaceParams {
    path: { spaceKey: string };
    start?: string;
    limit?: string;
}

/**
 * Get space watchers
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWatchersForSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWatchersForSpaceParams): Promise<Types.SpaceWatchArray> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/watch`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SpaceWatchArray>;
}

export interface GetLabelsForSpaceParams {
    path: { spaceKey: string };
    prefix?: "global" | "my" | "team";
    start?: number;
    limit?: number;
}

/**
 * Get Space Labels
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLabelsForSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetLabelsForSpaceParams): Promise<Types.LabelArray> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/label`;
    const queryParams = {
      prefix: params.prefix,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LabelArray>;
}

export interface AddLabelsToSpaceParams {
    path: { spaceKey: string };
    body?: Array<Types.LabelCreate>;
}

/**
 * Add labels to a space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addLabelsToSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddLabelsToSpaceParams): Promise<Types.LabelArray> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/label`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LabelArray>;
}

export interface DeleteLabelFromSpaceParams {
    path: { spaceKey: string };
    name: string;
    prefix?: string;
}

/**
 * Remove label from a space
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteLabelFromSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteLabelFromSpaceParams): Promise<void> {
    const path = `/wiki/rest/api/space/${params.path.spaceKey}/label`;
    const queryParams = {
      name: params.name,
      prefix: params.prefix,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface CreateContentTemplateParams {
    body?: Types.ContentTemplateCreate;
}

/**
 * Create content template
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createContentTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateContentTemplateParams): Promise<Types.ContentTemplate> {
    const path = '/wiki/rest/api/template';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentTemplate>;
}

export interface UpdateContentTemplateParams {
    body?: Types.ContentTemplateUpdate;
}

/**
 * Update content template
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateContentTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateContentTemplateParams): Promise<Types.ContentTemplate> {
    const path = '/wiki/rest/api/template';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentTemplate>;
}

export interface GetBlueprintTemplatesParams {
    spaceKey?: string;
    start?: number;
    limit?: number;
}

/**
 * Get blueprint templates
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBlueprintTemplates(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBlueprintTemplatesParams): Promise<Types.BlueprintTemplateArray> {
    const path = '/wiki/rest/api/template/blueprint';
    const queryParams = {
      spaceKey: params.spaceKey,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BlueprintTemplateArray>;
}

export interface GetContentTemplatesParams {
    spaceKey?: string;
    start?: number;
    limit?: number;
}

/**
 * Get content templates
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getContentTemplates(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentTemplatesParams): Promise<Types.ContentTemplateArray> {
    const path = '/wiki/rest/api/template/page';
    const queryParams = {
      spaceKey: params.spaceKey,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentTemplateArray>;
}

export interface GetContentTemplateParams {
    path: { contentTemplateId: string };
}

/**
 * Get content template
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getContentTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentTemplateParams): Promise<Types.ContentTemplate> {
    const path = `/wiki/rest/api/template/${params.path.contentTemplateId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContentTemplate>;
}

export interface RemoveTemplateParams {
    path: { contentTemplateId: string };
}

/**
 * Remove template
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveTemplateParams): Promise<void> {
    const path = `/wiki/rest/api/template/${params.path.contentTemplateId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetUserParams {
    accountId: string;
    expand?: Array<"operations" | "personalSpace" | "isExternalCollaborator">;
}

/**
 * Get user
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserParams): Promise<Types.User> {
    const path = '/wiki/rest/api/user';
    const queryParams = {
      accountId: params.accountId,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.User>;
}

export interface GetAnonymousUserParams {
    expand?: Array<"operations">;
}

/**
 * Get anonymous user
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getAnonymousUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAnonymousUserParams): Promise<Types.UserAnonymous> {
    const path = '/wiki/rest/api/user/anonymous';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserAnonymous>;
}

export interface GetCurrentUserParams {
    expand?: Array<"operations" | "personalSpace" | "isExternalCollaborator">;
}

/**
 * Get current user
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getCurrentUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCurrentUserParams): Promise<Types.User> {
    const path = '/wiki/rest/api/user/current';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.User>;
}

export interface GetGroupMembershipsForUserParams {
    accountId: string;
    start?: number;
    limit?: number;
}

/**
 * Get group memberships for user
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getGroupMembershipsForUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetGroupMembershipsForUserParams): Promise<Types.GroupArrayWithLinks> {
    const path = '/wiki/rest/api/user/memberof';
    const queryParams = {
      accountId: params.accountId,
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GroupArrayWithLinks>;
}

export interface GetBulkUserLookupParams {
    accountId: string;
    expand?: Array<"operations" | "personalSpace" | "isExternalCollaborator">;
}

/**
 * Get multiple users using ids
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getBulkUserLookup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBulkUserLookupParams): Promise<Types.BulkUserLookupArray> {
    const path = '/wiki/rest/api/user/bulk';
    const queryParams = {
      accountId: params.accountId,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BulkUserLookupArray>;
}

export interface GetContentWatchStatusParams {
    path: { contentId: string };
}

/**
 * Get content watch status
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getContentWatchStatus(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContentWatchStatusParams): Promise<Types.UserWatch> {
    const path = `/wiki/rest/api/user/watch/content/${params.path.contentId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserWatch>;
}

export interface AddContentWatcherParams {
    path: { contentId: string };
}

/**
 * Add content watcher
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addContentWatcher(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddContentWatcherParams): Promise<void> {
    const path = `/wiki/rest/api/user/watch/content/${params.path.contentId}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveContentWatcherParams {
    path: { contentId: string };
}

/**
 * Remove content watcher
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeContentWatcher(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveContentWatcherParams): Promise<void> {
    const path = `/wiki/rest/api/user/watch/content/${params.path.contentId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface IsWatchingLabelParams {
    path: { labelName: string };
}

/**
 * Get label watch status
 * @forge-scopes-asApp read:jira-work
 */
export async function isWatchingLabel(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: IsWatchingLabelParams): Promise<Types.UserWatch> {
    const path = `/wiki/rest/api/user/watch/label/${params.path.labelName}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserWatch>;
}

export interface AddLabelWatcherParams {
    path: { labelName: string };
}

/**
 * Add label watcher
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addLabelWatcher(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddLabelWatcherParams): Promise<void> {
    const path = `/wiki/rest/api/user/watch/label/${params.path.labelName}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveLabelWatcherParams {
    path: { labelName: string };
}

/**
 * Remove label watcher
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeLabelWatcher(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveLabelWatcherParams): Promise<void> {
    const path = `/wiki/rest/api/user/watch/label/${params.path.labelName}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface IsWatchingSpaceParams {
    path: { spaceKey: string };
}

/**
 * Get space watch status
 * @forge-scopes-asApp read:jira-work
 */
export async function isWatchingSpace(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: IsWatchingSpaceParams): Promise<Types.UserWatch> {
    const path = `/wiki/rest/api/user/watch/space/${params.path.spaceKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserWatch>;
}

export interface AddSpaceWatcherParams {
    path: { spaceKey: string };
}

/**
 * Add space watcher
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addSpaceWatcher(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddSpaceWatcherParams): Promise<void> {
    const path = `/wiki/rest/api/user/watch/space/${params.path.spaceKey}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveSpaceWatchParams {
    path: { spaceKey: string };
}

/**
 * Remove space watch
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeSpaceWatch(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveSpaceWatchParams): Promise<void> {
    const path = `/wiki/rest/api/user/watch/space/${params.path.spaceKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPrivacyUnsafeUserEmailParams {
    accountId: string;
}

/**
 * Get user email address
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getPrivacyUnsafeUserEmail(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPrivacyUnsafeUserEmailParams): Promise<Types.AccountIdEmailRecord> {
    const path = '/wiki/rest/api/user/email';
    const queryParams = {
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AccountIdEmailRecord>;
}

export interface GetPrivacyUnsafeUserEmailBulkParams {
    accountId: Array<string>;
}

/**
 * Get user email addresses in batch
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getPrivacyUnsafeUserEmailBulk(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPrivacyUnsafeUserEmailBulkParams): Promise<Types.AccountIdEmailRecordArray> {
    const path = '/wiki/rest/api/user/email/bulk';
    const queryParams = {
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AccountIdEmailRecordArray>;
}

/**
 * Get modules
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getModules(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<void> {
    const path = '/wiki/rest/atlassian-connect/1/app/module/dynamic';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RegisterModulesParams {
    body?: Record<string, unknown>;
}

/**
 * Register modules
 * @forge-scopes-asApp read:jira-work
 */
export async function registerModules(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RegisterModulesParams): Promise<void> {
    const path = '/wiki/rest/atlassian-connect/1/app/module/dynamic';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveModulesParams {
    moduleKey: Array<string>;
}

/**
 * Remove modules
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeModules(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveModulesParams): Promise<void> {
    const path = '/wiki/rest/atlassian-connect/1/app/module/dynamic';
    const queryParams = {
      moduleKey: params.moduleKey,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetViewsParams {
    path: { contentId: string };
    fromDate?: string;
}

/**
 * Get views
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getViews(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetViewsParams): Promise<{ id?: number; count?: number }> {
    const path = `/wiki/rest/api/analytics/content/${params.path.contentId}/views`;
    const queryParams = {
      fromDate: params.fromDate,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ id?: number; count?: number }>;
}

export interface GetViewersParams {
    path: { contentId: string };
    fromDate?: string;
}

/**
 * Get viewers
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getViewers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetViewersParams): Promise<{ id?: number; count?: number }> {
    const path = `/wiki/rest/api/analytics/content/${params.path.contentId}/viewers`;
    const queryParams = {
      fromDate: params.fromDate,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ id?: number; count?: number }>;
}

export interface GetUserPropertiesParams {
    path: { userId: string };
    start?: number;
    limit?: number;
}

/**
 * Get user properties
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserProperties(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserPropertiesParams): Promise<Types.UserPropertyKeyArray> {
    const path = `/wiki/rest/api/user/${params.path.userId}/property`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserPropertyKeyArray>;
}

export interface GetUserPropertyParams {
    path: { userId: string; key: string };
}

/**
 * Get user property
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserPropertyParams): Promise<Types.UserProperty> {
    const path = `/wiki/rest/api/user/${params.path.userId}/property/${params.path.key}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserProperty>;
}

export interface CreateUserPropertyParams {
    path: { userId: string; key: string };
    body?: Types.UserPropertyCreate;
}

/**
 * Create user property by key
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function createUserProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateUserPropertyParams): Promise<void> {
    const path = `/wiki/rest/api/user/${params.path.userId}/property/${params.path.key}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface UpdateUserPropertyParams {
    path: { userId: string; key: string };
    body?: Types.UserPropertyUpdate;
}

/**
 * Update user property
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function updateUserProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateUserPropertyParams): Promise<void> {
    const path = `/wiki/rest/api/user/${params.path.userId}/property/${params.path.key}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface DeleteUserPropertyParams {
    path: { userId: string; key: string };
}

/**
 * Delete user property
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function deleteUserProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteUserPropertyParams): Promise<void> {
    const path = `/wiki/rest/api/user/${params.path.userId}/property/${params.path.key}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}
