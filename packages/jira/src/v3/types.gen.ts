// Generated by @forge-clients/generator — DO NOT EDIT
// Source: Jira Cloud REST API v3
/* eslint-disable */
// prettier-ignore
export interface ActorInputBean {
    /** The name of the group to add as a default actor. This parameter cannot be used with the `groupId` parameter. As a group's name can change,use of `groupId` is recommended. This parameter accepts a comma-separated list. For example, `"group":["project-admin", "jira-developers"]`. */
    group?: Array<string>;
    /** The ID of the group to add as a default actor. This parameter cannot be used with the `group` parameter This parameter accepts a comma-separated list. For example, `"groupId":["77f6ab39-e755-4570-a6ae-2d7a8df0bcb8", "0c011f85-69ed-49c4-a801-3b18d0f771bc"]`. */
    groupId?: Array<string>;
    /** The account IDs of the users to add as default actors. This parameter accepts a comma-separated list. For example, `"user":["5b10a2844c20165700ede21g", "5b109f2e9729b51b54dc274d"]`. */
    user?: Array<string>;
}

export interface ActorsMap {
    /** The name of the group to add. This parameter cannot be used with the `groupId` parameter. As a group's name can change, use of `groupId` is recommended. */
    group?: Array<string>;
    /** The ID of the group to add. This parameter cannot be used with the `group` parameter. */
    groupId?: Array<string>;
    /** The user account ID of the user to add. */
    user?: Array<string>;
}

export interface AddAtlassianTeamRequest {
    /** The capacity for the Atlassian team. */
    capacity?: number;
    /** The Atlassian team ID. */
    id: string;
    /** The ID of the issue source for the Atlassian team. */
    issueSourceId?: number;
    /** The planning style for the Atlassian team. This must be "Scrum" or "Kanban". */
    planningStyle: "Scrum" | "Kanban";
    /** The sprint length for the Atlassian team. */
    sprintLength?: number;
}

export interface AddFieldBean {
    /** The ID of the field to add. */
    fieldId: string;
}

export type AddGroupBean = Record<string, unknown>;
/** Details of notifications which should be added to the notification scheme. */
export type AddNotificationsDetails = Record<string, unknown>;

export interface AddSecuritySchemeLevelsRequestBean {
    /** The list of scheme levels which should be added to the security scheme. */
    levels?: Array<{ description?: string; isDefault?: boolean; members?: Array<{ parameter?: string; type: string }>; name: string }>;
}

/** Announcement banner configuration. */
export interface AnnouncementBannerConfiguration {
    /** Hash of the banner data. The client detects updates by comparing hash IDs. */
    readonly hashId?: string;
    /** Flag indicating if the announcement banner can be dismissed by the user. */
    readonly isDismissible?: boolean;
    /** Flag indicating if the announcement banner is enabled or not. */
    readonly isEnabled?: boolean;
    /** The text on the announcement banner. */
    readonly message?: string;
    /** Visibility of the announcement banner. */
    readonly visibility?: "public" | "private";
}

/** Configuration of the announcement banner. */
export interface AnnouncementBannerConfigurationUpdate {
    /** Flag indicating if the announcement banner can be dismissed by the user. */
    isDismissible?: boolean;
    /** Flag indicating if the announcement banner is enabled or not. */
    isEnabled?: boolean;
    /** The text on the announcement banner. */
    message?: string;
    /** Visibility of the announcement banner. Can be public or private. */
    visibility?: string;
}

/** A workflow transition rule. */
export interface AppWorkflowTransitionRule {
    /** A rule configuration. */
    configuration: { disabled?: boolean; tag?: string; value: string };
    /** The ID of the transition rule. */
    id: string;
    /** The key of the rule, as defined in the Connect or the Forge app descriptor. */
    readonly key: string;
    readonly transition?: { id: number; name: string };
}

/** The application the linked item is in. */
export type Application = Record<string, unknown>;

/** Details of an application property. */
export interface ApplicationProperty {
    /** The allowed values, if applicable. */
    allowedValues?: Array<string>;
    /** The default value of the application property. */
    defaultValue?: string;
    /** The description of the application property. */
    desc?: string;
    example?: string;
    /** The ID of the application property. The ID and key are the same. */
    id?: string;
    /** The key of the application property. The ID and key are the same. */
    key?: string;
    /** The name of the application property. */
    name?: string;
    /** The data type of the application property. */
    type?: string;
    /** The new value. */
    value?: string;
}

/** Details of an application role. */
export interface ApplicationRole {
    /** The groups that are granted default access for this application role. As a group's name can change, use of `defaultGroupsDetails` is recommended to identify a groups. */
    defaultGroups?: Array<string>;
    /** The groups that are granted default access for this application role. */
    defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>;
    /** Deprecated. */
    defined?: boolean;
    /** The groups associated with the application role. */
    groupDetails?: Array<unknown>;
    /** The groups associated with the application role. As a group's name can change, use of `groupDetails` is recommended to identify a groups. */
    groups?: Array<string>;
    hasUnlimitedSeats?: boolean;
    /** The key of the application role. */
    key?: string;
    /** The display name of the application role. */
    name?: string;
    /** The maximum count of users on your license. */
    numberOfSeats?: number;
    /** Indicates if the application role belongs to Jira platform (`jira-core`). */
    platform?: boolean;
    /** The count of users remaining on your license. */
    remainingSeats?: number;
    /** Determines whether this application role should be selected by default on user creation. */
    selectedByDefault?: boolean;
    /** The number of users counting against your license. */
    userCount?: number;
    /** The [type of users](https://confluence.atlassian.com/x/lRW3Ng) being counted against your license. */
    userCountDescription?: string;
}

/** The approval configuration of a status within a workflow. Applies only to Jira Service Management approvals. */
export interface ApprovalConfiguration {
    /** Whether the approval configuration is active. */
    active: "true" | "false";
    /** How the required approval count is calculated. It may be configured to require a specific number of approvals, or approval by a percentage of approvers. If the approvers source field is Approver groups, you can configure how many approvals per group are required for the request to be approved. The number will be the same across all groups. */
    conditionType: "number" | "percent" | "numberPerPrincipal";
    /** The number or percentage of approvals required for a request to be approved. If `conditionType` is `number`, the value must be 20 or less. If `conditionType` is `percent`, the value must be 100 or less. */
    conditionValue: string;
    /** A list of roles that should be excluded as possible approvers. */
    exclude?: Array<"assignee" | "reporter">;
    /** The custom field ID of the "Approvers" or "Approver Groups" field. */
    fieldId: string;
    /** The custom field ID of the field used to pre-populate the Approver field. Only supports the "Affected Services" field. */
    prePopulatedFieldId?: string;
    /** The numeric ID of the transition to be executed if the request is approved. */
    transitionApproved: string;
    /** The numeric ID of the transition to be executed if the request is declined. */
    transitionRejected: string;
}

/** Approval configuration. */
export interface ApprovalConfigurationPreview {
    /** The active approval configuration. */
    active?: string;
    /** The transition ID for approved state. */
    transitionApproved?: string;
    /** The transition ID for rejected state. */
    transitionRejected?: string;
}

export interface ArchiveIssueAsyncRequest {
    jql?: string;
}

/** Details of a filter for exporting archived issues. */
export type ArchivedIssuesFilterRequest = Record<string, unknown>;

/** Details of a field configuration to issue type mappings. */
export interface AssociateFieldConfigurationsWithIssueTypesRequest {
    /** Field configuration to issue type mappings. */
    mappings: Array<{ fieldConfigurationId: string; issueTypeId: string }>;
}

/** Issue security scheme, project, and remapping details. */
export interface AssociateSecuritySchemeWithProjectDetails {
    /** The list of scheme levels which should be remapped to new levels of the issue security scheme. */
    oldToNewSecurityLevelMappings?: Array<{ newLevelId: string; oldLevelId: string }>;
    /** The ID of the project. */
    projectId: string;
    /** The ID of the issue security scheme. Providing null will clear the association with the issue security scheme. */
    schemeId: string;
}

/** Details of an item associated with the changed record. */
export interface AssociatedItemBean {
    /** The ID of the associated record. */
    readonly id?: string;
    /** The name of the associated record. */
    readonly name?: string;
    /** The ID of the associated parent record. */
    readonly parentId?: string;
    /** The name of the associated parent record. */
    readonly parentName?: string;
    /** The type of the associated record. */
    readonly typeName?: string;
}

/** Field association for example PROJECT\_ID. */
export interface AssociationContextObject {
    identifier?: Record<string, unknown>;
    type: string;
}

/** Details about an attachment. */
export type Attachment = Record<string, unknown>;

export interface AttachmentArchive {
    entries?: Array<{ abbreviatedName?: string; entryIndex?: number; mediaType?: string; name?: string; size?: number }>;
    moreAvailable?: boolean;
    totalEntryCount?: number;
    totalNumberOfEntriesAvailable?: number;
}

export interface AttachmentArchiveEntry {
    abbreviatedName?: string;
    entryIndex?: number;
    mediaType?: string;
    name?: string;
    size?: number;
}

export interface AttachmentArchiveImpl {
    /** The list of the items included in the archive. */
    entries?: Array<{ abbreviatedName?: string; entryIndex?: number; mediaType?: string; name?: string; size?: number }>;
    /** The number of items in the archive. */
    totalEntryCount?: number;
}

/** Metadata for an item in an attachment archive. */
export interface AttachmentArchiveItemReadable {
    /** The position of the item within the archive. */
    readonly index?: number;
    /** The label for the archive item. */
    readonly label?: string;
    /** The MIME type of the archive item. */
    readonly mediaType?: string;
    /** The path of the archive item. */
    readonly path?: string;
    /** The size of the archive item. */
    readonly size?: string;
}

/** Metadata for an archive (for example a zip) and its contents. */
export interface AttachmentArchiveMetadataReadable {
    /** The list of the items included in the archive. */
    readonly entries?: Array<{ readonly index?: number; readonly label?: string; readonly mediaType?: string; readonly path?: string; readonly size?: string }>;
    /** The ID of the attachment. */
    readonly id?: number;
    /** The MIME type of the attachment. */
    readonly mediaType?: string;
    /** The name of the archive file. */
    readonly name?: string;
    /** The number of items included in the archive. */
    readonly totalEntryCount?: number;
}

/** Metadata for an issue attachment. */
export interface AttachmentMetadata {
    /** Details of the user who attached the file. */
    readonly author?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string };
    /** The URL of the attachment. */
    readonly content?: string;
    /** The datetime the attachment was created. */
    readonly created?: string;
    /** The name of the attachment file. */
    readonly filename?: string;
    /** The ID of the attachment. */
    readonly id?: number;
    /** The MIME type of the attachment. */
    readonly mimeType?: string;
    /** Additional properties of the attachment. */
    readonly properties?: Record<string, Record<string, unknown>>;
    /** The URL of the attachment metadata details. */
    readonly self?: string;
    /** The size of the attachment. */
    readonly size?: number;
    /** The URL of a thumbnail representing the attachment. */
    readonly thumbnail?: string;

/** Details of the instance's attachment settings. */
export interface AttachmentSettings {
    /** Whether the ability to add attachments is enabled. */
    readonly enabled?: boolean;
    /** The maximum size of attachments permitted, in bytes. */
    readonly uploadLimit?: number;
}

/** An audit record. */
export interface AuditRecordBean {
    /** The list of items associated with the changed record. */
    readonly associatedItems?: Array<{ readonly id?: string; readonly name?: string; readonly parentId?: string; readonly parentName?: string; readonly typeName?: string }>;
    /** Deprecated, use `authorAccountId` instead. The key of the user who created the audit record. */
    readonly authorKey?: string;
    /** The category of the audit record. For a list of these categories, see the help article [Auditing in Jira applications](https://confluence.atlassian.com/x/noXKM). */
    readonly category?: string;
    /** The list of values changed in the record event. */
    readonly changedValues?: Array<{ readonly changedFrom?: string; readonly changedTo?: string; readonly fieldName?: string }>;
    /** The date and time on which the audit record was created. */
    readonly created?: string;
    /** The description of the audit record. */
    readonly description?: string;
    /** The event the audit record originated from. */
    readonly eventSource?: string;
    /** The ID of the audit record. */
    readonly id?: number;
    /** Details of an item associated with the changed record. */
    objectItem?: unknown;
    /** The URL of the computer where the creation of the audit record was initiated. */
    readonly remoteAddress?: string;
    /** The summary of the audit record. */
    readonly summary?: string;
}

/** Container for a list of audit records. */
export interface AuditRecords {
    /** The requested or default limit on the number of audit items to be returned. */
    readonly limit?: number;
    /** The number of audit items skipped before the first item in this list. */
    readonly offset?: number;
    /** The list of audit items. */
    readonly records?: Array<{ readonly associatedItems?: Array<{ readonly id?: string; readonly name?: string; readonly parentId?: string; readonly parentName?: string; readonly typeName?: string }>; readonly authorKey?: string; readonly category?: string; readonly changedValues?: Array<{ readonly changedFrom?: string; readonly changedTo?: string; readonly fieldName?: string }>; readonly created?: string; readonly description?: string; readonly eventSource?: string; readonly id?: number; objectItem?: unknown; readonly remoteAddress?: string; readonly summary?: string }>;
    /** The total number of audit items returned. */
    readonly total?: number;
}

/** A field auto-complete suggestion. */
export interface AutoCompleteSuggestion {
    /** The display name of a suggested item. If `fieldValue` or `predicateValue` are provided, the matching text is highlighted with the HTML bold tag. */
    displayName?: string;
    /** The value of a suggested item. */
    value?: string;
}

/** The results from a JQL query. */
export interface AutoCompleteSuggestions {
    /** The list of suggested item. */
    results?: Array<{ displayName?: string; value?: string }>;
}

/** The details of the available dashboard gadget. */
export interface AvailableDashboardGadget {
    /** The module key of the gadget type. */
    readonly moduleKey?: string;
    /** The title of the gadget. */
    readonly title: string;
    /** The URI of the gadget type. */
    readonly uri?: string;
}

/** The list of available gadgets. */
export interface AvailableDashboardGadgetsResponse {
    /** The list of available gadgets. */
    readonly gadgets: Array<{ readonly moduleKey?: string; readonly title: string; readonly uri?: string }>;
}

/** The Connect provided ecosystem rules available. */
export interface AvailableWorkflowConnectRule {
    /** The add-on providing the rule. */
    addonKey?: string;
    /** The URL creation path segment defined in the Connect module. */
    createUrl?: string;
    /** The rule description. */
    description?: string;
    /** The URL edit path segment defined in the Connect module. */
    editUrl?: string;
    /** The module providing the rule. */
    moduleKey?: string;
    /** The rule name. */
    name?: string;
    /** The rule key. */
    ruleKey?: string;
    /** The rule type. */
    ruleType?: "Condition" | "Validator" | "Function" | "Screen";
    /** The URL view path segment defined in the Connect module. */
    viewUrl?: string;
}

/** The Forge provided ecosystem rules available. */
export interface AvailableWorkflowForgeRule {
    /** The rule description. */
    description?: string;
    /** The unique ARI of the forge rule type. */
    id?: string;
    /** The rule name. */
    name?: string;
    /** The rule key. */
    ruleKey?: string;
    /** The rule type. */
    ruleType?: "Condition" | "Validator" | "Function" | "Screen";
}

/** The Atlassian provided system rules available. */
export interface AvailableWorkflowSystemRule {
    /** The rule description. */
    description: string;
    /** List of rules that conflict with this one. */
    incompatibleRuleKeys: Array<string>;
    /** Whether the rule can be added added to an initial transition. */
    isAvailableForInitialTransition: boolean;
    /** Whether the rule is visible. */
    isVisible: boolean;
    /** The rule name. */
    name: string;
    /** The rule key. */
    ruleKey: string;
    /** The rule type. */
    ruleType: "Condition" | "Validator" | "Function" | "Screen";
}

/** The list of available trigger types. */
export interface AvailableWorkflowTriggerTypes {
    /** The description of the trigger rule. */
    description?: string;
    /** The name of the trigger rule. */
    name?: string;
    /** The type identifier of trigger rule. */
    type?: string;
}

/** The trigger rules available. */
export interface AvailableWorkflowTriggers {
    /** The list of available trigger types. */
    availableTypes: Array<{ description?: string; name?: string; type?: string }>;
    /** The rule key of the rule. */
    ruleKey: string;
}

/** Details of an avatar. */
export type Avatar = Record<string, unknown>;

export interface AvatarUrlsBean {
    /** The URL of the item's 16x16 pixel avatar. */
    16x16?: string;
    /** The URL of the item's 24x24 pixel avatar. */
    24x24?: string;
    /** The URL of the item's 32x32 pixel avatar. */
    32x32?: string;
    /** The URL of the item's 48x48 pixel avatar. */
    48x48?: string;

/** Details about system and custom avatars. */
export interface Avatars {
    /** Custom avatars list. */
    readonly custom?: Array<Record<string, unknown>>;
    /** System avatars list. */
    readonly system?: Array<unknown>;
}

/** The payload for creating a board column */
export interface BoardColumnPayload {
    /** The maximum issue constraint for the column */
    maximumIssueConstraint?: number;
    /** The minimum issue constraint for the column */
    minimumIssueConstraint?: number;
    /** The name of the column */
    name?: string;
    /** The status IDs for the column */
    statusIds?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }>;
}

/** The payload for setting a board feature */
export interface BoardFeaturePayload {
    /** The key of the feature */
    featureKey?: "ESTIMATION" | "SPRINTS";
    /** Whether the feature should be turned on or off */
    state?: boolean;
}

/** Configuration of features for one or more boards. Replaces the deprecated features field on BoardPayload */
export interface BoardFeaturesPayload {
    /** A map of board PCRIs to the list of features to enable on each board. */
    boardFeatures?: Record<string, Array<{ featureKey?: "ESTIMATION" | "SPRINTS"; state?: boolean }>>;
}

/** The payload for creating a board */
export interface BoardPayload {
    /** Takes in a JQL string to create a new filter. If no value is provided, it'll default to a JQL filter for the project creating */
    boardFilterJQL?: string;
    /** Card color settings of the board */
    cardColorStrategy?: "ISSUE_TYPE" | "REQUEST_TYPE" | "ASSIGNEE" | "PRIORITY" | "NONE" | "CUSTOM";
    /** Card layout configuration. */
    cardLayout?: { showDaysInColumn?: boolean };
    /** Card layout settings of the board */
    cardLayouts?: Array<{ fieldId?: string; id?: number; mode?: "PLAN" | "WORK"; position?: number }>;
    /** The columns of the board */
    columns?: Array<{ maximumIssueConstraint?: number; minimumIssueConstraint?: number; name?: string; statusIds?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }> }>;
    /** Feature settings for the board. Deprecated: use boardFeatures capability instead. */
    /** @deprecated */
    features?: Array<{ featureKey?: "ESTIMATION" | "SPRINTS"; state?: boolean }>;
    /** The name of the board */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
    /** The quick filters for the board. */
    quickFilters?: Array<{ description?: string; jqlQuery?: string; name?: string }>;
    /** Whether sprints are supported on the board */
    supportsSprint?: boolean;
    /** The payload for customising a swimlanes on a board */
    swimlanes?: { customSwimlanes?: Array<{ description?: string; jqlQuery?: string; name?: string }>; defaultCustomSwimlaneName?: string; swimlaneStrategy?: "none" | "custom" | "parentChild" | "assignee" | "assigneeUnassignedFirst" | "epic" | "project" | "issueparent" | "issuechildren" | "request_type" };
    /** Working days configuration */
    workingDaysConfig?: { friday?: boolean; id?: number; monday?: boolean; nonWorkingDays?: Array<{ id?: number; iso8601Date?: string }>; saturday?: boolean; sunday?: boolean; thursday?: boolean; timezoneId?: string; tuesday?: boolean; wednesday?: boolean };
}

export interface BoardsPayload {
    /** The boards to be associated with the project. */
    boards?: Array<{ boardFilterJQL?: string; cardColorStrategy?: "ISSUE_TYPE" | "REQUEST_TYPE" | "ASSIGNEE" | "PRIORITY" | "NONE" | "CUSTOM"; cardLayout?: { showDaysInColumn?: boolean }; cardLayouts?: Array<{ fieldId?: string; id?: number; mode?: "PLAN" | "WORK"; position?: number }>; columns?: Array<{ maximumIssueConstraint?: number; minimumIssueConstraint?: number; name?: string; statusIds?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }> }>; features?: Array<{ featureKey?: "ESTIMATION" | "SPRINTS"; state?: boolean }>; name?: string; pcri?: unknown; quickFilters?: Array<{ description?: string; jqlQuery?: string; name?: string }>; supportsSprint?: boolean; swimlanes?: { customSwimlanes?: Array<{ description?: string; jqlQuery?: string; name?: string }>; defaultCustomSwimlaneName?: string; swimlaneStrategy?: "none" | "custom" | "parentChild" | "assignee" | "assigneeUnassignedFirst" | "epic" | "project" | "issueparent" | "issuechildren" | "request_type" }; workingDaysConfig?: { friday?: boolean; id?: number; monday?: boolean; nonWorkingDays?: Array<{ id?: number; iso8601Date?: string }>; saturday?: boolean; sunday?: boolean; thursday?: boolean; timezoneId?: string; tuesday?: boolean; wednesday?: boolean } }>;
}

/** Details for changing owners of shareable entities */
export interface BulkChangeOwnerDetails {
    /** Whether the name is fixed automatically if it's duplicated after changing owner. */
    autofixName: boolean;
    /** The account id of the new owner. */
    newOwner: string;
}

/** Request bean for bulk changelog retrieval */
export interface BulkChangelogRequestBean {
    /** List of field IDs to filter changelogs */
    fieldIds?: Array<string>;
    /** List of issue IDs/keys to fetch changelogs for */
    issueIdsOrKeys: Array<string>;
    /** The maximum number of items to return per page */
    maxResults?: number;
    /** The cursor for pagination */
    nextPageToken?: string;
}

/** A page of changelogs which is designed to handle multiple issues */
export interface BulkChangelogResponseBean {
    /** The list of issues changelogs. */
    readonly issueChangeLogs?: Array<{ readonly changeHistories?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>; readonly issueId?: string }>;
    /** Continuation token to fetch the next page. If this result represents the last or the only page, this token will be null. */
    readonly nextPageToken?: string;

/** Details of the contextual configuration for a custom field. */
export interface BulkContextualConfiguration {
    /** The field configuration. */
    configuration?: Record<string, unknown>;
    /** The ID of the custom field. */
    customFieldId: string;
    /** The ID of the field context the configuration is associated with. */
    readonly fieldContextId: string;
    /** The ID of the configuration. */
    id: string;
    /** The field value schema. */
    schema?: Record<string, unknown>;
}

/** Details of the options to create for a custom field. */
export interface BulkCustomFieldOptionCreateRequest {
    /** Details of options to create. */
    options?: Array<{ disabled?: boolean; optionId?: string; value: string }>;
}

/** Details of the options to update for a custom field. */
export interface BulkCustomFieldOptionUpdateRequest {
    /** Details of the options to update. */
    options?: Array<{ disabled?: boolean; id: string; value?: string }>;
}

/** Errors of bulk edit action. */
export interface BulkEditActionError {
    /** The error messages. */
    errorMessages: Array<string>;
    /** The errors. */
    errors: Record<string, string>;
}

/** Bulk Edit Get Fields Response. */
export interface BulkEditGetFields {
    /** The end cursor for use in pagination. */
    readonly endingBefore?: string;
    /** List of all the fields */
    readonly fields?: Array<{ description?: string; fieldOptions?: Array<Record<string, unknown>>; id?: string; isRequired?: boolean; multiSelectFieldOptions?: Array<"ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL">; name?: string; searchUrl?: string; type?: string; unavailableMessage?: string }>;
    /** The start cursor for use in pagination. */
    readonly startingAfter?: string;
}

/** Details of a request to bulk edit shareable entity. */
export interface BulkEditShareableEntityRequest {
    /** Allowed action for bulk edit shareable entity */
    action: "changeOwner" | "changePermission" | "addPermission" | "removePermission";
    /** The details of change owner action. */
    changeOwnerDetails?: { autofixName: boolean; newOwner: string };
    /** The id list of shareable entities to be changed. */
    entityIds: Array<number>;
    /** Whether the actions are executed by users with Administer Jira global permission. */
    extendAdminPermissions?: boolean;
    /** The permission details to be changed. */
    permissionDetails?: { editPermissions: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>; sharePermissions: Array<unknown> };

/** Details of a request to bulk edit shareable entity. */
export interface BulkEditShareableEntityResponse {
    /** Allowed action for bulk edit shareable entity */
    action: "changeOwner" | "changePermission" | "addPermission" | "removePermission";
    /** The mapping dashboard id to errors if any. */
    entityErrors?: Record<string, { errorMessages: Array<string>; errors: Record<string, string> }>;
}

export interface BulkFetchIssueRequestBean {
    /**
     * Use [expand](#expansion) to include additional information about issues in the response. Note that, unlike the majority of instances where `expand` is specified, `expand` is defined as a list of values. The expand options are:
     *
     *  *  `renderedFields` Returns field values rendered in HTML format.
     *  *  `names` Returns the display name of each field.
     *  *  `schema` Returns the schema describing a field type.
     *  *  `transitions` Returns all possible transitions for the issue.
     *  *  `operations` Returns all possible operations for the issue.
     *  *  `editmeta` Returns information about how each field can be edited.
     *  *  `changelog` Returns a list of recent updates to an issue, sorted by date, starting from the most recent. This returns a maximum of 40 changelogs. If you require more, please refer to [Bulk fetch changelogs](#api-rest-api-3-changelog-bulkfetch-post).
     *  *  `versionedRepresentations` Instead of `fields`, returns `versionedRepresentations` a JSON array containing each version of a field's value, with the highest numbered item representing the most recent version.
     */
    expand?: Array<string>;
    /**
     * A list of fields to return for each issue, use it to retrieve a subset of fields. This parameter accepts a comma-separated list. Expand options include:
     *
     *  *  `*all` Returns all fields.
     *  *  `*navigable` Returns navigable fields.
     *  *  Any issue field, prefixed with a minus to exclude.
     *
     * The default is `*navigable`.
     *
     * Examples:
     *
     *  *  `summary,comment` Returns the summary and comments fields only.
     *  *  `-description` Returns all navigable (default) fields except description.
     *  *  `*all,-comment` Returns all fields except comments.
     *
     * Multiple `fields` parameters can be included in a request.
     *
     * Note: All navigable fields are returned by default. This differs from [GET issue](#api-rest-api-3-issue-issueIdOrKey-get) where the default is all fields.
     */
    fields?: Array<string>;
    /** Reference fields by their key (rather than ID). The default is `false`. */
    fieldsByKeys?: boolean;
    /** An array of issue IDs or issue keys to fetch. You can mix issue IDs and keys in the same query. */
    issueIdsOrKeys: Array<string>;
    /** A list of issue property keys of issue properties to be included in the results. A maximum of 5 issue property keys can be specified. */
    properties?: Array<string>;
}

/** A container for the watch status of a list of issues. */
export interface BulkIssueIsWatching {
    /** The map of issue ID to boolean watch status. */
    readonly issuesIsWatching?: Record<string, boolean>;
}

/** Bulk issue property update request details. */
export interface BulkIssuePropertyUpdateRequest {
    /** EXPERIMENTAL. The Jira expression to calculate the value of the property. The value of the expression must be an object that can be converted to JSON, such as a number, boolean, string, list, or map. The context variables available to the expression are `issue` and `user`. Issues for which the expression returns a value whose JSON representation is longer than 32768 characters are ignored. */
    expression?: string;
    /** The bulk operation filter. */
    filter?: { currentValue?: Record<string, unknown>; entityIds?: Array<number>; hasProperty?: boolean };
    /** The value of the property. The value must be a [valid](https://tools.ietf.org/html/rfc4627), non-empty JSON blob. The maximum length is 32768 characters. */
    value?: Record<string, unknown>;
}

/** The list of requested issues & fields. */
export interface BulkIssueResults {
    /** When Jira can't return an issue enumerated in a request due to a retriable error or payload constraint, we'll return the respective issue ID with a corresponding error message. This list is empty when there are no errors Issues which aren't found or that the user doesn't have permission to view won't be returned in this list. */
    readonly issueErrors?: Array<{ readonly errorMessage?: string; readonly id?: string }>;
    /** The list of issues. */
    readonly issues?: Array<{ readonly changelog?: { readonly histories?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>; readonly maxResults?: number; readonly startAt?: number; readonly total?: number }; readonly editmeta?: { readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }> }; readonly expand?: string; fields?: Record<string, Record<string, unknown>>; fieldsToInclude?: { actuallyIncluded?: Array<string>; excluded?: Array<string>; included?: Array<string> }; readonly id?: string; readonly key?: string; readonly names?: Record<string, string>; readonly operations?: Record<string, unknown>; readonly properties?: Record<string, Record<string, unknown>>; readonly renderedFields?: Record<string, Record<string, unknown>>; readonly schema?: Record<string, unknown>; readonly self?: string; readonly transitions?: Array<Record<string, unknown>>; readonly versionedRepresentations?: Record<string, Record<string, Record<string, unknown>>> }>;

export interface BulkOperationErrorResponse {
    errors?: Array<{ message?: string }>;
}

export interface BulkOperationErrorResult {
    /** Error messages from an operation. */
    elementErrors?: { errorMessages?: Array<string>; errors?: Record<string, string>; status?: number };
    failedElementNumber?: number;
    status?: number;
}

export interface BulkOperationProgress {
    /** A timestamp of when the task was submitted. */
    created?: string;
    /** Map of issue IDs for which the operation failed and that the user has permission to view, to their one or more reasons for failure. These reasons are open-ended text descriptions of the error and are not selected from a predefined list of standard reasons. */
    failedAccessibleIssues?: Record<string, Array<string>>;
    /** The number of issues that are either invalid or issues that the user doesn't have permission to view, regardless of the success or failure of the operation. */
    invalidOrInaccessibleIssueCount?: number;
    /** List of issue IDs for which the operation was successful and that the user has permission to view. */
    processedAccessibleIssues?: Array<number>;
    /** Progress of the task as a percentage. */
    progressPercent?: number;
    /** A timestamp of when the task was started. */
    started?: string;
    /** The status of the task. */
    status?: "ENQUEUED" | "RUNNING" | "COMPLETE" | "FAILED" | "CANCEL_REQUESTED" | "CANCELLED" | "DEAD";
    /**
     * A user with details as permitted by the user's Atlassian Account privacy settings. However, be aware of these exceptions:
     *
     *  *  User record deleted from Atlassian: This occurs as the result of a right to be forgotten request. In this case, `displayName` provides an indication and other parameters have default values or are blank (for example, email is blank).
     *  *  User record corrupted: This occurs as a results of events such as a server import and can only happen to deleted users. In this case, `accountId` returns *unknown* and all other parameters have fallback values.
     *  *  User record unavailable: This usually occurs due to an internal service outage. In this case, all parameters have fallback values.
     */
    submittedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string };
    /** The ID of the task. */
    readonly taskId?: string;
    /** The number of issues that the bulk operation was attempted on. */
    totalIssueCount?: number;
    /** A timestamp of when the task progress was last updated. */
    updated?: string;

/** Details of global and project permissions granted to the user. */
export interface BulkPermissionGrants {
    /** List of permissions granted to the user. */
    globalPermissions: Array<string>;
    /** List of project permissions and the projects and issues those permissions provide access to. */
    projectPermissions: Array<{ issues: Array<number>; permission: string; projects: Array<number> }>;
}

/** Details of global permissions to look up and project permissions with associated projects and issues to look up. */
export interface BulkPermissionsRequestBean {
    /** The account ID of a user. */
    accountId?: string;
    /** Global permissions to look up. */
    globalPermissions?: Array<string>;
    /** Project permissions with associated projects and issues to look up. */
    projectPermissions?: Array<{ issues?: Array<number>; permissions: Array<string>; projects?: Array<number> }>;
}

/** List of project permissions and the projects and issues those permissions grant access to. */
export interface BulkProjectPermissionGrants {
    /** IDs of the issues the user has the permission for. */
    issues: Array<number>;
    /** A project permission, */
    permission: string;
    /** IDs of the projects the user has the permission for. */
    projects: Array<number>;
}

/** Details of project permissions and associated issues and projects to look up. */
export interface BulkProjectPermissions {
    /** List of issue IDs. */
    issues?: Array<number>;
    /** List of project permissions. */
    permissions: Array<string>;
    /** List of project IDs. */
    projects?: Array<number>;
}

export interface BulkRedactionRequest {
    redactions?: Array<{ contentItem: { entityId: string; entityType: "issuefieldvalue" | "issue-comment" | "issue-worklog"; id: string }; externalId: string; reason: string; redactionPosition: { adfPointer?: string; expectedText: string; from: number; to: number } }>;
}

export interface BulkRedactionResponse {
    /** Result for requested redactions */
    results: Array<{ externalId: string; successful: boolean }>;
}

/** Bulk Transition Get Available Transitions Response. */
export interface BulkTransitionGetAvailableTransitions {
    /** List of available transitions for bulk transition operation for requested issues grouped by workflow */
    readonly availableTransitions?: Array<{ readonly isTransitionsFiltered?: boolean; readonly issues?: Array<string>; readonly transitions?: Array<{ readonly to?: { readonly statusId?: number; readonly statusName?: string }; readonly transitionId?: number; readonly transitionName?: string }> }>;
    /** The end cursor for use in pagination. */
    readonly endingBefore?: string;
    /** The start cursor for use in pagination. */
    readonly startingAfter?: string;
}

export interface BulkTransitionSubmitInput {
    /** List of all the issue IDs or keys that are to be bulk transitioned. */
    selectedIssueIdsOrKeys: Array<string>;
    /** The ID of the transition that is to be performed on the issues. */
    transitionId: string;
}

export interface BulkWorklogKeyRequestBean {
    /** A list of issue and worklog ID pairs. */
    requests?: Array<{ issueId?: number; worklogId?: number }>;
}

export interface BulkWorklogKeyResponseBean {
    /** A list of successfully retrieved worklogs with their issue and worklog IDs. */
    worklogs?: Array<{ issueId?: number; worklogId?: number }>;
}

/** Card layout configuration. */
export interface CardLayout {
    /** Whether to show days in column */
    showDaysInColumn?: boolean;
}

/** Card layout settings of the board */
export interface CardLayoutField {
    fieldId?: string;
    id?: number;
    mode?: "PLAN" | "WORK";
    position?: number;
}

/** A change item. */
export interface ChangeDetails {
    /** The name of the field changed. */
    readonly field?: string;
    /** The ID of the field changed. */
    readonly fieldId?: string;
    /** The type of the field changed. */
    readonly fieldtype?: string;
    /** The details of the original value. */
    readonly from?: string;
    /** The details of the original value as a string. */
    readonly fromString?: string;
    /** The details of the new value. */
    readonly to?: string;
    /** The details of the new value as a string. */
    readonly toString?: string;
}

/** The account ID of the new owner. */
export interface ChangeFilterOwner {
    /** The account ID of the new owner. */
    accountId: string;
}

/** Details of names changed in the record event. */
export interface ChangedValueBean {
    /** The value of the field before the change. */
    readonly changedFrom?: string;
    /** The value of the field after the change. */
    readonly changedTo?: string;
    /** The name of the field changed. */
    readonly fieldName?: string;
}

/** Details of a changed worklog. */
export interface ChangedWorklog {
    /** Details of properties associated with the change. */
    readonly properties?: Array<{ key?: string; value?: Record<string, unknown> }>;
    /** The datetime of the change. */
    readonly updatedTime?: number;
    /** The ID of the worklog. */
    readonly worklogId?: number;
}

/** List of changed worklogs. */
export interface ChangedWorklogs {
    lastPage?: boolean;
    /** The URL of the next list of changed worklogs. */
    readonly nextPage?: string;
    /** The URL of this changed worklogs list. */
    readonly self?: string;
    /** The datetime of the first worklog item in the list. */
    readonly since?: number;
    /** The datetime of the last worklog item in the list. */
    readonly until?: number;
    /** Changed worklog list. */
    readonly values?: Array<{ readonly properties?: Array<{ key?: string; value?: Record<string, unknown> }>; readonly updatedTime?: number; readonly worklogId?: number }>;
}

/** A log of changes made to issue fields. Changelogs related to workflow associations are currently being deprecated. */
export interface Changelog {
    /** The user who made the change. */
    readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string };
    /** The date on which the change took place. */
    readonly created?: string;
    /** The history metadata associated with the changed. */
    readonly historyMetadata?: Record<string, unknown>;
    /** The ID of the changelog. */
    readonly id?: string;
    /** The list of items changed. */
    readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }>;

/** Details of an issue navigator column item. */
export interface ColumnItem {
    /** The issue navigator column label. */
    label?: string;
    /** The issue navigator column value. */
    value?: string;
}

export interface ColumnRequestBody {
    columns?: Array<string>;
}

/** A comment. */
export type Comment = Record<string, unknown>;

/** Count of issues assigned to a component. */
export interface ComponentIssuesCount {
    /** The count of issues assigned to a component. */
    readonly issueCount?: number;
    /** The URL for this count of issues for a component. */
    readonly self?: string;
}

export type ComponentJsonBean = Record<string, unknown>;

/** Details about a component with a count of the issues it contains. */
export interface ComponentWithIssueCount {
    /** The details of the user associated with `assigneeType`, if any. See `realAssignee` for details of the user assigned to issues created with this component. */
    assignee?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string };
    /**
     * The nominal user type used to determine the assignee for issues created with this component. See `realAssigneeType` for details on how the type of the user, and hence the user, assigned to issues is determined. Takes the following values:
     *
     *  *  `PROJECT_LEAD` the assignee to any issues created with this component is nominally the lead for the project the component is in.
     *  *  `COMPONENT_LEAD` the assignee to any issues created with this component is nominally the lead for the component.
     *  *  `UNASSIGNED` an assignee is not set for issues created with this component.
     *  *  `PROJECT_DEFAULT` the assignee to any issues created with this component is nominally the default assignee for the project that the component is in.
     */
    readonly assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED";
    /** The description for the component. */
    readonly description?: string;
    /** The unique identifier for the component. */
    readonly id?: string;
    /** Whether a user is associated with `assigneeType`. For example, if the `assigneeType` is set to `COMPONENT_LEAD` but the component lead is not set, then `false` is returned. */
    readonly isAssigneeTypeValid?: boolean;
    /** Count of issues for the component. */
    readonly issueCount?: number;
    /** The user details for the component's lead user. */
    lead?: unknown;
    /** The name for the component. */
    readonly name?: string;
    /** The key of the project to which the component is assigned. */
    readonly project?: string;
    /** Not used. */
    readonly projectId?: number;
    /** The user assigned to issues created with this component, when `assigneeType` does not identify a valid assignee. */
    realAssignee?: unknown;
    /**
     * The type of the assignee that is assigned to issues created with this component, when an assignee cannot be set from the `assigneeType`. For example, `assigneeType` is set to `COMPONENT_LEAD` but no component lead is set. This property is set to one of the following values:
     *
     *  *  `PROJECT_LEAD` when `assigneeType` is `PROJECT_LEAD` and the project lead has permission to be assigned issues in the project that the component is in.
     *  *  `COMPONENT_LEAD` when `assignee`Type is `COMPONENT_LEAD` and the component lead has permission to be assigned issues in the project that the component is in.
     *  *  `UNASSIGNED` when `assigneeType` is `UNASSIGNED` and Jira is configured to allow unassigned issues.
     *  *  `PROJECT_DEFAULT` when none of the preceding cases are true.
     */
    readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED";
    /** The URL for this count of the issues contained in the component. */
    readonly self?: string;

/** A JQL query clause that consists of nested clauses. For example, `(labels in (urgent, blocker) OR lastCommentedBy = currentUser()). Note that, where nesting is not defined, the parser nests JQL clauses based on the operator precedence. For example, "A OR B AND C" is parsed as "(A OR B) AND C". See Setting the precedence of operators for more information about precedence in JQL queries.` */
export interface CompoundClause {
    /** The list of nested clauses. */
    clauses: Array<unknown | { field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> }; operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown; operator: "=" | "!=" | ">" | "<" | ">=" | "<=" | "in" | "not in" | "~" | "~=" | "is" | "is not" } | { field: unknown; operand: unknown; operator: "was" | "was in" | "was not in" | "was not"; predicates: Array<{ operand: unknown; operator: "before" | "after" | "from" | "to" | "on" | "during" | "by" }> } | { field: unknown; operator: "changed"; predicates: Array<unknown> }>;
    /** The operator between the clauses. */
    operator: "and" | "or" | "not";
}

/** The conditions group associated with the transition. */
export interface ConditionGroupConfiguration {
    /** The nested conditions of the condition group. */
    conditionGroups?: Array<unknown>;
    /** The rules for this condition. */
    conditions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>;
    /** Determines how the conditions in the group are evaluated. Accepts either `ANY` or `ALL`. If `ANY` is used, at least one condition in the group must be true for the group to evaluate to true. If `ALL` is used, all conditions in the group must be true for the group to evaluate to true. */
    operation?: "ANY" | "ALL";
}

/** The payload for creating a condition group in a workflow */
export interface ConditionGroupPayload {
    /** The nested conditions of the condition group. */
    conditionGroup?: Array<unknown>;
    /** The rules for this condition. */
    conditions?: Array<{ parameters?: Record<string, string>; ruleKey?: string }>;
    /** Determines how the conditions in the group are evaluated. Accepts either `ANY` or `ALL`. If `ANY` is used, at least one condition in the group must be true for the group to evaluate to true. If `ALL` is used, all conditions in the group must be true for the group to evaluate to true. */
    operation?: "ANY" | "ALL";
}

/** The conditions group associated with the transition. */
export interface ConditionGroupUpdate {
    /** The nested conditions of the condition group. */
    conditionGroups?: Array<unknown>;
    /** The rules for this condition. */
    conditions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>;
    /** Determines how the conditions in the group are evaluated. Accepts either `ANY` or `ALL`. If `ANY` is used, at least one condition in the group must be true for the group to evaluate to true. If `ALL` is used, all conditions in the group must be true for the group to evaluate to true. */
    operation: "ANY" | "ALL";
}

/** Details about the configuration of Jira. */
export interface Configuration {
    /** Whether the ability to add attachments to issues is enabled. */
    readonly attachmentsEnabled?: boolean;
    /** Whether the ability to link issues is enabled. */
    readonly issueLinkingEnabled?: boolean;
    /** Whether the ability to create subtasks for issues is enabled. */
    readonly subTasksEnabled?: boolean;
    /** The configuration of time tracking. */
    readonly timeTrackingConfiguration?: { defaultUnit: "minute" | "hour" | "day" | "week"; timeFormat: "pretty" | "days" | "hours"; workingDaysPerWeek: number; workingHoursPerDay: number };
    /** Whether the ability to track time is enabled. This property is deprecated. */
    readonly timeTrackingEnabled?: boolean;
    /** Whether the ability to create unassigned issues is enabled. See [Configuring Jira application options](https://confluence.atlassian.com/x/uYXKM) for details. */
    readonly unassignedIssuesAllowed?: boolean;
    /** Whether the ability for users to vote on issues is enabled. See [Configuring Jira application options](https://confluence.atlassian.com/x/uYXKM) for details. */
    readonly votingEnabled?: boolean;
    /** Whether the ability for users to watch issues is enabled. See [Configuring Jira application options](https://confluence.atlassian.com/x/uYXKM) for details. */
    readonly watchingEnabled?: boolean;
}

/** List of custom fields identifiers which will be used to filter configurations */
export interface ConfigurationsListParameters {
    /** List of IDs or keys of the custom fields. It can be a mix of IDs and keys in the same query. */
    fieldIdsOrKeys: Array<string>;
}

/** A list of custom field details. */
export interface ConnectCustomFieldValue {
    /** The type of custom field. */
    _type: "StringIssueField" | "NumberIssueField" | "RichTextIssueField" | "SingleSelectIssueField" | "MultiSelectIssueField" | "TextIssueField";
    /** The custom field ID. */
    fieldID: number;
    /** The issue ID. */
    issueID: number;
    /** The value of number type custom field when `_type` is `NumberIssueField`. */
    number?: number;
    /** The value of single select and multiselect custom field type when `_type` is `SingleSelectIssueField` or `MultiSelectIssueField`. */
    optionID?: string;
    /** The value of richText type custom field when `_type` is `RichTextIssueField`. */
    richText?: string;
    /** The value of string type custom field when `_type` is `StringIssueField`. */
    string?: string;
    /** The value of of text custom field type when `_type` is `TextIssueField`. */
    text?: string;
}

/** Details of updates for a custom field. */
export interface ConnectCustomFieldValues {
    /** The list of custom field update details. */
    updateValueList?: Array<{ _type: "StringIssueField" | "NumberIssueField" | "RichTextIssueField" | "SingleSelectIssueField" | "MultiSelectIssueField" | "TextIssueField"; fieldID: number; issueID: number; number?: number; optionID?: string; richText?: string; string?: string; text?: string }>;
}

/**
 * A [Connect module](https://developer.atlassian.com/cloud/jira/platform/about-jira-modules/) in the same format as in the
 * [app descriptor](https://developer.atlassian.com/cloud/jira/platform/app-descriptor/).
 */
export interface ConnectModule {
}

export interface ConnectModules {
    /**
     * A list of app modules in the same format as the `modules` property in the
     * [app descriptor](https://developer.atlassian.com/cloud/jira/platform/app-descriptor/).
     */
    modules: Array<Record<string, unknown>>;
}

/** A workflow transition rule. */
export interface ConnectWorkflowTransitionRule {
    /** A rule configuration. */
    configuration: { disabled?: boolean; tag?: string; value: string };
    /** The ID of the transition rule. */
    id: string;
    /** The key of the rule, as defined in the Connect app descriptor. */
    key: string;
    /** A workflow transition. */
    transition?: { id: number; name: string };
}

/** The list of features on a project. */
export interface ContainerForProjectFeatures {
    /** The project features. */
    features?: Array<{ feature?: string; imageUri?: string; localisedDescription?: string; localisedName?: string; prerequisites?: Array<string>; projectId?: number; state?: "ENABLED" | "DISABLED" | "COMING_SOON"; toggleLocked?: boolean }>;
}

/** Container for a list of registered webhooks. Webhook details are returned in the same order as the request. */
export interface ContainerForRegisteredWebhooks {
    /** A list of registered webhooks. */
    webhookRegistrationResult?: Array<{ createdWebhookId?: number; errors?: Array<string> }>;
}

/** Container for a list of webhook IDs. */
export interface ContainerForWebhookIDs {
    /** A list of webhook IDs. */
    webhookIds: Array<number>;
}

/** A container for a list of workflow schemes together with the projects they are associated with. */
export interface ContainerOfWorkflowSchemeAssociations {
    /** A list of workflow schemes together with projects they are associated with. */
    values: Array<{ projectIds: Array<string>; workflowScheme: { defaultWorkflow?: string; description?: string; readonly draft?: boolean; readonly id?: number; issueTypeMappings?: Record<string, string>; readonly issueTypes?: Record<string, { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly lastModified?: string; readonly lastModifiedUser?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; name?: string; readonly originalDefaultWorkflow?: string; readonly originalIssueTypeMappings?: Record<string, string>; readonly self?: string; updateDraftIfNeeded?: boolean } }>;

/** Represents the content to redact */
export interface ContentItem {
    /**
     * The ID of the content entity.
     *
     *  *  For redacting an issue field, this will be the field ID (e.g., summary, customfield\_10000).
     *  *  For redacting a comment, this will be the comment ID.
     *  *  For redacting a worklog, this will be the worklog ID.
     */
    entityId: string;
    /**
     * The type of the entity to redact; It will be one of the following:
     *
     *  *  **issuefieldvalue** \- To redact in issue fields
     *  *  **issue-comment** \- To redact in issue comments.
     *  *  **issue-worklog** \- To redact in issue worklogs
     */
    entityType: "issuefieldvalue" | "issue-comment" | "issue-worklog";
    /** This would be the issue ID */
    id: string;
}

/** A context. */
export interface Context {
    /** The ID of the context. */
    readonly id?: number;
    /** The name of the context. */
    readonly name?: string;
    /** The scope of the context. */
    scope?: Record<string, unknown>;
}

/** The project and issue type mapping with a matching custom field context. */
export interface ContextForProjectAndIssueType {
    /** The ID of the custom field context. */
    contextId: string;
    /** The ID of the issue type. */
    issueTypeId: string;
    /** The ID of the project. */
    projectId: string;
}

/** Details of the contextual configuration for a custom field. */
export interface ContextualConfiguration {
    /** The field configuration. */
    configuration?: Record<string, unknown>;
    /** The ID of the field context the configuration is associated with. */
    readonly fieldContextId: string;
    /** The ID of the configuration. */
    id: string;
    /** The field value schema. */
    schema?: Record<string, unknown>;
}

/** The converted JQL queries. */
export interface ConvertedJQLQueries {
    /** List of queries containing user information that could not be mapped to an existing user */
    queriesWithUnknownUsers?: Array<{ convertedQuery?: string; originalQuery?: string }>;
    /** The list of converted query strings with account IDs in place of user identifiers. */
    queryStrings?: Array<string>;
}

export interface CreateCrossProjectReleaseRequest {
    /** The cross-project release name. */
    name: string;
    /** The IDs of the releases to include in the cross-project release. */
    releaseIds?: Array<number>;
}

/** The details of a created custom field context. */
export interface CreateCustomFieldContext {
    /** The description of the context. */
    description?: string;
    /** The ID of the context. */
    readonly id?: string;
    /** The list of issue types IDs for the context. If the list is empty, the context refers to all issue types. */
    issueTypeIds?: Array<string>;
    /** The name of the context. */
    name: string;
    /** The list of project IDs associated with the context. If the list is empty, the context is global. */
    projectIds?: Array<string>;
}

export interface CreateCustomFieldRequest {
    /** The custom field ID. */
    customFieldId: number;
    /** Allows filtering issues based on their values for the custom field. */
    filter?: boolean;
}

export interface CreateDateFieldRequest {
    /** A date custom field ID. This is required if the type is "DateCustomField". */
    dateCustomFieldId?: number;
    /** The date field type. This must be "DueDate", "TargetStartDate", "TargetEndDate" or "DateCustomField". */
    type: "DueDate" | "TargetStartDate" | "TargetEndDate" | "DateCustomField";
}

export interface CreateExclusionRulesRequest {
    /** The IDs of the issues to exclude from the plan. */
    issueIds?: Array<number>;
    /** The IDs of the issue types to exclude from the plan. */
    issueTypeIds?: Array<number>;
    /** Issues completed this number of days ago will be excluded from the plan. */
    numberOfDaysToShowCompletedIssues?: number;
    /** The IDs of the releases to exclude from the plan. */
    releaseIds?: Array<number>;
    /** The IDs of the work status categories to exclude from the plan. */
    workStatusCategoryIds?: Array<number>;
    /** The IDs of the work statuses to exclude from the plan. */
    workStatusIds?: Array<number>;
}

export interface CreateFieldAssociationSchemeLinksBean {
    associations?: string;
    projects?: string;
}

/** Request object for creating a new field association scheme. */
export interface CreateFieldAssociationSchemeRequest {
    /** Description of the scheme to be created */
    description?: string;
    /** The name of the scheme to be created */
    name: string;
}

/** Response object after successfully creating a new field association scheme. */
export interface CreateFieldAssociationSchemeResponse {
    description?: string;
    id?: number;
    links?: { associations?: string; projects?: string };
    name?: string;
}

/** Issue security scheme and it's details */
export type CreateIssueSecuritySchemeDetails = Record<string, unknown>;

export interface CreateIssueSourceRequest {
    /** The issue source type. This must be "Board", "Project" or "Filter". */
    type: "Board" | "Project" | "Filter";
    /** The issue source value. This must be a board ID if the type is "Board", a project ID if the type is "Project" or a filter ID if the type is "Filter". */
    value: number;
}

/** Details of an notification scheme. */
export type CreateNotificationSchemeDetails = Record<string, unknown>;

export interface CreatePermissionHolderRequest {
    /** The permission holder type. This must be "Group" or "AccountId". */
    type: "Group" | "AccountId";
    /** The permission holder value. This must be a group name if the type is "Group" or an account ID if the type is "AccountId". */
    value: string;
}

export interface CreatePermissionRequest {
    /** The permission holder. */
    holder: { type: "Group" | "AccountId"; value: string };
    /** The permission type. This must be "View" or "Edit". */
    type: "View" | "Edit";
}

export interface CreatePlanOnlyTeamRequest {
    /** The capacity for the plan-only team. */
    capacity?: number;
    /** The ID of the issue source for the plan-only team. */
    issueSourceId?: number;
    /** The account IDs of the plan-only team members. */
    memberAccountIds?: Array<string>;
    /** The plan-only team name. */
    name: string;
    /** The planning style for the plan-only team. This must be "Scrum" or "Kanban". */
    planningStyle: "Scrum" | "Kanban";
    /** The sprint length for the plan-only team. */
    sprintLength?: number;
}

export interface CreatePlanRequest {
    /** The cross-project releases to include in the plan. */
    crossProjectReleases?: Array<{ name: string; releaseIds?: Array<number> }>;
    /** The custom fields for the plan. */
    customFields?: Array<{ customFieldId: number; filter?: boolean }>;
    /** The exclusion rules for the plan. */
    exclusionRules?: { issueIds?: Array<number>; issueTypeIds?: Array<number>; numberOfDaysToShowCompletedIssues?: number; releaseIds?: Array<number>; workStatusCategoryIds?: Array<number>; workStatusIds?: Array<number> };
    /** The issue sources to include in the plan. */
    issueSources: Array<{ type: "Board" | "Project" | "Filter"; value: number }>;
    /** The account ID of the plan lead. */
    leadAccountId?: string;
    /** The plan name. */
    name: string;
    /** The permissions for the plan. */
    permissions?: Array<{ holder: { type: "Group" | "AccountId"; value: string }; type: "View" | "Edit" }>;
    /** The scheduling settings for the plan. */
    scheduling: { dependencies?: "Sequential" | "Concurrent"; endDate?: { dateCustomFieldId?: number; type: "DueDate" | "TargetStartDate" | "TargetEndDate" | "DateCustomField" }; estimation: "StoryPoints" | "Days" | "Hours"; inferredDates?: "None" | "SprintDates" | "ReleaseDates"; startDate?: unknown };
}

/** Details of an issue priority. */
export type CreatePriorityDetails = Record<string, unknown>;

/** Details of a new priority scheme */
export interface CreatePrioritySchemeDetails {
    /** The ID of the default priority for the priority scheme. */
    defaultPriorityId: number;
    /** The description of the priority scheme. */
    description?: string;
    /**
     * Instructions to migrate the priorities of issues.
     *
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     *
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     *
     *  *  When **priorities** are **added** to the new priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the new priority scheme, no mapping needs to be provided as the removed priorities are not used by any issues.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, no mapping needs to be provided as the removed projects are not using the priorities of the new priority scheme.
     *
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     */
    mappings?: { in?: Record<string, number>; out?: Record<string, number> };
    /** The name of the priority scheme. Must be unique. */
    name: string;
    /** The IDs of priorities in the scheme. */
    priorityIds: Array<number>;
    /** The IDs of projects that will use the priority scheme. */
    projectIds?: Array<number>;
}

/** Details about the project. */
export interface CreateProjectDetails {
    /** The default assignee when creating issues for this project. */
    assigneeType?: "PROJECT_LEAD" | "UNASSIGNED";
    /** An integer value for the project's avatar. */
    avatarId?: number;
    /** The ID of the project's category. A complete list of category IDs is found using the [Get all project categories](#api-rest-api-3-projectCategory-get) operation. */
    categoryId?: number;
    /** A brief description of the project. */
    description?: string;
    /** Deprecated use [fieldScheme](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-field-schemes/#api-group-field-schemes) instead. The ID of the field configuration scheme for the project. Use the [Get all field configuration schemes](#api-rest-api-3-fieldconfigurationscheme-get) operation to get a list of field configuration scheme IDs. If you specify the field configuration scheme you cannot specify the project template key. */
    /** @deprecated */
    fieldConfigurationScheme?: number;
    /** The ID of the field scheme for the project. Use the [Get field schemes](#api-rest-api-3-config-fieldschemes-get) operation to get a list of field scheme IDs. If you specify the field scheme you cannot specify the project template key. */
    fieldScheme?: number;
    /** The ID of the issue security scheme for the project, which enables you to control who can and cannot view issues. Use the [Get issue security schemes](#api-rest-api-3-issuesecurityschemes-get) resource to get all issue security scheme IDs. */
    issueSecurityScheme?: number;
    /** The ID of the issue type scheme for the project. Use the [Get all issue type schemes](#api-rest-api-3-issuetypescheme-get) operation to get a list of issue type scheme IDs. If you specify the issue type scheme you cannot specify the project template key. */
    issueTypeScheme?: number;
    /** The ID of the issue type screen scheme for the project. Use the [Get all issue type screen schemes](#api-rest-api-3-issuetypescreenscheme-get) operation to get a list of issue type screen scheme IDs. If you specify the issue type screen scheme you cannot specify the project template key. */
    issueTypeScreenScheme?: number;
    /** Project keys must be unique and start with an uppercase letter followed by one or more uppercase alphanumeric characters. The maximum length is 10 characters. */
    key: string;
    /** This parameter is deprecated because of privacy changes. Use `leadAccountId` instead. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. The user name of the project lead. Either `lead` or `leadAccountId` must be set when creating a project. Cannot be provided with `leadAccountId`. */
    lead?: string;
    /** The account ID of the project lead. Either `lead` or `leadAccountId` must be set when creating a project. Cannot be provided with `lead`. */
    leadAccountId?: string;
    /** The name of the project. */
    name: string;
    /** The ID of the notification scheme for the project. Use the [Get notification schemes](#api-rest-api-3-notificationscheme-get) resource to get a list of notification scheme IDs. */
    notificationScheme?: number;
    /** The ID of the permission scheme for the project. Use the [Get all permission schemes](#api-rest-api-3-permissionscheme-get) resource to see a list of all permission scheme IDs. */
    permissionScheme?: number;
    /** A predefined configuration for a project. The type of the `projectTemplateKey` must match with the type of the `projectTypeKey`. */
    projectTemplateKey?: "com.pyxis.greenhopper.jira:gh-simplified-agility-kanban" | "com.pyxis.greenhopper.jira:gh-simplified-agility-scrum" | "com.pyxis.greenhopper.jira:gh-simplified-basic" | "com.pyxis.greenhopper.jira:gh-simplified-kanban-classic" | "com.pyxis.greenhopper.jira:gh-simplified-scrum-classic" | "com.pyxis.greenhopper.jira:gh-cross-team-template" | "com.pyxis.greenhopper.jira:gh-cross-team-planning-template" | "com.atlassian.servicedesk:simplified-it-service-management" | "com.atlassian.servicedesk:simplified-it-service-management-basic" | "com.atlassian.servicedesk:simplified-it-service-management-operations" | "com.atlassian.servicedesk:simplified-internal-service-desk" | "com.atlassian.servicedesk:simplified-external-service-desk" | "com.atlassian.servicedesk:simplified-hr-service-desk" | "com.atlassian.servicedesk:simplified-facilities-service-desk" | "com.atlassian.servicedesk:simplified-legal-service-desk" | "com.atlassian.servicedesk:simplified-marketing-service-desk" | "com.atlassian.servicedesk:simplified-finance-service-desk" | "com.atlassian.servicedesk:simplified-analytics-service-desk" | "com.atlassian.servicedesk:simplified-design-service-desk" | "com.atlassian.servicedesk:simplified-sales-service-desk" | "com.atlassian.servicedesk:simplified-halp-service-desk" | "com.atlassian.servicedesk:next-gen-it-service-desk" | "com.atlassian.servicedesk:next-gen-hr-service-desk" | "com.atlassian.servicedesk:next-gen-legal-service-desk" | "com.atlassian.servicedesk:next-gen-marketing-service-desk" | "com.atlassian.servicedesk:next-gen-facilities-service-desk" | "com.atlassian.servicedesk:next-gen-general-service-desk" | "com.atlassian.servicedesk:next-gen-analytics-service-desk" | "com.atlassian.servicedesk:next-gen-finance-service-desk" | "com.atlassian.servicedesk:next-gen-design-service-desk" | "com.atlassian.servicedesk:next-gen-sales-service-desk" | "com.atlassian.jira-core-project-templates:jira-core-simplified-content-management" | "com.atlassian.jira-core-project-templates:jira-core-simplified-document-approval" | "com.atlassian.jira-core-project-templates:jira-core-simplified-lead-tracking" | "com.atlassian.jira-core-project-templates:jira-core-simplified-process-control" | "com.atlassian.jira-core-project-templates:jira-core-simplified-procurement" | "com.atlassian.jira-core-project-templates:jira-core-simplified-project-management" | "com.atlassian.jira-core-project-templates:jira-core-simplified-recruitment" | "com.atlassian.jira-core-project-templates:jira-core-simplified-task-" | "com.atlassian.jcs:customer-service-management";
    /** The [project type](https://confluence.atlassian.com/x/GwiiLQ#Jiraapplicationsoverview-Productfeaturesandprojecttypes), which defines the application-specific feature set. If you don't specify the project template you have to specify the project type. */
    projectTypeKey?: "software" | "service_desk" | "business";
    /** A link to information about this project, such as project documentation */
    url?: string;
    /** The ID of the workflow scheme for the project. Use the [Get all workflow schemes](#api-rest-api-3-workflowscheme-get) operation to get a list of workflow scheme IDs. If you specify the workflow scheme you cannot specify the project template key. */
    workflowScheme?: number;
}

/** Details of an issue resolution. */
export type CreateResolutionDetails = Record<string, unknown>;

export interface CreateSchedulingRequest {
    /** The dependencies for the plan. This must be "Sequential" or "Concurrent". */
    dependencies?: "Sequential" | "Concurrent";
    /** The end date field for the plan. */
    endDate?: { dateCustomFieldId?: number; type: "DueDate" | "TargetStartDate" | "TargetEndDate" | "DateCustomField" };
    /** The estimation unit for the plan. This must be "StoryPoints", "Days" or "Hours". */
    estimation: "StoryPoints" | "Days" | "Hours";
    /** The inferred dates for the plan. This must be "None", "SprintDates" or "ReleaseDates". */
    inferredDates?: "None" | "SprintDates" | "ReleaseDates";
    /** The start date field for the plan. */
    startDate?: unknown;
}

/** The details of a UI modification. */
export interface CreateUiModificationDetails {
    /** List of contexts of the UI modification. The maximum number of contexts is 1000. */
    contexts?: Array<{ readonly id?: string; readonly isAvailable?: boolean; issueTypeId?: string; portalId?: string; projectId?: string; requestTypeId?: string; viewType?: "GIC" | "IssueView" | "IssueTransition" | "JSMRequestCreate" }>;
    /** The data of the UI modification. The maximum size of the data is 50000 characters. */
    data?: string;
    /** The description of the UI modification. The maximum length is 255 characters. */
    description?: string;
    /** The name of the UI modification. The maximum length is 255 characters. */
    name: string;
}

export interface CreateUpdateRoleRequestBean {
    /** A description of the project role. Required when fully updating a project role. Optional when creating or partially updating a project role. */
    description?: string;
    /** The name of the project role. Must be unique. Cannot begin or end with whitespace. The maximum length is 255 characters. Required when creating a project role. Optional when partially updating a project role. */
    name?: string;
}

/** A workflow transition condition. */
export interface CreateWorkflowCondition {
    /** The list of workflow conditions. */
    conditions?: Array<unknown>;
    /** EXPERIMENTAL. The configuration of the transition rule. */
    configuration?: Record<string, Record<string, unknown>>;
    /** The compound condition operator. */
    operator?: "AND" | "OR";
    /** The type of the transition rule. */
    type?: string;
}

/** The details of a workflow. */
export interface CreateWorkflowDetails {
    /** The description of the workflow. The maximum length is 1000 characters. */
    description?: string;
    /** The name of the workflow. The name must be unique. The maximum length is 255 characters. Characters can be separated by a whitespace but the name cannot start or end with a whitespace. */
    name: string;
    /** The statuses of the workflow. Any status that does not include a transition is added to the workflow without a transition. */
    statuses: Array<{ id: string; properties?: Record<string, string> }>;
    /**
     * The transitions of the workflow. For the request to be valid, these transitions must:
     *
     *  *  include one *initial* transition.
     *  *  not use the same name for a *global* and *directed* transition.
     *  *  have a unique name for each *global* transition.
     *  *  have a unique 'to' status for each *global* transition.
     *  *  have unique names for each transition from a status.
     *  *  not have a 'from' status on *initial* and *global* transitions.
     *  *  have a 'from' status on *directed* transitions.
     *
     * All the transition statuses must be included in `statuses`.
     */
    transitions: Array<{ description?: string; from?: Array<string>; name: string; properties?: Record<string, string>; rules?: { conditions?: { conditions?: Array<unknown>; configuration?: Record<string, Record<string, unknown>>; operator?: "AND" | "OR"; type?: string }; postFunctions?: Array<{ configuration?: Record<string, Record<string, unknown>>; type: string }>; validators?: Array<unknown> }; screen?: { id: string }; to: string; type: "global" | "initial" | "directed" }>;
}

/** The details of a transition status. */
export interface CreateWorkflowStatusDetails {
    /** The ID of the status. */
    id: string;
    /** The properties of the status. */
    properties?: Record<string, string>;
}

/** The details of a workflow transition. */
export interface CreateWorkflowTransitionDetails {
    /** The description of the transition. The maximum length is 1000 characters. */
    description?: string;
    /** The statuses the transition can start from. */
    from?: Array<string>;
    /** The name of the transition. The maximum length is 60 characters. */
    name: string;
    /** The properties of the transition. */
    properties?: Record<string, string>;
    /** The rules of the transition. */
    rules?: { conditions?: { conditions?: Array<unknown>; configuration?: Record<string, Record<string, unknown>>; operator?: "AND" | "OR"; type?: string }; postFunctions?: Array<{ configuration?: Record<string, Record<string, unknown>>; type: string }>; validators?: Array<unknown> };
    /** The screen of the transition. */
    screen?: { id: string };
    /** The status the transition goes to. */
    to: string;
    /** The type of the transition. */
    type: "global" | "initial" | "directed";
}

/** A workflow transition rule. */
export interface CreateWorkflowTransitionRule {
    /** EXPERIMENTAL. The configuration of the transition rule. */
    configuration?: Record<string, Record<string, unknown>>;
    /** The type of the transition rule. */
    type: string;
}

/** The details of a workflow transition rules. */
export interface CreateWorkflowTransitionRulesDetails {
    /** The workflow conditions. */
    conditions?: { conditions?: Array<unknown>; configuration?: Record<string, Record<string, unknown>>; operator?: "AND" | "OR"; type?: string };
    /**
     * The workflow post functions.
     *
     * **Note:** The default post functions are always added to the *initial* transition, as in:
     *
     *     "postFunctions": [
     *         {
     *             "type": "IssueCreateFunction"
     *         },
     *         {
     *             "type": "IssueReindexFunction"
     *         },
     *         {
     *             "type": "FireIssueEventFunction",
     *             "configuration": {
     *                 "event": {
     *                     "id": "1",
     *                     "name": "issue_created"
     *                 }
     *             }
     *         }
     *     ]
     *
     * **Note:** The default post functions are always added to the *global* and *directed* transitions, as in:
     *
     *     "postFunctions": [
     *         {
     *             "type": "UpdateIssueStatusFunction"
     *         },
     *         {
     *             "type": "CreateCommentFunction"
     *         },
     *         {
     *             "type": "GenerateChangeHistoryFunction"
     *         },
     *         {
     *             "type": "IssueReindexFunction"
     *         },
     *         {
     *             "type": "FireIssueEventFunction",
     *             "configuration": {
     *                 "event": {
     *                     "id": "13",
     *                     "name": "issue_generic"
     *                 }
     *             }
     *         }
     *     ]
     */
    postFunctions?: Array<{ configuration?: Record<string, Record<string, unknown>>; type: string }>;
    /**
     * The workflow validators.
     *
     * **Note:** The default permission validator is always added to the *initial* transition, as in:
     *
     *     "validators": [
     *         {
     *             "type": "PermissionValidator",
     *             "configuration": {
     *                 "permissionKey": "CREATE_ISSUES"
     *             }
     *         }
     *     ]
     */
    validators?: Array<unknown>;
}

/** The details of a transition screen. */
export interface CreateWorkflowTransitionScreenDetails {
    /** The ID of the screen. */
    id: string;
}

/** Details about a created issue or subtask. */
export interface CreatedIssue {
    /** The ID of the created issue or subtask. */
    readonly id?: string;
    /** The key of the created issue or subtask. */
    readonly key?: string;
    /** The URL of the created issue or subtask. */
    readonly self?: string;
    /** The response code and messages related to any requested transition. */
    readonly transition?: { errorCollection?: { errorMessages?: Array<string>; errors?: Record<string, string>; status?: number }; status?: number; warningCollection?: { warnings?: Array<string> } };
    /** The response code and messages related to any requested watchers. */
    readonly watchers?: unknown;
}

/** Details about the issues created and the errors for requests that failed. */
export interface CreatedIssues {
    /** Error details for failed issue creation requests. */
    readonly errors?: Array<{ elementErrors?: { errorMessages?: Array<string>; errors?: Record<string, string>; status?: number }; failedElementNumber?: number; status?: number }>;
    /** Details of the issues created. */
    readonly issues?: Array<{ readonly id?: string; readonly key?: string; readonly self?: string; readonly transition?: { errorCollection?: unknown; status?: number; warningCollection?: { warnings?: Array<string> } }; readonly watchers?: unknown }>;
}

export type CustomContextVariable = { accountId: string; type: string } | { id?: number; key?: string; type: string } | { type: string; value?: Record<string, unknown> };

/** Details of configurations for a custom field. */
export interface CustomFieldConfigurations {
    /** The list of custom field configuration details. */
    configurations: Array<{ configuration?: Record<string, unknown>; readonly fieldContextId: string; id: string; schema?: Record<string, unknown> }>;
}

/** The details of a custom field context. */
export interface CustomFieldContext {
    /** The description of the context. */
    description: string;
    /** The ID of the context. */
    id: string;
    /** Whether the context apply to all issue types. */
    isAnyIssueType: boolean;
    /** Whether the context is global. */
    isGlobalContext: boolean;
    /** The name of the context. */
    name: string;
}

export type CustomFieldContextDefaultValue = { cascadingOptionId?: string; contextId: string; optionId: string; type: string } | { contextId: string; optionIds: Array<string>; type: string } | { contextId: string; optionId: string; type: string } | { accountId: string; contextId: string; type: string; userFilter: { enabled: boolean; groups?: Array<string>; roleIds?: Array<number> } } | { accountIds: Array<string>; contextId: string; type: string } | { contextId: string; groupId: string; type: string } | { contextId: string; groupIds: Array<string>; type: string } | { date?: string; type: string; useCurrent?: boolean } | { dateTime?: string; type: string; useCurrent?: boolean } | { contextId: string; type: string; url: string } | { contextId: string; projectId: string; type: string } | { number: number; type: string } | { labels: Array<string>; type: string } | { text?: string; type: string } | { text?: string; type: string } | { text?: string; type: string } | { type: string; versionId: string; versionOrder?: string } | { type: string; versionIds: Array<string>; versionOrder?: string } | { contextId: string; text?: string; type: string } | { type: string; values?: Array<string> } | { object?: Record<string, unknown>; type: string } | { contextId: string; dateTime?: string; type: string; useCurrent?: boolean } | { contextId: string; groupId: string; type: string } | { contextId: string; groupIds: Array<string>; type: string } | { contextId: string; number: number; type: string } | { accountId: string; contextId: string; type: string; userFilter: unknown } | { accountIds: Array<string>; contextId: string; type: string };

/** The default value for a cascading select custom field. */
export interface CustomFieldContextDefaultValueCascadingOption {
    /** The ID of the default cascading option. */
    cascadingOptionId?: string;
    /** The ID of the context. */
    contextId: string;
    /** The ID of the default option. */
    optionId: string;
    type: string;
}

/** The default value for a Date custom field. */
export interface CustomFieldContextDefaultValueDate {
    /** The default date in ISO format. Ignored if `useCurrent` is true. */
    date?: string;
    type: string;
    /** Whether to use the current date. */
    useCurrent?: boolean;
}

/** The default value for a date time custom field. */
export interface CustomFieldContextDefaultValueDateTime {
    /** The default date-time in ISO format. Ignored if `useCurrent` is true. */
    dateTime?: string;
    type: string;
    /** Whether to use the current date. */
    useCurrent?: boolean;
}

/** Default value for a float (number) custom field. */
export interface CustomFieldContextDefaultValueFloat {
    /** The default floating-point number. */
    number: number;
    type: string;
}

/** The default value for a Forge date time custom field. */
export interface CustomFieldContextDefaultValueForgeDateTimeField {
    /** The ID of the context. */
    contextId: string;
    /** The default date-time in ISO format. Ignored if `useCurrent` is true. */
    dateTime?: string;
    type: string;
    /** Whether to use the current date. */
    useCurrent?: boolean;
}

/** The default value for a Forge group custom field. */
export interface CustomFieldContextDefaultValueForgeGroupField {
    /** The ID of the context. */
    contextId: string;
    /** The ID of the the default group. */
    groupId: string;
    type: string;
}

/** The default value for a Forge collection of groups custom field. */
export interface CustomFieldContextDefaultValueForgeMultiGroupField {
    /** The ID of the context. */
    contextId: string;
    /** The IDs of the default groups. */
    groupIds: Array<string>;
    type: string;
}

/** The default text for a Forge collection of strings custom field. */
export interface CustomFieldContextDefaultValueForgeMultiStringField {
    type: string;
    /** List of string values. The maximum length for a value is 254 characters. */
    values?: Array<string>;
}

/** Defaults for a Forge collection of users custom field. */
export interface CustomFieldContextDefaultValueForgeMultiUserField {
    /** The IDs of the default users. */
    accountIds: Array<string>;
    /** The ID of the context. */
    contextId: string;
    type: string;
}

/** Default value for a Forge number custom field. */
export interface CustomFieldContextDefaultValueForgeNumberField {
    /** The ID of the context. */
    contextId: string;
    /** The default floating-point number. */
    number: number;
    type: string;
}

/** The default value for a Forge object custom field. */
export interface CustomFieldContextDefaultValueForgeObjectField {
    /** The default JSON object. */
    object?: Record<string, unknown>;
    type: string;
}

/** The default text for a Forge string custom field. */
export interface CustomFieldContextDefaultValueForgeStringField {
    /** The ID of the context. */
    contextId: string;
    /** The default text. The maximum length is 254 characters. */
    text?: string;
    type: string;
}

/** Defaults for a Forge user custom field. */
export interface CustomFieldContextDefaultValueForgeUserField {
    /** The ID of the default user. */
    accountId: string;
    /** The ID of the context. */
    contextId: string;
    type: string;
    /** Filter for a User Picker (single) custom field. */
    userFilter: { enabled: boolean; groups?: Array<string>; roleIds?: Array<number> };
}

/** Default value for a labels custom field. */
export interface CustomFieldContextDefaultValueLabels {
    /** The default labels value. */
    labels: Array<string>;
    type: string;
}

/** The default value for a User Picker (multiple) custom field. */
export interface CustomFieldContextDefaultValueMultiUserPicker {
    /** The IDs of the default users. */
    accountIds: Array<string>;
    /** The ID of the context. */
    contextId: string;
    type: string;
}

/** The default value for a multiple group picker custom field. */
export interface CustomFieldContextDefaultValueMultipleGroupPicker {
    /** The ID of the context. */
    contextId: string;
    /** The IDs of the default groups. */
    groupIds: Array<string>;
    type: string;
}

/** The default value for a multi-select custom field. */
export interface CustomFieldContextDefaultValueMultipleOption {
    /** The ID of the context. */
    contextId: string;
    /** The list of IDs of the default options. */
    optionIds: Array<string>;
    type: string;
}

/** The default value for a multiple version picker custom field. */
export interface CustomFieldContextDefaultValueMultipleVersionPicker {
    type: string;
    /** The IDs of the default versions. */
    versionIds: Array<string>;
    /** The order the pickable versions are displayed in. If not provided, the released-first order is used. Available version orders are `"releasedFirst"` and `"unreleasedFirst"`. */
    versionOrder?: string;
}

/** The default value for a project custom field. */
export interface CustomFieldContextDefaultValueProject {
    /** The ID of the context. */
    contextId: string;
    /** The ID of the default project. */
    projectId: string;
    type: string;
}

/** The default text for a read only custom field. */
export interface CustomFieldContextDefaultValueReadOnly {
    /** The default text. The maximum length is 255 characters. */
    text?: string;
    type: string;
}

/** The default value for a group picker custom field. */
export interface CustomFieldContextDefaultValueSingleGroupPicker {
    /** The ID of the context. */
    contextId: string;
    /** The ID of the the default group. */
    groupId: string;
    type: string;
}

/** The default value for a single select custom field. */
export interface CustomFieldContextDefaultValueSingleOption {
    /** The ID of the context. */
    contextId: string;
    /** The ID of the default option. */
    optionId: string;
    type: string;
}

/** The default value for a version picker custom field. */
export interface CustomFieldContextDefaultValueSingleVersionPicker {
    type: string;
    /** The ID of the default version. */
    versionId: string;
    /** The order the pickable versions are displayed in. If not provided, the released-first order is used. Available version orders are `"releasedFirst"` and `"unreleasedFirst"`. */
    versionOrder?: string;
}

/** The default text for a text area custom field. */
export interface CustomFieldContextDefaultValueTextArea {
    /** The default text. The maximum length is 32767 characters. */
    text?: string;
    type: string;
}

/** The default text for a text custom field. */
export interface CustomFieldContextDefaultValueTextField {
    /** The default text. The maximum length is 254 characters. */
    text?: string;
    type: string;
}

/** The default value for a URL custom field. */
export interface CustomFieldContextDefaultValueURL {
    /** The ID of the context. */
    contextId: string;
    type: string;
    /** The default URL. */
    url: string;
}

/** Default values to update. */
export interface CustomFieldContextDefaultValueUpdate {
    defaultValues?: Array<{ cascadingOptionId?: string; contextId: string; optionId: string; type: string } | { contextId: string; optionIds: Array<string>; type: string } | { contextId: string; optionId: string; type: string } | { accountId: string; contextId: string; type: string; userFilter: { enabled: boolean; groups?: Array<string>; roleIds?: Array<number> } } | { accountIds: Array<string>; contextId: string; type: string } | { contextId: string; groupId: string; type: string } | { contextId: string; groupIds: Array<string>; type: string } | { date?: string; type: string; useCurrent?: boolean } | { dateTime?: string; type: string; useCurrent?: boolean } | { contextId: string; type: string; url: string } | { contextId: string; projectId: string; type: string } | { number: number; type: string } | { labels: Array<string>; type: string } | { text?: string; type: string } | { text?: string; type: string } | { text?: string; type: string } | { type: string; versionId: string; versionOrder?: string } | { type: string; versionIds: Array<string>; versionOrder?: string } | { contextId: string; text?: string; type: string } | { type: string; values?: Array<string> } | { object?: Record<string, unknown>; type: string } | { contextId: string; dateTime?: string; type: string; useCurrent?: boolean } | { contextId: string; groupId: string; type: string } | { contextId: string; groupIds: Array<string>; type: string } | { contextId: string; number: number; type: string } | { accountId: string; contextId: string; type: string; userFilter: unknown } | { accountIds: Array<string>; contextId: string; type: string }>;
}

/** Details of the custom field options for a context. */
export interface CustomFieldContextOption {
    /** Whether the option is disabled. */
    disabled: boolean;
    /** The ID of the custom field option. */
    id: string;
    /** For cascading options, the ID of the custom field option containing the cascading option. */
    optionId?: string;
    /** The value of the custom field option. */
    value: string;
}

/** Details of a context to project association. */
export interface CustomFieldContextProjectMapping {
    /** The ID of the context. */
    readonly contextId: string;
    /** Whether context is global. */
    readonly isGlobalContext?: boolean;
    /** The ID of the project. */
    readonly projectId?: string;
}

/** Defaults for a User Picker (single) custom field. */
export interface CustomFieldContextSingleUserPickerDefaults {
    /** The ID of the default user. */
    accountId: string;
    /** The ID of the context. */
    contextId: string;
    type: string;
    /** Filter for a User Picker (single) custom field. */
    userFilter: { enabled: boolean; groups?: Array<string>; roleIds?: Array<number> };
}

/** Details of a custom field context. */
export interface CustomFieldContextUpdateDetails {
    /** The description of the custom field context. The maximum length is 255 characters. */
    description?: string;
    /** The name of the custom field context. The name must be unique. The maximum length is 255 characters. */
    name?: string;
}

/** A list of custom field options for a context. */
export interface CustomFieldCreatedContextOptionsList {
    /** The created custom field options. */
    options?: Array<{ disabled: boolean; id: string; optionId?: string; value: string }>;
}

export interface CustomFieldDefinitionJsonBean {
    /** The description of the custom field, which is displayed in Jira. */
    description?: string;
    /** The name of the custom field, which is displayed in Jira. This is not the unique identifier. */
    name: string;
    /**
     * The searcher defines the way the field is searched in Jira. For example, *com.atlassian.jira.plugin.system.customfieldtypes:grouppickersearcher*.  
     * The search UI (basic search and JQL search) will display different operations and values for the field, based on the field searcher. You must specify a searcher that is valid for the field type, as listed below (abbreviated values shown):
     *
     *  *  `cascadingselect`: `cascadingselectsearcher`
     *  *  `datepicker`: `daterange`
     *  *  `datetime`: `datetimerange`
     *  *  `float`: `exactnumber` or `numberrange`
     *  *  `grouppicker`: `grouppickersearcher`
     *  *  `importid`: `exactnumber` or `numberrange`
     *  *  `labels`: `labelsearcher`
     *  *  `multicheckboxes`: `multiselectsearcher`
     *  *  `multigrouppicker`: `multiselectsearcher`
     *  *  `multiselect`: `multiselectsearcher`
     *  *  `multiuserpicker`: `userpickergroupsearcher`
     *  *  `multiversion`: `versionsearcher`
     *  *  `project`: `projectsearcher`
     *  *  `radiobuttons`: `multiselectsearcher`
     *  *  `readonlyfield`: `textsearcher`
     *  *  `select`: `multiselectsearcher`
     *  *  `textarea`: `textsearcher`
     *  *  `textfield`: `textsearcher`
     *  *  `url`: `exacttextsearcher`
     *  *  `userpicker`: `userpickergroupsearcher`
     *  *  `version`: `versionsearcher`
     *
     * If no searcher is provided, the field isn't searchable. However, [Forge custom fields](https://developer.atlassian.com/platform/forge/manifest-reference/modules/#jira-custom-field-type--beta-) have a searcher set automatically, so are always searchable.
     */
    searcherKey?: "com.atlassian.jira.plugin.system.customfieldtypes:cascadingselectsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:daterange" | "com.atlassian.jira.plugin.system.customfieldtypes:datetimerange" | "com.atlassian.jira.plugin.system.customfieldtypes:exactnumber" | "com.atlassian.jira.plugin.system.customfieldtypes:exacttextsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:grouppickersearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:labelsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:multiselectsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:numberrange" | "com.atlassian.jira.plugin.system.customfieldtypes:projectsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:textsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:userpickergroupsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:versionsearcher";
    /**
     * The type of the custom field. These built-in custom field types are available:
     *
     *  *  `cascadingselect`: Enables values to be selected from two levels of select lists (value: `com.atlassian.jira.plugin.system.customfieldtypes:cascadingselect`)
     *  *  `datepicker`: Stores a date using a picker control (value: `com.atlassian.jira.plugin.system.customfieldtypes:datepicker`)
     *  *  `datetime`: Stores a date with a time component (value: `com.atlassian.jira.plugin.system.customfieldtypes:datetime`)
     *  *  `float`: Stores and validates a numeric (floating point) input (value: `com.atlassian.jira.plugin.system.customfieldtypes:float`)
     *  *  `grouppicker`: Stores a user group using a picker control (value: `com.atlassian.jira.plugin.system.customfieldtypes:grouppicker`)
     *  *  `importid`: A read-only field that stores the ID the issue had in the system it was imported from (value: `com.atlassian.jira.plugin.system.customfieldtypes:importid`)
     *  *  `labels`: Stores labels (value: `com.atlassian.jira.plugin.system.customfieldtypes:labels`)
     *  *  `multicheckboxes`: Stores multiple values using checkboxes (value: ``)
     *  *  `multigrouppicker`: Stores multiple user groups using a picker control (value: ``)
     *  *  `multiselect`: Stores multiple values using a select list (value: `com.atlassian.jira.plugin.system.customfieldtypes:multicheckboxes`)
     *  *  `multiuserpicker`: Stores multiple users using a picker control (value: `com.atlassian.jira.plugin.system.customfieldtypes:multigrouppicker`)
     *  *  `multiversion`: Stores multiple versions from the versions available in a project using a picker control (value: `com.atlassian.jira.plugin.system.customfieldtypes:multiversion`)
     *  *  `project`: Stores a project from a list of projects that the user is permitted to view (value: `com.atlassian.jira.plugin.system.customfieldtypes:project`)
     *  *  `radiobuttons`: Stores a value using radio buttons (value: `com.atlassian.jira.plugin.system.customfieldtypes:radiobuttons`)
     *  *  `readonlyfield`: Stores a read-only text value, which can only be populated via the API (value: `com.atlassian.jira.plugin.system.customfieldtypes:readonlyfield`)
     *  *  `select`: Stores a value from a configurable list of options (value: `com.atlassian.jira.plugin.system.customfieldtypes:select`)
     *  *  `textarea`: Stores a long text string using a multiline text area (value: `com.atlassian.jira.plugin.system.customfieldtypes:textarea`)
     *  *  `textfield`: Stores a text string using a single-line text box (value: `com.atlassian.jira.plugin.system.customfieldtypes:textfield`)
     *  *  `url`: Stores a URL (value: `com.atlassian.jira.plugin.system.customfieldtypes:url`)
     *  *  `userpicker`: Stores a user using a picker control (value: `com.atlassian.jira.plugin.system.customfieldtypes:userpicker`)
     *  *  `version`: Stores a version using a picker control (value: `com.atlassian.jira.plugin.system.customfieldtypes:version`)
     *
     * To create a field based on a [Forge custom field type](https://developer.atlassian.com/platform/forge/manifest-reference/modules/#jira-custom-field-type--beta-), use the ID of the Forge custom field type as the value. For example, `ari:cloud:ecosystem::extension/e62f20a2-4b61-4dbe-bfb9-9a88b5e3ac84/548c5df1-24aa-4f7c-bbbb-3038d947cb05/static/my-cf-type-key`.
     */
    type: string;
}

/** Details of a custom option for a field. */
export interface CustomFieldOption {
    /** The URL of these custom field option details. */
    readonly self?: string;
    /** The value of the custom field option. */
    readonly value?: string;
}

/** Details of a custom field option to create. */
export interface CustomFieldOptionCreate {
    /** Whether the option is disabled. */
    disabled?: boolean;
    /** For cascading options, the ID of a parent option. */
    optionId?: string;
    /** The value of the custom field option. */
    value: string;
}

/** Details of a custom field option for a context. */
export interface CustomFieldOptionUpdate {
    /** Whether the option is disabled. */
    disabled?: boolean;
    /** The ID of the custom field option. */
    id: string;
    /** The value of the custom field option. */
    value?: string;
}

/** Defines the payload for the custom field definitions. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-fields/\#api-rest-api-3-field-post */
export interface CustomFieldPayload {
    /** The type of the custom field */
    cfType?: string;
    /** The description of the custom field */
    description?: string;
    /** The name of the custom field */
    name?: string;
    /** The strategy to use when there is a conflict with an existing custom field. FAIL - Fail execution, this always needs to be unique; USE - Use the existing entity and ignore new entity parameters */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** Allows an overwrite to declare the new Custom Field to be created as a GLOBAL-scoped field. Leave this as empty or null to use the project's default scope. */
    scope?: "GLOBAL" | "TEMPLATE" | "PROJECT";
    /** The searcher key of the custom field */
    searcherKey?: string;
}

/** Details about the replacement for a deleted version. */
export interface CustomFieldReplacement {
    /** The ID of the custom field in which to replace the version number. */
    customFieldId?: number;
    /** The version number to use as a replacement for the deleted version. */
    moveTo?: number;
}

/** A list of custom field options for a context. */
export interface CustomFieldUpdatedContextOptionsList {
    /** The updated custom field options. */
    options?: Array<{ disabled?: boolean; id: string; value?: string }>;
}

/** A list of issue IDs and the value to update a custom field to. */
export interface CustomFieldValueUpdate {
    /** The list of issue IDs. */
    issueIds: Array<number>;
    /**
     * The value for the custom field. The value must be compatible with the [custom field type](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/#data-types) as follows:
     *
     *  *  `string` the value must be a string.
     *  *  `number` the value must be a number.
     *  *  `datetime` the value must be a string that represents a date in the ISO format or the simplified extended ISO format. For example, `"2023-01-18T12:00:00-03:00"` or `"2023-01-18T12:00:00.000Z"`. However, the milliseconds part is ignored.
     *  *  `user` the value must be an object that contains the `accountId` field.
     *  *  `group` the value must be an object that contains the group `name` or `groupId` field. Because group names can change, we recommend using `groupId`.
     *
     * A list of appropriate values must be provided if the field is of the `list` [collection type](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/#collection-types).
     */
    value: Record<string, unknown>;
}

/** Details of updates for a custom field. */
export interface CustomFieldValueUpdateDetails {
    /** The list of custom field update details. */
    updates?: Array<{ issueIds: Array<number>; value: Record<string, unknown> }>;
}

export interface CustomTemplateOptions {
    /** Enable screen delegated admin support for the template. This means screen and associated schemes will be copied rather than referenced. */
    enableScreenDelegatedAdminSupport?: boolean;
    /** Enable workflow delegated admin support for the template. This means workflows and workflow schemes will be copied rather than referenced. */
    enableWorkflowDelegatedAdminSupport?: boolean;
}

/** The specific request object for creating a project with template. */
export interface CustomTemplateRequestDTO {
    /** Configuration of features for one or more boards. Replaces the deprecated features field on BoardPayload */
    boardFeatures?: { boardFeatures?: Record<string, Array<{ featureKey?: "ESTIMATION" | "SPRINTS"; state?: boolean }>> };
    boards?: { boards?: Array<{ boardFilterJQL?: string; cardColorStrategy?: "ISSUE_TYPE" | "REQUEST_TYPE" | "ASSIGNEE" | "PRIORITY" | "NONE" | "CUSTOM"; cardLayout?: { showDaysInColumn?: boolean }; cardLayouts?: Array<{ fieldId?: string; id?: number; mode?: "PLAN" | "WORK"; position?: number }>; columns?: Array<{ maximumIssueConstraint?: number; minimumIssueConstraint?: number; name?: string; statusIds?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }> }>; features?: Array<unknown>; name?: string; pcri?: unknown; quickFilters?: Array<{ description?: string; jqlQuery?: string; name?: string }>; supportsSprint?: boolean; swimlanes?: { customSwimlanes?: Array<{ description?: string; jqlQuery?: string; name?: string }>; defaultCustomSwimlaneName?: string; swimlaneStrategy?: "none" | "custom" | "parentChild" | "assignee" | "assigneeUnassignedFirst" | "epic" | "project" | "issueparent" | "issuechildren" | "request_type" }; workingDaysConfig?: { friday?: boolean; id?: number; monday?: boolean; nonWorkingDays?: Array<{ id?: number; iso8601Date?: string }>; saturday?: boolean; sunday?: boolean; thursday?: boolean; timezoneId?: string; tuesday?: boolean; wednesday?: boolean } }> };
    /** Defines the payload for the fields, screens, screen schemes, issue type screen schemes, field layouts, and field layout schemes */
    field?: { customFieldDefinitions?: Array<{ cfType?: string; description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; scope?: "GLOBAL" | "TEMPLATE" | "PROJECT"; searcherKey?: string }>; fieldLayoutScheme?: { defaultFieldLayout?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; pcri?: unknown }; fieldLayouts?: Array<{ configuration?: Array<{ field?: boolean; pcri?: unknown; required?: boolean }>; description?: string; name?: string; pcri?: unknown }>; fieldScheme?: { description?: string; items?: Array<{ description?: string; pcri?: unknown; qualifierId?: unknown; qualifierType?: unknown; rendererType?: string; required?: boolean }>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }; issueLayouts?: Array<{ containerId?: unknown; issueLayoutType?: "ISSUE_VIEW" | "ISSUE_CREATE" | "REQUEST_FORM"; items?: Array<{ itemKey?: unknown; properties?: Record<string, Record<string, unknown>>; sectionType?: "content" | "primaryContext" | "secondaryContext"; type?: "FIELD" }>; pcri?: unknown }>; issueTypeScreenScheme?: { defaultScreenScheme?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; pcri?: unknown }; screenScheme?: Array<{ defaultScreen?: unknown; description?: string; name?: string; pcri?: unknown; screens?: Record<string, unknown> }>; screens?: Array<{ description?: string; name?: string; pcri?: unknown; tabs?: Array<{ fields?: Array<unknown>; name?: string }> }> };
    /** The payload for creating issue types in a project */
    issueType?: { issueTypeHierarchy?: Array<{ hierarchyLevel?: number; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }>; issueTypeScheme?: { defaultIssueTypeId?: unknown; description?: string; issueTypeIds?: Array<unknown>; name?: string; pcri?: unknown }; issueTypes?: Array<{ avatarId?: number; description?: string; hierarchyLevel?: number; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }> };
    /** The payload for creating a notification scheme. The user has to supply the ID for the default notification scheme. For CMP this is provided in the project payload and should be left empty, for TMP it's provided using this payload */
    notification?: { description?: string; name?: string; notificationSchemeEvents?: Array<{ event?: { id?: string }; notifications?: Array<{ notificationType?: string; parameter?: string }> }>; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown };
    /** The payload to create a permission scheme */
    permissionScheme?: { addAddonRole?: boolean; description?: string; grants?: Array<{ applicationAccess?: Array<string>; groupCustomFields?: Array<unknown>; groups?: Array<unknown>; permissionKeys?: Array<string>; projectRoles?: Array<unknown>; specialGrants?: Array<string>; userCustomFields?: Array<unknown>; users?: Array<unknown> }>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown };
    /** The payload for creating a project */
    project?: { fieldLayoutSchemeId?: unknown; issueSecuritySchemeId?: unknown; issueTypeSchemeId?: unknown; issueTypeScreenSchemeId?: unknown; notificationSchemeId?: unknown; pcri?: unknown; permissionSchemeId?: unknown; projectTypeKey?: "software" | "business" | "service_desk" | "product_discovery"; workflowSchemeId?: unknown };
    role?: { roleToProjectActors?: Record<string, Array<unknown>>; roles?: Array<{ defaultActors?: Array<unknown>; description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; type?: "HIDDEN" | "VIEWABLE" | "EDITABLE" | "GUEST" }> };
    /** The payload for creating a scope. Defines if a project is team-managed project or company-managed project */
    scope?: { type?: "GLOBAL" | "PROJECT" };
    /** The payload for creating a security scheme. See https://support.atlassian.com/jira-cloud-administration/docs/configure-issue-security-schemes/ */
    security?: { description?: string; name?: string; pcri?: unknown; securityLevels?: Array<{ description?: string; isDefault?: boolean; name?: string; securityLevelMembers?: Array<{ parameter?: string; type?: "group" | "reporter" | "users" }> }> };
    /** The payload for creating a workflows. See https://www.atlassian.com/software/jira/guides/workflows/overview\#what-is-a-jira-workflow */
    workflow?: { statuses?: Array<{ description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE" }>; workflowScheme?: { defaultWorkflow?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }; workflows?: Array<{ description?: string; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; startPointLayout?: unknown; statuses?: Array<{ layout?: unknown; pcri?: unknown; properties?: Record<string, string> }>; transitions?: Array<{ actions?: Array<{ parameters?: Record<string, string>; ruleKey?: string }>; conditions?: { conditionGroup?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; from?: Array<{ fromPort?: number; status?: unknown; toPortOverride?: number }>; id?: number; name?: string; properties?: Record<string, string>; to?: { port?: number; status?: unknown }; transitionScreen?: unknown; triggers?: Array<unknown>; type?: "global" | "initial" | "directed"; validators?: Array<unknown> }> }> };
}

/** Project Details */
export interface CustomTemplatesProjectDetails {
    /** The access level of the project. Only used by team-managed project */
    accessLevel?: "open" | "limited" | "private" | "free";
    /** Additional properties of the project */
    additionalProperties?: Record<string, string>;
    /** The default assignee when creating issues in the project */
    assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED";
    /** The ID of the project's avatar. Use the \[Get project avatars\](\#api-rest-api-3-project-projectIdOrKey-avatar-get) operation to list the available avatars in a project. */
    avatarId?: number;
    /** The ID of the project's category. A complete list of category IDs is found using the [Get all project categories](#api-rest-api-3-projectCategory-get) operation. */
    categoryId?: number;
    /** Brief description of the project */
    description?: string;
    /** Whether components are enabled for the project. Only used by company-managed project */
    enableComponents?: boolean;
    /** Project keys must be unique and start with an uppercase letter followed by one or more uppercase alphanumeric characters. The maximum length is 10 characters. */
    key?: string;
    /** The default language for the project */
    language?: string;
    /** The account ID of the project lead. Either `lead` or `leadAccountId` must be set when creating a project. Cannot be provided with `lead`. */
    leadAccountId?: string;
    /** Name of the project */
    name?: string;
    /** A link to information about this project, such as project documentation */
    url?: string;
}

/** Details of a dashboard. */
export interface Dashboard {
    /** The automatic refresh interval for the dashboard in milliseconds. */
    readonly automaticRefreshMs?: number;
    description?: string;
    /** The details of any edit share permissions for the dashboard. */
    readonly editPermissions?: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>;
    /** The ID of the dashboard. */
    readonly id?: string;
    /** Whether the dashboard is selected as a favorite by the user. */
    readonly isFavourite?: boolean;
    /** Whether the current user has permission to edit the dashboard. */
    readonly isWritable?: boolean;
    /** The name of the dashboard. */
    readonly name?: string;
    /** The owner of the dashboard. */
    readonly owner?: unknown;
    /** The number of users who have this dashboard as a favorite. */
    readonly popularity?: number;
    /** The rank of this dashboard. */
    readonly rank?: number;
    /** The URL of these dashboard details. */
    readonly self?: string;
    /** The details of any view share permissions for the dashboard. */
    readonly sharePermissions?: Array<unknown>;
    /** Whether the current dashboard is system dashboard. */
    readonly systemDashboard?: boolean;
    /** The URL of the dashboard. */
    readonly view?: string;

/** Details of a dashboard. */
export interface DashboardDetails {
    /** The description of the dashboard. */
    description?: string;
    /** The edit permissions for the dashboard. */
    editPermissions: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>;
    /** The name of the dashboard. */
    name: string;
    /** The share permissions for the dashboard. */
    sharePermissions: Array<unknown>;

/** Details of a gadget. */
export interface DashboardGadget {
    /** The color of the gadget. Should be one of `blue`, `red`, `yellow`, `green`, `cyan`, `purple`, `gray`, or `white`. */
    readonly color: "blue" | "red" | "yellow" | "green" | "cyan" | "purple" | "gray" | "white";
    /** The ID of the gadget instance. */
    readonly id: number;
    /** The module key of the gadget type. */
    readonly moduleKey?: string;
    /** The position of the gadget. */
    readonly position: { The column position of the gadget.: number; The row position of the gadget.: number };
    /** The title of the gadget. */
    readonly title: string;
    /** The URI of the gadget type. */
    readonly uri?: string;

/** Details of a gadget position. */
export interface DashboardGadgetPosition {
    The column position of the gadget.: number;
    The row position of the gadget.: number;

/** The list of gadgets on the dashboard. */
export interface DashboardGadgetResponse {
    /** The list of gadgets. */
    readonly gadgets: Array<{ readonly color: "blue" | "red" | "yellow" | "green" | "cyan" | "purple" | "gray" | "white"; readonly id: number; readonly moduleKey?: string; readonly position: { The column position of the gadget.: number; The row position of the gadget.: number }; readonly title: string; readonly uri?: string }>;

/** Details of the settings for a dashboard gadget. */
export interface DashboardGadgetSettings {
    /** The color of the gadget. Should be one of `blue`, `red`, `yellow`, `green`, `cyan`, `purple`, `gray`, or `white`. */
    color?: string;
    /** Whether to ignore the validation of module key and URI. For example, when a gadget is created that is a part of an application that isn't installed. */
    ignoreUriAndModuleKeyValidation?: boolean;
    /** The module key of the gadget type. Can't be provided with `uri`. */
    moduleKey?: string;
    /** The position of the gadget. When the gadget is placed into the position, other gadgets in the same column are moved down to accommodate it. */
    position?: { The column position of the gadget.: number; The row position of the gadget.: number };
    /** The title of the gadget. */
    title?: string;
    /** The URI of the gadget type. Can't be provided with `moduleKey`. */
    uri?: string;

/** The details of the gadget to update. */
export interface DashboardGadgetUpdateRequest {
    /** The color of the gadget. Should be one of `blue`, `red`, `yellow`, `green`, `cyan`, `purple`, `gray`, or `white`. */
    color?: string;
    /** The position of the gadget. */
    position?: { The column position of the gadget.: number; The row position of the gadget.: number };
    /** The title of the gadget. */
    title?: string;

/** The data classification. */
export interface DataClassificationLevelsBean {
    /** The data classifications. */
    classifications?: Array<{ color?: string; description?: string; guideline?: string; guidelineADF?: string; id: string; name?: string; rank?: number; status: string }>;
}

/** The data classification. */
export interface DataClassificationTagBean {
    /** The color of the data classification object. */
    color?: string;
    /** The description of the data classification object. */
    description?: string;
    /** The guideline of the data classification object. */
    guideline?: string;
    /** The guideline in ADF (Atlassian Document Format) for rich text rendering. */
    guidelineADF?: string;
    /** The ID of the data classification object. */
    id: string;
    /** The name of the data classification object. */
    name?: string;
    /** The rank of the data classification object. */
    rank?: number;
    /** The status of the data classification object. */
    status: string;
}

/** List issues archived within a specified date range. */
export interface DateRangeFilterRequest {
    /** List issues archived after a specified date, passed in the YYYY-MM-DD format. */
    dateAfter: string;
    /** List issues archived before a specified date provided in the YYYY-MM-DD format. */
    dateBefore: string;
}

/** Details of scheme and new default level. */
export type DefaultLevelValue = Record<string, unknown>;

/** Details of the scope of the default sharing for new filters and dashboards. */
export interface DefaultShareScope {
    /**
     * The scope of the default sharing for new filters and dashboards:
     *
     *  *  `AUTHENTICATED` Shared with all logged-in users.
     *  *  `GLOBAL` Shared with all logged-in users. This shows as `AUTHENTICATED` in the response.
     *  *  `PRIVATE` Not shared with any users.
     */
    scope: "GLOBAL" | "AUTHENTICATED" | "PRIVATE";
}

/** Details about the default workflow. */
export interface DefaultWorkflow {
    /** Whether a draft workflow scheme is created or updated when updating an active workflow scheme. The draft is updated with the new default workflow. Defaults to `false`. */
    updateDraftIfNeeded?: boolean;
    /** The name of the workflow to set as the default workflow. */
    workflow: string;
}

export interface DefaultWorkflowEditorResponse {
    value?: "NEW" | "LEGACY";
}

export interface DeleteAndReplaceVersionBean {
    /** An array of custom field IDs (`customFieldId`) and version IDs (`moveTo`) to update when the fields contain the deleted version. */
    customFieldReplacementList?: Array<{ customFieldId?: number; moveTo?: number }>;
    /** The ID of the version to update `affectedVersion` to when the field contains the deleted version. */
    moveAffectedIssuesTo?: number;
    /** The ID of the version to update `fixVersion` to when the field contains the deleted version. */
    moveFixIssuesTo?: number;
}

/** Response object after successfully deleting a field association scheme. */
export interface DeleteFieldAssociationSchemeResponse {
    deleted?: boolean;
    id?: string;
}

/** Details about a workflow. */
export interface DeprecatedWorkflow {
    default?: boolean;
    /** The description of the workflow. */
    readonly description?: string;
    /** The datetime the workflow was last modified. */
    readonly lastModifiedDate?: string;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    readonly lastModifiedUser?: string;
    /** The account ID of the user that last modified the workflow. */
    readonly lastModifiedUserAccountId?: string;
    /** The name of the workflow. */
    readonly name?: string;
    /** The scope where this workflow applies */
    readonly scope?: Record<string, unknown>;
    /** The number of steps included in the workflow. */
    readonly steps?: number;
}

export interface DetailedErrorCollection {
    /** Map of objects representing additional details for an error */
    details?: Record<string, Record<string, unknown>>;
    /** The list of error messages produced by this operation. For example, "input parameter 'key' must be provided" */
    errorMessages?: Array<string>;
    /** The list of errors by parameter returned by the operation. For example,"projectKey": "Project keys must start with an uppercase letter, followed by one or more uppercase alphanumeric characters." */
    errors?: Record<string, string>;
}

/** The current version details of this workflow scheme. */
export interface DocumentVersion {
    /** The version UUID. */
    id?: string;
    /** The version number. */
    versionNumber?: number;
}

export interface DuplicatePlanRequest {
    /** The plan name. */
    name: string;
}

/** Request to edit a custom template */
export interface EditTemplateRequest {
    /** The description of the template */
    templateDescription?: string;
    templateGenerationOptions?: { enableScreenDelegatedAdminSupport?: boolean; enableWorkflowDelegatedAdminSupport?: boolean };
    /** The unique identifier of the template */
    templateKey?: string;
    /** The name of the template */
    templateName?: string;
}

/** An entity property, for more information see [Entity properties](https://developer.atlassian.com/cloud/jira/platform/jira-entity-properties/). */
export interface EntityProperty {
    /** The key of the property. Required on create and update. */
    key?: string;
    /** The value of the property. Required on create and update. */
    value?: Record<string, unknown>;
}

export interface EntityPropertyDetails {
    /** The entity property ID. */
    entityId: number;
    /** The entity property key. */
    key: string;
    /** The new value of the entity property. */
    value: string;
}

export interface Error {
    count?: number;
    issueIdsOrKeys?: Array<string>;
    message?: string;
}

/** Error messages from an operation. */
export interface ErrorCollection {
    /** The list of error messages produced by this operation. For example, "input parameter 'key' must be provided" */
    errorMessages?: Array<string>;
    /** The list of errors by parameter returned by the operation. For example,"projectKey": "Project keys must start with an uppercase letter, followed by one or more uppercase alphanumeric characters." */
    errors?: Record<string, string>;
    status?: number;
}

export interface ErrorCollections {
}

export interface ErrorMessage {
    message?: string;
}

export interface Errors {
    issueIsSubtask?: { count?: number; issueIdsOrKeys?: Array<string>; message?: string };
    issuesInArchivedProjects?: unknown;
    issuesInUnlicensedProjects?: unknown;
    issuesNotFound?: unknown;
    userDoesNotHavePermission?: unknown;
}

/** Details about a notification associated with an event. */
export interface EventNotification {
    /** The email address. */
    emailAddress?: string;
    /** Expand options that include additional event notification details in the response. */
    expand?: string;
    /** The custom user or group field. */
    field?: { clauseNames?: Array<string>; custom?: boolean; id?: string; key?: string; name?: string; navigable?: boolean; orderable?: boolean; schema?: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string }; scope?: Record<string, unknown>; searchable?: boolean };
    /** The specified group. */
    group?: { groupId?: string; name?: string; readonly self?: string };
    /** The ID of the notification. */
    id?: number;
    /** Identifies the recipients of the notification. */
    notificationType?: "CurrentAssignee" | "Reporter" | "CurrentUser" | "ProjectLead" | "ComponentLead" | "User" | "Group" | "ProjectRole" | "EmailAddress" | "AllWatchers" | "UserCustomField" | "GroupCustomField";
    /**
     * As a group's name can change, use of `recipient` is recommended. The identifier associated with the `notificationType` value that defines the receiver of the notification, where the receiver isn't implied by `notificationType` value. So, when `notificationType` is:
     *
     *  *  `User` The `parameter` is the user account ID.
     *  *  `Group` The `parameter` is the group name.
     *  *  `ProjectRole` The `parameter` is the project role ID.
     *  *  `UserCustomField` The `parameter` is the ID of the custom field.
     *  *  `GroupCustomField` The `parameter` is the ID of the custom field.
     */
    parameter?: string;
    /** The specified project role. */
    projectRole?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string };
    /**
     * The identifier associated with the `notificationType` value that defines the receiver of the notification, where the receiver isn't implied by the `notificationType` value. So, when `notificationType` is:
     *
     *  *  `User`, `recipient` is the user account ID.
     *  *  `Group`, `recipient` is the group ID.
     *  *  `ProjectRole`, `recipient` is the project role ID.
     *  *  `UserCustomField`, `recipient` is the ID of the custom field.
     *  *  `GroupCustomField`, `recipient` is the ID of the custom field.
     */
    recipient?: string;
    /** The specified user. */
    user?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string };

/** A priority scheme with less fields to be used in for an API expand response. */
export interface ExpandPrioritySchemeBean {
    /** The ID of the priority scheme. */
    readonly id?: string;
    /** The name of the priority scheme. */
    readonly name?: string;
    /** The URL of the priority scheme. */
    readonly self?: string;
}

export type ExpandPrioritySchemePage = Record<string, unknown>;

/** The response for status request for a running/completed export task. */
export interface ExportArchivedIssuesTaskProgressResponse {
    fileUrl?: string;
    payload?: string;
    progress?: number;
    status?: string;
    submittedTime?: string;
    taskId?: string;
}

/** Details about a failed webhook. */
export interface FailedWebhook {
    /** The webhook body. */
    body?: string;
    /** The time the webhook was added to the list of failed webhooks (that is, the time of the last failed retry). */
    failureTime: number;
    /** The webhook ID, as sent in the `X-Atlassian-Webhook-Identifier` header with the webhook. */
    id: string;
    /** The original webhook destination. */
    url: string;
}

/** A page of failed webhooks. */
export interface FailedWebhooks {
    /** The maximum number of items on the page. If the list of values is shorter than this number, then there are no more pages. */
    maxResults: number;
    /** The URL to the next page of results. Present only if the request returned at least one result.The next page may be empty at the time of receiving the response, but new failed webhooks may appear in time. You can save the URL to the next page and query for new results periodically (for example, every hour). */
    next?: string;
    /** The list of webhooks. */
    values: Array<{ body?: string; failureTime: number; id: string; url: string }>;
}

/** Details of a field. */
export interface Field {
    /** Number of contexts where the field is used. */
    contextsCount?: number;
    /** The description of the field. */
    description?: string;
    /** The ID of the field. */
    id: string;
    /** Whether the field is locked. */
    isLocked?: boolean;
    /** Whether the field is shown on screen or not. */
    isUnscreenable?: boolean;
    /** The key of the field. */
    key?: string;
    /** Information about the most recent use of a field. */
    lastUsed?: { type?: "TRACKED" | "NOT_TRACKED" | "NO_INFORMATION"; value?: string };
    /** The name of the field. */
    name: string;
    /** Number of projects where the field is used. */
    projectsCount?: number;
    /** The schema of a field. */
    schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string };
    /** Number of screens where the field is used. */
    screensCount?: number;
    /** The searcher key of the field. Returned for custom fields. */
    searcherKey?: string;
    /** The stable ID of the field. */
    stableId?: string;
    /** The display name of the field type */
    typeDisplayName?: string;
}

/** Defines the payload for the field association scheme. */
export interface FieldAssociationItemPayload {
    /** The description of the field association item */
    description?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    qualifierId?: unknown;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    qualifierType?: unknown;
    /** The renderer type of the field */
    rendererType?: string;
    /** Whether the field is required */
    required?: boolean;
}

export interface FieldAssociationParameters {
    description?: string;
    isRequired: boolean;
}

/** Field association scheme field search results. */
export interface FieldAssociationSchemeFieldSearchResult {
    allowedOperations?: Array<string>;
    fieldId?: string;
    parameters?: { description?: string; isRequired?: boolean };
    restrictedToWorkTypes?: Array<string>;
    workTypeParameters?: Array<{ description?: string; isRequired?: boolean; workTypeId?: string }>;
}

export interface FieldAssociationSchemeLinks {
    associations?: string;
    projects?: string;
}

export interface FieldAssociationSchemeLinksBean {
    associations?: string;
    projects?: string;
}

/** Matched filters for field association scheme search. */
export interface FieldAssociationSchemeMatchedFilters {
    projectIds?: Array<number>;
    query?: string;
}

/** Project search results for field association scheme. */
export interface FieldAssociationSchemeProjectSearchResult {
    avatarUrls?: Record<string, string>;
    id?: string;
    key?: string;
    name?: string;
}

/** Details of field associations with projects. */
export interface FieldAssociationsRequest {
    /** Contexts to associate/unassociate the fields with. */
    associationContexts: Array<{ identifier?: Record<string, unknown>; type: string }>;
    /** Fields to associate/unassociate with projects. */
    fields: Array<{ identifier?: Record<string, unknown>; type: string }>;
}

/** Defines the payload for the fields, screens, screen schemes, issue type screen schemes, field layouts, and field layout schemes */
export interface FieldCapabilityPayload {
    /** The custom field definitions. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-fields/\#api-rest-api-3-field-post */
    customFieldDefinitions?: Array<{ cfType?: string; description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }; scope?: "GLOBAL" | "TEMPLATE" | "PROJECT"; searcherKey?: string }>;
    /**
     * Deprecated use [fieldAssociationScheme](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-field-schemes/#api-group-field-schemes) instead Defines the payload for the field layout schemes. See [ Field configuration scheme](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-field-configurations/#api-rest-api-3-fieldconfigurationscheme-post).
     *
     * [ How to configure a field configuration scheme](https://support.atlassian.com/jira-cloud-administration/docs/configure-a-field-configuration-scheme/).
     */
    /** @deprecated */
    fieldLayoutScheme?: { defaultFieldLayout?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; pcri?: unknown };
    /** The field layouts configuration. */
    /** @deprecated */
    fieldLayouts?: Array<{ configuration?: Array<{ field?: boolean; pcri?: unknown; required?: boolean }>; description?: string; name?: string; pcri?: unknown }>;
    /** Defines the payload to configure the field scheme for a project. See [Field schemes](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-field-schemes/#api-group-field-schemes). */
    fieldScheme?: { description?: string; items?: Array<{ description?: string; pcri?: unknown; qualifierId?: unknown; qualifierType?: unknown; rendererType?: string; required?: boolean }>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown };
    /** The issue layouts configuration */
    issueLayouts?: Array<{ containerId?: unknown; issueLayoutType?: "ISSUE_VIEW" | "ISSUE_CREATE" | "REQUEST_FORM"; items?: Array<{ itemKey?: unknown; properties?: Record<string, Record<string, unknown>>; sectionType?: "content" | "primaryContext" | "secondaryContext"; type?: "FIELD" }>; pcri?: unknown }>;
    /** Defines the payload for the issue type screen schemes. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-type-screen-schemes/\#api-rest-api-3-issuetypescreenscheme-post */
    issueTypeScreenScheme?: { defaultScreenScheme?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; pcri?: unknown };
    /** The screen schemes See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-screen-schemes/\#api-rest-api-3-screenscheme-post */
    screenScheme?: Array<{ defaultScreen?: unknown; description?: string; name?: string; pcri?: unknown; screens?: Record<string, unknown> }>;
    /** The screens. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-screens/\#api-rest-api-3-screens-post */
    screens?: Array<{ description?: string; name?: string; pcri?: unknown; tabs?: Array<{ fields?: Array<unknown>; name?: string }> }>;
}

/** A clause that asserts whether a field was changed. For example, `status CHANGED AFTER startOfMonth(-1M)`.See [CHANGED](https://confluence.atlassian.com/x/dgiiLQ#Advancedsearching-operatorsreference-CHANGEDCHANGED) for more information about the CHANGED operator. */
export interface FieldChangedClause {
    /** A field used in a JQL query. See [Advanced searching - fields reference](https://confluence.atlassian.com/x/dAiiLQ) for more information about fields in JQL queries. */
    field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> };
    /** The operator applied to the field. */
    operator: "changed";
    /** The list of time predicates. */
    predicates: Array<{ operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown; operator: "before" | "after" | "from" | "to" | "on" | "during" | "by" }>;
}

/** Details of a field configuration. */
export interface FieldConfiguration {
    /** The description of the field configuration. */
    description: string;
    /** The ID of the field configuration. */
    id: number;
    /** Whether the field configuration is the default. */
    isDefault?: boolean;
    /** The name of the field configuration. */
    name: string;
}

/** Details of a field configuration. */
export interface FieldConfigurationDetails {
    /** The description of the field configuration. */
    description?: string;
    /** The name of the field configuration. Must be unique. */
    name: string;
}

/** The field configuration for an issue type. */
export interface FieldConfigurationIssueTypeItem {
    /** The ID of the field configuration. */
    fieldConfigurationId: string;
    /** The ID of the field configuration scheme. */
    fieldConfigurationSchemeId: string;
    /** The ID of the issue type or *default*. When set to *default* this field configuration issue type item applies to all issue types without a field configuration. */
    issueTypeId: string;
}

/** A field within a field configuration. */
export interface FieldConfigurationItem {
    /** The description of the field within the field configuration. */
    description?: string;
    /** The ID of the field within the field configuration. */
    id: string;
    /** Whether the field is hidden in the field configuration. */
    isHidden?: boolean;
    /** Whether the field is required in the field configuration. */
    isRequired?: boolean;
    /** The renderer type for the field within the field configuration. */
    renderer?: string;
}

/** Details of field configuration items. */
export interface FieldConfigurationItemsDetails {
    /** Details of fields in a field configuration. */
    fieldConfigurationItems: Array<{ description?: string; id: string; isHidden?: boolean; isRequired?: boolean; renderer?: string }>;
}

/** Details of a field configuration scheme. */
export interface FieldConfigurationScheme {
    /** The description of the field configuration scheme. */
    description?: string;
    /** The ID of the field configuration scheme. */
    id: string;
    /** The name of the field configuration scheme. */
    name: string;
}

/** Associated field configuration scheme and project. */
export interface FieldConfigurationSchemeProjectAssociation {
    /** The ID of the field configuration scheme. If the field configuration scheme ID is `null`, the operation assigns the default field configuration scheme. */
    fieldConfigurationSchemeId?: string;
    /** The ID of the project. */
    projectId: string;
}

/** Project list with assigned field configuration schema. */
export interface FieldConfigurationSchemeProjects {
    /** Details of a field configuration scheme. */
    fieldConfigurationScheme?: { description?: string; id: string; name: string };
    /** The IDs of projects using the field configuration scheme. */
    projectIds: Array<string>;
}

/** The field configuration to issue type mapping. */
export interface FieldConfigurationToIssueTypeMapping {
    /** The ID of the field configuration. */
    fieldConfigurationId: string;
    /** The ID of the issue type or *default*. When set to *default* this field configuration issue type item applies to all issue types without a field configuration. An issue type can be included only once in a request. */
    issueTypeId: string;
}

/** The metadata describing an issue field for createmeta. */
export interface FieldCreateMetadata {
    /** The list of values allowed in the field. */
    readonly allowedValues?: Array<Record<string, unknown>>;
    /** The URL that can be used to automatically complete the field. */
    readonly autoCompleteUrl?: string;
    /** The configuration properties. */
    readonly configuration?: Record<string, Record<string, unknown>>;
    /** The default value of the field. */
    readonly defaultValue?: Record<string, unknown>;
    /** The field id. */
    readonly fieldId: string;
    /** Whether the field has a default value. */
    readonly hasDefaultValue?: boolean;
    /** The key of the field. */
    readonly key: string;
    /** The name of the field. */
    readonly name: string;
    /** The list of operations that can be performed on the field. */
    readonly operations: Array<string>;
    /** Whether the field is required. */
    readonly required: boolean;
    /** The data type of the field. */
    readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string };
}

/** Details about a field. */
export interface FieldDetails {
    /** The names that can be used to reference the field in an advanced search. For more information, see [Advanced searching - fields reference](https://confluence.atlassian.com/x/gwORLQ). */
    clauseNames?: Array<string>;
    /** Whether the field is a custom field. */
    custom?: boolean;
    /** The ID of the field. */
    id?: string;
    /** The key of the field. */
    key?: string;
    /** The name of the field. */
    name?: string;
    /** Whether the field can be used as a column on the issue navigator. */
    navigable?: boolean;
    /** Whether the content of the field can be used to order lists. */
    orderable?: boolean;
    /** The data schema for the field. */
    schema?: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string };
    /** The scope of the field. */
    scope?: Record<string, unknown>;
    /** Whether the content of the field can be searched. */
    searchable?: boolean;
}

export type FieldIdIdentifier = { identifier?: Record<string, unknown>; type: string } & { identifier?: string };

/** Identifier for a field for example FIELD\_ID. */
export interface FieldIdentifierObject {
    identifier?: Record<string, unknown>;
    type: string;
}

/** Information about the most recent use of a field. */
export interface FieldLastUsed {
    /**
     * Last used value type:
     *
     *  *  *TRACKED*: field is tracked and a last used date is available.
     *  *  *NOT\_TRACKED*: field is not tracked, last used date is not available.
     *  *  *NO\_INFORMATION*: field is tracked, but no last used date is available.
     */
    type?: "TRACKED" | "NOT_TRACKED" | "NO_INFORMATION";
    /** The date when the value of the field last changed. */
    value?: string;
}

/** Defines the payload for the field layout configuration. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-field-configurations/\#api-rest-api-3-fieldconfiguration-post */
export interface FieldLayoutConfiguration {
    /** Whether to show the field */
    field?: boolean;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** Whether the field is required */
    required?: boolean;
}

/** Defines the payload for the field layouts. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-field-configurations/\#api-group-issue-field-configurations" + fieldlayout is what users would see as "Field Configuration" in Jira's UI - https://support.atlassian.com/jira-cloud-administration/docs/manage-issue-field-configurations/ */
export interface FieldLayoutPayload {
    /** The field layout configuration. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-field-configurations/\#api-rest-api-3-fieldconfiguration-post */
    configuration?: Array<{ field?: boolean; pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }; required?: boolean }>;
    /** The description of the field layout */
    description?: string;
    /** The name of the field layout */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

/**
 * Deprecated use [fieldAssociationScheme](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-field-schemes/#api-group-field-schemes) instead Defines the payload for the field layout schemes. See [ Field configuration scheme](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-field-configurations/#api-rest-api-3-fieldconfigurationscheme-post).
 *
 * [ How to configure a field configuration scheme](https://support.atlassian.com/jira-cloud-administration/docs/configure-a-field-configuration-scheme/).
 */
export interface FieldLayoutSchemePayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    defaultFieldLayout?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The description of the field layout scheme */
    description?: string;
    /** There is a default configuration "fieldlayout" that is applied to all issue types using this scheme that don't have an explicit mapping users can create (or re-use existing) configurations for other issue types and map them to this scheme */
    explicitMappings?: Record<string, unknown>;
    /** The name of the field layout scheme */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

/** The metadata describing an issue field. */
export interface FieldMetadata {
    /** The list of values allowed in the field. */
    readonly allowedValues?: Array<Record<string, unknown>>;
    /** The URL that can be used to automatically complete the field. */
    readonly autoCompleteUrl?: string;
    /** The configuration properties. */
    readonly configuration?: Record<string, Record<string, unknown>>;
    /** The default value of the field. */
    readonly defaultValue?: Record<string, unknown>;
    /** Whether the field has a default value. */
    readonly hasDefaultValue?: boolean;
    /** The key of the field. */
    readonly key: string;
    /** The name of the field. */
    readonly name: string;
    /** The list of operations that can be performed on the field. */
    readonly operations: Array<string>;
    /** Whether the field is required. */
    readonly required: boolean;
    /** The data type of the field. */
    readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string };
}

/** List of project associations. */
export interface FieldProjectAssociation {
    projectId?: string;
}

/** Details of a field that can be used in advanced searches. */
export interface FieldReferenceData {
    /** Whether the field provide auto-complete suggestions. */
    auto?: "true" | "false";
    /** If the item is a custom field, the ID of the custom field. */
    cfid?: string;
    /** Whether this field has been deprecated. */
    deprecated?: "true" | "false";
    /** The searcher key of the field, only passed when the field is deprecated. */
    deprecatedSearcherKey?: string;
    /**
     * The display name contains the following:
     *
     *  *  for system fields, the field name. For example, `Summary`.
     *  *  for collapsed custom fields, the field name followed by a hyphen and then the field name and field type. For example, `Component - Component[Dropdown]`.
     *  *  for other custom fields, the field name followed by a hyphen and then the custom field ID. For example, `Component - cf[10061]`.
     */
    displayName?: string;
    /** The valid search operators for the field. */
    operators?: Array<string>;
    /** Whether the field can be used in a query's `ORDER BY` clause. */
    orderable?: "true" | "false";
    /** Whether the content of this field can be searched. */
    searchable?: "true" | "false";
    /** The data types of items in the field. */
    types?: Array<string>;
    /** The field identifier. */
    value?: string;
}

/** Defines the payload to configure the field scheme for a project. See [Field schemes](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-field-schemes/#api-group-field-schemes). */
export interface FieldSchemePayload {
    /** The description of the field scheme */
    description?: string;
    /** The field association items for this field scheme. */
    items?: Array<{ description?: string; pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }; qualifierId?: unknown; qualifierType?: unknown; rendererType?: string; required?: boolean }>;
    /** The name of the field scheme */
    name?: string;
    /** The strategy to use when there is a conflict with an existing field scheme. FAIL - Fail execution, this always needs to be unique; USE - Use the existing entity and ignore new entity parameters */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

/** Partial failure result when updating field scheme to fields associations. */
export interface FieldSchemeToFieldsPartialFailure {
    error?: string;
    fieldId: string;
    schemeId: number;
    success: boolean;
    workTypeIds: Array<number>;
}

/** Response for updating field associations. */
export interface FieldSchemeToFieldsResponse {
    results: Array<{ error?: string; fieldId: string; schemeId: number; success: boolean; workTypeIds: Array<number> }>;
}

/** Partial failure result when updating field scheme to projects associations. */
export interface FieldSchemeToProjectsPartialFailure {
    error?: string;
    projectId: number;
    schemeId: number;
    success: boolean;
}

/** Request for associating field schemes to projects. */
export interface FieldSchemeToProjectsRequest {
    /** List of project IDs to associate with field schemes */
    projectIds: Array<number>;
}

/** Response for updating field scheme to projects associations. */
export interface FieldSchemeToProjectsResponse {
    results: Array<{ error?: string; projectId: number; schemeId: number; success: boolean }>;
}

/** Details of an operation to perform on a field. */
export interface FieldUpdateOperation {
    /** The value to add to the field. */
    add?: Record<string, unknown>;
    /** The field value to copy from another issue. */
    copy?: Record<string, unknown>;
    /** The value to edit in the field. */
    edit?: Record<string, unknown>;
    /** The value to removed from the field. */
    remove?: Record<string, unknown>;
    /** The value to set in the field. */
    set?: Record<string, unknown>;
}

/** A clause that asserts the current value of a field. For example, `summary ~ test`. */
export interface FieldValueClause {
    /** A field used in a JQL query. See [Advanced searching - fields reference](https://confluence.atlassian.com/x/dAiiLQ) for more information about fields in JQL queries. */
    field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> };
    /** Details of an operand in a JQL clause. */
    operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown;
    /** The operator between the field and operand. */
    operator: "=" | "!=" | ">" | "<" | ">=" | "<=" | "in" | "not in" | "~" | "~=" | "is" | "is not";
}

/** A clause that asserts a previous value of a field. For example, `status WAS "Resolved" BY currentUser() BEFORE "2019/02/02"`. See [WAS](https://confluence.atlassian.com/x/dgiiLQ#Advancedsearching-operatorsreference-WASWAS) for more information about the WAS operator. */
export interface FieldWasClause {
    /** A field used in a JQL query. See [Advanced searching - fields reference](https://confluence.atlassian.com/x/dAiiLQ) for more information about fields in JQL queries. */
    field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> };
    /** Details of an operand in a JQL clause. */
    operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown;
    /** The operator between the field and operand. */
    operator: "was" | "was in" | "was not in" | "was not";
    /** The list of time predicates. */
    predicates: Array<{ operand: unknown; operator: "before" | "after" | "from" | "to" | "on" | "during" | "by" }>;
}

/** Key fields from the linked issue. */
export interface Fields {
    /** The assignee of the linked issue. */
    readonly assignee?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string };
    /** The type of the linked issue. */
    readonly issueType?: { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean };
    /** Details about an issue type. */
    issuetype?: { readonly avatarId?: unknown; readonly description?: unknown; readonly entityId?: unknown; readonly hierarchyLevel?: unknown; readonly iconUrl?: unknown; readonly id?: unknown; readonly name?: unknown; readonly scope?: unknown; readonly self?: unknown; readonly subtask?: unknown };
    /** The priority of the linked issue. */
    readonly priority?: Record<string, unknown>;
    /** The status of the linked issue. */
    readonly status?: Record<string, unknown>;
    /** The summary description of the linked issue. */
    readonly summary?: string;
    /** The time tracking of the linked issue. */
    readonly timetracking?: { readonly originalEstimate?: string; readonly originalEstimateSeconds?: number; readonly remainingEstimate?: string; readonly remainingEstimateSeconds?: number; readonly timeSpent?: string; readonly timeSpentSeconds?: number };

/** The default parameters to apply to the field across all work types in the specified schemes, may be null if only work type-specific updates are needed */
export interface FieldsSchemeItemParameter {
    /** The custom description for the field, null to preserve current description */
    description?: string;
    /** Whether the field is required, null to preserve current requirement setting */
    isRequired?: boolean;
}

/** The list of work type-specific parameter overrides, may be empty if only default parameters are being updated */
export interface FieldsSchemeItemWorkTypeParameter {
    /** The custom description for the field for this work type, null to use default or preserve current */
    description?: string;
    /** Whether the field is required for this work type, null to use default or preserve current */
    isRequired?: boolean;
    /** The ID of the work type (issue type) for which these parameters apply */
    workTypeId?: number;
}

/** Details about a filter. */
export interface Filter {
    /** \[Experimental\] Approximate last used time. Returns the date and time when the filter was last used. Returns `null` if the filter hasn't been used after tracking was enabled. For performance reasons, timestamps aren't updated in real time and therefore may not be exactly accurate. */
    readonly approximateLastUsed?: string;
    /** A description of the filter. */
    description?: string;
    /** The groups and projects that can edit the filter. */
    editPermissions?: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>;
    /** Whether the filter is selected as a favorite. */
    favourite?: boolean;
    /** The count of how many users have selected this filter as a favorite, including the filter owner. */
    readonly favouritedCount?: number;
    /** The unique identifier for the filter. */
    readonly id?: string;
    /** The JQL query for the filter. For example, *project = SSP AND issuetype = Bug*. */
    jql?: string;
    /** The name of the filter. Must be unique. */
    name: string;
    /** The user who owns the filter. This is defaulted to the creator of the filter, however Jira administrators can change the owner of a shared filter in the admin settings. */
    readonly owner?: unknown;
    /** A URL to view the filter results in Jira, using the [Search for issues using JQL](#api-rest-api-3-filter-search-get) operation with the filter's JQL string to return the filter results. For example, *https://your-domain.atlassian.net/rest/api/3/search?jql=project+%3D+SSP+AND+issuetype+%3D+Bug*. */
    readonly searchUrl?: string;
    /** The URL of the filter. */
    readonly self?: string;
    /** The groups and projects that the filter is shared with. */
    sharePermissions?: Array<unknown>;
    /** A paginated list of the users that the filter is shared with. This includes users that are members of the groups or can browse the projects that the filter is shared with. */
    readonly sharedUsers?: { readonly end-index?: number; readonly items?: Array<unknown>; readonly max-results?: number; readonly size?: number; readonly start-index?: number };
    /** A paginated list of the users that are subscribed to the filter. */
    readonly subscriptions?: { readonly end-index?: number; readonly items?: Array<{ readonly group?: unknown; readonly id?: number; readonly user?: unknown }>; readonly max-results?: number; readonly size?: number; readonly start-index?: number };
    /** A URL to view the filter results in Jira, using the ID of the filter. For example, *https://your-domain.atlassian.net/issues/?filter=10100*. */
    readonly viewUrl?: string;

/** Details of a filter. */
export interface FilterDetails {
    /** \[Experimental\] Approximate last used time. Returns the date and time when the filter was last used. Returns `null` if the filter hasn't been used after tracking was enabled. For performance reasons, timestamps aren't updated in real time and therefore may not be exactly accurate. */
    readonly approximateLastUsed?: string;
    /** The description of the filter. */
    description?: string;
    /** The groups and projects that can edit the filter. This can be specified when updating a filter, but not when creating a filter. */
    editPermissions?: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>;
    /** Expand options that include additional filter details in the response. */
    readonly expand?: string;
    /** Whether the filter is selected as a favorite by any users, not including the filter owner. */
    readonly favourite?: boolean;
    /** The count of how many users have selected this filter as a favorite, including the filter owner. */
    readonly favouritedCount?: number;
    /** The unique identifier for the filter. */
    readonly id?: string;
    /** The JQL query for the filter. For example, *project = SSP AND issuetype = Bug*. */
    readonly jql?: string;
    /** The name of the filter. */
    name: string;
    /** The user who owns the filter. Defaults to the creator of the filter, however, Jira administrators can change the owner of a shared filter in the admin settings. */
    readonly owner?: unknown;
    /** A URL to view the filter results in Jira, using the [Search for issues using JQL](#api-rest-api-3-filter-search-get) operation with the filter's JQL string to return the filter results. For example, *https://your-domain.atlassian.net/rest/api/3/search?jql=project+%3D+SSP+AND+issuetype+%3D+Bug*. */
    readonly searchUrl?: string;
    /** The URL of the filter. */
    readonly self?: string;
    /** The groups and projects that the filter is shared with. This can be specified when updating a filter, but not when creating a filter. */
    sharePermissions?: Array<unknown>;
    /** The users that are subscribed to the filter. */
    readonly subscriptions?: Array<{ readonly group?: unknown; readonly id?: number; readonly user?: unknown }>;
    /** A URL to view the filter results in Jira, using the ID of the filter. For example, *https://your-domain.atlassian.net/issues/?filter=10100*. */
    readonly viewUrl?: string;

/** Details of a user or group subscribing to a filter. */
export interface FilterSubscription {
    /** The group subscribing to filter. */
    readonly group?: { groupId?: string; name?: string; readonly self?: string };
    /** The ID of the filter subscription. */
    readonly id?: number;
    /** The user subscribing to filter. */
    readonly user?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string };

/** A paginated list of subscriptions to a filter. */
export interface FilterSubscriptionsList {
    /** The index of the last item returned on the page. */
    readonly end-index?: number;
    /** The list of items. */
    readonly items?: Array<{ readonly group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; readonly user?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string } }>;
    /** The maximum number of results that could be on the page. */
    readonly max-results?: number;
    /** The number of items on the page. */
    readonly size?: number;
    /** The index of the first item returned on the page. */
    readonly start-index?: number;

export interface ForgePanelProjectPinAsyncResponse {
    taskId?: string;
}

export interface ForgePanelProjectPinRequest {
    /** The moduleId of the Forge panel in the format `ari:cloud:ecosystem::extension/{app-id}/{environment-id}/static/{module-key}` */
    moduleId: string;
    /** The list of projects to pin or unpin the issue panel to or from. */
    projectList: Array<{ action: "PIN" | "UNPIN"; projectIdOrKey: string }>;
}

/** A group found in a search. */
export interface FoundGroup {
    /** Avatar url for the group/team if present. */
    avatarUrl?: string;
    /** The ID of the group, which uniquely identifies the group across all Atlassian products. For example, *952d12c3-5b5b-4d04-bb32-44d383afc4b2*. */
    groupId?: string;
    /** The group name with the matched query string highlighted with the HTML bold tag. */
    html?: string;
    labels?: Array<{ text?: string; title?: string; type?: "ADMIN" | "SINGLE" | "MULTIPLE" }>;
    /**
     * Describes who/how the team is managed. The possible values are  
     * \* external - when team is synced from an external directory like SCIM or HRIS, and team members cannot be modified.  
     * \* admins - when a team is managed by an admin (team members can only be modified by admins).  
     * \* team-members - managed by existing team members, new members need to be invited to join.  
     * \* open - anyone can join or modify this team.
     */
    managedBy?: "EXTERNAL" | "ADMINS" | "TEAM_MEMBERS" | "OPEN";
    /** The name of the group. The name of a group is mutable, to reliably identify a group use ``groupId`.` */
    name?: string;
    /**
     * Describes the type of group. The possible values are  
     * \* team-collaboration - A platform team managed in people directory.  
     * \* userbase-group - a group of users created in adminhub.  
     * \* admin-oversight - currently unused.
     */
    usageType?: "USERBASE_GROUP" | "TEAM_COLLABORATION" | "ADMIN_OVERSIGHT";
}

/** The list of groups found in a search, including header text (Showing X of Y matching groups) and total of matched groups. */
export interface FoundGroups {
    groups?: Array<{ avatarUrl?: string; groupId?: string; html?: string; labels?: Array<{ text?: string; title?: string; type?: "ADMIN" | "SINGLE" | "MULTIPLE" }>; managedBy?: "EXTERNAL" | "ADMINS" | "TEAM_MEMBERS" | "OPEN"; name?: string; usageType?: "USERBASE_GROUP" | "TEAM_COLLABORATION" | "ADMIN_OVERSIGHT" }>;
    /** Header text indicating the number of groups in the response and the total number of groups found in the search. */
    header?: string;
    /** The total number of groups found in the search. */
    total?: number;
}

/** The list of users found in a search, including header text (Showing X of Y matching users) and total of matched users. */
export interface FoundUsers {
    /** Header text indicating the number of users in the response and the total number of users found in the search. */
    header?: string;
    /** The total number of users found in the search. */
    total?: number;
    users?: Array<{ accountId?: string; accountType?: "atlassian" | "app" | "customer" | "unknown"; avatarUrl?: string; displayName?: string; html?: string; key?: string; name?: string }>;
}

/** List of users and groups found in a search. */
export interface FoundUsersAndGroups {
    /** The list of groups found in a search, including header text (Showing X of Y matching groups) and total of matched groups. */
    groups?: { groups?: Array<{ avatarUrl?: string; groupId?: string; html?: string; labels?: Array<{ text?: string; title?: string; type?: "ADMIN" | "SINGLE" | "MULTIPLE" }>; managedBy?: "EXTERNAL" | "ADMINS" | "TEAM_MEMBERS" | "OPEN"; name?: string; usageType?: "USERBASE_GROUP" | "TEAM_COLLABORATION" | "ADMIN_OVERSIGHT" }>; header?: string; total?: number };
    /** The list of users found in a search, including header text (Showing X of Y matching users) and total of matched users. */
    users?: { header?: string; total?: number; users?: Array<{ accountId?: string; accountType?: "atlassian" | "app" | "customer" | "unknown"; avatarUrl?: string; displayName?: string; html?: string; key?: string; name?: string }> };
}

/** The payload for the layout details for the start end of a transition */
export interface FromLayoutPayload {
    /** The port that the transition can be made from */
    fromPort?: number;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    status?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The port that the transition goes to */
    toPortOverride?: number;
}

/** An operand that is a function. See [Advanced searching - functions reference](https://confluence.atlassian.com/x/dwiiLQ) for more information about JQL functions. */
export interface FunctionOperand {
    /** The list of function arguments. */
    arguments: Array<string>;
    /** Encoded operand, which can be used directly in a JQL query. */
    encodedOperand?: string;
    /** The name of the function. */
    function: string;
}

/** Details of functions that can be used in advanced searches. */
export interface FunctionReferenceData {
    /** The display name of the function. */
    displayName?: string;
    /** Whether the function can take a list of arguments. */
    isList?: "true" | "false";
    /** Whether the function supports both single and list value operators. */
    supportsListAndSingleValueOperators?: "true" | "false";
    /** The data types returned by the function. */
    types?: Array<string>;
    /** The function identifier. */
    value?: string;
}

export interface GetAtlassianTeamResponse {
    /** The capacity for the Atlassian team. */
    capacity?: number;
    /** The Atlassian team ID. */
    id: string;
    /** The ID of the issue source for the Atlassian team. */
    issueSourceId?: number;
    /** The planning style for the Atlassian team. This is "Scrum" or "Kanban". */
    planningStyle: "Scrum" | "Kanban";
    /** The sprint length for the Atlassian team. */
    sprintLength?: number;
}

export interface GetCrossProjectReleaseResponse {
    /** The cross-project release name. */
    name?: string;
    /** The IDs of the releases included in the cross-project release. */
    releaseIds?: Array<number>;
}

export interface GetCustomFieldResponse {
    /** The custom field ID. */
    customFieldId: number;
    /** Allows filtering issues based on their values for the custom field. */
    filter?: boolean;
}

export interface GetDateFieldResponse {
    /** A date custom field ID. This is returned if the type is "DateCustomField". */
    dateCustomFieldId?: number;
    /** The date field type. This is "DueDate", "TargetStartDate", "TargetEndDate" or "DateCustomField". */
    type: "DueDate" | "TargetStartDate" | "TargetEndDate" | "DateCustomField";
}

export interface GetExclusionRulesResponse {
    /** The IDs of the issues excluded from the plan. */
    issueIds?: Array<number>;
    /** The IDs of the issue types excluded from the plan. */
    issueTypeIds?: Array<number>;
    /** Issues completed this number of days ago are excluded from the plan. */
    numberOfDaysToShowCompletedIssues: number;
    /** The IDs of the releases excluded from the plan. */
    releaseIds?: Array<number>;
    /** The IDs of the work status categories excluded from the plan. */
    workStatusCategoryIds?: Array<number>;
    /** The IDs of the work statuses excluded from the plan. */
    workStatusIds?: Array<number>;
}

/** Response object for getting field association parameters. */
export interface GetFieldAssociationParametersResponse {
    fieldId: string;
    parameters?: { description?: string; isRequired: boolean };
    workTypeParameters?: Array<{ description?: string; isRequired: boolean; workTypeId: number }>;
}

/** Response object for getting a field association scheme by ID. */
export interface GetFieldAssociationSchemeByIdResponse {
    description?: string;
    id?: string;
    isDefault?: boolean;
    links?: { associations?: string; projects?: string };
    name?: string;
}

/** Response object for getting a field association scheme. */
export interface GetFieldAssociationSchemeResponse {
    description?: string;
    id?: number;
    isDefault?: boolean;
    links?: { associations?: string; projects?: string };
    /** Matched filters for field association scheme search. */
    matchedFilters?: { projectIds?: Array<number>; query?: string };
    name?: string;
}

export interface GetIssueSourceResponse {
    /** The issue source type. This is "Board", "Project" or "Filter". */
    type: "Board" | "Project" | "Filter" | "Custom";
    /** The issue source value. This is a board ID if the type is "Board", a project ID if the type is "Project" or a filter ID if the type is "Filter". */
    value: number;
}

export interface GetPermissionHolderResponse {
    /** The permission holder type. This is "Group" or "AccountId". */
    type: "Group" | "AccountId";
    /** The permission holder value. This is a group name if the type is "Group" or an account ID if the type is "AccountId". */
    value: string;
}

export interface GetPermissionResponse {
    /** The permission holder. */
    holder: { type: "Group" | "AccountId"; value: string };
    /** The permission type. This is "View" or "Edit". */
    type: "View" | "Edit";
}

export interface GetPlanOnlyTeamResponse {
    /** The capacity for the plan-only team. */
    capacity?: number;
    /** The plan-only team ID. */
    id: number;
    /** The ID of the issue source for the plan-only team. */
    issueSourceId?: number;
    /** The account IDs of the plan-only team members. */
    memberAccountIds?: Array<string>;
    /** The plan-only team name. */
    name: string;
    /** The planning style for the plan-only team. This is "Scrum" or "Kanban". */
    planningStyle: "Scrum" | "Kanban";
    /** The sprint length for the plan-only team. */
    sprintLength?: number;
}

export interface GetPlanResponse {
    /** The cross-project releases included in the plan. */
    crossProjectReleases?: Array<{ name?: string; releaseIds?: Array<number> }>;
    /** The custom fields for the plan. */
    customFields?: Array<{ customFieldId: number; filter?: boolean }>;
    /** The exclusion rules for the plan. */
    exclusionRules?: { issueIds?: Array<number>; issueTypeIds?: Array<number>; numberOfDaysToShowCompletedIssues: number; releaseIds?: Array<number>; workStatusCategoryIds?: Array<number>; workStatusIds?: Array<number> };
    /** The plan ID. */
    id: number;
    /** The issue sources included in the plan. */
    issueSources?: Array<{ type: "Board" | "Project" | "Filter" | "Custom"; value: number }>;
    /** The date when the plan was last saved in UTC. */
    lastSaved?: string;
    /** The account ID of the plan lead. */
    leadAccountId?: string;
    /** The plan name. */
    name?: string;
    /** The permissions for the plan. */
    permissions?: Array<{ holder: { type: "Group" | "AccountId"; value: string }; type: "View" | "Edit" }>;
    /** The scheduling settings for the plan. */
    scheduling: { dependencies: "Sequential" | "Concurrent"; endDate: { dateCustomFieldId?: number; type: "DueDate" | "TargetStartDate" | "TargetEndDate" | "DateCustomField" }; estimation: "StoryPoints" | "Days" | "Hours"; inferredDates: "None" | "SprintDates" | "ReleaseDates"; startDate: unknown };
    /** The plan status. This is "Active", "Trashed" or "Archived". */
    status: "Active" | "Trashed" | "Archived";
}

export interface GetPlanResponseForPage {
    /** The plan ID. */
    id: string;
    /** The issue sources included in the plan. */
    issueSources?: Array<{ type: "Board" | "Project" | "Filter" | "Custom"; value: number }>;
    /** The plan name. */
    name: string;
    /** Default scenario ID. */
    scenarioId: string;
    /** The plan status. This is "Active", "Trashed" or "Archived". */
    status: "Active" | "Trashed" | "Archived";
}

/** Response item returned from get projects with field schemes. */
export interface GetProjectsWithFieldSchemesResponse {
    projectId?: number;
    schemeId?: number;
}

export interface GetSchedulingResponse {
    /** The dependencies for the plan. This is "Sequential" or "Concurrent". */
    dependencies: "Sequential" | "Concurrent";
    /** The end date field for the plan. */
    endDate: { dateCustomFieldId?: number; type: "DueDate" | "TargetStartDate" | "TargetEndDate" | "DateCustomField" };
    /** The estimation unit for the plan. This is "StoryPoints", "Days" or "Hours". */
    estimation: "StoryPoints" | "Days" | "Hours";
    /** The inferred dates for the plan. This is "None", "SprintDates" or "ReleaseDates". */
    inferredDates: "None" | "SprintDates" | "ReleaseDates";
    /** The start date field for the plan. */
    startDate: unknown;
}

export interface GetTeamResponseForPage {
    /** The team ID. */
    id: string;
    /** The team name. This is returned if the type is "PlanOnly". */
    name?: string;
    /** The team type. This is "PlanOnly" or "Atlassian". */
    type: "PlanOnly" | "Atlassian";
}

export interface GlobalScopeBean {
    /** Defines the behavior of the option in the global context.If notSelectable is set, the option cannot be set as the field's value. This is useful for archiving an option that has previously been selected but shouldn't be used anymore.If defaultValue is set, the option is selected by default. */
    attributes?: Array<"notSelectable" | "defaultValue">;
}

export interface Group {
    /** Expand options that include additional group details in the response. */
    readonly expand?: string;
    /** The ID of the group, which uniquely identifies the group across all Atlassian products. For example, *952d12c3-5b5b-4d04-bb32-44d383afc4b2*. */
    groupId?: string;
    /** The name of group. */
    name?: string;
    /** The URL for these group details. */
    readonly self?: string;
    /** A paginated list of the users that are members of the group. A maximum of 50 users is returned in the list, to access additional users append `[start-index:end-index]` to the expand request. For example, to access the next 50 users, use`?expand=users[51:100]`. */
    readonly users?: { readonly end-index?: number; readonly items?: Array<{ accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }>; readonly max-results?: number; readonly size?: number; readonly start-index?: number };

/** Details about a group. */
export interface GroupDetails {
    /** The ID of the group, which uniquely identifies the group across all Atlassian products. For example, *952d12c3-5b5b-4d04-bb32-44d383afc4b2*. */
    groupId?: string;
    /** The name of the group. */
    name?: string;
}

/** A group label. */
export interface GroupLabel {
    /** The group label name. */
    text?: string;
    /** The title of the group label. */
    title?: string;
    /** The type of the group label. */
    type?: "ADMIN" | "SINGLE" | "MULTIPLE";
}

/** Details about a group. */
export interface GroupName {
    /** The ID of the group, which uniquely identifies the group across all Atlassian products. For example, *952d12c3-5b5b-4d04-bb32-44d383afc4b2*. */
    groupId?: string;
    /** The name of group. */
    name?: string;
    /** The URL for these group details. */
    readonly self?: string;
}

/** Jira instance health check results. Deprecated and no longer returned. */
export interface HealthCheckResult {
    /** The description of the Jira health check item. */
    description?: string;
    /** The name of the Jira health check item. */
    name?: string;
    /** Whether the Jira health check item passed or failed. */
    passed?: boolean;
}

/** The project issue type hierarchy. */
export interface Hierarchy {
    /** The ID of the base level. This property is deprecated, see [Change notice: Removing hierarchy level IDs from next-gen APIs](https://developer.atlassian.com/cloud/jira/platform/change-notice-removing-hierarchy-level-ids-from-next-gen-apis/). */
    baseLevelId?: number;
    /** Details about the hierarchy level. */
    readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }>;
}

/** Details of issue history metadata. */
export type HistoryMetadata = Record<string, unknown>;
/** Details of user or system associated with a issue history metadata item. */
export type HistoryMetadataParticipant = Record<string, unknown>;
/**
 * An icon. If no icon is defined:
 *
 *  *  for a status icon, no status icon displays in Jira.
 *  *  for the remote object icon, the default link icon displays in Jira.
 */
export type Icon = Record<string, unknown>;

/** An icon. */
export interface IconBean {
    /** The URL of the tooltip, used only for a status icon. */
    link?: string;
    /** The title of the icon, for use as a tooltip on the icon. */
    title?: string;
    /** The URL of a 16x16 pixel icon. */
    url16x16?: string;
}

export interface IdBean {
    /** The ID of the permission scheme to associate with the project. Use the [Get all permission schemes](#api-rest-api-3-permissionscheme-get) resource to get a list of permission scheme IDs. */
    id: number;
}

export interface IdOrKeyBean {
    /** The ID of the referenced item. */
    id?: number;
    /** The key of the referenced item. */
    key?: string;
}

export interface IncludedFields {
    actuallyIncluded?: Array<string>;
    excluded?: Array<string>;
    included?: Array<string>;
}

export interface InputStreamSource {
    inputStream?: Record<string, unknown>;
}

/** List of Issue Ids Or Keys that are to be archived or unarchived */
export interface IssueArchivalSyncRequest {
    issueIdsOrKeys?: Array<string>;
}

/** Number of archived/unarchived issues and list of errors that occurred during the action, if any. */
export interface IssueArchivalSyncResponse {
    errors?: { issueIsSubtask?: { count?: number; issueIdsOrKeys?: Array<string>; message?: string }; issuesInArchivedProjects?: unknown; issuesInUnlicensedProjects?: unknown; issuesNotFound?: unknown; userDoesNotHavePermission?: unknown };
    numberOfIssuesUpdated?: number;
}

/** Details about an issue. */
export interface IssueBean {
    /** Details of changelogs associated with the issue. */
    readonly changelog?: { readonly histories?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>; readonly maxResults?: number; readonly startAt?: number; readonly total?: number };
    /** The metadata for the fields on the issue that can be amended. */
    readonly editmeta?: { readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }> };
    /** Expand options that include additional issue details in the response. */
    readonly expand?: string;
    fields?: Record<string, Record<string, unknown>>;
    fieldsToInclude?: { actuallyIncluded?: Array<string>; excluded?: Array<string>; included?: Array<string> };
    /** The ID of the issue. */
    readonly id?: string;
    /** The key of the issue. */
    readonly key?: string;
    /** The ID and name of each field present on the issue. */
    readonly names?: Record<string, string>;
    /** The operations that can be performed on the issue. */
    readonly operations?: Record<string, unknown>;
    /** Details of the issue properties identified in the request. */
    readonly properties?: Record<string, Record<string, unknown>>;
    /** The rendered value of each field present on the issue. */
    readonly renderedFields?: Record<string, Record<string, unknown>>;
    /** The schema describing each field present on the issue. */
    readonly schema?: Record<string, unknown>;
    /** The URL of the issue details. */
    readonly self?: string;
    /** The transitions that can be performed on the issue. */
    readonly transitions?: Array<Record<string, unknown>>;
    /** The versions of each field on the issue. */
    readonly versionedRepresentations?: Record<string, Record<string, Record<string, unknown>>>;

/** Issue Bulk Delete Payload */
export interface IssueBulkDeletePayload {
    /** List of issue IDs or keys which are to be bulk deleted. These IDs or keys can be from different projects and issue types. */
    selectedIssueIdsOrKeys: Array<string>;
    /**
     * A boolean value that indicates whether to send a bulk change notification when the issues are being deleted.
     *
     * If `true`, dispatches a bulk notification email to users about the updates.
     */
    sendBulkNotification?: boolean;
}

export interface IssueBulkEditField {
    /** Description of the field. */
    description?: string;
    /** A list of options related to the field, applicable in contexts where multiple selections are allowed. */
    fieldOptions?: Array<Record<string, unknown>>;
    /** The unique ID of the field. */
    id?: string;
    /** Indicates whether the field is mandatory for the operation. */
    isRequired?: boolean;
    /** Specifies supported actions (like add, replace, remove) on multi-select fields via an enum. */
    multiSelectFieldOptions?: Array<"ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL">;
    /** The display name of the field. */
    name?: string;
    /** A URL to fetch additional data for the field */
    searchUrl?: string;
    /** The type of the field. */
    type?: string;
    /** A message indicating why the field is unavailable for editing. */
    unavailableMessage?: string;
}

/** Issue Bulk Edit Payload */
export interface IssueBulkEditPayload {
    /** An object that defines the values to be updated in specified fields of an issue. The structure and content of this parameter vary depending on the type of field being edited. Although the order is not significant, ensure that field IDs align with those in selectedActions. */
    editedFieldsInput: { cascadingSelectFields?: Array<{ childOptionValue?: { optionId?: number }; fieldId: string; parentOptionValue: unknown }>; clearableNumberFields?: Array<{ fieldId: string; value?: number }>; colorFields?: Array<{ color: { name: string }; fieldId: string }>; datePickerFields?: Array<{ date?: { formattedDate: string }; fieldId: string }>; dateTimePickerFields?: Array<{ dateTime: { formattedDateTime: string }; fieldId: string }>; issueType?: { issueTypeId: string }; labelsFields?: Array<{ bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL"; fieldId: string; labelProperties?: Array<{ color?: "GREY_LIGHTEST" | "GREY_LIGHTER" | "GREY" | "GREY_DARKER" | "GREY_DARKEST" | "PURPLE_LIGHTEST" | "PURPLE_LIGHTER" | "PURPLE" | "PURPLE_DARKER" | "PURPLE_DARKEST" | "BLUE_LIGHTEST" | "BLUE_LIGHTER" | "BLUE" | "BLUE_DARKER" | "BLUE_DARKEST" | "TEAL_LIGHTEST" | "TEAL_LIGHTER" | "TEAL" | "TEAL_DARKER" | "TEAL_DARKEST" | "GREEN_LIGHTEST" | "GREEN_LIGHTER" | "GREEN" | "GREEN_DARKER" | "GREEN_DARKEST" | "LIME_LIGHTEST" | "LIME_LIGHTER" | "LIME" | "LIME_DARKER" | "LIME_DARKEST" | "YELLOW_LIGHTEST" | "YELLOW_LIGHTER" | "YELLOW" | "YELLOW_DARKER" | "YELLOW_DARKEST" | "ORANGE_LIGHTEST" | "ORANGE_LIGHTER" | "ORANGE" | "ORANGE_DARKER" | "ORANGE_DARKEST" | "RED_LIGHTEST" | "RED_LIGHTER" | "RED" | "RED_DARKER" | "RED_DARKEST" | "MAGENTA_LIGHTEST" | "MAGENTA_LIGHTER" | "MAGENTA" | "MAGENTA_DARKER" | "MAGENTA_DARKEST"; name?: string }>; labels: Array<{ name: string }> }>; multipleGroupPickerFields?: Array<{ fieldId: string; groups: Array<{ groupName: string }> }>; multipleSelectClearableUserPickerFields?: Array<{ fieldId: string; users?: Array<{ accountId: string }> }>; multipleSelectFields?: Array<{ fieldId: string; options: Array<unknown> }>; multipleVersionPickerFields?: Array<{ bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL"; fieldId: string; versions: Array<{ versionId?: string }> }>; multiselectComponents?: { bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL"; components: Array<{ componentId: number }>; fieldId: string }; originalEstimateField?: { originalEstimateField: string }; priority?: { priorityId: string }; richTextFields?: Array<{ fieldId: string; richText: { adfValue?: Record<string, Record<string, unknown>> } }>; singleGroupPickerFields?: Array<{ fieldId: string; group: unknown }>; singleLineTextFields?: Array<{ fieldId: string; text: string }>; singleSelectClearableUserPickerFields?: Array<{ fieldId: string; user?: unknown }>; singleSelectFields?: Array<{ fieldId: string; option: unknown }>; singleVersionPickerFields?: Array<{ fieldId: string; version: unknown }>; status?: { statusId: string }; timeTrackingField?: { timeRemaining: string }; urlFields?: Array<{ fieldId: string; url: string }> };
    /** List of all the field IDs that are to be bulk edited. Each field ID in this list corresponds to a specific attribute of an issue that is set to be modified in the bulk edit operation. The relevant field ID can be obtained by calling the Bulk Edit Get Fields REST API (documentation available on this page itself). */
    selectedActions: Array<string>;
    /** List of issue IDs or keys which are to be bulk edited. These IDs or keys can be from different projects and issue types. */
    selectedIssueIdsOrKeys: Array<string>;
    /**
     * A boolean value that indicates whether to send a bulk change notification when the issues are being edited.
     *
     * If `true`, dispatches a bulk notification email to users about the updates.
     */
    sendBulkNotification?: boolean;
}

/** Issue Bulk Move Payload */
export interface IssueBulkMovePayload {
    /**
     * A boolean value that indicates whether to send a bulk change notification when the issues are being moved.
     *
     * If `true`, dispatches a bulk notification email to users about the updates.
     */
    sendBulkNotification?: boolean;
    /**
     * An object representing the mapping of issues and data related to destination entities, like fields and statuses, that are required during a bulk move.
     *
     * The key is a string that is created by concatenating the following three entities in order, separated by commas. The format is `<project ID or key>,<issueType ID>,<parent ID or key>`. It should be unique across mappings provided in the payload. If you provide multiple mappings for the same key, only one will be processed. However, the operation won't fail, so the error may be hard to track down.
     *
     *  *  ***Destination project*** (Required): ID or key of the project to which the issues are being moved.
     *  *  ***Destination issueType*** (Required): ID of the issueType to which the issues are being moved.
     *  *  ***Destination parent ID or key*** (Optional): ID or key of the issue which will become the parent of the issues being moved. Only required when the destination issueType is a subtask.
     */
    targetToSourcesMapping?: Record<string, { inferClassificationDefaults: boolean; inferFieldDefaults: boolean; inferStatusDefaults: boolean; inferSubtaskTypeDefault: boolean; issueIdsOrKeys?: Array<string>; targetClassification?: Array<{ classifications: Record<string, Array<string>>; issueType?: string; projectKeyOrId?: string }>; targetMandatoryFields?: Array<{ fields: Record<string, { retain?: boolean; type?: "adf" | "raw"; value: Array<string> } | { retain?: boolean; type: "adf" | "raw"; value: Record<string, unknown> }> }>; targetStatus?: Array<{ statuses: Record<string, Array<string>> }> }>;
}

export interface IssueBulkOperationsFieldOption {
}

export interface IssueBulkTransitionForWorkflow {
    /** Indicates whether all the transitions of this workflow are available in the transitions list or not. */
    readonly isTransitionsFiltered?: boolean;
    /** List of issue keys from the request which are associated with this workflow. */
    readonly issues?: Array<string>;
    /**
     * List of transitions available for issues from the request which are associated with this workflow.
     *
     *  **This list includes only those transitions that are common across the issues in this workflow and do not involve any additional field updates.** 
     */
    readonly transitions?: Array<{ readonly to?: { readonly statusId?: number; readonly statusName?: string }; readonly transitionId?: number; readonly transitionName?: string }>;
}

/** Issue Bulk Transition Payload */
export interface IssueBulkTransitionPayload {
    /**
     * List of objects and each object has two properties:
     *
     *  *  Issues that will be bulk transitioned.
     *  *  TransitionId that corresponds to a specific transition of issues that share the same workflow.
     */
    bulkTransitionInputs: Array<{ selectedIssueIdsOrKeys: Array<string>; transitionId: string }>;
    /**
     * A boolean value that indicates whether to send a bulk change notification when the issues are being transitioned.
     *
     * If `true`, dispatches a bulk notification email to users about the updates.
     */
    sendBulkNotification?: boolean;
}

/** Issue Bulk Watch Or Unwatch Payload */
export interface IssueBulkWatchOrUnwatchPayload {
    /** List of issue IDs or keys which are to be bulk watched or unwatched. These IDs or keys can be from different projects and issue types. */
    selectedIssueIdsOrKeys: Array<string>;
}

/** List of changelogs that belong to single issue */
export interface IssueChangeLog {
    /** List of changelogs that belongs to given issueId. */
    readonly changeHistories?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>;
    /** The ID of the issue. */
    readonly issueId?: string;

/** A list of changelog IDs. */
export interface IssueChangelogIds {
    /** The list of changelog IDs. */
    changelogIds: Array<number>;
}

export interface IssueCommentListRequestBean {
    /** The list of comment IDs. A maximum of 1000 IDs can be specified. */
    ids: Array<number>;
}

/** An [issue](https://developer.atlassian.com/cloud/jira/platform/jira-expressions-type-reference#issue) specified by ID or key. All the fields of the issue object are available in the Jira expression. */
export interface IssueContextVariable {
    /** The issue ID. */
    id?: number;
    /** The issue key. */
    key?: string;
    /** Type of custom context variable. */
    type: string;
}

/** The wrapper for the issue creation metadata for a list of projects. */
export interface IssueCreateMetadata {
    /** Expand options that include additional project details in the response. */
    readonly expand?: string;
    /** List of projects and their issue creation metadata. */
    readonly projects?: Array<{ readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly expand?: string; readonly id?: string; readonly issuetypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly expand?: string; readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }>; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly name?: string; readonly self?: string }>;

/** Lists of issues and entity properties. See [Entity properties](https://developer.atlassian.com/cloud/jira/platform/jira-entity-properties/) for more information. */
export interface IssueEntityProperties {
    /** A list of entity property IDs. */
    entitiesIds?: Array<number>;
    /** A list of entity property keys and values. */
    properties?: Record<string, { array?: boolean; bigDecimal?: boolean; bigInteger?: boolean; bigIntegerValue?: number; binary?: boolean; binaryValue?: Array<string>; boolean?: boolean; booleanValue?: boolean; containerNode?: boolean; decimalValue?: number; double?: boolean; doubleValue?: number; elements?: Record<string, unknown>; fieldNames?: Record<string, unknown>; fields?: Record<string, unknown>; floatingPointNumber?: boolean; int?: boolean; intValue?: number; integralNumber?: boolean; long?: boolean; longValue?: number; missingNode?: boolean; null?: boolean; number?: boolean; numberType?: "INT" | "LONG" | "BIG_INTEGER" | "FLOAT" | "DOUBLE" | "BIG_DECIMAL"; numberValue?: number; object?: boolean; pojo?: boolean; textValue?: string; textual?: boolean; valueAsBoolean?: boolean; valueAsDouble?: number; valueAsInt?: number; valueAsLong?: number; valueAsText?: string; valueNode?: boolean }>;
}

/** An issue ID with entity property values. See [Entity properties](https://developer.atlassian.com/cloud/jira/platform/jira-entity-properties/) for more information. */
export interface IssueEntityPropertiesForMultiUpdate {
    /** The ID of the issue. */
    issueID?: number;
    /** Entity properties to set on the issue. The maximum length of an issue property value is 32768 characters. */
    properties?: Record<string, { array?: boolean; bigDecimal?: boolean; bigInteger?: boolean; bigIntegerValue?: number; binary?: boolean; binaryValue?: Array<string>; boolean?: boolean; booleanValue?: boolean; containerNode?: boolean; decimalValue?: number; double?: boolean; doubleValue?: number; elements?: Record<string, unknown>; fieldNames?: Record<string, unknown>; fields?: Record<string, unknown>; floatingPointNumber?: boolean; int?: boolean; intValue?: number; integralNumber?: boolean; long?: boolean; longValue?: number; missingNode?: boolean; null?: boolean; number?: boolean; numberType?: "INT" | "LONG" | "BIG_INTEGER" | "FLOAT" | "DOUBLE" | "BIG_DECIMAL"; numberValue?: number; object?: boolean; pojo?: boolean; textValue?: string; textual?: boolean; valueAsBoolean?: boolean; valueAsDouble?: number; valueAsInt?: number; valueAsLong?: number; valueAsText?: string; valueNode?: boolean }>;
}

/** Describes the error that occurred when retrieving data for a particular issue. */
export interface IssueError {
    /** The error that occurred when fetching this issue. */
    readonly errorMessage?: string;
    /** The ID of the issue. */
    readonly id?: string;
}

/** Details about an issue event. */
export interface IssueEvent {
    /** The ID of the event. */
    readonly id?: number;
    /** The name of the event. */
    readonly name?: string;
}

/** Details of the options for a select list issue field. */
export interface IssueFieldOption {
    /** Details of the projects the option is available in. */
    config?: { attributes?: Array<"notSelectable" | "defaultValue">; scope?: { global?: { attributes?: Array<"notSelectable" | "defaultValue"> }; projects?: Array<number>; projects2?: Array<{ attributes?: Array<"notSelectable" | "defaultValue">; id?: number }> } };
    /** The unique identifier for the option. This is only unique within the select field's set of options. */
    id: number;
    /** The properties of the object, as arbitrary key-value pairs. These properties can be searched using JQL, if the extractions (see [Issue Field Option Property Index](https://developer.atlassian.com/cloud/jira/platform/modules/issue-field-option-property-index/)) are defined in the descriptor for the issue field module. */
    properties?: Record<string, Record<string, unknown>>;
    /** The option's name, which is displayed in Jira. */
    value: string;
}

/** Details of the projects the option is available in. */
export interface IssueFieldOptionConfiguration {
    /** DEPRECATED */
    attributes?: Array<"notSelectable" | "defaultValue">;
    /** Defines the projects that the option is available in. If the scope is not defined, then the option is available in all projects. */
    scope?: { global?: { attributes?: Array<"notSelectable" | "defaultValue"> }; projects?: Array<number>; projects2?: Array<{ attributes?: Array<"notSelectable" | "defaultValue">; id?: number }> };
}

export type IssueFieldOptionCreateBean = Record<string, unknown>;

export interface IssueFieldOptionScopeBean {
    /** Defines the behavior of the option within the global context. If this property is set, even if set to an empty object, then the option is available in all projects. */
    global?: { attributes?: Array<"notSelectable" | "defaultValue"> };
    /** DEPRECATED */
    projects?: Array<number>;
    /** Defines the projects in which the option is available and the behavior of the option within each project. Specify one object per project. The behavior of the option in a project context overrides the behavior in the global context. */
    projects2?: Array<{ attributes?: Array<"notSelectable" | "defaultValue">; id?: number }>;
}

/** Bulk operation filter details. */
export interface IssueFilterForBulkPropertyDelete {
    /** The value of properties to perform the bulk operation on. */
    currentValue?: Record<string, unknown>;
    /** List of issues to perform the bulk delete operation on. */
    entityIds?: Array<number>;
}

/** Bulk operation filter details. */
export interface IssueFilterForBulkPropertySet {
    /** The value of properties to perform the bulk operation on. */
    currentValue?: Record<string, unknown>;
    /** List of issues to perform the bulk operation on. */
    entityIds?: Array<number>;
    /** Whether the bulk operation occurs only when the property is present on or absent from an issue. */
    hasProperty?: boolean;
}

/** Defines the payload to configure the issue layout item for a project. */
export interface IssueLayoutItemPayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    itemKey?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** Additional properties for this item. This field is only used when the type is FIELD. */
    properties?: Record<string, Record<string, unknown>>;
    /** The item section type */
    sectionType?: "content" | "primaryContext" | "secondaryContext";
    /** The item type. Currently only support FIELD */
    type?: "FIELD";
}

/** Defines the payload to configure the issue layouts for a project. */
export interface IssueLayoutPayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    containerId?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The issue layout type */
    issueLayoutType?: "ISSUE_VIEW" | "ISSUE_CREATE" | "REQUEST_FORM";
    /** The configuration of items in the issue layout */
    items?: Array<{ itemKey?: unknown; properties?: Record<string, Record<string, unknown>>; sectionType?: "content" | "primaryContext" | "secondaryContext"; type?: "FIELD" }>;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

export interface IssueLimitReportResponseBean {
    /** A list of ids of issues approaching the limit and their field count */
    issuesApproachingLimit?: Record<string, Record<string, number>>;
    /** A list of ids of issues breaching the limit and their field count */
    issuesBreachingLimit?: Record<string, Record<string, number>>;
    /** The fields and their defined limits */
    limits?: Record<string, number>;
}

/** Details of a link between issues. */
export interface IssueLink {
    /** The ID of the issue link. */
    readonly id?: string;
    /** Provides details about the linked issue. If presenting this link in a user interface, use the `inward` field of the issue link type to label the link. */
    inwardIssue: { readonly fields?: { readonly assignee?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly issueType?: { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }; issuetype?: { readonly avatarId?: unknown; readonly description?: unknown; readonly entityId?: unknown; readonly hierarchyLevel?: unknown; readonly iconUrl?: unknown; readonly id?: unknown; readonly name?: unknown; readonly scope?: unknown; readonly self?: unknown; readonly subtask?: unknown }; readonly priority?: Record<string, unknown>; readonly status?: Record<string, unknown>; readonly summary?: string; readonly timetracking?: { readonly originalEstimate?: string; readonly originalEstimateSeconds?: number; readonly remainingEstimate?: string; readonly remainingEstimateSeconds?: number; readonly timeSpent?: string; readonly timeSpentSeconds?: number } }; id?: string; key?: string; readonly self?: string };
    /** Provides details about the linked issue. If presenting this link in a user interface, use the `outward` field of the issue link type to label the link. */
    outwardIssue: unknown;
    /** The URL of the issue link. */
    readonly self?: string;
    /** The type of link between the issues. */
    type: { id?: string; inward?: string; name?: string; outward?: string; readonly self?: string };

/**
 * This object is used as follows:
 *
 *  *  In the [ issueLink](#api-rest-api-3-issueLink-post) resource it defines and reports on the type of link between the issues. Find a list of issue link types with [Get issue link types](#api-rest-api-3-issueLinkType-get).
 *  *  In the [ issueLinkType](#api-rest-api-3-issueLinkType-post) resource it defines and reports on issue link types.
 */
export interface IssueLinkType {
    /**
     * The ID of the issue link type and is used as follows:
     *
     *  *  In the [ issueLink](#api-rest-api-3-issueLink-post) resource it is the type of issue link. Required on create when `name` isn't provided. Otherwise, read only.
     *  *  In the [ issueLinkType](#api-rest-api-3-issueLinkType-post) resource it is read only.
     */
    id?: string;
    /**
     * The description of the issue link type inward link and is used as follows:
     *
     *  *  In the [ issueLink](#api-rest-api-3-issueLink-post) resource it is read only.
     *  *  In the [ issueLinkType](#api-rest-api-3-issueLinkType-post) resource it is required on create and optional on update. Otherwise, read only.
     */
    inward?: string;
    /**
     * The name of the issue link type and is used as follows:
     *
     *  *  In the [ issueLink](#api-rest-api-3-issueLink-post) resource it is the type of issue link. Required on create when `id` isn't provided. Otherwise, read only.
     *  *  In the [ issueLinkType](#api-rest-api-3-issueLinkType-post) resource it is required on create and optional on update. Otherwise, read only.
     */
    name?: string;
    /**
     * The description of the issue link type outward link and is used as follows:
     *
     *  *  In the [ issueLink](#api-rest-api-3-issueLink-post) resource it is read only.
     *  *  In the [ issueLinkType](#api-rest-api-3-issueLinkType-post) resource it is required on create and optional on update. Otherwise, read only.
     */
    outward?: string;
    /** The URL of the issue link type. Read only. */
    readonly self?: string;
}

/** A list of issue link type beans. */
export interface IssueLinkTypes {
    /** The issue link type bean. */
    readonly issueLinkTypes?: Array<{ id?: string; inward?: string; name?: string; outward?: string; readonly self?: string }>;
}

/** A list of issue IDs. */
export interface IssueList {
    /** The list of issue IDs. */
    issueIds: Array<string>;
}

/** A list of matched issues or errors for each JQL query, in the order the JQL queries were passed. */
export interface IssueMatches {
    matches: Array<{ errors: Array<string>; matchedIssues: Array<number> }>;
}

/** A list of the issues matched to a JQL query or details of errors encountered during matching. */
export interface IssueMatchesForJQL {
    /** A list of errors. */
    errors: Array<string>;
    /** A list of issue IDs. */
    matchedIssues: Array<number>;
}

/** A list of issues suggested for use in auto-completion. */
export interface IssuePickerSuggestions {
    /** A list of issues for an issue type suggested for use in auto-completion. */
    readonly sections?: Array<{ readonly id?: string; readonly issues?: Array<{ readonly id?: number; readonly img?: string; readonly key?: string; readonly keyHtml?: string; readonly summary?: string; readonly summaryText?: string }>; readonly label?: string; readonly msg?: string; readonly sub?: string }>;
}

/** A type of issue suggested for use in auto-completion. */
export interface IssuePickerSuggestionsIssueType {
    /** The ID of the type of issues suggested for use in auto-completion. */
    readonly id?: string;
    /** A list of issues suggested for use in auto-completion. */
    readonly issues?: Array<{ readonly id?: number; readonly img?: string; readonly key?: string; readonly keyHtml?: string; readonly summary?: string; readonly summaryText?: string }>;
    /** The label of the type of issues suggested for use in auto-completion. */
    readonly label?: string;
    /** If no issue suggestions are found, returns a message indicating no suggestions were found, */
    readonly msg?: string;
    /** If issue suggestions are found, returns a message indicating the number of issues suggestions found and returned. */
    readonly sub?: string;
}

/** Issue security level member. */
export interface IssueSecurityLevelMember {
    /** The user or group being granted the permission. It consists of a `type` and a type-dependent `parameter`. See [Holder object](../api-group-permission-schemes/#holder-object) in *Get all permission schemes* for more information. */
    holder: { readonly expand?: string; parameter?: string; type: string; value?: string };
    /** The ID of the issue security level member. */
    id: number;
    /** The ID of the issue security level. */
    issueSecurityLevelId: number;
}

/** Details about an project using security scheme mapping. */
export type IssueSecuritySchemeToProjectMapping = Record<string, unknown>;
/** Details of an issue transition. */
export type IssueTransition = Record<string, unknown>;

export interface IssueTransitionStatus {
    /** The unique ID of the status. */
    readonly statusId?: number;
    /** The name of the status. */
    readonly statusName?: string;
}

export interface IssueTypeCreateBean {
    /** The description of the issue type. */
    description?: string;
    /**
     * The hierarchy level of the issue type. Use:
     *
     *  *  `-1` for Subtask.
     *  *  `0` for Base.
     *
     * Defaults to `0`.
     */
    hierarchyLevel?: number;
    /** The unique name for the issue type. The maximum length is 60 characters. */
    name: string;
    /**
     * Deprecated. Use `hierarchyLevel` instead. See the [deprecation notice](https://community.developer.atlassian.com/t/deprecation-of-the-epic-link-parent-link-and-other-related-fields-in-rest-apis-and-webhooks/54048) for details.
     *
     * Whether the issue type is `subtype` or `standard`. Defaults to `standard`.
     */
    type?: "subtask" | "standard";
}

/** Details about an issue type. */
export interface IssueTypeDetails {
    /** The ID of the issue type's avatar. */
    readonly avatarId?: number;
    /** The description of the issue type. */
    readonly description?: string;
    /** Unique ID for next-gen projects. */
    readonly entityId?: string;
    /** Hierarchy level of the issue type. */
    readonly hierarchyLevel?: number;
    /** The URL of the issue type's avatar. */
    readonly iconUrl?: string;
    /** The ID of the issue type. */
    readonly id?: string;
    /** The name of the issue type. */
    readonly name?: string;
    /** Details of the next-gen projects the issue type is available in. */
    readonly scope?: Record<string, unknown>;
    /** The URL of these issue type details. */
    readonly self?: string;
    /** Whether this issue type is used to create subtasks. */
    readonly subtask?: boolean;
}

/** The payload for creating an issue type hierarchy */
export interface IssueTypeHierarchyPayload {
    /** The hierarchy level of the issue type. 0, 1, 2, 3 .. n; Negative values for subtasks */
    hierarchyLevel?: number;
    /** The name of the issue type */
    name?: string;
    /** The conflict strategy to use when the issue type already exists. FAIL - Fail execution, this always needs to be unique; USE - Use the existing entity and ignore new entity parameters */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
}

/** The list of issue type IDs. */
export interface IssueTypeIds {
    /** The list of issue type IDs. */
    issueTypeIds: Array<string>;
}

/** The list of issue type IDs to be removed from the field configuration scheme. */
export interface IssueTypeIdsToRemove {
    /** The list of issue type IDs. Must contain unique values not longer than 255 characters and not be empty. Maximum of 100 IDs. */
    issueTypeIds: Array<string>;
}

/** Details of an issue type. */
export interface IssueTypeInfo {
    /** The avatar of the issue type. */
    readonly avatarId?: number;
    /** The ID of the issue type. */
    readonly id?: number;
    /** The name of the issue type. */
    readonly name?: string;
}

/** Details of the issue creation metadata for an issue type. */
export interface IssueTypeIssueCreateMetadata {
    /** The ID of the issue type's avatar. */
    readonly avatarId?: number;
    /** The description of the issue type. */
    readonly description?: string;
    /** Unique ID for next-gen projects. */
    readonly entityId?: string;
    /** Expand options that include additional issue type metadata details in the response. */
    readonly expand?: string;
    /** List of the fields available when creating an issue for the issue type. */
    readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }>;
    /** Hierarchy level of the issue type. */
    readonly hierarchyLevel?: number;
    /** The URL of the issue type's avatar. */
    readonly iconUrl?: string;
    /** The ID of the issue type. */
    readonly id?: string;
    /** The name of the issue type. */
    readonly name?: string;
    /** Details of the next-gen projects the issue type is available in. */
    readonly scope?: Record<string, unknown>;
    /** The URL of these issue type details. */
    readonly self?: string;
    /** Whether this issue type is used to create subtasks. */
    readonly subtask?: boolean;
}

/** The payload for creating an issue type */
export interface IssueTypePayload {
    /** The avatar ID of the issue type. Go to https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-avatars/\#api-rest-api-3-avatar-type-system-get to choose an avatarId existing in Jira */
    avatarId?: number;
    /** The description of the issue type */
    description?: string;
    /** The hierarchy level of the issue type. 0, 1, 2, 3 .. n; Negative values for subtasks */
    hierarchyLevel?: number;
    /** The name of the issue type */
    name?: string;
    /** The conflict strategy to use when the issue type already exists. FAIL - Fail execution, this always needs to be unique; USE - Use the existing entity and ignore new entity parameters */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
}

/** The payload for creating issue types in a project */
export interface IssueTypeProjectCreatePayload {
    /** Defines the issue type hierarhy to be created and used during this project creation. This will only add new levels if there isn't an existing level */
    issueTypeHierarchy?: Array<{ hierarchyLevel?: number; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" } }>;
    /** The payload for creating issue type schemes */
    issueTypeScheme?: { defaultIssueTypeId?: unknown; description?: string; issueTypeIds?: Array<unknown>; name?: string; pcri?: unknown };
    /** Only needed if you want to create issue types, you can otherwise use the ids of issue types in the scheme configuration */
    issueTypes?: Array<{ avatarId?: number; description?: string; hierarchyLevel?: number; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }>;
}

/** Details of an issue type scheme. */
export interface IssueTypeScheme {
    /** The ID of the default issue type of the issue type scheme. */
    defaultIssueTypeId?: string;
    /** The description of the issue type scheme. */
    description?: string;
    /** The ID of the issue type scheme. */
    id: string;
    /** Whether the issue type scheme is the default. */
    isDefault?: boolean;
    /** The name of the issue type scheme. */
    name: string;
}

/** Details of an issue type scheme and its associated issue types. */
export interface IssueTypeSchemeDetails {
    /** The ID of the default issue type of the issue type scheme. This ID must be included in `issueTypeIds`. */
    defaultIssueTypeId?: string;
    /** The description of the issue type scheme. The maximum length is 4000 characters. */
    description?: string;
    /** The list of issue types IDs of the issue type scheme. At least one standard issue type ID is required. */
    issueTypeIds: Array<string>;
    /** The name of the issue type scheme. The name must be unique. The maximum length is 255 characters. */
    name: string;
}

/** The ID of an issue type scheme. */
export interface IssueTypeSchemeID {
    /** The ID of the issue type scheme. */
    readonly issueTypeSchemeId: string;
}

/** Issue type scheme item. */
export interface IssueTypeSchemeMapping {
    /** The ID of the issue type. */
    issueTypeId: string;
    /** The ID of the issue type scheme. */
    issueTypeSchemeId: string;
}

/** The payload for creating issue type schemes */
export interface IssueTypeSchemePayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    defaultIssueTypeId?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The description of the issue type scheme */
    description?: string;
    /** The issue type IDs for the issue type scheme */
    issueTypeIds?: Array<unknown>;
    /** The name of the issue type scheme */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

/** Details of the association between an issue type scheme and project. */
export interface IssueTypeSchemeProjectAssociation {
    /** The ID of the issue type scheme. */
    issueTypeSchemeId: string;
    /** The ID of the project. */
    projectId: string;
}

/** Issue type scheme with a list of the projects that use it. */
export interface IssueTypeSchemeProjects {
    /** Details of an issue type scheme. */
    issueTypeScheme: { defaultIssueTypeId?: string; description?: string; id: string; isDefault?: boolean; name: string };
    /** The IDs of the projects using the issue type scheme. */
    projectIds: Array<string>;
}

/** Details of the name, description, and default issue type for an issue type scheme. */
export interface IssueTypeSchemeUpdateDetails {
    /** The ID of the default issue type of the issue type scheme. */
    defaultIssueTypeId?: string;
    /** The description of the issue type scheme. The maximum length is 4000 characters. */
    description?: string;
    /** The name of the issue type scheme. The name must be unique. The maximum length is 255 characters. */
    name?: string;
}

/** Details of an issue type screen scheme. */
export interface IssueTypeScreenScheme {
    /** The description of the issue type screen scheme. */
    description?: string;
    /** The ID of the issue type screen scheme. */
    id: string;
    /** The name of the issue type screen scheme. */
    name: string;
}

/** The details of an issue type screen scheme. */
export interface IssueTypeScreenSchemeDetails {
    /** The description of the issue type screen scheme. The maximum length is 255 characters. */
    description?: string;
    /** The IDs of the screen schemes for the issue type IDs and *default*. A *default* entry is required to create an issue type screen scheme, it defines the mapping for all issue types without a screen scheme. */
    issueTypeMappings: Array<{ issueTypeId: string; screenSchemeId: string }>;
    /** The name of the issue type screen scheme. The name must be unique. The maximum length is 255 characters. */
    name: string;
}

/** The ID of an issue type screen scheme. */
export interface IssueTypeScreenSchemeId {
    /** The ID of the issue type screen scheme. */
    readonly id: string;
}

/** The screen scheme for an issue type. */
export interface IssueTypeScreenSchemeItem {
    /** The ID of the issue type or *default*. Only issue types used in classic projects are accepted. When creating an issue screen scheme, an entry for *default* must be provided and defines the mapping for all issue types without a screen scheme. Otherwise, a *default* entry can't be provided. */
    issueTypeId: string;
    /** The ID of the issue type screen scheme. */
    issueTypeScreenSchemeId: string;
    /** The ID of the screen scheme. */
    screenSchemeId: string;
}

/** The IDs of the screen schemes for the issue type IDs. */
export interface IssueTypeScreenSchemeMapping {
    /** The ID of the issue type or *default*. Only issue types used in classic projects are accepted. An entry for *default* must be provided and defines the mapping for all issue types without a screen scheme. */
    issueTypeId: string;
    /** The ID of the screen scheme. Only screen schemes used in classic projects are accepted. */
    screenSchemeId: string;
}

/** A list of issue type screen scheme mappings. */
export interface IssueTypeScreenSchemeMappingDetails {
    /** The list of issue type to screen scheme mappings. A *default* entry cannot be specified because a default entry is added when an issue type screen scheme is created. */
    issueTypeMappings: Array<{ issueTypeId: string; screenSchemeId: string }>;
}

/** Defines the payload for the issue type screen schemes. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-type-screen-schemes/\#api-rest-api-3-issuetypescreenscheme-post */
export interface IssueTypeScreenSchemePayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    defaultScreenScheme?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The description of the issue type screen scheme */
    description?: string;
    /** The IDs of the screen schemes for the issue type IDs and default. A default entry is required to create an issue type screen scheme, it defines the mapping for all issue types without a screen scheme. */
    explicitMappings?: Record<string, unknown>;
    /** The name of the issue type screen scheme */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

/** Associated issue type screen scheme and project. */
export interface IssueTypeScreenSchemeProjectAssociation {
    /** The ID of the issue type screen scheme. */
    issueTypeScreenSchemeId?: string;
    /** The ID of the project. */
    projectId?: string;
}

/** Details of an issue type screen scheme. */
export interface IssueTypeScreenSchemeUpdateDetails {
    /** The description of the issue type screen scheme. The maximum length is 255 characters. */
    description?: string;
    /** The name of the issue type screen scheme. The name must be unique. The maximum length is 255 characters. */
    name?: string;
}

/** Issue type screen scheme with a list of the projects that use it. */
export interface IssueTypeScreenSchemesProjects {
    /** Details of an issue type screen scheme. */
    issueTypeScreenScheme: { description?: string; id: string; name: string };
    /** The IDs of the projects using the issue type screen scheme. */
    projectIds: Array<string>;
}

/** Mapping of an issue type to a context. */
export interface IssueTypeToContextMapping {
    /** The ID of the context. */
    contextId: string;
    /** Whether the context is mapped to any issue type. */
    isAnyIssueType?: boolean;
    /** The ID of the issue type. */
    issueTypeId?: string;
}

export interface IssueTypeUpdateBean {
    /**
     * The ID of an issue type avatar. This can be obtained be obtained from the following endpoints:
     *
     *  *  [System issue type avatar IDs only](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-avatars/#api-rest-api-3-avatar-type-system-get)
     *  *  [System and custom issue type avatar IDs](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-avatars/#api-rest-api-3-universal-avatar-type-type-owner-entityid-get)
     */
    avatarId?: number;
    /** The description of the issue type. */
    description?: string;
    /** The unique name for the issue type. The maximum length is 60 characters. */
    name?: string;
}

/** Status details for an issue type. */
export interface IssueTypeWithStatus {
    /** The ID of the issue type. */
    readonly id: string;
    /** The name of the issue type. */
    readonly name: string;
    /** The URL of the issue type's status details. */
    readonly self: string;
    /** List of status details for the issue type. */
    readonly statuses: Array<Record<string, unknown>>;
    /** Whether this issue type represents subtasks. */
    readonly subtask: boolean;
}

/** Details about the mapping between an issue type and a workflow. */
export interface IssueTypeWorkflowMapping {
    /** The ID of the issue type. Not required if updating the issue type-workflow mapping. */
    issueType?: string;
    /** Set to true to create or update the draft of a workflow scheme and update the mapping in the draft, when the workflow scheme cannot be edited. Defaults to `false`. Only applicable when updating the workflow-issue types mapping. */
    updateDraftIfNeeded?: boolean;
    /** The name of the workflow. */
    workflow?: string;
}

/** Details about the mapping between issue types and a workflow. */
export interface IssueTypesWorkflowMapping {
    /** Whether the workflow is the default workflow for the workflow scheme. */
    defaultMapping?: boolean;
    /** The list of issue type IDs. */
    issueTypes?: Array<string>;
    /** Whether a draft workflow scheme is created or updated when updating an active workflow scheme. The draft is updated with the new workflow-issue types mapping. Defaults to `false`. */
    updateDraftIfNeeded?: boolean;
    /** The name of the workflow. Optional if updating the workflow-issue types mapping. */
    workflow?: string;
}

/** Details of an issue update request. */
export type IssueUpdateDetails = Record<string, unknown>;

/** A list of editable field details. */
export interface IssueUpdateMetadata {
    readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }>;
}

/** List of issues and JQL queries. */
export interface IssuesAndJQLQueries {
    /** A list of issue IDs. */
    issueIds: Array<number>;
    /** A list of JQL queries. */
    jqls: Array<string>;
}

/** The description of the page of issues loaded by the provided JQL query. */
export interface IssuesJqlMetaDataBean {
    /** The number of issues that were loaded in this evaluation. */
    count: number;
    /** The maximum number of issues that could be loaded in this evaluation. */
    maxResults: number;
    /** The index of the first issue. */
    startAt: number;
    /** The total number of issues the JQL returned. */
    totalCount: number;
    /** Any warnings related to the JQL query. Present only if the validation mode was set to `warn`. */
    validationWarnings?: Array<string>;
}

/** Meta data describing the `issues` context variable. */
export interface IssuesMetaBean {
    /** The description of the page of issues loaded by the provided JQL query. */
    jql?: { count: number; maxResults: number; startAt: number; totalCount: number; validationWarnings?: Array<string> };
}

export type IssuesUpdateBean = Record<string, unknown>;

/** The description of the page of issues loaded by the provided JQL query.This bean will be replacing IssuesJqlMetaDataBean bean as part of new `evaluate` endpoint */
export interface JExpEvaluateIssuesJqlMetaDataBean {
    /** Indicates whether this is the last page of the paginated response. */
    readonly isLast?: boolean;
    /** Next Page token for the next page of issues. */
    nextPageToken: string;
}

/** Meta data describing the `issues` context variable.This bean will be replacing IssuesMetaBean bean as part of new `evaluate` endpoint */
export interface JExpEvaluateIssuesMetaBean {
    /** The description of the page of issues loaded by the provided JQL query.This bean will be replacing IssuesJqlMetaDataBean bean as part of new `evaluate` endpoint */
    jql?: { readonly isLast?: boolean; nextPageToken: string };
}

/** The result of evaluating a Jira expression.This bean will be replacing `JiraExpressionResultBean` bean as part of new evaluate endpoint */
export interface JExpEvaluateJiraExpressionResultBean {
    /** Contains various characteristics of the performed expression evaluation. */
    meta?: { complexity?: { beans: { limit: number; value: number }; expensiveOperations: unknown; primitiveValues: unknown; steps: unknown }; issues?: { jql?: { readonly isLast?: boolean; nextPageToken: string } } };
    /** The value of the evaluated expression. It may be a primitive JSON value or a Jira REST API object. (Some expressions do not produce any meaningful results—for example, an expression that returns a lambda function—if that's the case a simple string representation is returned. These string representations should not be relied upon and may change without notice.) */
    value: Record<string, unknown>;
}

/** Contains information about the expression evaluation. This bean will be replacing `JiraExpressionEvaluationMetaDataBean` bean as part of new `evaluate` endpoint */
export interface JExpEvaluateMetaDataBean {
    /** Contains information about the expression complexity. For example, the number of steps it took to evaluate the expression. */
    complexity?: { beans: { limit: number; value: number }; expensiveOperations: unknown; primitiveValues: unknown; steps: unknown };
    /** Contains information about the `issues` variable in the context. For example, is the issues were loaded with JQL, information about the page will be included here. */
    issues?: { jql?: { readonly isLast?: boolean; nextPageToken: string } };
}

export interface JQLCountRequestBean {
    /** A [JQL](https://confluence.atlassian.com/x/egORLQ) expression. For performance reasons, this parameter requires a bounded query. A bounded query is a query with a search restriction. */
    jql?: string;
}

export interface JQLCountResultsBean {
    /** Number of issues matching JQL query. */
    count?: number;
}

/** The JQL queries to be converted. */
export interface JQLPersonalDataMigrationRequest {
    /** A list of queries with user identifiers. Maximum of 100 queries. */
    queryStrings?: Array<string>;
}

/** JQL queries that contained users that could not be found */
export interface JQLQueryWithUnknownUsers {
    /** The converted query, with accountIDs instead of user identifiers, or 'unknown' for users that could not be found */
    convertedQuery?: string;
    /** The original query, for reference */
    originalQuery?: string;
}

/** Lists of JQL reference data. */
export interface JQLReferenceData {
    /** List of JQL query reserved words. */
    jqlReservedWords?: Array<string>;
    /** List of fields usable in JQL queries. */
    visibleFieldNames?: Array<{ auto?: "true" | "false"; cfid?: string; deprecated?: "true" | "false"; deprecatedSearcherKey?: string; displayName?: string; operators?: Array<string>; orderable?: "true" | "false"; searchable?: "true" | "false"; types?: Array<string>; value?: string }>;
    /** List of functions usable in JQL queries. */
    visibleFunctionNames?: Array<{ displayName?: string; isList?: "true" | "false"; supportsListAndSingleValueOperators?: "true" | "false"; types?: Array<string>; value?: string }>;
}

/** The JQL specifying the issues available in the evaluated Jira expression under the `issues` context variable. This bean will be replacing `JexpIssues` bean as part of new `evaluate` endpoint */
export interface JexpEvaluateCtxIssues {
    /** The JQL query that specifies the set of issues available in the Jira expression. */
    jql?: { maxResults?: number; nextPageToken?: string; query?: string };
}

/** The JQL specifying the issues available in the evaluated Jira expression under the `issues` context variable. Not all issues returned by the JQL query are loaded, only those described by the `nextPageToken` and `maxResults` properties. This bean will be replacing JexpJqlIssues bean as part of new `evaluate` endpoint */
export interface JexpEvaluateCtxJqlIssues {
    /** The maximum number of issues to return from the JQL query. max results value considered may be lower than the number specific here. */
    maxResults?: number;
    /** The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. */
    nextPageToken?: string;
    /** The JQL query, required to be bounded. Additionally, `orderBy` clause can contain a maximum of 7 fields */
    query?: string;
}

/** The JQL specifying the issues available in the evaluated Jira expression under the `issues` context variable. */
export interface JexpIssues {
    /** The JQL query that specifies the set of issues available in the Jira expression. */
    jql?: { maxResults?: number; query?: string; startAt?: number; validation?: "strict" | "warn" | "none" };
}

/** The JQL specifying the issues available in the evaluated Jira expression under the `issues` context variable. Not all issues returned by the JQL query are loaded, only those described by the `startAt` and `maxResults` properties. To determine whether it is necessary to iterate to ensure all the issues returned by the JQL query are evaluated, inspect `meta.issues.jql.count` in the response. */
export interface JexpJqlIssues {
    /** The maximum number of issues to return from the JQL query. Inspect `meta.issues.jql.maxResults` in the response to ensure the maximum value has not been exceeded. */
    maxResults?: number;
    /** The JQL query. */
    query?: string;
    /** The index of the first issue to return from the JQL query. */
    startAt?: number;
    /** Determines how to validate the JQL query and treat the validation results. */
    validation?: "strict" | "warn" | "none";
}

export interface JiraCascadingSelectField {
    childOptionValue?: { optionId?: number };
    fieldId: string;
    parentOptionValue: unknown;
}

export interface JiraColorField {
    color: { name: string };
    fieldId: string;
}

export interface JiraColorInput {
    name: string;
}

export interface JiraComponentField {
    componentId: number;
}

export interface JiraDateField {
    date?: { formattedDate: string };
    fieldId: string;
}

export interface JiraDateInput {
    formattedDate: string;
}

export interface JiraDateTimeField {
    dateTime: { formattedDateTime: string };
    fieldId: string;
}

export interface JiraDateTimeInput {
    formattedDateTime: string;
}

export interface JiraDurationField {
    originalEstimateField: string;
}

/** Details about the analysed Jira expression. */
export interface JiraExpressionAnalysis {
    /** Details about the complexity of the analysed Jira expression. */
    complexity?: { expensiveOperations: string; variables?: Record<string, string> };
    /** A list of validation errors. Not included if the expression is valid. */
    errors?: Array<{ column?: number; expression?: string; line?: number; message: string; type: "syntax" | "type" | "other" }>;
    /** The analysed expression. */
    expression: string;
    /** EXPERIMENTAL. The inferred type of the expression. */
    type?: string;
    /** Whether the expression is valid and the interpreter will evaluate it. Note that the expression may fail at runtime (for example, if it executes too many expensive operations). */
    valid: boolean;
}

/** Details about the complexity of the analysed Jira expression. */
export interface JiraExpressionComplexity {
    /**
     * Information that can be used to determine how many [expensive operations](https://developer.atlassian.com/cloud/jira/platform/jira-expressions/#expensive-operations) the evaluation of the expression will perform. This information may be a formula or number. For example:
     *
     *  *  `issues.map(i => i.comments)` performs as many expensive operations as there are issues on the issues list. So this parameter returns `N`, where `N` is the size of issue list.
     *  *  `new Issue(10010).comments` gets comments for one issue, so its complexity is `2` (`1` to retrieve issue 10010 from the database plus `1` to get its comments).
     */
    expensiveOperations: string;
    /** Variables used in the formula, mapped to the parts of the expression they refer to. */
    variables?: Record<string, string>;
}

export interface JiraExpressionEvalContextBean {
    /** The ID of the board that is available under the `board` variable when evaluating the expression. */
    board?: number;
    /**
     * Custom context variables and their types. These variable types are available for use in a custom context:
     *
     *  *  `user`: A [user](https://developer.atlassian.com/cloud/jira/platform/jira-expressions-type-reference#user) specified as an Atlassian account ID.
     *  *  `issue`: An [issue](https://developer.atlassian.com/cloud/jira/platform/jira-expressions-type-reference#issue) specified by ID or key. All the fields of the issue object are available in the Jira expression.
     *  *  `json`: A JSON object containing custom content.
     *  *  `list`: A JSON list of `user`, `issue`, or `json` variable types.
     */
    custom?: Array<{ accountId: string; type: string } | { id?: number; key?: string; type: string } | { type: string; value?: Record<string, unknown> }>;
    /** The ID of the customer request that is available under the `customerRequest` variable when evaluating the expression. This is the same as the ID of the underlying Jira issue, but the customer request context variable will have a different type. */
    customerRequest?: number;
    /** The issue that is available under the `issue` variable when evaluating the expression. */
    issue?: { id?: number; key?: string };
    /** The collection of issues that is available under the `issues` variable when evaluating the expression. */
    issues?: { jql?: { maxResults?: number; query?: string; startAt?: number; validation?: "strict" | "warn" | "none" } };
    /** The project that is available under the `project` variable when evaluating the expression. */
    project?: unknown;
    /** The ID of the service desk that is available under the `serviceDesk` variable when evaluating the expression. */
    serviceDesk?: number;
    /** The ID of the sprint that is available under the `sprint` variable when evaluating the expression. */
    sprint?: number;
}

export interface JiraExpressionEvalRequestBean {
    /** The context in which the Jira expression is evaluated. */
    context?: { board?: number; custom?: Array<{ accountId: string; type: string } | { id?: number; key?: string; type: string } | { type: string; value?: Record<string, unknown> }>; customerRequest?: number; issue?: { id?: number; key?: string }; issues?: { jql?: { maxResults?: number; query?: string; startAt?: number; validation?: "strict" | "warn" | "none" } }; project?: unknown; serviceDesk?: number; sprint?: number };
    /** The Jira expression to evaluate. */
    expression: string;
}

export interface JiraExpressionEvaluateContextBean {
    /** The ID of the board that is available under the `board` variable when evaluating the expression. */
    board?: number;
    /**
     * Custom context variables and their types. These variable types are available for use in a custom context:
     *
     *  *  `user`: A [user](https://developer.atlassian.com/cloud/jira/platform/jira-expressions-type-reference#user) specified as an Atlassian account ID.
     *  *  `issue`: An [issue](https://developer.atlassian.com/cloud/jira/platform/jira-expressions-type-reference#issue) specified by ID or key. All the fields of the issue object are available in the Jira expression.
     *  *  `json`: A JSON object containing custom content.
     *  *  `list`: A JSON list of `user`, `issue`, or `json` variable types.
     */
    custom?: Array<{ accountId: string; type: string } | { id?: number; key?: string; type: string } | { type: string; value?: Record<string, unknown> }>;
    /** The ID of the customer request that is available under the `customerRequest` variable when evaluating the expression. This is the same as the ID of the underlying Jira issue, but the customer request context variable will have a different type. */
    customerRequest?: number;
    /** The issue that is available under the `issue` variable when evaluating the expression. */
    issue?: { id?: number; key?: string };
    /** The collection of issues that is available under the `issues` variable when evaluating the expression. */
    issues?: { jql?: { maxResults?: number; nextPageToken?: string; query?: string } };
    /** The project that is available under the `project` variable when evaluating the expression. */
    project?: unknown;
    /** The ID of the service desk that is available under the `serviceDesk` variable when evaluating the expression. */
    serviceDesk?: number;
    /** The ID of the sprint that is available under the `sprint` variable when evaluating the expression. */
    sprint?: number;
}

/** The request to evaluate a Jira expression. This bean will be replacing `JiraExpressionEvaluateRequest` as part of new `evaluate` endpoint */
export interface JiraExpressionEvaluateRequestBean {
    /** The context in which the Jira expression is evaluated. */
    context?: { board?: number; custom?: Array<{ accountId: string; type: string } | { id?: number; key?: string; type: string } | { type: string; value?: Record<string, unknown> }>; customerRequest?: number; issue?: { id?: number; key?: string }; issues?: { jql?: { maxResults?: number; nextPageToken?: string; query?: string } }; project?: unknown; serviceDesk?: number; sprint?: number };
    /** The Jira expression to evaluate. */
    expression: string;
}

export interface JiraExpressionEvaluationMetaDataBean {
    /** Contains information about the expression complexity. For example, the number of steps it took to evaluate the expression. */
    complexity?: { beans: { limit: number; value: number }; expensiveOperations: unknown; primitiveValues: unknown; steps: unknown };
    /** Contains information about the `issues` variable in the context. For example, is the issues were loaded with JQL, information about the page will be included here. */
    issues?: { jql?: { count: number; maxResults: number; startAt: number; totalCount: number; validationWarnings?: Array<string> } };
}

/** Details of Jira expressions for analysis. */
export interface JiraExpressionForAnalysis {
    /** Context variables and their types. The type checker assumes that [common context variables](https://developer.atlassian.com/cloud/jira/platform/jira-expressions/#context-variables), such as `issue` or `project`, are available in context and sets their type. Use this property to override the default types or provide details of new variables. */
    contextVariables?: Record<string, string>;
    /** The list of Jira expressions to analyse. */
    expressions: Array<string>;
}

/** The result of evaluating a Jira expression. */
export interface JiraExpressionResult {
    /** Contains various characteristics of the performed expression evaluation. */
    meta?: { complexity?: { beans: { limit: number; value: number }; expensiveOperations: unknown; primitiveValues: unknown; steps: unknown }; issues?: { jql?: { count: number; maxResults: number; startAt: number; totalCount: number; validationWarnings?: Array<string> } } };
    /** The value of the evaluated expression. It may be a primitive JSON value or a Jira REST API object. (Some expressions do not produce any meaningful results—for example, an expression that returns a lambda function—if that's the case a simple string representation is returned. These string representations should not be relied upon and may change without notice.) */
    value: Record<string, unknown>;
}

/**
 * Details about syntax and type errors. The error details apply to the entire expression, unless the object includes:
 *
 *  *  `line` and `column`
 *  *  `expression`
 */
export interface JiraExpressionValidationError {
    /** The text column in which the error occurred. */
    column?: number;
    /** The part of the expression in which the error occurred. */
    expression?: string;
    /** The text line in which the error occurred. */
    line?: number;
    /** Details about the error. */
    message: string;
    /** The error type. */
    type: "syntax" | "type" | "other";
}

/** Details about the analysed Jira expression. */
export interface JiraExpressionsAnalysis {
    /** The results of Jira expressions analysis. */
    results: Array<{ complexity?: { expensiveOperations: string; variables?: Record<string, string> }; errors?: Array<{ column?: number; expression?: string; line?: number; message: string; type: "syntax" | "type" | "other" }>; expression: string; type?: string; valid: boolean }>;
}

export interface JiraExpressionsComplexityBean {
    /** The number of Jira REST API beans returned in the response. */
    beans: { limit: number; value: number };
    /** The number of expensive operations executed while evaluating the expression. Expensive operations are those that load additional data, such as entity properties, comments, or custom fields. */
    expensiveOperations: unknown;
    /** The number of primitive values returned in the response. */
    primitiveValues: unknown;
    /** The number of steps it took to evaluate the expression, where a step is a high-level operation performed by the expression. A step is an operation such as arithmetic, accessing a property, accessing a context variable, or calling a function. */
    steps: unknown;
}

export interface JiraExpressionsComplexityValueBean {
    /** The maximum allowed complexity. The evaluation will fail if this value is exceeded. */
    limit: number;
    /** The complexity value of the current expression. */
    value: number;
}

export interface JiraGroupInput {
    groupName: string;
}

export interface JiraIssueFields {
    /**
     * Add or clear a cascading select field:
     *
     *  *  To add, specify `optionId` for both parent and child.
     *  *  To clear the child, set its `optionId` to null.
     *  *  To clear both, set the parent's `optionId` to null.
     */
    cascadingSelectFields?: Array<{ childOptionValue?: { optionId?: number }; fieldId: string; parentOptionValue: unknown }>;
    /**
     * Add or clear a number field:
     *
     *  *  To add, specify a numeric `value`.
     *  *  To clear, set `value` to `null`.
     */
    clearableNumberFields?: Array<{ fieldId: string; value?: number }>;
    /**
     * Add or clear a color field:
     *
     *  *  To add, specify the color `name`. Available colors are: `purple`, `blue`, `green`, `teal`, `yellow`, `orange`, `grey`, `dark purple`, `dark blue`, `dark green`, `dark teal`, `dark yellow`, `dark orange`, `dark grey`.
     *  *  To clear, set the color `name` to an empty string.
     */
    colorFields?: Array<{ color: { name: string }; fieldId: string }>;
    /**
     * Add or clear a date picker field:
     *
     *  *  To add, specify the date in `d/mmm/yy` format or ISO format `dd-mm-yyyy`.
     *  *  To clear, set `formattedDate` to an empty string.
     */
    datePickerFields?: Array<{ date?: { formattedDate: string }; fieldId: string }>;
    /**
     * Add or clear the planned start date and time:
     *
     *  *  To add, specify the date and time in ISO format for `formattedDateTime`.
     *  *  To clear, provide an empty string for `formattedDateTime`.
     */
    dateTimePickerFields?: Array<{ dateTime: { formattedDateTime: string }; fieldId: string }>;
    /** Set the issue type field by providing an `issueTypeId`. */
    issueType?: { issueTypeId: string };
    /**
     * Edit a labels field:
     *
     *  *  Options include `ADD`, `REPLACE`, `REMOVE`, or `REMOVE_ALL` for bulk edits.
     *  *  To clear labels, use the `REMOVE_ALL` option with an empty `labels` array.
     */
    labelsFields?: Array<{ bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL"; fieldId: string; labelProperties?: Array<{ color?: "GREY_LIGHTEST" | "GREY_LIGHTER" | "GREY" | "GREY_DARKER" | "GREY_DARKEST" | "PURPLE_LIGHTEST" | "PURPLE_LIGHTER" | "PURPLE" | "PURPLE_DARKER" | "PURPLE_DARKEST" | "BLUE_LIGHTEST" | "BLUE_LIGHTER" | "BLUE" | "BLUE_DARKER" | "BLUE_DARKEST" | "TEAL_LIGHTEST" | "TEAL_LIGHTER" | "TEAL" | "TEAL_DARKER" | "TEAL_DARKEST" | "GREEN_LIGHTEST" | "GREEN_LIGHTER" | "GREEN" | "GREEN_DARKER" | "GREEN_DARKEST" | "LIME_LIGHTEST" | "LIME_LIGHTER" | "LIME" | "LIME_DARKER" | "LIME_DARKEST" | "YELLOW_LIGHTEST" | "YELLOW_LIGHTER" | "YELLOW" | "YELLOW_DARKER" | "YELLOW_DARKEST" | "ORANGE_LIGHTEST" | "ORANGE_LIGHTER" | "ORANGE" | "ORANGE_DARKER" | "ORANGE_DARKEST" | "RED_LIGHTEST" | "RED_LIGHTER" | "RED" | "RED_DARKER" | "RED_DARKEST" | "MAGENTA_LIGHTEST" | "MAGENTA_LIGHTER" | "MAGENTA" | "MAGENTA_DARKER" | "MAGENTA_DARKEST"; name?: string }>; labels: Array<{ name: string }> }>;
    /**
     * Add or clear a multi-group picker field:
     *
     *  *  To add groups, provide an array of groups with `groupName`s.
     *  *  To clear all groups, use an empty `groups` array.
     */
    multipleGroupPickerFields?: Array<{ fieldId: string; groups: Array<{ groupName: string }> }>;
    /**
     * Assign or unassign multiple users to/from a field:
     *
     *  *  To assign, provide an array of user `accountId`s.
     *  *  To clear, set `users` to `null`.
     */
    multipleSelectClearableUserPickerFields?: Array<{ fieldId: string; users?: Array<{ accountId: string }> }>;
    /**
     * Add or clear a multi-select field:
     *
     *  *  To add, provide an array of options with `optionId`s.
     *  *  To clear, use an empty `options` array.
     */
    multipleSelectFields?: Array<{ fieldId: string; options: Array<unknown> }>;
    /**
     * Edit a multi-version picker field like Fix Versions/Affects Versions:
     *
     *  *  Options include `ADD`, `REPLACE`, `REMOVE`, or `REMOVE_ALL` for bulk edits.
     *  *  To clear the field, use the `REMOVE_ALL` option with an empty `versions` array.
     */
    multipleVersionPickerFields?: Array<{ bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL"; fieldId: string; versions: Array<{ versionId?: string }> }>;
    /**
     * Edit a multi select components field:
     *
     *  *  Options include `ADD`, `REPLACE`, `REMOVE`, or `REMOVE_ALL` for bulk edits.
     *  *  To clear, use the `REMOVE_ALL` option with an empty `components` array.
     */
    multiselectComponents?: { bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL"; components: Array<{ componentId: number }>; fieldId: string };
    /** Edit the original estimate field. */
    originalEstimateField?: { originalEstimateField: string };
    /** Set the priority of an issue by specifying a `priorityId`. */
    priority?: { priorityId: string };
    /**
     * Add or clear a rich text field:
     *
     *  *  To add, provide `adfValue`. Note that rich text fields only support ADF values.
     *  *  To clear, use an empty `richText` object.
     *
     * For ADF format details, refer to: [Atlassian Document Format](https://developer.atlassian.com/cloud/jira/platform/apis/document/structure).
     */
    richTextFields?: Array<{ fieldId: string; richText: { adfValue?: Record<string, Record<string, unknown>> } }>;
    /**
     * Add or clear a single group picker field:
     *
     *  *  To add, specify the group with `groupName`.
     *  *  To clear, set `groupName` to an empty string.
     */
    singleGroupPickerFields?: Array<{ fieldId: string; group: unknown }>;
    /**
     * Add or clear a single line text field:
     *
     *  *  To add, provide the `text` value.
     *  *  To clear, set `text` to an empty string.
     */
    singleLineTextFields?: Array<{ fieldId: string; text: string }>;
    /**
     * Edit assignment for single select user picker fields like Assignee/Reporter:
     *
     *  *  To assign an issue, specify the user's `accountId`.
     *  *  To unassign an issue, set `user` to `null`.
     *  *  For automatic assignment, set `accountId` to `-1`.
     */
    singleSelectClearableUserPickerFields?: Array<{ fieldId: string; user?: unknown }>;
    /**
     * Add or clear a single select field:
     *
     *  *  To add, specify the option with an `optionId`.
     *  *  To clear, pass an option with `optionId` as `-1`.
     */
    singleSelectFields?: Array<{ fieldId: string; option: unknown }>;
    /**
     * Add or clear a single version picker field:
     *
     *  *  To add, specify the version with a `versionId`.
     *  *  To clear, set `versionId` to `-1`.
     */
    singleVersionPickerFields?: Array<{ fieldId: string; version: unknown }>;
    status?: { statusId: string };
    /** Edit the time tracking field. */
    timeTrackingField?: { timeRemaining: string };
    /**
     * Add or clear a URL field:
     *
     *  *  To add, provide the `url` with the desired URL value.
     *  *  To clear, set `url` to an empty string.
     */
    urlFields?: Array<{ fieldId: string; url: string }>;
}

export interface JiraIssueTypeField {
    issueTypeId: string;
}

export interface JiraLabelPropertiesInputJackson1 {
    color?: "GREY_LIGHTEST" | "GREY_LIGHTER" | "GREY" | "GREY_DARKER" | "GREY_DARKEST" | "PURPLE_LIGHTEST" | "PURPLE_LIGHTER" | "PURPLE" | "PURPLE_DARKER" | "PURPLE_DARKEST" | "BLUE_LIGHTEST" | "BLUE_LIGHTER" | "BLUE" | "BLUE_DARKER" | "BLUE_DARKEST" | "TEAL_LIGHTEST" | "TEAL_LIGHTER" | "TEAL" | "TEAL_DARKER" | "TEAL_DARKEST" | "GREEN_LIGHTEST" | "GREEN_LIGHTER" | "GREEN" | "GREEN_DARKER" | "GREEN_DARKEST" | "LIME_LIGHTEST" | "LIME_LIGHTER" | "LIME" | "LIME_DARKER" | "LIME_DARKEST" | "YELLOW_LIGHTEST" | "YELLOW_LIGHTER" | "YELLOW" | "YELLOW_DARKER" | "YELLOW_DARKEST" | "ORANGE_LIGHTEST" | "ORANGE_LIGHTER" | "ORANGE" | "ORANGE_DARKER" | "ORANGE_DARKEST" | "RED_LIGHTEST" | "RED_LIGHTER" | "RED" | "RED_DARKER" | "RED_DARKEST" | "MAGENTA_LIGHTEST" | "MAGENTA_LIGHTER" | "MAGENTA" | "MAGENTA_DARKER" | "MAGENTA_DARKEST";
    name?: string;
}

export interface JiraLabelsField {
    bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL";
    fieldId: string;
    labelProperties?: Array<{ color?: "GREY_LIGHTEST" | "GREY_LIGHTER" | "GREY" | "GREY_DARKER" | "GREY_DARKEST" | "PURPLE_LIGHTEST" | "PURPLE_LIGHTER" | "PURPLE" | "PURPLE_DARKER" | "PURPLE_DARKEST" | "BLUE_LIGHTEST" | "BLUE_LIGHTER" | "BLUE" | "BLUE_DARKER" | "BLUE_DARKEST" | "TEAL_LIGHTEST" | "TEAL_LIGHTER" | "TEAL" | "TEAL_DARKER" | "TEAL_DARKEST" | "GREEN_LIGHTEST" | "GREEN_LIGHTER" | "GREEN" | "GREEN_DARKER" | "GREEN_DARKEST" | "LIME_LIGHTEST" | "LIME_LIGHTER" | "LIME" | "LIME_DARKER" | "LIME_DARKEST" | "YELLOW_LIGHTEST" | "YELLOW_LIGHTER" | "YELLOW" | "YELLOW_DARKER" | "YELLOW_DARKEST" | "ORANGE_LIGHTEST" | "ORANGE_LIGHTER" | "ORANGE" | "ORANGE_DARKER" | "ORANGE_DARKEST" | "RED_LIGHTEST" | "RED_LIGHTER" | "RED" | "RED_DARKER" | "RED_DARKEST" | "MAGENTA_LIGHTEST" | "MAGENTA_LIGHTER" | "MAGENTA" | "MAGENTA_DARKER" | "MAGENTA_DARKEST"; name?: string }>;
    labels: Array<{ name: string }>;
}

export interface JiraLabelsInput {
    name: string;
}

export interface JiraMultiSelectComponentField {
    bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL";
    components: Array<{ componentId: number }>;
    fieldId: string;
}

export interface JiraMultipleGroupPickerField {
    fieldId: string;
    groups: Array<{ groupName: string }>;
}

export interface JiraMultipleSelectField {
    fieldId: string;
    options: Array<{ optionId?: number }>;
}

export interface JiraMultipleSelectUserPickerField {
    fieldId: string;
    users?: Array<{ accountId: string }>;
}

export interface JiraMultipleVersionPickerField {
    bulkEditMultiSelectFieldOption: "ADD" | "REMOVE" | "REPLACE" | "REMOVE_ALL";
    fieldId: string;
    versions: Array<{ versionId?: string }>;
}

export interface JiraNumberField {
    fieldId: string;
    value?: number;
}

export interface JiraPriorityField {
    priorityId: string;
}

export interface JiraRichTextField {
    fieldId: string;
    richText: { adfValue?: Record<string, Record<string, unknown>> };
}

export interface JiraRichTextInput {
    adfValue?: Record<string, Record<string, unknown>>;
}

export interface JiraSelectedOptionField {
    optionId?: number;
}

export interface JiraSingleGroupPickerField {
    fieldId: string;
    group: { groupName: string };
}

export interface JiraSingleLineTextField {
    fieldId: string;
    text: string;
}

/**
 * Add or clear a single select field:
 *
 *  *  To add, specify the option with an `optionId`.
 *  *  To clear, pass an option with `optionId` as `-1`.
 */
export interface JiraSingleSelectField {
    fieldId: string;
    option: { optionId?: number };
}

export interface JiraSingleSelectUserPickerField {
    fieldId: string;
    user?: { accountId: string };
}

export interface JiraSingleVersionPickerField {
    fieldId: string;
    version: { versionId?: string };
}

/** Details of a status. */
export interface JiraStatus {
    /** The description of the status. */
    description?: string;
    /** The ID of the status. */
    id?: string;
    /** The name of the status. */
    name?: string;
    /** The scope of the status. */
    scope?: { project?: { id: string }; type: "PROJECT" | "GLOBAL" };
    /** The category of the status. */
    statusCategory?: "TODO" | "IN_PROGRESS" | "DONE";
}

export interface JiraStatusInput {
    statusId: string;
}

export interface JiraTimeTrackingField {
    timeRemaining: string;
}

export interface JiraUrlField {
    fieldId: string;
    url: string;
}

export interface JiraUserField {
    accountId: string;
}

export interface JiraVersionField {
    versionId?: string;
}

/** Details of a workflow. */
export interface JiraWorkflow {
    /** The creation date of the workflow. */
    created?: string;
    /** The description of the workflow. */
    description?: string;
    /** The ID of the workflow. */
    id?: string;
    /** Indicates if the workflow can be edited. */
    isEditable?: boolean;
    /** The starting point for the statuses in the workflow. */
    loopedTransitionContainerLayout?: { x?: number; y?: number };
    /** The name of the workflow. */
    name?: string;
    /** The scope of the workflow. */
    scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" };
    /** The starting point for the statuses in the workflow. */
    startPointLayout?: unknown;
    /** The statuses referenced in this workflow. */
    statuses?: Array<{ approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string }; deprecated?: boolean; layout?: { x?: number; y?: number }; properties?: Record<string, string>; statusReference?: string }>;
    /** If there is a current [asynchronous task](#async-operations) operation for this workflow. */
    taskId?: string;
    /** The transitions of the workflow. */
    transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; properties?: Record<string, string>; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>;
    /** The last edited date of the workflow. */
    updated?: string;
    /** The current version details of this workflow scheme. */
    version?: { id?: string; versionNumber?: number };
}

/** Details of a status. */
export interface JiraWorkflowPreviewStatus {
    /** The description of the status. */
    description?: string;
    /** The ID of the status. */
    id?: string;
    /** The name of the status. */
    name?: string;
    /** The raw name of the status. */
    rawName?: string;
    /** The scope of the workflow. */
    scope?: { project?: { id?: string }; type?: "PROJECT" | "GLOBAL" };
    /** The category of the status. */
    statusCategory?: "TODO" | "IN_PROGRESS" | "DONE";
    /** The reference of the status. Unique within this response but not guaranteed to be stable across requests. */
    statusReference?: string;
}

/** Details of a status. */
export interface JiraWorkflowStatus {
    /** The description of the status. */
    description?: string;
    /** The ID of the status. */
    id?: string;
    /** The name of the status. */
    name?: string;
    /** The scope of the workflow. */
    scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" };
    /** The category of the status. */
    statusCategory?: "TODO" | "IN_PROGRESS" | "DONE";
    /** The reference of the status. */
    statusReference?: string;
}

/** Jql function precomputation. */
export interface JqlFunctionPrecomputationBean {
    /** The list of arguments function was invoked with. */
    readonly arguments?: Array<string>;
    /** The timestamp of the precomputation creation. */
    readonly created?: string;
    /** The error message to be displayed to the user. */
    readonly error?: string;
    /** The field the function was executed against. */
    readonly field?: string;
    /** The function key. */
    readonly functionKey?: string;
    /** The name of the function. */
    readonly functionName?: string;
    /** The id of the precomputation. */
    readonly id?: string;
    /** The operator in context of which function was executed. */
    readonly operator?: string;
    /** The timestamp of the precomputation last update. */
    readonly updated?: string;
    /** The timestamp of the precomputation last usage. */
    readonly used?: string;
    /** The JQL fragment stored as the precomputation. */
    readonly value?: string;
}

/** Request to fetch precomputations by ID. */
export interface JqlFunctionPrecomputationGetByIdRequest {
    precomputationIDs?: Array<string>;
}

/** Get precomputations by ID response. */
export interface JqlFunctionPrecomputationGetByIdResponse {
    /** List of precomputations that were not found. */
    readonly notFoundPrecomputationIDs?: Array<string>;
    /** The list of precomputations. */
    readonly precomputations?: Array<{ readonly arguments?: Array<string>; readonly created?: string; readonly error?: string; readonly field?: string; readonly functionKey?: string; readonly functionName?: string; readonly id?: string; readonly operator?: string; readonly updated?: string; readonly used?: string; readonly value?: string }>;
}

/** Precomputation id and its new value. */
export interface JqlFunctionPrecomputationUpdateBean {
    /** The error message to be displayed to the user if the given function clause is no longer valid during recalculation of the precomputation. */
    error?: string;
    /** The id of the precomputation to update. */
    id: string;
    /** The new value of the precomputation. */
    value?: string;
}

/** Error response returned updating JQL Function precomputations fails. */
export interface JqlFunctionPrecomputationUpdateErrorResponse {
    /** The list of error messages produced by this operation. */
    readonly errorMessages?: Array<string>;
    /** List of precomputations that were not found. */
    readonly notFoundPrecomputationIDs?: Array<string>;
}

/** List of pairs (id and value) for precomputation updates. */
export interface JqlFunctionPrecomputationUpdateRequestBean {
    values?: Array<{ error?: string; id: string; value?: string }>;
}

/** Result of updating JQL Function precomputations. */
export interface JqlFunctionPrecomputationUpdateResponse {
    /** List of precomputations that were not found and skipped. Only returned if the request passed skipNotFoundPrecomputations=true. */
    readonly notFoundPrecomputationIDs?: Array<string>;
}

/** A list of JQL queries to parse. */
export interface JqlQueriesToParse {
    /** A list of queries to parse. */
    queries: Array<string>;
}

/** The list of JQL queries to sanitize for the given account IDs. */
export interface JqlQueriesToSanitize {
    /** The list of JQL queries to sanitize. Must contain unique values. Maximum of 20 queries. */
    queries: Array<{ accountId?: string; query: string }>;
}

/** A parsed JQL query. */
export interface JqlQuery {
    /** Details of the order-by JQL clause. */
    orderBy?: { fields: Array<{ direction?: "asc" | "desc"; field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> } }> };
    /** A JQL query clause. */
    where?: { clauses: Array<unknown>; operator: "and" | "or" | "not" } | { field: unknown; operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown; operator: "=" | "!=" | ">" | "<" | ">=" | "<=" | "in" | "not in" | "~" | "~=" | "is" | "is not" } | { field: unknown; operand: unknown; operator: "was" | "was in" | "was not in" | "was not"; predicates: Array<{ operand: unknown; operator: "before" | "after" | "from" | "to" | "on" | "during" | "by" }> } | { field: unknown; operator: "changed"; predicates: Array<unknown> };
}

/** A JQL query clause. */
export type JqlQueryClause = { clauses: Array<unknown>; operator: "and" | "or" | "not" } | { field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> }; operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown; operator: "=" | "!=" | ">" | "<" | ">=" | "<=" | "in" | "not in" | "~" | "~=" | "is" | "is not" } | { field: unknown; operand: unknown; operator: "was" | "was in" | "was not in" | "was not"; predicates: Array<{ operand: unknown; operator: "before" | "after" | "from" | "to" | "on" | "during" | "by" }> } | { field: unknown; operator: "changed"; predicates: Array<unknown> };
/** Details of an operand in a JQL clause. */
export type JqlQueryClauseOperand = { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown;

/** A time predicate for a temporal JQL clause. */
export interface JqlQueryClauseTimePredicate {
    /** Details of an operand in a JQL clause. */
    operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown;
    /** The operator between the field and the operand. */
    operator: "before" | "after" | "from" | "to" | "on" | "during" | "by";
}

/** A field used in a JQL query. See [Advanced searching - fields reference](https://confluence.atlassian.com/x/dAiiLQ) for more information about fields in JQL queries. */
export interface JqlQueryField {
    /** The encoded name of the field, which can be used directly in a JQL query. */
    encodedName?: string;
    /** The name of the field. */
    name: string;
    /** When the field refers to a value in an entity property, details of the entity property value. */
    property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }>;
}

/** Details of an entity property. */
export interface JqlQueryFieldEntityProperty {
    /** The object on which the property is set. */
    entity: string;
    /** The key of the property. */
    key: string;
    /** The path in the property value to query. */
    path: string;
    /** The type of the property value extraction. Not available if the extraction for the property is not registered on the instance with the [Entity property](https://developer.atlassian.com/cloud/jira/platform/modules/entity-property/) module. */
    type?: "number" | "string" | "text" | "date" | "user";
}

/** Details of the order-by JQL clause. */
export interface JqlQueryOrderByClause {
    /** The list of order-by clause fields and their ordering directives. */
    fields: Array<{ direction?: "asc" | "desc"; field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> } }>;
}

/** An element of the order-by JQL clause. */
export interface JqlQueryOrderByClauseElement {
    /** The direction in which to order the results. */
    direction?: "asc" | "desc";
    /** A field used in a JQL query. See [Advanced searching - fields reference](https://confluence.atlassian.com/x/dAiiLQ) for more information about fields in JQL queries. */
    field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> };
}

/** The JQL query to sanitize for the account ID. If the account ID is null, sanitizing is performed for an anonymous user. */
export interface JqlQueryToSanitize {
    /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. */
    accountId?: string;
    /** The query to sanitize. */
    query: string;
}

/** An operand that can be part of a list operand. */
export type JqlQueryUnitaryOperand = { encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" };

/** A JSON object with custom content. */
export interface JsonContextVariable {
    /** Type of custom context variable. */
    type: string;
    /** A JSON object containing custom content. */
    value?: Record<string, unknown>;
}

export interface JsonNode {
    array?: boolean;
    bigDecimal?: boolean;
    bigInteger?: boolean;
    bigIntegerValue?: number;
    binary?: boolean;
    binaryValue?: Array<string>;
    boolean?: boolean;
    booleanValue?: boolean;
    containerNode?: boolean;
    decimalValue?: number;
    double?: boolean;
    doubleValue?: number;
    elements?: Record<string, unknown>;
    fieldNames?: Record<string, unknown>;
    fields?: Record<string, unknown>;
    floatingPointNumber?: boolean;
    int?: boolean;
    intValue?: number;
    integralNumber?: boolean;
    long?: boolean;
    longValue?: number;
    missingNode?: boolean;
    null?: boolean;
    number?: boolean;
    numberType?: "INT" | "LONG" | "BIG_INTEGER" | "FLOAT" | "DOUBLE" | "BIG_DECIMAL";
    numberValue?: number;
    object?: boolean;
    pojo?: boolean;
    textValue?: string;
    textual?: boolean;
    valueAsBoolean?: boolean;
    valueAsDouble?: number;
    valueAsInt?: number;
    valueAsLong?: number;
    valueAsText?: string;
    valueNode?: boolean;
}

/** The schema of a field. */
export interface JsonTypeBean {
    /** If the field is a custom field, the configuration of the field. */
    readonly configuration?: Record<string, Record<string, unknown>>;
    /** If the field is a custom field, the URI of the field. */
    readonly custom?: string;
    /** If the field is a custom field, the custom ID of the field. */
    readonly customId?: number;
    /** When the data type is an array, the name of the field items within the array. */
    readonly items?: string;
    /** If the field is a system field, the name of the field. */
    readonly system?: string;
    /** The data type of the field. */
    readonly type: string;
}

/** An operand that is a JQL keyword. See [Advanced searching - keywords reference](https://confluence.atlassian.com/jiracorecloud/advanced-searching-keywords-reference-765593717.html#Advancedsearching-keywordsreference-EMPTYEMPTY) for more information about operand keywords. */
export interface KeywordOperand {
    /** The keyword that is the operand value. */
    keyword: "empty";
}

export type LegacyJackson1ListAttachment = Array<Record<string, unknown>>;
export type LegacyJackson1ListColumnItem = Array<{ label?: string; value?: string }>;
export type LegacyJackson1ListIssueEvent = Array<{ readonly id?: number; readonly name?: string }>;
export type LegacyJackson1ListIssueTypeWithStatus = Array<{ readonly id: string; readonly name: string; readonly self: string; readonly statuses: Array<Record<string, unknown>>; readonly subtask: boolean }>;
export type LegacyJackson1ListProject = Array<{ readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }>;

export type LegacyJackson1ListProjectComponent = Array<{ readonly ari?: string; readonly assignee?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>;

export type LegacyJackson1ListProjectRoleDetails = Array<{ readonly admin?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: Record<string, unknown>; readonly self?: string; translatedName?: string; readonly type?: "DEFAULT" | "GUEST_ROLE" }>;
export type LegacyJackson1ListProjectType = Array<{ readonly color?: string; readonly descriptionI18nKey?: string; readonly formattedKey?: string; readonly icon?: string; readonly key?: string }>;
export type LegacyJackson1ListUserMigrationBean = Array<{ accountId?: string; key?: string; username?: string }>;
export type LegacyJackson1ListVersion = Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }>;
export type LegacyJackson1ListWorklog = Array<Record<string, unknown>>;

/** Details about a license for the Jira instance. */
export interface License {
    /** The applications under this license. */
    readonly applications: Array<{ readonly id: string; readonly plan: "UNLICENSED" | "FREE" | "PAID" }>;
}

/** A metric that provides insight into the active licence details */
export interface LicenseMetric {
    /** The key of a specific license metric. */
    key?: string;
    /** The calculated value of a licence metric linked to the key. An example licence metric is the approximate number of user accounts. */
    value?: string;
}

/** Details about a licensed Jira application. */
export interface LicensedApplication {
    /** The ID of the application. */
    readonly id: string;
    /** The licensing plan. */
    readonly plan: "UNLICENSED" | "FREE" | "PAID";
}

/** Details a link group, which defines issue operations. */
export interface LinkGroup {
    groups?: Array<unknown>;
    /** Details about the operations available in this version. */
    header?: { href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number };
    id?: string;
    links?: Array<unknown>;
    styleClass?: string;
    weight?: number;
}

export interface LinkIssueRequestJsonBean {
    /** A comment. */
    comment?: Record<string, unknown>;
    /** The ID or key of a linked issue. */
    inwardIssue: { readonly fields?: { readonly assignee?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly issueType?: { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }; issuetype?: { readonly avatarId?: unknown; readonly description?: unknown; readonly entityId?: unknown; readonly hierarchyLevel?: unknown; readonly iconUrl?: unknown; readonly id?: unknown; readonly name?: unknown; readonly scope?: unknown; readonly self?: unknown; readonly subtask?: unknown }; readonly priority?: Record<string, unknown>; readonly status?: Record<string, unknown>; readonly summary?: string; readonly timetracking?: { readonly originalEstimate?: string; readonly originalEstimateSeconds?: number; readonly remainingEstimate?: string; readonly remainingEstimateSeconds?: number; readonly timeSpent?: string; readonly timeSpentSeconds?: number } }; id?: string; key?: string; readonly self?: string };
    /** The ID or key of a linked issue. */
    outwardIssue: unknown;
    /**
     * This object is used as follows:
     *
     *  *  In the [ issueLink](#api-rest-api-3-issueLink-post) resource it defines and reports on the type of link between the issues. Find a list of issue link types with [Get issue link types](#api-rest-api-3-issueLinkType-get).
     *  *  In the [ issueLinkType](#api-rest-api-3-issueLinkType-post) resource it defines and reports on issue link types.
     */
    type: { id?: string; inward?: string; name?: string; outward?: string; readonly self?: string };

/** The ID or key of a linked issue. */
export interface LinkedIssue {
    /** The fields associated with the issue. */
    readonly fields?: { readonly assignee?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly issueType?: { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }; issuetype?: { readonly avatarId?: unknown; readonly description?: unknown; readonly entityId?: unknown; readonly hierarchyLevel?: unknown; readonly iconUrl?: unknown; readonly id?: unknown; readonly name?: unknown; readonly scope?: unknown; readonly self?: unknown; readonly subtask?: unknown }; readonly priority?: Record<string, unknown>; readonly status?: Record<string, unknown>; readonly summary?: string; readonly timetracking?: { readonly originalEstimate?: string; readonly originalEstimateSeconds?: number; readonly remainingEstimate?: string; readonly remainingEstimateSeconds?: number; readonly timeSpent?: string; readonly timeSpentSeconds?: number } };
    /** The ID of an issue. Required if `key` isn't provided. */
    id?: string;
    /** The key of an issue. Required if `id` isn't provided. */
    key?: string;
    /** The URL of the issue. */
    readonly self?: string;

/** An operand that is a list of values. */
export interface ListOperand {
    /** Encoded operand, which can be used directly in a JQL query. */
    encodedOperand?: string;
    /** The list of operand values. */
    values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }>;
}

export interface ListWrapperCallbackApplicationRole {
}

export interface ListWrapperCallbackGroupName {
}

/** Details of a locale. */
export interface Locale {
    /** The locale code. The Java the locale format is used: a two character language code (ISO 639), an underscore, and two letter country code (ISO 3166). For example, en\_US represents a locale of English (United States). Required on create. */
    locale?: string;
}

/** List of string of inputs */
export interface MandatoryFieldValue {
    /** If `true`, will try to retain original non-null issue field values on move. */
    retain?: boolean;
    /** Will treat as `MandatoryFieldValue` if type is `raw` or `empty` */
    type?: "adf" | "raw";
    /** Value for each field. Provide a `list of strings` for non-ADF fields. */
    value: Array<string>;
}

/** An object notation input */
export interface MandatoryFieldValueForADF {
    /** If `true`, will try to retain original non-null issue field values on move. */
    retain?: boolean;
    /** Will treat as `MandatoryFieldValueForADF` if type is `adf` */
    type: "adf" | "raw";
    /** Value for each field. Accepts Atlassian Document Format (ADF) for rich text fields like `description`, `environments`. For ADF format details, refer to: [Atlassian Document Format](https://developer.atlassian.com/cloud/jira/platform/apis/document/structure) */
    value: Record<string, unknown>;
}

/** The mappings for migrating issues from old statuses to new statuses when switching from one workflow scheme to another. This field is required if any statuses in the current project's workflows would no longer exist in the target workflow scheme. Each mapping defines how to update issues from an old status to the corresponding new status in the issue’s new workflow. */
export interface MappingsByIssueTypeOverride {
    issueTypeId?: string;
    statusMappings?: Array<{ newStatusId?: string; oldStatusId?: string }>;
}

/** The status mappings by workflows. Status mappings are required when the new workflow for an issue type doesn't contain all statuses that the old workflow has. Status mappings can be provided by a combination of `statusMappingsByWorkflows` and `statusMappingsByIssueTypeOverride`. */
export interface MappingsByWorkflow {
    /** The ID of the new workflow. */
    newWorkflowId: string;
    /** The ID of the old workflow. */
    oldWorkflowId: string;
    /** The list of status mappings. */
    statusMappings: Array<{ newStatusId?: string; oldStatusId?: string }>;
}

/** Minimal partial failure result when updating field scheme to fields associations. */
export interface MinimalFieldSchemeToFieldsPartialFailure {
    error?: string;
    fieldId: string;
    schemeId: number;
    success: boolean;
}

/** Minimal response for updating field scheme to fields associations. */
export interface MinimalFieldSchemeToFieldsResponse {
    results: Array<{ error?: string; fieldId: string; schemeId: number; success: boolean }>;
}

export interface MoveFieldBean {
    /** The ID of the screen tab field after which to place the moved screen tab field. Required if `position` isn't provided. */
    after?: string;
    /** The named position to which the screen tab field should be moved. Required if `after` isn't provided. */
    position?: "Earlier" | "Later" | "First" | "Last";
}

/** A list of issues and their respective properties to set or update. See [Entity properties](https://developer.atlassian.com/cloud/jira/platform/jira-entity-properties/) for more information. */
export interface MultiIssueEntityProperties {
    /** A list of issue IDs and their respective properties. */
    issues?: Array<{ issueID?: number; properties?: Record<string, { array?: boolean; bigDecimal?: boolean; bigInteger?: boolean; bigIntegerValue?: number; binary?: boolean; binaryValue?: Array<string>; boolean?: boolean; booleanValue?: boolean; containerNode?: boolean; decimalValue?: number; double?: boolean; doubleValue?: number; elements?: Record<string, unknown>; fieldNames?: Record<string, unknown>; fields?: Record<string, unknown>; floatingPointNumber?: boolean; int?: boolean; intValue?: number; integralNumber?: boolean; long?: boolean; longValue?: number; missingNode?: boolean; null?: boolean; number?: boolean; numberType?: "INT" | "LONG" | "BIG_INTEGER" | "FLOAT" | "DOUBLE" | "BIG_DECIMAL"; numberValue?: number; object?: boolean; pojo?: boolean; textValue?: string; textual?: boolean; valueAsBoolean?: boolean; valueAsDouble?: number; valueAsInt?: number; valueAsLong?: number; valueAsText?: string; valueNode?: boolean }> }>;
}

export interface MultipartFile {
    bytes?: Array<string>;
    contentType?: string;
    empty?: boolean;
    inputStream?: Record<string, unknown>;
    name?: string;
    originalFilename?: string;
    resource?: { contentAsByteArray?: Array<string>; description?: string; file?: string; filename?: string; inputStream?: Record<string, unknown>; open?: boolean; readable?: boolean; uri?: string; url?: string };
    size?: number;
}

/** A custom field and its new value with a list of issue to update. */
export interface MultipleCustomFieldValuesUpdate {
    /** The ID or key of the custom field. For example, `customfield_10010`. */
    customField: string;
    /** The list of issue IDs. */
    issueIds: Array<number>;
    /**
     * The value for the custom field. The value must be compatible with the [custom field type](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/#data-types) as follows:
     *
     *  *  `string` the value must be a string.
     *  *  `number` the value must be a number.
     *  *  `datetime` the value must be a string that represents a date in the ISO format or the simplified extended ISO format. For example, `"2023-01-18T12:00:00-03:00"` or `"2023-01-18T12:00:00.000Z"`. However, the milliseconds part is ignored.
     *  *  `user` the value must be an object that contains the `accountId` field.
     *  *  `group` the value must be an object that contains the group `name` or `groupId` field. Because group names can change, we recommend using `groupId`.
     *
     * A list of appropriate values must be provided if the field is of the `list` [collection type](https://developer.atlassian.com/platform/forge/manifest-reference/modules/jira-custom-field/#collection-types).
     */
    value: Record<string, unknown>;
}

/** List of updates for a custom fields. */
export interface MultipleCustomFieldValuesUpdateDetails {
    updates?: Array<{ customField: string; issueIds: Array<number>; value: Record<string, unknown> }>;
}

export interface NestedResponse {
    /** Error messages from an operation. */
    errorCollection?: { errorMessages?: Array<string>; errors?: Record<string, string>; status?: number };
    status?: number;
    warningCollection?: { warnings?: Array<string> };
}

/** The user details. */
export type NewUserDetails = Record<string, unknown>;

export interface NonWorkingDay {
    id?: number;
    iso8601Date?: string;
}

/** Details about a notification. */
export type Notification = Record<string, unknown>;

/** Details about a notification event. */
export interface NotificationEvent {
    /** The description of the event. */
    description?: string;
    /** The ID of the event. The event can be a [Jira system event](https://confluence.atlassian.com/x/8YdKLg#Creatinganotificationscheme-eventsEvents) or a [custom event](https://confluence.atlassian.com/x/AIlKLg). */
    id?: number;
    /** The name of the event. */
    name?: string;
    /** The template of the event. Only custom events configured by Jira administrators have template. */
    templateEvent?: unknown;
}

/** Details of the users and groups to receive the notification. */
export type NotificationRecipients = Record<string, unknown>;

/** Details of the group membership or permissions needed to receive the notification. */
export interface NotificationRecipientsRestrictions {
    /** List of groupId memberships required to receive the notification. */
    groupIds?: Array<string>;
    /** List of group memberships required to receive the notification. */
    groups?: Array<{ groupId?: string; name?: string; readonly self?: string }>;
    /** List of permissions required to receive the notification. */
    permissions?: Array<Record<string, unknown>>;
}

/** Details about a notification scheme. */
export interface NotificationScheme {
    /** The description of the notification scheme. */
    description?: string;
    /** Expand options that include additional notification scheme details in the response. */
    expand?: string;
    /** The ID of the notification scheme. */
    id?: number;
    /** The name of the notification scheme. */
    name?: string;
    /** The notification events and associated recipients. */
    notificationSchemeEvents?: Array<{ event?: { description?: string; id?: number; name?: string; templateEvent?: unknown }; notifications?: Array<{ emailAddress?: string; expand?: string; field?: { clauseNames?: Array<string>; custom?: boolean; id?: string; key?: string; name?: string; navigable?: boolean; orderable?: boolean; schema?: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string }; scope?: Record<string, unknown>; searchable?: boolean }; group?: { groupId?: string; name?: string; readonly self?: string }; id?: number; notificationType?: "CurrentAssignee" | "Reporter" | "CurrentUser" | "ProjectLead" | "ComponentLead" | "User" | "Group" | "ProjectRole" | "EmailAddress" | "AllWatchers" | "UserCustomField" | "GroupCustomField"; parameter?: string; projectRole?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; recipient?: string; user?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string } }> }>;
    /** The list of project IDs associated with the notification scheme. */
    projects?: Array<number>;
    /** The scope of the notification scheme. */
    scope?: unknown;
    self?: string;

export interface NotificationSchemeAndProjectMappingJsonBean {
    notificationSchemeId?: string;
    projectId?: string;
}

/** Details about a notification scheme event. */
export interface NotificationSchemeEvent {
    /** Details about a notification event. */
    event?: { description?: string; id?: number; name?: string; templateEvent?: unknown };
    notifications?: Array<{ emailAddress?: string; expand?: string; field?: { clauseNames?: Array<string>; custom?: boolean; id?: string; key?: string; name?: string; navigable?: boolean; orderable?: boolean; schema?: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string }; scope?: Record<string, unknown>; searchable?: boolean }; group?: { groupId?: string; name?: string; readonly self?: string }; id?: number; notificationType?: "CurrentAssignee" | "Reporter" | "CurrentUser" | "ProjectLead" | "ComponentLead" | "User" | "Group" | "ProjectRole" | "EmailAddress" | "AllWatchers" | "UserCustomField" | "GroupCustomField"; parameter?: string; projectRole?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; recipient?: string; user?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string } }>;

/** Details of a notification scheme event. */
export type NotificationSchemeEventDetails = Record<string, unknown>;

/** The event ID to use for reference in the payload */
export interface NotificationSchemeEventIDPayload {
    /** The event ID to use for reference in the payload */
    id?: string;
}

/** The payload for creating a notification scheme event. Defines which notifications should be sent for a specific event */
export interface NotificationSchemeEventPayload {
    /** The event ID to use for reference in the payload */
    event?: { id?: string };
    /** The configuration for notification recipents */
    notifications?: Array<{ notificationType?: string; parameter?: string }>;
}

/** The ID of an event that is being mapped to notifications. */
export type NotificationSchemeEventTypeId = Record<string, unknown>;
/** The ID of a notification scheme. */
export type NotificationSchemeId = Record<string, unknown>;
/** Details of a notification within a notification scheme. */
export type NotificationSchemeNotificationDetails = Record<string, unknown>;

/** The configuration for notification recipents */
export interface NotificationSchemeNotificationDetailsPayload {
    /** The type of notification. */
    notificationType?: string;
    /** The parameter of the notification, should be eiither null if not required, or PCRI. */
    parameter?: string;
}

/** The payload for creating a notification scheme. The user has to supply the ID for the default notification scheme. For CMP this is provided in the project payload and should be left empty, for TMP it's provided using this payload */
export interface NotificationSchemePayload {
    /** The description of the notification scheme */
    description?: string;
    /** The name of the notification scheme */
    name?: string;
    /** The events and notifications for the notification scheme */
    notificationSchemeEvents?: Array<{ event?: { id?: string }; notifications?: Array<{ notificationType?: string; parameter?: string }> }>;
    /** The strategy to use when there is a conflict with an existing entity */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
}

export interface OldToNewSecurityLevelMappingsBean {
    /** The new issue security level ID. Providing null will clear the assigned old level from issues. */
    newLevelId: string;
    /** The old issue security level ID. Providing null will remap all issues without any assigned levels. */
    oldLevelId: string;
}

export interface OperationMessage {
    /** The human-readable message that describes the result. */
    message: string;
    /** The status code of the response. */
    statusCode: number;
}

/** Details of the operations that can be performed on the issue. */
export type Operations = Record<string, unknown>;

/** An ordered list of custom field option IDs and information on where to move them. */
export interface OrderOfCustomFieldOptions {
    /** The ID of the custom field option or cascading option to place the moved options after. Required if `position` isn't provided. */
    after?: string;
    /** A list of IDs of custom field options to move. The order of the custom field option IDs in the list is the order they are given after the move. The list must contain custom field options or cascading options, but not both. */
    customFieldOptionIds: Array<string>;
    /** The position the custom field options should be moved to. Required if `after` isn't provided. */
    position?: "First" | "Last";
}

/** An ordered list of issue type IDs and information about where to move them. */
export interface OrderOfIssueTypes {
    /** The ID of the issue type to place the moved issue types after. Required if `position` isn't provided. */
    after?: string;
    /** A list of the issue type IDs to move. The order of the issue type IDs in the list is the order they are given after the move. */
    issueTypeIds: Array<string>;
    /** The position the issue types should be moved to. Required if `after` isn't provided. */
    position?: "First" | "Last";
}

/** A page of items. */
export interface PageBean2ComponentJsonBean {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<Record<string, unknown>>;
}

/** A page of items. */
export interface PageBean2FieldAssociationSchemeFieldSearchResult {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ allowedOperations?: Array<string>; fieldId?: string; parameters?: { description?: string; isRequired?: boolean }; restrictedToWorkTypes?: Array<string>; workTypeParameters?: Array<{ description?: string; isRequired?: boolean; workTypeId?: string }> }>;
}

/** A page of items. */
export interface PageBean2FieldAssociationSchemeProjectSearchResult {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ avatarUrls?: Record<string, string>; id?: string; key?: string; name?: string }>;
}

/** A page of items. */
export interface PageBean2GetFieldAssociationSchemeResponse {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; id?: number; isDefault?: boolean; links?: { associations?: string; projects?: string }; matchedFilters?: { projectIds?: Array<number>; query?: string }; name?: string }>;
}

/** A page of items. */
export interface PageBean2GetProjectsWithFieldSchemesResponse {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ projectId?: number; schemeId?: number }>;
}

/** A page of items. */
export interface PageBean2JqlFunctionPrecomputationBean {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly arguments?: Array<string>; readonly created?: string; readonly error?: string; readonly field?: string; readonly functionKey?: string; readonly functionName?: string; readonly id?: string; readonly operator?: string; readonly updated?: string; readonly used?: string; readonly value?: string }>;
}

/** A page of items. */
export interface PageBean2ProjectFieldBean {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; fieldId?: string; isRequired?: boolean; projectId?: number; workTypeId?: number }>;
}

/** A page of items. */
export interface PageBeanBulkContextualConfiguration {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ configuration?: Record<string, unknown>; customFieldId: string; readonly fieldContextId: string; id: string; schema?: Record<string, unknown> }>;
}

/** A page of items. */
export interface PageBeanChangelog {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>;

/** A page of items. */
export interface PageBeanComment {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<Record<string, unknown>>;
}

/** A page of items. */
export interface PageBeanComponentWithIssueCount {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ assignee?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly issueCount?: number; lead?: unknown; readonly name?: string; readonly project?: string; readonly projectId?: number; realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>;

/** A page of items. */
export interface PageBeanContext {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly id?: number; readonly name?: string; scope?: Record<string, unknown> }>;
}

/** A page of items. */
export interface PageBeanContextForProjectAndIssueType {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ contextId: string; issueTypeId: string; projectId: string }>;
}

/** A page of items. */
export interface PageBeanContextualConfiguration {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ configuration?: Record<string, unknown>; readonly fieldContextId: string; id: string; schema?: Record<string, unknown> }>;
}

/** A page of items. */
export interface PageBeanCustomFieldContext {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description: string; id: string; isAnyIssueType: boolean; isGlobalContext: boolean; name: string }>;
}

/** A page of items. */
export interface PageBeanCustomFieldContextDefaultValue {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ cascadingOptionId?: string; contextId: string; optionId: string; type: string } | { contextId: string; optionIds: Array<string>; type: string } | { contextId: string; optionId: string; type: string } | { accountId: string; contextId: string; type: string; userFilter: { enabled: boolean; groups?: Array<string>; roleIds?: Array<number> } } | { accountIds: Array<string>; contextId: string; type: string } | { contextId: string; groupId: string; type: string } | { contextId: string; groupIds: Array<string>; type: string } | { date?: string; type: string; useCurrent?: boolean } | { dateTime?: string; type: string; useCurrent?: boolean } | { contextId: string; type: string; url: string } | { contextId: string; projectId: string; type: string } | { number: number; type: string } | { labels: Array<string>; type: string } | { text?: string; type: string } | { text?: string; type: string } | { text?: string; type: string } | { type: string; versionId: string; versionOrder?: string } | { type: string; versionIds: Array<string>; versionOrder?: string } | { contextId: string; text?: string; type: string } | { type: string; values?: Array<string> } | { object?: Record<string, unknown>; type: string } | { contextId: string; dateTime?: string; type: string; useCurrent?: boolean } | { contextId: string; groupId: string; type: string } | { contextId: string; groupIds: Array<string>; type: string } | { contextId: string; number: number; type: string } | { accountId: string; contextId: string; type: string; userFilter: unknown } | { accountIds: Array<string>; contextId: string; type: string }>;
}

/** A page of items. */
export interface PageBeanCustomFieldContextOption {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ disabled: boolean; id: string; optionId?: string; value: string }>;
}

/** A page of items. */
export interface PageBeanCustomFieldContextProjectMapping {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly contextId: string; readonly isGlobalContext?: boolean; readonly projectId?: string }>;
}

/** A page of items. */
export interface PageBeanDashboard {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly automaticRefreshMs?: number; description?: string; readonly editPermissions?: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>; readonly id?: string; readonly isFavourite?: boolean; readonly isWritable?: boolean; readonly name?: string; readonly owner?: unknown; readonly popularity?: number; readonly rank?: number; readonly self?: string; readonly sharePermissions?: Array<unknown>; readonly systemDashboard?: boolean; readonly view?: string }>;

/** A page of items. */
export interface PageBeanField {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ contextsCount?: number; description?: string; id: string; isLocked?: boolean; isUnscreenable?: boolean; key?: string; lastUsed?: { type?: "TRACKED" | "NOT_TRACKED" | "NO_INFORMATION"; value?: string }; name: string; projectsCount?: number; schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string }; screensCount?: number; searcherKey?: string; stableId?: string; typeDisplayName?: string }>;
}

/** A page of items. */
export interface PageBeanFieldConfigurationDetails {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; name: string }>;
}

/** A page of items. */
export interface PageBeanFieldConfigurationIssueTypeItem {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ fieldConfigurationId: string; fieldConfigurationSchemeId: string; issueTypeId: string }>;
}

/** A page of items. */
export interface PageBeanFieldConfigurationItem {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; id: string; isHidden?: boolean; isRequired?: boolean; renderer?: string }>;
}

/** A page of items. */
export interface PageBeanFieldConfigurationScheme {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; id: string; name: string }>;
}

/** A page of items. */
export interface PageBeanFieldConfigurationSchemeProjects {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ fieldConfigurationScheme?: { description?: string; id: string; name: string }; projectIds: Array<string> }>;
}

/** A page of items. */
export interface PageBeanFieldProjectAssociation {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ projectId?: string }>;
}

/** A page of items. */
export interface PageBeanFilterDetails {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly approximateLastUsed?: string; description?: string; editPermissions?: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>; readonly expand?: string; readonly favourite?: boolean; readonly favouritedCount?: number; readonly id?: string; readonly jql?: string; name: string; readonly owner?: unknown; readonly searchUrl?: string; readonly self?: string; sharePermissions?: Array<unknown>; readonly subscriptions?: Array<{ readonly group?: unknown; readonly id?: number; readonly user?: unknown }>; readonly viewUrl?: string }>;

/** A page of items. */
export interface PageBeanGroupDetails {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ groupId?: string; name?: string }>;
}

/** A page of items. */
export interface PageBeanIssueFieldOption {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ config?: { attributes?: Array<"notSelectable" | "defaultValue">; scope?: { global?: { attributes?: Array<"notSelectable" | "defaultValue"> }; projects?: Array<number>; projects2?: Array<{ attributes?: Array<"notSelectable" | "defaultValue">; id?: number }> } }; id: number; properties?: Record<string, Record<string, unknown>>; value: string }>;
}

/** A page of items. */
export interface PageBeanIssueSecurityLevelMember {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ holder: { readonly expand?: string; parameter?: string; type: string; value?: string }; id: number; issueSecurityLevelId: number }>;
}

/** A page of items. */
export interface PageBeanIssueSecuritySchemeToProjectMapping {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<Record<string, unknown>>;
}

/** A page of items. */
export interface PageBeanIssueTypeScheme {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ defaultIssueTypeId?: string; description?: string; id: string; isDefault?: boolean; name: string }>;
}

/** A page of items. */
export interface PageBeanIssueTypeSchemeMapping {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ issueTypeId: string; issueTypeSchemeId: string }>;
}

/** A page of items. */
export interface PageBeanIssueTypeSchemeProjects {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ issueTypeScheme: { defaultIssueTypeId?: string; description?: string; id: string; isDefault?: boolean; name: string }; projectIds: Array<string> }>;
}

/** A page of items. */
export interface PageBeanIssueTypeScreenScheme {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; id: string; name: string }>;
}

/** A page of items. */
export interface PageBeanIssueTypeScreenSchemeItem {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ issueTypeId: string; issueTypeScreenSchemeId: string; screenSchemeId: string }>;
}

/** A page of items. */
export interface PageBeanIssueTypeScreenSchemesProjects {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ issueTypeScreenScheme: { description?: string; id: string; name: string }; projectIds: Array<string> }>;
}

/** A page of items. */
export interface PageBeanIssueTypeToContextMapping {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ contextId: string; isAnyIssueType?: boolean; issueTypeId?: string }>;
}

/** A page of items. */
export interface PageBeanNotificationScheme {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; expand?: string; id?: number; name?: string; notificationSchemeEvents?: Array<{ event?: { description?: string; id?: number; name?: string; templateEvent?: unknown }; notifications?: Array<{ emailAddress?: string; expand?: string; field?: { clauseNames?: Array<string>; custom?: boolean; id?: string; key?: string; name?: string; navigable?: boolean; orderable?: boolean; schema?: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string }; scope?: Record<string, unknown>; searchable?: boolean }; group?: { groupId?: string; name?: string; readonly self?: string }; id?: number; notificationType?: "CurrentAssignee" | "Reporter" | "CurrentUser" | "ProjectLead" | "ComponentLead" | "User" | "Group" | "ProjectRole" | "EmailAddress" | "AllWatchers" | "UserCustomField" | "GroupCustomField"; parameter?: string; projectRole?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; recipient?: string; user?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string } }> }>; projects?: Array<number>; scope?: unknown; self?: string }>;

/** A page of items. */
export interface PageBeanNotificationSchemeAndProjectMappingJsonBean {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ notificationSchemeId?: string; projectId?: string }>;
}

/** A page of items. */
export interface PageBeanPriority {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<Record<string, unknown>>;
}

/** A page of items. */
export interface PageBeanPrioritySchemeWithPaginatedPrioritiesAndProjects {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<Record<string, unknown>>;
}

/** A page of items. */
export interface PageBeanPriorityWithSequence {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; iconUrl?: string; id?: string; isDefault?: boolean; name?: string; self?: string; sequence?: string; statusColor?: string }>;
}

/** A page of items. */
export interface PageBeanProject {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }>;

/** A page of items. */
export interface PageBeanProjectDetails {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; id?: string; readonly key?: string; readonly name?: string; readonly projectCategory?: { readonly description?: string; readonly id?: string; readonly name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly self?: string; readonly simplified?: boolean }>;

/** A page of items. */
export interface PageBeanResolutionJsonBean {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ default?: boolean; description?: string; iconUrl?: string; id?: string; name?: string; self?: string }>;
}

/** A page of items. */
export interface PageBeanScreen {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly description?: string; readonly id?: number; readonly name?: string; scope?: Record<string, unknown> }>;
}

/** A page of items. */
export interface PageBeanScreenScheme {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ description?: string; id?: number; issueTypeScreenSchemes?: { readonly isLast?: boolean; readonly maxResults?: number; readonly nextPage?: string; readonly self?: string; readonly startAt?: number; readonly total?: number; readonly values?: Array<{ description?: string; id: string; name: string }> }; name?: string; screens?: { create?: number; default: number; edit?: number; view?: number } }>;
}

/** A page of items. */
export interface PageBeanScreenWithTab {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly description?: string; readonly id?: number; readonly name?: string; scope?: Record<string, unknown>; tab?: { readonly id?: number; name: string } }>;
}

/** A page of items. */
export interface PageBeanSecurityLevel {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly description?: string; readonly id?: string; readonly isDefault?: boolean; readonly issueSecuritySchemeId?: string; readonly name?: string; readonly self?: string }>;
}

/** A page of items. */
export interface PageBeanSecurityLevelMember {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<Record<string, unknown>>;
}

/** A page of items. */
export interface PageBeanSecuritySchemeWithProjects {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<Record<string, unknown>>;
}

/** A page of items. */
export interface PageBeanString {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<string>;
}

/** A page of items. */
export interface PageBeanUiModificationDetails {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly contexts?: Array<{ readonly id?: string; readonly isAvailable?: boolean; issueTypeId?: string; portalId?: string; projectId?: string; requestTypeId?: string; viewType?: "GIC" | "IssueView" | "IssueTransition" | "JSMRequestCreate" }>; readonly data?: string; readonly description?: string; readonly id: string; readonly name: string; readonly self: string }>;
}

/** A page of items. */
export interface PageBeanUser {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }>;

/** A page of items. */
export interface PageBeanUserDetails {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }>;

/** A page of items. */
export interface PageBeanUserKey {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ accountId?: string; key?: string }>;
}

/** A page of items. */
export interface PageBeanVersion {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }>;
}

/** A page of items. */
export interface PageBeanWebhook {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ events: Array<"jira:issue_created" | "jira:issue_updated" | "jira:issue_deleted" | "comment_created" | "comment_updated" | "comment_deleted" | "issue_property_set" | "issue_property_deleted" | "sprint_created" | "sprint_updated" | "sprint_closed" | "sprint_deleted" | "sprint_started" | "jira:version_released" | "jira:version_unreleased" | "jira:version_created" | "jira:version_moved" | "jira:version_updated" | "jira:version_merged" | "jira:version_deleted">; readonly expirationDate?: number; fieldIdsFilter?: Array<string>; id: number; issuePropertyKeysFilter?: Array<string>; jqlFilter: string; url: string }>;
}

/** A page of items. */
export interface PageBeanWorkflow {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ created?: string; description: string; hasDraftWorkflow?: boolean; id: { entityId?: string; name: string }; isDefault?: boolean; operations?: { canDelete: boolean; canEdit: boolean }; projects?: Array<{ readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; id?: string; readonly key?: string; readonly name?: string; readonly projectCategory?: { readonly description?: string; readonly id?: string; readonly name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly self?: string; readonly simplified?: boolean }>; schemes?: Array<{ id: string; name: string }>; statuses?: Array<{ id: string; name: string; properties?: Record<string, Record<string, unknown>> }>; transitions?: Array<{ description: string; from: Array<string>; id: string; name: string; properties?: Record<string, Record<string, unknown>>; rules?: { conditionsTree?: { configuration?: Record<string, unknown>; nodeType: string; type: string } | { conditions: Array<unknown>; nodeType: string; operator: "AND" | "OR" }; postFunctions?: Array<{ configuration?: Record<string, unknown>; type: string }>; validators?: Array<unknown> }; screen?: { id: string; name?: string }; to: string; type: "global" | "initial" | "directed" }>; updated?: string }>;

/** A page of items. */
export interface PageBeanWorkflowScheme {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ defaultWorkflow?: string; description?: string; readonly draft?: boolean; readonly id?: number; issueTypeMappings?: Record<string, string>; readonly issueTypes?: Record<string, { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly lastModified?: string; readonly lastModifiedUser?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; name?: string; readonly originalDefaultWorkflow?: string; readonly originalIssueTypeMappings?: Record<string, string>; readonly self?: string; updateDraftIfNeeded?: boolean }>;

/** A page of items. */
export interface PageBeanWorkflowTransitionRules {
    /** Whether this is the last page. */
    readonly isLast?: boolean;
    /** The maximum number of items that could be returned. */
    readonly maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    readonly nextPage?: string;
    /** The URL of the page. */
    readonly self?: string;
    /** The index of the first item returned. */
    readonly startAt?: number;
    /** The number of items returned. */
    readonly total?: number;
    /** The list of items. */
    readonly values?: Array<{ conditions?: Array<{ configuration: { disabled?: boolean; tag?: string; value: string }; id: string; readonly key: string; readonly transition?: { id: number; name: string } }>; postFunctions?: Array<unknown>; validators?: Array<unknown>; workflowId: { draft?: boolean; name: string } }>;
}

/** A page of changelogs. */
export interface PageOfChangelogs {
    /** The list of changelogs. */
    readonly histories?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>;
    /** The maximum number of results that could be on the page. */
    readonly maxResults?: number;
    /** The index of the first item returned on the page. */
    readonly startAt?: number;
    /** The number of results on the page. */
    readonly total?: number;

/** A page of comments. */
export type PageOfComments = Record<string, unknown>;
/** A page of CreateMetaIssueType with Field. */
export type PageOfCreateMetaIssueTypeWithField = Record<string, unknown>;
/** A page of CreateMetaIssueTypes. */
export type PageOfCreateMetaIssueTypes = Record<string, unknown>;

/** A page containing dashboard details. */
export interface PageOfDashboards {
    /** List of dashboards. */
    readonly dashboards?: Array<{ readonly automaticRefreshMs?: number; description?: string; readonly editPermissions?: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>; readonly id?: string; readonly isFavourite?: boolean; readonly isWritable?: boolean; readonly name?: string; readonly owner?: unknown; readonly popularity?: number; readonly rank?: number; readonly self?: string; readonly sharePermissions?: Array<unknown>; readonly systemDashboard?: boolean; readonly view?: string }>;
    /** The maximum number of results that could be on the page. */
    readonly maxResults?: number;
    /** The URL of the next page of results, if any. */
    readonly next?: string;
    /** The URL of the previous page of results, if any. */
    readonly prev?: string;
    /** The index of the first item returned on the page. */
    readonly startAt?: number;
    /** The number of results on the page. */
    readonly total?: number;

export interface PageOfStatuses {
    /** Whether this is the last page. */
    isLast?: boolean;
    /** The maximum number of items that could be returned. */
    maxResults?: number;
    /** The URL of the next page of results, if any. */
    nextPage?: string;
    /** The URL of this page. */
    self?: string;
    /** The index of the first item returned on the page. */
    startAt?: number;
    /** Number of items that satisfy the search. */
    total?: number;
    /** The list of items. */
    values?: Array<{ description?: string; id?: string; name?: string; scope?: { project?: { id: string }; type: "PROJECT" | "GLOBAL" }; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE" }>;
}

/** Paginated list of worklog details */
export type PageOfWorklogs = Record<string, unknown>;

export interface PageWithCursorGetPlanResponseForPage {
    cursor?: string;
    last?: boolean;
    nextPageCursor?: string;
    size?: number;
    total?: number;
    values?: Array<{ id: string; issueSources?: Array<{ type: "Board" | "Project" | "Filter" | "Custom"; value: number }>; name: string; scenarioId: string; status: "Active" | "Trashed" | "Archived" }>;
}

export interface PageWithCursorGetTeamResponseForPage {
    cursor?: string;
    last?: boolean;
    nextPageCursor?: string;
    size?: number;
    total?: number;
    values?: Array<{ id: string; name?: string; type: "PlanOnly" | "Atlassian" }>;
}

/** A paged list. To access additional details append `[start-index:end-index]` to the expand request. For example, `?expand=sharedUsers[10:40]` returns a list starting at item 10 and finishing at item 40. */
export interface PagedListUserDetailsApplicationUser {
    /** The index of the last item returned on the page. */
    readonly end-index?: number;
    /** The list of items. */
    readonly items?: Array<{ accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }>;
    /** The maximum number of results that could be on the page. */
    readonly max-results?: number;
    /** The number of items on the page. */
    readonly size?: number;
    /** The index of the first item returned on the page. */
    readonly start-index?: number;

export interface PaginatedResponseComment {
    maxResults?: number;
    results?: Array<Record<string, unknown>>;
    startAt?: number;
    total?: number;
}

export interface PaginatedResponseFieldCreateMetadata {
    maxResults?: number;
    results?: Array<{ readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly fieldId: string; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }>;
    startAt?: number;
    total?: number;
}

export interface PaginatedResponseIssueTypeIssueCreateMetadata {
    maxResults?: number;
    results?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly expand?: string; readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }>; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>;
    startAt?: number;
    total?: number;
}

export interface ParameterRemovalDetails {
    /** Set of parameter names to remove */
    parameters?: Array<string>;
    /** ID of the field scheme */
    schemeId?: number;
    /** Set of work type (issue type) IDs */
    workTypeIds?: Array<number>;
}

/** A list of parsed JQL queries. */
export interface ParsedJqlQueries {
    /** A list of parsed JQL queries. */
    queries: Array<{ errors?: Array<string>; query: string; structure?: { orderBy?: { fields: Array<{ direction?: "asc" | "desc"; field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> } }> }; where?: { clauses: Array<unknown>; operator: "and" | "or" | "not" } | { field: unknown; operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown; operator: "=" | "!=" | ">" | "<" | ">=" | "<=" | "in" | "not in" | "~" | "~=" | "is" | "is not" } | { field: unknown; operand: unknown; operator: "was" | "was in" | "was not in" | "was not"; predicates: Array<{ operand: unknown; operator: "before" | "after" | "from" | "to" | "on" | "during" | "by" }> } | { field: unknown; operator: "changed"; predicates: Array<unknown> } }; warnings?: Array<string> }>;
}

/** Details of a parsed JQL query. */
export interface ParsedJqlQuery {
    /** The list of syntax or validation errors. */
    errors?: Array<string>;
    /** The JQL query that was parsed and validated. */
    query: string;
    /** The syntax tree of the query. Empty if the query was invalid. */
    structure?: { orderBy?: { fields: Array<{ direction?: "asc" | "desc"; field: { encodedName?: string; name: string; property?: Array<{ entity: string; key: string; path: string; type?: "number" | "string" | "text" | "date" | "user" }> } }> }; where?: { clauses: Array<unknown>; operator: "and" | "or" | "not" } | { field: unknown; operand: { encodedOperand?: string; values: Array<{ encodedValue?: string; value: string } | { arguments: Array<string>; encodedOperand?: string; function: string } | { keyword: "empty" }> } | unknown | unknown | unknown; operator: "=" | "!=" | ">" | "<" | ">=" | "<=" | "in" | "not in" | "~" | "~=" | "is" | "is not" } | { field: unknown; operand: unknown; operator: "was" | "was in" | "was not in" | "was not"; predicates: Array<{ operand: unknown; operator: "before" | "after" | "from" | "to" | "on" | "during" | "by" }> } | { field: unknown; operator: "changed"; predicates: Array<unknown> } };
    /** The list of warning messages */
    warnings?: Array<string>;
}

/** Details for permissions of shareable entities */
export interface PermissionDetails {
    /** The edit permissions for the shareable entities. */
    editPermissions: Array<{ group?: { groupId?: string; name?: string; readonly self?: string }; readonly id?: number; project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> }; role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string }; type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown"; user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string } }>;
    /** The share permissions for the shareable entities. */
    sharePermissions: Array<unknown>;

/** Details about a permission granted to a user or group. */
export type PermissionGrant = Record<string, unknown>;

/** List of permission grants */
export interface PermissionGrantDTO {
    applicationAccess?: Array<string>;
    groupCustomFields?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }>;
    groups?: Array<unknown>;
    permissionKeys?: Array<string>;
    projectRoles?: Array<unknown>;
    specialGrants?: Array<string>;
    userCustomFields?: Array<unknown>;
    users?: Array<unknown>;
}

/** List of permission grants. */
export interface PermissionGrants {
    /** Expand options that include additional permission grant details in the response. */
    readonly expand?: string;
    /** Permission grants list. */
    readonly permissions?: Array<Record<string, unknown>>;
}

/** Details of a user, group, field, or project role that holds a permission. See [Holder object](../api-group-permission-schemes/#holder-object) in *Get all permission schemes* for more information. */
export interface PermissionHolder {
    /** Expand options that include additional permission holder details in the response. */
    readonly expand?: string;
    /** As a group's name can change, use of `value` is recommended. The identifier associated withthe `type` value that defines the holder of the permission. */
    parameter?: string;
    /** The type of permission holder. */
    type: string;
    /** The identifier associated with the `type` value that defines the holder of the permission. */
    value?: string;
}

/** The payload to create a permission scheme */
export interface PermissionPayloadDTO {
    /** Configuration to generate addon role. Default is false if null. Only applies to GLOBAL-scoped permission scheme */
    addAddonRole?: boolean;
    /** The description of the permission scheme */
    description?: string;
    /** List of permission grants */
    grants?: Array<{ applicationAccess?: Array<string>; groupCustomFields?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }>; groups?: Array<unknown>; permissionKeys?: Array<string>; projectRoles?: Array<unknown>; specialGrants?: Array<string>; userCustomFields?: Array<unknown>; users?: Array<unknown> }>;
    /** The name of the permission scheme */
    name?: string;
    /** The strategy to use when there is a conflict with an existing permission scheme. FAIL - Fail execution, this always needs to be unique; USE - Use the existing entity and ignore new entity parameters; NEW - If the entity exist, try and create a new one with a different name */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

/** Details of a permission scheme. */
export type PermissionScheme = Record<string, unknown>;

/** List of all permission schemes. */
export interface PermissionSchemes {
    /** Permission schemes list. */
    readonly permissionSchemes?: Array<Record<string, unknown>>;
}

/** Details about permissions. */
export interface Permissions {
    /** List of permissions. */
    readonly permissions?: Record<string, Record<string, unknown>>;
}

export interface PermissionsKeysBean {
    /** A list of permission keys. */
    permissions: Array<string>;
}

/** A list of projects in which a user is granted permissions. */
export interface PermittedProjects {
    /** A list of projects. */
    readonly projects?: Array<{ readonly id?: number; readonly key?: string }>;
}

/** Condition group configuration for workflow transitions. */
export interface PreviewConditionGroupConfiguration {
    /** The nested conditions of the condition group. */
    conditionGroups?: Array<unknown>;
    /** The rules for this condition. */
    conditions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey?: string }>;
    /** Determines how the conditions in the group are evaluated. Accepts either `ANY` or `ALL`. If `ANY` is used, at least one condition in the group must be true for the group to evaluate to true. If `ALL` is used, all conditions in the group must be true for the group to evaluate to true. */
    operation?: "ANY" | "ALL";
}

/** Rule configuration for workflow transitions. */
export interface PreviewRuleConfiguration {
    /** A transient identifier for this element, unique within this response but not guaranteed to stable across requests. */
    id?: string;
    /** The parameters of the rule. */
    parameters?: Record<string, string>;
    /** The rule key of the rule. */
    ruleKey?: string;
}

/** Trigger configuration for workflow transitions. */
export interface PreviewTrigger {
    /** The ID of the trigger. */
    id?: string;
    /** The key of the trigger rule. */
    ruleKey?: string;
}

/** An issue priority. */
export type Priority = Record<string, unknown>;
/** The ID of an issue priority. */
export type PriorityId = Record<string, unknown>;

/** Mapping of issue priorities for changes in priority schemes. */
export interface PriorityMapping {
    /**
     * The mapping of priorities for issues being migrated **into** this priority scheme. Key is the old priority ID, value is the new priority ID (must exist in this priority scheme).
     *
     * E.g. The current priority scheme has priority ID `10001`. Issues with priority ID `10000` are being migrated into this priority scheme will need mapping to new priorities. The `in` mapping would be `{"10000": 10001}`.
     */
    in?: Record<string, number>;
    /**
     * The mapping of priorities for issues being migrated **out of** this priority scheme. Key is the old priority ID (must exist in this priority scheme), value is the new priority ID (must exist in the default priority scheme). Required for updating an existing priority scheme. Not used when creating a new priority scheme.
     *
     * E.g. The current priority scheme has priority ID `10001`. Issues with priority ID `10001` are being migrated out of this priority scheme will need mapping to new priorities. The `out` mapping would be `{"10001": 10000}`.
     */
    out?: Record<string, number>;
}

export interface PrioritySchemeChangesWithoutMappings {
    /** Affected entity ids. */
    ids: Array<number>;
}

/** The ID of a priority scheme. */
export interface PrioritySchemeId {
    /** The ID of the priority scheme. */
    readonly id?: string;
    /** The in-progress issue migration task. */
    readonly task?: { description?: string; elapsedRuntime: number; finished?: number; id: string; lastUpdate: number; message?: string; progress: number; result?: { array?: boolean; bigDecimal?: boolean; bigInteger?: boolean; bigIntegerValue?: number; binary?: boolean; binaryValue?: Array<string>; boolean?: boolean; booleanValue?: boolean; containerNode?: boolean; decimalValue?: number; double?: boolean; doubleValue?: number; elements?: Record<string, unknown>; fieldNames?: Record<string, unknown>; fields?: Record<string, unknown>; floatingPointNumber?: boolean; int?: boolean; intValue?: number; integralNumber?: boolean; long?: boolean; longValue?: number; missingNode?: boolean; null?: boolean; number?: boolean; numberType?: "INT" | "LONG" | "BIG_INTEGER" | "FLOAT" | "DOUBLE" | "BIG_DECIMAL"; numberValue?: number; object?: boolean; pojo?: boolean; textValue?: string; textual?: boolean; valueAsBoolean?: boolean; valueAsDouble?: number; valueAsInt?: number; valueAsLong?: number; valueAsText?: string; valueNode?: boolean }; self: string; started?: number; status: "ENQUEUED" | "RUNNING" | "COMPLETE" | "FAILED" | "CANCEL_REQUESTED" | "CANCELLED" | "DEAD"; submitted: number; submittedBy: number };
}

/** A priority scheme with paginated priorities and projects. */
export type PrioritySchemeWithPaginatedPrioritiesAndProjects = Record<string, unknown>;

/** An issue priority with sequence information. */
export interface PriorityWithSequence {
    /** The description of the issue priority. */
    description?: string;
    /** The URL of the icon for the issue priority. */
    iconUrl?: string;
    /** The ID of the issue priority. */
    id?: string;
    /** Whether this priority is the default. */
    isDefault?: boolean;
    /** The name of the issue priority. */
    name?: string;
    /** The URL of the issue priority. */
    self?: string;
    /** The sequence of the issue priority. */
    sequence?: string;
    /** The color used to indicate the issue priority. */
    statusColor?: string;
}

/** Details about a project. */
export interface Project {
    /** Whether the project is archived. */
    readonly archived?: boolean;
    /** The user who archived the project. */
    readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string };
    /** The date when the project was archived. */
    readonly archivedDate?: string;
    /** The default assignee when creating issues for this project. */
    readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED";
    /** The URLs of the project's avatars. */
    readonly avatarUrls?: unknown;
    /** List of the components contained in the project. */
    readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>;
    /** Whether the project is marked as deleted. */
    readonly deleted?: boolean;
    /** The user who marked the project as deleted. */
    readonly deletedBy?: unknown;
    /** The date when the project was marked as deleted. */
    readonly deletedDate?: string;
    /** A brief description of the project. */
    readonly description?: string;
    /** An email address associated with the project. */
    email?: string;
    /** Expand options that include additional project details in the response. */
    readonly expand?: string;
    /** Whether the project is selected as a favorite. */
    favourite?: boolean;
    /** The ID of the project. */
    id?: string;
    /** Insights about the project. */
    readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number };
    /** Whether the project is private from the user's perspective. This means the user can't see the project or any associated issues. */
    readonly isPrivate?: boolean;
    /** The issue type hierarchy for the project. */
    readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> };
    /** List of the issue types available in the project. */
    readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>;
    /** The key of the project. */
    readonly key?: string;
    /** The project landing page info. */
    readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string };
    /** The username of the project lead. */
    readonly lead?: unknown;
    /** The name of the project. */
    readonly name?: string;
    /** User permissions on the project */
    readonly permissions?: { readonly canEdit?: boolean };
    /** The category the project belongs to. */
    readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string };
    /** The [project type](https://confluence.atlassian.com/x/GwiiLQ#Jiraapplicationsoverview-Productfeaturesandprojecttypes) of the project. */
    readonly projectTypeKey?: "software" | "service_desk" | "business";
    /** Map of project properties */
    readonly properties?: Record<string, Record<string, unknown>>;
    /** The date when the project is deleted permanently. */
    readonly retentionTillDate?: string;
    /** The name and self URL for each role defined in the project. For more information, see [Create project role](#api-rest-api-3-role-post). */
    readonly roles?: Record<string, string>;
    /** The URL of the project details. */
    readonly self?: string;
    /** Whether the project is simplified. */
    readonly simplified?: boolean;
    /** The type of the project. */
    readonly style?: "classic" | "next-gen";
    /** A link to information about this project, such as project documentation. */
    readonly url?: string;
    /** Unique ID for next-gen projects. */
    readonly uuid?: string;
    /** The versions defined in the project. For more information, see [Create version](#api-rest-api-3-version-post). */
    readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }>;

/** A project and issueType ID pair that identifies a status mapping. */
export interface ProjectAndIssueTypePair {
    /** The ID of the issue type. */
    issueTypeId: string;
    /** The ID of the project. */
    projectId: string;
}

export interface ProjectArchetype {
    realType?: "BUSINESS" | "SOFTWARE" | "PRODUCT_DISCOVERY" | "SERVICE_DESK" | "CUSTOMER_SERVICE" | "OPS";
    style?: "classic" | "next-gen";
    type?: "BUSINESS" | "SOFTWARE" | "PRODUCT_DISCOVERY" | "SERVICE_DESK" | "CUSTOMER_SERVICE" | "OPS";
}

/** List of project avatars. */
export interface ProjectAvatars {
    /** List of avatars added to Jira. These avatars may be deleted. */
    readonly custom?: Array<Record<string, unknown>>;
    /** List of avatars included with Jira. These avatars cannot be deleted. */
    readonly system?: Array<unknown>;
}

/** A project category. */
export interface ProjectCategory {
    /** The description of the project category. */
    description?: string;
    /** The ID of the project category. */
    readonly id?: string;
    /** The name of the project category. Required on create, optional on update. */
    name?: string;
    /** The URL of the project category. */
    readonly self?: string;
}

/** Details about a project component. */
export interface ProjectComponent {
    /** Compass component's ID. Can't be updated. Not required for creating a Project Component. */
    readonly ari?: string;
    /** The details of the user associated with `assigneeType`, if any. See `realAssignee` for details of the user assigned to issues created with this component. */
    readonly assignee?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string };
    /**
     * The nominal user type used to determine the assignee for issues created with this component. See `realAssigneeType` for details on how the type of the user, and hence the user, assigned to issues is determined. Can take the following values:
     *
     *  *  `PROJECT_LEAD` the assignee to any issues created with this component is nominally the lead for the project the component is in.
     *  *  `COMPONENT_LEAD` the assignee to any issues created with this component is nominally the lead for the component.
     *  *  `UNASSIGNED` an assignee is not set for issues created with this component.
     *  *  `PROJECT_DEFAULT` the assignee to any issues created with this component is nominally the default assignee for the project that the component is in.
     *
     * Default value: `PROJECT_DEFAULT`.  
     * Optional when creating or updating a component.
     */
    assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED";
    /** The description for the component. Optional when creating or updating a component. */
    description?: string;
    /** The unique identifier for the component. */
    readonly id?: string;
    /** Whether a user is associated with `assigneeType`. For example, if the `assigneeType` is set to `COMPONENT_LEAD` but the component lead is not set, then `false` is returned. */
    readonly isAssigneeTypeValid?: boolean;
    /** The user details for the component's lead user. */
    readonly lead?: unknown;
    /** The accountId of the component's lead user. The accountId uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. */
    leadAccountId?: string;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    leadUserName?: string;
    /** Compass component's metadata. Can't be updated. Not required for creating a Project Component. */
    readonly metadata?: Record<string, string>;
    /** The unique name for the component in the project. Required when creating a component. Optional when updating a component. The maximum length is 255 characters. */
    name?: string;
    /** The key of the project the component is assigned to. Required when creating a component. Can't be updated. */
    project?: string;
    /** The ID of the project the component is assigned to. */
    readonly projectId?: number;
    /** The user assigned to issues created with this component, when `assigneeType` does not identify a valid assignee. */
    readonly realAssignee?: unknown;
    /**
     * The type of the assignee that is assigned to issues created with this component, when an assignee cannot be set from the `assigneeType`. For example, `assigneeType` is set to `COMPONENT_LEAD` but no component lead is set. This property is set to one of the following values:
     *
     *  *  `PROJECT_LEAD` when `assigneeType` is `PROJECT_LEAD` and the project lead has permission to be assigned issues in the project that the component is in.
     *  *  `COMPONENT_LEAD` when `assignee`Type is `COMPONENT_LEAD` and the component lead has permission to be assigned issues in the project that the component is in.
     *  *  `UNASSIGNED` when `assigneeType` is `UNASSIGNED` and Jira is configured to allow unassigned issues.
     *  *  `PROJECT_DEFAULT` when none of the preceding cases are true.
     */
    readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED";
    /** The URL of the component. */
    readonly self?: string;

/** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
export interface ProjectCreateResourceIdentifier {
    anID?: boolean;
    areference?: boolean;
    entityId?: string;
    entityType?: string;
    id?: string;
    type?: "id" | "ref";
}

/** Request to create a project using a custom template */
export interface ProjectCustomTemplateCreateRequestDTO {
    /** Project Details */
    details?: { accessLevel?: "open" | "limited" | "private" | "free"; additionalProperties?: Record<string, string>; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; avatarId?: number; categoryId?: number; description?: string; enableComponents?: boolean; key?: string; language?: string; leadAccountId?: string; name?: string; url?: string };
    /** The specific request object for creating a project with template. */
    template?: { boardFeatures?: { boardFeatures?: Record<string, Array<{ featureKey?: "ESTIMATION" | "SPRINTS"; state?: boolean }>> }; boards?: { boards?: Array<{ boardFilterJQL?: string; cardColorStrategy?: "ISSUE_TYPE" | "REQUEST_TYPE" | "ASSIGNEE" | "PRIORITY" | "NONE" | "CUSTOM"; cardLayout?: { showDaysInColumn?: boolean }; cardLayouts?: Array<{ fieldId?: string; id?: number; mode?: "PLAN" | "WORK"; position?: number }>; columns?: Array<{ maximumIssueConstraint?: number; minimumIssueConstraint?: number; name?: string; statusIds?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }> }>; features?: Array<unknown>; name?: string; pcri?: unknown; quickFilters?: Array<{ description?: string; jqlQuery?: string; name?: string }>; supportsSprint?: boolean; swimlanes?: { customSwimlanes?: Array<{ description?: string; jqlQuery?: string; name?: string }>; defaultCustomSwimlaneName?: string; swimlaneStrategy?: "none" | "custom" | "parentChild" | "assignee" | "assigneeUnassignedFirst" | "epic" | "project" | "issueparent" | "issuechildren" | "request_type" }; workingDaysConfig?: { friday?: boolean; id?: number; monday?: boolean; nonWorkingDays?: Array<{ id?: number; iso8601Date?: string }>; saturday?: boolean; sunday?: boolean; thursday?: boolean; timezoneId?: string; tuesday?: boolean; wednesday?: boolean } }> }; field?: { customFieldDefinitions?: Array<{ cfType?: string; description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; scope?: "GLOBAL" | "TEMPLATE" | "PROJECT"; searcherKey?: string }>; fieldLayoutScheme?: { defaultFieldLayout?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; pcri?: unknown }; fieldLayouts?: Array<{ configuration?: Array<{ field?: boolean; pcri?: unknown; required?: boolean }>; description?: string; name?: string; pcri?: unknown }>; fieldScheme?: { description?: string; items?: Array<{ description?: string; pcri?: unknown; qualifierId?: unknown; qualifierType?: unknown; rendererType?: string; required?: boolean }>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }; issueLayouts?: Array<{ containerId?: unknown; issueLayoutType?: "ISSUE_VIEW" | "ISSUE_CREATE" | "REQUEST_FORM"; items?: Array<{ itemKey?: unknown; properties?: Record<string, Record<string, unknown>>; sectionType?: "content" | "primaryContext" | "secondaryContext"; type?: "FIELD" }>; pcri?: unknown }>; issueTypeScreenScheme?: { defaultScreenScheme?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; pcri?: unknown }; screenScheme?: Array<{ defaultScreen?: unknown; description?: string; name?: string; pcri?: unknown; screens?: Record<string, unknown> }>; screens?: Array<{ description?: string; name?: string; pcri?: unknown; tabs?: Array<{ fields?: Array<unknown>; name?: string }> }> }; issueType?: { issueTypeHierarchy?: Array<{ hierarchyLevel?: number; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }>; issueTypeScheme?: { defaultIssueTypeId?: unknown; description?: string; issueTypeIds?: Array<unknown>; name?: string; pcri?: unknown }; issueTypes?: Array<{ avatarId?: number; description?: string; hierarchyLevel?: number; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }> }; notification?: { description?: string; name?: string; notificationSchemeEvents?: Array<{ event?: { id?: string }; notifications?: Array<{ notificationType?: string; parameter?: string }> }>; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }; permissionScheme?: { addAddonRole?: boolean; description?: string; grants?: Array<{ applicationAccess?: Array<string>; groupCustomFields?: Array<unknown>; groups?: Array<unknown>; permissionKeys?: Array<string>; projectRoles?: Array<unknown>; specialGrants?: Array<string>; userCustomFields?: Array<unknown>; users?: Array<unknown> }>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }; project?: { fieldLayoutSchemeId?: unknown; issueSecuritySchemeId?: unknown; issueTypeSchemeId?: unknown; issueTypeScreenSchemeId?: unknown; notificationSchemeId?: unknown; pcri?: unknown; permissionSchemeId?: unknown; projectTypeKey?: "software" | "business" | "service_desk" | "product_discovery"; workflowSchemeId?: unknown }; role?: { roleToProjectActors?: Record<string, Array<unknown>>; roles?: Array<{ defaultActors?: Array<unknown>; description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; type?: "HIDDEN" | "VIEWABLE" | "EDITABLE" | "GUEST" }> }; scope?: { type?: "GLOBAL" | "PROJECT" }; security?: { description?: string; name?: string; pcri?: unknown; securityLevels?: Array<{ description?: string; isDefault?: boolean; name?: string; securityLevelMembers?: Array<{ parameter?: string; type?: "group" | "reporter" | "users" }> }> }; workflow?: { statuses?: Array<{ description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE" }>; workflowScheme?: { defaultWorkflow?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown }; workflows?: Array<{ description?: string; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; startPointLayout?: unknown; statuses?: Array<{ layout?: unknown; pcri?: unknown; properties?: Record<string, string> }>; transitions?: Array<{ actions?: Array<{ parameters?: Record<string, string>; ruleKey?: string }>; conditions?: { conditionGroup?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; from?: Array<{ fromPort?: number; status?: unknown; toPortOverride?: number }>; id?: number; name?: string; properties?: Record<string, string>; to?: { port?: number; status?: unknown }; transitionScreen?: unknown; triggers?: Array<unknown>; type?: "global" | "initial" | "directed"; validators?: Array<unknown> }> }> } };
}

/** Details about data policies for a list of projects. */
export interface ProjectDataPolicies {
    /** List of projects with data policies. */
    readonly projectDataPolicies?: Array<{ readonly dataPolicy?: { readonly anyContentBlocked?: boolean }; readonly id?: number }>;
}

/** Details about data policy. */
export interface ProjectDataPolicy {
    /** Whether the project contains any content inaccessible to the requesting application. */
    readonly anyContentBlocked?: boolean;
}

/** Details about a project. */
export interface ProjectDetails {
    /** The URLs of the project's avatars. */
    readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string };
    /** The ID of the project. */
    id?: string;
    /** The key of the project. */
    readonly key?: string;
    /** The name of the project. */
    readonly name?: string;
    /** The category the project belongs to. */
    readonly projectCategory?: { readonly description?: string; readonly id?: string; readonly name?: string; readonly self?: string };
    /** The [project type](https://confluence.atlassian.com/x/GwiiLQ#Jiraapplicationsoverview-Productfeaturesandprojecttypes) of the project. */
    readonly projectTypeKey?: "software" | "service_desk" | "business";
    /** The URL of the project details. */
    readonly self?: string;
    /** Whether or not the project is simplified. */
    readonly simplified?: boolean;

/** A project's sender email address. */
export interface ProjectEmailAddress {
    /** The email address. */
    emailAddress?: string;
    /** When using a custom domain, the status of the email address. */
    emailAddressStatus?: Array<string>;
}

/** Details of a project feature. */
export interface ProjectFeature {
    /** The key of the feature. */
    feature?: string;
    /** URI for the image representing the feature. */
    imageUri?: string;
    /** Localized display description for the feature. */
    localisedDescription?: string;
    /** Localized display name for the feature. */
    localisedName?: string;
    /** List of keys of the features required to enable the feature. */
    prerequisites?: Array<string>;
    /** The ID of the project. */
    projectId?: number;
    /** The state of the feature. When updating the state of a feature, only ENABLED and DISABLED are supported. Responses can contain all values */
    state?: "ENABLED" | "DISABLED" | "COMING_SOON";
    /** Whether the state of the feature can be updated. */
    toggleLocked?: boolean;
}

/** Details of the feature state. */
export interface ProjectFeatureState {
    /** The feature state. */
    state?: "ENABLED" | "DISABLED" | "COMING_SOON";
}

export interface ProjectFieldBean {
    description?: string;
    fieldId?: string;
    isRequired?: boolean;
    projectId?: number;
    workTypeId?: number;
}

/** Project ID details. */
export interface ProjectId {
    /** The ID of the project. */
    id: string;
}

export type ProjectIdAssociationContext = { identifier?: Record<string, unknown>; type: string } & { identifier?: number };

/** The identifiers for a project. */
export interface ProjectIdentifierBean {
    /** The ID of the project. */
    readonly id?: number;
    /** The key of the project. */
    readonly key?: string;
}

/** Identifiers for a project. */
export interface ProjectIdentifiers {
    /** The ID of the created project. */
    readonly id: number;
    /** The key of the created project. */
    readonly key: string;
    /** The URL of the created project. */
    readonly self: string;
}

/** A list of project IDs. */
export interface ProjectIds {
    /** The IDs of projects. */
    projectIds: Array<string>;
}

/** Additional details about a project. */
export interface ProjectInsight {
    /** The last issue update time. */
    readonly lastIssueUpdateTime?: string;
    /** Total issue count. */
    readonly totalIssueCount?: number;
}

/** Details of the issue creation metadata for a project. */
export interface ProjectIssueCreateMetadata {
    /** List of the project's avatars, returning the avatar size and associated URL. */
    readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string };
    /** Expand options that include additional project issue create metadata details in the response. */
    readonly expand?: string;
    /** The ID of the project. */
    readonly id?: string;
    /** List of the issue types supported by the project. */
    readonly issuetypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly expand?: string; readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }>; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>;
    /** The key of the project. */
    readonly key?: string;
    /** The name of the project. */
    readonly name?: string;
    /** The URL of the project. */
    readonly self?: string;

/** List of issue level security items in a project. */
export interface ProjectIssueSecurityLevels {
    /** Issue level security items list. */
    readonly levels: Array<{ readonly description?: string; readonly id?: string; readonly isDefault?: boolean; readonly issueSecuritySchemeId?: string; readonly name?: string; readonly self?: string }>;
}

/** The hierarchy of issue types within a project. */
export interface ProjectIssueTypeHierarchy {
    /** Details of an issue type hierarchy level. */
    readonly hierarchy?: Array<{ readonly entityId?: string; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly id?: number; readonly name?: string }>; readonly level?: number; readonly name?: string }>;
    /** The ID of the project. */
    readonly projectId?: number;
}

/** The project and issue type mapping. */
export interface ProjectIssueTypeMapping {
    /** The ID of the issue type. */
    issueTypeId: string;
    /** The ID of the project. */
    projectId: string;
}

/** The project and issue type mappings. */
export interface ProjectIssueTypeMappings {
    /** The project and issue type mappings. */
    mappings: Array<{ issueTypeId: string; projectId: string }>;
}

/** Project and issue type context for workflow queries made using issue types. */
export interface ProjectIssueTypeQueryContext {
    /** The set of issue type IDs. */
    issueTypes?: Array<string>;
    /** The ID of the project. */
    project?: string;
}

/** Details of an issue type hierarchy level. */
export interface ProjectIssueTypesHierarchyLevel {
    /** The ID of the issue type hierarchy level. This property is deprecated, see [Change notice: Removing hierarchy level IDs from next-gen APIs](https://developer.atlassian.com/cloud/jira/platform/change-notice-removing-hierarchy-level-ids-from-next-gen-apis/). */
    readonly entityId?: string;
    /** The list of issue types in the hierarchy level. */
    readonly issueTypes?: Array<{ readonly avatarId?: number; readonly id?: number; readonly name?: string }>;
    /** The level of the issue type hierarchy level. */
    readonly level?: number;
    /** The name of the issue type hierarchy level. */
    readonly name?: string;
}

export interface ProjectLandingPageInfo {
    attributes?: Record<string, string>;
    boardId?: number;
    boardName?: string;
    projectKey?: string;
    projectType?: string;
    queueCategory?: string;
    queueId?: number;
    queueName?: string;
    simpleBoard?: boolean;
    simplified?: boolean;
    url?: string;
}

/** The payload for creating a project */
export interface ProjectPayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    fieldLayoutSchemeId?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    issueSecuritySchemeId?: unknown;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    issueTypeSchemeId?: unknown;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    issueTypeScreenSchemeId?: unknown;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    notificationSchemeId?: unknown;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    permissionSchemeId?: unknown;
    /** The [project type](https://confluence.atlassian.com/x/GwiiLQ#Jiraapplicationsoverview-Productfeaturesandprojecttypes), which defines the application-specific feature set. If you don't specify the project template you have to specify the project type. */
    projectTypeKey?: "software" | "business" | "service_desk" | "product_discovery";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    workflowSchemeId?: unknown;
}

/** Permissions which a user has on a project. */
export interface ProjectPermissions {
    /** Whether the logged user can edit the project. */
    readonly canEdit?: boolean;
}

/** The list of projects to pin or unpin the issue panel to or from. */
export interface ProjectPinAction {
    /** The action to perform: PIN or UNPIN. */
    action: "PIN" | "UNPIN";
    /** The project ID or key. */
    projectIdOrKey: string;
}

/** Details about the roles in a project. */
export interface ProjectRole {
    /** The list of users who act in this role. */
    readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>;
    /** Whether this role is the admin role for the project. */
    readonly admin?: boolean;
    /** Whether the calling user is part of this role. */
    currentUserRole?: boolean;
    /** Whether this role is the default role for the project */
    readonly default?: boolean;
    /** The description of the project role. */
    readonly description?: string;
    /** The ID of the project role. */
    readonly id?: number;
    /** The name of the project role. */
    name?: string;
    /** Whether the roles are configurable for this project. */
    readonly roleConfigurable?: boolean;
    /** The scope of the role. Indicated for roles associated with [next-gen projects](https://confluence.atlassian.com/x/loMyO). */
    readonly scope?: Record<string, unknown>;
    /** The URL the project role details. */
    readonly self?: string;
    /** The translated name of the project role. */
    translatedName?: string;
}

export interface ProjectRoleActorsUpdateBean {
    /**
     * The actors to add to the project role.
     *
     * Add groups using:
     *
     *  *  `atlassian-group-role-actor` and a list of group names.
     *  *  `atlassian-group-role-actor-id` and a list of group IDs.
     *
     * As a group's name can change, use of `atlassian-group-role-actor-id` is recommended. For example, `"atlassian-group-role-actor-id":["eef79f81-0b89-4fca-a736-4be531a10869","77f6ab39-e755-4570-a6ae-2d7a8df0bcb8"]`.
     *
     * Add users using `atlassian-user-role-actor` and a list of account IDs. For example, `"atlassian-user-role-actor":["12345678-9abc-def1-2345-6789abcdef12", "abcdef12-3456-789a-bcde-f123456789ab"]`.
     */
    categorisedActors?: Record<string, Array<string>>;
    /** The ID of the project role. Use [Get all project roles](#api-rest-api-3-role-get) to get a list of project role IDs. */
    readonly id?: number;
}

/** Details about a project role. */
export interface ProjectRoleDetails {
    /** Whether this role is the admin role for the project. */
    readonly admin?: boolean;
    /** Whether this role is the default role for the project. */
    readonly default?: boolean;
    /** The description of the project role. */
    readonly description?: string;
    /** The ID of the project role. */
    readonly id?: number;
    /** The name of the project role. */
    name?: string;
    /** Whether the roles are configurable for this project. */
    readonly roleConfigurable?: boolean;
    /** The scope of the role. Indicated for roles associated with [next-gen projects](https://confluence.atlassian.com/x/loMyO). */
    readonly scope?: Record<string, unknown>;
    /** The URL the project role details. */
    readonly self?: string;
    /** The translated name of the project role. */
    translatedName?: string;
    /** The type of the project role. This is "DEFAULT" or "GUEST\_ROLE". */
    readonly type?: "DEFAULT" | "GUEST_ROLE";
}

/** Details of the group associated with the role. */
export interface ProjectRoleGroup {
    /** The display name of the group. */
    displayName?: string;
    /** The ID of the group. */
    groupId?: string;
    /** The name of the group. As a group's name can change, use of `groupId` is recommended to identify the group. */
    name?: string;
}

/** Details of the user associated with the role. */
export interface ProjectRoleUser {
    /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. Returns *unknown* if the record is deleted and corrupted, for example, as the result of a server import. */
    readonly accountId?: string;
}

export interface ProjectScopeBean {
    /** Defines the behavior of the option in the project.If notSelectable is set, the option cannot be set as the field's value. This is useful for archiving an option that has previously been selected but shouldn't be used anymore.If defaultValue is set, the option is selected by default. */
    attributes?: Array<"notSelectable" | "defaultValue">;
    /** The ID of the project that the option's behavior applies to. */
    id?: number;
}

export interface ProjectTemplateKey {
    key?: string;
    uuid?: string;
}

export interface ProjectTemplateModel {
    archetype?: { realType?: "BUSINESS" | "SOFTWARE" | "PRODUCT_DISCOVERY" | "SERVICE_DESK" | "CUSTOMER_SERVICE" | "OPS"; style?: "classic" | "next-gen"; type?: "BUSINESS" | "SOFTWARE" | "PRODUCT_DISCOVERY" | "SERVICE_DESK" | "CUSTOMER_SERVICE" | "OPS" };
    defaultBoardView?: string;
    description?: string;
    liveTemplateProjectIdReference?: number;
    name?: string;
    projectTemplateKey?: { key?: string; uuid?: string };
    snapshotTemplate?: Record<string, Record<string, unknown>>;
    templateGenerationOptions?: { enableScreenDelegatedAdminSupport?: boolean; enableWorkflowDelegatedAdminSupport?: boolean };
    type?: "LIVE" | "SNAPSHOT";
}

/** Details about a project type. */
export interface ProjectType {
    /** The color of the project type. */
    readonly color?: string;
    /** The key of the project type's description. */
    readonly descriptionI18nKey?: string;
    /** The formatted key of the project type. */
    readonly formattedKey?: string;
    /** The icon of the project type. */
    readonly icon?: string;
    /** The key of the project type. */
    readonly key?: string;
}

/** The project. */
export interface ProjectUsage {
    /** The project ID. */
    id?: string;
}

/** A page of projects. */
export interface ProjectUsagePage {
    /** Page token for the next page of project usages. */
    nextPageToken?: string;
    /** The list of projects. */
    values?: Array<{ id?: string }>;
}

/** Details about data policies for a project. */
export interface ProjectWithDataPolicy {
    /** Data policy. */
    readonly dataPolicy?: { readonly anyContentBlocked?: boolean };
    /** The project ID. */
    readonly id?: number;
}

/** Property key details. */
export interface PropertyKey {
    /** The key of the property. */
    readonly key?: string;
    /** The URL of the property. */
    readonly self?: string;
}

/** List of property keys. */
export interface PropertyKeys {
    /** Property key details. */
    readonly keys?: Array<{ readonly key?: string; readonly self?: string }>;
}

/** Details about the status mappings for publishing a draft workflow scheme. */
export interface PublishDraftWorkflowScheme {
    /** Mappings of statuses to new statuses for issue types. */
    statusMappings?: Array<{ issueTypeId: string; newStatusId: string; statusId: string }>;
}

/** Properties that identify a published workflow. */
export interface PublishedWorkflowId {
    /** The entity ID of the workflow. */
    entityId?: string;
    /** The name of the workflow. */
    name: string;
}

/** The payload for defining quick filters */
export interface QuickFilterPayload {
    /** The description of the quick filter */
    description?: string;
    /** The jql query for the quick filter */
    jqlQuery?: string;
    /** The name of the quick filter */
    name?: string;
}

export interface RedactionJobStatusResponse {
    bulkRedactionResponse?: { results: Array<{ externalId: string; successful: boolean }> };
    jobStatus?: "PENDING" | "IN_PROGRESS" | "COMPLETED";
}

/** Represents the position of the redaction */
export interface RedactionPosition {
    /** The ADF pointer indicating the position of the text to be redacted. This is only required when redacting from rich text(ADF) fields. For plain text fields, this field can be omitted. */
    adfPointer?: string;
    /** The text which will be redacted, encoded using SHA256 hash and Base64 digest */
    expectedText: string;
    /** The start index(inclusive) for the redaction in specified content */
    from: number;
    /** The ending index(exclusive) for the redaction in specified content */
    to: number;
}

/** ID of a registered webhook or error messages explaining why a webhook wasn't registered. */
export interface RegisteredWebhook {
    /** The ID of the webhook. Returned if the webhook is created. */
    createdWebhookId?: number;
    /** Error messages specifying why the webhook creation failed. */
    errors?: Array<string>;
}

/** Details of an issue remote link. */
export interface RemoteIssueLink {
    /** Details of the remote application the linked item is in. */
    application?: Record<string, unknown>;
    /** The global ID of the link, such as the ID of the item on the remote system. */
    globalId?: string;
    /** The ID of the link. */
    id?: number;
    /** Details of the item linked to. */
    object?: Record<string, unknown>;
    /** Description of the relationship between the issue and the linked item. */
    relationship?: string;
    /** The URL of the link. */
    self?: string;
}

/** Details of the identifiers for a created or updated remote issue link. */
export interface RemoteIssueLinkIdentifies {
    /** The ID of the remote issue link, such as the ID of the item on the remote system. */
    readonly id?: number;
    /** The URL of the remote issue link. */
    readonly self?: string;
}

/** Details of a remote issue link. */
export type RemoteIssueLinkRequest = Record<string, unknown>;
/** The linked item. */
export type RemoteObject = Record<string, unknown>;

/** Request item for removing field associations. */
export interface RemoveFieldAssociationsRequestItem {
    /** Set of scheme IDs from which to remove field associations */
    schemeIds: Array<number>;
}

/** Result of remove field parameters operation. */
export interface RemoveFieldParametersResult {
    results?: Array<{ error?: { code?: string; message?: string }; fieldId?: string; schemeId?: number; success?: boolean; workTypeIds?: Array<number> }>;
}

/** Error during remove field parameters operation. */
export interface RemoveFieldParametersResultError {
    code?: string;
    message?: string;
}

export interface RemoveOptionFromIssuesResult {
    /** A collection of errors related to unchanged issues. The collection size is limited, which means not all errors may be returned. */
    errors?: { errorMessages?: Array<string>; errors?: Record<string, string>; httpStatusCode?: number };
    /** The IDs of the modified issues. */
    modifiedIssues?: Array<number>;
    /** The IDs of the unchanged issues, those issues where errors prevent modification. */
    unmodifiedIssues?: Array<number>;
}

/** Change the order of issue priorities. */
export interface ReorderIssuePriorities {
    /** The ID of the priority. Required if `position` isn't provided. */
    after?: string;
    /** The list of issue IDs to be reordered. Cannot contain duplicates nor after ID. */
    ids: Array<string>;
    /** The position for issue priorities to be moved to. Required if `after` isn't provided. */
    position?: string;
}

/** Change the order of issue resolutions. */
export interface ReorderIssueResolutionsRequest {
    /** The ID of the resolution. Required if `position` isn't provided. */
    after?: string;
    /** The list of resolution IDs to be reordered. Cannot contain duplicates nor after ID. */
    ids: Array<string>;
    /** The position for issue resolutions to be moved to. Required if `after` isn't provided. */
    position?: string;
}

/** The list of required status mappings by issue type. */
export interface RequiredMappingByIssueType {
    /** The ID of the issue type. */
    issueTypeId?: string;
    /** The status IDs requiring mapping. */
    statusIds?: Array<string>;
}

/** The list of required status mappings by workflow. */
export interface RequiredMappingByWorkflows {
    /** The ID of the source workflow. */
    sourceWorkflowId?: string;
    /** The status IDs requiring mapping. */
    statusIds?: Array<string>;
    /** The ID of the target workflow. */
    targetWorkflowId?: string;
}

/** Details of an issue resolution. */
export interface Resolution {
    /** The description of the issue resolution. */
    description?: string;
    /** The ID of the issue resolution. */
    id?: string;
    /** The name of the issue resolution. */
    name?: string;
    /** The URL of the issue resolution. */
    self?: string;
}

/** The ID of an issue resolution. */
export type ResolutionId = Record<string, unknown>;

export interface ResolutionJsonBean {
    default?: boolean;
    description?: string;
    iconUrl?: string;
    id?: string;
    name?: string;
    self?: string;
}

export interface Resource {
    contentAsByteArray?: Array<string>;
    description?: string;
    file?: string;
    filename?: string;
    inputStream?: Record<string, unknown>;
    open?: boolean;
    readable?: boolean;
    uri?: string;
    url?: string;
}

/** Details of the permission. */
export type RestrictedPermission = Record<string, unknown>;

export interface RichText {
    empty?: boolean;
    emptyAdf?: boolean;
    finalised?: boolean;
    valueSet?: boolean;
}

/** Details about a user assigned to a project role. */
export interface RoleActor {
    readonly actorGroup?: { displayName?: string; groupId?: string; name?: string };
    readonly actorUser?: { readonly accountId?: string };
    /** The avatar of the role actor. */
    readonly avatarUrl?: string;
    /** The display name of the role actor. For users, depending on the user’s privacy setting, this may return an alternative value for the user's name. */
    readonly displayName?: string;
    /** The ID of the role actor. */
    readonly id?: number;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    readonly name?: string;
    /** The type of role actor. */
    readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor";
}

/** The payload used to create a project role. It is optional for CMP projects, as a default role actor will be provided. TMP will add new role actors to the table. */
export interface RolePayload {
    /** The default actors for the role. By adding default actors, the role will be added to any future projects created */
    defaultActors?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }>;
    /** The description of the role */
    description?: string;
    /** The name of the role */
    name?: string;
    /** The strategy to use when there is a conflict with an existing project role. FAIL - Fail execution, this always needs to be unique; USE - Use the existing entity and ignore new entity parameters */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
    /** The type of the role. Only used by project-scoped project */
    type?: "HIDDEN" | "VIEWABLE" | "EDITABLE" | "GUEST";
}

export interface RolesCapabilityPayload {
    /** A map of role PCRI (can be ID or REF) to a list of user or group PCRI IDs to associate with the role and project. */
    roleToProjectActors?: Record<string, Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }>>;
    /** The list of roles to create. */
    roles?: Array<{ defaultActors?: Array<unknown>; description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; type?: "HIDDEN" | "VIEWABLE" | "EDITABLE" | "GUEST" }>;
}

/** A rule configuration. */
export interface RuleConfiguration {
    /** Whether the rule is disabled. */
    disabled?: boolean;
    /** A tag used to filter rules in [Get workflow transition rule configurations](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-workflow-transition-rules/#api-rest-api-3-workflow-rule-config-get). */
    tag?: string;
    /** Configuration of the rule, as it is stored by the Connect or the Forge app on the rule configuration page. */
    value: string;
}

/** The payload for creating rules in a workflow */
export interface RulePayload {
    /** The parameters of the rule */
    parameters?: Record<string, string>;
    /** The key of the rule. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-workflows/\#api-rest-api-3-workflows-capabilities-get */
    ruleKey?: string;
}

/** The sanitized JQL queries for the given account IDs. */
export interface SanitizedJqlQueries {
    /** The list of sanitized JQL queries. */
    queries?: Array<{ accountId?: string; errors?: { errorMessages?: Array<string>; errors?: Record<string, string>; status?: number }; initialQuery?: string; sanitizedQuery?: string }>;
}

/** Details of the sanitized JQL query. */
export interface SanitizedJqlQuery {
    /** The account ID of the user for whom sanitization was performed. */
    accountId?: string;
    /** The list of errors. */
    errors?: { errorMessages?: Array<string>; errors?: Record<string, string>; status?: number };
    /** The initial query. */
    initialQuery?: string;
    /** The sanitized query, if there were no errors. */
    sanitizedQuery?: string;
}

/** The request details to generate template from a project */
export interface SaveProjectTemplateRequest {
    /** The ID of the target project */
    projectId?: number;
    templateGenerationOptions?: { enableScreenDelegatedAdminSupport?: boolean; enableWorkflowDelegatedAdminSupport?: boolean };
    /** The type of the template: LIVE | SNAPSHOT */
    templateType?: "LIVE" | "SNAPSHOT";
}

/** Request to save a custom template */
export interface SaveTemplateRequest {
    /** The description of the template */
    templateDescription?: string;
    /** The request details to generate template from a project */
    templateFromProjectRequest?: { projectId?: number; templateGenerationOptions?: { enableScreenDelegatedAdminSupport?: boolean; enableWorkflowDelegatedAdminSupport?: boolean }; templateType?: "LIVE" | "SNAPSHOT" };
    /** The name of the template */
    templateName?: string;
}

export interface SaveTemplateResponse {
    projectTemplateKey?: { key?: string; uuid?: string };
}

/** The projects the item is associated with. Indicated for items associated with [next-gen projects](https://confluence.atlassian.com/x/loMyO). */
export type Scope = Record<string, unknown>;

/** The payload for creating a scope. Defines if a project is team-managed project or company-managed project */
export interface ScopePayload {
    /** The type of the scope. Use `GLOBAL` or empty for company-managed project, and `PROJECT` for team-managed project */
    type?: "GLOBAL" | "PROJECT";
}

/** A screen. */
export interface Screen {
    /** The description of the screen. */
    readonly description?: string;
    /** The ID of the screen. */
    readonly id?: number;
    /** The name of the screen. */
    readonly name?: string;
    /** The scope of the screen. */
    scope?: Record<string, unknown>;
}

/** Details of a screen. */
export interface ScreenDetails {
    /** The description of the screen. The maximum length is 255 characters. */
    description?: string;
    /** The name of the screen. The name must be unique. The maximum length is 255 characters. */
    name: string;
}

/** Defines the payload for the field screens. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-screens/\#api-rest-api-3-screens-post */
export interface ScreenPayload {
    /** The description of the screen */
    description?: string;
    /** The name of the screen */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The tabs of the screen. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-screen-tab-fields/\#api-rest-api-3-screens-screenid-tabs-tabid-fields-post */
    tabs?: Array<{ fields?: Array<unknown>; name?: string }>;
}

/** A screen scheme. */
export interface ScreenScheme {
    /** The description of the screen scheme. */
    description?: string;
    /** The ID of the screen scheme. */
    id?: number;
    /** Details of the issue type screen schemes associated with the screen scheme. */
    issueTypeScreenSchemes?: { readonly isLast?: boolean; readonly maxResults?: number; readonly nextPage?: string; readonly self?: string; readonly startAt?: number; readonly total?: number; readonly values?: Array<{ description?: string; id: string; name: string }> };
    /** The name of the screen scheme. */
    name?: string;
    /** The IDs of the screens for the screen types of the screen scheme. */
    screens?: { create?: number; default: number; edit?: number; view?: number };
}

/** Details of a screen scheme. */
export interface ScreenSchemeDetails {
    /** The description of the screen scheme. The maximum length is 255 characters. */
    description?: string;
    /** The name of the screen scheme. The name must be unique. The maximum length is 255 characters. */
    name: string;
    /** The IDs of the screens for the screen types of the screen scheme. Only screens used in classic projects are accepted. */
    screens: { create?: number; default: number; edit?: number; view?: number };
}

/** The ID of a screen scheme. */
export interface ScreenSchemeId {
    /** The ID of the screen scheme. */
    readonly id: number;
}

/** Defines the payload for the screen schemes. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-screen-schemes/\#api-rest-api-3-screenscheme-post */
export interface ScreenSchemePayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    defaultScreen?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The description of the screen scheme */
    description?: string;
    /** The name of the screen scheme */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
    /** Similar to the field layout scheme those mappings allow users to set different screens for different operations: default - always there, applied to all operations that don't have an explicit mapping `create`, `view`, `edit` - specific operations that are available and users can assign a different screen for each one of them https://support.atlassian.com/jira-cloud-administration/docs/manage-screen-schemes/\#Associating-a-screen-with-an-issue-operation */
    screens?: Record<string, unknown>;
}

/** The IDs of the screens for the screen types of the screen scheme. */
export interface ScreenTypes {
    /** The ID of the create screen. */
    create?: number;
    /** The ID of the default screen. Required when creating a screen scheme. */
    default: number;
    /** The ID of the edit screen. */
    edit?: number;
    /** The ID of the view screen. */
    view?: number;
}

/** A screen with tab details. */
export interface ScreenWithTab {
    /** The description of the screen. */
    readonly description?: string;
    /** The ID of the screen. */
    readonly id?: number;
    /** The name of the screen. */
    readonly name?: string;
    /** The scope of the screen. */
    scope?: Record<string, unknown>;
    /** The tab for the screen. */
    tab?: { readonly id?: number; name: string };
}

/** A screen tab field. */
export interface ScreenableField {
    /** The ID of the screen tab field. */
    readonly id?: string;
    /** The name of the screen tab field. Required on create and update. The maximum length is 255 characters. */
    name?: string;
}

/** A screen tab. */
export interface ScreenableTab {
    /** The ID of the screen tab. */
    readonly id?: number;
    /** The name of the screen tab. The maximum length is 255 characters. */
    name: string;
}

export interface SearchAndReconcileRequestBean {
    /**
     * Use [expand](#expansion) to include additional information about issues in the response. Note that, unlike the majority of instances where `expand` is specified, `expand` is defined as a comma-delimited string of values. The expand options are:
     *
     *  *  `renderedFields` Returns field values rendered in HTML format.
     *  *  `names` Returns the display name of each field.
     *  *  `schema` Returns the schema describing a field type.
     *  *  `transitions` Returns all possible transitions for the issue.
     *  *  `operations` Returns all possible operations for the issue.
     *  *  `editmeta` Returns information about how each field can be edited.
     *  *  `changelog` Returns a list of recent updates to an issue, sorted by date, starting from the most recent.
     *  *  `versionedRepresentations` Instead of `fields`, returns `versionedRepresentations` a JSON array containing each version of a field's value, with the highest numbered item representing the most recent version.
     *
     * Examples: `"names,changelog"` Returns the display name of each field as well as a list of recent updates to an issue.
     */
    expand?: string;
    /**
     * A list of fields to return for each issue. Use it to retrieve a subset of fields. This parameter accepts a comma-separated list. Expand options include:
     *
     *  *  `*all` Returns all fields.
     *  *  `*navigable` Returns navigable fields.
     *  *  `id` Returns only issue IDs.
     *  *  Any issue field, prefixed with a dash to exclude.
     *
     * The default is `id`.
     *
     * Examples:
     *
     *  *  `summary,comment` Returns the summary and comments fields only.
     *  *  `*all,-comment` Returns all fields except comments.
     *
     * Multiple `fields` parameters can be included in a request.
     *
     * Note: By default, this resource returns IDs only. This differs from [GET issue](#api-rest-api-3-issue-issueIdOrKey-get) where the default is all fields.
     */
    fields?: Array<string>;
    /** Reference fields by their key (rather than ID). The default is `false`. */
    fieldsByKeys?: boolean;
    /**
     * A [JQL](https://confluence.atlassian.com/x/egORLQ) expression. For performance reasons, this parameter requires a bounded query. A bounded query is a query with a search restriction.
     *
     *  *  Example of an unbounded query: `order by key desc`.
     *  *  Example of a bounded query: `assignee = currentUser() order by key`.
     *
     * Additionally, `orderBy` clause can contain a maximum of 7 fields.
     */
    jql?: string;
    /** The maximum number of items to return per page. To manage page size, API may return fewer items per page where a large number of fields are requested. The greatest number of items returned per page is achieved when requesting `id` or `key` only. It returns max 5000 issues. */
    maxResults?: number;
    /** The token for a page to fetch that is not the first page. The first page has a `nextPageToken` of `null`. Use the `nextPageToken` to fetch the next page of issues. */
    nextPageToken?: string;
    /** A list of up to 5 issue properties to include in the results. This parameter accepts a comma-separated list. */
    properties?: Array<string>;
    /** Strong consistency issue ids to be reconciled with search results. Accepts max 50 ids. This list of ids should be consistent with each paginated request across different pages. */
    reconcileIssues?: Array<number>;
}

/** The result of a JQL search with issues reconsilation. */
export interface SearchAndReconcileResults {
    /** Indicates whether this is the last page of the paginated response. */
    readonly isLast?: boolean;
    /** The list of issues found by the search or reconsiliation. */
    readonly issues?: Array<{ readonly changelog?: { readonly histories?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>; readonly maxResults?: number; readonly startAt?: number; readonly total?: number }; readonly editmeta?: { readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }> }; readonly expand?: string; fields?: Record<string, Record<string, unknown>>; fieldsToInclude?: { actuallyIncluded?: Array<string>; excluded?: Array<string>; included?: Array<string> }; readonly id?: string; readonly key?: string; readonly names?: Record<string, string>; readonly operations?: Record<string, unknown>; readonly properties?: Record<string, Record<string, unknown>>; readonly renderedFields?: Record<string, Record<string, unknown>>; readonly schema?: Record<string, unknown>; readonly self?: string; readonly transitions?: Array<Record<string, unknown>>; readonly versionedRepresentations?: Record<string, Record<string, Record<string, unknown>>> }>;
    /** The ID and name of each field in the search results. */
    readonly names?: Record<string, string>;
    /** Continuation token to fetch the next page. If this result represents the last or the only page this token will be null. This token will expire in 7 days. */
    readonly nextPageToken?: string;
    /** The schema describing the field types in the search results. */
    readonly schema?: Record<string, unknown>;

/** Details of how to filter and list search auto complete information. */
export interface SearchAutoCompleteFilter {
    /** Include collapsed fields for fields that have non-unique names. */
    includeCollapsedFields?: boolean;
    /** List of project IDs used to filter the visible field details returned. */
    projectIds?: Array<number>;
}

export interface SearchRequestBean {
    /**
     * Use [expand](#expansion) to include additional information about issues in the response. Note that, unlike the majority of instances where `expand` is specified, `expand` is defined as a list of values. The expand options are:
     *
     *  *  `renderedFields` Returns field values rendered in HTML format.
     *  *  `names` Returns the display name of each field.
     *  *  `schema` Returns the schema describing a field type.
     *  *  `transitions` Returns all possible transitions for the issue.
     *  *  `operations` Returns all possible operations for the issue.
     *  *  `editmeta` Returns information about how each field can be edited.
     *  *  `changelog` Returns a list of recent updates to an issue, sorted by date, starting from the most recent.
     *  *  `versionedRepresentations` Instead of `fields`, returns `versionedRepresentations` a JSON array containing each version of a field's value, with the highest numbered item representing the most recent version.
     */
    expand?: Array<string>;
    /**
     * A list of fields to return for each issue, use it to retrieve a subset of fields. This parameter accepts a comma-separated list. Expand options include:
     *
     *  *  `*all` Returns all fields.
     *  *  `*navigable` Returns navigable fields.
     *  *  Any issue field, prefixed with a minus to exclude.
     *
     * The default is `*navigable`.
     *
     * Examples:
     *
     *  *  `summary,comment` Returns the summary and comments fields only.
     *  *  `-description` Returns all navigable (default) fields except description.
     *  *  `*all,-comment` Returns all fields except comments.
     *
     * Multiple `fields` parameters can be included in a request.
     *
     * Note: All navigable fields are returned by default. This differs from [GET issue](#api-rest-api-3-issue-issueIdOrKey-get) where the default is all fields.
     */
    fields?: Array<string>;
    /** Reference fields by their key (rather than ID). The default is `false`. */
    fieldsByKeys?: boolean;
    /** A [JQL](https://confluence.atlassian.com/x/egORLQ) expression. */
    jql?: string;
    /** The maximum number of items to return per page. */
    maxResults?: number;
    /** A list of up to 5 issue properties to include in the results. This parameter accepts a comma-separated list. */
    properties?: Array<string>;
    /** The index of the first item to return in the page of results (page offset). The base index is `0`. */
    startAt?: number;
    /**
     * Determines how to validate the JQL query and treat the validation results. Supported values:
     *
     *  *  `strict` Returns a 400 response code if any errors are found, along with a list of all errors (and warnings).
     *  *  `warn` Returns all errors as warnings.
     *  *  `none` No validation is performed.
     *  *  `true` *Deprecated* A legacy synonym for `strict`.
     *  *  `false` *Deprecated* A legacy synonym for `warn`.
     *
     * The default is `strict`.
     *
     * Note: If the JQL is not correctly formed a 400 response code is returned, regardless of the `validateQuery` value.
     */
    validateQuery?: "strict" | "warn" | "none" | "true" | "false";
}

export interface SearchResultFieldParameters {
    description?: string;
    isRequired?: boolean;
}

export interface SearchResultWorkTypeParameters {
    description?: string;
    isRequired?: boolean;
    workTypeId?: string;
}

/** The result of a JQL search. */
export interface SearchResults {
    /** Expand options that include additional search result details in the response. */
    readonly expand?: string;
    /** The list of issues found by the search. */
    readonly issues?: Array<{ readonly changelog?: { readonly histories?: Array<{ readonly author?: { accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }; readonly created?: string; readonly historyMetadata?: Record<string, unknown>; readonly id?: string; readonly items?: Array<{ readonly field?: string; readonly fieldId?: string; readonly fieldtype?: string; readonly from?: string; readonly fromString?: string; readonly to?: string; readonly toString?: string }> }>; readonly maxResults?: number; readonly startAt?: number; readonly total?: number }; readonly editmeta?: { readonly fields?: Record<string, { readonly allowedValues?: Array<Record<string, unknown>>; readonly autoCompleteUrl?: string; readonly configuration?: Record<string, Record<string, unknown>>; readonly defaultValue?: Record<string, unknown>; readonly hasDefaultValue?: boolean; readonly key: string; readonly name: string; readonly operations: Array<string>; readonly required: boolean; readonly schema: { readonly configuration?: Record<string, Record<string, unknown>>; readonly custom?: string; readonly customId?: number; readonly items?: string; readonly system?: string; readonly type: string } }> }; readonly expand?: string; fields?: Record<string, Record<string, unknown>>; fieldsToInclude?: { actuallyIncluded?: Array<string>; excluded?: Array<string>; included?: Array<string> }; readonly id?: string; readonly key?: string; readonly names?: Record<string, string>; readonly operations?: Record<string, unknown>; readonly properties?: Record<string, Record<string, unknown>>; readonly renderedFields?: Record<string, Record<string, unknown>>; readonly schema?: Record<string, unknown>; readonly self?: string; readonly transitions?: Array<Record<string, unknown>>; readonly versionedRepresentations?: Record<string, Record<string, Record<string, unknown>>> }>;
    /** The maximum number of results that could be on the page. */
    readonly maxResults?: number;
    /** The ID and name of each field in the search results. */
    readonly names?: Record<string, string>;
    /** The schema describing the field types in the search results. */
    readonly schema?: Record<string, unknown>;
    /** The index of the first item returned on the page. */
    readonly startAt?: number;
    /** The number of results on the page. */
    readonly total?: number;
    /** Any warnings related to the JQL query. */
    readonly warningMessages?: Array<string>;

/** Details of an issue level security item. */
export interface SecurityLevel {
    /** The description of the issue level security item. */
    readonly description?: string;
    /** The ID of the issue level security item. */
    readonly id?: string;
    /** Whether the issue level security item is the default. */
    readonly isDefault?: boolean;
    /** The ID of the issue level security scheme. */
    readonly issueSecuritySchemeId?: string;
    /** The name of the issue level security item. */
    readonly name?: string;
    /** The URL of the issue level security item. */
    readonly self?: string;
}

/** Issue security level member. */
export type SecurityLevelMember = Record<string, unknown>;

/** The payload for creating a security level member. See https://support.atlassian.com/jira-cloud-administration/docs/configure-issue-security-schemes/ */
export interface SecurityLevelMemberPayload {
    /** Defines the value associated with the type. For reporter this would be \{"null"\}; for users this would be the names of specific users); for group this would be group names like \{"administrators", "jira-administrators", "jira-users"\} */
    parameter?: string;
    /** The type of the security level member */
    type?: "group" | "reporter" | "users";
}

/** The payload for creating a security level. See https://support.atlassian.com/jira-cloud-administration/docs/configure-issue-security-schemes/ */
export interface SecurityLevelPayload {
    /** The description of the security level */
    description?: string;
    /** Whether the security level is default for the security scheme */
    isDefault?: boolean;
    /** The name of the security level */
    name?: string;
    /** The members of the security level */
    securityLevelMembers?: Array<{ parameter?: string; type?: "group" | "reporter" | "users" }>;
}

/** Details about a security scheme. */
export interface SecurityScheme {
    /** The ID of the default security level. */
    readonly defaultSecurityLevelId?: number;
    /** The description of the issue security scheme. */
    readonly description?: string;
    /** The ID of the issue security scheme. */
    readonly id?: number;
    levels?: Array<{ readonly description?: string; readonly id?: string; readonly isDefault?: boolean; readonly issueSecuritySchemeId?: string; readonly name?: string; readonly self?: string }>;
    /** The name of the issue security scheme. */
    readonly name?: string;
    /** The URL of the issue security scheme. */
    readonly self?: string;
}

/** The ID of the issue security scheme. */
export type SecuritySchemeId = Record<string, unknown>;

export interface SecuritySchemeLevelBean {
    /** The description of the issue security scheme level. */
    description?: string;
    /** Specifies whether the level is the default level. False by default. */
    isDefault?: boolean;
    /** The list of level members which should be added to the issue security scheme level. */
    members?: Array<{ parameter?: string; type: string }>;
    /** The name of the issue security scheme level. Must be unique. */
    name: string;
}

export interface SecuritySchemeLevelMemberBean {
    /** The value corresponding to the specified member type. */
    parameter?: string;
    /** The issue security level member type, e.g `reporter`, `group`, `user`, `projectrole`, `applicationRole`. */
    type: string;
}

/** Details of issue security scheme level new members. */
export interface SecuritySchemeMembersRequest {
    /** The list of level members which should be added to the issue security scheme level. */
    members?: Array<{ parameter?: string; type: string }>;
}

/** The payload for creating a security scheme. See https://support.atlassian.com/jira-cloud-administration/docs/configure-issue-security-schemes/ */
export interface SecuritySchemePayload {
    /** The description of the security scheme */
    description?: string;
    /** The name of the security scheme */
    name?: string;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The security levels for the security scheme */
    securityLevels?: Array<{ description?: string; isDefault?: boolean; name?: string; securityLevelMembers?: Array<{ parameter?: string; type?: "group" | "reporter" | "users" }> }>;
}

/** Details about an issue security scheme. */
export type SecuritySchemeWithProjects = Record<string, unknown>;

/** List of security schemes. */
export interface SecuritySchemes {
    /** List of security schemes. */
    readonly issueSecuritySchemes?: Array<{ readonly defaultSecurityLevelId?: number; readonly description?: string; readonly id?: number; levels?: Array<{ readonly description?: string; readonly id?: string; readonly isDefault?: boolean; readonly issueSecuritySchemeId?: string; readonly name?: string; readonly self?: string }>; readonly name?: string; readonly self?: string }>;
}

/** Details about the Jira instance. */
export interface ServerInformation {
    /** The base URL of the Jira instance. */
    baseUrl?: string;
    /** The timestamp when the Jira version was built. */
    buildDate?: string;
    /** The build number of the Jira version. */
    buildNumber?: number;
    /** The type of server deployment. This is always returned as *Cloud*. */
    deploymentType?: string;
    /** The display URL of the Jira instance. */
    displayUrl?: string;
    /** The display URL of Confluence. */
    displayUrlConfluence?: string;
    /** The display URL of the Servicedesk Help Center. */
    displayUrlServicedeskHelpCenter?: string;
    /** Jira instance health check results. Deprecated and no longer returned. */
    healthChecks?: Array<{ description?: string; name?: string; passed?: boolean }>;
    /** The unique identifier of the Jira version. */
    scmInfo?: string;
    /** The time in Jira when this request was responded to. */
    serverTime?: string;
    /** The default timezone of the Jira server. In a format known as Olson Time Zones, IANA Time Zones or TZ Database Time Zones. */
    serverTimeZone?: string;
    /** The name of the Jira instance. */
    serverTitle?: string;
    /** The version of Jira. */
    version?: string;
    /** The major, minor, and revision version numbers of the Jira version. */
    versionNumbers?: Array<number>;
}

export interface ServiceManagementNavigationInfo {
    queueCategory?: string;
    queueId?: number;
    queueName?: string;
}

export interface ServiceRegistry {
    /** service description */
    description?: string;
    /** service ID */
    id?: string;
    /** service name */
    name?: string;
    /** organization ID */
    organizationId?: string;
    /** service revision */
    revision?: string;
    serviceTier?: { description?: string; id?: string; level?: number; name?: string; nameKey?: string };
}

export interface ServiceRegistryTier {
    /** tier description */
    description?: string;
    /** tier ID */
    id?: string;
    /** tier level */
    level?: number;
    /** tier name */
    name?: string;
    /** name key of the tier */
    nameKey?: string;
}

/** Details of new default levels. */
export type SetDefaultLevelsRequest = Record<string, unknown>;

/** The new default issue priority. */
export interface SetDefaultPriorityRequest {
    /** The ID of the new default issue priority. Must be an existing ID or null. Setting this to null erases the default priority setting. */
    id: string;
}

/** The new default issue resolution. */
export interface SetDefaultResolutionRequest {
    /** The ID of the new default issue resolution. Must be an existing ID or null. Setting this to null erases the default resolution setting. */
    id: string;
}

/** Details of a share permission for the filter. */
export interface SharePermission {
    /** The group that the filter is shared with. For a request, specify the `groupId` or `name` property for the group. As a group's name can change, use of `groupId` is recommended. */
    group?: { groupId?: string; name?: string; readonly self?: string };
    /** The unique identifier of the share permission. */
    readonly id?: number;
    /**
     * The project that the filter is shared with. This is similar to the project object returned by [Get project](#api-rest-api-3-project-projectIdOrKey-get) but it contains a subset of the properties, which are: `self`, `id`, `key`, `assigneeType`, `name`, `roles`, `avatarUrls`, `projectType`, `simplified`.  
     * For a request, specify the `id` for the project.
     */
    project?: { readonly archived?: boolean; readonly archivedBy?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<unknown>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; readonly archivedDate?: string; readonly assigneeType?: "PROJECT_LEAD" | "UNASSIGNED"; readonly avatarUrls?: unknown; readonly components?: Array<{ readonly ari?: string; readonly assignee?: unknown; assigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; description?: string; readonly id?: string; readonly isAssigneeTypeValid?: boolean; readonly lead?: unknown; leadAccountId?: string; leadUserName?: string; readonly metadata?: Record<string, string>; name?: string; project?: string; readonly projectId?: number; readonly realAssignee?: unknown; readonly realAssigneeType?: "PROJECT_DEFAULT" | "COMPONENT_LEAD" | "PROJECT_LEAD" | "UNASSIGNED"; readonly self?: string }>; readonly deleted?: boolean; readonly deletedBy?: unknown; readonly deletedDate?: string; readonly description?: string; email?: string; readonly expand?: string; favourite?: boolean; id?: string; readonly insight?: { readonly lastIssueUpdateTime?: string; readonly totalIssueCount?: number }; readonly isPrivate?: boolean; readonly issueTypeHierarchy?: { baseLevelId?: number; readonly levels?: Array<{ aboveLevelId?: number; belowLevelId?: number; externalUuid?: string; hierarchyLevelNumber?: number; id?: number; issueTypeIds?: Array<number>; level?: number; name?: string; projectConfigurationId?: number }> }; readonly issueTypes?: Array<{ readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly key?: string; readonly landingPageInfo?: { attributes?: Record<string, string>; boardId?: number; boardName?: string; projectKey?: string; projectType?: string; queueCategory?: string; queueId?: number; queueName?: string; simpleBoard?: boolean; simplified?: boolean; url?: string }; readonly lead?: unknown; readonly name?: string; readonly permissions?: { readonly canEdit?: boolean }; readonly projectCategory?: { description?: string; readonly id?: string; name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly properties?: Record<string, Record<string, unknown>>; readonly retentionTillDate?: string; readonly roles?: Record<string, string>; readonly self?: string; readonly simplified?: boolean; readonly style?: "classic" | "next-gen"; readonly url?: string; readonly uuid?: string; readonly versions?: Array<{ readonly approvers?: Array<Record<string, unknown>>; archived?: boolean; description?: string; driver?: string; expand?: string; readonly id?: string; readonly issuesStatusForFixVersion?: Record<string, unknown>; moveUnfixedIssuesTo?: string; name?: string; readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>; readonly overdue?: boolean; project?: string; projectId?: number; releaseDate?: string; released?: boolean; readonly self?: string; startDate?: string; readonly userReleaseDate?: string; readonly userStartDate?: string }> };
    /**
     * The project role that the filter is shared with.  
     * For a request, specify the `id` for the role. You must also specify the `project` object and `id` for the project that the role is in.
     */
    role?: { readonly actors?: Array<{ readonly actorGroup?: { displayName?: string; groupId?: string; name?: string }; readonly actorUser?: { readonly accountId?: string }; readonly avatarUrl?: string; readonly displayName?: string; readonly id?: number; readonly name?: string; readonly type?: "atlassian-group-role-actor" | "atlassian-user-role-actor" }>; readonly admin?: boolean; currentUserRole?: boolean; readonly default?: boolean; readonly description?: string; readonly id?: number; name?: string; readonly roleConfigurable?: boolean; readonly scope?: unknown; readonly self?: string; translatedName?: string };
    /**
     * The type of share permission:
     *
     *  *  `user` Shared with a user.
     *  *  `group` Shared with a group. If set in a request, then specify `sharePermission.group` as well.
     *  *  `project` Shared with a project. If set in a request, then specify `sharePermission.project` as well.
     *  *  `projectRole` Share with a project role in a project. This value is not returned in responses. It is used in requests, where it needs to be specify with `projectId` and `projectRoleId`.
     *  *  `global` Shared globally. If set in a request, no other `sharePermission` properties need to be specified.
     *  *  `loggedin` Shared with all logged-in users. Note: This value is set in a request by specifying `authenticated` as the `type`.
     *  *  `project-unknown` Shared with a project that the user does not have access to. Cannot be set in a request.
     */
    type: "user" | "group" | "project" | "projectRole" | "global" | "loggedin" | "authenticated" | "project-unknown";
    /** The user account ID that the filter is shared with. For a request, specify the `accountId` property for the user. */
    user?: { accountId?: string; active?: boolean; avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; displayName?: string; key?: string; name?: string; self?: string };

export interface SharePermissionInputBean {
    /** The user account ID that the filter is shared with. For a request, specify the `accountId` property for the user. */
    accountId?: string;
    /** The ID of the group, which uniquely identifies the group across all Atlassian products.For example, *952d12c3-5b5b-4d04-bb32-44d383afc4b2*. Cannot be provided with `groupname`. */
    groupId?: string;
    /** The name of the group to share the filter with. Set `type` to `group`. Please note that the name of a group is mutable, to reliably identify a group use `groupId`. */
    groupname?: string;
    /** The ID of the project to share the filter with. Set `type` to `project`. */
    projectId?: string;
    /** The ID of the project role to share the filter with. Set `type` to `projectRole` and the `projectId` for the project that the role is in. */
    projectRoleId?: string;
    /** The rights for the share permission. */
    rights?: number;
    /**
     * The type of the share permission.Specify the type as follows:
     *
     *  *  `user` Share with a user.
     *  *  `group` Share with a group. Specify `groupname` as well.
     *  *  `project` Share with a project. Specify `projectId` as well.
     *  *  `projectRole` Share with a project role in a project. Specify `projectId` and `projectRoleId` as well.
     *  *  `global` Share globally, including anonymous users. If set, this type overrides all existing share permissions and must be deleted before any non-global share permissions is set.
     *  *  `authenticated` Share with all logged-in users. This shows as `loggedin` in the response. If set, this type overrides all existing share permissions and must be deleted before any non-global share permissions is set.
     */
    type: "user" | "project" | "group" | "projectRole" | "global" | "authenticated";
}

export interface SimpleApplicationPropertyBean {
    /** The ID of the application property. */
    id?: string;
    /** The new value. */
    value?: string;
}

export interface SimpleErrorCollection {
    /** The list of error messages produced by this operation. For example, "input parameter 'key' must be provided" */
    errorMessages?: Array<string>;
    /** The list of errors by parameter returned by the operation. For example,"projectKey": "Project keys must start with an uppercase letter, followed by one or more uppercase alphanumeric characters." */
    errors?: Record<string, string>;
    httpStatusCode?: number;
}

/** Details about the operations available in this version. */
export interface SimpleLink {
    href?: string;
    iconClass?: string;
    id?: string;
    label?: string;
    styleClass?: string;
    title?: string;
    weight?: number;
}

export interface SimpleListWrapperApplicationRole {
    callback?: Record<string, unknown>;
    items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>;
    max-results?: number;
    pagingCallback?: unknown;
    size?: number;

export interface SimpleListWrapperGroupName {
    callback?: Record<string, unknown>;
    items?: Array<{ groupId?: string; name?: string; readonly self?: string }>;
    max-results?: number;
    pagingCallback?: unknown;
    size?: number;

export interface SimplifiedHierarchyLevel {
    /** The ID of the level above this one in the hierarchy. This property is deprecated, see [Change notice: Removing hierarchy level IDs from next-gen APIs](https://developer.atlassian.com/cloud/jira/platform/change-notice-removing-hierarchy-level-ids-from-next-gen-apis/). */
    aboveLevelId?: number;
    /** The ID of the level below this one in the hierarchy. This property is deprecated, see [Change notice: Removing hierarchy level IDs from next-gen APIs](https://developer.atlassian.com/cloud/jira/platform/change-notice-removing-hierarchy-level-ids-from-next-gen-apis/). */
    belowLevelId?: number;
    /** The external UUID of the hierarchy level. This property is deprecated, see [Change notice: Removing hierarchy level IDs from next-gen APIs](https://developer.atlassian.com/cloud/jira/platform/change-notice-removing-hierarchy-level-ids-from-next-gen-apis/). */
    externalUuid?: string;
    hierarchyLevelNumber?: number;
    /** The ID of the hierarchy level. This property is deprecated, see [Change notice: Removing hierarchy level IDs from next-gen APIs](https://developer.atlassian.com/cloud/jira/platform/change-notice-removing-hierarchy-level-ids-from-next-gen-apis/). */
    id?: number;
    /** The issue types available in this hierarchy level. */
    issueTypeIds?: Array<number>;
    /** The level of this item in the hierarchy. */
    level?: number;
    /** The name of this hierarchy level. */
    name?: string;
    /** The ID of the project configuration. This property is deprecated, see [Change oticen: Removing hierarchy level IDs from next-gen APIs](https://developer.atlassian.com/cloud/jira/platform/change-notice-removing-hierarchy-level-ids-from-next-gen-apis/). */
    projectConfigurationId?: number;
}

export interface SimplifiedIssueTransition {
    /** The issue status change of the transition. */
    readonly to?: { readonly statusId?: number; readonly statusName?: string };
    /** The unique ID of the transition. */
    readonly transitionId?: number;
    /** The name of the transition. */
    readonly transitionName?: string;
}

export interface SingleRedactionRequest {
    /** Represents the content to redact */
    contentItem: { entityId: string; entityType: "issuefieldvalue" | "issue-comment" | "issue-worklog"; id: string };
    /** Unique id for the redaction request; ID format should be of UUID */
    externalId: string;
    /** The reason why the content is being redacted */
    reason: string;
    /** Represents the position of the redaction */
    redactionPosition: { adfPointer?: string; expectedText: string; from: number; to: number };
}

/** Result for requested redactions */
export interface SingleRedactionResponse {
    /** An unique id for the redaction request */
    externalId: string;
    /** Indicates if redaction was success/failure */
    successful: boolean;
}

export interface SoftwareNavigationInfo {
    boardId?: number;
    boardName?: string;
    simpleBoard?: boolean;
    totalBoardsInProject?: number;
}

/** The status of the item. */
export type Status = Record<string, unknown>;
/** A status category. */
export type StatusCategory = Record<string, unknown>;

/** Details of the status being created. */
export interface StatusCreate {
    /** The description of the status. */
    description?: string;
    /** The name of the status. */
    name: string;
    /** The category of the status. */
    statusCategory: "TODO" | "IN_PROGRESS" | "DONE";
}

/** Details of the statuses being created and their scope. */
export interface StatusCreateRequest {
    /** The scope of the status. */
    scope: { project?: { id: string }; type: "PROJECT" | "GLOBAL" };
    /** Details of the statuses being created. */
    statuses: Array<{ description?: string; name: string; statusCategory: "TODO" | "IN_PROGRESS" | "DONE" }>;
}

/** A status. */
export type StatusDetails = Record<string, unknown>;
/** The statuses associated with this workflow. */
export type StatusLayoutUpdate = Record<string, unknown>;

/** Details about the mapping from a status to a new status for an issue type. */
export interface StatusMapping {
    /** The ID of the issue type. */
    issueTypeId: string;
    /** The ID of the new status. */
    newStatusId: string;
    /** The ID of the status. */
    statusId: string;
}

/** The mapping of old to new status ID for a specific project and issue type. */
export type StatusMappingDTO = Record<string, unknown>;

/** The details of the statuses in the associated workflows. */
export interface StatusMetadata {
    /** The category of the status. */
    category?: "TODO" | "IN_PROGRESS" | "DONE";
    /** The ID of the status. */
    id?: string;
    /** The name of the status. */
    name?: string;
}

/** The mapping of old to new status ID. */
export type StatusMigration = Record<string, unknown>;

/** The payload for creating a status */
export interface StatusPayload {
    /** The description of the status */
    description?: string;
    /** The name of the status */
    name?: string;
    /** The conflict strategy for the status already exists. FAIL - Fail execution, this always needs to be unique; USE - Use the existing entity and ignore new entity parameters; NEW - Create a new entity */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The status category of the status. The value is case-sensitive. */
    statusCategory?: "TODO" | "IN_PROGRESS" | "DONE";
}

/** The list of issue types. */
export interface StatusProjectIssueTypeUsage {
    /** The issue type ID. */
    id?: string;
}

/** The issue types using this status in a project. */
export interface StatusProjectIssueTypeUsageDTO {
    /** A page of issue types. */
    issueTypes?: { nextPageToken?: string; values?: Array<{ id?: string }> };
    /** The project ID. */
    projectId?: string;
    /** The status ID. */
    statusId?: string;
}

/** A page of issue types. */
export interface StatusProjectIssueTypeUsagePage {
    /** Page token for the next page of issue type usages. */
    nextPageToken?: string;
    /** The list of issue types. */
    values?: Array<{ id?: string }>;
}

/** The project. */
export interface StatusProjectUsage {
    /** The project ID. */
    id?: string;
}

/** The projects using this status. */
export interface StatusProjectUsageDTO {
    /** A page of projects. */
    projects?: { nextPageToken?: string; values?: Array<{ id?: string }> };
    /** The status ID. */
    statusId?: string;
}

/** A page of projects. */
export interface StatusProjectUsagePage {
    /** Page token for the next page of issue type usages. */
    nextPageToken?: string;
    /** The list of projects. */
    values?: Array<{ id?: string }>;
}

/** The scope of the status. */
export interface StatusScope {
    /** Project ID details. */
    project?: { id: string };
    /** The scope of the status. `GLOBAL` for company-managed projects and `PROJECT` for team-managed projects. */
    type: "PROJECT" | "GLOBAL";
}

/** Details of the status being updated. */
export type StatusUpdate = Record<string, unknown>;

/** The list of statuses that will be updated. */
export interface StatusUpdateRequest {
    /** The list of statuses that will be updated. */
    statuses: Array<Record<string, unknown>>;
}

/** Workflows using the status. */
export interface StatusWorkflowUsageDTO {
    /** The status ID. */
    statusId?: string;
    /** A page of workflows. */
    workflows?: { nextPageToken?: string; values?: Array<{ id?: string }> };
}

/** A page of workflows. */
export interface StatusWorkflowUsagePage {
    /** Page token for the next page of issue type usages. */
    nextPageToken?: string;
    /** The list of statuses. */
    values?: Array<{ id?: string }>;
}

/** The worflow. */
export interface StatusWorkflowUsageWorkflow {
    /** The workflow ID. */
    id?: string;
}

/** The statuses associated with each workflow. */
export interface StatusesPerWorkflow {
    /** The ID of the initial status for the workflow. */
    initialStatusId?: string;
    /** The status IDs associated with the workflow. */
    statuses?: Array<string>;
    /** The ID of the workflow. */
    workflowId?: string;
}

export interface StreamingResponseBody {
}

export interface StringList {
}

export interface SubmittedBulkOperation {
    taskId?: string;
}

export interface SuccessOrErrorResults {
    /** Error during remove field parameters operation. */
    error?: { code?: string; message?: string };
    fieldId?: string;
    schemeId?: number;
    success?: boolean;
    workTypeIds?: Array<number>;
}

/** An issue suggested for use in the issue picker auto-completion. */
export interface SuggestedIssue {
    /** The ID of the issue. */
    readonly id?: number;
    /** The URL of the issue type's avatar. */
    readonly img?: string;
    /** The key of the issue. */
    readonly key?: string;
    /** The key of the issue in HTML format. */
    readonly keyHtml?: string;
    /** The phrase containing the query string in HTML format, with the string highlighted with HTML bold tags. */
    readonly summary?: string;
    /** The phrase containing the query string, as plain text. */
    readonly summaryText?: string;
}

/** Details of changes to a priority scheme's priorities that require suggested priority mappings. */
export interface SuggestedMappingsForPrioritiesRequestBean {
    /** The ids of priorities being removed from the scheme. */
    add?: Array<number>;
    /** The ids of priorities being removed from the scheme. */
    remove?: Array<number>;
}

/** Details of changes to a priority scheme's projects that require suggested priority mappings. */
export interface SuggestedMappingsForProjectsRequestBean {
    /** The ids of projects being added to the scheme. */
    add?: Array<number>;
}

/** Details of changes to a priority scheme that require suggested priority mappings. */
export interface SuggestedMappingsRequestBean {
    /** The maximum number of results that could be on the page. */
    maxResults?: number;
    /** The priority changes in the scheme. */
    priorities?: { add?: Array<number>; remove?: Array<number> };
    /** The project changes in the scheme. */
    projects?: { add?: Array<number> };
    /** The id of the priority scheme. */
    schemeId?: number;
    /** The index of the first item returned on the page. */
    startAt?: number;
}

/** The payload for custom swimlanes */
export interface SwimlanePayload {
    /** The description of the quick filter */
    description?: string;
    /** The jql query for the quick filter */
    jqlQuery?: string;
    /** The name of the quick filter */
    name?: string;
}

/** The payload for customising a swimlanes on a board */
export interface SwimlanesPayload {
    /** The custom swimlane definitions. */
    customSwimlanes?: Array<{ description?: string; jqlQuery?: string; name?: string }>;
    /** The name of the custom swimlane to use for work items that don't match any other swimlanes. */
    defaultCustomSwimlaneName?: string;
    /** The swimlane strategy for the board. */
    swimlaneStrategy?: "none" | "custom" | "parentChild" | "assignee" | "assigneeUnassignedFirst" | "epic" | "project" | "issueparent" | "issuechildren" | "request_type";
}

/** List of system avatars. */
export interface SystemAvatars {
    /** A list of avatar details. */
    readonly system?: Array<Record<string, unknown>>;
}

/** Defines the payload for the tabs of the screen. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-screen-tab-fields/\#api-rest-api-3-screens-screenid-tabs-tabid-fields-post */
export interface TabPayload {
    /** The list of resource identifier of the field associated to the tab. See https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-screen-tab-fields/\#api-rest-api-3-screens-screenid-tabs-tabid-fields-post */
    fields?: Array<{ anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }>;
    /** The name of the tab */
    name?: string;
}

/** Details about a task. */
export interface TaskProgress {
    /** The description of the task. */
    description?: string;
    /** The execution time of the task, in milliseconds. */
    elapsedRuntime: number;
    /** A timestamp recording when the task was finished. */
    finished?: string;
    /** The ID of the task. */
    id: string;
    /** A timestamp recording when the task progress was last updated. */
    lastUpdate: string;
    /** Information about the progress of the task. */
    message?: string;
    /** The progress of the task, as a percentage complete. */
    progress: number;
    /** The result of the task execution. */
    result?: Record<string, unknown>;
    /** The URL of the task. */
    self: string;
    /** A timestamp recording when the task was started. */
    started?: string;
    /** The status of the task. */
    status: "ENQUEUED" | "RUNNING" | "COMPLETE" | "FAILED" | "CANCEL_REQUESTED" | "CANCELLED" | "DEAD";
    /** A timestamp recording when the task was submitted. */
    submitted?: string;
    /** The ID of the user who submitted the task. */
    submittedBy: number;
}

/** Details about a task. */
export interface TaskProgressBeanJsonNode {
    /** The description of the task. */
    description?: string;
    /** The execution time of the task, in milliseconds. */
    elapsedRuntime: number;
    /** A timestamp recording when the task was finished. */
    finished?: number;
    /** The ID of the task. */
    id: string;
    /** A timestamp recording when the task progress was last updated. */
    lastUpdate: number;
    /** Information about the progress of the task. */
    message?: string;
    /** The progress of the task, as a percentage complete. */
    progress: number;
    /** The result of the task execution. */
    result?: { array?: boolean; bigDecimal?: boolean; bigInteger?: boolean; bigIntegerValue?: number; binary?: boolean; binaryValue?: Array<string>; boolean?: boolean; booleanValue?: boolean; containerNode?: boolean; decimalValue?: number; double?: boolean; doubleValue?: number; elements?: Record<string, unknown>; fieldNames?: Record<string, unknown>; fields?: Record<string, unknown>; floatingPointNumber?: boolean; int?: boolean; intValue?: number; integralNumber?: boolean; long?: boolean; longValue?: number; missingNode?: boolean; null?: boolean; number?: boolean; numberType?: "INT" | "LONG" | "BIG_INTEGER" | "FLOAT" | "DOUBLE" | "BIG_DECIMAL"; numberValue?: number; object?: boolean; pojo?: boolean; textValue?: string; textual?: boolean; valueAsBoolean?: boolean; valueAsDouble?: number; valueAsInt?: number; valueAsLong?: number; valueAsText?: string; valueNode?: boolean };
    /** The URL of the task. */
    self: string;
    /** A timestamp recording when the task was started. */
    started?: number;
    /** The status of the task. */
    status: "ENQUEUED" | "RUNNING" | "COMPLETE" | "FAILED" | "CANCEL_REQUESTED" | "CANCELLED" | "DEAD";
    /** A timestamp recording when the task was submitted. */
    submitted: number;
    /** The ID of the user who submitted the task. */
    submittedBy: number;
}

/** Details about a task. */
export interface TaskProgressBeanObject {
    /** The description of the task. */
    description?: string;
    /** The execution time of the task, in milliseconds. */
    elapsedRuntime: number;
    /** A timestamp recording when the task was finished. */
    finished?: number;
    /** The ID of the task. */
    id: string;
    /** A timestamp recording when the task progress was last updated. */
    lastUpdate: number;
    /** Information about the progress of the task. */
    message?: string;
    /** The progress of the task, as a percentage complete. */
    progress: number;
    /** The result of the task execution. */
    result?: Record<string, unknown>;
    /** The URL of the task. */
    self: string;
    /** A timestamp recording when the task was started. */
    started?: number;
    /** The status of the task. */
    status: "ENQUEUED" | "RUNNING" | "COMPLETE" | "FAILED" | "CANCEL_REQUESTED" | "CANCELLED" | "DEAD";
    /** A timestamp recording when the task was submitted. */
    submitted: number;
    /** The ID of the user who submitted the task. */
    submittedBy: number;
}

/** Details about a task. */
export interface TaskProgressBeanRemoveOptionFromIssuesResult {
    /** The description of the task. */
    description?: string;
    /** The execution time of the task, in milliseconds. */
    elapsedRuntime: number;
    /** A timestamp recording when the task was finished. */
    finished?: number;
    /** The ID of the task. */
    id: string;
    /** A timestamp recording when the task progress was last updated. */
    lastUpdate: number;
    /** Information about the progress of the task. */
    message?: string;
    /** The progress of the task, as a percentage complete. */
    progress: number;
    /** The result of the task execution. */
    result?: { errors?: { errorMessages?: Array<string>; errors?: Record<string, string>; httpStatusCode?: number }; modifiedIssues?: Array<number>; unmodifiedIssues?: Array<number> };
    /** The URL of the task. */
    self: string;
    /** A timestamp recording when the task was started. */
    started?: number;
    /** The status of the task. */
    status: "ENQUEUED" | "RUNNING" | "COMPLETE" | "FAILED" | "CANCEL_REQUESTED" | "CANCELLED" | "DEAD";
    /** A timestamp recording when the task was submitted. */
    submitted: number;
    /** The ID of the user who submitted the task. */
    submittedBy: number;
}

/** Details of the time tracking configuration. */
export interface TimeTrackingConfiguration {
    /** The default unit of time applied to logged time. */
    defaultUnit: "minute" | "hour" | "day" | "week";
    /** The format that will appear on an issue's *Time Spent* field. */
    timeFormat: "pretty" | "days" | "hours";
    /** The number of days in a working week. */
    workingDaysPerWeek: number;
    /** The number of hours in a working day. */
    workingHoursPerDay: number;
}

/** Time tracking details. */
export interface TimeTrackingDetails {
    /** The original estimate of time needed for this issue in readable format. */
    readonly originalEstimate?: string;
    /** The original estimate of time needed for this issue in seconds. */
    readonly originalEstimateSeconds?: number;
    /** The remaining estimate of time needed for this issue in readable format. */
    readonly remainingEstimate?: string;
    /** The remaining estimate of time needed for this issue in seconds. */
    readonly remainingEstimateSeconds?: number;
    /** Time worked on this issue in readable format. */
    readonly timeSpent?: string;
    /** Time worked on this issue in seconds. */
    readonly timeSpentSeconds?: number;
}

/** Details about the time tracking provider. */
export interface TimeTrackingProvider {
    /** The key for the time tracking provider. For example, *JIRA*. */
    key: string;
    /** The name of the time tracking provider. For example, *JIRA provided time tracking*. */
    name?: string;
    /** The URL of the configuration page for the time tracking provider app. For example, */example/config/url*. This property is only returned if the `adminPageKey` property is set in the module descriptor of the time tracking provider app. */
    readonly url?: string;

/** The payload for the layout details for the destination end of a transition */
export interface ToLayoutPayload {
    /** Defines where the transition line will be connected to a status. Port 0 to 7 are acceptable values. */
    port?: number;
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    status?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
}

/** Details of a workflow transition. */
export interface Transition {
    /** The description of the transition. */
    description: string;
    /** The statuses the transition can start from. */
    from: Array<string>;
    /** The ID of the transition. */
    id: string;
    /** The name of the transition. */
    name: string;
    /** The properties of the transition. */
    properties?: Record<string, Record<string, unknown>>;
    /** A collection of transition rules. */
    rules?: { conditionsTree?: { configuration?: Record<string, unknown>; nodeType: string; type: string } | { conditions: Array<unknown>; nodeType: string; operator: "AND" | "OR" }; postFunctions?: Array<{ configuration?: Record<string, unknown>; type: string }>; validators?: Array<unknown> };
    /** The details of a transition screen. */
    screen?: { id: string; name?: string };
    /** The status the transition goes to. */
    to: string;
    /** The type of the transition. */
    type: "global" | "initial" | "directed";
}

/** Link information for workflow transitions. */
export interface TransitionLink {
    /** The from port number. */
    fromPort?: number;
    /** The from status reference. */
    fromStatusReference?: string;
    /** The to port number. */
    toPort?: number;
}

/** The payload for creating a transition in a workflow. Can be DIRECTED, GLOBAL, SELF-LOOPED, GLOBAL LOOPED */
export interface TransitionPayload {
    /** The actions that are performed when the transition is made */
    actions?: Array<{ parameters?: Record<string, string>; ruleKey?: string }>;
    /** The payload for creating a condition group in a workflow */
    conditions?: { conditionGroup?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" };
    /** Mechanism in Jira for triggering certain actions, like notifications, automations, etc. Unless a custom notification scheme is configure, it's better not to provide any value here */
    customIssueEventId?: string;
    /** The description of the transition */
    description?: string;
    /** The statuses that the transition can be made from */
    from?: Array<{ fromPort?: number; status?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }; toPortOverride?: number }>;
    /** The id of the transition */
    id?: number;
    /** The name of the transition */
    name?: string;
    /** The properties of the transition */
    properties?: Record<string, string>;
    /** The payload for the layout details for the destination end of a transition */
    to?: { port?: number; status?: unknown };
    /** The payload for creating rules in a workflow */
    transitionScreen?: unknown;
    /** The triggers that are performed when the transition is made */
    triggers?: Array<unknown>;
    /** The type of the transition */
    type?: "global" | "initial" | "directed";
    /** The validators that are performed when the transition is made */
    validators?: Array<unknown>;
}

/** Details about a workflow transition in preview context. */
export interface TransitionPreview {
    /** The post-functions of the transition. */
    actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey?: string }>;
    /** Condition group configuration for workflow transitions. */
    conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" };
    /** The custom issue event ID for the transition. */
    customIssueEventId?: string;
    /** The description of the transition. */
    description?: string;
    /** The ID of the transition. */
    id?: string;
    /** The statuses the transition can start from, and the mapping of ports between the statuses. */
    links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>;
    /** The name of the transition. */
    name?: string;
    /** The status the transition goes to. */
    toStatusReference?: string;
    /** Rule configuration for workflow transitions. */
    transitionScreen?: unknown;
    /** The triggers of the transition. */
    triggers?: Array<{ id?: string; ruleKey?: string }>;
    /** The transition type. */
    type?: "INITIAL" | "GLOBAL" | "DIRECTED";
    /** The validators of the transition. */
    validators?: Array<unknown>;
}

/** The details of a transition screen. */
export interface TransitionScreenDetails {
    /** The ID of the screen. */
    id: string;
    /** The name of the screen. */
    name?: string;
}

/** The transition update data. */
export type TransitionUpdateDTO = Record<string, unknown>;

/** List of issue transitions. */
export interface Transitions {
    /** Expand options that include additional transitions details in the response. */
    readonly expand?: string;
    /** List of issue transitions. */
    readonly transitions?: Array<Record<string, unknown>>;
}

/** The details of a UI modification's context, which define where to activate the UI modification. */
export interface UiModificationContextDetails {
    /** The ID of the UI modification context. */
    readonly id?: string;
    /** Whether a context is available. For example, when a project is deleted the context becomes unavailable. */
    readonly isAvailable?: boolean;
    /** The issue type ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all issue types. Each UI modification context can have a maximum of one wildcard. */
    issueTypeId?: string;
    /** The portal ID of the context. Only required for Jira Service Management request create portal view (`JSMRequestCreate`). */
    portalId?: string;
    /** The project ID of the context. Null is treated as a wildcard, meaning the UI modification will be applied to all projects. Each UI modification context can have a maximum of one wildcard. */
    projectId?: string;
    /** The request type ID of the context. Only required for Jira Service Management request create portal view (`JSMRequestCreate`). */
    requestTypeId?: string;
    /**
     * The view type of the context.  
     * Supported values:
     *
     *  *  `GIC` \- Jira global issue create
     *  *  `IssueView` \- Jira issue view
     *  *  `IssueTransition` \- Jira issue transition
     *  *  `JSMRequestCreate` \- Jira Service Management request create portal view
     *
     * For Jira view types (`GIC`, `IssueView`, `IssueTransition`), null is treated as a wildcard, meaning the UI modification will be applied to all view types. Each Jira context can have a maximum of one wildcard.  
     *   
     * Wildcards are not applicable for JSM contexts.
     */
    viewType?: "GIC" | "IssueView" | "IssueTransition" | "JSMRequestCreate";
}

/** The details of a UI modification. */
export interface UiModificationDetails {
    /** List of contexts of the UI modification. The maximum number of contexts is 1000. */
    readonly contexts?: Array<{ readonly id?: string; readonly isAvailable?: boolean; issueTypeId?: string; portalId?: string; projectId?: string; requestTypeId?: string; viewType?: "GIC" | "IssueView" | "IssueTransition" | "JSMRequestCreate" }>;
    /** The data of the UI modification. The maximum size of the data is 50000 characters. */
    readonly data?: string;
    /** The description of the UI modification. The maximum length is 255 characters. */
    readonly description?: string;
    /** The ID of the UI modification. */
    readonly id: string;
    /** The name of the UI modification. The maximum length is 255 characters. */
    readonly name: string;
    /** The URL of the UI modification. */
    readonly self: string;
}

/** Identifiers for a UI modification. */
export interface UiModificationIdentifiers {
    /** The ID of the UI modification. */
    readonly id: string;
    /** The URL of the UI modification. */
    readonly self: string;
}

export type UnrestrictedUserEmail = Record<string, unknown>;

/** Details of a custom field. */
export interface UpdateCustomFieldDetails {
    /** The description of the custom field. The maximum length is 40000 characters. */
    description?: string;
    /** The name of the custom field. It doesn't have to be unique. The maximum length is 255 characters. */
    name?: string;
    /**
     * The searcher that defines the way the field is searched in Jira. It can be set to `null`, otherwise you must specify the valid searcher for the field type, as listed below (abbreviated values shown):
     *
     *  *  `cascadingselect`: `cascadingselectsearcher`
     *  *  `datepicker`: `daterange`
     *  *  `datetime`: `datetimerange`
     *  *  `float`: `exactnumber` or `numberrange`
     *  *  `grouppicker`: `grouppickersearcher`
     *  *  `importid`: `exactnumber` or `numberrange`
     *  *  `labels`: `labelsearcher`
     *  *  `multicheckboxes`: `multiselectsearcher`
     *  *  `multigrouppicker`: `multiselectsearcher`
     *  *  `multiselect`: `multiselectsearcher`
     *  *  `multiuserpicker`: `userpickergroupsearcher`
     *  *  `multiversion`: `versionsearcher`
     *  *  `project`: `projectsearcher`
     *  *  `radiobuttons`: `multiselectsearcher`
     *  *  `readonlyfield`: `textsearcher`
     *  *  `select`: `multiselectsearcher`
     *  *  `textarea`: `textsearcher`
     *  *  `textfield`: `textsearcher`
     *  *  `url`: `exacttextsearcher`
     *  *  `userpicker`: `userpickergroupsearcher`
     *  *  `version`: `versionsearcher`
     */
    searcherKey?: "com.atlassian.jira.plugin.system.customfieldtypes:cascadingselectsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:daterange" | "com.atlassian.jira.plugin.system.customfieldtypes:datetimerange" | "com.atlassian.jira.plugin.system.customfieldtypes:exactnumber" | "com.atlassian.jira.plugin.system.customfieldtypes:exacttextsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:grouppickersearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:labelsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:multiselectsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:numberrange" | "com.atlassian.jira.plugin.system.customfieldtypes:projectsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:textsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:userpickergroupsearcher" | "com.atlassian.jira.plugin.system.customfieldtypes:versionsearcher";
}

/** The request for updating the default project classification level. */
export interface UpdateDefaultProjectClassificationBean {
    /** The ID of the project classification. */
    id: string;
}

/** The ID of a screen scheme. */
export interface UpdateDefaultScreenScheme {
    /** The ID of the screen scheme. */
    screenSchemeId: string;
}

export interface UpdateFieldAssociationSchemeLinksBean {
    associations?: string;
    projects?: string;
}

/** Request object for updating an existing field association scheme. */
export interface UpdateFieldAssociationSchemeRequest {
    /** The description value to update */
    description?: string;
    /** The name value to update */
    name?: string;
}

/** Response object after successfully updating an existing field association scheme. */
export interface UpdateFieldAssociationSchemeResponse {
    description?: string;
    id?: number;
    links?: { associations?: string; projects?: string };
    name?: string;
}

/** Represents an association between a field and its operations. */
export interface UpdateFieldAssociationsRequestItem {
    /** (optional) Work types to restrict field to. Replaces any existing work type associations for the field. If not provided, the field is associated to any work types. */
    restrictedToWorkTypes?: Array<number>;
    /** Scheme IDs to associate field with */
    schemeIds: Array<number>;
}

/** The details of the field configuration scheme. */
export interface UpdateFieldConfigurationSchemeDetails {
    /** The description of the field configuration scheme. */
    description?: string;
    /** The name of the field configuration scheme. The name must be unique. */
    name: string;
}

/** Result of updating field scheme parameters for a specific field, scheme, and optional work type. */
export interface UpdateFieldSchemeParametersPartialFailure {
    error?: string;
    fieldId: string;
    schemeId: number;
    success: boolean;
    workTypeId?: number;
}

/** Request bean for updating field scheme parameters across multiple schemes and work types. */
export interface UpdateFieldSchemeParametersRequest {
    /** The default parameters to apply to the field across all work types in the specified schemes, may be null if only work type-specific updates are needed */
    parameters?: { description?: string; isRequired?: boolean };
    /** The list of field scheme IDs to update */
    schemeIds?: Array<number>;
    /** The list of work type-specific parameter overrides, may be empty if only default parameters are being updated */
    workTypeParameters?: Array<{ description?: string; isRequired?: boolean; workTypeId?: number }>;
}

/** Response bean for field scheme parameter update operations. */
export interface UpdateFieldSchemeParametersResponse {
    results: Array<{ error?: string; fieldId: string; schemeId: number; success: boolean; workTypeId?: number }>;
}

/** Details of issue security scheme level. */
export type UpdateIssueSecurityLevelDetails = Record<string, unknown>;

export interface UpdateIssueSecuritySchemeRequestBean {
    /** The description of the security scheme scheme. */
    description?: string;
    /** The name of the security scheme scheme. Must be unique. */
    name?: string;
}

/** Details of a notification scheme. */
export type UpdateNotificationSchemeDetails = Record<string, unknown>;
/** Update priorities in a scheme */
export type UpdatePrioritiesInSchemeRequestBean = Record<string, unknown>;
/** Details of an issue priority. */
export type UpdatePriorityDetails = Record<string, unknown>;

/** Details of a priority scheme. */
export interface UpdatePrioritySchemeRequestBean {
    /** The default priority of the scheme. */
    defaultPriorityId?: number;
    /** The description of the priority scheme. */
    description?: string;
    /**
     * Instructions to migrate the priorities of issues.
     *
     * `in` mappings are used to migrate the priorities of issues to priorities used within the priority scheme.
     *
     * `out` mappings are used to migrate the priorities of issues to priorities not used within the priority scheme.
     *
     *  *  When **priorities** are **added** to the priority scheme, no mapping needs to be provided as the new priorities are not used by any issues.
     *  *  When **priorities** are **removed** from the priority scheme, issues that are using those priorities must be migrated to new priorities used by the priority scheme.
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **added** to the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities used by the priority scheme. This can occur when the current scheme does not use all the priorities in the project(s)' priority scheme(s).
     *     
     *      *  An `in` mapping must be provided for each of these priorities.
     *  *  When **projects** are **removed** from the priority scheme, the priorities of issues in those projects might need to be migrated to new priorities within the **Default Priority Scheme** that are not used by the priority scheme. This can occur when the **Default Priority Scheme** does not use all the priorities within the current scheme.
     *     
     *      *  An `out` mapping must be provided for each of these priorities.
     *
     * For more information on `in` and `out` mappings, see the child properties documentation for the `PriorityMapping` object below.
     */
    mappings?: { in?: Record<string, number>; out?: Record<string, number> };
    /** The name of the priority scheme. Must be unique. */
    name?: string;
    /** The priorities in the scheme. */
    priorities?: Record<string, unknown>;
    /** The projects in the scheme. */
    projects?: Record<string, unknown>;
}

/** Details of the updated priority scheme. */
export type UpdatePrioritySchemeResponseBean = Record<string, unknown>;

/** Details about the project. */
export interface UpdateProjectDetails {
    /** The default assignee when creating issues for this project. */
    assigneeType?: "PROJECT_LEAD" | "UNASSIGNED";
    /** An integer value for the project's avatar. */
    avatarId?: number;
    /** The ID of the project's category. A complete list of category IDs is found using the [Get all project categories](#api-rest-api-3-projectCategory-get) operation. To remove the project category from the project, set the value to `-1.` */
    categoryId?: number;
    /** A brief description of the project. */
    description?: string;
    /** The ID of the issue security scheme for the project, which enables you to control who can and cannot view issues. Use the [Get issue security schemes](#api-rest-api-3-issuesecurityschemes-get) resource to get all issue security scheme IDs. */
    issueSecurityScheme?: number;
    /** Project keys must be unique and start with an uppercase letter followed by one or more uppercase alphanumeric characters. The maximum length is 10 characters. */
    key?: string;
    /** This parameter is deprecated because of privacy changes. Use `leadAccountId` instead. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. The user name of the project lead. Cannot be provided with `leadAccountId`. */
    lead?: string;
    /** The account ID of the project lead. Cannot be provided with `lead`. */
    leadAccountId?: string;
    /** The name of the project. */
    name?: string;
    /** The ID of the notification scheme for the project. Use the [Get notification schemes](#api-rest-api-3-notificationscheme-get) resource to get a list of notification scheme IDs. */
    notificationScheme?: number;
    /** The ID of the permission scheme for the project. Use the [Get all permission schemes](#api-rest-api-3-permissionscheme-get) resource to see a list of all permission scheme IDs. */
    permissionScheme?: number;
    /** Previous project keys to be released from the current project. Released keys must belong to the current project and not contain the current project key */
    releasedProjectKeys?: Array<string>;
    /** A link to information about this project, such as project documentation */
    url?: string;
}

/** Update projects in a scheme */
export type UpdateProjectsInSchemeRequestBean = Record<string, unknown>;
/** Details of an issue resolution. */
export type UpdateResolutionDetails = Record<string, unknown>;

/** Details of a screen. */
export interface UpdateScreenDetails {
    /** The description of the screen. The maximum length is 255 characters. */
    description?: string;
    /** The name of the screen. The name must be unique. The maximum length is 255 characters. */
    name?: string;
}

/** Details of a screen scheme. */
export interface UpdateScreenSchemeDetails {
    /** The description of the screen scheme. The maximum length is 255 characters. */
    description?: string;
    /** The name of the screen scheme. The name must be unique. The maximum length is 255 characters. */
    name?: string;
    /** The IDs of the screens for the screen types of the screen scheme. Only screens used in classic projects are accepted. */
    screens?: { create?: string; default?: string; edit?: string; view?: string };
}

/** The IDs of the screens for the screen types of the screen scheme. */
export interface UpdateScreenTypes {
    /** The ID of the create screen. To remove the screen association, pass a null. */
    create?: string;
    /** The ID of the default screen. When specified, must include a screen ID as a default screen is required. */
    default?: string;
    /** The ID of the edit screen. To remove the screen association, pass a null. */
    edit?: string;
    /** The ID of the view screen. To remove the screen association, pass a null. */
    view?: string;
}

/** The details of a UI modification. */
export interface UpdateUiModificationDetails {
    /** List of contexts of the UI modification. The maximum number of contexts is 1000. If provided, replaces all existing contexts. */
    contexts?: Array<{ readonly id?: string; readonly isAvailable?: boolean; issueTypeId?: string; portalId?: string; projectId?: string; requestTypeId?: string; viewType?: "GIC" | "IssueView" | "IssueTransition" | "JSMRequestCreate" }>;
    /** The data of the UI modification. The maximum size of the data is 50000 characters. */
    data?: string;
    /** The description of the UI modification. The maximum length is 255 characters. */
    description?: string;
    /** The name of the UI modification. The maximum length is 255 characters. */
    name?: string;
}

export type UpdateUserToGroupBean = Record<string, unknown>;

/** A project category. */
export interface UpdatedProjectCategory {
    /** The name of the project category. */
    readonly description?: string;
    /** The ID of the project category. */
    readonly id?: string;
    /** The description of the project category. */
    readonly name?: string;
    /** The URL of the project category. */
    readonly self?: string;
}

/**
 * A user with details as permitted by the user's Atlassian Account privacy settings. However, be aware of these exceptions:
 *
 *  *  User record deleted from Atlassian: This occurs as the result of a right to be forgotten request. In this case, `displayName` provides an indication and other parameters have default values or are blank (for example, email is blank).
 *  *  User record corrupted: This occurs as a results of events such as a server import and can only happen to deleted users. In this case, `accountId` returns *unknown* and all other parameters have fallback values.
 *  *  User record unavailable: This usually occurs due to an internal service outage. In this case, all parameters have fallback values.
 */
export interface User {
    /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. Required in requests. */
    accountId?: string;
    /**
     * The user account type. Can take the following values:
     *
     *  *  `atlassian` regular Atlassian user account
     *  *  `app` system account used for Connect applications and OAuth to represent external systems
     *  *  `customer` Jira Service Desk account representing an external service desk
     */
    readonly accountType?: "atlassian" | "app" | "customer" | "unknown";
    /** Whether the user is active. */
    readonly active?: boolean;
    /**
     * The app type of the user account when accountType is 'app'. Can take the following values:
     *
     *  *  `service` Service Account
     *  *  `agent` Rovo Agent Account
     *  *  `unknown` Unknown app type
     */
    readonly appType?: string;
    /** The application roles the user is assigned to. */
    readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number };
    /** The avatars of the user. */
    readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string };
    /** The display name of the user. Depending on the user’s privacy setting, this may return an alternative value. */
    readonly displayName?: string;
    /** The email address of the user. Depending on the user’s privacy setting, this may be returned as null. */
    readonly emailAddress?: string;
    /** Expand options that include additional user details in the response. */
    readonly expand?: string;
    /** The groups that the user belongs to. */
    readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number };
    /** Whether the user is a guest. */
    readonly guest?: boolean;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    key?: string;
    /** The locale of the user. Depending on the user’s privacy setting, this may be returned as null. */
    readonly locale?: string;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    name?: string;
    /** The URL of the user. */
    readonly self?: string;
    /** The time zone specified in the user's profile. If the user's time zone is not visible to the current user (due to user's profile setting), or if a time zone has not been set, the instance's default time zone will be returned. */
    readonly timeZone?: string;

export interface UserBean {
    /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. */
    accountId?: string;
    /** Whether the user is active. */
    active?: boolean;
    /** The avatars of the user. */
    avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string };
    /** The display name of the user. Depending on the user’s privacy setting, this may return an alternative value. */
    displayName?: string;
    /**
     * This property is deprecated in favor of `accountId` because of privacy changes. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details.  
     * The key of the user.
     */
    key?: string;
    /**
     * This property is deprecated in favor of `accountId` because of privacy changes. See the [migration guide](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details.  
     * The username of the user.
     */
    name?: string;
    /** The URL of the user. */
    self?: string;

export interface UserBeanAvatarUrls {
    /** The URL of the user's 16x16 pixel avatar. */
    16x16?: string;
    /** The URL of the user's 24x24 pixel avatar. */
    24x24?: string;
    /** The URL of the user's 32x32 pixel avatar. */
    32x32?: string;
    /** The URL of the user's 48x48 pixel avatar. */
    48x48?: string;

export interface UserColumnRequestBody {
    columns?: Array<string>;
}

/** A [user](https://developer.atlassian.com/cloud/jira/platform/jira-expressions-type-reference#user) specified as an Atlassian account ID. */
export interface UserContextVariable {
    /** The account ID of the user. */
    accountId: string;
    /** Type of custom context variable. */
    type: string;
}

/**
 * User details permitted by the user's Atlassian Account privacy settings. However, be aware of these exceptions:
 *
 *  *  User record deleted from Atlassian: This occurs as the result of a right to be forgotten request. In this case, `displayName` provides an indication and other parameters have default values or are blank (for example, email is blank).
 *  *  User record corrupted: This occurs as a results of events such as a server import and can only happen to deleted users. In this case, `accountId` returns *unknown* and all other parameters have fallback values.
 *  *  User record unavailable: This usually occurs due to an internal service outage. In this case, all parameters have fallback values.
 */
export interface UserDetails {
    /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. */
    accountId?: string;
    /** The type of account represented by this user. This will be one of 'atlassian' (normal users), 'app' (application user) or 'customer' (Jira Service Desk customer user) */
    readonly accountType?: string;
    /** Whether the user is active. */
    readonly active?: boolean;
    /** The avatars of the user. */
    readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string };
    /** The display name of the user. Depending on the user’s privacy settings, this may return an alternative value. */
    readonly displayName?: string;
    /** The email address of the user. Depending on the user’s privacy settings, this may be returned as null. */
    readonly emailAddress?: string;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    readonly key?: string;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    readonly name?: string;
    /** The URL of the user. */
    readonly self?: string;
    /** The time zone specified in the user's profile. Depending on the user’s privacy settings, this may be returned as null. */
    readonly timeZone?: string;

/** Filter for a User Picker (single) custom field. */
export interface UserFilter {
    /** Whether the filter is enabled. */
    enabled: boolean;
    /** User groups autocomplete suggestion users must belong to. If not provided, the default values are used. A maximum of 10 groups can be provided. */
    groups?: Array<string>;
    /** Roles that autocomplete suggestion users must belong to. If not provided, the default values are used. A maximum of 10 roles can be provided. */
    roleIds?: Array<number>;
}

/** List of user account IDs. */
export interface UserKey {
    /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. Returns *unknown* if the record is deleted and corrupted, for example, as the result of a server import. */
    accountId?: string;
    /** This property is no longer available and will be removed from the documentation soon. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    key?: string;
}

/** A paginated list of users sharing the filter. This includes users that are members of the groups or can browse the projects that the filter is shared with. */
export interface UserList {
    /** The index of the last item returned on the page. */
    readonly end-index?: number;
    /** The list of items. */
    readonly items?: Array<{ accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }>;
    /** The maximum number of results that could be on the page. */
    readonly max-results?: number;
    /** The number of items on the page. */
    readonly size?: number;
    /** The index of the first item returned on the page. */
    readonly start-index?: number;

export interface UserMigrationBean {
    accountId?: string;
    key?: string;
    username?: string;
}

/** Details of a permission and its availability to a user. */
export type UserPermission = Record<string, unknown>;

/** A user found in a search. */
export interface UserPickerUser {
    /** The account ID of the user, which uniquely identifies the user across all Atlassian products. For example, *5b10ac8d82e05b22cc7d4ef5*. */
    accountId?: string;
    /**
     * The user account type. Can take the following values:
     *
     *  *  `atlassian` regular Atlassian user account
     *  *  `app` system account used for Connect applications and OAuth to represent external systems
     *  *  `customer` Jira Service Desk account representing an external service desk
     */
    accountType?: "atlassian" | "app" | "customer" | "unknown";
    /** The avatar URL of the user. */
    avatarUrl?: string;
    /** The display name of the user. Depending on the user’s privacy setting, this may be returned as null. */
    displayName?: string;
    /** The display name, email address, and key of the user with the matched query string highlighted with the HTML bold tag. */
    html?: string;
    /** This property is no longer available. See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    key?: string;
    /** This property is no longer available . See the [deprecation notice](https://developer.atlassian.com/cloud/jira/platform/deprecation-notice-user-privacy-api-migration-guide/) for details. */
    name?: string;
}

/** The level of validation to return from the API. If no values are provided, the default would return `WARNING` and `ERROR` level validation results. */
export interface ValidationOptionsForCreate {
    levels?: Array<"WARNING" | "ERROR">;
}

/** The level of validation to return from the API. If no values are provided, the default would return `WARNING` and `ERROR` level validation results. */
export interface ValidationOptionsForUpdate {
    levels?: Array<"WARNING" | "ERROR">;
}

/** An operand that is a user-provided value. */
export interface ValueOperand {
    /** Encoded value, which can be used directly in a JQL query. */
    encodedValue?: string;
    /** The operand value. */
    value: string;
}

/** Details about a project version. */
export interface Version {
    /** If the expand option `approvers` is used, returns a list containing the approvers for this version. */
    readonly approvers?: Array<Record<string, unknown>>;
    /** Indicates that the version is archived. Optional when creating or updating a version. */
    archived?: boolean;
    /** The description of the version. Optional when creating or updating a version. The maximum size is 16,384 bytes. */
    description?: string;
    /** The Atlassian account ID of the version driver. Optional when creating or updating a version. If the expand option `driver` is used, returns the Atlassian account ID of the driver. */
    driver?: string;
    /**
     * Use [expand](em>#expansion) to include additional information about version in the response. This parameter accepts a comma-separated list. Expand options include:
     *
     *  *  `operations` Returns the list of operations available for this version.
     *  *  `issuesstatus` Returns the count of issues in this version for each of the status categories *to do*, *in progress*, *done*, and *unmapped*. The *unmapped* property contains a count of issues with a status other than *to do*, *in progress*, and *done*.
     *  *  `driver` Returns the Atlassian account ID of the version driver.
     *  *  `approvers` Returns a list containing approvers for this version.
     *
     * Optional for create and update.
     */
    expand?: string;
    /** The ID of the version. */
    readonly id?: string;
    /** If the expand option `issuesstatus` is used, returns the count of issues in this version for each of the status categories *to do*, *in progress*, *done*, and *unmapped*. The *unmapped* property contains a count of issues with a status other than *to do*, *in progress*, and *done*. */
    readonly issuesStatusForFixVersion?: Record<string, unknown>;
    /** The URL of the self link to the version to which all unfixed issues are moved when a version is released. Not applicable when creating a version. Optional when updating a version. */
    moveUnfixedIssuesTo?: string;
    /** The unique name of the version. Required when creating a version. Optional when updating a version. The maximum length is 255 characters. */
    name?: string;
    /** If the expand option `operations` is used, returns the list of operations available for this version. */
    readonly operations?: Array<{ href?: string; iconClass?: string; id?: string; label?: string; styleClass?: string; title?: string; weight?: number }>;
    /** Indicates that the version is overdue. */
    readonly overdue?: boolean;
    /** Deprecated. Use `projectId`. */
    project?: string;
    /** The ID of the project to which this version is attached. Required when creating a version. Not applicable when updating a version. */
    projectId?: number;
    /** The release date of the version. Expressed in ISO 8601 format (yyyy-mm-dd). Optional when creating or updating a version. */
    releaseDate?: string;
    /** Indicates that the version is released. If the version is released a request to release again is ignored. Not applicable when creating a version. Optional when updating a version. */
    released?: boolean;
    /** The URL of the version. */
    readonly self?: string;
    /** The start date of the version. Expressed in ISO 8601 format (yyyy-mm-dd). Optional when creating or updating a version. */
    startDate?: string;
    /** The date on which work on this version is expected to finish, expressed in the instance's *Day/Month/Year Format* date format. */
    readonly userReleaseDate?: string;
    /** The date on which work on this version is expected to start, expressed in the instance's *Day/Month/Year Format* date format. */
    readonly userStartDate?: string;
}

/** Contains details about a version approver. */
export type VersionApprover = Record<string, unknown>;

/** Various counts of issues within a version. */
export interface VersionIssueCounts {
    /** List of custom fields using the version. */
    readonly customFieldUsage?: Array<{ readonly customFieldId?: number; readonly fieldName?: string; readonly issueCountWithVersionInCustomField?: number }>;
    /** Count of issues where a version custom field is set to the version. */
    readonly issueCountWithCustomFieldsShowingVersion?: number;
    /** Count of issues where the `affectedVersion` is set to the version. */
    readonly issuesAffectedCount?: number;
    /** Count of issues where the `fixVersion` is set to the version. */
    readonly issuesFixedCount?: number;
    /** The URL of these count details. */
    readonly self?: string;
}

/** Counts of the number of issues in various statuses. */
export type VersionIssuesStatus = Record<string, unknown>;

export interface VersionMoveBean {
    /** The URL (self link) of the version after which to place the moved version. Cannot be used with `position`. */
    after?: string;
    /** An absolute position in which to place the moved version. Cannot be used with `after`. */
    position?: "Earlier" | "Later" | "First" | "Last";
}

/** Associated related work to a version */
export interface VersionRelatedWork {
    /** The category of the related work */
    category: string;
    /** The ID of the issue associated with the related work (if there is one). Cannot be updated via the Rest API. */
    readonly issueId?: number;
    /** The id of the related work. For the native release note related work item, this will be null, and Rest API does not support updating it. */
    readonly relatedWorkId?: string;
    /** The title of the related work */
    title?: string;
    /** The URL of the related work. Will be null for the native release note related work item, but is otherwise required. */
    url?: string;
}

/** Count of a version's unresolved issues. */
export interface VersionUnresolvedIssuesCount {
    /** Count of issues. */
    readonly issuesCount?: number;
    /** Count of unresolved issues. */
    readonly issuesUnresolvedCount?: number;
    /** The URL of these count details. */
    readonly self?: string;
}

/** List of custom fields using the version. */
export interface VersionUsageInCustomField {
    /** The ID of the custom field. */
    readonly customFieldId?: number;
    /** The name of the custom field. */
    readonly fieldName?: string;
    /** Count of the issues where the custom field contains the version. */
    readonly issueCountWithVersionInCustomField?: number;
}

/** The group or role to which this item is visible. */
export type Visibility = Record<string, unknown>;

/** The details of votes on an issue. */
export interface Votes {
    /** Whether the user making this request has voted on the issue. */
    readonly hasVoted?: boolean;
    /** The URL of these issue vote details. */
    readonly self?: string;
    /** List of the users who have voted on this issue. An empty list is returned when the calling user doesn't have the *View voters and watchers* project permission. */
    readonly voters?: Array<{ accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }>;
    /** The number of votes on the issue. */
    readonly votes?: number;

export interface WarningCollection {
    warnings?: Array<string>;
}

/** The details of watchers on an issue. */
export interface Watchers {
    /** Whether the calling user is watching this issue. */
    readonly isWatching?: boolean;
    /** The URL of these issue watcher details. */
    readonly self?: string;
    /** The number of users watching this issue. */
    readonly watchCount?: number;
    /** Details of the users watching this issue. */
    readonly watchers?: Array<{ accountId?: string; readonly accountType?: string; readonly active?: boolean; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly key?: string; readonly name?: string; readonly self?: string; readonly timeZone?: string }>;

/** A webhook. */
export interface Webhook {
    /** The Jira events that trigger the webhook. */
    events: Array<"jira:issue_created" | "jira:issue_updated" | "jira:issue_deleted" | "comment_created" | "comment_updated" | "comment_deleted" | "issue_property_set" | "issue_property_deleted" | "sprint_created" | "sprint_updated" | "sprint_closed" | "sprint_deleted" | "sprint_started" | "jira:version_released" | "jira:version_unreleased" | "jira:version_created" | "jira:version_moved" | "jira:version_updated" | "jira:version_merged" | "jira:version_deleted">;
    /** The date after which the webhook is no longer sent. Use [Extend webhook life](https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-webhooks/#api-rest-api-3-webhook-refresh-put) to extend the date. */
    readonly expirationDate?: number;
    /** A list of field IDs. When the issue changelog contains any of the fields, the webhook `jira:issue_updated` is sent. If this parameter is not present, the app is notified about all field updates. */
    fieldIdsFilter?: Array<string>;
    /** The ID of the webhook. */
    id: number;
    /** A list of issue property keys. A change of those issue properties triggers the `issue_property_set` or `issue_property_deleted` webhooks. If this parameter is not present, the app is notified about all issue property updates. */
    issuePropertyKeysFilter?: Array<string>;
    /** The JQL filter that specifies which issues the webhook is sent for. */
    jqlFilter: string;
    /** The URL that specifies where the webhooks are sent. */
    url: string;
}

/** A list of webhooks. */
export interface WebhookDetails {
    /** The Jira events that trigger the webhook. */
    events: Array<"jira:issue_created" | "jira:issue_updated" | "jira:issue_deleted" | "comment_created" | "comment_updated" | "comment_deleted" | "issue_property_set" | "issue_property_deleted" | "sprint_created" | "sprint_updated" | "sprint_closed" | "sprint_deleted" | "sprint_started" | "jira:version_released" | "jira:version_unreleased" | "jira:version_created" | "jira:version_moved" | "jira:version_updated" | "jira:version_merged" | "jira:version_deleted">;
    /** A list of field IDs. When the issue changelog contains any of the fields, the webhook `jira:issue_updated` is sent. If this parameter is not present, the app is notified about all field updates. */
    fieldIdsFilter?: Array<string>;
    /** A list of issue property keys. A change of those issue properties triggers the `issue_property_set` or `issue_property_deleted` webhooks. If this parameter is not present, the app is notified about all issue property updates. */
    issuePropertyKeysFilter?: Array<string>;
    /**
     * The JQL filter that specifies which issues the webhook is sent for. Only a subset of JQL can be used. The supported elements are:
     *
     *  *  Fields: `issueKey`, `project`, `issuetype`, `status`, `assignee`, `reporter`, `issue.property`, and `cf[id]`. For custom fields (`cf[id]`), only the epic label custom field is supported.".
     *  *  Operators: `=`, `!=`, `IN`, and `NOT IN`.
     */
    jqlFilter: string;
}

/** Details of webhooks to register. */
export interface WebhookRegistrationDetails {
    /** The URL that specifies where to send the webhooks. This URL must use the same base URL as the Connect app. Only a single URL per app is allowed to be registered. */
    url: string;
    /** A list of webhooks. */
    webhooks: Array<{ events: Array<"jira:issue_created" | "jira:issue_updated" | "jira:issue_deleted" | "comment_created" | "comment_updated" | "comment_deleted" | "issue_property_set" | "issue_property_deleted" | "sprint_created" | "sprint_updated" | "sprint_closed" | "sprint_deleted" | "sprint_started" | "jira:version_released" | "jira:version_unreleased" | "jira:version_created" | "jira:version_moved" | "jira:version_updated" | "jira:version_merged" | "jira:version_deleted">; fieldIdsFilter?: Array<string>; issuePropertyKeysFilter?: Array<string>; jqlFilter: string }>;
}

/** The date the refreshed webhooks expire. */
export interface WebhooksExpirationDate {
    /** The expiration date of all the refreshed webhooks. */
    readonly expirationDate: number;
}

export interface WorkManagementNavigationInfo {
    boardName?: string;
}

export interface WorkTypeParameters {
    description?: string;
    isRequired: boolean;
    workTypeId: number;
}

/** Details about a workflow. */
export interface Workflow {
    /** The creation date of the workflow. */
    created?: string;
    /** The description of the workflow. */
    description: string;
    /** Whether the workflow has a draft version. */
    hasDraftWorkflow?: boolean;
    /** Properties that identify a published workflow. */
    id: { entityId?: string; name: string };
    /** Whether this is the default workflow. */
    isDefault?: boolean;
    /** Operations allowed on a workflow */
    operations?: { canDelete: boolean; canEdit: boolean };
    /** The projects the workflow is assigned to, through workflow schemes. */
    projects?: Array<{ readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; id?: string; readonly key?: string; readonly name?: string; readonly projectCategory?: { readonly description?: string; readonly id?: string; readonly name?: string; readonly self?: string }; readonly projectTypeKey?: "software" | "service_desk" | "business"; readonly self?: string; readonly simplified?: boolean }>;
    /** The workflow schemes the workflow is assigned to. */
    schemes?: Array<{ id: string; name: string }>;
    /** The statuses of the workflow. */
    statuses?: Array<{ id: string; name: string; properties?: Record<string, Record<string, unknown>> }>;
    /** The transitions of the workflow. */
    transitions?: Array<{ description: string; from: Array<string>; id: string; name: string; properties?: Record<string, Record<string, unknown>>; rules?: { conditionsTree?: { configuration?: Record<string, unknown>; nodeType: string; type: string } | { conditions: Array<unknown>; nodeType: string; operator: "AND" | "OR" }; postFunctions?: Array<{ configuration?: Record<string, unknown>; type: string }>; validators?: Array<unknown> }; screen?: { id: string; name?: string }; to: string; type: "global" | "initial" | "directed" }>;
    /** The last edited date of the workflow. */
    updated?: string;

export interface WorkflowAssociationStatusMapping {
    newStatusId?: string;
    oldStatusId?: string;
}

export interface WorkflowCapabilities {
    /** The Connect provided ecosystem rules available. */
    connectRules?: Array<{ addonKey?: string; createUrl?: string; description?: string; editUrl?: string; moduleKey?: string; name?: string; ruleKey?: string; ruleType?: "Condition" | "Validator" | "Function" | "Screen"; viewUrl?: string }>;
    /** The scope of the workflow capabilities. `GLOBAL` for company-managed projects and `PROJECT` for team-managed projects. */
    editorScope?: "PROJECT" | "GLOBAL";
    /** The Forge provided ecosystem rules available. */
    forgeRules?: Array<{ description?: string; id?: string; name?: string; ruleKey?: string; ruleType?: "Condition" | "Validator" | "Function" | "Screen" }>;
    /** The types of projects that this capability set is available for. */
    projectTypes?: Array<"software" | "service_desk" | "product_discovery" | "business" | "unknown">;
    /** The Atlassian provided system rules available. */
    systemRules?: Array<{ description: string; incompatibleRuleKeys: Array<string>; isAvailableForInitialTransition: boolean; isVisible: boolean; name: string; ruleKey: string; ruleType: "Condition" | "Validator" | "Function" | "Screen" }>;
    /** The trigger rules available. */
    triggerRules?: Array<{ availableTypes: Array<{ description?: string; name?: string; type?: string }>; ruleKey: string }>;
}

/** The payload for creating a workflows. See https://www.atlassian.com/software/jira/guides/workflows/overview\#what-is-a-jira-workflow */
export interface WorkflowCapabilityPayload {
    /** The statuses for the workflow */
    statuses?: Array<{ description?: string; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" }; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE" }>;
    /** The payload for creating a workflow scheme. See https://www.atlassian.com/software/jira/guides/workflows/overview\#what-is-a-jira-workflow-scheme */
    workflowScheme?: { defaultWorkflow?: unknown; description?: string; explicitMappings?: Record<string, unknown>; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown };
    /** The transitions for the workflow */
    workflows?: Array<{ description?: string; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; onConflict?: "FAIL" | "USE" | "NEW"; pcri?: unknown; startPointLayout?: unknown; statuses?: Array<{ layout?: unknown; pcri?: unknown; properties?: Record<string, string> }>; transitions?: Array<{ actions?: Array<{ parameters?: Record<string, string>; ruleKey?: string }>; conditions?: { conditionGroup?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; from?: Array<{ fromPort?: number; status?: unknown; toPortOverride?: number }>; id?: number; name?: string; properties?: Record<string, string>; to?: { port?: number; status?: unknown }; transitionScreen?: unknown; triggers?: Array<unknown>; type?: "global" | "initial" | "directed"; validators?: Array<unknown> }> }>;
}

/** A compound workflow transition rule condition. This object returns `nodeType` as `compound`. */
export interface WorkflowCompoundCondition {
    /** The list of workflow conditions. */
    conditions: Array<{ configuration?: Record<string, unknown>; nodeType: string; type: string } | unknown>;
    nodeType: string;
    /** The compound condition operator. */
    operator: "AND" | "OR";
}

/** The workflow transition rule conditions tree. */
export type WorkflowCondition = { configuration?: Record<string, unknown>; nodeType: string; type: string } | { conditions: Array<unknown>; nodeType: string; operator: "AND" | "OR" };

/** The details of the workflows to create. */
export interface WorkflowCreate {
    /** The description of the workflow to create. */
    description?: string;
    /** The starting point for the statuses in the workflow. */
    loopedTransitionContainerLayout?: { x?: number; y?: number };
    /** The name of the workflow to create. */
    name: string;
    /** The starting point for the statuses in the workflow. */
    startPointLayout?: unknown;
    /** The statuses associated with this workflow. */
    statuses: Array<Record<string, unknown>>;
    /** The transitions of this workflow. */
    transitions: Array<Record<string, unknown>>;
}

/** The create workflows payload. */
export interface WorkflowCreateRequest {
    /** The scope of the workflow. */
    scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" };
    /** The statuses to associate with the workflows. */
    statuses?: Array<Record<string, unknown>>;
    /** The details of the workflows to create. */
    workflows?: Array<{ description?: string; loopedTransitionContainerLayout?: { x?: number; y?: number }; name: string; startPointLayout?: unknown; statuses: Array<Record<string, unknown>>; transitions: Array<Record<string, unknown>> }>;
}

/** Details of the created workflows and statuses. */
export interface WorkflowCreateResponse {
    /** List of created statuses. */
    statuses?: Array<{ description?: string; id?: string; name?: string; scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" }; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE"; statusReference?: string }>;
    /** List of created workflows. */
    workflows?: Array<{ created?: string; description?: string; id?: string; isEditable?: boolean; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; scope?: unknown; startPointLayout?: unknown; statuses?: Array<{ approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string }; deprecated?: boolean; layout?: { x?: number; y?: number }; properties?: Record<string, string>; statusReference?: string }>; taskId?: string; transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; properties?: Record<string, string>; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>; updated?: string; version?: { id?: string; versionNumber?: number } }>;
}

export interface WorkflowCreateValidateRequest {
    /** The create workflows payload. */
    payload: { scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" }; statuses?: Array<Record<string, unknown>>; workflows?: Array<{ description?: string; loopedTransitionContainerLayout?: { x?: number; y?: number }; name: string; startPointLayout?: unknown; statuses: Array<Record<string, unknown>>; transitions: Array<Record<string, unknown>> }> };
    /** The level of validation to return from the API. If no values are provided, the default would return `WARNING` and `ERROR` level validation results. */
    validationOptions?: { levels?: Array<"WARNING" | "ERROR"> };
}

/** The workflow stored for the specified version. */
export interface WorkflowDocumentDTO {
    created?: string;
    description?: string;
    id?: string;
    lastUpdateAuthorAAID?: string;
    /** The starting point for the statuses in the workflow. */
    loopedTransitionContainerLayout?: { x?: number; y?: number };
    name?: string;
    /** The scope of the workflow. */
    scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" };
    /** The starting point for the statuses in the workflow. */
    startPointLayout?: unknown;
    statuses?: Array<{ approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string }; deprecated?: boolean; layout?: { x?: number; y?: number }; properties?: Record<string, string>; statusReference?: string }>;
    transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; properties?: Record<string, string>; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>;
    updated?: string;
    /** The current version details of this workflow scheme. */
    version?: { id?: string; versionNumber?: number };
}

/** The statuses stored for the specified version. */
export interface WorkflowDocumentStatusDTO {
    description?: string;
    id?: string;
    name?: string;
    /** The scope of the workflow. */
    scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" };
    statusCategory?: string;
    statusReference?: string;
}

/** The version details of the workflow. */
export interface WorkflowDocumentVersionBean {
    /** The version UUID. */
    id?: string;
    /** The version number. */
    versionNumber?: number;
}

/** A reference to the location of the error. This will be null if the error does not refer to a specific element. */
export interface WorkflowElementReference {
    /** A property key. */
    propertyKey?: string;
    /** A rule ID. */
    ruleId?: string;
    /** A project and issueType ID pair that identifies a status mapping. */
    statusMappingReference?: { issueTypeId: string; projectId: string };
    /** A status reference. */
    statusReference?: string;
    /** A transition ID. */
    transitionId?: string;
}

/** A single entry in the WorkflowHistoryPage. */
export interface WorkflowHistoryItemDTO {
    /** Whether the version is an intermediate workflow state, sometimes created during workflow updates. */
    isIntermediate?: boolean;
    workflowId?: string;
    workflowVersion?: number;
    /** The timestamp when this workflow version was created. */
    writtenAt?: string;
}

/** A request to read all the workflow history entries for a specific workflow. */
export interface WorkflowHistoryListRequest {
    /** The id of the workflow to read the history for. */
    workflowId?: string;
}

/** A list of workflow history entries. */
export interface WorkflowHistoryListResponseDTO {
    entries?: Array<{ isIntermediate?: boolean; workflowId?: string; workflowVersion?: number; writtenAt?: string }>;
}

/** A request to read a specific workflow version from history. */
export interface WorkflowHistoryReadRequest {
    version?: number;
    workflowId?: string;
}

/** The specified workflow version read from history. */
export interface WorkflowHistoryReadResponseDTO {
    statuses?: Array<{ description?: string; id?: string; name?: string; scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" }; statusCategory?: string; statusReference?: string }>;
    workflows?: Array<{ created?: string; description?: string; id?: string; lastUpdateAuthorAAID?: string; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; scope?: unknown; startPointLayout?: unknown; statuses?: Array<{ approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string }; deprecated?: boolean; layout?: { x?: number; y?: number }; properties?: Record<string, string>; statusReference?: string }>; transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; properties?: Record<string, string>; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>; updated?: string; version?: { id?: string; versionNumber?: number } }>;
}

/** The classic workflow identifiers. */
export interface WorkflowIDs {
    /** The entity ID of the workflow. */
    entityId?: string;
    /** The name of the workflow. */
    name: string;
}

/** Properties that identify a workflow. */
export interface WorkflowId {
    /** **Deprecated:** Whether the workflow is in the draft state. The 'draft' parameter will be removed from this API on [November 2, 2026](https://developer.atlassian.com/cloud/jira/platform/changelog/#CHANGE-3147). */
    draft?: boolean;
    /** The name of the workflow. */
    name: string;
}

/** The starting point for the statuses in the workflow. */
export interface WorkflowLayout {
    /** The x axis location. */
    x?: number;
    /** The y axis location. */
    y?: number;
}

/** The workflow metadata and issue type IDs which use this workflow. */
export interface WorkflowMetadataAndIssueTypeRestModel {
    /** The list of issue type IDs for the mapping. */
    issueTypeIds: Array<string>;
    /** Workflow metadata and usage detail. */
    workflow: { description: string; id: string; name: string; version: { id?: string; versionNumber?: number } };
}

/** Workflow metadata and usage detail. */
export interface WorkflowMetadataRestModel {
    /** The description of the workflow. */
    description: string;
    /** The ID of the workflow. */
    id: string;
    /** The name of the workflow. */
    name: string;
    /** The current version details of this workflow scheme. */
    version: { id?: string; versionNumber?: number };
}

/** Operations allowed on a workflow */
export interface WorkflowOperations {
    /** Whether the workflow can be deleted. */
    canDelete: boolean;
    /** Whether the workflow can be updated. */
    canEdit: boolean;
}

/** The payload for creating workflow, see https://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-workflows/\#api-rest-api-3-workflows-create-post */
export interface WorkflowPayload {
    /** The description of the workflow */
    description?: string;
    /** The layout of the workflow status. */
    loopedTransitionContainerLayout?: { x?: number; y?: number };
    /** The name of the workflow */
    name?: string;
    /** The strategy to use if there is a conflict with another workflow */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The layout of the workflow status. */
    startPointLayout?: unknown;
    /** The statuses to be used in the workflow */
    statuses?: Array<{ layout?: unknown; pcri?: unknown; properties?: Record<string, string> }>;
    /** The transitions for the workflow */
    transitions?: Array<{ actions?: Array<{ parameters?: Record<string, string>; ruleKey?: string }>; conditions?: { conditionGroup?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; from?: Array<{ fromPort?: number; status?: unknown; toPortOverride?: number }>; id?: number; name?: string; properties?: Record<string, string>; to?: { port?: number; status?: unknown }; transitionScreen?: unknown; triggers?: Array<unknown>; type?: "global" | "initial" | "directed"; validators?: Array<unknown> }>;
}

/** Details of a workflow. */
export interface WorkflowPreview {
    /** The description of the workflow. */
    description?: string;
    /** The ID of the workflow. */
    id?: string;
    /** Layout coordinates for workflow elements. */
    loopedTransitionContainerLayout?: { x?: number; y?: number };
    /** The name of the workflow. */
    name?: string;
    /** The project and issue type context for this workflow query. */
    queryContext?: Array<{ issueTypes?: Array<string>; project?: string }>;
    /** The scope of the workflow. */
    scope?: { project?: { id?: string }; type?: "PROJECT" | "GLOBAL" };
    /** Layout coordinates for workflow elements. */
    startPointLayout?: unknown;
    /** The statuses referenced in this workflow. */
    statuses?: Array<{ approvalConfiguration?: { active?: string; transitionApproved?: string; transitionRejected?: string }; deprecated?: boolean; layout?: unknown; statusReference?: string }>;
    /** The transitions of the workflow. */
    transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey?: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; ruleKey?: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>;
    /** The version details of the workflow. */
    version?: { id?: string; versionNumber?: number };
}

/** Layout coordinates for workflow elements. */
export interface WorkflowPreviewLayout {
    /** The X coordinate. */
    x?: number;
    /** The Y coordinate. */
    y?: number;
}

/** The details of the preview workflow request. */
export interface WorkflowPreviewRequest {
    /** The list of issue type IDs. At most 25 issue type IDs can be specified. */
    issueTypeIds?: Array<string>;
    /** The projectId parameter is required and will be used for permission checks. In addition, you must supply at least one of the following lookup terms: *workflowNames*, *workflowIds*, or *issueTypeIds*. The specified workflows must be associated with the given project. */
    projectId: string;
    /** The list of workflow IDs to be returned. At most 25 workflow IDs can be specified. */
    workflowIds?: Array<string>;
    /** The list of workflow names to be returned. At most 25 workflow names can be specified. */
    workflowNames?: Array<string>;
}

/** The preview workflow response containing workflows and statuses. */
export interface WorkflowPreviewResponse {
    /** The list of statuses referenced by the workflows. */
    statuses?: Array<{ description?: string; id?: string; name?: string; rawName?: string; scope?: { project?: { id?: string }; type?: "PROJECT" | "GLOBAL" }; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE"; statusReference?: string }>;
    /** The list of workflows. The workflows are returned in the same order as specified in the request. */
    workflows?: Array<{ description?: string; id?: string; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; queryContext?: Array<{ issueTypes?: Array<string>; project?: string }>; scope?: unknown; startPointLayout?: unknown; statuses?: Array<{ approvalConfiguration?: { active?: string; transitionApproved?: string; transitionRejected?: string }; deprecated?: boolean; layout?: unknown; statusReference?: string }>; transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey?: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; ruleKey?: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>; version?: { id?: string; versionNumber?: number } }>;
}

/** The scope of the workflow. */
export interface WorkflowPreviewScope {
    /** Project ID details. */
    project?: { id?: string };
    /** The scope of the workflow. `GLOBAL` for company-managed projects and `PROJECT` for team-managed projects. */
    type?: "PROJECT" | "GLOBAL";
}

/** Details about a workflow status in preview context. */
export interface WorkflowPreviewStatus {
    /** Approval configuration. */
    approvalConfiguration?: { active?: string; transitionApproved?: string; transitionRejected?: string };
    /** Whether the status is deprecated. */
    deprecated?: boolean;
    /** Layout coordinates for workflow elements. */
    layout?: { x?: number; y?: number };
    /** The reference of the status. */
    statusReference?: string;
}

/** Project ID details. */
export interface WorkflowProjectIdScope {
    /** The ID of the project. */
    id?: string;
}

/** The issue type. */
export interface WorkflowProjectIssueTypeUsage {
    /** The ID of the issue type. */
    id?: string;
}

/** Issue types associated with the workflow for a project. */
export interface WorkflowProjectIssueTypeUsageDTO {
    /** A page of issue types. */
    issueTypes?: { nextPageToken?: string; values?: Array<{ id?: string }> };
    /** The ID of the project. */
    projectId?: string;
    /** The ID of the workflow. */
    workflowId?: string;
}

/** A page of issue types. */
export interface WorkflowProjectIssueTypeUsagePage {
    /** Token for the next page of issue type usages. */
    nextPageToken?: string;
    /** The list of issue types. */
    values?: Array<{ id?: string }>;
}

/** Projects using the workflow. */
export interface WorkflowProjectUsageDTO {
    /** A page of projects. */
    projects?: { nextPageToken?: string; values?: Array<{ id?: string }> };
    /** The workflow ID. */
    workflowId?: string;
}

export interface WorkflowReadRequest {
    /** The list of projects and issue types to query. */
    projectAndIssueTypes?: Array<{ issueTypeId: string; projectId: string }>;
    /** The list of workflow IDs to query. */
    workflowIds?: Array<string>;
    /** The list of workflow names to query. */
    workflowNames?: Array<string>;
}

/** Details of workflows and related statuses. */
export interface WorkflowReadResponse {
    /** List of statuses. */
    statuses?: Array<{ description?: string; id?: string; name?: string; scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" }; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE"; statusReference?: string }>;
    /** List of workflows. */
    workflows?: Array<{ created?: string; description?: string; id?: string; isEditable?: boolean; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; scope?: unknown; startPointLayout?: unknown; statuses?: Array<{ approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string }; deprecated?: boolean; layout?: { x?: number; y?: number }; properties?: Record<string, string>; statusReference?: string }>; taskId?: string; transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; properties?: Record<string, string>; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>; updated?: string; version?: { id?: string; versionNumber?: number } }>;
}

/** The statuses referenced in the workflow. */
export interface WorkflowReferenceStatus {
    /** The approval configuration of a status within a workflow. Applies only to Jira Service Management approvals. */
    approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string };
    /** Indicates if the status is deprecated. */
    deprecated?: boolean;
    /** The x and y location of the status in the workflow. */
    layout?: { x?: number; y?: number };
    /** The properties associated with the status. */
    properties?: Record<string, string>;
    /** The reference of the status. */
    statusReference?: string;
}

/** The configuration of the rule. */
export interface WorkflowRuleConfiguration {
    /** The ID of the rule. */
    id?: string;
    /** The parameters related to the rule. */
    parameters?: Record<string, string>;
    /** The rule key of the rule. */
    ruleKey: string;
}

/** A collection of transition rules. */
export interface WorkflowRules {
    /** The workflow transition rule conditions tree. */
    conditionsTree?: { configuration?: Record<string, unknown>; nodeType: string; type: string } | { conditions: Array<unknown>; nodeType: string; operator: "AND" | "OR" };
    /** The workflow post functions. */
    postFunctions?: Array<{ configuration?: Record<string, unknown>; type: string }>;
    /** The workflow validators. */
    validators?: Array<unknown>;
}

/** Details of the workflow and its transition rules. */
export interface WorkflowRulesSearch {
    /** Use expand to include additional information in the response. This parameter accepts `transition` which, for each rule, returns information about the transition the rule is assigned to. */
    expand?: string;
    /** The list of workflow rule IDs. */
    ruleIds: Array<string>;
    /** The workflow ID. */
    workflowEntityId: string;
}

/** Details of workflow transition rules. */
export interface WorkflowRulesSearchDetails {
    /** List of workflow rule IDs that do not belong to the workflow or can not be found. */
    invalidRules?: Array<string>;
    /** List of valid workflow transition rules. */
    validRules?: Array<{ conditions?: Array<{ configuration: { disabled?: boolean; tag?: string; value: string }; id: string; readonly key: string; readonly transition?: { id: number; name: string } }>; postFunctions?: Array<unknown>; validators?: Array<unknown>; workflowId: { draft?: boolean; name: string } }>;
    /** The workflow ID. */
    workflowEntityId?: string;
}

/** Details about a workflow scheme. */
export interface WorkflowScheme {
    /** The name of the default workflow for the workflow scheme. The default workflow has *All Unassigned Issue Types* assigned to it in Jira. If `defaultWorkflow` is not specified when creating a workflow scheme, it is set to *Jira Workflow (jira)*. */
    defaultWorkflow?: string;
    /** The description of the workflow scheme. */
    description?: string;
    /** Whether the workflow scheme is a draft or not. */
    readonly draft?: boolean;
    /** The ID of the workflow scheme. */
    readonly id?: number;
    /** The issue type to workflow mappings, where each mapping is an issue type ID and workflow name pair. Note that an issue type can only be mapped to one workflow in a workflow scheme. */
    issueTypeMappings?: Record<string, string>;
    /** The issue types available in Jira. */
    readonly issueTypes?: Record<string, { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>;
    /** The date-time that the draft workflow scheme was last modified. A modification is a change to the issue type-project mappings only. This property does not apply to non-draft workflows. */
    readonly lastModified?: string;
    /** The user that last modified the draft workflow scheme. A modification is a change to the issue type-project mappings only. This property does not apply to non-draft workflows. */
    readonly lastModifiedUser?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string };
    /** The name of the workflow scheme. The name must be unique. The maximum length is 255 characters. Required when creating a workflow scheme. */
    name?: string;
    /** For draft workflow schemes, this property is the name of the default workflow for the original workflow scheme. The default workflow has *All Unassigned Issue Types* assigned to it in Jira. */
    readonly originalDefaultWorkflow?: string;
    /** For draft workflow schemes, this property is the issue type to workflow mappings for the original workflow scheme, where each mapping is an issue type ID and workflow name pair. Note that an issue type can only be mapped to one workflow in a workflow scheme. */
    readonly originalIssueTypeMappings?: Record<string, string>;
    readonly self?: string;
    /**
     * Whether to create or update a draft workflow scheme when updating an active workflow scheme. An active workflow scheme is a workflow scheme that is used by at least one project. The following examples show how this property works:
     *
     *  *  Update an active workflow scheme with `updateDraftIfNeeded` set to `true`: If a draft workflow scheme exists, it is updated. Otherwise, a draft workflow scheme is created.
     *  *  Update an active workflow scheme with `updateDraftIfNeeded` set to `false`: An error is returned, as active workflow schemes cannot be updated.
     *  *  Update an inactive workflow scheme with `updateDraftIfNeeded` set to `true`: The workflow scheme is updated, as inactive workflow schemes do not require drafts to update.
     *
     * Defaults to `false`.
     */
    updateDraftIfNeeded?: boolean;

/** The explicit association between issue types and a workflow in a workflow scheme. */
export interface WorkflowSchemeAssociation {
    /** The issue types assigned to the workflow. */
    issueTypeIds: Array<string>;
    /** The ID of the workflow. */
    workflowId: string;
}

/** A workflow scheme along with a list of projects that use it. */
export interface WorkflowSchemeAssociations {
    /** The list of projects that use the workflow scheme. */
    projectIds: Array<string>;
    /** The workflow scheme. */
    workflowScheme: { defaultWorkflow?: string; description?: string; readonly draft?: boolean; readonly id?: number; issueTypeMappings?: Record<string, string>; readonly issueTypes?: Record<string, { readonly avatarId?: number; readonly description?: string; readonly entityId?: string; readonly hierarchyLevel?: number; readonly iconUrl?: string; readonly id?: string; readonly name?: string; readonly scope?: Record<string, unknown>; readonly self?: string; readonly subtask?: boolean }>; readonly lastModified?: string; readonly lastModifiedUser?: { accountId?: string; readonly accountType?: "atlassian" | "app" | "customer" | "unknown"; readonly active?: boolean; readonly appType?: string; readonly applicationRoles?: { callback?: Record<string, unknown>; items?: Array<{ defaultGroups?: Array<string>; defaultGroupsDetails?: Array<{ groupId?: string; name?: string; readonly self?: string }>; defined?: boolean; groupDetails?: Array<unknown>; groups?: Array<string>; hasUnlimitedSeats?: boolean; key?: string; name?: string; numberOfSeats?: number; platform?: boolean; remainingSeats?: number; selectedByDefault?: boolean; userCount?: number; userCountDescription?: string }>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly avatarUrls?: { 16x16?: string; 24x24?: string; 32x32?: string; 48x48?: string }; readonly displayName?: string; readonly emailAddress?: string; readonly expand?: string; readonly groups?: { callback?: Record<string, unknown>; items?: Array<unknown>; max-results?: number; pagingCallback?: unknown; size?: number }; readonly guest?: boolean; key?: string; readonly locale?: string; name?: string; readonly self?: string; readonly timeZone?: string }; name?: string; readonly originalDefaultWorkflow?: string; readonly originalIssueTypeMappings?: Record<string, string>; readonly self?: string; updateDraftIfNeeded?: boolean };

/** The ID and the name of the workflow scheme. */
export interface WorkflowSchemeIdName {
    /** The ID of the workflow scheme. */
    id: string;
    /** The name of the workflow scheme. */
    name: string;
}

/** The payload for creating a workflow scheme. See https://www.atlassian.com/software/jira/guides/workflows/overview\#what-is-a-jira-workflow-scheme */
export interface WorkflowSchemePayload {
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    defaultWorkflow?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The description of the workflow scheme */
    description?: string;
    /** Association between issuetypes and workflows */
    explicitMappings?: Record<string, unknown>;
    /** The name of the workflow scheme */
    name?: string;
    /** The strategy to use if there is a conflict with another workflow scheme */
    onConflict?: "FAIL" | "USE" | "NEW";
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: unknown;
}

/** An associated workflow scheme and project. */
export interface WorkflowSchemeProjectAssociation {
    /** The ID of the project. */
    projectId: string;
    /** The ID of the workflow scheme. If the workflow scheme ID is `null`, the operation assigns the default workflow scheme. */
    workflowSchemeId?: string;
}

/** Request to switch a project's workflow scheme */
export type WorkflowSchemeProjectSwitchBean = Record<string, unknown>;

/** Projects using the workflow scheme. */
export interface WorkflowSchemeProjectUsageDTO {
    /** A page of projects. */
    projects?: { nextPageToken?: string; values?: Array<{ id?: string }> };
    /** The workflow scheme ID. */
    workflowSchemeId?: string;
}

/** The workflow scheme read request body. */
export interface WorkflowSchemeReadRequest {
    /** The list of project IDs to query. */
    projectIds?: Array<string>;
    /** The list of workflow scheme IDs to query. */
    workflowSchemeIds?: Array<string>;
}

export interface WorkflowSchemeReadResponse {
    /** Workflow metadata and usage detail. */
    defaultWorkflow?: { description: string; id: string; name: string; version: { id?: string; versionNumber?: number } };
    /** The description of the workflow scheme. */
    description?: string;
    /** The ID of the workflow scheme. */
    id: string;
    /** The name of the workflow scheme. */
    name: string;
    /** The scope of the workflow. */
    scope: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" };
    /** Indicates if there's an [asynchronous task](#async-operations) for this workflow scheme. */
    taskId?: string;
    /** The current version details of this workflow scheme. */
    version: unknown;
    /** Mappings from workflows to issue types. */
    workflowsForIssueTypes: Array<{ issueTypeIds: Array<string>; workflow: unknown }>;
}

/** The update workflow scheme payload. */
export type WorkflowSchemeUpdateRequest = Record<string, unknown>;

/** The request payload to get the required mappings for updating a workflow scheme. */
export interface WorkflowSchemeUpdateRequiredMappingsRequest {
    /** The ID of the new default workflow for this workflow scheme. Only used in global-scoped workflow schemes. If it isn't specified, is set to *Jira Workflow (jira)*. */
    defaultWorkflowId?: string;
    /** The ID of the workflow scheme. */
    id: string;
    /** The new workflow to issue type mappings for this workflow scheme. */
    workflowsForIssueTypes: Array<{ issueTypeIds: Array<string>; workflowId: string }>;
}

export interface WorkflowSchemeUpdateRequiredMappingsResponse {
    /** The list of required status mappings by issue type. */
    statusMappingsByIssueTypes?: Array<{ issueTypeId?: string; statusIds?: Array<string> }>;
    /** The list of required status mappings by workflow. */
    statusMappingsByWorkflows?: Array<{ sourceWorkflowId?: string; statusIds?: Array<string>; targetWorkflowId?: string }>;
    /** The details of the statuses in the associated workflows. */
    statuses?: Array<{ category?: "TODO" | "IN_PROGRESS" | "DONE"; id?: string; name?: string }>;
    /** The statuses associated with each workflow. */
    statusesPerWorkflow?: Array<{ initialStatusId?: string; statuses?: Array<string>; workflowId?: string }>;
}

/** The worflow scheme. */
export interface WorkflowSchemeUsage {
    /** The workflow scheme ID. */
    id?: string;
}

/** Workflow schemes using the workflow. */
export interface WorkflowSchemeUsageDTO {
    /** The workflow ID. */
    workflowId?: string;
    /** A page of workflow schemes. */
    workflowSchemes?: { nextPageToken?: string; values?: Array<{ id?: string }> };
}

/** A page of workflow schemes. */
export interface WorkflowSchemeUsagePage {
    /** Token for the next page of issue type usages. */
    nextPageToken?: string;
    /** The list of workflow schemes. */
    values?: Array<{ id?: string }>;
}

/** The scope of the workflow. */
export interface WorkflowScope {
    /** Project ID details. */
    project?: { id: string };
    /** The scope of the workflow. `GLOBAL` for company-managed projects and `PROJECT` for team-managed projects. */
    type?: "PROJECT" | "GLOBAL";
}

/** Page of items, including workflows and related statuses. */
export interface WorkflowSearchResponse {
    /** Whether this is the last page. */
    isLast?: boolean;
    /** The maximum number of items that could be returned. */
    maxResults?: number;
    /** If there is another page of results, the URL of the next page. */
    nextPage?: string;
    /** The URL of the page. */
    self?: string;
    /** The index of the first item returned. */
    startAt?: number;
    /** List of statuses. */
    statuses?: Array<{ description?: string; id?: string; name?: string; scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" }; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE"; statusReference?: string }>;
    /** The number of items returned. */
    total?: number;
    /** List of workflows. */
    values?: Array<{ created?: string; description?: string; id?: string; isEditable?: boolean; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; scope?: unknown; startPointLayout?: unknown; statuses?: Array<{ approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string }; deprecated?: boolean; layout?: { x?: number; y?: number }; properties?: Record<string, string>; statusReference?: string }>; taskId?: string; transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; properties?: Record<string, string>; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>; updated?: string; version?: { id?: string; versionNumber?: number } }>;
}

/** A workflow transition rule condition. This object returns `nodeType` as `simple`. */
export interface WorkflowSimpleCondition {
    /** EXPERIMENTAL. The configuration of the transition rule. */
    configuration?: Record<string, unknown>;
    nodeType: string;
    /** The type of the transition rule. */
    type: string;
}

/** Details of a workflow status. */
export interface WorkflowStatus {
    /** The ID of the issue status. */
    id: string;
    /** The name of the status in the workflow. */
    name: string;
    /** Additional properties that modify the behavior of issues in this status. Supports the properties `jira.issue.editable` and `issueEditable` (deprecated) that indicate whether issues are editable. */
    properties?: Record<string, Record<string, unknown>>;
}

/** The x and y location of the status in the workflow. */
export interface WorkflowStatusLayout {
    /** The x axis location. */
    x?: number;
    /** The y axis location. */
    y?: number;
}

/** The layout of the workflow status. */
export interface WorkflowStatusLayoutPayload {
    /** The x coordinate of the status. */
    x?: number;
    /** The y coordinate of the status. */
    y?: number;
}

/** The statuses to be used in the workflow */
export interface WorkflowStatusPayload {
    /** The layout of the workflow status. */
    layout?: { x?: number; y?: number };
    /** Every project-created entity has an ID that must be unique within the scope of the project creation. PCRI (Project Create Resource Identifier) is a standard format for creating IDs and references to other project entities. PCRI format is defined as follows: pcri:\[entityType\]:\[type\]:\[entityId\] entityType - the type of an entity, e.g. status, role, workflow type - PCRI type, either `id` - The ID of an entity that already exists in the target site, or `ref` - A unique reference to an entity that is being created entityId - entity identifier, if type is `id` - must be an existing entity ID that exists in the Jira site, if `ref` - must be unique across all entities in the scope of this project template creation */
    pcri?: { anID?: boolean; areference?: boolean; entityId?: string; entityType?: string; id?: string; type?: "id" | "ref" };
    /** The properties of the workflow status. */
    properties?: Record<string, string>;
}

/** Details of the status being updated. */
export type WorkflowStatusUpdate = Record<string, unknown>;

/** A workflow transition. */
export interface WorkflowTransition {
    /** The transition ID. */
    id: number;
    /** The transition name. */
    name: string;
}

/** The statuses the transition can start from, and the mapping of ports between the statuses. */
export interface WorkflowTransitionLinks {
    /** The port that the transition starts from. */
    fromPort?: number;
    /** The status that the transition starts from. */
    fromStatusReference?: string;
    /** The port that the transition goes to. */
    toPort?: number;
}

/** Details about the server Jira is running on. */
export type WorkflowTransitionProperty = Record<string, unknown>;

/** A workflow transition rule. */
export interface WorkflowTransitionRule {
    /** EXPERIMENTAL. The configuration of the transition rule. */
    configuration?: Record<string, unknown>;
    /** The type of the transition rule. */
    type: string;
}

/** A workflow with transition rules. */
export interface WorkflowTransitionRules {
    /** The list of conditions within the workflow. */
    conditions?: Array<{ configuration: { disabled?: boolean; tag?: string; value: string }; id: string; readonly key: string; readonly transition?: { id: number; name: string } }>;
    /** The list of post functions within the workflow. */
    postFunctions?: Array<unknown>;
    /** The list of validators within the workflow. */
    validators?: Array<unknown>;
    /** Properties that identify a workflow. */
    workflowId: { draft?: boolean; name: string };
}

/** Details about a workflow configuration update request. */
export interface WorkflowTransitionRulesDetails {
    /** Properties that identify a workflow. */
    workflowId: { draft?: boolean; name: string };
    /** The list of connect workflow rule IDs. */
    workflowRuleIds: Array<string>;
}

/** Details about a workflow configuration update request. */
export interface WorkflowTransitionRulesUpdate {
    /** The list of workflows with transition rules to update. */
    workflows: Array<{ conditions?: Array<{ configuration: { disabled?: boolean; tag?: string; value: string }; id: string; readonly key: string; readonly transition?: { id: number; name: string } }>; postFunctions?: Array<unknown>; validators?: Array<unknown>; workflowId: { draft?: boolean; name: string } }>;
}

/** Details of any errors encountered while updating workflow transition rules for a workflow. */
export interface WorkflowTransitionRulesUpdateErrorDetails {
    /** A list of transition rule update errors, indexed by the transition rule ID. Any transition rule that appears here wasn't updated. */
    ruleUpdateErrors: Record<string, Array<string>>;
    /** The list of errors that specify why the workflow update failed. The workflow was not updated if the list contains any entries. */
    updateErrors: Array<string>;
    /** Properties that identify a workflow. */
    workflowId: { draft?: boolean; name: string };
}

/** Details of any errors encountered while updating workflow transition rules. */
export interface WorkflowTransitionRulesUpdateErrors {
    /** A list of workflows. */
    updateResults: Array<{ ruleUpdateErrors: Record<string, Array<string>>; updateErrors: Array<string>; workflowId: { draft?: boolean; name: string } }>;
}

/** The transitions of the workflow. */
export interface WorkflowTransitions {
    /** The post-functions of the transition. */
    actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>;
    /** The conditions group associated with the transition. */
    conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" };
    /** The custom event ID of the transition. */
    customIssueEventId?: string;
    /** The description of the transition. */
    description?: string;
    /** The ID of the transition. */
    id?: string;
    /** The statuses the transition can start from, and the mapping of ports between the statuses. */
    links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>;
    /** The name of the transition. */
    name?: string;
    /** The properties of the transition. */
    properties?: Record<string, string>;
    /** The status the transition goes to. */
    toStatusReference?: string;
    /** The configuration of the rule. */
    transitionScreen?: unknown;
    /** The triggers of the transition. */
    triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>;
    /** The transition type. */
    type?: "INITIAL" | "GLOBAL" | "DIRECTED";
    /** The validators of the transition. */
    validators?: Array<unknown>;
}

/** The trigger configuration associated with a workflow. */
export interface WorkflowTrigger {
    /** The ID of the trigger. */
    id?: string;
    /** The parameters of the trigger. */
    parameters: Record<string, string>;
    /** The rule key of the trigger. */
    ruleKey: string;
}

/** The details of the workflows to update. */
export type WorkflowUpdate = Record<string, unknown>;

/** The update workflows payload. */
export interface WorkflowUpdateRequest {
    /** The statuses to associate with the workflows. */
    statuses?: Array<Record<string, unknown>>;
    /** The details of the workflows to update. */
    workflows?: Array<Record<string, unknown>>;
}

export interface WorkflowUpdateResponse {
    /** List of updated statuses. */
    statuses?: Array<{ description?: string; id?: string; name?: string; scope?: { project?: { id: string }; type?: "PROJECT" | "GLOBAL" }; statusCategory?: "TODO" | "IN_PROGRESS" | "DONE"; statusReference?: string }>;
    /** If there is a [asynchronous task](#async-operations) operation, as a result of this update. */
    taskId?: string;
    /** List of updated workflows. */
    workflows?: Array<{ created?: string; description?: string; id?: string; isEditable?: boolean; loopedTransitionContainerLayout?: { x?: number; y?: number }; name?: string; scope?: unknown; startPointLayout?: unknown; statuses?: Array<{ approvalConfiguration?: { active: "true" | "false"; conditionType: "number" | "percent" | "numberPerPrincipal"; conditionValue: string; exclude?: Array<"assignee" | "reporter">; fieldId: string; prePopulatedFieldId?: string; transitionApproved: string; transitionRejected: string }; deprecated?: boolean; layout?: { x?: number; y?: number }; properties?: Record<string, string>; statusReference?: string }>; taskId?: string; transitions?: Array<{ actions?: Array<{ id?: string; parameters?: Record<string, string>; ruleKey: string }>; conditions?: { conditionGroups?: Array<unknown>; conditions?: Array<unknown>; operation?: "ANY" | "ALL" }; customIssueEventId?: string; description?: string; id?: string; links?: Array<{ fromPort?: number; fromStatusReference?: string; toPort?: number }>; name?: string; properties?: Record<string, string>; toStatusReference?: string; transitionScreen?: unknown; triggers?: Array<{ id?: string; parameters: Record<string, string>; ruleKey: string }>; type?: "INITIAL" | "GLOBAL" | "DIRECTED"; validators?: Array<unknown> }>; updated?: string; version?: { id?: string; versionNumber?: number } }>;
}

export interface WorkflowUpdateValidateRequestBean {
    /** The update workflows payload. */
    payload: { statuses?: Array<Record<string, unknown>>; workflows?: Array<Record<string, unknown>> };
    /** The level of validation to return from the API. If no values are provided, the default would return `WARNING` and `ERROR` level validation results. */
    validationOptions?: { levels?: Array<"WARNING" | "ERROR"> };
}

/** The details about a workflow validation error. */
export interface WorkflowValidationError {
    /** An error code. */
    code?: string;
    /** A reference to the location of the error. This will be null if the error does not refer to a specific element. */
    elementReference?: { propertyKey?: string; ruleId?: string; statusMappingReference?: { issueTypeId: string; projectId: string }; statusReference?: string; transitionId?: string };
    /** The validation error level. */
    level?: "WARNING" | "ERROR";
    /** An error message. */
    message?: string;
    /** The type of element the error or warning references. */
    type?: "RULE" | "STATUS" | "STATUS_LAYOUT" | "STATUS_PROPERTY" | "WORKFLOW" | "TRANSITION" | "TRANSITION_PROPERTY" | "SCOPE" | "STATUS_MAPPING" | "TRIGGER";
}

export interface WorkflowValidationErrorList {
    /** The list of validation errors. */
    errors?: Array<{ code?: string; elementReference?: { propertyKey?: string; ruleId?: string; statusMappingReference?: { issueTypeId: string; projectId: string }; statusReference?: string; transitionId?: string }; level?: "WARNING" | "ERROR"; message?: string; type?: "RULE" | "STATUS" | "STATUS_LAYOUT" | "STATUS_PROPERTY" | "WORKFLOW" | "TRANSITION" | "TRANSITION_PROPERTY" | "SCOPE" | "STATUS_MAPPING" | "TRIGGER" }>;
}

/** Details of workflows and their transition rules to delete. */
export interface WorkflowsWithTransitionRulesDetails {
    /** The list of workflows with transition rules to delete. */
    workflows: Array<{ workflowId: { draft?: boolean; name: string }; workflowRuleIds: Array<string> }>;
}

/** Working days configuration */
export interface WorkingDaysConfig {
    friday?: boolean;
    id?: number;
    monday?: boolean;
    nonWorkingDays?: Array<{ id?: number; iso8601Date?: string }>;
    saturday?: boolean;
    sunday?: boolean;
    thursday?: boolean;
    timezoneId?: string;
    tuesday?: boolean;
    wednesday?: boolean;
}

/** Details of a worklog. */
export type Worklog = Record<string, unknown>;

export interface WorklogCompositeKey {
    /** The issue ID. */
    issueId?: number;
    /** The worklog ID. */
    worklogId?: number;
}

export interface WorklogIdsRequestBean {
    /** A list of worklog IDs. */
    ids: Array<number>;
}

export interface WorklogKeyResult {
    /** The issue ID. */
    issueId?: number;
    /** The worklog ID. */
    worklogId?: number;
}

export interface WorklogsMoveRequestBean {
    /** A list of worklog IDs. */
    ids?: Array<number>;
    /** The issue id or key of the destination issue */
    issueIdOrKey?: string;
}

/** Details about data policy. */
export interface WorkspaceDataPolicy {
    /** Whether the workspace contains any content inaccessible to the requesting application. */
    readonly anyContentBlocked?: boolean;
}

/** Can contain multiple field values of following types depending on `type` key */
export type fields = { retain?: boolean; type?: "adf" | "raw"; value: Array<string> } | { retain?: boolean; type: "adf" | "raw"; value: Record<string, unknown> };

/** Classification mapping for classifications in source issues to respective target classification. */
export interface targetClassification {
    /** An object with the key as the ID of the target classification and value with the list of the IDs of the current source classifications. */
    classifications: Record<string, Array<string>>;
    /** ID of the source issueType to which issues present in `issueIdOrKeys` belongs. */
    issueType?: string;
    /** ID or key of the source project to which issues present in `issueIdOrKeys` belongs. */
    projectKeyOrId?: string;
}

/** Field mapping for mandatory fields in target */
export interface targetMandatoryFields {
    /** Contains the value of mandatory fields */
    fields: Record<string, { retain?: boolean; type?: "adf" | "raw"; value: Array<string> } | { retain?: boolean; type: "adf" | "raw"; value: Record<string, unknown> }>;
}

/** Status mapping for statuses in source workflow to respective target status in target workflow. */
export interface targetStatus {
    /** An object with the key as the ID of the target status and value with the list of the IDs of the current source statuses. */
    statuses: Record<string, Array<string>>;
}

/** An object representing the mapping of issues and data related to destination entities, like fields and statuses, that are required during a bulk move. */
export interface targetToSourcesMapping {
    /**
     * If `true`, when issues are moved into this target group, they will adopt the target project's default classification, if they don't have a classification already. If they do have a classification, it will be kept the same even after the move. Leave `targetClassification` empty when using this.
     *
     * If `false`, you must provide a `targetClassification` mapping for each classification associated with the selected issues.
     *
     * [Benefit from data classification](https://support.atlassian.com/security-and-access-policies/docs/what-is-data-classification/)
     */
    inferClassificationDefaults: boolean;
    /**
     * If `true`, values from the source issues will be retained for the mandatory fields in the field configuration of the destination project. The `targetMandatoryFields` property shouldn't be defined.
     *
     * If `false`, the user is required to set values for mandatory fields present in the field configuration of the destination project. Provide input by defining the `targetMandatoryFields` property
     */
    inferFieldDefaults: boolean;
    /**
     * If `true`, the statuses of issues being moved in this target group that are not present in the target workflow will be changed to the default status of the target workflow (see below). Leave `targetStatus` empty when using this.
     *
     * If `false`, you must provide a `targetStatus` for each status not present in the target workflow.
     *
     * The default status in a workflow is referred to as the "initial status". Each workflow has its own unique initial status. When an issue is created, it is automatically assigned to this initial status. Read more about configuring initial statuses: [Configure the initial status | Atlassian Support.](https://support.atlassian.com/jira-cloud-administration/docs/configure-the-initial-status/)
     */
    inferStatusDefaults: boolean;
    /**
     * When an issue is moved, its subtasks (if there are any) need to be moved with it. `inferSubtaskTypeDefault` helps with moving the subtasks by picking a random subtask type in the target project.
     *
     * If `true`, subtasks will automatically move to the same project as their parent.
     *
     * When they move:
     *
     *  *  Their `issueType` will be set to the default for subtasks in the target project.
     *  *  Values for mandatory fields will be retained from the source issues
     *  *  Specifying separate mapping for implicit subtasks won’t be allowed.
     *
     * If `false`, you must manually move the subtasks. They will retain the parent which they had in the current project after being moved.
     */
    inferSubtaskTypeDefault: boolean;
    /** List of issue IDs or keys to be moved. */
    issueIdsOrKeys?: Array<string>;
    /**
     * List of the objects containing classifications in the source issues and their new values which need to be set during the bulk move operation.
     *
     * It is mandatory to provide source classification to target classification mapping when the source classification is invalid for the target project and issue type.
     *
     *  *  **You should only define this property when `inferClassificationDefaults` is `false`.**
     *  *  **In order to provide mapping for issues which don't have a classification, use `"-1"`.**
     */
    targetClassification?: Array<{ classifications: Record<string, Array<string>>; issueType?: string; projectKeyOrId?: string }>;
    /**
     * List of objects containing mandatory fields in the target field configuration and new values that need to be set during the bulk move operation.
     *
     * The new values will only be applied if the field is mandatory in the target project and at least one issue from the source has that field empty, or if the field context is different in the target project (e.g. project-scoped version fields).
     *
     * **You should only define this property when `inferFieldDefaults` is `false`.**
     */
    targetMandatoryFields?: Array<{ fields: Record<string, { retain?: boolean; type?: "adf" | "raw"; value: Array<string> } | { retain?: boolean; type: "adf" | "raw"; value: Record<string, unknown> }> }>;
    /**
     * List of the objects containing statuses in the source workflow and their new values which need to be set during the bulk move operation.
     *
     * The new values will only be applied if the source status is invalid for the target project and issue type.
     *
     * It is mandatory to provide source status to target status mapping when the source status is invalid for the target project and issue type.
     *
     * **You should only define this property when `inferStatusDefaults` is `false`.**
     */
    targetStatus?: Array<{ statuses: Record<string, Array<string>> }>;
}

export interface ErrorResponse {
    errorMessages?: Array<string>;
    errors?: Record<string, string>;
    status?: number;
    message?: string;
}

export interface RateLimitError {
    message?: string;
    retryAfter?: number;
}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}

}
