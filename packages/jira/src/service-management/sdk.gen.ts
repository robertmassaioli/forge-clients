// @ts-nocheck
import { ForgeApiError } from "@forge-clients/core";
import type { BoundClient } from "@forge-clients/core";
import type * as Types from "./types.gen.js";

// Generated by @forge-clients/generator — DO NOT EDIT
// Source: Jira Service Management Cloud REST API
/* eslint-disable */
export * from './types.gen.js';

export interface GetAssetsWorkspacesParams {
    start?: number;
    limit?: number;
}

/**
 * Get assets workspaces
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAssetsWorkspaces(client: BoundClient, params: GetAssetsWorkspacesParams): Promise<Types.PagedDTOAssetsWorkspaceDTO> {
    const path = '/rest/servicedeskapi/assets/workspace';
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOAssetsWorkspaceDTO>;
}

export interface CreateCustomerParams {
    strictConflictStatusCode?: boolean;
    body?: Types.CustomerCreateDTO;
}

/**
 * Create customer
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCustomer(client: BoundClient, params: CreateCustomerParams): Promise<Types.UserDTO> {
    const path = '/rest/servicedeskapi/customer';
    const queryParams = {
      strictConflictStatusCode: params.strictConflictStatusCode,
    };
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UserDTO>;
}

export interface RevokePortalOnlyAccessForUserParams {
    path: { accountId: string };
}

/**
 * Revoke portal only access for user
 * @forge-scopes-asApp read:jira-user
 * @forge-scopes-asUser read:jira-user
 */
export async function revokePortalOnlyAccessForUser(client: BoundClient, params: RevokePortalOnlyAccessForUserParams): Promise<Record<string, unknown>> {
    const path = `/rest/servicedeskapi/customer/user/${params.path.accountId}/revoke-portal-only-access`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

/**
 * Get info
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInfo(client: BoundClient): Promise<Types.SoftwareInfoDTO> {
    const path = '/rest/servicedeskapi/info';
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SoftwareInfoDTO>;
}

export interface GetInsightWorkspacesParams {
    start?: number;
    limit?: number;
}

/**
 * Get insight workspaces
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getInsightWorkspaces(client: BoundClient, params: GetInsightWorkspacesParams): Promise<Types.PagedDTOInsightWorkspaceDTO> {
    const path = '/rest/servicedeskapi/insight/workspace';
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOInsightWorkspaceDTO>;
}

export interface GetArticlesGlobalParams {
    query: string;
    highlight: boolean;
    start?: number;
    limit?: number;
    cursor?: string;
    prev?: boolean;
}

/**
 * Get articles
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getArticlesGlobal(client: BoundClient, params: GetArticlesGlobalParams): Promise<Types.PagedDTOArticleDTO> {
    const path = '/rest/servicedeskapi/knowledgebase/article';
    const queryParams = {
      query: params.query,
      highlight: params.highlight,
      start: params.start,
      limit: params.limit,
      cursor: params.cursor,
      prev: params.prev,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOArticleDTO>;
}

export interface GetOrganizationsGlobalParams {
    start?: number;
    limit?: number;
    accountId?: string;
}

/**
 * Get organizations
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getOrganizationsGlobal(client: BoundClient, params: GetOrganizationsGlobalParams): Promise<Types.PagedDTOOrganizationDTO> {
    const path = '/rest/servicedeskapi/organization';
    const queryParams = {
      start: params.start,
      limit: params.limit,
      accountId: params.accountId,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOOrganizationDTO>;
}

export interface CreateOrganizationParams {
    body?: Types.OrganizationCreateDTO;
}

/**
 * Create organization
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createOrganization(client: BoundClient, params: CreateOrganizationParams): Promise<Types.OrganizationDTO> {
    const path = '/rest/servicedeskapi/organization';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.OrganizationDTO>;
}

export interface GetOrganizationParams {
    path: { organizationId: string };
}

/**
 * Get organization
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getOrganization(client: BoundClient, params: GetOrganizationParams): Promise<Types.OrganizationDTO> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.OrganizationDTO>;
}

export interface DeleteOrganizationParams {
    path: { organizationId: string };
}

/**
 * Delete organization
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteOrganization(client: BoundClient, params: DeleteOrganizationParams): Promise<void> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPropertiesKeysForOrgParams {
    path: { organizationId: string };
}

/**
 * Get properties keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPropertiesKeysForOrg(client: BoundClient, params: GetPropertiesKeysForOrgParams): Promise<Types.PropertyKeys> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}/property`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetPropertyForOrgParams {
    path: { organizationId: string; propertyKey: string };
}

/**
 * Get property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPropertyForOrg(client: BoundClient, params: GetPropertyForOrgParams): Promise<Types.EntityProperty> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}/property/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetPropertyForOrgParams {
    path: { organizationId: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setPropertyForOrg(client: BoundClient, params: SetPropertyForOrgParams): Promise<Record<string, unknown>> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}/property/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeletePropertyForOrgParams {
    path: { organizationId: string; propertyKey: string };
}

/**
 * Delete property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePropertyForOrg(client: BoundClient, params: DeletePropertyForOrgParams): Promise<void> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}/property/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetUsersInOrganizationParams {
    path: { organizationId: string };
    start?: number;
    limit?: number;
}

/**
 * Get users in organization
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUsersInOrganization(client: BoundClient, params: GetUsersInOrganizationParams): Promise<Types.PagedDTOUserDTO> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}/user`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOUserDTO>;
}

export interface AddUsersToOrganizationParams {
    path: { organizationId: string };
    body?: Types.UsersOrganizationUpdateDTO;
}

/**
 * Add users to organization
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function addUsersToOrganization(client: BoundClient, params: AddUsersToOrganizationParams): Promise<void> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}/user`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveUsersFromOrganizationParams {
    path: { organizationId: string };
    body?: Types.UsersOrganizationUpdateDTO;
}

/**
 * Remove users from organization
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function removeUsersFromOrganization(client: BoundClient, params: RemoveUsersFromOrganizationParams): Promise<void> {
    const path = `/rest/servicedeskapi/organization/${params.path.organizationId}/user`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetCustomerRequestsParams {
    searchTerm?: string;
    requestOwnership?: Array<string>;
    requestStatus?: string;
    approvalStatus?: string;
    organizationId?: number;
    serviceDeskId?: number;
    requestTypeId?: number;
    expand?: Array<string>;
    start?: number;
    limit?: number;
}

/**
 * Get customer requests
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomerRequests(client: BoundClient, params: GetCustomerRequestsParams): Promise<Types.PagedDTOCustomerRequestDTO> {
    const path = '/rest/servicedeskapi/request';
    const queryParams = {
      searchTerm: params.searchTerm,
      requestOwnership: params.requestOwnership,
      requestStatus: params.requestStatus,
      approvalStatus: params.approvalStatus,
      organizationId: params.organizationId,
      serviceDeskId: params.serviceDeskId,
      requestTypeId: params.requestTypeId,
      expand: params.expand,
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOCustomerRequestDTO>;
}

export interface CreateCustomerRequestParams {
    body?: Types.RequestCreateDTO;
}

/**
 * Create customer request
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCustomerRequest(client: BoundClient, params: CreateCustomerRequestParams): Promise<Types.CustomerRequestDTO> {
    const path = '/rest/servicedeskapi/request';
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomerRequestDTO>;
}

export interface GetCustomerRequestByIdOrKeyParams {
    path: { issueIdOrKey: string };
    expand?: Array<string>;
}

/**
 * Get customer request by id or key
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomerRequestByIdOrKey(client: BoundClient, params: GetCustomerRequestByIdOrKeyParams): Promise<Types.CustomerRequestDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomerRequestDTO>;
}

export interface GetApprovalsParams {
    path: { issueIdOrKey: string };
    start?: number;
    limit?: number;
}

/**
 * Get approvals
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getApprovals(client: BoundClient, params: GetApprovalsParams): Promise<Types.PagedDTOApprovalDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/approval`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOApprovalDTO>;
}

export interface GetApprovalByIdParams {
    path: { issueIdOrKey: string; approvalId: string };
}

/**
 * Get approval by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getApprovalById(client: BoundClient, params: GetApprovalByIdParams): Promise<Types.ApprovalDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/approval/${params.path.approvalId}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ApprovalDTO>;
}

export interface AnswerApprovalParams {
    path: { issueIdOrKey: string; approvalId: string };
    body?: Types.ApprovalDecisionRequestDTO;
}

/**
 * Answer approval
 * @forge-scopes-asApp read:jira-work
 */
export async function answerApproval(client: BoundClient, params: AnswerApprovalParams): Promise<Types.ApprovalDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/approval/${params.path.approvalId}`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ApprovalDTO>;
}

export interface GetAttachmentsForRequestParams {
    path: { issueIdOrKey: string };
    start: number;
    limit: number;
}

/**
 * Get attachments for request
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentsForRequest(client: BoundClient, params: GetAttachmentsForRequestParams): Promise<Types.PagedDTOAttachmentDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/attachment`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOAttachmentDTO>;
}

export interface CreateCommentWithAttachmentParams {
    path: { issueIdOrKey: string };
    body?: Types.AttachmentCreateDTO;
}

/**
 * Create comment with attachment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCommentWithAttachment(client: BoundClient, params: CreateCommentWithAttachmentParams): Promise<Types.AttachmentCreateResultDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/attachment`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AttachmentCreateResultDTO>;
}

export interface GetAttachmentContentParams {
    path: { issueIdOrKey: string; attachmentId: string };
}

/**
 * Get attachment content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentContent(client: BoundClient, params: GetAttachmentContentParams): Promise<Record<string, unknown>> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/attachment/${params.path.attachmentId}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetAttachmentThumbnailParams {
    path: { issueIdOrKey: string; attachmentId: string };
}

/**
 * Get attachment thumbnail
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentThumbnail(client: BoundClient, params: GetAttachmentThumbnailParams): Promise<Record<string, unknown>> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/attachment/${params.path.attachmentId}/thumbnail`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetRequestCommentsParams {
    path: { issueIdOrKey: string };
    public?: boolean;
    internal?: boolean;
    expand?: Array<string>;
    start?: number;
    limit?: number;
}

/**
 * Get request comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequestComments(client: BoundClient, params: GetRequestCommentsParams): Promise<Types.PagedDTOCommentDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/comment`;
    const queryParams = {
      public: params.public,
      internal: params.internal,
      expand: params.expand,
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOCommentDTO>;
}

export interface CreateRequestCommentParams {
    path: { issueIdOrKey: string };
    body?: Types.CommentCreateDTO;
}

/**
 * Create request comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createRequestComment(client: BoundClient, params: CreateRequestCommentParams): Promise<Types.CommentDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/comment`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CommentDTO>;
}

export interface GetRequestCommentByIdParams {
    path: { issueIdOrKey: string; commentId: string };
    expand?: Array<string>;
}

/**
 * Get request comment by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequestCommentById(client: BoundClient, params: GetRequestCommentByIdParams): Promise<Types.CommentDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/comment/${params.path.commentId}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CommentDTO>;
}

export interface GetCommentAttachmentsParams {
    path: { issueIdOrKey: string; commentId: string };
    start?: number;
    limit?: number;
}

/**
 * Get comment attachments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCommentAttachments(client: BoundClient, params: GetCommentAttachmentsParams): Promise<Types.PagedDTOAttachmentDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/comment/${params.path.commentId}/attachment`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOAttachmentDTO>;
}

export interface GetSubscriptionStatusParams {
    path: { issueIdOrKey: string };
}

/**
 * Get subscription status
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSubscriptionStatus(client: BoundClient, params: GetSubscriptionStatusParams): Promise<Types.RequestNotificationSubscriptionDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/notification`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RequestNotificationSubscriptionDTO>;
}

export interface SubscribeParams {
    path: { issueIdOrKey: string };
}

/**
 * Subscribe
 * @forge-scopes-asApp read:jira-work
 */
export async function subscribe(client: BoundClient, params: SubscribeParams): Promise<void> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/notification`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface UnsubscribeParams {
    path: { issueIdOrKey: string };
}

/**
 * Unsubscribe
 * @forge-scopes-asApp read:jira-work
 */
export async function unsubscribe(client: BoundClient, params: UnsubscribeParams): Promise<void> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/notification`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetRequestParticipantsParams {
    path: { issueIdOrKey: string };
    start?: number;
    limit?: number;
}

/**
 * Get request participants
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequestParticipants(client: BoundClient, params: GetRequestParticipantsParams): Promise<Types.PagedDTOUserDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/participant`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOUserDTO>;
}

export interface AddRequestParticipantsParams {
    path: { issueIdOrKey: string };
    body?: Types.RequestParticipantUpdateDTO;
}

/**
 * Add request participants
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addRequestParticipants(client: BoundClient, params: AddRequestParticipantsParams): Promise<Types.PagedDTOUserDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/participant`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOUserDTO>;
}

export interface RemoveRequestParticipantsParams {
    path: { issueIdOrKey: string };
    body?: Types.RequestParticipantUpdateDTO;
}

/**
 * Remove request participants
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeRequestParticipants(client: BoundClient, params: RemoveRequestParticipantsParams): Promise<Types.PagedDTOUserDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/participant`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOUserDTO>;
}

export interface GetSlaInformationParams {
    path: { issueIdOrKey: string };
    start?: number;
    limit?: number;
}

/**
 * Get sla information
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSlaInformation(client: BoundClient, params: GetSlaInformationParams): Promise<Types.PagedDTOSlaInformationDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/sla`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOSlaInformationDTO>;
}

export interface GetSlaInformationByIdParams {
    path: { issueIdOrKey: string; slaMetricId: string };
}

/**
 * Get sla information by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSlaInformationById(client: BoundClient, params: GetSlaInformationByIdParams): Promise<Types.SlaInformationDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/sla/${params.path.slaMetricId}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SlaInformationDTO>;
}

export interface GetCustomerRequestStatusParams {
    path: { issueIdOrKey: string };
    start?: number;
    limit?: number;
}

/**
 * Get customer request status
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomerRequestStatus(client: BoundClient, params: GetCustomerRequestStatusParams): Promise<Types.PagedDTOCustomerRequestStatusDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/status`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOCustomerRequestStatusDTO>;
}

export interface GetCustomerTransitionsParams {
    path: { issueIdOrKey: string };
    start?: number;
    limit?: number;
}

/**
 * Get customer transitions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomerTransitions(client: BoundClient, params: GetCustomerTransitionsParams): Promise<Types.PagedDTOCustomerTransitionDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/transition`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOCustomerTransitionDTO>;
}

export interface PerformCustomerTransitionParams {
    path: { issueIdOrKey: string };
    body?: Types.CustomerTransitionExecutionDTO;
}

/**
 * Perform customer transition
 * @forge-scopes-asApp read:jira-work
 */
export async function performCustomerTransition(client: BoundClient, params: PerformCustomerTransitionParams): Promise<void> {
    const path = `/rest/servicedeskapi/request/${params.path.issueIdOrKey}/transition`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFeedbackParams {
    path: { requestIdOrKey: string };
}

/**
 * Get feedback
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFeedback(client: BoundClient, params: GetFeedbackParams): Promise<Types.CSATFeedbackFullDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.requestIdOrKey}/feedback`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CSATFeedbackFullDTO>;
}

export interface PostFeedbackParams {
    path: { requestIdOrKey: string };
    body?: Types.CSATFeedbackFullDTO;
}

/**
 * Post feedback
 * @forge-scopes-asApp read:jira-work
 */
export async function postFeedback(client: BoundClient, params: PostFeedbackParams): Promise<Types.CSATFeedbackFullDTO> {
    const path = `/rest/servicedeskapi/request/${params.path.requestIdOrKey}/feedback`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CSATFeedbackFullDTO>;
}

export interface DeleteFeedbackParams {
    path: { requestIdOrKey: string };
}

/**
 * Delete feedback
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFeedback(client: BoundClient, params: DeleteFeedbackParams): Promise<Record<string, unknown>> {
    const path = `/rest/servicedeskapi/request/${params.path.requestIdOrKey}/feedback`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetAllRequestTypesParams {
    searchQuery?: string;
    serviceDeskId?: Array<number>;
    start?: number;
    limit?: number;
    expand?: Array<string>;
    includeHiddenRequestTypesInSearch?: boolean;
    restrictionStatus?: string;
}

/**
 * Get all request types
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllRequestTypes(client: BoundClient, params: GetAllRequestTypesParams): Promise<Types.PagedDTORequestTypeDTO> {
    const path = '/rest/servicedeskapi/requesttype';
    const queryParams = {
      searchQuery: params.searchQuery,
      serviceDeskId: params.serviceDeskId,
      start: params.start,
      limit: params.limit,
      expand: params.expand,
      includeHiddenRequestTypesInSearch: params.includeHiddenRequestTypesInSearch,
      restrictionStatus: params.restrictionStatus,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTORequestTypeDTO>;
}

export interface GetServiceDesksParams {
    start?: number;
    limit?: number;
}

/**
 * Get service desks
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getServiceDesks(client: BoundClient, params: GetServiceDesksParams): Promise<Types.PagedDTOServiceDeskDTO> {
    const path = '/rest/servicedeskapi/servicedesk';
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOServiceDeskDTO>;
}

export interface GetServiceDeskByIdParams {
    path: { serviceDeskId: string };
}

/**
 * Get service desk by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getServiceDeskById(client: BoundClient, params: GetServiceDeskByIdParams): Promise<Types.ServiceDeskDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ServiceDeskDTO>;
}

export interface AttachTemporaryFileParams {
    path: { serviceDeskId: string };
    body?: Record<string, unknown>;
}

/**
 * Attach temporary file
 * @forge-scopes-asApp read:jira-work
 */
export async function attachTemporaryFile(client: BoundClient, params: AttachTemporaryFileParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/attachTemporaryFile`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetCustomersParams {
    path: { serviceDeskId: string };
    query?: string;
    start?: number;
    limit?: number;
}

/**
 * Get customers
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomers(client: BoundClient, params: GetCustomersParams): Promise<Types.PagedDTOUserDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/customer`;
    const queryParams = {
      query: params.query,
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOUserDTO>;
}

export interface AddCustomersParams {
    path: { serviceDeskId: string };
    body?: Types.ServiceDeskCustomerDTO;
}

/**
 * Add customers
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addCustomers(client: BoundClient, params: AddCustomersParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/customer`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveCustomersParams {
    path: { serviceDeskId: string };
    body?: Types.ServiceDeskCustomerDTO;
}

/**
 * Remove customers
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeCustomers(client: BoundClient, params: RemoveCustomersParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/customer`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface InviteCustomerParams {
    path: { serviceDeskId: string };
    strictConflictStatusCode?: boolean;
    body?: Types.ServiceDeskCustomerInviteDTO;
}

/**
 * Invite customer
 * @forge-scopes-asApp read:jira-work
 */
export async function inviteCustomer(client: BoundClient, params: InviteCustomerParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/customer/invite`;
    const queryParams = {
      strictConflictStatusCode: params.strictConflictStatusCode,
    };
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetArticlesForServiceDeskParams {
    path: { serviceDeskId: string };
    query: string;
    highlight?: boolean;
    start?: number;
    limit?: number;
    cursor?: string;
    prev?: boolean;
}

/**
 * Get articles
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getArticlesForServiceDesk(client: BoundClient, params: GetArticlesForServiceDeskParams): Promise<Types.PagedDTOArticleDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/knowledgebase/article`;
    const queryParams = {
      query: params.query,
      highlight: params.highlight,
      start: params.start,
      limit: params.limit,
      cursor: params.cursor,
      prev: params.prev,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOArticleDTO>;
}

export interface GetOrganizationsForServiceDeskParams {
    path: { serviceDeskId: string };
    start?: number;
    limit?: number;
    accountId?: string;
}

/**
 * Get organizations
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getOrganizationsForServiceDesk(client: BoundClient, params: GetOrganizationsForServiceDeskParams): Promise<Types.PagedDTOOrganizationDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/organization`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
      accountId: params.accountId,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOOrganizationDTO>;
}

export interface AddOrganizationParams {
    path: { serviceDeskId: string };
    body?: Types.OrganizationServiceDeskUpdateDTO;
}

/**
 * Add organization
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addOrganization(client: BoundClient, params: AddOrganizationParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/organization`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveOrganizationParams {
    path: { serviceDeskId: string };
    body?: Types.OrganizationServiceDeskUpdateDTO;
}

/**
 * Remove organization
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeOrganization(client: BoundClient, params: RemoveOrganizationParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/organization`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetQueuesParams {
    path: { serviceDeskId: string };
    includeCount?: boolean;
    start?: number;
    limit?: number;
}

/**
 * Get queues
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getQueues(client: BoundClient, params: GetQueuesParams): Promise<Types.PagedDTOQueueDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/queue`;
    const queryParams = {
      includeCount: params.includeCount,
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOQueueDTO>;
}

export interface GetQueueParams {
    path: { serviceDeskId: string; queueId: string };
    includeCount?: boolean;
}

/**
 * Get queue
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getQueue(client: BoundClient, params: GetQueueParams): Promise<Types.QueueDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/queue/${params.path.queueId}`;
    const queryParams = {
      includeCount: params.includeCount,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.QueueDTO>;
}

export interface GetIssuesInQueueParams {
    path: { serviceDeskId: string; queueId: string };
    start?: number;
    limit?: number;
}

/**
 * Get issues in queue
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuesInQueue(client: BoundClient, params: GetIssuesInQueueParams): Promise<Types.PagedDTOIssueBean> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/queue/${params.path.queueId}/issue`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTOIssueBean>;
}

export interface GetRequestTypesParams {
    path: { serviceDeskId: string };
    groupId?: number;
    expand?: Array<string>;
    searchQuery?: string;
    start?: number;
    limit?: number;
    includeHiddenRequestTypesInSearch?: boolean;
    restrictionStatus?: string;
}

/**
 * Get request types
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequestTypes(client: BoundClient, params: GetRequestTypesParams): Promise<Types.PagedDTORequestTypeDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype`;
    const queryParams = {
      groupId: params.groupId,
      expand: params.expand,
      searchQuery: params.searchQuery,
      start: params.start,
      limit: params.limit,
      includeHiddenRequestTypesInSearch: params.includeHiddenRequestTypesInSearch,
      restrictionStatus: params.restrictionStatus,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTORequestTypeDTO>;
}

export interface CreateRequestTypeParams {
    path: { serviceDeskId: string };
    body?: Types.RequestTypeCreateDTO;
}

/**
 * Create request type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createRequestType(client: BoundClient, params: CreateRequestTypeParams): Promise<Types.RequestTypeDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RequestTypeDTO>;
}

export interface CheckRequestTypePermissionsParams {
    path: { serviceDeskId: string };
    body?: Types.RequestTypePermissionCheckRequestDTO;
}

/**
 * Check request type permissions
 * @forge-scopes-asApp read:jira-work
 */
export async function checkRequestTypePermissions(client: BoundClient, params: CheckRequestTypePermissionsParams): Promise<Types.RequestTypePermissionCheckResponse> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/permissions/check`;
    const response = await client.adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RequestTypePermissionCheckResponse>;
}

export interface GetRequestTypeByIdParams {
    path: { serviceDeskId: string; requestTypeId: string };
    expand?: Array<string>;
}

/**
 * Get request type by id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequestTypeById(client: BoundClient, params: GetRequestTypeByIdParams): Promise<Types.RequestTypeDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/${params.path.requestTypeId}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RequestTypeDTO>;
}

export interface DeleteRequestTypeParams {
    path: { serviceDeskId: string; requestTypeId: string };
}

/**
 * Delete request type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRequestType(client: BoundClient, params: DeleteRequestTypeParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/${params.path.requestTypeId}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetRequestTypeFieldsParams {
    path: { serviceDeskId: string; requestTypeId: string };
    expand?: Array<string>;
}

/**
 * Get request type fields
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequestTypeFields(client: BoundClient, params: GetRequestTypeFieldsParams): Promise<Types.CustomerRequestCreateMetaDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/${params.path.requestTypeId}/field`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomerRequestCreateMetaDTO>;
}

export interface GetPropertiesKeysForRequestTypeParams {
    path: { requestTypeId: string; serviceDeskId: string };
}

/**
 * Get properties keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPropertiesKeysForRequestType(client: BoundClient, params: GetPropertiesKeysForRequestTypeParams): Promise<Types.PropertyKeys> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/${params.path.requestTypeId}/property`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetPropertyForRequestTypeParams {
    path: { serviceDeskId: string; requestTypeId: string; propertyKey: string };
}

/**
 * Get property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPropertyForRequestType(client: BoundClient, params: GetPropertyForRequestTypeParams): Promise<Types.EntityProperty> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/${params.path.requestTypeId}/property/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetPropertyForRequestTypeParams {
    path: { serviceDeskId: string; requestTypeId: string; propertyKey: string };
}

/**
 * Set property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setPropertyForRequestType(client: BoundClient, params: SetPropertyForRequestTypeParams): Promise<Record<string, unknown>> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/${params.path.requestTypeId}/property/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'PUT',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeletePropertyForRequestTypeParams {
    path: { serviceDeskId: string; requestTypeId: string; propertyKey: string };
}

/**
 * Delete property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePropertyForRequestType(client: BoundClient, params: DeletePropertyForRequestTypeParams): Promise<void> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttype/${params.path.requestTypeId}/property/${params.path.propertyKey}`;
    const response = await client.adapter.fetch({
      method: 'DELETE',
      path,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetRequestTypeGroupsParams {
    path: { serviceDeskId: string };
    start?: number;
    limit?: number;
}

/**
 * Get request type groups
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequestTypeGroups(client: BoundClient, params: GetRequestTypeGroupsParams): Promise<Types.PagedDTORequestTypeGroupDTO> {
    const path = `/rest/servicedeskapi/servicedesk/${params.path.serviceDeskId}/requesttypegroup`;
    const queryParams = {
      start: params.start,
      limit: params.limit,
    };
    const response = await client.adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext: client.authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PagedDTORequestTypeGroupDTO>;
}
