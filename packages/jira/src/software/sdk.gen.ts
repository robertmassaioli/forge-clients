// @ts-nocheck
import { ForgeApiError } from "@forge-clients/core";
import type { ForgeAdapter, AuthContext } from "@forge-clients/core";
import type * as Types from "./types.gen.js";

// Generated by @forge-clients/generator — DO NOT EDIT
// Source: Jira Software Cloud REST API
/* eslint-disable */
export type * from './types.gen.js';

export interface MoveIssuesToBacklogParams {
    body?: { issues?: Array<string> };
}

/**
 * Move issues to backlog
 * @forge-scopes-asApp read:jira-work
 */
export async function moveIssuesToBacklog(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveIssuesToBacklogParams): Promise<void> {
    const path = '/rest/agile/1.0/backlog/issue';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface MoveIssuesToBacklogForBoardParams {
    path: { boardId: string };
    body?: { issues?: Array<string>; rankAfterIssue?: string; rankBeforeIssue?: string; rankCustomFieldId?: number };
}

/**
 * Move issues to backlog for board
 * @forge-scopes-asApp read:jira-work
 */
export async function moveIssuesToBacklogForBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveIssuesToBacklogForBoardParams): Promise<void> {
    const path = `/rest/agile/1.0/backlog/${params.path.boardId}/issue`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAllBoardsParams {
    startAt?: number;
    maxResults?: number;
    type?: Record<string, unknown>;
    name?: string;
    projectKeyOrId?: string;
    accountIdLocation?: string;
    projectLocation?: string;
    includePrivate?: boolean;
    negateLocationFiltering?: boolean;
    orderBy?: "name" | "-name" | "+name";
    expand?: string;
    projectTypeLocation?: Array<string>;
    filterId?: number;
}

/**
 * Get all boards
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllBoards(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllBoardsParams): Promise<{ isLast?: boolean; maxResults?: number; startAt?: number; total?: number; values?: Array<{ readonly admins?: { groups?: Array<{ name?: string; self?: string }>; users?: Array<{ accountId?: string; active?: boolean; avatarUrls?: { '16x16'?: string; '24x24'?: string; '32x32'?: string; '48x48'?: string }; displayName?: string; key?: string; name?: string; self?: string }> }; readonly canEdit?: boolean; readonly favourite?: boolean; id?: number; readonly isPrivate?: boolean; location?: { avatarURI?: string; displayName?: string; name?: string; projectId?: number; projectKey?: string; projectName?: string; projectTypeKey?: string; userAccountId?: string; userId?: number }; name?: string; readonly self?: string; type?: string }> }> {
    const path = '/rest/agile/1.0/board';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      type: params.type,
      name: params.name,
      projectKeyOrId: params.projectKeyOrId,
      accountIdLocation: params.accountIdLocation,
      projectLocation: params.projectLocation,
      includePrivate: params.includePrivate,
      negateLocationFiltering: params.negateLocationFiltering,
      orderBy: params.orderBy,
      expand: params.expand,
      projectTypeLocation: params.projectTypeLocation,
      filterId: params.filterId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ isLast?: boolean; maxResults?: number; startAt?: number; total?: number; values?: Array<{ readonly admins?: { groups?: Array<{ name?: string; self?: string }>; users?: Array<{ accountId?: string; active?: boolean; avatarUrls?: { '16x16'?: string; '24x24'?: string; '32x32'?: string; '48x48'?: string }; displayName?: string; key?: string; name?: string; self?: string }> }; readonly canEdit?: boolean; readonly favourite?: boolean; id?: number; readonly isPrivate?: boolean; location?: { avatarURI?: string; displayName?: string; name?: string; projectId?: number; projectKey?: string; projectName?: string; projectTypeKey?: string; userAccountId?: string; userId?: number }; name?: string; readonly self?: string; type?: string }> }>;
}

export interface CreateBoardParams {
    body?: { filterId?: number; location?: { projectKeyOrId?: string; type?: "project" | "user" }; name?: string; type?: "kanban" | "scrum" | "agility" };
}

/**
 * Create board
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateBoardParams): Promise<{ readonly admins?: { groups?: Array<{ name?: string; self?: string }>; users?: Array<{ accountId?: string; active?: boolean; avatarUrls?: { '16x16'?: string; '24x24'?: string; '32x32'?: string; '48x48'?: string }; displayName?: string; key?: string; name?: string; self?: string }> }; readonly canEdit?: boolean; readonly favourite?: boolean; id?: number; readonly isPrivate?: boolean; location?: { avatarURI?: string; displayName?: string; name?: string; projectId?: number; projectKey?: string; projectName?: string; projectTypeKey?: string; userAccountId?: string; userId?: number }; name?: string; readonly self?: string; type?: string }> {
    const path = '/rest/agile/1.0/board';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ readonly admins?: { groups?: Array<{ name?: string; self?: string }>; users?: Array<{ accountId?: string; active?: boolean; avatarUrls?: { '16x16'?: string; '24x24'?: string; '32x32'?: string; '48x48'?: string }; displayName?: string; key?: string; name?: string; self?: string }> }; readonly canEdit?: boolean; readonly favourite?: boolean; id?: number; readonly isPrivate?: boolean; location?: { avatarURI?: string; displayName?: string; name?: string; projectId?: number; projectKey?: string; projectName?: string; projectTypeKey?: string; userAccountId?: string; userId?: number }; name?: string; readonly self?: string; type?: string }>;
}

export interface GetBoardByFilterIdParams {
    path: { filterId: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get board by filter id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBoardByFilterId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBoardByFilterIdParams): Promise<{ isLast?: boolean; maxResults?: number; startAt?: number; total?: number; values?: Array<{ id?: number; name?: string; self?: string }> }> {
    const path = `/rest/agile/1.0/board/filter/${params.path.filterId}`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ isLast?: boolean; maxResults?: number; startAt?: number; total?: number; values?: Array<{ id?: number; name?: string; self?: string }> }>;
}

export interface GetBoardParams {
    path: { boardId: string };
}

/**
 * Get board
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBoardParams): Promise<{ readonly admins?: { groups?: Array<{ name?: string; self?: string }>; users?: Array<{ accountId?: string; active?: boolean; avatarUrls?: { '16x16'?: string; '24x24'?: string; '32x32'?: string; '48x48'?: string }; displayName?: string; key?: string; name?: string; self?: string }> }; readonly canEdit?: boolean; readonly favourite?: boolean; id?: number; readonly isPrivate?: boolean; location?: { avatarURI?: string; displayName?: string; name?: string; projectId?: number; projectKey?: string; projectName?: string; projectTypeKey?: string; userAccountId?: string; userId?: number }; name?: string; readonly self?: string; type?: string }> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ readonly admins?: { groups?: Array<{ name?: string; self?: string }>; users?: Array<{ accountId?: string; active?: boolean; avatarUrls?: { '16x16'?: string; '24x24'?: string; '32x32'?: string; '48x48'?: string }; displayName?: string; key?: string; name?: string; self?: string }> }; readonly canEdit?: boolean; readonly favourite?: boolean; id?: number; readonly isPrivate?: boolean; location?: { avatarURI?: string; displayName?: string; name?: string; projectId?: number; projectKey?: string; projectName?: string; projectTypeKey?: string; userAccountId?: string; userId?: number }; name?: string; readonly self?: string; type?: string }>;
}

export interface DeleteBoardParams {
    path: { boardId: string };
}

/**
 * Delete board
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteBoardParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssuesForBacklogParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get issues for backlog
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuesForBacklog(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuesForBacklogParams): Promise<Types.SearchResults> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/backlog`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchResults>;
}

export interface GetConfigurationParams {
    path: { boardId: string };
}

/**
 * Get configuration
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetConfigurationParams): Promise<{ columnConfig?: { columns?: Array<{ max?: number; min?: number; name?: string; statuses?: Array<{ id?: string; self?: string }> }>; constraintType?: string }; estimation?: { field?: { displayName?: string; fieldId?: string }; type?: string }; filter?: { id?: string; self?: string }; id?: number; location?: { projectKeyOrId?: string; type?: "project" | "user" }; name?: string; ranking?: { rankCustomFieldId?: number }; self?: string; subQuery?: { query?: string }; type?: string }> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/configuration`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ columnConfig?: { columns?: Array<{ max?: number; min?: number; name?: string; statuses?: Array<{ id?: string; self?: string }> }>; constraintType?: string }; estimation?: { field?: { displayName?: string; fieldId?: string }; type?: string }; filter?: { id?: string; self?: string }; id?: number; location?: { projectKeyOrId?: string; type?: "project" | "user" }; name?: string; ranking?: { rankCustomFieldId?: number }; self?: string; subQuery?: { query?: string }; type?: string }>;
}

export interface GetEpicsParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
    done?: string;
}

/**
 * Get epics
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getEpics(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetEpicsParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/epic`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      done: params.done,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssuesWithoutEpicForBoardParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get issues without epic for board
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuesWithoutEpicForBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuesWithoutEpicForBoardParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/epic/none/issue`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBoardIssuesForEpicParams {
    path: { boardId: string; epicId: string };
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get board issues for epic
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBoardIssuesForEpic(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBoardIssuesForEpicParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/epic/${params.path.epicId}/issue`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFeaturesForBoardParams {
    path: { boardId: string };
}

/**
 * Get features for board
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFeaturesForBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFeaturesForBoardParams): Promise<{ features?: Array<{ boardFeature?: "SIMPLE_ROADMAP" | "BACKLOG" | "SPRINTS" | "CALENDAR" | "DEVTOOLS" | "REPORTS" | "ESTIMATION" | "PAGES" | "CODE" | "SECURITY" | "REQUESTS" | "INCIDENTS" | "RELEASES" | "DEPLOYMENTS" | "ISSUE_NAVIGATOR" | "ON_CALL_SCHEDULE" | "BOARD" | "GOALS" | "LIST_VIEW"; boardId?: number; featureId?: string; featureType?: "BASIC" | "ESTIMATION"; imageUri?: string; learnMoreArticleId?: string; learnMoreLink?: string; localisedDescription?: string; localisedGroup?: string; localisedName?: string; permissibleEstimationTypes?: Array<{ localisedDescription?: string; localisedName?: string; value?: "STORY_POINTS" | "ORIGINAL_ESTIMATE" }>; state?: "ENABLED" | "DISABLED" | "COMING_SOON"; toggleLocked?: boolean }> }> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/features`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ features?: Array<{ boardFeature?: "SIMPLE_ROADMAP" | "BACKLOG" | "SPRINTS" | "CALENDAR" | "DEVTOOLS" | "REPORTS" | "ESTIMATION" | "PAGES" | "CODE" | "SECURITY" | "REQUESTS" | "INCIDENTS" | "RELEASES" | "DEPLOYMENTS" | "ISSUE_NAVIGATOR" | "ON_CALL_SCHEDULE" | "BOARD" | "GOALS" | "LIST_VIEW"; boardId?: number; featureId?: string; featureType?: "BASIC" | "ESTIMATION"; imageUri?: string; learnMoreArticleId?: string; learnMoreLink?: string; localisedDescription?: string; localisedGroup?: string; localisedName?: string; permissibleEstimationTypes?: Array<{ localisedDescription?: string; localisedName?: string; value?: "STORY_POINTS" | "ORIGINAL_ESTIMATE" }>; state?: "ENABLED" | "DISABLED" | "COMING_SOON"; toggleLocked?: boolean }> }>;
}

export interface ToggleFeaturesParams {
    path: { boardId: string };
    body?: { boardId?: number; enabling?: boolean; feature?: string };
}

/**
 * Toggle features
 * @forge-scopes-asApp read:jira-work
 */
export async function toggleFeatures(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ToggleFeaturesParams): Promise<{ features?: Array<{ boardFeature?: "SIMPLE_ROADMAP" | "BACKLOG" | "SPRINTS" | "CALENDAR" | "DEVTOOLS" | "REPORTS" | "ESTIMATION" | "PAGES" | "CODE" | "SECURITY" | "REQUESTS" | "INCIDENTS" | "RELEASES" | "DEPLOYMENTS" | "ISSUE_NAVIGATOR" | "ON_CALL_SCHEDULE" | "BOARD" | "GOALS" | "LIST_VIEW"; boardId?: number; featureId?: string; featureType?: "BASIC" | "ESTIMATION"; imageUri?: string; learnMoreArticleId?: string; learnMoreLink?: string; localisedDescription?: string; localisedGroup?: string; localisedName?: string; permissibleEstimationTypes?: Array<{ localisedDescription?: string; localisedName?: string; value?: "STORY_POINTS" | "ORIGINAL_ESTIMATE" }>; state?: "ENABLED" | "DISABLED" | "COMING_SOON"; toggleLocked?: boolean }> }> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/features`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ features?: Array<{ boardFeature?: "SIMPLE_ROADMAP" | "BACKLOG" | "SPRINTS" | "CALENDAR" | "DEVTOOLS" | "REPORTS" | "ESTIMATION" | "PAGES" | "CODE" | "SECURITY" | "REQUESTS" | "INCIDENTS" | "RELEASES" | "DEPLOYMENTS" | "ISSUE_NAVIGATOR" | "ON_CALL_SCHEDULE" | "BOARD" | "GOALS" | "LIST_VIEW"; boardId?: number; featureId?: string; featureType?: "BASIC" | "ESTIMATION"; imageUri?: string; learnMoreArticleId?: string; learnMoreLink?: string; localisedDescription?: string; localisedGroup?: string; localisedName?: string; permissibleEstimationTypes?: Array<{ localisedDescription?: string; localisedName?: string; value?: "STORY_POINTS" | "ORIGINAL_ESTIMATE" }>; state?: "ENABLED" | "DISABLED" | "COMING_SOON"; toggleLocked?: boolean }> }>;
}

export interface GetIssuesForBoardParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get issues for board
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuesForBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuesForBoardParams): Promise<Types.SearchResults> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/issue`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchResults>;
}

export interface MoveIssuesToBoardParams {
    path: { boardId: string };
    body?: { issues?: Array<string>; rankAfterIssue?: string; rankBeforeIssue?: string; rankCustomFieldId?: number };
}

/**
 * Move issues to board
 * @forge-scopes-asApp read:jira-work
 */
export async function moveIssuesToBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveIssuesToBoardParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/issue`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetProjectsParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectsParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/project`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetProjectsFullParams {
    path: { boardId: string };
}

/**
 * Get projects full
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectsFull(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectsFullParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/project/full`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBoardPropertyKeysParams {
    path: { boardId: string };
}

/**
 * Get board property keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBoardPropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBoardPropertyKeysParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBoardPropertyParams {
    path: { boardId: string; propertyKey: string };
}

/**
 * Get board property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBoardProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBoardPropertyParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SetBoardPropertyParams {
    path: { boardId: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set board property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setBoardProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetBoardPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteBoardPropertyParams {
    path: { boardId: string; propertyKey: string };
}

/**
 * Delete board property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteBoardProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteBoardPropertyParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAllQuickFiltersParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get all quick filters
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllQuickFilters(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllQuickFiltersParams): Promise<{ isLast?: boolean; maxResults?: number; startAt?: number; total?: number; values?: Array<{ boardId?: number; description?: string; id?: number; jql?: string; name?: string; position?: number }> }> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/quickfilter`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ isLast?: boolean; maxResults?: number; startAt?: number; total?: number; values?: Array<{ boardId?: number; description?: string; id?: number; jql?: string; name?: string; position?: number }> }>;
}

export interface GetQuickFilterParams {
    path: { boardId: string; quickFilterId: string };
}

/**
 * Get quick filter
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getQuickFilter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetQuickFilterParams): Promise<{ boardId?: number; description?: string; id?: number; jql?: string; name?: string; position?: number }> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/quickfilter/${params.path.quickFilterId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ boardId?: number; description?: string; id?: number; jql?: string; name?: string; position?: number }>;
}

export interface GetReportsForBoardParams {
    path: { boardId: string };
}

/**
 * Get reports for board
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getReportsForBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetReportsForBoardParams): Promise<{ reports?: Array<Record<string, unknown>> }> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/reports`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ reports?: Array<Record<string, unknown>> }>;
}

export interface GetAllSprintsParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
    state?: Record<string, unknown>;
}

/**
 * Get all sprints
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllSprints(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllSprintsParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/sprint`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      state: params.state,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBoardIssuesForSprintParams {
    path: { boardId: string; sprintId: string };
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get board issues for sprint
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBoardIssuesForSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBoardIssuesForSprintParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/sprint/${params.path.sprintId}/issue`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAllVersionsParams {
    path: { boardId: string };
    startAt?: number;
    maxResults?: number;
    released?: string;
}

/**
 * Get all versions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllVersions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllVersionsParams): Promise<void> {
    const path = `/rest/agile/1.0/board/${params.path.boardId}/version`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      released: params.released,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssuesWithoutEpicParams {
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get issues without epic
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuesWithoutEpic(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuesWithoutEpicParams): Promise<void> {
    const path = '/rest/agile/1.0/epic/none/issue';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveIssuesFromEpicParams {
    body?: { issues?: Array<string> };
}

/**
 * Remove issues from epic
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeIssuesFromEpic(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveIssuesFromEpicParams): Promise<void> {
    const path = '/rest/agile/1.0/epic/none/issue';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetEpicParams {
    path: { epicIdOrKey: string };
}

/**
 * Get epic
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getEpic(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetEpicParams): Promise<void> {
    const path = `/rest/agile/1.0/epic/${params.path.epicIdOrKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface PartiallyUpdateEpicParams {
    path: { epicIdOrKey: string };
    body?: { color?: { key?: "color_1" | "color_2" | "color_3" | "color_4" | "color_5" | "color_6" | "color_7" | "color_8" | "color_9" | "color_10" | "color_11" | "color_12" | "color_13" | "color_14" }; done?: boolean; name?: string; summary?: string };
}

/**
 * Partially update epic
 * @forge-scopes-asApp read:jira-work
 */
export async function partiallyUpdateEpic(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PartiallyUpdateEpicParams): Promise<void> {
    const path = `/rest/agile/1.0/epic/${params.path.epicIdOrKey}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssuesForEpicParams {
    path: { epicIdOrKey: string };
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get issues for epic
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuesForEpic(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuesForEpicParams): Promise<void> {
    const path = `/rest/agile/1.0/epic/${params.path.epicIdOrKey}/issue`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface MoveIssuesToEpicParams {
    path: { epicIdOrKey: string };
    body?: { issues?: Array<string> };
}

/**
 * Move issues to epic
 * @forge-scopes-asApp read:jira-work
 */
export async function moveIssuesToEpic(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveIssuesToEpicParams): Promise<void> {
    const path = `/rest/agile/1.0/epic/${params.path.epicIdOrKey}/issue`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RankEpicsParams {
    path: { epicIdOrKey: string };
    body?: { rankAfterEpic?: string; rankBeforeEpic?: string; rankCustomFieldId?: number };
}

/**
 * Rank epics
 * @forge-scopes-asApp read:jira-work
 */
export async function rankEpics(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RankEpicsParams): Promise<void> {
    const path = `/rest/agile/1.0/epic/${params.path.epicIdOrKey}/rank`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RankIssuesParams {
    body?: { issues?: Array<string>; rankAfterIssue?: string; rankBeforeIssue?: string; rankCustomFieldId?: number };
}

/**
 * Rank issues
 * @forge-scopes-asApp read:jira-work
 */
export async function rankIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RankIssuesParams): Promise<void> {
    const path = '/rest/agile/1.0/issue/rank';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssueParams {
    path: { issueIdOrKey: string };
    fields?: Array<Record<string, unknown>>;
    expand?: string;
    updateHistory?: boolean;
}

/**
 * Get issue
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueParams): Promise<void> {
    const path = `/rest/agile/1.0/issue/${params.path.issueIdOrKey}`;
    const queryParams = {
      fields: params.fields,
      expand: params.expand,
      updateHistory: params.updateHistory,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssueEstimationForBoardParams {
    path: { issueIdOrKey: string };
    boardId?: number;
}

/**
 * Get issue estimation for board
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueEstimationForBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueEstimationForBoardParams): Promise<void> {
    const path = `/rest/agile/1.0/issue/${params.path.issueIdOrKey}/estimation`;
    const queryParams = {
      boardId: params.boardId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface EstimateIssueForBoardParams {
    path: { issueIdOrKey: string };
    boardId?: number;
    body?: { value?: string };
}

/**
 * Estimate issue for board
 * @forge-scopes-asApp read:jira-work
 */
export async function estimateIssueForBoard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: EstimateIssueForBoardParams): Promise<void> {
    const path = `/rest/agile/1.0/issue/${params.path.issueIdOrKey}/estimation`;
    const queryParams = {
      boardId: params.boardId,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface CreateSprintParams {
    body?: { endDate?: string; goal?: string; name?: string; originBoardId?: number; startDate?: string };
}

/**
 * Create sprint
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateSprintParams): Promise<void> {
    const path = '/rest/agile/1.0/sprint';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetSprintParams {
    path: { sprintId: string };
}

/**
 * Get sprint
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSprintParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface PartiallyUpdateSprintParams {
    path: { sprintId: string };
    body?: { completeDate?: string; createdDate?: string; endDate?: string; goal?: string; id?: number; name?: string; originBoardId?: number; self?: string; startDate?: string; state?: string };
}

/**
 * Partially update sprint
 * @forge-scopes-asApp read:jira-work
 */
export async function partiallyUpdateSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PartiallyUpdateSprintParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface UpdateSprintParams {
    path: { sprintId: string };
    body?: { completeDate?: string; createdDate?: string; endDate?: string; goal?: string; id?: number; name?: string; originBoardId?: number; self?: string; startDate?: string; state?: string };
}

/**
 * Update sprint
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateSprintParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface DeleteSprintParams {
    path: { sprintId: string };
}

/**
 * Delete sprint
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteSprintParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssuesForSprintParams {
    path: { sprintId: string };
    startAt?: number;
    maxResults?: number;
    jql?: string;
    validateQuery?: boolean;
    fields?: Array<Record<string, unknown>>;
    expand?: string;
}

/**
 * Get issues for sprint
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuesForSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuesForSprintParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}/issue`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      jql: params.jql,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface MoveIssuesToSprintAndRankParams {
    path: { sprintId: string };
    body?: { issues?: Array<string>; rankAfterIssue?: string; rankBeforeIssue?: string; rankCustomFieldId?: number };
}

/**
 * Move issues to sprint and rank
 * @forge-scopes-asApp read:jira-work
 */
export async function moveIssuesToSprintAndRank(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveIssuesToSprintAndRankParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}/issue`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPropertiesKeysParams {
    path: { sprintId: string };
}

/**
 * Get properties keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPropertiesKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPropertiesKeysParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPropertyParams {
    path: { sprintId: string; propertyKey: string };
}

/**
 * Get property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPropertyParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SetPropertyParams {
    path: { sprintId: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeletePropertyParams {
    path: { sprintId: string; propertyKey: string };
}

/**
 * Delete property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeletePropertyParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SwapSprintParams {
    path: { sprintId: string };
    body?: { sprintToSwapWith?: number };
}

/**
 * Swap sprint
 * @forge-scopes-asApp read:jira-work
 */
export async function swapSprint(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SwapSprintParams): Promise<void> {
    const path = `/rest/agile/1.0/sprint/${params.path.sprintId}/swap`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface StoreDevelopmentInformationParams {
    body?: { repositories: Array<{ name: string; description?: string; forkOf?: string; url: string; commits?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; hash?: string; flags?: Array<"MERGE_COMMIT">; message: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; fileCount: number; url: string; files?: Array<{ path: string; url: string; changeType: "ADDED" | "COPIED" | "DELETED" | "MODIFIED" | "MOVED" | "UNKNOWN"; linesAdded: number; linesRemoved: number }>; authorTimestamp: string; displayId: string }>; branches?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; name: string; lastCommit: { id: string; issueKeys: Array<string>; updateSequenceId: number; hash?: string; flags?: Array<"MERGE_COMMIT">; message: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; fileCount: number; url: string; files?: Array<{ path: string; url: string; changeType: "ADDED" | "COPIED" | "DELETED" | "MODIFIED" | "MOVED" | "UNKNOWN"; linesAdded: number; linesRemoved: number }>; authorTimestamp: string; displayId: string }; createPullRequestUrl?: string; url: string }>; pullRequests?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; status: "OPEN" | "MERGED" | "DECLINED" | "UNKNOWN"; title: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; commentCount: number; sourceBranch: string; sourceBranchUrl?: string; lastUpdate: string; destinationBranch?: string; destinationBranchUrl?: string; reviewers?: Array<{ name?: string; approvalStatus?: "APPROVED" | "UNAPPROVED"; url?: string; avatar?: string; email?: string; accountId?: string }>; url: string; displayId: string }>; avatar?: string; avatarDescription?: string; id: string; updateSequenceId: number }>; preventTransitions?: boolean; operationType?: "NORMAL" | "BACKFILL"; properties?: Record<string, string>; providerMetadata?: { product?: string } };
}

/**
 * Store development information
 * @forge-scopes-asApp read:jira-work
 */
export async function storeDevelopmentInformation(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: StoreDevelopmentInformationParams): Promise<{ acceptedDevinfoEntities?: Record<string, { commits?: Array<string>; branches?: Array<string>; pullRequests?: Array<string> }>; failedDevinfoEntities?: Record<string, { errorMessages?: Array<{ message: string; errorTraceId?: string }>; commits?: Array<{ id: string; errorMessages?: Array<{ message: string; errorTraceId?: string }> }>; branches?: Array<{ id: string; errorMessages?: Array<{ message: string; errorTraceId?: string }> }>; pullRequests?: Array<{ id: string; errorMessages?: Array<{ message: string; errorTraceId?: string }> }> }>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation> }> {
    const path = '/rest/devinfo/0.10/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedDevinfoEntities?: Record<string, { commits?: Array<string>; branches?: Array<string>; pullRequests?: Array<string> }>; failedDevinfoEntities?: Record<string, { errorMessages?: Array<{ message: string; errorTraceId?: string }>; commits?: Array<{ id: string; errorMessages?: Array<{ message: string; errorTraceId?: string }> }>; branches?: Array<{ id: string; errorMessages?: Array<{ message: string; errorTraceId?: string }> }>; pullRequests?: Array<{ id: string; errorMessages?: Array<{ message: string; errorTraceId?: string }> }> }>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation> }>;
}

export interface GetRepositoryParams {
    path: { repositoryId: string };
}

/**
 * Get repository
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRepository(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRepositoryParams): Promise<{ name: string; description?: string; forkOf?: string; url: string; commits?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; hash?: string; flags?: Array<"MERGE_COMMIT">; message: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; fileCount: number; url: string; files?: Array<{ path: string; url: string; changeType: "ADDED" | "COPIED" | "DELETED" | "MODIFIED" | "MOVED" | "UNKNOWN"; linesAdded: number; linesRemoved: number }>; authorTimestamp: string; displayId: string }>; branches?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; name: string; lastCommit: { id: string; issueKeys: Array<string>; updateSequenceId: number; hash?: string; flags?: Array<"MERGE_COMMIT">; message: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; fileCount: number; url: string; files?: Array<{ path: string; url: string; changeType: "ADDED" | "COPIED" | "DELETED" | "MODIFIED" | "MOVED" | "UNKNOWN"; linesAdded: number; linesRemoved: number }>; authorTimestamp: string; displayId: string }; createPullRequestUrl?: string; url: string }>; pullRequests?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; status: "OPEN" | "MERGED" | "DECLINED" | "UNKNOWN"; title: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; commentCount: number; sourceBranch: string; sourceBranchUrl?: string; lastUpdate: string; destinationBranch?: string; destinationBranchUrl?: string; reviewers?: Array<{ name?: string; approvalStatus?: "APPROVED" | "UNAPPROVED"; url?: string; avatar?: string; email?: string; accountId?: string }>; url: string; displayId: string }>; avatar?: string; avatarDescription?: string; id: string; updateSequenceId: number }> {
    const path = `/rest/devinfo/0.10/repository/${params.path.repositoryId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ name: string; description?: string; forkOf?: string; url: string; commits?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; hash?: string; flags?: Array<"MERGE_COMMIT">; message: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; fileCount: number; url: string; files?: Array<{ path: string; url: string; changeType: "ADDED" | "COPIED" | "DELETED" | "MODIFIED" | "MOVED" | "UNKNOWN"; linesAdded: number; linesRemoved: number }>; authorTimestamp: string; displayId: string }>; branches?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; name: string; lastCommit: { id: string; issueKeys: Array<string>; updateSequenceId: number; hash?: string; flags?: Array<"MERGE_COMMIT">; message: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; fileCount: number; url: string; files?: Array<{ path: string; url: string; changeType: "ADDED" | "COPIED" | "DELETED" | "MODIFIED" | "MOVED" | "UNKNOWN"; linesAdded: number; linesRemoved: number }>; authorTimestamp: string; displayId: string }; createPullRequestUrl?: string; url: string }>; pullRequests?: Array<{ id: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; updateSequenceId: number; status: "OPEN" | "MERGED" | "DECLINED" | "UNKNOWN"; title: string; author: { name?: string; email?: string; username?: string; url?: string; avatar?: string }; commentCount: number; sourceBranch: string; sourceBranchUrl?: string; lastUpdate: string; destinationBranch?: string; destinationBranchUrl?: string; reviewers?: Array<{ name?: string; approvalStatus?: "APPROVED" | "UNAPPROVED"; url?: string; avatar?: string; email?: string; accountId?: string }>; url: string; displayId: string }>; avatar?: string; avatarDescription?: string; id: string; updateSequenceId: number }>;
}

export interface DeleteRepositoryParams {
    path: { repositoryId: string };
    _updateSequenceId?: number;
}

/**
 * Delete repository
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRepository(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRepositoryParams): Promise<void> {
    const path = `/rest/devinfo/0.10/repository/${params.path.repositoryId}`;
    const queryParams = {
      _updateSequenceId: params._updateSequenceId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface DeleteByPropertiesParams {
    _updateSequenceId?: number;
}

/**
 * Delete development information by properties
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteByProperties(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteByPropertiesParams): Promise<void> {
    const path = '/rest/devinfo/0.10/bulkByProperties';
    const queryParams = {
      _updateSequenceId: params._updateSequenceId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface ExistsByPropertiesParams {
    _updateSequenceId?: number;
}

/**
 * Check if data exists for the supplied properties
 * @forge-scopes-asApp read:jira-work
 */
export async function existsByProperties(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ExistsByPropertiesParams): Promise<{ readonly hasDataMatchingProperties?: boolean }> {
    const path = '/rest/devinfo/0.10/existsByProperties';
    const queryParams = {
      _updateSequenceId: params._updateSequenceId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ readonly hasDataMatchingProperties?: boolean }>;
}

export interface DeleteEntityParams {
    path: { repositoryId: string; entityType: string; entityId: string };
    _updateSequenceId?: number;
}

/**
 * Delete development information entity
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteEntity(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteEntityParams): Promise<void> {
    const path = `/rest/devinfo/0.10/repository/${params.path.repositoryId}/${params.path.entityType}/${params.path.entityId}`;
    const queryParams = {
      _updateSequenceId: params._updateSequenceId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SubmitFeatureFlagsParams {
    body?: { properties?: Record<string, string>; flags: Array<{ schemaVersion?: "1.0"; id: string; key: string; updateSequenceId: number; displayName?: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; summary: { url?: string; status: { enabled: boolean; defaultValue?: string; rollout?: { percentage?: number; text?: string; rules?: number } }; lastUpdated: string }; details: Array<{ url: string; lastUpdated: string; environment: { name: string; type?: "development" | "testing" | "staging" | "production" }; status: { enabled: boolean; defaultValue?: string; rollout?: { percentage?: number; text?: string; rules?: number } } }> }>; providerMetadata?: { product?: string } };
}

/**
 * Submit Feature Flag data
 * @forge-scopes-asApp read:jira-work
 */
export async function submitFeatureFlags(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitFeatureFlagsParams): Promise<{ acceptedFeatureFlags?: Array<string>; failedFeatureFlags?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation> }> {
    const path = '/rest/featureflags/0.1/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedFeatureFlags?: Array<string>; failedFeatureFlags?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation> }>;
}

export interface DeleteFeatureFlagsByPropertyParams {
    _updateSequenceId?: number;
}

/**
 * Delete Feature Flags by Property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFeatureFlagsByProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteFeatureFlagsByPropertyParams): Promise<void> {
    const path = '/rest/featureflags/0.1/bulkByProperties';
    const queryParams = {
      _updateSequenceId: params._updateSequenceId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFeatureFlagByIdParams {
    path: { featureFlagId: string };
}

/**
 * Get a Feature Flag by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFeatureFlagById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFeatureFlagByIdParams): Promise<{ schemaVersion?: "1.0"; id: string; key: string; updateSequenceId: number; displayName?: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; summary: { url?: string; status: { enabled: boolean; defaultValue?: string; rollout?: { percentage?: number; text?: string; rules?: number } }; lastUpdated: string }; details: Array<{ url: string; lastUpdated: string; environment: { name: string; type?: "development" | "testing" | "staging" | "production" }; status: { enabled: boolean; defaultValue?: string; rollout?: { percentage?: number; text?: string; rules?: number } } }> }> {
    const path = `/rest/featureflags/0.1/flag/${params.path.featureFlagId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ schemaVersion?: "1.0"; id: string; key: string; updateSequenceId: number; displayName?: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; summary: { url?: string; status: { enabled: boolean; defaultValue?: string; rollout?: { percentage?: number; text?: string; rules?: number } }; lastUpdated: string }; details: Array<{ url: string; lastUpdated: string; environment: { name: string; type?: "development" | "testing" | "staging" | "production" }; status: { enabled: boolean; defaultValue?: string; rollout?: { percentage?: number; text?: string; rules?: number } } }> }>;
}

export interface DeleteFeatureFlagByIdParams {
    path: { featureFlagId: string };
    _updateSequenceId?: number;
}

/**
 * Delete a Feature Flag by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFeatureFlagById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteFeatureFlagByIdParams): Promise<void> {
    const path = `/rest/featureflags/0.1/flag/${params.path.featureFlagId}`;
    const queryParams = {
      _updateSequenceId: params._updateSequenceId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SubmitDeploymentsParams {
    body?: { properties?: Record<string, string>; deployments: Array<{ deploymentSequenceNumber: number; updateSequenceNumber: number; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation>; displayName: string; url: string; description: string; lastUpdated: string; label?: string; duration?: number; state: "unknown" | "pending" | "in_progress" | "cancelled" | "failed" | "rolled_back" | "successful"; pipeline: { id: string; displayName: string; url: string }; environment: { id: string; displayName: string; type: "unmapped" | "development" | "testing" | "staging" | "production" }; commands?: Array<{ command?: string }>; schemaVersion?: "1.0" }>; providerMetadata?: { product?: string } };
}

/**
 * Submit deployment data
 * @forge-scopes-asApp read:jira-work
 */
export async function submitDeployments(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitDeploymentsParams): Promise<{ acceptedDeployments?: Array<{ pipelineId: string; environmentId: string; deploymentSequenceNumber: number }>; rejectedDeployments?: Array<{ key: { pipelineId: string; environmentId: string; deploymentSequenceNumber: number }; errors: Array<{ message: string; errorTraceId?: string }> }>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation> }> {
    const path = '/rest/deployments/0.1/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedDeployments?: Array<{ pipelineId: string; environmentId: string; deploymentSequenceNumber: number }>; rejectedDeployments?: Array<{ key: { pipelineId: string; environmentId: string; deploymentSequenceNumber: number }; errors: Array<{ message: string; errorTraceId?: string }> }>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation> }>;
}

export interface DeleteDeploymentsByPropertyParams {
    _updateSequenceNumber?: number;
}

/**
 * Delete deployments by Property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDeploymentsByProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteDeploymentsByPropertyParams): Promise<void> {
    const path = '/rest/deployments/0.1/bulkByProperties';
    const queryParams = {
      _updateSequenceNumber: params._updateSequenceNumber,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDeploymentByKeyParams {
    path: { pipelineId: string; environmentId: string; deploymentSequenceNumber: string };
}

/**
 * Get a deployment by key
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDeploymentByKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDeploymentByKeyParams): Promise<{ deploymentSequenceNumber: number; updateSequenceNumber: number; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation>; displayName: string; url: string; description: string; lastUpdated: string; label?: string; duration?: number; state: "unknown" | "pending" | "in_progress" | "cancelled" | "failed" | "rolled_back" | "successful"; pipeline: { id: string; displayName: string; url: string }; environment: { id: string; displayName: string; type: "unmapped" | "development" | "testing" | "staging" | "production" }; commands?: Array<{ command?: string }>; schemaVersion?: "1.0" }> {
    const path = `/rest/deployments/0.1/pipelines/${params.path.pipelineId}/environments/${params.path.environmentId}/deployments/${params.path.deploymentSequenceNumber}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ deploymentSequenceNumber: number; updateSequenceNumber: number; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation | EntityAssociation>; displayName: string; url: string; description: string; lastUpdated: string; label?: string; duration?: number; state: "unknown" | "pending" | "in_progress" | "cancelled" | "failed" | "rolled_back" | "successful"; pipeline: { id: string; displayName: string; url: string }; environment: { id: string; displayName: string; type: "unmapped" | "development" | "testing" | "staging" | "production" }; commands?: Array<{ command?: string }>; schemaVersion?: "1.0" }>;
}

export interface DeleteDeploymentByKeyParams {
    path: { pipelineId: string; environmentId: string; deploymentSequenceNumber: string };
    _updateSequenceNumber?: number;
}

/**
 * Delete a deployment by key
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDeploymentByKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteDeploymentByKeyParams): Promise<void> {
    const path = `/rest/deployments/0.1/pipelines/${params.path.pipelineId}/environments/${params.path.environmentId}/deployments/${params.path.deploymentSequenceNumber}`;
    const queryParams = {
      _updateSequenceNumber: params._updateSequenceNumber,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDeploymentGatingStatusByKeyParams {
    path: { pipelineId: string; environmentId: string; deploymentSequenceNumber: string };
}

/**
 * Get deployment gating status by key
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDeploymentGatingStatusByKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDeploymentGatingStatusByKeyParams): Promise<{ deploymentSequenceNumber?: number; pipelineId?: string; environmentId?: string; updatedTimestamp?: string; gatingStatus?: "allowed" | "prevented" | "awaiting" | "invalid"; details?: Array<{ type: "issue"; issueKey: string; issueLink: string }> }> {
    const path = `/rest/deployments/0.1/pipelines/${params.path.pipelineId}/environments/${params.path.environmentId}/deployments/${params.path.deploymentSequenceNumber}/gating-status`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ deploymentSequenceNumber?: number; pipelineId?: string; environmentId?: string; updatedTimestamp?: string; gatingStatus?: "allowed" | "prevented" | "awaiting" | "invalid"; details?: Array<{ type: "issue"; issueKey: string; issueLink: string }> }>;
}

export interface SubmitBuildsParams {
    body?: { properties?: Record<string, string>; builds: Array<{ schemaVersion?: "1.0"; pipelineId: string; buildNumber: number; updateSequenceNumber: number; displayName: string; description?: string; label?: string; url: string; state: "pending" | "in_progress" | "successful" | "failed" | "cancelled" | "unknown"; lastUpdated: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; testInfo?: { totalNumber: number; numberPassed: number; numberFailed: number; numberSkipped?: number }; references?: Array<{ commit?: { id: string; repositoryUri: string }; ref?: { name: string; uri: string } }> }>; providerMetadata?: { product?: string } };
}

/**
 * Submit build data
 * @forge-scopes-asApp read:jira-work
 */
export async function submitBuilds(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitBuildsParams): Promise<{ acceptedBuilds?: Array<{ pipelineId: string; buildNumber: number }>; rejectedBuilds?: Array<{ key: { pipelineId: string; buildNumber: number }; errors: Array<{ message: string; errorTraceId?: string }> }>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation> }> {
    const path = '/rest/builds/0.1/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedBuilds?: Array<{ pipelineId: string; buildNumber: number }>; rejectedBuilds?: Array<{ key: { pipelineId: string; buildNumber: number }; errors: Array<{ message: string; errorTraceId?: string }> }>; unknownIssueKeys?: Array<string>; unknownAssociations?: Array<IssueIdOrKeysAssociation> }>;
}

export interface DeleteBuildsByPropertyParams {
    _updateSequenceNumber?: number;
}

/**
 * Delete builds by Property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteBuildsByProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteBuildsByPropertyParams): Promise<void> {
    const path = '/rest/builds/0.1/bulkByProperties';
    const queryParams = {
      _updateSequenceNumber: params._updateSequenceNumber,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetBuildByKeyParams {
    path: { pipelineId: string; buildNumber: string };
}

/**
 * Get a build by key
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBuildByKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBuildByKeyParams): Promise<{ schemaVersion?: "1.0"; pipelineId: string; buildNumber: number; updateSequenceNumber: number; displayName: string; description?: string; label?: string; url: string; state: "pending" | "in_progress" | "successful" | "failed" | "cancelled" | "unknown"; lastUpdated: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; testInfo?: { totalNumber: number; numberPassed: number; numberFailed: number; numberSkipped?: number }; references?: Array<{ commit?: { id: string; repositoryUri: string }; ref?: { name: string; uri: string } }> }> {
    const path = `/rest/builds/0.1/pipelines/${params.path.pipelineId}/builds/${params.path.buildNumber}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ schemaVersion?: "1.0"; pipelineId: string; buildNumber: number; updateSequenceNumber: number; displayName: string; description?: string; label?: string; url: string; state: "pending" | "in_progress" | "successful" | "failed" | "cancelled" | "unknown"; lastUpdated: string; issueKeys?: Array<string>; associations?: Array<IssueIdOrKeysAssociation>; testInfo?: { totalNumber: number; numberPassed: number; numberFailed: number; numberSkipped?: number }; references?: Array<{ commit?: { id: string; repositoryUri: string }; ref?: { name: string; uri: string } }> }>;
}

export interface DeleteBuildByKeyParams {
    path: { pipelineId: string; buildNumber: string };
    _updateSequenceNumber?: number;
}

/**
 * Delete a build by key
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteBuildByKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteBuildByKeyParams): Promise<void> {
    const path = `/rest/builds/0.1/pipelines/${params.path.pipelineId}/builds/${params.path.buildNumber}`;
    const queryParams = {
      _updateSequenceNumber: params._updateSequenceNumber,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SubmitRemoteLinksParams {
    body?: { properties?: Record<string, string>; remoteLinks: Array<{ schemaVersion?: "1.0"; id: string; updateSequenceNumber: number; displayName: string; url: string; type: "document" | "alert" | "test" | "security" | "logFile" | "prototype" | "coverage" | "bugReport" | "other"; description?: string; lastUpdated: string; associations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation>; status?: { appearance: "default" | "inprogress" | "moved" | "new" | "removed" | "prototype" | "success"; label: string }; actionIds?: Array<string>; attributeMap?: Record<string, string> }>; providerMetadata?: { product?: string } };
}

/**
 * Submit Remote Link data
 * @forge-scopes-asApp read:jira-work
 */
export async function submitRemoteLinks(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitRemoteLinksParams): Promise<{ acceptedRemoteLinks?: Array<string>; rejectedRemoteLinks?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownAssociations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation> }> {
    const path = '/rest/remotelinks/1.0/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedRemoteLinks?: Array<string>; rejectedRemoteLinks?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownAssociations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation> }>;
}

export interface DeleteRemoteLinksByPropertyParams {
    _updateSequenceNumber?: number;
    params?: Record<string, unknown>;
}

/**
 * Delete Remote Links by Property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRemoteLinksByProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRemoteLinksByPropertyParams): Promise<void> {
    const path = '/rest/remotelinks/1.0/bulkByProperties';
    const queryParams = {
      _updateSequenceNumber: params._updateSequenceNumber,
      params: params.params,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetRemoteLinkByIdParams {
    path: { remoteLinkId: string };
}

/**
 * Get a Remote Link by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRemoteLinkById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRemoteLinkByIdParams): Promise<{ schemaVersion?: "1.0"; id: string; updateSequenceNumber: number; displayName: string; url: string; type: "document" | "alert" | "test" | "security" | "logFile" | "prototype" | "coverage" | "bugReport" | "other"; description?: string; lastUpdated: string; associations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation>; status?: { appearance: "default" | "inprogress" | "moved" | "new" | "removed" | "prototype" | "success"; label: string }; actionIds?: Array<string>; attributeMap?: Record<string, string> }> {
    const path = `/rest/remotelinks/1.0/remotelink/${params.path.remoteLinkId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ schemaVersion?: "1.0"; id: string; updateSequenceNumber: number; displayName: string; url: string; type: "document" | "alert" | "test" | "security" | "logFile" | "prototype" | "coverage" | "bugReport" | "other"; description?: string; lastUpdated: string; associations?: Array<IssueIdOrKeysAssociation | ServiceIdOrKeysAssociation>; status?: { appearance: "default" | "inprogress" | "moved" | "new" | "removed" | "prototype" | "success"; label: string }; actionIds?: Array<string>; attributeMap?: Record<string, string> }>;
}

export interface DeleteRemoteLinkByIdParams {
    path: { remoteLinkId: string };
    _updateSequenceNumber?: number;
}

/**
 * Delete a Remote Link by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRemoteLinkById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRemoteLinkByIdParams): Promise<void> {
    const path = `/rest/remotelinks/1.0/remotelink/${params.path.remoteLinkId}`;
    const queryParams = {
      _updateSequenceNumber: params._updateSequenceNumber,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SubmitWorkspacesParams {
    body?: { workspaceIds: Array<string> };
}

/**
 * Submit Security Workspaces to link
 * @forge-scopes-asApp read:jira-work
 */
export async function submitWorkspaces(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitWorkspacesParams): Promise<void> {
    const path = '/rest/security/1.0/linkedWorkspaces/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Delete linked Security Workspaces
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteLinkedWorkspaces(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<void> {
    const path = '/rest/security/1.0/linkedWorkspaces/bulk';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get linked Security Workspaces
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLinkedWorkspaces(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<{ workspaceIds: Array<string> }> {
    const path = '/rest/security/1.0/linkedWorkspaces';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ workspaceIds: Array<string> }>;
}

export interface GetLinkedWorkspaceByIdParams {
    path: { workspaceId: string };
}

/**
 * Get a linked Security Workspace by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLinkedWorkspaceById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetLinkedWorkspaceByIdParams): Promise<{ workspaceId: string; updatedAt: string }> {
    const path = `/rest/security/1.0/linkedWorkspaces/${params.path.workspaceId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ workspaceId: string; updatedAt: string }>;
}

export interface SubmitVulnerabilitiesParams {
    body?: { operationType?: "NORMAL" | "SCAN" | "BACKFILL"; properties?: Record<string, string>; vulnerabilities: Array<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; containerId: string; displayName: string; description: string; url: string; type: "sca" | "sast" | "dast" | "unknown"; introducedDate: string; lastUpdated: string; severity: { level: "critical" | "high" | "medium" | "low" | "unknown" }; identifiers?: Array<{ displayName: string; url: string }>; status: "open" | "closed" | "ignored" | "unknown"; additionalInfo?: { content: string; url?: string }; addAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }>; removeAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }>; associationsLastUpdated?: string; associationsUpdateSequenceNumber?: number }>; providerMetadata?: { product?: string } };
}

/**
 * Submit Vulnerability data
 * @forge-scopes-asApp read:jira-work
 */
export async function submitVulnerabilities(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitVulnerabilitiesParams): Promise<{ acceptedVulnerabilities?: Array<string>; failedVulnerabilities?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }> }> {
    const path = '/rest/security/1.0/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedVulnerabilities?: Array<string>; failedVulnerabilities?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }> }>;
}

/**
 * Delete Vulnerabilities by Property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteVulnerabilitiesByProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<void> {
    const path = '/rest/security/1.0/bulkByProperties';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetVulnerabilityByIdParams {
    path: { vulnerabilityId: string };
}

/**
 * Get a Vulnerability by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getVulnerabilityById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetVulnerabilityByIdParams): Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; containerId: string; displayName: string; description: string; url: string; type: "sca" | "sast" | "dast" | "unknown"; introducedDate: string; lastUpdated: string; severity: { level: "critical" | "high" | "medium" | "low" | "unknown" }; identifiers?: Array<{ displayName: string; url: string }>; status: "open" | "closed" | "ignored" | "unknown"; additionalInfo?: { content: string; url?: string }; addAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }>; removeAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }>; associationsLastUpdated?: string; associationsUpdateSequenceNumber?: number }> {
    const path = `/rest/security/1.0/vulnerability/${params.path.vulnerabilityId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; containerId: string; displayName: string; description: string; url: string; type: "sca" | "sast" | "dast" | "unknown"; introducedDate: string; lastUpdated: string; severity: { level: "critical" | "high" | "medium" | "low" | "unknown" }; identifiers?: Array<{ displayName: string; url: string }>; status: "open" | "closed" | "ignored" | "unknown"; additionalInfo?: { content: string; url?: string }; addAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }>; removeAssociations?: Array<{ associationType: "issueIdOrKeys"; values: Array<string> }>; associationsLastUpdated?: string; associationsUpdateSequenceNumber?: number }>;
}

export interface DeleteVulnerabilityByIdParams {
    path: { vulnerabilityId: string };
}

/**
 * Delete a Vulnerability by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteVulnerabilityById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteVulnerabilityByIdParams): Promise<void> {
    const path = `/rest/security/1.0/vulnerability/${params.path.vulnerabilityId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SubmitOperationsWorkspacesParams {
    body?: { workspaceIds: Array<string> };
}

/**
 * Submit Operations Workspace Ids
 * @forge-scopes-asApp read:jira-work
 */
export async function submitOperationsWorkspaces(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitOperationsWorkspacesParams): Promise<{ acceptedWorkspaceIds?: Array<string> }> {
    const path = '/rest/operations/1.0/linkedWorkspaces/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedWorkspaceIds?: Array<string> }>;
}

/**
 * Delete Operations Workpaces by Id
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorkspaces(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<void> {
    const path = '/rest/operations/1.0/linkedWorkspaces/bulk';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get all Operations Workspace IDs or a specific Operations Workspace by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkspaces(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<{ workspaceIds: Array<string> }> {
    const path = '/rest/operations/1.0/linkedWorkspaces';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ workspaceIds: Array<string> }>;
}

export interface SubmitEntityParams {
    body?: { incidents?: Array<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; affectedComponents: Array<string>; summary: string; description: string; url: string; createdDate: string; lastUpdated: string; severity?: { level: "P1" | "P2" | "P3" | "P4" | "P5" | "unknown" }; status: "open" | "resolved" | "unknown"; associations?: Array<{ associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source"; values?: Array<string> }> }> };
}

/**
 * Submit Incident or Review data
 * @forge-scopes-asApp read:jira-work
 */
export async function submitEntity(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitEntityParams): Promise<{ acceptedIncidents?: Array<string>; failedIncidents?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownProjectKeys?: Array<string> }> {
    const path = '/rest/operations/1.0/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedIncidents?: Array<string>; failedIncidents?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownProjectKeys?: Array<string> }>;
}

/**
 * Delete Incidents or Review by Property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteEntityByProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<void> {
    const path = '/rest/operations/1.0/bulkByProperties';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIncidentByIdParams {
    path: { incidentId: string };
}

/**
 * Get a Incident by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIncidentById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIncidentByIdParams): Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; affectedComponents: Array<string>; summary: string; description: string; url: string; createdDate: string; lastUpdated: string; severity?: { level: "P1" | "P2" | "P3" | "P4" | "P5" | "unknown" }; status: "open" | "resolved" | "unknown"; associations?: Array<{ associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source"; values?: Array<string> }> }> {
    const path = `/rest/operations/1.0/incidents/${params.path.incidentId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; affectedComponents: Array<string>; summary: string; description: string; url: string; createdDate: string; lastUpdated: string; severity?: { level: "P1" | "P2" | "P3" | "P4" | "P5" | "unknown" }; status: "open" | "resolved" | "unknown"; associations?: Array<{ associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source"; values?: Array<string> }> }>;
}

export interface DeleteIncidentByIdParams {
    path: { incidentId: string };
}

/**
 * Delete a Incident by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIncidentById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIncidentByIdParams): Promise<void> {
    const path = `/rest/operations/1.0/incidents/${params.path.incidentId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetReviewByIdParams {
    path: { reviewId: string };
}

/**
 * Get a Review by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getReviewById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetReviewByIdParams): Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; reviews: Array<string>; summary: string; description: string; url: string; createdDate: string; lastUpdated: string; status: "in progress" | "outstanding actions" | "completed" | "unknown"; associations?: Array<{ associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source"; values?: Array<string> }> }> {
    const path = `/rest/operations/1.0/post-incident-reviews/${params.path.reviewId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; reviews: Array<string>; summary: string; description: string; url: string; createdDate: string; lastUpdated: string; status: "in progress" | "outstanding actions" | "completed" | "unknown"; associations?: Array<{ associationType?: "issueIdOrKeys" | "serviceIdOrKeys" | "ati:cloud:compass:event-source"; values?: Array<string> }> }>;
}

export interface DeleteReviewByIdParams {
    path: { reviewId: string };
}

/**
 * Delete a Review by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteReviewById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteReviewByIdParams): Promise<void> {
    const path = `/rest/operations/1.0/post-incident-reviews/${params.path.reviewId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SubmitComponentsParams {
    body?: { properties?: Record<string, string>; devopsComponents: Array<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; name: string; providerName?: string; description: string; url: string; avatarUrl: string; tier: "Tier 1" | "Tier 2" | "Tier 3" | "Tier 4"; componentType: "Service" | "Application" | "Library" | "Capability" | "Cloud resource" | "Data pipeline" | "Machine learning model" | "UI element" | "Website" | "Other"; lastUpdated: string }>; providerMetadata?: { product?: string } };
}

/**
 * Submit DevOps Components
 * @forge-scopes-asApp read:jira-work
 */
export async function submitComponents(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitComponentsParams): Promise<{ acceptedComponents?: Array<string>; failedComponents?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownProjectKeys?: Array<string> }> {
    const path = '/rest/devopscomponents/1.0/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ acceptedComponents?: Array<string>; failedComponents?: Record<string, Array<{ message: string; errorTraceId?: string }>>; unknownProjectKeys?: Array<string> }>;
}

/**
 * Delete DevOps Components by Property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteComponentsByProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<void> {
    const path = '/rest/devopscomponents/1.0/bulkByProperties';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetComponentByIdParams {
    path: { componentId: string };
}

/**
 * Get a Component by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getComponentById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetComponentByIdParams): Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; name: string; providerName?: string; description: string; url: string; avatarUrl: string; tier: "Tier 1" | "Tier 2" | "Tier 3" | "Tier 4"; componentType: "Service" | "Application" | "Library" | "Capability" | "Cloud resource" | "Data pipeline" | "Machine learning model" | "UI element" | "Website" | "Other"; lastUpdated: string }> {
    const path = `/rest/devopscomponents/1.0/devopscomponents/${params.path.componentId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ schemaVersion: "1.0"; id: string; updateSequenceNumber: number; name: string; providerName?: string; description: string; url: string; avatarUrl: string; tier: "Tier 1" | "Tier 2" | "Tier 3" | "Tier 4"; componentType: "Service" | "Application" | "Library" | "Capability" | "Cloud resource" | "Data pipeline" | "Machine learning model" | "UI element" | "Website" | "Other"; lastUpdated: string }>;
}

export interface DeleteComponentByIdParams {
    path: { componentId: string };
}

/**
 * Delete a Component by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteComponentById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteComponentByIdParams): Promise<void> {
    const path = `/rest/devopscomponents/1.0/devopscomponents/${params.path.componentId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}
