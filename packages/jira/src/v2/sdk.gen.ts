// @ts-nocheck
import { ForgeApiError } from "@forge-clients/core";
import type { ForgeAdapter, AuthContext } from "@forge-clients/core";
import type * as Types from "./types.gen.js";

// Generated by @forge-clients/generator — DO NOT EDIT
// Source: Jira Cloud REST API v2
/* eslint-disable */
export type * from './types.gen.js';

/**
 * Get announcement banner configuration
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBanner(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.AnnouncementBannerConfiguration> {
    const path = '/rest/api/2/announcementBanner';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AnnouncementBannerConfiguration>;
}

export interface SetBannerParams {
    body?: Types.AnnouncementBannerConfigurationUpdate;
}

/**
 * Update announcement banner configuration
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setBanner(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetBannerParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/announcementBanner';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetCustomFieldsConfigurationsParams {
    id?: Array<number>;
    fieldContextId?: Array<number>;
    issueId?: number;
    projectKeyOrId?: string;
    issueTypeId?: string;
    startAt?: number;
    maxResults?: number;
    body?: Types.ConfigurationsListParameters;
}

/**
 * Bulk get custom field configurations
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomFieldsConfigurations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCustomFieldsConfigurationsParams): Promise<Types.PageBeanBulkContextualConfiguration> {
    const path = '/rest/api/2/app/field/context/configuration/list';
    const queryParams = {
      id: params.id,
      fieldContextId: params.fieldContextId,
      issueId: params.issueId,
      projectKeyOrId: params.projectKeyOrId,
      issueTypeId: params.issueTypeId,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanBulkContextualConfiguration>;
}

export interface UpdateMultipleCustomFieldValuesParams {
    generateChangelog?: boolean;
    body?: Types.MultipleCustomFieldValuesUpdateDetails;
}

/**
 * Update custom fields
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateMultipleCustomFieldValues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateMultipleCustomFieldValuesParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/app/field/value';
    const queryParams = {
      generateChangelog: params.generateChangelog,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetCustomFieldConfigurationParams {
    path: { fieldIdOrKey: string };
    id?: Array<number>;
    fieldContextId?: Array<number>;
    issueId?: number;
    projectKeyOrId?: string;
    issueTypeId?: string;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get custom field configurations
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomFieldConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCustomFieldConfigurationParams): Promise<Types.PageBeanContextualConfiguration> {
    const path = `/rest/api/2/app/field/${params.path.fieldIdOrKey}/context/configuration`;
    const queryParams = {
      id: params.id,
      fieldContextId: params.fieldContextId,
      issueId: params.issueId,
      projectKeyOrId: params.projectKeyOrId,
      issueTypeId: params.issueTypeId,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanContextualConfiguration>;
}

export interface UpdateCustomFieldConfigurationParams {
    path: { fieldIdOrKey: string };
    body?: Types.CustomFieldConfigurations;
}

/**
 * Update custom field configurations
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCustomFieldConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateCustomFieldConfigurationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/app/field/${params.path.fieldIdOrKey}/context/configuration`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateCustomFieldValueParams {
    path: { fieldIdOrKey: string };
    generateChangelog?: boolean;
    body?: Types.CustomFieldValueUpdateDetails;
}

/**
 * Update custom field value
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCustomFieldValue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateCustomFieldValueParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/app/field/${params.path.fieldIdOrKey}/value`;
    const queryParams = {
      generateChangelog: params.generateChangelog,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetApplicationPropertyParams {
    key?: string;
    permissionLevel?: string;
    keyFilter?: string;
}

/**
 * Get application property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getApplicationProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetApplicationPropertyParams): Promise<Array<Types.ApplicationProperty>> {
    const path = '/rest/api/2/application-properties';
    const queryParams = {
      key: params.key,
      permissionLevel: params.permissionLevel,
      keyFilter: params.keyFilter,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ApplicationProperty>>;
}

/**
 * Get advanced settings
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getAdvancedSettings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ApplicationProperty>> {
    const path = '/rest/api/2/application-properties/advanced-settings';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ApplicationProperty>>;
}

export interface SetApplicationPropertyParams {
    path: { id: string };
    body?: Types.SimpleApplicationPropertyBean;
}

/**
 * Set application property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setApplicationProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetApplicationPropertyParams): Promise<Types.ApplicationProperty> {
    const path = `/rest/api/2/application-properties/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ApplicationProperty>;
}

/**
 * Get all application roles
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllApplicationRoles(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ApplicationRole>> {
    const path = '/rest/api/2/applicationrole';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ApplicationRole>>;
}

export interface GetApplicationRoleParams {
    path: { key: string };
}

/**
 * Get application role
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getApplicationRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetApplicationRoleParams): Promise<Types.ApplicationRole> {
    const path = `/rest/api/2/applicationrole/${params.path.key}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ApplicationRole>;
}

export interface GetAttachmentContentParams {
    path: { id: string };
    redirect?: boolean;
}

/**
 * Get attachment content
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentContent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAttachmentContentParams): Promise<void> {
    const path = `/rest/api/2/attachment/content/${params.path.id}`;
    const queryParams = {
      redirect: params.redirect,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get Jira attachment settings
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentMeta(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.AttachmentSettings> {
    const path = '/rest/api/2/attachment/meta';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AttachmentSettings>;
}

export interface GetAttachmentThumbnailParams {
    path: { id: string };
    redirect?: boolean;
    fallbackToDefault?: boolean;
    width?: number;
    height?: number;
}

/**
 * Get attachment thumbnail
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachmentThumbnail(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAttachmentThumbnailParams): Promise<void> {
    const path = `/rest/api/2/attachment/thumbnail/${params.path.id}`;
    const queryParams = {
      redirect: params.redirect,
      fallbackToDefault: params.fallbackToDefault,
      width: params.width,
      height: params.height,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAttachmentParams {
    path: { id: string };
}

/**
 * Get attachment metadata
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAttachment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAttachmentParams): Promise<Types.AttachmentMetadata> {
    const path = `/rest/api/2/attachment/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AttachmentMetadata>;
}

export interface RemoveAttachmentParams {
    path: { id: string };
}

/**
 * Delete attachment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeAttachment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveAttachmentParams): Promise<void> {
    const path = `/rest/api/2/attachment/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface ExpandAttachmentForHumansParams {
    path: { id: string };
}

/**
 * Get all metadata for an expanded attachment
 * @forge-scopes-asApp read:jira-work
 */
export async function expandAttachmentForHumans(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ExpandAttachmentForHumansParams): Promise<Types.AttachmentArchiveMetadataReadable> {
    const path = `/rest/api/2/attachment/${params.path.id}/expand/human`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AttachmentArchiveMetadataReadable>;
}

export interface ExpandAttachmentForMachinesParams {
    path: { id: string };
}

/**
 * Get contents metadata for an expanded attachment
 * @forge-scopes-asApp read:jira-work
 */
export async function expandAttachmentForMachines(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ExpandAttachmentForMachinesParams): Promise<Types.AttachmentArchiveImpl> {
    const path = `/rest/api/2/attachment/${params.path.id}/expand/raw`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AttachmentArchiveImpl>;
}

export interface GetAuditRecordsParams {
    offset?: number;
    limit?: number;
    filter?: string;
    from?: string;
    to?: string;
}

/**
 * Get audit records
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAuditRecords(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAuditRecordsParams): Promise<Types.AuditRecords> {
    const path = '/rest/api/2/auditing/record';
    const queryParams = {
      offset: params.offset,
      limit: params.limit,
      filter: params.filter,
      from: params.from,
      to: params.to,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AuditRecords>;
}

export interface GetAllSystemAvatarsParams {
    path: { type: string };
}

/**
 * Get system avatars by type
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllSystemAvatars(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllSystemAvatarsParams): Promise<Types.SystemAvatars> {
    const path = `/rest/api/2/avatar/${params.path.type}/system`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SystemAvatars>;
}

export interface GetBulkChangelogsParams {
    body?: Types.BulkChangelogRequestBean;
}

/**
 * Bulk fetch changelogs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBulkChangelogs(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBulkChangelogsParams): Promise<Types.BulkChangelogResponseBean> {
    const path = '/rest/api/2/changelog/bulkfetch';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BulkChangelogResponseBean>;
}

export interface GetAllUserDataClassificationLevelsParams {
    status?: Array<"PUBLISHED" | "ARCHIVED" | "DRAFT">;
    orderBy?: "rank" | "-rank" | "+rank";
}

/**
 * Get all classification levels
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getAllUserDataClassificationLevels(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllUserDataClassificationLevelsParams): Promise<Types.DataClassificationLevelsBean> {
    const path = '/rest/api/2/classification-levels';
    const queryParams = {
      status: params.status,
      orderBy: params.orderBy,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DataClassificationLevelsBean>;
}

export interface GetCommentsByIdsParams {
    expand?: string;
    body?: Types.IssueCommentListRequestBean;
}

/**
 * Get comments by IDs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCommentsByIds(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCommentsByIdsParams): Promise<Types.PageBeanComment> {
    const path = '/rest/api/2/comment/list';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanComment>;
}

export interface GetCommentPropertyKeysParams {
    path: { commentId: string };
}

/**
 * Get comment property keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCommentPropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCommentPropertyKeysParams): Promise<Types.PropertyKeys> {
    const path = `/rest/api/2/comment/${params.path.commentId}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetCommentPropertyParams {
    path: { commentId: string; propertyKey: string };
}

/**
 * Get comment property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCommentProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCommentPropertyParams): Promise<Types.EntityProperty> {
    const path = `/rest/api/2/comment/${params.path.commentId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetCommentPropertyParams {
    path: { commentId: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set comment property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setCommentProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetCommentPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/comment/${params.path.commentId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteCommentPropertyParams {
    path: { commentId: string; propertyKey: string };
}

/**
 * Delete comment property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteCommentProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteCommentPropertyParams): Promise<void> {
    const path = `/rest/api/2/comment/${params.path.commentId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface FindComponentsForProjectsParams {
    projectIdsOrKeys?: Array<string>;
    startAt?: number;
    maxResults?: number;
    orderBy?: "description" | "-description" | "+description" | "name" | "-name" | "+name";
    query?: string;
}

/**
 * Find components for projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function findComponentsForProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindComponentsForProjectsParams): Promise<Types.PageBean2ComponentJsonBean> {
    const path = '/rest/api/2/component';
    const queryParams = {
      projectIdsOrKeys: params.projectIdsOrKeys,
      startAt: params.startAt,
      maxResults: params.maxResults,
      orderBy: params.orderBy,
      query: params.query,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBean2ComponentJsonBean>;
}

export interface CreateComponentParams {
    body?: Types.ProjectComponent;
}

/**
 * Create component
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createComponent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateComponentParams): Promise<Types.ProjectComponent> {
    const path = '/rest/api/2/component';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectComponent>;
}

export interface GetComponentParams {
    path: { id: string };
}

/**
 * Get component
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getComponent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetComponentParams): Promise<Types.ProjectComponent> {
    const path = `/rest/api/2/component/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectComponent>;
}

export interface UpdateComponentParams {
    path: { id: string };
    body?: Types.ProjectComponent;
}

/**
 * Update component
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateComponent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateComponentParams): Promise<Types.ProjectComponent> {
    const path = `/rest/api/2/component/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectComponent>;
}

export interface DeleteComponentParams {
    path: { id: string };
    moveIssuesTo?: string;
}

/**
 * Delete component
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteComponent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteComponentParams): Promise<void> {
    const path = `/rest/api/2/component/${params.path.id}`;
    const queryParams = {
      moveIssuesTo: params.moveIssuesTo,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetComponentRelatedIssuesParams {
    path: { id: string };
}

/**
 * Get component issues count
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getComponentRelatedIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetComponentRelatedIssuesParams): Promise<Types.ComponentIssuesCount> {
    const path = `/rest/api/2/component/${params.path.id}/relatedIssueCounts`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ComponentIssuesCount>;
}

export interface GetFieldAssociationSchemesParams {
    projectId?: Array<number>;
    query?: string;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get field schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFieldAssociationSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldAssociationSchemesParams): Promise<Types.PageBean2GetFieldAssociationSchemeResponse> {
    const path = '/rest/api/2/config/fieldschemes';
    const queryParams = {
      projectId: params.projectId,
      query: params.query,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBean2GetFieldAssociationSchemeResponse>;
}

export interface CreateFieldAssociationSchemeParams {
    body?: Types.CreateFieldAssociationSchemeRequest;
}

/**
 * Create field scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createFieldAssociationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateFieldAssociationSchemeParams): Promise<Types.CreateFieldAssociationSchemeResponse> {
    const path = '/rest/api/2/config/fieldschemes';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CreateFieldAssociationSchemeResponse>;
}

export interface UpdateFieldsAssociatedWithSchemesParams {
    body?: Record<string, Array<Types.UpdateFieldAssociationsRequestItem>>;
}

/**
 * Update fields associated with field schemes
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateFieldsAssociatedWithSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateFieldsAssociatedWithSchemesParams): Promise<Types.FieldSchemeToFieldsResponse> {
    const path = '/rest/api/2/config/fieldschemes/fields';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FieldSchemeToFieldsResponse>;
}

export interface RemoveFieldsAssociatedWithSchemesParams {
    body?: Record<string, Types.RemoveFieldAssociationsRequestItem>;
}

/**
 * Remove fields associated with field schemes
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeFieldsAssociatedWithSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveFieldsAssociatedWithSchemesParams): Promise<Types.MinimalFieldSchemeToFieldsResponse> {
    const path = '/rest/api/2/config/fieldschemes/fields';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.MinimalFieldSchemeToFieldsResponse>;
}

export interface UpdateFieldAssociationSchemeItemParametersParams {
    body?: Record<string, Array<Types.UpdateFieldSchemeParametersRequest>>;
}

/**
 * Update field parameters
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateFieldAssociationSchemeItemParameters(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateFieldAssociationSchemeItemParametersParams): Promise<Types.UpdateFieldSchemeParametersResponse> {
    const path = '/rest/api/2/config/fieldschemes/fields/parameters';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UpdateFieldSchemeParametersResponse>;
}

export interface RemoveFieldAssociationSchemeItemParametersParams {
    body?: Record<string, Array<Types.ParameterRemovalDetails>>;
}

/**
 * Remove field parameters
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeFieldAssociationSchemeItemParameters(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveFieldAssociationSchemeItemParametersParams): Promise<void> {
    const path = '/rest/api/2/config/fieldschemes/fields/parameters';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetProjectsWithFieldSchemesParams {
    startAt?: number;
    maxResults?: number;
    projectId: Array<number>;
}

/**
 * Get projects with field schemes
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectsWithFieldSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectsWithFieldSchemesParams): Promise<Types.PageBean2GetProjectsWithFieldSchemesResponse> {
    const path = '/rest/api/2/config/fieldschemes/projects';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBean2GetProjectsWithFieldSchemesResponse>;
}

export interface AssociateProjectsToFieldAssociationSchemesParams {
    body?: Record<string, Types.FieldSchemeToProjectsRequest>;
}

/**
 * Associate projects to field schemes
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function associateProjectsToFieldAssociationSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssociateProjectsToFieldAssociationSchemesParams): Promise<Types.FieldSchemeToProjectsResponse> {
    const path = '/rest/api/2/config/fieldschemes/projects';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FieldSchemeToProjectsResponse>;
}

export interface GetFieldAssociationSchemeByIdParams {
    path: { id: string };
}

/**
 * Get field scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFieldAssociationSchemeById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldAssociationSchemeByIdParams): Promise<Types.GetFieldAssociationSchemeByIdResponse> {
    const path = `/rest/api/2/config/fieldschemes/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GetFieldAssociationSchemeByIdResponse>;
}

export interface UpdateFieldAssociationSchemeParams {
    path: { id: string };
    body?: Types.UpdateFieldAssociationSchemeRequest;
}

/**
 * Update field scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateFieldAssociationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateFieldAssociationSchemeParams): Promise<Types.UpdateFieldAssociationSchemeResponse> {
    const path = `/rest/api/2/config/fieldschemes/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UpdateFieldAssociationSchemeResponse>;
}

export interface DeleteFieldAssociationSchemeParams {
    path: { id: string };
}

/**
 * Delete a field scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFieldAssociationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteFieldAssociationSchemeParams): Promise<Types.DeleteFieldAssociationSchemeResponse> {
    const path = `/rest/api/2/config/fieldschemes/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DeleteFieldAssociationSchemeResponse>;
}

export interface CloneFieldAssociationSchemeParams {
    path: { id: string };
    body?: Types.CreateFieldAssociationSchemeRequest;
}

/**
 * Clone field scheme
 * @forge-scopes-asApp read:jira-work
 */
export async function cloneFieldAssociationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CloneFieldAssociationSchemeParams): Promise<Types.CreateFieldAssociationSchemeResponse> {
    const path = `/rest/api/2/config/fieldschemes/${params.path.id}/clone`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CreateFieldAssociationSchemeResponse>;
}

export interface SearchFieldAssociationSchemeFieldsParams {
    path: { id: string };
    startAt?: number;
    maxResults?: number;
    fieldId?: Array<string>;
}

/**
 * Search field scheme fields
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchFieldAssociationSchemeFields(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchFieldAssociationSchemeFieldsParams): Promise<Types.PageBean2FieldAssociationSchemeFieldSearchResult> {
    const path = `/rest/api/2/config/fieldschemes/${params.path.id}/fields`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      fieldId: params.fieldId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBean2FieldAssociationSchemeFieldSearchResult>;
}

export interface GetFieldAssociationSchemeItemParametersParams {
    path: { id: string; fieldId: string };
}

/**
 * Get field parameters
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFieldAssociationSchemeItemParameters(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldAssociationSchemeItemParametersParams): Promise<Types.GetFieldAssociationParametersResponse> {
    const path = `/rest/api/2/config/fieldschemes/${params.path.id}/fields/${params.path.fieldId}/parameters`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GetFieldAssociationParametersResponse>;
}

export interface SearchFieldAssociationSchemeProjectsParams {
    path: { id: string };
    startAt?: number;
    maxResults?: number;
    projectId?: Array<number>;
}

/**
 * Search field scheme projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function searchFieldAssociationSchemeProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchFieldAssociationSchemeProjectsParams): Promise<Types.PageBean2FieldAssociationSchemeProjectSearchResult> {
    const path = `/rest/api/2/config/fieldschemes/${params.path.id}/projects`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBean2FieldAssociationSchemeProjectSearchResult>;
}

/**
 * Get global settings
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.Configuration> {
    const path = '/rest/api/2/configuration';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Configuration>;
}

/**
 * Get selected time tracking provider
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSelectedTimeTrackingImplementation(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.TimeTrackingProvider> {
    const path = '/rest/api/2/configuration/timetracking';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.TimeTrackingProvider>;
}

export interface SelectTimeTrackingImplementationParams {
    body?: Types.TimeTrackingProvider;
}

/**
 * Select time tracking provider
 * @forge-scopes-asApp read:jira-work
 */
export async function selectTimeTrackingImplementation(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SelectTimeTrackingImplementationParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/configuration/timetracking';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

/**
 * Get all time tracking providers
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvailableTimeTrackingImplementations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.TimeTrackingProvider>> {
    const path = '/rest/api/2/configuration/timetracking/list';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.TimeTrackingProvider>>;
}

/**
 * Get time tracking settings
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getSharedTimeTrackingConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.TimeTrackingConfiguration> {
    const path = '/rest/api/2/configuration/timetracking/options';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.TimeTrackingConfiguration>;
}

export interface SetSharedTimeTrackingConfigurationParams {
    body?: Types.TimeTrackingConfiguration;
}

/**
 * Set time tracking settings
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 */
export async function setSharedTimeTrackingConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetSharedTimeTrackingConfigurationParams): Promise<Types.TimeTrackingConfiguration> {
    const path = '/rest/api/2/configuration/timetracking/options';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.TimeTrackingConfiguration>;
}

export interface GetCustomFieldOptionParams {
    path: { id: string };
}

/**
 * Get custom field option
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCustomFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCustomFieldOptionParams): Promise<Types.CustomFieldOption> {
    const path = `/rest/api/2/customFieldOption/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomFieldOption>;
}

export interface GetAllDashboardsParams {
    filter?: "my" | "favourite";
    startAt?: number;
    maxResults?: number;
}

/**
 * Get all dashboards
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllDashboards(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllDashboardsParams): Promise<Types.PageOfDashboards> {
    const path = '/rest/api/2/dashboard';
    const queryParams = {
      filter: params.filter,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageOfDashboards>;
}

export interface CreateDashboardParams {
    extendAdminPermissions?: boolean;
    body?: Types.DashboardDetails;
}

/**
 * Create dashboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createDashboard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateDashboardParams): Promise<Types.Dashboard> {
    const path = '/rest/api/2/dashboard';
    const queryParams = {
      extendAdminPermissions: params.extendAdminPermissions,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Dashboard>;
}

export interface BulkEditDashboardsParams {
    body?: Types.BulkEditShareableEntityRequest;
}

/**
 * Bulk edit dashboards
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkEditDashboards(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkEditDashboardsParams): Promise<Types.BulkEditShareableEntityResponse> {
    const path = '/rest/api/2/dashboard/bulk/edit';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BulkEditShareableEntityResponse>;
}

/**
 * Get available gadgets
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllAvailableDashboardGadgets(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.AvailableDashboardGadgetsResponse> {
    const path = '/rest/api/2/dashboard/gadgets';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AvailableDashboardGadgetsResponse>;
}

export interface GetDashboardsPaginatedParams {
    dashboardName?: string;
    accountId?: string;
    owner?: string;
    groupname?: string;
    groupId?: string;
    projectId?: number;
    orderBy?: "description" | "-description" | "+description" | "favorite_count" | "-favorite_count" | "+favorite_count" | "id" | "-id" | "+id" | "is_favorite" | "-is_favorite" | "+is_favorite" | "name" | "-name" | "+name" | "owner" | "-owner" | "+owner";
    startAt?: number;
    maxResults?: number;
    status?: "active" | "archived" | "deleted";
    expand?: string;
}

/**
 * Search for dashboards
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDashboardsPaginated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDashboardsPaginatedParams): Promise<Types.PageBeanDashboard> {
    const path = '/rest/api/2/dashboard/search';
    const queryParams = {
      dashboardName: params.dashboardName,
      accountId: params.accountId,
      owner: params.owner,
      groupname: params.groupname,
      groupId: params.groupId,
      projectId: params.projectId,
      orderBy: params.orderBy,
      startAt: params.startAt,
      maxResults: params.maxResults,
      status: params.status,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanDashboard>;
}

export interface GetAllGadgetsParams {
    path: { dashboardId: string };
    moduleKey?: Array<string>;
    uri?: Array<string>;
    gadgetId?: Array<number>;
}

/**
 * Get gadgets
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllGadgets(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllGadgetsParams): Promise<Types.DashboardGadgetResponse> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/gadget`;
    const queryParams = {
      moduleKey: params.moduleKey,
      uri: params.uri,
      gadgetId: params.gadgetId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DashboardGadgetResponse>;
}

export interface AddGadgetParams {
    path: { dashboardId: string };
    body?: Types.DashboardGadgetSettings;
}

/**
 * Add gadget to dashboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addGadget(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddGadgetParams): Promise<Types.DashboardGadget> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/gadget`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DashboardGadget>;
}

export interface UpdateGadgetParams {
    path: { dashboardId: string; gadgetId: string };
    body?: Types.DashboardGadgetUpdateRequest;
}

/**
 * Update gadget on dashboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateGadget(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateGadgetParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/gadget/${params.path.gadgetId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveGadgetParams {
    path: { dashboardId: string; gadgetId: string };
}

/**
 * Remove gadget from dashboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeGadget(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveGadgetParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/gadget/${params.path.gadgetId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetDashboardItemPropertyKeysParams {
    path: { dashboardId: string; itemId: string };
}

/**
 * Get dashboard item property keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDashboardItemPropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDashboardItemPropertyKeysParams): Promise<Types.PropertyKeys> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/items/${params.path.itemId}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetDashboardItemPropertyParams {
    path: { dashboardId: string; itemId: string; propertyKey: string };
}

/**
 * Get dashboard item property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDashboardItemProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDashboardItemPropertyParams): Promise<Types.EntityProperty> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/items/${params.path.itemId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetDashboardItemPropertyParams {
    path: { dashboardId: string; itemId: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set dashboard item property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setDashboardItemProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetDashboardItemPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/items/${params.path.itemId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteDashboardItemPropertyParams {
    path: { dashboardId: string; itemId: string; propertyKey: string };
}

/**
 * Delete dashboard item property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDashboardItemProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteDashboardItemPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/dashboard/${params.path.dashboardId}/items/${params.path.itemId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetDashboardParams {
    path: { id: string };
}

/**
 * Get dashboard
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDashboard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDashboardParams): Promise<Types.Dashboard> {
    const path = `/rest/api/2/dashboard/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Dashboard>;
}

export interface UpdateDashboardParams {
    path: { id: string };
    extendAdminPermissions?: boolean;
    body?: Types.DashboardDetails;
}

/**
 * Update dashboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateDashboard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateDashboardParams): Promise<Types.Dashboard> {
    const path = `/rest/api/2/dashboard/${params.path.id}`;
    const queryParams = {
      extendAdminPermissions: params.extendAdminPermissions,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Dashboard>;
}

export interface DeleteDashboardParams {
    path: { id: string };
}

/**
 * Delete dashboard
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDashboard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteDashboardParams): Promise<void> {
    const path = `/rest/api/2/dashboard/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface CopyDashboardParams {
    path: { id: string };
    extendAdminPermissions?: boolean;
    body?: Types.DashboardDetails;
}

/**
 * Copy dashboard
 * @forge-scopes-asApp read:jira-work
 */
export async function copyDashboard(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CopyDashboardParams): Promise<Types.Dashboard> {
    const path = `/rest/api/2/dashboard/${params.path.id}/copy`;
    const queryParams = {
      extendAdminPermissions: params.extendAdminPermissions,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Dashboard>;
}

/**
 * Get data policy for the workspace
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPolicy(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.WorkspaceDataPolicy> {
    const path = '/rest/api/2/data-policy';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkspaceDataPolicy>;
}

export interface GetPoliciesParams {
    ids?: string;
}

/**
 * Get data policy for projects
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPolicies(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPoliciesParams): Promise<Types.ProjectDataPolicies> {
    const path = '/rest/api/2/data-policy/project';
    const queryParams = {
      ids: params.ids,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectDataPolicies>;
}

/**
 * Get events
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getEvents(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.IssueEvent>> {
    const path = '/rest/api/2/events';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.IssueEvent>>;
}

export interface AnalyseExpressionParams {
    check?: "syntax" | "type" | "complexity";
    body?: Types.JiraExpressionForAnalysis;
}

/**
 * Analyse Jira expression
 * @forge-scopes-asApp read:jira-work
 */
export async function analyseExpression(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AnalyseExpressionParams): Promise<Types.JiraExpressionsAnalysis> {
    const path = '/rest/api/2/expression/analyse';
    const queryParams = {
      check: params.check,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JiraExpressionsAnalysis>;
}

export interface EvaluateJiraExpressionParams {
    expand?: string;
    body?: Types.JiraExpressionEvalRequestBean;
}

/**
 * Currently being removed. Evaluate Jira expression
 * @forge-scopes-asApp read:jira-work
 * @deprecated
 */
export async function evaluateJiraExpression(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: EvaluateJiraExpressionParams): Promise<Types.JiraExpressionResult> {
    const path = '/rest/api/2/expression/eval';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JiraExpressionResult>;
}

export interface EvaluateJSISJiraExpressionParams {
    expand?: string;
    body?: Types.JiraExpressionEvaluateRequestBean;
}

/**
 * Evaluate Jira expression using enhanced search API
 * @forge-scopes-asApp read:jira-work
 */
export async function evaluateJSISJiraExpression(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: EvaluateJSISJiraExpressionParams): Promise<Types.JExpEvaluateJiraExpressionResultBean> {
    const path = '/rest/api/2/expression/evaluate';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JExpEvaluateJiraExpressionResultBean>;
}

/**
 * Get fields
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFields(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.FieldDetails>> {
    const path = '/rest/api/2/field';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.FieldDetails>>;
}

export interface CreateCustomFieldParams {
    body?: Types.CustomFieldDefinitionJsonBean;
}

/**
 * Create custom field
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCustomField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateCustomFieldParams): Promise<Types.FieldDetails> {
    const path = '/rest/api/2/field';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FieldDetails>;
}

export interface CreateAssociationsParams {
    body?: Types.FieldAssociationsRequest;
}

/**
 * Create associations
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createAssociations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateAssociationsParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/field/association';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveAssociationsParams {
    body?: Types.FieldAssociationsRequest;
}

/**
 * Remove associations
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeAssociations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveAssociationsParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/field/association';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetFieldsPaginatedParams {
    startAt?: number;
    maxResults?: number;
    type?: Array<"custom" | "system">;
    id?: Array<string>;
    query?: string;
    orderBy?: "contextsCount" | "-contextsCount" | "+contextsCount" | "lastUsed" | "-lastUsed" | "+lastUsed" | "name" | "-name" | "+name" | "screensCount" | "-screensCount" | "+screensCount" | "projectsCount" | "-projectsCount" | "+projectsCount";
    expand?: string;
    projectIds?: Array<number>;
}

/**
 * Get fields paginated
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFieldsPaginated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldsPaginatedParams): Promise<Types.PageBeanField> {
    const path = '/rest/api/2/field/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      type: params.type,
      id: params.id,
      query: params.query,
      orderBy: params.orderBy,
      expand: params.expand,
      projectIds: params.projectIds,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanField>;
}

export interface GetTrashedFieldsPaginatedParams {
    startAt?: number;
    maxResults?: number;
    id?: Array<string>;
    query?: string;
    expand?: "name" | "-name" | "+name" | "trashDate" | "-trashDate" | "+trashDate" | "plannedDeletionDate" | "-plannedDeletionDate" | "+plannedDeletionDate" | "projectsCount" | "-projectsCount" | "+projectsCount";
    orderBy?: string;
}

/**
 * Get fields in trash paginated
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTrashedFieldsPaginated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetTrashedFieldsPaginatedParams): Promise<Types.PageBeanField> {
    const path = '/rest/api/2/field/search/trashed';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      query: params.query,
      expand: params.expand,
      orderBy: params.orderBy,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanField>;
}

export interface UpdateCustomFieldParams {
    path: { fieldId: string };
    body?: Types.UpdateCustomFieldDetails;
}

/**
 * Update custom field
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCustomField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateCustomFieldParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetFieldProjectAssociationsParams {
    path: { fieldId: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get field project associations
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getFieldProjectAssociations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldProjectAssociationsParams): Promise<Types.PageBeanFieldProjectAssociation> {
    const path = `/rest/api/2/field/${params.path.fieldId}/association/project`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanFieldProjectAssociation>;
}

export interface GetContextsForFieldParams {
    path: { fieldId: string };
    isAnyIssueType?: boolean;
    isGlobalContext?: boolean;
    contextId?: Array<number>;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get custom field contexts
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getContextsForField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContextsForFieldParams): Promise<Types.PageBeanCustomFieldContext> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context`;
    const queryParams = {
      isAnyIssueType: params.isAnyIssueType,
      isGlobalContext: params.isGlobalContext,
      contextId: params.contextId,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanCustomFieldContext>;
}

export interface CreateCustomFieldContextParams {
    path: { fieldId: string };
    body?: Types.CreateCustomFieldContext;
}

/**
 * Create custom field context
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCustomFieldContext(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateCustomFieldContextParams): Promise<Types.CreateCustomFieldContext> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CreateCustomFieldContext>;
}

export interface GetDefaultValuesParams {
    path: { fieldId: string };
    contextId?: Array<number>;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get custom field contexts default values
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getDefaultValues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDefaultValuesParams): Promise<Types.PageBeanCustomFieldContextDefaultValue> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/defaultValue`;
    const queryParams = {
      contextId: params.contextId,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanCustomFieldContextDefaultValue>;
}

export interface SetDefaultValuesParams {
    path: { fieldId: string };
    body?: Types.CustomFieldContextDefaultValueUpdate;
}

/**
 * Set custom field contexts default values
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function setDefaultValues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetDefaultValuesParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/defaultValue`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetIssueTypeMappingsForContextsParams {
    path: { fieldId: string };
    contextId?: Array<number>;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get issue types for custom field context
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueTypeMappingsForContexts(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypeMappingsForContextsParams): Promise<Types.PageBeanIssueTypeToContextMapping> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/issuetypemapping`;
    const queryParams = {
      contextId: params.contextId,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueTypeToContextMapping>;
}

export interface GetCustomFieldContextsForProjectsAndIssueTypesParams {
    path: { fieldId: string };
    startAt?: number;
    maxResults?: number;
    body?: Types.ProjectIssueTypeMappings;
}

/**
 * Get custom field contexts for projects and issue types
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getCustomFieldContextsForProjectsAndIssueTypes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCustomFieldContextsForProjectsAndIssueTypesParams): Promise<Types.PageBeanContextForProjectAndIssueType> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/mapping`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanContextForProjectAndIssueType>;
}

export interface GetProjectContextMappingParams {
    path: { fieldId: string };
    contextId?: Array<number>;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get project mappings for custom field context
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectContextMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectContextMappingParams): Promise<Types.PageBeanCustomFieldContextProjectMapping> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/projectmapping`;
    const queryParams = {
      contextId: params.contextId,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanCustomFieldContextProjectMapping>;
}

export interface UpdateCustomFieldContextParams {
    path: { fieldId: string; contextId: string };
    body?: Types.CustomFieldContextUpdateDetails;
}

/**
 * Update custom field context
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCustomFieldContext(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateCustomFieldContextParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteCustomFieldContextParams {
    path: { fieldId: string; contextId: string };
}

/**
 * Delete custom field context
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteCustomFieldContext(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteCustomFieldContextParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface AddIssueTypesToContextParams {
    path: { fieldId: string; contextId: string };
    body?: Types.IssueTypeIds;
}

/**
 * Add issue types to context
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addIssueTypesToContext(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddIssueTypesToContextParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/issuetype`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveIssueTypesFromContextParams {
    path: { fieldId: string; contextId: string };
    body?: Types.IssueTypeIds;
}

/**
 * Remove issue types from context
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeIssueTypesFromContext(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveIssueTypesFromContextParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/issuetype/remove`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetOptionsForContextParams {
    path: { fieldId: string; contextId: string };
    optionId?: number;
    onlyOptions?: boolean;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get custom field options (context)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getOptionsForContext(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetOptionsForContextParams): Promise<Types.PageBeanCustomFieldContextOption> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/option`;
    const queryParams = {
      optionId: params.optionId,
      onlyOptions: params.onlyOptions,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanCustomFieldContextOption>;
}

export interface CreateCustomFieldOptionParams {
    path: { fieldId: string; contextId: string };
    body?: Types.BulkCustomFieldOptionCreateRequest;
}

/**
 * Create custom field options (context)
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createCustomFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateCustomFieldOptionParams): Promise<Types.CustomFieldCreatedContextOptionsList> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/option`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomFieldCreatedContextOptionsList>;
}

export interface UpdateCustomFieldOptionParams {
    path: { fieldId: string; contextId: string };
    body?: Types.BulkCustomFieldOptionUpdateRequest;
}

/**
 * Update custom field options (context)
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateCustomFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateCustomFieldOptionParams): Promise<Types.CustomFieldUpdatedContextOptionsList> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/option`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CustomFieldUpdatedContextOptionsList>;
}

export interface ReorderCustomFieldOptionsParams {
    path: { fieldId: string; contextId: string };
    body?: Types.OrderOfCustomFieldOptions;
}

/**
 * Reorder custom field options (context)
 * @forge-scopes-asApp read:jira-work
 */
export async function reorderCustomFieldOptions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReorderCustomFieldOptionsParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/option/move`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteCustomFieldOptionParams {
    path: { fieldId: string; contextId: string; optionId: string };
}

/**
 * Delete custom field options (context)
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteCustomFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteCustomFieldOptionParams): Promise<void> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/option/${params.path.optionId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface ReplaceCustomFieldOptionParams {
    path: { fieldId: string; optionId: string; contextId: string };
    replaceWith?: number;
    jql?: string;
}

/**
 * Replace custom field options
 * @forge-scopes-asApp read:jira-work
 */
export async function replaceCustomFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReplaceCustomFieldOptionParams): Promise<void> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/option/${params.path.optionId}/issue`;
    const queryParams = {
      replaceWith: params.replaceWith,
      jql: params.jql,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface AssignProjectsToCustomFieldContextParams {
    path: { fieldId: string; contextId: string };
    body?: Types.ProjectIds;
}

/**
 * Assign custom field context to projects
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function assignProjectsToCustomFieldContext(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssignProjectsToCustomFieldContextParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/project`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveCustomFieldContextFromProjectsParams {
    path: { fieldId: string; contextId: string };
    body?: Types.ProjectIds;
}

/**
 * Remove custom field context from projects
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function removeCustomFieldContextFromProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveCustomFieldContextFromProjectsParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldId}/context/${params.path.contextId}/project/remove`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetContextsForFieldDeprecatedParams {
    path: { fieldId: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get contexts for a field
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getContextsForFieldDeprecated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetContextsForFieldDeprecatedParams): Promise<Types.PageBeanContext> {
    const path = `/rest/api/2/field/${params.path.fieldId}/contexts`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanContext>;
}

export interface GetScreensForFieldParams {
    path: { fieldId: string };
    startAt?: number;
    maxResults?: number;
    expand?: string;
}

/**
 * Get screens for a field
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getScreensForField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetScreensForFieldParams): Promise<Types.PageBeanScreenWithTab> {
    const path = `/rest/api/2/field/${params.path.fieldId}/screens`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanScreenWithTab>;
}

export interface GetAllIssueFieldOptionsParams {
    path: { fieldKey: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get all issue field options
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllIssueFieldOptions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllIssueFieldOptionsParams): Promise<Types.PageBeanIssueFieldOption> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueFieldOption>;
}

export interface CreateIssueFieldOptionParams {
    path: { fieldKey: string };
    body?: Types.IssueFieldOptionCreateBean;
}

/**
 * Create issue field option
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssueFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueFieldOptionParams): Promise<Types.IssueFieldOption> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueFieldOption>;
}

export interface GetSelectableIssueFieldOptionsParams {
    path: { fieldKey: string };
    startAt?: number;
    maxResults?: number;
    projectId?: number;
}

/**
 * Get selectable issue field options
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSelectableIssueFieldOptions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSelectableIssueFieldOptionsParams): Promise<Types.PageBeanIssueFieldOption> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option/suggestions/edit`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueFieldOption>;
}

export interface GetVisibleIssueFieldOptionsParams {
    path: { fieldKey: string };
    startAt?: number;
    maxResults?: number;
    projectId?: number;
}

/**
 * Get visible issue field options
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getVisibleIssueFieldOptions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetVisibleIssueFieldOptionsParams): Promise<Types.PageBeanIssueFieldOption> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option/suggestions/search`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueFieldOption>;
}

export interface GetIssueFieldOptionParams {
    path: { fieldKey: string; optionId: string };
}

/**
 * Get issue field option
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueFieldOptionParams): Promise<Types.IssueFieldOption> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option/${params.path.optionId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueFieldOption>;
}

export interface UpdateIssueFieldOptionParams {
    path: { fieldKey: string; optionId: string };
    body?: Types.IssueFieldOption;
}

/**
 * Update issue field option
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateIssueFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateIssueFieldOptionParams): Promise<Types.IssueFieldOption> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option/${params.path.optionId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueFieldOption>;
}

export interface DeleteIssueFieldOptionParams {
    path: { fieldKey: string; optionId: string };
}

/**
 * Delete issue field option
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueFieldOptionParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option/${params.path.optionId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface ReplaceIssueFieldOptionParams {
    path: { fieldKey: string; optionId: string };
    replaceWith?: number;
    jql?: string;
    overrideScreenSecurity?: boolean;
    overrideEditableFlag?: boolean;
}

/**
 * Replace issue field option
 * @forge-scopes-asApp read:jira-work
 */
export async function replaceIssueFieldOption(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReplaceIssueFieldOptionParams): Promise<void> {
    const path = `/rest/api/2/field/${params.path.fieldKey}/option/${params.path.optionId}/issue`;
    const queryParams = {
      replaceWith: params.replaceWith,
      jql: params.jql,
      overrideScreenSecurity: params.overrideScreenSecurity,
      overrideEditableFlag: params.overrideEditableFlag,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface DeleteCustomFieldParams {
    path: { id: string };
}

/**
 * Delete custom field
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteCustomField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteCustomFieldParams): Promise<void> {
    const path = `/rest/api/2/field/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RestoreCustomFieldParams {
    path: { id: string };
}

/**
 * Restore custom field from trash
 * @forge-scopes-asApp read:jira-work
 */
export async function restoreCustomField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RestoreCustomFieldParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.id}/restore`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface TrashCustomFieldParams {
    path: { id: string };
}

/**
 * Move custom field to trash
 * @forge-scopes-asApp read:jira-work
 */
export async function trashCustomField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: TrashCustomFieldParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/field/${params.path.id}/trash`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetAllFieldConfigurationsParams {
    startAt?: number;
    maxResults?: number;
    id?: Array<number>;
    isDefault?: boolean;
    query?: string;
}

/**
 * Get all field configurations
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getAllFieldConfigurations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllFieldConfigurationsParams): Promise<Types.PageBeanFieldConfigurationDetails> {
    const path = '/rest/api/2/fieldconfiguration';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      isDefault: params.isDefault,
      query: params.query,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanFieldConfigurationDetails>;
}

export interface CreateFieldConfigurationParams {
    body?: Types.FieldConfigurationDetails;
}

/**
 * Create field configuration
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function createFieldConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateFieldConfigurationParams): Promise<Types.FieldConfiguration> {
    const path = '/rest/api/2/fieldconfiguration';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FieldConfiguration>;
}

export interface UpdateFieldConfigurationParams {
    path: { id: string };
    body?: Types.FieldConfigurationDetails;
}

/**
 * Update field configuration
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function updateFieldConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateFieldConfigurationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/fieldconfiguration/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteFieldConfigurationParams {
    path: { id: string };
}

/**
 * Delete field configuration
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function deleteFieldConfiguration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteFieldConfigurationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/fieldconfiguration/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetFieldConfigurationItemsParams {
    path: { id: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get field configuration items
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getFieldConfigurationItems(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldConfigurationItemsParams): Promise<Types.PageBeanFieldConfigurationItem> {
    const path = `/rest/api/2/fieldconfiguration/${params.path.id}/fields`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanFieldConfigurationItem>;
}

export interface UpdateFieldConfigurationItemsParams {
    path: { id: string };
    body?: Types.FieldConfigurationItemsDetails;
}

/**
 * Update field configuration items
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function updateFieldConfigurationItems(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateFieldConfigurationItemsParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/fieldconfiguration/${params.path.id}/fields`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetAllFieldConfigurationSchemesParams {
    startAt?: number;
    maxResults?: number;
    id?: Array<number>;
}

/**
 * Get all field configuration schemes
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getAllFieldConfigurationSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllFieldConfigurationSchemesParams): Promise<Types.PageBeanFieldConfigurationScheme> {
    const path = '/rest/api/2/fieldconfigurationscheme';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanFieldConfigurationScheme>;
}

export interface CreateFieldConfigurationSchemeParams {
    body?: Types.UpdateFieldConfigurationSchemeDetails;
}

/**
 * Create field configuration scheme
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function createFieldConfigurationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateFieldConfigurationSchemeParams): Promise<Types.FieldConfigurationScheme> {
    const path = '/rest/api/2/fieldconfigurationscheme';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FieldConfigurationScheme>;
}

export interface GetFieldConfigurationSchemeMappingsParams {
    startAt?: number;
    maxResults?: number;
    fieldConfigurationSchemeId?: Array<number>;
}

/**
 * Get field configuration issue type items
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getFieldConfigurationSchemeMappings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldConfigurationSchemeMappingsParams): Promise<Types.PageBeanFieldConfigurationIssueTypeItem> {
    const path = '/rest/api/2/fieldconfigurationscheme/mapping';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      fieldConfigurationSchemeId: params.fieldConfigurationSchemeId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanFieldConfigurationIssueTypeItem>;
}

export interface GetFieldConfigurationSchemeProjectMappingParams {
    startAt?: number;
    maxResults?: number;
    projectId: Array<number>;
}

/**
 * Get field configuration schemes for projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 * @deprecated
 */
export async function getFieldConfigurationSchemeProjectMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldConfigurationSchemeProjectMappingParams): Promise<Types.PageBeanFieldConfigurationSchemeProjects> {
    const path = '/rest/api/2/fieldconfigurationscheme/project';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanFieldConfigurationSchemeProjects>;
}

export interface AssignFieldConfigurationSchemeToProjectParams {
    body?: Types.FieldConfigurationSchemeProjectAssociation;
}

/**
 * Assign field configuration scheme to project
 * @forge-scopes-asApp manage:jira-project, manage:jira-configuration
 * @forge-scopes-asUser manage:jira-project
 * @deprecated
 */
export async function assignFieldConfigurationSchemeToProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssignFieldConfigurationSchemeToProjectParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/fieldconfigurationscheme/project';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateFieldConfigurationSchemeParams {
    path: { id: string };
    body?: Types.UpdateFieldConfigurationSchemeDetails;
}

/**
 * Update field configuration scheme
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function updateFieldConfigurationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateFieldConfigurationSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/fieldconfigurationscheme/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteFieldConfigurationSchemeParams {
    path: { id: string };
}

/**
 * Delete field configuration scheme
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function deleteFieldConfigurationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteFieldConfigurationSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/fieldconfigurationscheme/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface SetFieldConfigurationSchemeMappingParams {
    path: { id: string };
    body?: Types.AssociateFieldConfigurationsWithIssueTypesRequest;
}

/**
 * Assign issue types to field configurations
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function setFieldConfigurationSchemeMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetFieldConfigurationSchemeMappingParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/fieldconfigurationscheme/${params.path.id}/mapping`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveIssueTypesFromGlobalFieldConfigurationSchemeParams {
    path: { id: string };
    body?: Types.IssueTypeIdsToRemove;
}

/**
 * Remove issue types from field configuration scheme
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function removeIssueTypesFromGlobalFieldConfigurationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveIssueTypesFromGlobalFieldConfigurationSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/fieldconfigurationscheme/${params.path.id}/mapping/delete`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface CreateFilterParams {
    expand?: string;
    overrideSharePermissions?: boolean;
    body?: Types.Filter;
}

/**
 * Create filter
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createFilter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateFilterParams): Promise<Types.Filter> {
    const path = '/rest/api/2/filter';
    const queryParams = {
      expand: params.expand,
      overrideSharePermissions: params.overrideSharePermissions,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Filter>;
}

/**
 * Get default share scope
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDefaultShareScope(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.DefaultShareScope> {
    const path = '/rest/api/2/filter/defaultShareScope';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DefaultShareScope>;
}

export interface SetDefaultShareScopeParams {
    body?: Types.DefaultShareScope;
}

/**
 * Set default share scope
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setDefaultShareScope(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetDefaultShareScopeParams): Promise<Types.DefaultShareScope> {
    const path = '/rest/api/2/filter/defaultShareScope';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DefaultShareScope>;
}

export interface GetFavouriteFiltersParams {
    expand?: string;
}

/**
 * Get favorite filters
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFavouriteFilters(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFavouriteFiltersParams): Promise<Array<Types.Filter>> {
    const path = '/rest/api/2/filter/favourite';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Filter>>;
}

export interface GetMyFiltersParams {
    expand?: string;
    includeFavourites?: boolean;
}

/**
 * Get my filters
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getMyFilters(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetMyFiltersParams): Promise<Array<Types.Filter>> {
    const path = '/rest/api/2/filter/my';
    const queryParams = {
      expand: params.expand,
      includeFavourites: params.includeFavourites,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Filter>>;
}

export interface GetFiltersPaginatedParams {
    filterName?: string;
    accountId?: string;
    owner?: string;
    groupname?: string;
    groupId?: string;
    projectId?: number;
    id?: Array<number>;
    orderBy?: "description" | "-description" | "+description" | "favourite_count" | "-favourite_count" | "+favourite_count" | "id" | "-id" | "+id" | "is_favourite" | "-is_favourite" | "+is_favourite" | "name" | "-name" | "+name" | "owner" | "-owner" | "+owner" | "is_shared" | "-is_shared" | "+is_shared";
    startAt?: number;
    maxResults?: number;
    expand?: string;
    overrideSharePermissions?: boolean;
    isSubstringMatch?: boolean;
}

/**
 * Search for filters
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFiltersPaginated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFiltersPaginatedParams): Promise<Types.PageBeanFilterDetails> {
    const path = '/rest/api/2/filter/search';
    const queryParams = {
      filterName: params.filterName,
      accountId: params.accountId,
      owner: params.owner,
      groupname: params.groupname,
      groupId: params.groupId,
      projectId: params.projectId,
      id: params.id,
      orderBy: params.orderBy,
      startAt: params.startAt,
      maxResults: params.maxResults,
      expand: params.expand,
      overrideSharePermissions: params.overrideSharePermissions,
      isSubstringMatch: params.isSubstringMatch,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanFilterDetails>;
}

export interface GetFilterParams {
    path: { id: string };
    expand?: string;
    overrideSharePermissions?: boolean;
}

/**
 * Get filter
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFilter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFilterParams): Promise<Types.Filter> {
    const path = `/rest/api/2/filter/${params.path.id}`;
    const queryParams = {
      expand: params.expand,
      overrideSharePermissions: params.overrideSharePermissions,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Filter>;
}

export interface UpdateFilterParams {
    path: { id: string };
    expand?: string;
    overrideSharePermissions?: boolean;
    body?: Types.Filter;
}

/**
 * Update filter
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateFilter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateFilterParams): Promise<Types.Filter> {
    const path = `/rest/api/2/filter/${params.path.id}`;
    const queryParams = {
      expand: params.expand,
      overrideSharePermissions: params.overrideSharePermissions,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Filter>;
}

export interface DeleteFilterParams {
    path: { id: string };
}

/**
 * Delete filter
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFilter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteFilterParams): Promise<void> {
    const path = `/rest/api/2/filter/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetColumnsParams {
    path: { id: string };
}

/**
 * Get columns
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetColumnsParams): Promise<Array<Types.ColumnItem>> {
    const path = `/rest/api/2/filter/${params.path.id}/columns`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ColumnItem>>;
}

export interface SetColumnsParams {
    path: { id: string };
    body?: Types.ColumnRequestBody;
}

/**
 * Set columns
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetColumnsParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/filter/${params.path.id}/columns`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface ResetColumnsParams {
    path: { id: string };
}

/**
 * Reset columns
 * @forge-scopes-asApp read:jira-work
 */
export async function resetColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ResetColumnsParams): Promise<void> {
    const path = `/rest/api/2/filter/${params.path.id}/columns`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SetFavouriteForFilterParams {
    path: { id: string };
    expand?: string;
}

/**
 * Add filter as favorite
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setFavouriteForFilter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetFavouriteForFilterParams): Promise<Types.Filter> {
    const path = `/rest/api/2/filter/${params.path.id}/favourite`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Filter>;
}

export interface DeleteFavouriteForFilterParams {
    path: { id: string };
    expand?: string;
}

/**
 * Remove filter as favorite
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteFavouriteForFilter(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteFavouriteForFilterParams): Promise<Types.Filter> {
    const path = `/rest/api/2/filter/${params.path.id}/favourite`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Filter>;
}

export interface ChangeFilterOwnerParams {
    path: { id: string };
    body?: Types.ChangeFilterOwner;
}

/**
 * Change filter owner
 * @forge-scopes-asApp read:jira-work
 */
export async function changeFilterOwner(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ChangeFilterOwnerParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/filter/${params.path.id}/owner`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetSharePermissionsParams {
    path: { id: string };
}

/**
 * Get share permissions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSharePermissions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSharePermissionsParams): Promise<Array<Types.SharePermission>> {
    const path = `/rest/api/2/filter/${params.path.id}/permission`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.SharePermission>>;
}

export interface AddSharePermissionParams {
    path: { id: string };
    body?: Types.SharePermissionInputBean;
}

/**
 * Add share permission
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addSharePermission(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddSharePermissionParams): Promise<Array<Types.SharePermission>> {
    const path = `/rest/api/2/filter/${params.path.id}/permission`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.SharePermission>>;
}

export interface GetSharePermissionParams {
    path: { id: string; permissionId: string };
}

/**
 * Get share permission
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSharePermission(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSharePermissionParams): Promise<Types.SharePermission> {
    const path = `/rest/api/2/filter/${params.path.id}/permission/${params.path.permissionId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SharePermission>;
}

export interface DeleteSharePermissionParams {
    path: { id: string; permissionId: string };
}

/**
 * Delete share permission
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSharePermission(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteSharePermissionParams): Promise<void> {
    const path = `/rest/api/2/filter/${params.path.id}/permission/${params.path.permissionId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface BulkPinUnpinProjectsAsyncParams {
    body?: Types.ForgePanelProjectPinRequest;
}

/**
 * Bulk pin or unpin issue panel to projects
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function bulkPinUnpinProjectsAsync(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkPinUnpinProjectsAsyncParams): Promise<Types.ForgePanelProjectPinAsyncResponse> {
    const path = '/rest/api/2/forge/panel/action/bulk/async';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ForgePanelProjectPinAsyncResponse>;
}

export interface GetGroupParams {
    groupname?: string;
    groupId?: string;
    expand?: string;
}

/**
 * Get group
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getGroup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetGroupParams): Promise<Types.Group> {
    const path = '/rest/api/2/group';
    const queryParams = {
      groupname: params.groupname,
      groupId: params.groupId,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Group>;
}

export interface CreateGroupParams {
    body?: Types.AddGroupBean;
}

/**
 * Create group
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createGroup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateGroupParams): Promise<Types.Group> {
    const path = '/rest/api/2/group';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Group>;
}

export interface RemoveGroupParams {
    groupname?: string;
    groupId?: string;
    swapGroup?: string;
    swapGroupId?: string;
}

/**
 * Remove group
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeGroup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveGroupParams): Promise<void> {
    const path = '/rest/api/2/group';
    const queryParams = {
      groupname: params.groupname,
      groupId: params.groupId,
      swapGroup: params.swapGroup,
      swapGroupId: params.swapGroupId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface BulkGetGroupsParams {
    startAt?: number;
    maxResults?: number;
    groupId?: Array<string>;
    groupName?: Array<string>;
    accessType?: string;
    applicationKey?: string;
}

/**
 * Bulk get groups
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkGetGroups(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkGetGroupsParams): Promise<Types.PageBeanGroupDetails> {
    const path = '/rest/api/2/group/bulk';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      groupId: params.groupId,
      groupName: params.groupName,
      accessType: params.accessType,
      applicationKey: params.applicationKey,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanGroupDetails>;
}

export interface GetUsersFromGroupParams {
    groupname?: string;
    groupId?: string;
    includeInactiveUsers?: boolean;
    startAt?: number;
    maxResults?: number;
}

/**
 * Get users from group
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUsersFromGroup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUsersFromGroupParams): Promise<Types.PageBeanUserDetails> {
    const path = '/rest/api/2/group/member';
    const queryParams = {
      groupname: params.groupname,
      groupId: params.groupId,
      includeInactiveUsers: params.includeInactiveUsers,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanUserDetails>;
}

export interface AddUserToGroupParams {
    groupname?: string;
    groupId?: string;
    body?: Types.UpdateUserToGroupBean;
}

/**
 * Add user to group
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function addUserToGroup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddUserToGroupParams): Promise<Types.Group> {
    const path = '/rest/api/2/group/user';
    const queryParams = {
      groupname: params.groupname,
      groupId: params.groupId,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Group>;
}

export interface RemoveUserFromGroupParams {
    groupname?: string;
    groupId?: string;
    username?: string;
    accountId: string;
}

/**
 * Remove user from group
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function removeUserFromGroup(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveUserFromGroupParams): Promise<void> {
    const path = '/rest/api/2/group/user';
    const queryParams = {
      groupname: params.groupname,
      groupId: params.groupId,
      username: params.username,
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface FindGroupsParams {
    accountId?: string;
    query?: string;
    exclude?: Array<string>;
    excludeId?: Array<string>;
    maxResults?: number;
    caseInsensitive?: boolean;
    userName?: string;
}

/**
 * Find groups
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function findGroups(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindGroupsParams): Promise<Types.FoundGroups> {
    const path = '/rest/api/2/groups/picker';
    const queryParams = {
      accountId: params.accountId,
      query: params.query,
      exclude: params.exclude,
      excludeId: params.excludeId,
      maxResults: params.maxResults,
      caseInsensitive: params.caseInsensitive,
      userName: params.userName,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FoundGroups>;
}

export interface FindUsersAndGroupsParams {
    query: string;
    maxResults?: number;
    showAvatar?: boolean;
    fieldId?: string;
    projectId?: Array<string>;
    issueTypeId?: Array<string>;
    avatarSize?: "xsmall" | "xsmall@2x" | "xsmall@3x" | "small" | "small@2x" | "small@3x" | "medium" | "medium@2x" | "medium@3x" | "large" | "large@2x" | "large@3x" | "xlarge" | "xlarge@2x" | "xlarge@3x" | "xxlarge" | "xxlarge@2x" | "xxlarge@3x" | "xxxlarge" | "xxxlarge@2x" | "xxxlarge@3x";
    caseInsensitive?: boolean;
    excludeConnectAddons?: boolean;
}

/**
 * Find users and groups
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findUsersAndGroups(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindUsersAndGroupsParams): Promise<Types.FoundUsersAndGroups> {
    const path = '/rest/api/2/groupuserpicker';
    const queryParams = {
      query: params.query,
      maxResults: params.maxResults,
      showAvatar: params.showAvatar,
      fieldId: params.fieldId,
      projectId: params.projectId,
      issueTypeId: params.issueTypeId,
      avatarSize: params.avatarSize,
      caseInsensitive: params.caseInsensitive,
      excludeConnectAddons: params.excludeConnectAddons,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FoundUsersAndGroups>;
}

/**
 * Get license
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLicense(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.License> {
    const path = '/rest/api/2/instance/license';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.License>;
}

export interface CreateIssueParams {
    updateHistory?: boolean;
    body?: Types.IssueUpdateDetails;
}

/**
 * Create issue
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueParams): Promise<Types.CreatedIssue> {
    const path = '/rest/api/2/issue';
    const queryParams = {
      updateHistory: params.updateHistory,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CreatedIssue>;
}

export interface ArchiveIssuesAsyncParams {
    body?: Types.ArchiveIssueAsyncRequest;
}

/**
 * Archive issue(s) by JQL
 * @forge-scopes-asApp read:jira-work
 */
export async function archiveIssuesAsync(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ArchiveIssuesAsyncParams): Promise<string> {
    const path = '/rest/api/2/issue/archive';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<string>;
}

export interface ArchiveIssuesParams {
    body?: Types.IssueArchivalSyncRequest;
}

/**
 * Archive issue(s) by issue ID/key
 * @forge-scopes-asApp read:jira-work
 */
export async function archiveIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ArchiveIssuesParams): Promise<Types.IssueArchivalSyncResponse> {
    const path = '/rest/api/2/issue/archive';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueArchivalSyncResponse>;
}

export interface CreateIssuesParams {
    body?: Types.IssuesUpdateBean;
}

/**
 * Bulk create issue
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssuesParams): Promise<Types.CreatedIssues> {
    const path = '/rest/api/2/issue/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.CreatedIssues>;
}

export interface BulkFetchIssuesParams {
    body?: Types.BulkFetchIssueRequestBean;
}

/**
 * Bulk fetch issues
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkFetchIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkFetchIssuesParams): Promise<Types.BulkIssueResults> {
    const path = '/rest/api/2/issue/bulkfetch';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BulkIssueResults>;
}

export interface GetCreateIssueMetaParams {
    projectIds?: Array<string>;
    projectKeys?: Array<string>;
    issuetypeIds?: Array<string>;
    issuetypeNames?: Array<string>;
    expand?: string;
}

/**
 * Get create issue metadata
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getCreateIssueMeta(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCreateIssueMetaParams): Promise<Types.IssueCreateMetadata> {
    const path = '/rest/api/2/issue/createmeta';
    const queryParams = {
      projectIds: params.projectIds,
      projectKeys: params.projectKeys,
      issuetypeIds: params.issuetypeIds,
      issuetypeNames: params.issuetypeNames,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueCreateMetadata>;
}

export interface GetCreateIssueMetaIssueTypesParams {
    path: { projectIdOrKey: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get create metadata issue types for a project
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCreateIssueMetaIssueTypes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCreateIssueMetaIssueTypesParams): Promise<Types.PageOfCreateMetaIssueTypes> {
    const path = `/rest/api/2/issue/createmeta/${params.path.projectIdOrKey}/issuetypes`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageOfCreateMetaIssueTypes>;
}

export interface GetCreateIssueMetaIssueTypeIdParams {
    path: { projectIdOrKey: string; issueTypeId: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get create field metadata for a project and issue type id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getCreateIssueMetaIssueTypeId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCreateIssueMetaIssueTypeIdParams): Promise<Types.PageOfCreateMetaIssueTypeWithField> {
    const path = `/rest/api/2/issue/createmeta/${params.path.projectIdOrKey}/issuetypes/${params.path.issueTypeId}`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageOfCreateMetaIssueTypeWithField>;
}

export interface GetIssueLimitReportParams {
    isReturningKeys?: boolean;
}

/**
 * Get issue limit report
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueLimitReport(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueLimitReportParams): Promise<Types.IssueLimitReportResponseBean> {
    const path = '/rest/api/2/issue/limit/report';
    const queryParams = {
      isReturningKeys: params.isReturningKeys,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueLimitReportResponseBean>;
}

export interface GetIssuePickerResourceParams {
    query?: string;
    currentJQL?: string;
    currentIssueKey?: string;
    currentProjectId?: string;
    showSubTasks?: boolean;
    showSubTaskParent?: boolean;
}

/**
 * Get issue picker suggestions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getIssuePickerResource(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuePickerResourceParams): Promise<Types.IssuePickerSuggestions> {
    const path = '/rest/api/2/issue/picker';
    const queryParams = {
      query: params.query,
      currentJQL: params.currentJQL,
      currentIssueKey: params.currentIssueKey,
      currentProjectId: params.currentProjectId,
      showSubTasks: params.showSubTasks,
      showSubTaskParent: params.showSubTaskParent,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssuePickerSuggestions>;
}

export interface BulkSetIssuesPropertiesListParams {
    body?: Types.IssueEntityProperties;
}

/**
 * Bulk set issues properties by list
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkSetIssuesPropertiesList(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkSetIssuesPropertiesListParams): Promise<void> {
    const path = '/rest/api/2/issue/properties';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface BulkSetIssuePropertiesByIssueParams {
    body?: Types.MultiIssueEntityProperties;
}

/**
 * Bulk set issue properties by issue
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkSetIssuePropertiesByIssue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkSetIssuePropertiesByIssueParams): Promise<void> {
    const path = '/rest/api/2/issue/properties/multi';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface BulkSetIssuePropertyParams {
    path: { propertyKey: string };
    body?: Types.BulkIssuePropertyUpdateRequest;
}

/**
 * Bulk set issue property
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkSetIssueProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkSetIssuePropertyParams): Promise<void> {
    const path = `/rest/api/2/issue/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface BulkDeleteIssuePropertyParams {
    path: { propertyKey: string };
    body?: Types.IssueFilterForBulkPropertyDelete;
}

/**
 * Bulk delete issue property
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkDeleteIssueProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkDeleteIssuePropertyParams): Promise<void> {
    const path = `/rest/api/2/issue/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface UnarchiveIssuesParams {
    body?: Types.IssueArchivalSyncRequest;
}

/**
 * Unarchive issue(s) by issue keys/ID
 * @forge-scopes-asApp read:jira-work
 */
export async function unarchiveIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UnarchiveIssuesParams): Promise<Types.IssueArchivalSyncResponse> {
    const path = '/rest/api/2/issue/unarchive';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueArchivalSyncResponse>;
}

export interface GetIsWatchingIssueBulkParams {
    body?: Types.IssueList;
}

/**
 * Get is watching issue bulk
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIsWatchingIssueBulk(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIsWatchingIssueBulkParams): Promise<Types.BulkIssueIsWatching> {
    const path = '/rest/api/2/issue/watching';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BulkIssueIsWatching>;
}

export interface GetIssueParams {
    path: { issueIdOrKey: string };
    fields?: Array<string>;
    fieldsByKeys?: boolean;
    expand?: string;
    properties?: Array<string>;
    updateHistory?: boolean;
    failFast?: boolean;
}

/**
 * Get issue
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueParams): Promise<Types.IssueBean> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}`;
    const queryParams = {
      fields: params.fields,
      fieldsByKeys: params.fieldsByKeys,
      expand: params.expand,
      properties: params.properties,
      updateHistory: params.updateHistory,
      failFast: params.failFast,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueBean>;
}

export interface EditIssueParams {
    path: { issueIdOrKey: string };
    notifyUsers?: boolean;
    overrideScreenSecurity?: boolean;
    overrideEditableFlag?: boolean;
    returnIssue?: boolean;
    expand?: string;
    body?: Types.IssueUpdateDetails;
}

/**
 * Edit issue
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function editIssue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: EditIssueParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}`;
    const queryParams = {
      notifyUsers: params.notifyUsers,
      overrideScreenSecurity: params.overrideScreenSecurity,
      overrideEditableFlag: params.overrideEditableFlag,
      returnIssue: params.returnIssue,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteIssueParams {
    path: { issueIdOrKey: string };
    deleteSubtasks?: "true" | "false";
}

/**
 * Delete issue
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}`;
    const queryParams = {
      deleteSubtasks: params.deleteSubtasks,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface AssignIssueParams {
    path: { issueIdOrKey: string };
    body?: Types.User;
}

/**
 * Assign issue
 * @forge-scopes-asApp read:jira-work
 */
export async function assignIssue(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssignIssueParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/assignee`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface AddAttachmentParams {
    path: { issueIdOrKey: string };
    body?: Record<string, unknown>;
}

/**
 * Add attachment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addAttachment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddAttachmentParams): Promise<Array<Types.Attachment>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/attachments`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Attachment>>;
}

export interface GetChangeLogsParams {
    path: { issueIdOrKey: string };
    startAt?: number;
    maxResults?: number;
}

/**
 * Get changelogs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getChangeLogs(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetChangeLogsParams): Promise<Types.PageBeanChangelog> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/changelog`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanChangelog>;
}

export interface GetChangeLogsByIdsParams {
    path: { issueIdOrKey: string };
    body?: Types.IssueChangelogIds;
}

/**
 * Get changelogs by IDs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getChangeLogsByIds(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetChangeLogsByIdsParams): Promise<Types.PageOfChangelogs> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/changelog/list`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageOfChangelogs>;
}

export interface GetCommentsParams {
    path: { issueIdOrKey: string };
    startAt?: number;
    maxResults?: number;
    orderBy?: "created" | "-created" | "+created";
    expand?: string;
}

/**
 * Get comments
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getComments(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCommentsParams): Promise<Types.PageOfComments> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/comment`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      orderBy: params.orderBy,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageOfComments>;
}

export interface AddCommentParams {
    path: { issueIdOrKey: string };
    expand?: string;
    body?: Types.Comment;
}

/**
 * Add comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addComment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddCommentParams): Promise<Types.Comment> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/comment`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Comment>;
}

export interface GetCommentParams {
    path: { issueIdOrKey: string; id: string };
    expand?: string;
}

/**
 * Get comment
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getComment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCommentParams): Promise<Types.Comment> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/comment/${params.path.id}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Comment>;
}

export interface UpdateCommentParams {
    path: { issueIdOrKey: string; id: string };
    notifyUsers?: boolean;
    overrideEditableFlag?: boolean;
    expand?: string;
    body?: Types.Comment;
}

/**
 * Update comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateComment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateCommentParams): Promise<Types.Comment> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/comment/${params.path.id}`;
    const queryParams = {
      notifyUsers: params.notifyUsers,
      overrideEditableFlag: params.overrideEditableFlag,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Comment>;
}

export interface DeleteCommentParams {
    path: { issueIdOrKey: string; id: string };
}

/**
 * Delete comment
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteComment(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteCommentParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/comment/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetEditIssueMetaParams {
    path: { issueIdOrKey: string };
    overrideScreenSecurity?: boolean;
    overrideEditableFlag?: boolean;
}

/**
 * Get edit issue metadata
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getEditIssueMeta(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetEditIssueMetaParams): Promise<Types.IssueUpdateMetadata> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/editmeta`;
    const queryParams = {
      overrideScreenSecurity: params.overrideScreenSecurity,
      overrideEditableFlag: params.overrideEditableFlag,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueUpdateMetadata>;
}

export interface NotifyParams {
    path: { issueIdOrKey: string };
    body?: Types.Notification;
}

/**
 * Send notification for issue
 * @forge-scopes-asApp read:jira-work
 */
export async function notify(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: NotifyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/notify`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetIssuePropertyKeysParams {
    path: { issueIdOrKey: string };
}

/**
 * Get issue property keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssuePropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuePropertyKeysParams): Promise<Types.PropertyKeys> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetIssuePropertyParams {
    path: { issueIdOrKey: string; propertyKey: string };
}

/**
 * Get issue property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssuePropertyParams): Promise<Types.EntityProperty> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetIssuePropertyParams {
    path: { issueIdOrKey: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set issue property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setIssueProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetIssuePropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteIssuePropertyParams {
    path: { issueIdOrKey: string; propertyKey: string };
}

/**
 * Delete issue property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssuePropertyParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetRemoteIssueLinksParams {
    path: { issueIdOrKey: string };
    globalId?: string;
}

/**
 * Get remote issue links
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRemoteIssueLinks(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRemoteIssueLinksParams): Promise<Array<Types.RemoteIssueLink> | Types.RemoteIssueLink> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/remotelink`;
    const queryParams = {
      globalId: params.globalId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.RemoteIssueLink> | Types.RemoteIssueLink>;
}

export interface CreateOrUpdateRemoteIssueLinkParams {
    path: { issueIdOrKey: string };
    body?: Types.RemoteIssueLinkRequest;
}

/**
 * Create or update remote issue link
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createOrUpdateRemoteIssueLink(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateOrUpdateRemoteIssueLinkParams): Promise<Types.RemoteIssueLinkIdentifies> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/remotelink`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RemoteIssueLinkIdentifies>;
}

export interface DeleteRemoteIssueLinkByGlobalIdParams {
    path: { issueIdOrKey: string };
    globalId: string;
}

/**
 * Delete remote issue link by global ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRemoteIssueLinkByGlobalId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRemoteIssueLinkByGlobalIdParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/remotelink`;
    const queryParams = {
      globalId: params.globalId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetRemoteIssueLinkByIdParams {
    path: { issueIdOrKey: string; linkId: string };
}

/**
 * Get remote issue link by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRemoteIssueLinkById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRemoteIssueLinkByIdParams): Promise<Types.RemoteIssueLink> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/remotelink/${params.path.linkId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RemoteIssueLink>;
}

export interface UpdateRemoteIssueLinkParams {
    path: { issueIdOrKey: string; linkId: string };
    body?: Types.RemoteIssueLinkRequest;
}

/**
 * Update remote issue link by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateRemoteIssueLink(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateRemoteIssueLinkParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/remotelink/${params.path.linkId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteRemoteIssueLinkByIdParams {
    path: { issueIdOrKey: string; linkId: string };
}

/**
 * Delete remote issue link by ID
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRemoteIssueLinkById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRemoteIssueLinkByIdParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/remotelink/${params.path.linkId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetTransitionsParams {
    path: { issueIdOrKey: string };
    expand?: string;
    transitionId?: string;
    skipRemoteOnlyCondition?: boolean;
    includeUnavailableTransitions?: boolean;
    sortByOpsBarAndStatus?: boolean;
}

/**
 * Get transitions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTransitions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetTransitionsParams): Promise<Types.Transitions> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/transitions`;
    const queryParams = {
      expand: params.expand,
      transitionId: params.transitionId,
      skipRemoteOnlyCondition: params.skipRemoteOnlyCondition,
      includeUnavailableTransitions: params.includeUnavailableTransitions,
      sortByOpsBarAndStatus: params.sortByOpsBarAndStatus,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Transitions>;
}

export interface DoTransitionParams {
    path: { issueIdOrKey: string };
    body?: Types.IssueUpdateDetails;
}

/**
 * Transition issue
 * @forge-scopes-asApp read:jira-work
 */
export async function doTransition(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DoTransitionParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/transitions`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetVotesParams {
    path: { issueIdOrKey: string };
}

/**
 * Get votes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getVotes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetVotesParams): Promise<Types.Votes> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/votes`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Votes>;
}

export interface AddVoteParams {
    path: { issueIdOrKey: string };
}

/**
 * Add vote
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addVote(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddVoteParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/votes`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveVoteParams {
    path: { issueIdOrKey: string };
}

/**
 * Delete vote
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeVote(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveVoteParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/votes`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssueWatchersParams {
    path: { issueIdOrKey: string };
}

/**
 * Get issue watchers
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueWatchers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueWatchersParams): Promise<Types.Watchers> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/watchers`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Watchers>;
}

export interface AddWatcherParams {
    path: { issueIdOrKey: string };
    body?: string;
}

/**
 * Add watcher
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addWatcher(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddWatcherParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/watchers`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveWatcherParams {
    path: { issueIdOrKey: string };
    username?: string;
    accountId?: string;
}

/**
 * Delete watcher
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeWatcher(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveWatcherParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/watchers`;
    const queryParams = {
      username: params.username,
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetIssueWorklogParams {
    path: { issueIdOrKey: string };
    startAt?: number;
    maxResults?: number;
    startedAfter?: number;
    startedBefore?: number;
    expand?: string;
}

/**
 * Get issue worklogs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueWorklog(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueWorklogParams): Promise<Types.PageOfWorklogs> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      startedAfter: params.startedAfter,
      startedBefore: params.startedBefore,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageOfWorklogs>;
}

export interface AddWorklogParams {
    path: { issueIdOrKey: string };
    notifyUsers?: boolean;
    adjustEstimate?: "new" | "leave" | "manual" | "auto";
    newEstimate?: string;
    reduceBy?: string;
    expand?: string;
    overrideEditableFlag?: boolean;
    body?: Types.Worklog;
}

/**
 * Add worklog
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addWorklog(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddWorklogParams): Promise<Types.Worklog> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog`;
    const queryParams = {
      notifyUsers: params.notifyUsers,
      adjustEstimate: params.adjustEstimate,
      newEstimate: params.newEstimate,
      reduceBy: params.reduceBy,
      expand: params.expand,
      overrideEditableFlag: params.overrideEditableFlag,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Worklog>;
}

export interface BulkDeleteWorklogsParams {
    path: { issueIdOrKey: string };
    adjustEstimate?: "leave" | "auto";
    overrideEditableFlag?: boolean;
    body?: Types.WorklogIdsRequestBean;
}

/**
 * Bulk delete worklogs
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkDeleteWorklogs(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkDeleteWorklogsParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog`;
    const queryParams = {
      adjustEstimate: params.adjustEstimate,
      overrideEditableFlag: params.overrideEditableFlag,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface BulkMoveWorklogsParams {
    path: { issueIdOrKey: string };
    adjustEstimate?: "leave" | "auto";
    overrideEditableFlag?: boolean;
    body?: Types.WorklogsMoveRequestBean;
}

/**
 * Bulk move worklogs
 * @forge-scopes-asApp read:jira-work
 */
export async function bulkMoveWorklogs(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkMoveWorklogsParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/move`;
    const queryParams = {
      adjustEstimate: params.adjustEstimate,
      overrideEditableFlag: params.overrideEditableFlag,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWorklogParams {
    path: { issueIdOrKey: string; id: string };
    expand?: string;
}

/**
 * Get worklog
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorklog(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorklogParams): Promise<Types.Worklog> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/${params.path.id}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Worklog>;
}

export interface UpdateWorklogParams {
    path: { issueIdOrKey: string; id: string };
    notifyUsers?: boolean;
    adjustEstimate?: "new" | "leave" | "manual" | "auto";
    newEstimate?: string;
    expand?: string;
    overrideEditableFlag?: boolean;
    body?: Types.Worklog;
}

/**
 * Update worklog
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateWorklog(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateWorklogParams): Promise<Types.Worklog> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/${params.path.id}`;
    const queryParams = {
      notifyUsers: params.notifyUsers,
      adjustEstimate: params.adjustEstimate,
      newEstimate: params.newEstimate,
      expand: params.expand,
      overrideEditableFlag: params.overrideEditableFlag,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Worklog>;
}

export interface DeleteWorklogParams {
    path: { issueIdOrKey: string; id: string };
    notifyUsers?: boolean;
    adjustEstimate?: "new" | "leave" | "manual" | "auto";
    newEstimate?: string;
    increaseBy?: string;
    overrideEditableFlag?: boolean;
}

/**
 * Delete worklog
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorklog(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorklogParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/${params.path.id}`;
    const queryParams = {
      notifyUsers: params.notifyUsers,
      adjustEstimate: params.adjustEstimate,
      newEstimate: params.newEstimate,
      increaseBy: params.increaseBy,
      overrideEditableFlag: params.overrideEditableFlag,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWorklogPropertyKeysParams {
    path: { issueIdOrKey: string; worklogId: string };
}

/**
 * Get worklog property keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorklogPropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorklogPropertyKeysParams): Promise<Types.PropertyKeys> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/${params.path.worklogId}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetWorklogPropertyParams {
    path: { issueIdOrKey: string; worklogId: string; propertyKey: string };
}

/**
 * Get worklog property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorklogProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorklogPropertyParams): Promise<Types.EntityProperty> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/${params.path.worklogId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetWorklogPropertyParams {
    path: { issueIdOrKey: string; worklogId: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set worklog property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setWorklogProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetWorklogPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/${params.path.worklogId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteWorklogPropertyParams {
    path: { issueIdOrKey: string; worklogId: string; propertyKey: string };
}

/**
 * Delete worklog property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorklogProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorklogPropertyParams): Promise<void> {
    const path = `/rest/api/2/issue/${params.path.issueIdOrKey}/worklog/${params.path.worklogId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface LinkIssuesParams {
    body?: Types.LinkIssueRequestJsonBean;
}

/**
 * Create issue link
 * @forge-scopes-asApp read:jira-work
 */
export async function linkIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: LinkIssuesParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/issueLink';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetIssueLinkParams {
    path: { linkId: string };
}

/**
 * Get issue link
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueLink(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueLinkParams): Promise<Types.IssueLink> {
    const path = `/rest/api/2/issueLink/${params.path.linkId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueLink>;
}

export interface DeleteIssueLinkParams {
    path: { linkId: string };
}

/**
 * Delete issue link
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueLink(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueLinkParams): Promise<void> {
    const path = `/rest/api/2/issueLink/${params.path.linkId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get issue link types
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueLinkTypes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.IssueLinkTypes> {
    const path = '/rest/api/2/issueLinkType';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueLinkTypes>;
}

export interface CreateIssueLinkTypeParams {
    body?: Types.IssueLinkType;
}

/**
 * Create issue link type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssueLinkType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueLinkTypeParams): Promise<Types.IssueLinkType> {
    const path = '/rest/api/2/issueLinkType';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueLinkType>;
}

export interface GetIssueLinkTypeParams {
    path: { issueLinkTypeId: string };
}

/**
 * Get issue link type
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueLinkType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueLinkTypeParams): Promise<Types.IssueLinkType> {
    const path = `/rest/api/2/issueLinkType/${params.path.issueLinkTypeId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueLinkType>;
}

export interface UpdateIssueLinkTypeParams {
    path: { issueLinkTypeId: string };
    body?: Types.IssueLinkType;
}

/**
 * Update issue link type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateIssueLinkType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateIssueLinkTypeParams): Promise<Types.IssueLinkType> {
    const path = `/rest/api/2/issueLinkType/${params.path.issueLinkTypeId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueLinkType>;
}

export interface DeleteIssueLinkTypeParams {
    path: { issueLinkTypeId: string };
}

/**
 * Delete issue link type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueLinkType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueLinkTypeParams): Promise<void> {
    const path = `/rest/api/2/issueLinkType/${params.path.issueLinkTypeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface ExportArchivedIssuesParams {
    body?: Types.ArchivedIssuesFilterRequest;
}

/**
 * Export archived issue(s)
 * @forge-scopes-asApp read:jira-work
 */
export async function exportArchivedIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ExportArchivedIssuesParams): Promise<Types.ExportArchivedIssuesTaskProgressResponse> {
    const path = '/rest/api/2/issues/archive/export';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ExportArchivedIssuesTaskProgressResponse>;
}

/**
 * Get issue security schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueSecuritySchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.SecuritySchemes> {
    const path = '/rest/api/2/issuesecurityschemes';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SecuritySchemes>;
}

export interface CreateIssueSecuritySchemeParams {
    body?: Types.CreateIssueSecuritySchemeDetails;
}

/**
 * Create issue security scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssueSecurityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueSecuritySchemeParams): Promise<Types.SecuritySchemeId> {
    const path = '/rest/api/2/issuesecurityschemes';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SecuritySchemeId>;
}

export interface GetSecurityLevelsParams {
    startAt?: string;
    maxResults?: string;
    id?: Array<string>;
    schemeId?: Array<string>;
    onlyDefault?: boolean;
}

/**
 * Get issue security levels
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSecurityLevels(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSecurityLevelsParams): Promise<Types.PageBeanSecurityLevel> {
    const path = '/rest/api/2/issuesecurityschemes/level';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      schemeId: params.schemeId,
      onlyDefault: params.onlyDefault,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanSecurityLevel>;
}

export interface SetDefaultLevelsParams {
    body?: Types.SetDefaultLevelsRequest;
}

/**
 * Set default issue security levels
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setDefaultLevels(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetDefaultLevelsParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/issuesecurityschemes/level/default';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetSecurityLevelMembersParams {
    startAt?: string;
    maxResults?: string;
    id?: Array<string>;
    schemeId?: Array<string>;
    levelId?: Array<string>;
    expand?: string;
}

/**
 * Get issue security level members
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getSecurityLevelMembers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSecurityLevelMembersParams): Promise<Types.PageBeanSecurityLevelMember> {
    const path = '/rest/api/2/issuesecurityschemes/level/member';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      schemeId: params.schemeId,
      levelId: params.levelId,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanSecurityLevelMember>;
}

export interface SearchProjectsUsingSecuritySchemesParams {
    startAt?: string;
    maxResults?: string;
    issueSecuritySchemeId?: Array<string>;
    projectId?: Array<string>;
}

/**
 * Get projects using issue security schemes
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function searchProjectsUsingSecuritySchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchProjectsUsingSecuritySchemesParams): Promise<Types.PageBeanIssueSecuritySchemeToProjectMapping> {
    const path = '/rest/api/2/issuesecurityschemes/project';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      issueSecuritySchemeId: params.issueSecuritySchemeId,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueSecuritySchemeToProjectMapping>;
}

export interface AssociateSchemesToProjectsParams {
    body?: Types.AssociateSecuritySchemeWithProjectDetails;
}

/**
 * Associate security scheme to project
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function associateSchemesToProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssociateSchemesToProjectsParams): Promise<void> {
    const path = '/rest/api/2/issuesecurityschemes/project';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SearchSecuritySchemesParams {
    startAt?: string;
    maxResults?: string;
    id?: Array<string>;
    projectId?: Array<string>;
}

/**
 * Search issue security schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchSecuritySchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchSecuritySchemesParams): Promise<Types.PageBeanSecuritySchemeWithProjects> {
    const path = '/rest/api/2/issuesecurityschemes/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanSecuritySchemeWithProjects>;
}

export interface GetIssueSecuritySchemeParams {
    path: { id: string };
}

/**
 * Get issue security scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueSecurityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueSecuritySchemeParams): Promise<Types.SecurityScheme> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SecurityScheme>;
}

export interface UpdateIssueSecuritySchemeParams {
    path: { id: string };
    body?: Types.UpdateIssueSecuritySchemeRequestBean;
}

/**
 * Update issue security scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateIssueSecurityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateIssueSecuritySchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetIssueSecurityLevelMembersParams {
    path: { issueSecuritySchemeId: string };
    startAt?: number;
    maxResults?: number;
    issueSecurityLevelId?: Array<string>;
    expand?: string;
}

/**
 * Get issue security level members by issue security scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueSecurityLevelMembers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueSecurityLevelMembersParams): Promise<Types.PageBeanIssueSecurityLevelMember> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.issueSecuritySchemeId}/members`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      issueSecurityLevelId: params.issueSecurityLevelId,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueSecurityLevelMember>;
}

export interface DeleteSecuritySchemeParams {
    path: { schemeId: string };
}

/**
 * Delete issue security scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteSecurityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteSecuritySchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.schemeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface AddSecurityLevelParams {
    path: { schemeId: string };
    body?: Types.AddSecuritySchemeLevelsRequestBean;
}

/**
 * Add issue security levels
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addSecurityLevel(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddSecurityLevelParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.schemeId}/level`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateSecurityLevelParams {
    path: { schemeId: string; levelId: string };
    body?: Types.UpdateIssueSecurityLevelDetails;
}

/**
 * Update issue security level
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSecurityLevel(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateSecurityLevelParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.schemeId}/level/${params.path.levelId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveLevelParams {
    path: { schemeId: string; levelId: string };
    replaceWith?: string;
}

/**
 * Remove issue security level
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeLevel(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveLevelParams): Promise<void> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.schemeId}/level/${params.path.levelId}`;
    const queryParams = {
      replaceWith: params.replaceWith,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface AddSecurityLevelMembersParams {
    path: { schemeId: string; levelId: string };
    body?: Types.SecuritySchemeMembersRequest;
}

/**
 * Add issue security level members
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addSecurityLevelMembers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddSecurityLevelMembersParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.schemeId}/level/${params.path.levelId}/member`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveMemberFromSecurityLevelParams {
    path: { schemeId: string; levelId: string; memberId: string };
}

/**
 * Remove member from issue security level
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeMemberFromSecurityLevel(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveMemberFromSecurityLevelParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuesecurityschemes/${params.path.schemeId}/level/${params.path.levelId}/member/${params.path.memberId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

/**
 * Get all issue types for user
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueAllTypes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.IssueTypeDetails>> {
    const path = '/rest/api/2/issuetype';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.IssueTypeDetails>>;
}

export interface CreateIssueTypeParams {
    body?: Types.IssueTypeCreateBean;
}

/**
 * Create issue type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueTypeParams): Promise<Types.IssueTypeDetails> {
    const path = '/rest/api/2/issuetype';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypeDetails>;
}

export interface GetIssueTypesForProjectParams {
    projectId: number;
    level?: number;
}

/**
 * Get issue types for project
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getIssueTypesForProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypesForProjectParams): Promise<Array<Types.IssueTypeDetails>> {
    const path = '/rest/api/2/issuetype/project';
    const queryParams = {
      projectId: params.projectId,
      level: params.level,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.IssueTypeDetails>>;
}

export interface GetIssueTypeParams {
    path: { id: string };
}

/**
 * Get issue type
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypeParams): Promise<Types.IssueTypeDetails> {
    const path = `/rest/api/2/issuetype/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypeDetails>;
}

export interface UpdateIssueTypeParams {
    path: { id: string };
    body?: Types.IssueTypeUpdateBean;
}

/**
 * Update issue type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateIssueTypeParams): Promise<Types.IssueTypeDetails> {
    const path = `/rest/api/2/issuetype/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypeDetails>;
}

export interface DeleteIssueTypeParams {
    path: { id: string };
    alternativeIssueTypeId?: string;
}

/**
 * Delete issue type
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueTypeParams): Promise<void> {
    const path = `/rest/api/2/issuetype/${params.path.id}`;
    const queryParams = {
      alternativeIssueTypeId: params.alternativeIssueTypeId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAlternativeIssueTypesParams {
    path: { id: string };
}

/**
 * Get alternative issue types
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAlternativeIssueTypes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAlternativeIssueTypesParams): Promise<Array<Types.IssueTypeDetails>> {
    const path = `/rest/api/2/issuetype/${params.path.id}/alternatives`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.IssueTypeDetails>>;
}

export interface CreateIssueTypeAvatarParams {
    path: { id: string };
    x?: number;
    y?: number;
    size: number;
    body?: Record<string, unknown>;
}

/**
 * Load issue type avatar
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssueTypeAvatar(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueTypeAvatarParams): Promise<Types.Avatar> {
    const path = `/rest/api/2/issuetype/${params.path.id}/avatar2`;
    const queryParams = {
      x: params.x,
      y: params.y,
      size: params.size,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Avatar>;
}

export interface GetIssueTypePropertyKeysParams {
    path: { issueTypeId: string };
}

/**
 * Get issue type property keys
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueTypePropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypePropertyKeysParams): Promise<Types.PropertyKeys> {
    const path = `/rest/api/2/issuetype/${params.path.issueTypeId}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetIssueTypePropertyParams {
    path: { issueTypeId: string; propertyKey: string };
}

/**
 * Get issue type property
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueTypeProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypePropertyParams): Promise<Types.EntityProperty> {
    const path = `/rest/api/2/issuetype/${params.path.issueTypeId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetIssueTypePropertyParams {
    path: { issueTypeId: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set issue type property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setIssueTypeProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetIssueTypePropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetype/${params.path.issueTypeId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteIssueTypePropertyParams {
    path: { issueTypeId: string; propertyKey: string };
}

/**
 * Delete issue type property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueTypeProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueTypePropertyParams): Promise<void> {
    const path = `/rest/api/2/issuetype/${params.path.issueTypeId}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAllIssueTypeSchemesParams {
    startAt?: number;
    maxResults?: number;
    id?: Array<number>;
    orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
    expand?: string;
    queryString?: string;
}

/**
 * Get all issue type schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllIssueTypeSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllIssueTypeSchemesParams): Promise<Types.PageBeanIssueTypeScheme> {
    const path = '/rest/api/2/issuetypescheme';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      orderBy: params.orderBy,
      expand: params.expand,
      queryString: params.queryString,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueTypeScheme>;
}

export interface CreateIssueTypeSchemeParams {
    body?: Types.IssueTypeSchemeDetails;
}

/**
 * Create issue type scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssueTypeScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueTypeSchemeParams): Promise<Types.IssueTypeSchemeID> {
    const path = '/rest/api/2/issuetypescheme';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypeSchemeID>;
}

export interface GetIssueTypeSchemesMappingParams {
    startAt?: number;
    maxResults?: number;
    issueTypeSchemeId?: Array<number>;
}

/**
 * Get issue type scheme items
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueTypeSchemesMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypeSchemesMappingParams): Promise<Types.PageBeanIssueTypeSchemeMapping> {
    const path = '/rest/api/2/issuetypescheme/mapping';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      issueTypeSchemeId: params.issueTypeSchemeId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueTypeSchemeMapping>;
}

export interface GetIssueTypeSchemeForProjectsParams {
    startAt?: number;
    maxResults?: number;
    projectId: Array<number>;
}

/**
 * Get issue type schemes for projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getIssueTypeSchemeForProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypeSchemeForProjectsParams): Promise<Types.PageBeanIssueTypeSchemeProjects> {
    const path = '/rest/api/2/issuetypescheme/project';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueTypeSchemeProjects>;
}

export interface AssignIssueTypeSchemeToProjectParams {
    body?: Types.IssueTypeSchemeProjectAssociation;
}

/**
 * Assign issue type scheme to project
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function assignIssueTypeSchemeToProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssignIssueTypeSchemeToProjectParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/issuetypescheme/project';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateIssueTypeSchemeParams {
    path: { issueTypeSchemeId: string };
    body?: Types.IssueTypeSchemeUpdateDetails;
}

/**
 * Update issue type scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateIssueTypeScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateIssueTypeSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescheme/${params.path.issueTypeSchemeId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteIssueTypeSchemeParams {
    path: { issueTypeSchemeId: string };
}

/**
 * Delete issue type scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueTypeScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueTypeSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescheme/${params.path.issueTypeSchemeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface AddIssueTypesToIssueTypeSchemeParams {
    path: { issueTypeSchemeId: string };
    body?: Types.IssueTypeIds;
}

/**
 * Add issue types to issue type scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addIssueTypesToIssueTypeScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddIssueTypesToIssueTypeSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescheme/${params.path.issueTypeSchemeId}/issuetype`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface ReorderIssueTypesInIssueTypeSchemeParams {
    path: { issueTypeSchemeId: string };
    body?: Types.OrderOfIssueTypes;
}

/**
 * Change order of issue types
 * @forge-scopes-asApp read:jira-work
 */
export async function reorderIssueTypesInIssueTypeScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReorderIssueTypesInIssueTypeSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescheme/${params.path.issueTypeSchemeId}/issuetype/move`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveIssueTypeFromIssueTypeSchemeParams {
    path: { issueTypeSchemeId: string; issueTypeId: string };
}

/**
 * Remove issue type from issue type scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeIssueTypeFromIssueTypeScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveIssueTypeFromIssueTypeSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescheme/${params.path.issueTypeSchemeId}/issuetype/${params.path.issueTypeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetIssueTypeScreenSchemesParams {
    startAt?: number;
    maxResults?: number;
    id?: Array<number>;
    queryString?: string;
    orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
    expand?: string;
}

/**
 * Get issue type screen schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueTypeScreenSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypeScreenSchemesParams): Promise<Types.PageBeanIssueTypeScreenScheme> {
    const path = '/rest/api/2/issuetypescreenscheme';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      queryString: params.queryString,
      orderBy: params.orderBy,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueTypeScreenScheme>;
}

export interface CreateIssueTypeScreenSchemeParams {
    body?: Types.IssueTypeScreenSchemeDetails;
}

/**
 * Create issue type screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createIssueTypeScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateIssueTypeScreenSchemeParams): Promise<Types.IssueTypeScreenSchemeId> {
    const path = '/rest/api/2/issuetypescreenscheme';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypeScreenSchemeId>;
}

export interface GetIssueTypeScreenSchemeMappingsParams {
    startAt?: number;
    maxResults?: number;
    issueTypeScreenSchemeId?: Array<number>;
}

/**
 * Get issue type screen scheme items
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueTypeScreenSchemeMappings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypeScreenSchemeMappingsParams): Promise<Types.PageBeanIssueTypeScreenSchemeItem> {
    const path = '/rest/api/2/issuetypescreenscheme/mapping';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      issueTypeScreenSchemeId: params.issueTypeScreenSchemeId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueTypeScreenSchemeItem>;
}

export interface GetIssueTypeScreenSchemeProjectAssociationsParams {
    startAt?: number;
    maxResults?: number;
    projectId: Array<number>;
}

/**
 * Get issue type screen schemes for projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getIssueTypeScreenSchemeProjectAssociations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueTypeScreenSchemeProjectAssociationsParams): Promise<Types.PageBeanIssueTypeScreenSchemesProjects> {
    const path = '/rest/api/2/issuetypescreenscheme/project';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanIssueTypeScreenSchemesProjects>;
}

export interface AssignIssueTypeScreenSchemeToProjectParams {
    body?: Types.IssueTypeScreenSchemeProjectAssociation;
}

/**
 * Assign issue type screen scheme to project
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function assignIssueTypeScreenSchemeToProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssignIssueTypeScreenSchemeToProjectParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/issuetypescreenscheme/project';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateIssueTypeScreenSchemeParams {
    path: { issueTypeScreenSchemeId: string };
    body?: Types.IssueTypeScreenSchemeUpdateDetails;
}

/**
 * Update issue type screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateIssueTypeScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateIssueTypeScreenSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescreenscheme/${params.path.issueTypeScreenSchemeId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteIssueTypeScreenSchemeParams {
    path: { issueTypeScreenSchemeId: string };
}

/**
 * Delete issue type screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteIssueTypeScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteIssueTypeScreenSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescreenscheme/${params.path.issueTypeScreenSchemeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface AppendMappingsForIssueTypeScreenSchemeParams {
    path: { issueTypeScreenSchemeId: string };
    body?: Types.IssueTypeScreenSchemeMappingDetails;
}

/**
 * Append mappings to issue type screen scheme
 * @forge-scopes-asApp read:jira-work
 */
export async function appendMappingsForIssueTypeScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AppendMappingsForIssueTypeScreenSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescreenscheme/${params.path.issueTypeScreenSchemeId}/mapping`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateDefaultScreenSchemeParams {
    path: { issueTypeScreenSchemeId: string };
    body?: Types.UpdateDefaultScreenScheme;
}

/**
 * Update issue type screen scheme default screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateDefaultScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateDefaultScreenSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescreenscheme/${params.path.issueTypeScreenSchemeId}/mapping/default`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveMappingsFromIssueTypeScreenSchemeParams {
    path: { issueTypeScreenSchemeId: string };
    body?: Types.IssueTypeIds;
}

/**
 * Remove mappings from issue type screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeMappingsFromIssueTypeScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveMappingsFromIssueTypeScreenSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/issuetypescreenscheme/${params.path.issueTypeScreenSchemeId}/mapping/remove`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetProjectsForIssueTypeScreenSchemeParams {
    path: { issueTypeScreenSchemeId: string };
    startAt?: number;
    maxResults?: number;
    query?: string;
}

/**
 * Get issue type screen scheme projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectsForIssueTypeScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectsForIssueTypeScreenSchemeParams): Promise<Types.PageBeanProjectDetails> {
    const path = `/rest/api/2/issuetypescreenscheme/${params.path.issueTypeScreenSchemeId}/project`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      query: params.query,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanProjectDetails>;
}

/**
 * Get field reference data (GET)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAutoComplete(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.JQLReferenceData> {
    const path = '/rest/api/2/jql/autocompletedata';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JQLReferenceData>;
}

export interface GetAutoCompletePostParams {
    body?: Types.SearchAutoCompleteFilter;
}

/**
 * Get field reference data (POST)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAutoCompletePost(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAutoCompletePostParams): Promise<Types.JQLReferenceData> {
    const path = '/rest/api/2/jql/autocompletedata';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JQLReferenceData>;
}

export interface GetFieldAutoCompleteForQueryStringParams {
    fieldName?: string;
    fieldValue?: string;
    predicateName?: string;
    predicateValue?: string;
}

/**
 * Get field auto complete suggestions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getFieldAutoCompleteForQueryString(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFieldAutoCompleteForQueryStringParams): Promise<Types.AutoCompleteSuggestions> {
    const path = '/rest/api/2/jql/autocompletedata/suggestions';
    const queryParams = {
      fieldName: params.fieldName,
      fieldValue: params.fieldValue,
      predicateName: params.predicateName,
      predicateValue: params.predicateValue,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.AutoCompleteSuggestions>;
}

export interface GetPrecomputationsParams {
    functionKey?: Array<string>;
    startAt?: number;
    maxResults?: number;
    orderBy?: string;
}

/**
 * Get precomputations (apps)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPrecomputations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPrecomputationsParams): Promise<Types.PageBean2JqlFunctionPrecomputationBean> {
    const path = '/rest/api/2/jql/function/computation';
    const queryParams = {
      functionKey: params.functionKey,
      startAt: params.startAt,
      maxResults: params.maxResults,
      orderBy: params.orderBy,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBean2JqlFunctionPrecomputationBean>;
}

export interface UpdatePrecomputationsParams {
    skipNotFoundPrecomputations?: boolean;
    body?: Types.JqlFunctionPrecomputationUpdateRequestBean;
}

/**
 * Update precomputations (apps)
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePrecomputations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdatePrecomputationsParams): Promise<Types.JqlFunctionPrecomputationUpdateResponse> {
    const path = '/rest/api/2/jql/function/computation';
    const queryParams = {
      skipNotFoundPrecomputations: params.skipNotFoundPrecomputations,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JqlFunctionPrecomputationUpdateResponse>;
}

export interface GetPrecomputationsByIDParams {
    orderBy?: string;
    body?: Types.JqlFunctionPrecomputationGetByIdRequest;
}

/**
 * Get precomputations by ID (apps)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPrecomputationsByID(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPrecomputationsByIDParams): Promise<Types.JqlFunctionPrecomputationGetByIdResponse> {
    const path = '/rest/api/2/jql/function/computation/search';
    const queryParams = {
      orderBy: params.orderBy,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JqlFunctionPrecomputationGetByIdResponse>;
}

export interface MatchIssuesParams {
    body?: Types.IssuesAndJQLQueries;
}

/**
 * Check issues against JQL
 * @forge-scopes-asApp read:jira-work
 */
export async function matchIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MatchIssuesParams): Promise<Types.IssueMatches> {
    const path = '/rest/api/2/jql/match';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueMatches>;
}

export interface ParseJqlQueriesParams {
    validation: "strict" | "warn" | "none";
    body?: Types.JqlQueriesToParse;
}

/**
 * Parse JQL query
 * @forge-scopes-asApp read:jira-work
 */
export async function parseJqlQueries(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ParseJqlQueriesParams): Promise<Types.ParsedJqlQueries> {
    const path = '/rest/api/2/jql/parse';
    const queryParams = {
      validation: params.validation,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ParsedJqlQueries>;
}

export interface MigrateQueriesParams {
    body?: Types.JQLPersonalDataMigrationRequest;
}

/**
 * Convert user identifiers to account IDs in JQL queries
 * @forge-scopes-asApp read:jira-work
 */
export async function migrateQueries(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MigrateQueriesParams): Promise<Types.ConvertedJQLQueries> {
    const path = '/rest/api/2/jql/pdcleaner';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ConvertedJQLQueries>;
}

export interface SanitiseJqlQueriesParams {
    body?: Types.JqlQueriesToSanitize;
}

/**
 * Sanitize JQL queries
 * @forge-scopes-asApp read:jira-work
 */
export async function sanitiseJqlQueries(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SanitiseJqlQueriesParams): Promise<Types.SanitizedJqlQueries> {
    const path = '/rest/api/2/jql/sanitize';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SanitizedJqlQueries>;
}

export interface GetAllLabelsParams {
    startAt?: number;
    maxResults?: number;
}

/**
 * Get all labels
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllLabels(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllLabelsParams): Promise<Types.PageBeanString> {
    const path = '/rest/api/2/label';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanString>;
}

/**
 * Get approximate license count
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getApproximateLicenseCount(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.LicenseMetric> {
    const path = '/rest/api/2/license/approximateLicenseCount';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LicenseMetric>;
}

export interface GetApproximateApplicationLicenseCountParams {
    path: { applicationKey: string };
}

/**
 * Get approximate application license count
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getApproximateApplicationLicenseCount(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetApproximateApplicationLicenseCountParams): Promise<Types.LicenseMetric> {
    const path = `/rest/api/2/license/approximateLicenseCount/product/${params.path.applicationKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.LicenseMetric>;
}

export interface GetMyPermissionsParams {
    projectKey?: string;
    projectId?: string;
    issueKey?: string;
    issueId?: string;
    permissions?: string;
    projectUuid?: string;
    projectConfigurationUuid?: string;
    commentId?: string;
}

/**
 * Get my permissions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getMyPermissions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetMyPermissionsParams): Promise<Types.Permissions> {
    const path = '/rest/api/2/mypermissions';
    const queryParams = {
      projectKey: params.projectKey,
      projectId: params.projectId,
      issueKey: params.issueKey,
      issueId: params.issueId,
      permissions: params.permissions,
      projectUuid: params.projectUuid,
      projectConfigurationUuid: params.projectConfigurationUuid,
      commentId: params.commentId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Permissions>;
}

export interface GetPreferenceParams {
    key: string;
}

/**
 * Get preference
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPreference(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPreferenceParams): Promise<string> {
    const path = '/rest/api/2/mypreferences';
    const queryParams = {
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<string>;
}

export interface SetPreferenceParams {
    key: string;
    body?: string;
}

/**
 * Set preference
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setPreference(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetPreferenceParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/mypreferences';
    const queryParams = {
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemovePreferenceParams {
    key: string;
}

/**
 * Delete preference
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removePreference(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemovePreferenceParams): Promise<void> {
    const path = '/rest/api/2/mypreferences';
    const queryParams = {
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get locale
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getLocale(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.Locale> {
    const path = '/rest/api/2/mypreferences/locale';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Locale>;
}

export interface SetLocaleParams {
    body?: Types.Locale;
}

/**
 * Set locale
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function setLocale(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetLocaleParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/mypreferences/locale';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetCurrentUserParams {
    expand?: string;
}

/**
 * Get current user
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getCurrentUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetCurrentUserParams): Promise<Types.User> {
    const path = '/rest/api/2/myself';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.User>;
}

export interface GetNotificationSchemesParams {
    startAt?: string;
    maxResults?: string;
    id?: Array<string>;
    projectId?: Array<string>;
    onlyDefault?: boolean;
    expand?: string;
}

/**
 * Get notification schemes paginated
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getNotificationSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetNotificationSchemesParams): Promise<Types.PageBeanNotificationScheme> {
    const path = '/rest/api/2/notificationscheme';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      projectId: params.projectId,
      onlyDefault: params.onlyDefault,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanNotificationScheme>;
}

export interface CreateNotificationSchemeParams {
    body?: Types.CreateNotificationSchemeDetails;
}

/**
 * Create notification scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createNotificationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateNotificationSchemeParams): Promise<Types.NotificationSchemeId> {
    const path = '/rest/api/2/notificationscheme';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.NotificationSchemeId>;
}

export interface GetNotificationSchemeToProjectMappingsParams {
    startAt?: string;
    maxResults?: string;
    notificationSchemeId?: Array<string>;
    projectId?: Array<string>;
}

/**
 * Get projects using notification schemes paginated
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getNotificationSchemeToProjectMappings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetNotificationSchemeToProjectMappingsParams): Promise<Types.PageBeanNotificationSchemeAndProjectMappingJsonBean> {
    const path = '/rest/api/2/notificationscheme/project';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      notificationSchemeId: params.notificationSchemeId,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanNotificationSchemeAndProjectMappingJsonBean>;
}

export interface GetNotificationSchemeParams {
    path: { id: string };
    expand?: string;
}

/**
 * Get notification scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getNotificationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetNotificationSchemeParams): Promise<Types.NotificationScheme> {
    const path = `/rest/api/2/notificationscheme/${params.path.id}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.NotificationScheme>;
}

export interface UpdateNotificationSchemeParams {
    path: { id: string };
    body?: Types.UpdateNotificationSchemeDetails;
}

/**
 * Update notification scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateNotificationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateNotificationSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/notificationscheme/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface AddNotificationsParams {
    path: { id: string };
    body?: Types.AddNotificationsDetails;
}

/**
 * Add notifications to notification scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addNotifications(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddNotificationsParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/notificationscheme/${params.path.id}/notification`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteNotificationSchemeParams {
    path: { notificationSchemeId: string };
}

/**
 * Delete notification scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteNotificationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteNotificationSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/notificationscheme/${params.path.notificationSchemeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveNotificationFromNotificationSchemeParams {
    path: { notificationSchemeId: string; notificationId: string };
}

/**
 * Remove notification from notification scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeNotificationFromNotificationScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveNotificationFromNotificationSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/notificationscheme/${params.path.notificationSchemeId}/notification/${params.path.notificationId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

/**
 * Get all permissions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllPermissions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.Permissions> {
    const path = '/rest/api/2/permissions';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Permissions>;
}

export interface GetBulkPermissionsParams {
    body?: Types.BulkPermissionsRequestBean;
}

/**
 * Get bulk permissions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBulkPermissions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBulkPermissionsParams): Promise<Types.BulkPermissionGrants> {
    const path = '/rest/api/2/permissions/check';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BulkPermissionGrants>;
}

export interface GetPermittedProjectsParams {
    body?: Types.PermissionsKeysBean;
}

/**
 * Get permitted projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getPermittedProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPermittedProjectsParams): Promise<Types.PermittedProjects> {
    const path = '/rest/api/2/permissions/project';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermittedProjects>;
}

export interface GetAllPermissionSchemesParams {
    expand?: string;
}

/**
 * Get all permission schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllPermissionSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllPermissionSchemesParams): Promise<Types.PermissionSchemes> {
    const path = '/rest/api/2/permissionscheme';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionSchemes>;
}

export interface CreatePermissionSchemeParams {
    expand?: string;
    body?: Types.PermissionScheme;
}

/**
 * Create permission scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPermissionScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreatePermissionSchemeParams): Promise<Types.PermissionScheme> {
    const path = '/rest/api/2/permissionscheme';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionScheme>;
}

export interface GetPermissionSchemeParams {
    path: { schemeId: string };
    expand?: string;
}

/**
 * Get permission scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPermissionScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPermissionSchemeParams): Promise<Types.PermissionScheme> {
    const path = `/rest/api/2/permissionscheme/${params.path.schemeId}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionScheme>;
}

export interface UpdatePermissionSchemeParams {
    path: { schemeId: string };
    expand?: string;
    body?: Types.PermissionScheme;
}

/**
 * Update permission scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePermissionScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdatePermissionSchemeParams): Promise<Types.PermissionScheme> {
    const path = `/rest/api/2/permissionscheme/${params.path.schemeId}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionScheme>;
}

export interface DeletePermissionSchemeParams {
    path: { schemeId: string };
}

/**
 * Delete permission scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePermissionScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeletePermissionSchemeParams): Promise<void> {
    const path = `/rest/api/2/permissionscheme/${params.path.schemeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPermissionSchemeGrantsParams {
    path: { schemeId: string };
    expand?: string;
}

/**
 * Get permission scheme grants
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPermissionSchemeGrants(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPermissionSchemeGrantsParams): Promise<Types.PermissionGrants> {
    const path = `/rest/api/2/permissionscheme/${params.path.schemeId}/permission`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionGrants>;
}

export interface CreatePermissionGrantParams {
    path: { schemeId: string };
    expand?: string;
    body?: Types.PermissionGrant;
}

/**
 * Create permission grant
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPermissionGrant(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreatePermissionGrantParams): Promise<Types.PermissionGrant> {
    const path = `/rest/api/2/permissionscheme/${params.path.schemeId}/permission`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionGrant>;
}

export interface GetPermissionSchemeGrantParams {
    path: { schemeId: string; permissionId: string };
    expand?: string;
}

/**
 * Get permission scheme grant
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPermissionSchemeGrant(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPermissionSchemeGrantParams): Promise<Types.PermissionGrant> {
    const path = `/rest/api/2/permissionscheme/${params.path.schemeId}/permission/${params.path.permissionId}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionGrant>;
}

export interface DeletePermissionSchemeEntityParams {
    path: { schemeId: string; permissionId: string };
}

/**
 * Delete permission scheme grant
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePermissionSchemeEntity(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeletePermissionSchemeEntityParams): Promise<void> {
    const path = `/rest/api/2/permissionscheme/${params.path.schemeId}/permission/${params.path.permissionId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPlansParams {
    includeTrashed?: boolean;
    includeArchived?: boolean;
    cursor?: string;
    maxResults?: number;
}

/**
 * Get plans paginated
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPlans(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPlansParams): Promise<Types.PageWithCursorGetPlanResponseForPage> {
    const path = '/rest/api/2/plans/plan';
    const queryParams = {
      includeTrashed: params.includeTrashed,
      includeArchived: params.includeArchived,
      cursor: params.cursor,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageWithCursorGetPlanResponseForPage>;
}

export interface CreatePlanParams {
    useGroupId?: boolean;
    body?: Types.CreatePlanRequest;
}

/**
 * Create plan
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPlan(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreatePlanParams): Promise<number> {
    const path = '/rest/api/2/plans/plan';
    const queryParams = {
      useGroupId: params.useGroupId,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<number>;
}

export interface GetPlanParams {
    path: { planId: string };
    useGroupId?: boolean;
}

/**
 * Get plan
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPlan(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPlanParams): Promise<Types.GetPlanResponse> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}`;
    const queryParams = {
      useGroupId: params.useGroupId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GetPlanResponse>;
}

export interface UpdatePlanParams {
    path: { planId: string };
    useGroupId?: boolean;
    body?: Record<string, unknown>;
}

/**
 * Update plan
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePlan(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdatePlanParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}`;
    const queryParams = {
      useGroupId: params.useGroupId,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface ArchivePlanParams {
    path: { planId: string };
}

/**
 * Archive plan
 * @forge-scopes-asApp read:jira-work
 */
export async function archivePlan(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ArchivePlanParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/archive`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DuplicatePlanParams {
    path: { planId: string };
    body?: Types.DuplicatePlanRequest;
}

/**
 * Duplicate plan
 * @forge-scopes-asApp read:jira-work
 */
export async function duplicatePlan(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DuplicatePlanParams): Promise<number> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/duplicate`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<number>;
}

export interface GetTeamsParams {
    path: { planId: string };
    cursor?: string;
    maxResults?: number;
}

/**
 * Get teams in plan paginated
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTeams(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetTeamsParams): Promise<Types.PageWithCursorGetTeamResponseForPage> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team`;
    const queryParams = {
      cursor: params.cursor,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageWithCursorGetTeamResponseForPage>;
}

export interface AddAtlassianTeamParams {
    path: { planId: string };
    body?: Types.AddAtlassianTeamRequest;
}

/**
 * Add Atlassian team to plan
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addAtlassianTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddAtlassianTeamParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/atlassian`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetAtlassianTeamParams {
    path: { planId: string; atlassianTeamId: string };
}

/**
 * Get Atlassian team in plan
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAtlassianTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAtlassianTeamParams): Promise<Types.GetAtlassianTeamResponse> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/atlassian/${params.path.atlassianTeamId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GetAtlassianTeamResponse>;
}

export interface UpdateAtlassianTeamParams {
    path: { planId: string; atlassianTeamId: string };
    body?: Record<string, unknown>;
}

/**
 * Update Atlassian team in plan
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateAtlassianTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateAtlassianTeamParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/atlassian/${params.path.atlassianTeamId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveAtlassianTeamParams {
    path: { planId: string; atlassianTeamId: string };
}

/**
 * Remove Atlassian team from plan
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeAtlassianTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveAtlassianTeamParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/atlassian/${params.path.atlassianTeamId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface CreatePlanOnlyTeamParams {
    path: { planId: string };
    body?: Types.CreatePlanOnlyTeamRequest;
}

/**
 * Create plan-only team
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPlanOnlyTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreatePlanOnlyTeamParams): Promise<number> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/planonly`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<number>;
}

export interface GetPlanOnlyTeamParams {
    path: { planId: string; planOnlyTeamId: string };
}

/**
 * Get plan-only team
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPlanOnlyTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPlanOnlyTeamParams): Promise<Types.GetPlanOnlyTeamResponse> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/planonly/${params.path.planOnlyTeamId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.GetPlanOnlyTeamResponse>;
}

export interface UpdatePlanOnlyTeamParams {
    path: { planId: string; planOnlyTeamId: string };
    body?: Record<string, unknown>;
}

/**
 * Update plan-only team
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePlanOnlyTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdatePlanOnlyTeamParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/planonly/${params.path.planOnlyTeamId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeletePlanOnlyTeamParams {
    path: { planId: string; planOnlyTeamId: string };
}

/**
 * Delete plan-only team
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePlanOnlyTeam(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeletePlanOnlyTeamParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/team/planonly/${params.path.planOnlyTeamId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface TrashPlanParams {
    path: { planId: string };
}

/**
 * Trash plan
 * @forge-scopes-asApp read:jira-work
 */
export async function trashPlan(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: TrashPlanParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/plans/plan/${params.path.planId}/trash`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

/**
 * Get priorities
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getPriorities(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.Priority>> {
    const path = '/rest/api/2/priority';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Priority>>;
}

export interface CreatePriorityParams {
    body?: Types.CreatePriorityDetails;
}

/**
 * Create priority
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function createPriority(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreatePriorityParams): Promise<Types.PriorityId> {
    const path = '/rest/api/2/priority';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PriorityId>;
}

export interface SetDefaultPriorityParams {
    body?: Types.SetDefaultPriorityRequest;
}

/**
 * Set default priority
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setDefaultPriority(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetDefaultPriorityParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/priority/default';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface MovePrioritiesParams {
    body?: Types.ReorderIssuePriorities;
}

/**
 * Move priorities
 * @forge-scopes-asApp read:jira-work
 */
export async function movePriorities(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MovePrioritiesParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/priority/move';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface SearchPrioritiesParams {
    startAt?: string;
    maxResults?: string;
    id?: Array<string>;
    projectId?: Array<string>;
    priorityName?: string;
    onlyDefault?: boolean;
    expand?: string;
}

/**
 * Search priorities
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function searchPriorities(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchPrioritiesParams): Promise<Types.PageBeanPriority> {
    const path = '/rest/api/2/priority/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      projectId: params.projectId,
      priorityName: params.priorityName,
      onlyDefault: params.onlyDefault,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanPriority>;
}

export interface GetPriorityParams {
    path: { id: string };
}

/**
 * Get priority
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPriority(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPriorityParams): Promise<Types.Priority> {
    const path = `/rest/api/2/priority/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Priority>;
}

export interface UpdatePriorityParams {
    path: { id: string };
    body?: Types.UpdatePriorityDetails;
}

/**
 * Update priority
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function updatePriority(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdatePriorityParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/priority/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeletePriorityParams {
    path: { id: string };
}

/**
 * Delete priority
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePriority(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeletePriorityParams): Promise<void> {
    const path = `/rest/api/2/priority/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetPrioritySchemesParams {
    startAt?: string;
    maxResults?: string;
    priorityId?: Array<number>;
    schemeId?: Array<number>;
    schemeName?: string;
    onlyDefault?: boolean;
    orderBy?: "name" | "+name" | "-name";
    expand?: string;
}

/**
 * Get priority schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPrioritySchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPrioritySchemesParams): Promise<Types.PageBeanPrioritySchemeWithPaginatedPrioritiesAndProjects> {
    const path = '/rest/api/2/priorityscheme';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      priorityId: params.priorityId,
      schemeId: params.schemeId,
      schemeName: params.schemeName,
      onlyDefault: params.onlyDefault,
      orderBy: params.orderBy,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanPrioritySchemeWithPaginatedPrioritiesAndProjects>;
}

export interface CreatePrioritySchemeParams {
    body?: Types.CreatePrioritySchemeDetails;
}

/**
 * Create priority scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createPriorityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreatePrioritySchemeParams): Promise<Types.PrioritySchemeId> {
    const path = '/rest/api/2/priorityscheme';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PrioritySchemeId>;
}

export interface SuggestedPrioritiesForMappingsParams {
    body?: Types.SuggestedMappingsRequestBean;
}

/**
 * Suggested priorities for mappings
 * @forge-scopes-asApp read:jira-work
 */
export async function suggestedPrioritiesForMappings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SuggestedPrioritiesForMappingsParams): Promise<Types.PageBeanPriorityWithSequence> {
    const path = '/rest/api/2/priorityscheme/mappings';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanPriorityWithSequence>;
}

export interface GetAvailablePrioritiesByPrioritySchemeParams {
    startAt?: string;
    maxResults?: string;
    query?: string;
    schemeId: string;
    exclude?: Array<string>;
}

/**
 * Get available priorities by priority scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvailablePrioritiesByPriorityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAvailablePrioritiesByPrioritySchemeParams): Promise<Types.PageBeanPriorityWithSequence> {
    const path = '/rest/api/2/priorityscheme/priorities/available';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      query: params.query,
      schemeId: params.schemeId,
      exclude: params.exclude,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanPriorityWithSequence>;
}

export interface UpdatePrioritySchemeParams {
    path: { schemeId: string };
    body?: Types.UpdatePrioritySchemeRequestBean;
}

/**
 * Update priority scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updatePriorityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdatePrioritySchemeParams): Promise<Types.UpdatePrioritySchemeResponseBean> {
    const path = `/rest/api/2/priorityscheme/${params.path.schemeId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UpdatePrioritySchemeResponseBean>;
}

export interface DeletePrioritySchemeParams {
    path: { schemeId: string };
}

/**
 * Delete priority scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deletePriorityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeletePrioritySchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/priorityscheme/${params.path.schemeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetPrioritiesByPrioritySchemeParams {
    path: { schemeId: string };
    startAt?: string;
    maxResults?: string;
}

/**
 * Get priorities by priority scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getPrioritiesByPriorityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetPrioritiesByPrioritySchemeParams): Promise<Types.PageBeanPriorityWithSequence> {
    const path = `/rest/api/2/priorityscheme/${params.path.schemeId}/priorities`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanPriorityWithSequence>;
}

export interface GetProjectsByPrioritySchemeParams {
    path: { schemeId: string };
    startAt?: string;
    maxResults?: string;
    projectId?: Array<number>;
    query?: string;
}

/**
 * Get projects by priority scheme
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectsByPriorityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectsByPrioritySchemeParams): Promise<Types.PageBeanProject> {
    const path = `/rest/api/2/priorityscheme/${params.path.schemeId}/projects`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
      query: params.query,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanProject>;
}

export interface GetAllProjectsParams {
    expand?: string;
    recent?: number;
    properties?: Array<string>;
}

/**
 * Get all projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 * @deprecated
 */
export async function getAllProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllProjectsParams): Promise<Array<Types.Project>> {
    const path = '/rest/api/2/project';
    const queryParams = {
      expand: params.expand,
      recent: params.recent,
      properties: params.properties,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Project>>;
}

export interface CreateProjectParams {
    body?: Types.CreateProjectDetails;
}

/**
 * Create project
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function createProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateProjectParams): Promise<Types.ProjectIdentifiers> {
    const path = '/rest/api/2/project';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectIdentifiers>;
}

export interface CreateProjectWithCustomTemplateParams {
    body?: Types.ProjectCustomTemplateCreateRequestDTO;
}

/**
 * Create custom project
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function createProjectWithCustomTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateProjectWithCustomTemplateParams): Promise<void> {
    const path = '/rest/api/2/project-template';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface EditTemplateParams {
    body?: Types.EditTemplateRequest;
}

/**
 * Edit a custom project template
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function editTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: EditTemplateParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/project-template/edit-template';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface LiveTemplateParams {
    projectId?: string;
    templateKey?: string;
}

/**
 * Gets a custom project template
 * @forge-scopes-asApp read:jira-work
 */
export async function liveTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: LiveTemplateParams): Promise<Types.ProjectTemplateModel> {
    const path = '/rest/api/2/project-template/live-template';
    const queryParams = {
      projectId: params.projectId,
      templateKey: params.templateKey,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectTemplateModel>;
}

export interface RemoveTemplateParams {
    templateKey: string;
}

/**
 * Deletes a custom project template
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveTemplateParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/project-template/remove-template';
    const queryParams = {
      templateKey: params.templateKey,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface SaveTemplateParams {
    body?: Types.SaveTemplateRequest;
}

/**
 * Save a custom project template
 * @forge-scopes-asApp read:jira-work
 */
export async function saveTemplate(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SaveTemplateParams): Promise<Types.SaveTemplateResponse> {
    const path = '/rest/api/2/project-template/save-template';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SaveTemplateResponse>;
}

export interface GetRecentParams {
    expand?: string;
    properties?: Array<StringList>;
}

/**
 * Get recent projects
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRecent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRecentParams): Promise<Array<Types.Project>> {
    const path = '/rest/api/2/project/recent';
    const queryParams = {
      expand: params.expand,
      properties: params.properties,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Project>>;
}

export interface SearchProjectsParams {
    startAt?: number;
    maxResults?: number;
    orderBy?: "category" | "-category" | "+category" | "key" | "-key" | "+key" | "name" | "-name" | "+name" | "owner" | "-owner" | "+owner" | "issueCount" | "-issueCount" | "+issueCount" | "lastIssueUpdatedDate" | "-lastIssueUpdatedDate" | "+lastIssueUpdatedDate" | "archivedDate" | "+archivedDate" | "-archivedDate" | "deletedDate" | "+deletedDate" | "-deletedDate";
    id?: Array<number>;
    keys?: Array<string>;
    query?: string;
    typeKey?: string;
    categoryId?: number;
    action?: "view" | "browse" | "edit" | "create";
    expand?: string;
    status?: Array<"live" | "archived" | "deleted">;
    properties?: Array<StringList>;
    propertyQuery?: string;
}

/**
 * Get projects paginated
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function searchProjects(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchProjectsParams): Promise<Types.PageBeanProject> {
    const path = '/rest/api/2/project/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      orderBy: params.orderBy,
      id: params.id,
      keys: params.keys,
      query: params.query,
      typeKey: params.typeKey,
      categoryId: params.categoryId,
      action: params.action,
      expand: params.expand,
      status: params.status,
      properties: params.properties,
      propertyQuery: params.propertyQuery,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanProject>;
}

/**
 * Get all project types
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getAllProjectTypes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ProjectType>> {
    const path = '/rest/api/2/project/type';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ProjectType>>;
}

/**
 * Get licensed project types
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getAllAccessibleProjectTypes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ProjectType>> {
    const path = '/rest/api/2/project/type/accessible';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ProjectType>>;
}

export interface GetProjectTypeByKeyParams {
    path: { projectTypeKey: string };
}

/**
 * Get project type by key
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectTypeByKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectTypeByKeyParams): Promise<Types.ProjectType> {
    const path = `/rest/api/2/project/type/${params.path.projectTypeKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectType>;
}

export interface GetAccessibleProjectTypeByKeyParams {
    path: { projectTypeKey: string };
}

/**
 * Get accessible project type by key
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getAccessibleProjectTypeByKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAccessibleProjectTypeByKeyParams): Promise<Types.ProjectType> {
    const path = `/rest/api/2/project/type/${params.path.projectTypeKey}/accessible`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectType>;
}

export interface GetProjectParams {
    path: { projectIdOrKey: string };
    expand?: string;
    properties?: Array<string>;
}

/**
 * Get project
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectParams): Promise<Types.Project> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}`;
    const queryParams = {
      expand: params.expand,
      properties: params.properties,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Project>;
}

export interface UpdateProjectParams {
    path: { projectIdOrKey: string };
    expand?: string;
    body?: Types.UpdateProjectDetails;
}

/**
 * Update project
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function updateProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateProjectParams): Promise<Types.Project> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Project>;
}

export interface DeleteProjectParams {
    path: { projectIdOrKey: string };
    enableUndo?: boolean;
}

/**
 * Delete project
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function deleteProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteProjectParams): Promise<void> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}`;
    const queryParams = {
      enableUndo: params.enableUndo,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface ArchiveProjectParams {
    path: { projectIdOrKey: string };
}

/**
 * Archive project
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function archiveProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ArchiveProjectParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/archive`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateProjectAvatarParams {
    path: { projectIdOrKey: string };
    body?: Types.Avatar;
}

/**
 * Set project avatar
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function updateProjectAvatar(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateProjectAvatarParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/avatar`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteProjectAvatarParams {
    path: { projectIdOrKey: string; id: string };
}

/**
 * Delete project avatar
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function deleteProjectAvatar(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteProjectAvatarParams): Promise<void> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/avatar/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface CreateProjectAvatarParams {
    path: { projectIdOrKey: string };
    x?: number;
    y?: number;
    size?: number;
    body?: Record<string, unknown>;
}

/**
 * Load project avatar
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function createProjectAvatar(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateProjectAvatarParams): Promise<Types.Avatar> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/avatar2`;
    const queryParams = {
      x: params.x,
      y: params.y,
      size: params.size,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Avatar>;
}

export interface GetAllProjectAvatarsParams {
    path: { projectIdOrKey: string };
}

/**
 * Get all project avatars
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getAllProjectAvatars(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllProjectAvatarsParams): Promise<Types.ProjectAvatars> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/avatars`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectAvatars>;
}

export interface GetProjectClassificationConfigParams {
    path: { projectIdOrKey: string };
}

/**
 * Get the classification configuration for a project
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectClassificationConfig(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectClassificationConfigParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/classification-config`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetDefaultProjectClassificationParams {
    path: { projectIdOrKey: string };
}

/**
 * Get the default data classification level of a project
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getDefaultProjectClassification(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDefaultProjectClassificationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/classification-level/default`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateDefaultProjectClassificationParams {
    path: { projectIdOrKey: string };
    body?: Types.UpdateDefaultProjectClassificationBean;
}

/**
 * Update the default data classification level of a project
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function updateDefaultProjectClassification(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateDefaultProjectClassificationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/classification-level/default`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface RemoveDefaultProjectClassificationParams {
    path: { projectIdOrKey: string };
}

/**
 * Remove the default data classification level from a project
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function removeDefaultProjectClassification(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveDefaultProjectClassificationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/classification-level/default`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetProjectComponentsPaginatedParams {
    path: { projectIdOrKey: string };
    startAt?: number;
    maxResults?: number;
    orderBy?: "description" | "-description" | "+description" | "issueCount" | "-issueCount" | "+issueCount" | "lead" | "-lead" | "+lead" | "name" | "-name" | "+name";
    componentSource?: "jira" | "compass" | "auto";
    query?: string;
}

/**
 * Get project components paginated
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectComponentsPaginated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectComponentsPaginatedParams): Promise<Types.PageBeanComponentWithIssueCount> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/component`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      orderBy: params.orderBy,
      componentSource: params.componentSource,
      query: params.query,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanComponentWithIssueCount>;
}

export interface GetProjectComponentsParams {
    path: { projectIdOrKey: string };
    componentSource?: "jira" | "compass" | "auto";
}

/**
 * Get project components
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectComponents(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectComponentsParams): Promise<Array<Types.ProjectComponent>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/components`;
    const queryParams = {
      componentSource: params.componentSource,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ProjectComponent>>;
}

export interface DeleteProjectAsynchronouslyParams {
    path: { projectIdOrKey: string };
}

/**
 * Delete project asynchronously
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function deleteProjectAsynchronously(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteProjectAsynchronouslyParams): Promise<void> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/delete`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFeaturesForProjectParams {
    path: { projectIdOrKey: string };
}

/**
 * Get project features
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getFeaturesForProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFeaturesForProjectParams): Promise<Types.ContainerForProjectFeatures> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/features`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContainerForProjectFeatures>;
}

export interface ToggleFeatureForProjectParams {
    path: { projectIdOrKey: string; featureKey: string };
    body?: Types.ProjectFeatureState;
}

/**
 * Set project feature state
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function toggleFeatureForProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ToggleFeatureForProjectParams): Promise<Types.ContainerForProjectFeatures> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/features/${params.path.featureKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContainerForProjectFeatures>;
}

export interface GetProjectPropertyKeysParams {
    path: { projectIdOrKey: string };
}

/**
 * Get project property keys
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectPropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectPropertyKeysParams): Promise<Types.PropertyKeys> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetProjectPropertyParams {
    path: { projectIdOrKey: string; propertyKey: string };
}

/**
 * Get project property
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectPropertyParams): Promise<Types.EntityProperty> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetProjectPropertyParams {
    path: { projectIdOrKey: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set project property
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function setProjectProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetProjectPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteProjectPropertyParams {
    path: { projectIdOrKey: string; propertyKey: string };
}

/**
 * Delete project property
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function deleteProjectProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteProjectPropertyParams): Promise<void> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RestoreParams {
    path: { projectIdOrKey: string };
}

/**
 * Restore deleted or archived project
 * @forge-scopes-asApp read:jira-work
 */
export async function restore(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RestoreParams): Promise<Types.Project> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/restore`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Project>;
}

export interface GetProjectRolesParams {
    path: { projectIdOrKey: string };
}

/**
 * Get project roles for project
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectRoles(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectRolesParams): Promise<Record<string, string>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/role`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, string>>;
}

export interface GetProjectRoleParams {
    path: { projectIdOrKey: string; id: string };
    excludeInactiveUsers?: boolean;
}

/**
 * Get project role for project
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectRoleParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/role/${params.path.id}`;
    const queryParams = {
      excludeInactiveUsers: params.excludeInactiveUsers,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface AddActorUsersParams {
    path: { projectIdOrKey: string; id: string };
    body?: Types.ActorsMap;
}

/**
 * Add actors to project role
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function addActorUsers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddActorUsersParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/role/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface SetActorsParams {
    path: { projectIdOrKey: string; id: string };
    body?: Types.ProjectRoleActorsUpdateBean;
}

/**
 * Set actors for project role
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setActors(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetActorsParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/role/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface DeleteActorParams {
    path: { projectIdOrKey: string; id: string };
    user?: string;
    group?: string;
    groupId?: string;
}

/**
 * Delete actors from project role
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteActor(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteActorParams): Promise<void> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/role/${params.path.id}`;
    const queryParams = {
      user: params.user,
      group: params.group,
      groupId: params.groupId,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetProjectRoleDetailsParams {
    path: { projectIdOrKey: string };
    currentMember?: boolean;
    excludeConnectAddons?: boolean;
    excludeOtherServiceRoles?: boolean;
}

/**
 * Get project role details
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectRoleDetails(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectRoleDetailsParams): Promise<Array<Types.ProjectRoleDetails>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/roledetails`;
    const queryParams = {
      currentMember: params.currentMember,
      excludeConnectAddons: params.excludeConnectAddons,
      excludeOtherServiceRoles: params.excludeOtherServiceRoles,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ProjectRoleDetails>>;
}

export interface GetAllStatusesParams {
    path: { projectIdOrKey: string };
}

/**
 * Get all statuses for project
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllStatuses(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllStatusesParams): Promise<Array<Types.IssueTypeWithStatus>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/statuses`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.IssueTypeWithStatus>>;
}

export interface GetProjectVersionsPaginatedParams {
    path: { projectIdOrKey: string };
    startAt?: number;
    maxResults?: number;
    orderBy?: "description" | "-description" | "+description" | "name" | "-name" | "+name" | "releaseDate" | "-releaseDate" | "+releaseDate" | "sequence" | "-sequence" | "+sequence" | "startDate" | "-startDate" | "+startDate";
    query?: string;
    status?: string;
    expand?: string;
}

/**
 * Get project versions paginated
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectVersionsPaginated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectVersionsPaginatedParams): Promise<Types.PageBeanVersion> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/version`;
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      orderBy: params.orderBy,
      query: params.query,
      status: params.status,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanVersion>;
}

export interface GetProjectVersionsParams {
    path: { projectIdOrKey: string };
    expand?: string;
}

/**
 * Get project versions
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectVersions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectVersionsParams): Promise<Array<Types.Version>> {
    const path = `/rest/api/2/project/${params.path.projectIdOrKey}/versions`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Version>>;
}

export interface GetProjectEmailParams {
    path: { projectId: string };
}

/**
 * Get project's sender email
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectEmail(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectEmailParams): Promise<Types.ProjectEmailAddress> {
    const path = `/rest/api/2/project/${params.path.projectId}/email`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectEmailAddress>;
}

export interface UpdateProjectEmailParams {
    path: { projectId: string };
    body?: Types.ProjectEmailAddress;
}

/**
 * Set project's sender email
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function updateProjectEmail(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateProjectEmailParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/project/${params.path.projectId}/email`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetHierarchyParams {
    path: { projectId: string };
}

/**
 * Get project issue type hierarchy
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getHierarchy(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetHierarchyParams): Promise<Types.ProjectIssueTypeHierarchy> {
    const path = `/rest/api/2/project/${params.path.projectId}/hierarchy`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectIssueTypeHierarchy>;
}

export interface GetProjectIssueSecuritySchemeParams {
    path: { projectKeyOrId: string };
}

/**
 * Get project issue security scheme
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectIssueSecurityScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectIssueSecuritySchemeParams): Promise<Types.SecurityScheme> {
    const path = `/rest/api/2/project/${params.path.projectKeyOrId}/issuesecuritylevelscheme`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SecurityScheme>;
}

export interface GetNotificationSchemeForProjectParams {
    path: { projectKeyOrId: string };
    expand?: string;
}

/**
 * Get project notification scheme
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getNotificationSchemeForProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetNotificationSchemeForProjectParams): Promise<Types.NotificationScheme> {
    const path = `/rest/api/2/project/${params.path.projectKeyOrId}/notificationscheme`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.NotificationScheme>;
}

export interface GetAssignedPermissionSchemeParams {
    path: { projectKeyOrId: string };
    expand?: string;
}

/**
 * Get assigned permission scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAssignedPermissionScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAssignedPermissionSchemeParams): Promise<Types.PermissionScheme> {
    const path = `/rest/api/2/project/${params.path.projectKeyOrId}/permissionscheme`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionScheme>;
}

export interface AssignPermissionSchemeParams {
    path: { projectKeyOrId: string };
    expand?: string;
    body?: Types.IdBean;
}

/**
 * Assign permission scheme
 * @forge-scopes-asApp read:jira-work
 */
export async function assignPermissionScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssignPermissionSchemeParams): Promise<Types.PermissionScheme> {
    const path = `/rest/api/2/project/${params.path.projectKeyOrId}/permissionscheme`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PermissionScheme>;
}

export interface GetSecurityLevelsForProjectParams {
    path: { projectKeyOrId: string };
}

/**
 * Get project issue security levels
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getSecurityLevelsForProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetSecurityLevelsForProjectParams): Promise<Types.ProjectIssueSecurityLevels> {
    const path = `/rest/api/2/project/${params.path.projectKeyOrId}/securitylevel`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectIssueSecurityLevels>;
}

/**
 * Get all project categories
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getAllProjectCategories(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ProjectCategory>> {
    const path = '/rest/api/2/projectCategory';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ProjectCategory>>;
}

export interface CreateProjectCategoryParams {
    body?: Types.ProjectCategory;
}

/**
 * Create project category
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function createProjectCategory(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateProjectCategoryParams): Promise<Types.ProjectCategory> {
    const path = '/rest/api/2/projectCategory';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectCategory>;
}

export interface GetProjectCategoryByIdParams {
    path: { id: string };
}

/**
 * Get project category by ID
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectCategoryById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectCategoryByIdParams): Promise<Types.ProjectCategory> {
    const path = `/rest/api/2/projectCategory/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectCategory>;
}

export interface UpdateProjectCategoryParams {
    path: { id: string };
    body?: Types.ProjectCategory;
}

/**
 * Update project category
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function updateProjectCategory(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateProjectCategoryParams): Promise<Types.UpdatedProjectCategory> {
    const path = `/rest/api/2/projectCategory/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UpdatedProjectCategory>;
}

export interface RemoveProjectCategoryParams {
    path: { id: string };
}

/**
 * Delete project category
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function removeProjectCategory(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveProjectCategoryParams): Promise<void> {
    const path = `/rest/api/2/projectCategory/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetProjectFieldsParams {
    startAt?: number;
    maxResults?: number;
    projectId: Array<number>;
    workTypeId: Array<number>;
    fieldId?: Array<string>;
}

/**
 * Get fields for projects
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectFields(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectFieldsParams): Promise<Types.PageBean2ProjectFieldBean> {
    const path = '/rest/api/2/projects/fields';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      projectId: params.projectId,
      workTypeId: params.workTypeId,
      fieldId: params.fieldId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBean2ProjectFieldBean>;
}

export interface ValidateProjectKeyParams {
    key?: string;
}

/**
 * Validate project key
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function validateProjectKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ValidateProjectKeyParams): Promise<Types.ErrorCollection> {
    const path = '/rest/api/2/projectvalidate/key';
    const queryParams = {
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ErrorCollection>;
}

export interface GetValidProjectKeyParams {
    key?: string;
}

/**
 * Get valid project key
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getValidProjectKey(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetValidProjectKeyParams): Promise<string> {
    const path = '/rest/api/2/projectvalidate/validProjectKey';
    const queryParams = {
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<string>;
}

export interface GetValidProjectNameParams {
    name: string;
}

/**
 * Get valid project name
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getValidProjectName(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetValidProjectNameParams): Promise<string> {
    const path = '/rest/api/2/projectvalidate/validProjectName';
    const queryParams = {
      name: params.name,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<string>;
}

export interface RedactParams {
    body?: Types.BulkRedactionRequest;
}

/**
 * Redact
 * @forge-scopes-asApp read:jira-work
 */
export async function redact(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RedactParams): Promise<string> {
    const path = '/rest/api/2/redact';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<string>;
}

export interface GetRedactionStatusParams {
    path: { jobId: string };
}

/**
 * Get redaction status
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRedactionStatus(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRedactionStatusParams): Promise<Types.RedactionJobStatusResponse> {
    const path = `/rest/api/2/redact/status/${params.path.jobId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.RedactionJobStatusResponse>;
}

/**
 * Get resolutions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getResolutions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.Resolution>> {
    const path = '/rest/api/2/resolution';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Resolution>>;
}

export interface CreateResolutionParams {
    body?: Types.CreateResolutionDetails;
}

/**
 * Create resolution
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createResolution(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateResolutionParams): Promise<Types.ResolutionId> {
    const path = '/rest/api/2/resolution';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ResolutionId>;
}

export interface SetDefaultResolutionParams {
    body?: Types.SetDefaultResolutionRequest;
}

/**
 * Set default resolution
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setDefaultResolution(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetDefaultResolutionParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/resolution/default';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface MoveResolutionsParams {
    body?: Types.ReorderIssueResolutionsRequest;
}

/**
 * Move resolutions
 * @forge-scopes-asApp read:jira-work
 */
export async function moveResolutions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveResolutionsParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/resolution/move';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface SearchResolutionsParams {
    startAt?: string;
    maxResults?: string;
    id?: Array<string>;
    onlyDefault?: boolean;
}

/**
 * Search resolutions
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchResolutions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchResolutionsParams): Promise<Types.PageBeanResolutionJsonBean> {
    const path = '/rest/api/2/resolution/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      onlyDefault: params.onlyDefault,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanResolutionJsonBean>;
}

export interface GetResolutionParams {
    path: { id: string };
}

/**
 * Get resolution
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getResolution(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetResolutionParams): Promise<Types.Resolution> {
    const path = `/rest/api/2/resolution/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Resolution>;
}

export interface UpdateResolutionParams {
    path: { id: string };
    body?: Types.UpdateResolutionDetails;
}

/**
 * Update resolution
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateResolution(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateResolutionParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/resolution/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteResolutionParams {
    path: { id: string };
    replaceWith: string;
}

/**
 * Delete resolution
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteResolution(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteResolutionParams): Promise<void> {
    const path = `/rest/api/2/resolution/${params.path.id}`;
    const queryParams = {
      replaceWith: params.replaceWith,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get all project roles
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getAllProjectRoles(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ProjectRole>> {
    const path = '/rest/api/2/role';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ProjectRole>>;
}

export interface CreateProjectRoleParams {
    body?: Types.CreateUpdateRoleRequestBean;
}

/**
 * Create project role
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function createProjectRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateProjectRoleParams): Promise<Types.ProjectRole> {
    const path = '/rest/api/2/role';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface GetProjectRoleByIdParams {
    path: { id: string };
}

/**
 * Get project role by ID
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectRoleById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectRoleByIdParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/role/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface PartialUpdateProjectRoleParams {
    path: { id: string };
    body?: Types.CreateUpdateRoleRequestBean;
}

/**
 * Partial update project role
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function partialUpdateProjectRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PartialUpdateProjectRoleParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/role/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface FullyUpdateProjectRoleParams {
    path: { id: string };
    body?: Types.CreateUpdateRoleRequestBean;
}

/**
 * Fully update project role
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function fullyUpdateProjectRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FullyUpdateProjectRoleParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/role/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface DeleteProjectRoleParams {
    path: { id: string };
    swap?: number;
}

/**
 * Delete project role
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function deleteProjectRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteProjectRoleParams): Promise<void> {
    const path = `/rest/api/2/role/${params.path.id}`;
    const queryParams = {
      swap: params.swap,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetProjectRoleActorsForRoleParams {
    path: { id: string };
}

/**
 * Get default actors for project role
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectRoleActorsForRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectRoleActorsForRoleParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/role/${params.path.id}/actors`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface AddProjectRoleActorsToRoleParams {
    path: { id: string };
    body?: Types.ActorInputBean;
}

/**
 * Add default actors to project role
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function addProjectRoleActorsToRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddProjectRoleActorsToRoleParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/role/${params.path.id}/actors`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface DeleteProjectRoleActorsFromRoleParams {
    path: { id: string };
    user?: string;
    groupId?: string;
    group?: string;
}

/**
 * Delete default actors from project role
 * @forge-scopes-asApp write:jira-work, manage:jira-project
 * @forge-scopes-asUser write:jira-work, manage:jira-project
 */
export async function deleteProjectRoleActorsFromRole(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteProjectRoleActorsFromRoleParams): Promise<Types.ProjectRole> {
    const path = `/rest/api/2/role/${params.path.id}/actors`;
    const queryParams = {
      user: params.user,
      groupId: params.groupId,
      group: params.group,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ProjectRole>;
}

export interface GetScreensParams {
    startAt?: number;
    maxResults?: number;
    id?: Array<number>;
    queryString?: string;
    scope?: Array<"GLOBAL" | "TEMPLATE" | "PROJECT">;
    orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
}

/**
 * Get screens
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getScreens(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetScreensParams): Promise<Types.PageBeanScreen> {
    const path = '/rest/api/2/screens';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      queryString: params.queryString,
      scope: params.scope,
      orderBy: params.orderBy,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanScreen>;
}

export interface CreateScreenParams {
    body?: Types.ScreenDetails;
}

/**
 * Create screen
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createScreen(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateScreenParams): Promise<Types.Screen> {
    const path = '/rest/api/2/screens';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Screen>;
}

export interface AddFieldToDefaultScreenParams {
    path: { fieldId: string };
}

/**
 * Add field to default screen
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addFieldToDefaultScreen(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddFieldToDefaultScreenParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/screens/addToDefault/${params.path.fieldId}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetBulkScreenTabsParams {
    screenId?: Array<number>;
    tabId?: Array<number>;
    startAt?: number;
    maxResult?: number;
}

/**
 * Get bulk screen tabs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getBulkScreenTabs(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetBulkScreenTabsParams): Promise<void> {
    const path = '/rest/api/2/screens/tabs';
    const queryParams = {
      screenId: params.screenId,
      tabId: params.tabId,
      startAt: params.startAt,
      maxResult: params.maxResult,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface UpdateScreenParams {
    path: { screenId: string };
    body?: Types.UpdateScreenDetails;
}

/**
 * Update screen
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateScreen(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateScreenParams): Promise<Types.Screen> {
    const path = `/rest/api/2/screens/${params.path.screenId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Screen>;
}

export interface DeleteScreenParams {
    path: { screenId: string };
}

/**
 * Delete screen
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteScreen(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteScreenParams): Promise<void> {
    const path = `/rest/api/2/screens/${params.path.screenId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAvailableScreenFieldsParams {
    path: { screenId: string };
}

/**
 * Get available screen fields
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvailableScreenFields(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAvailableScreenFieldsParams): Promise<Array<Types.ScreenableField>> {
    const path = `/rest/api/2/screens/${params.path.screenId}/availableFields`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ScreenableField>>;
}

export interface GetAllScreenTabsParams {
    path: { screenId: string };
    projectKey?: string;
}

/**
 * Get all screen tabs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllScreenTabs(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllScreenTabsParams): Promise<Array<Types.ScreenableTab>> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs`;
    const queryParams = {
      projectKey: params.projectKey,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ScreenableTab>>;
}

export interface AddScreenTabParams {
    path: { screenId: string };
    body?: Types.ScreenableTab;
}

/**
 * Create screen tab
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addScreenTab(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddScreenTabParams): Promise<Types.ScreenableTab> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ScreenableTab>;
}

export interface RenameScreenTabParams {
    path: { screenId: string; tabId: string };
    body?: Types.ScreenableTab;
}

/**
 * Update screen tab
 * @forge-scopes-asApp read:jira-work
 */
export async function renameScreenTab(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RenameScreenTabParams): Promise<Types.ScreenableTab> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs/${params.path.tabId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ScreenableTab>;
}

export interface DeleteScreenTabParams {
    path: { screenId: string; tabId: string };
}

/**
 * Delete screen tab
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteScreenTab(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteScreenTabParams): Promise<void> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs/${params.path.tabId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAllScreenTabFieldsParams {
    path: { screenId: string; tabId: string };
    projectKey?: string;
}

/**
 * Get all screen tab fields
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllScreenTabFields(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllScreenTabFieldsParams): Promise<Array<Types.ScreenableField>> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs/${params.path.tabId}/fields`;
    const queryParams = {
      projectKey: params.projectKey,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ScreenableField>>;
}

export interface AddScreenTabFieldParams {
    path: { screenId: string; tabId: string };
    body?: Types.AddFieldBean;
}

/**
 * Add screen tab field
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function addScreenTabField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AddScreenTabFieldParams): Promise<Types.ScreenableField> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs/${params.path.tabId}/fields`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ScreenableField>;
}

export interface RemoveScreenTabFieldParams {
    path: { screenId: string; tabId: string; id: string };
}

/**
 * Remove screen tab field
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function removeScreenTabField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveScreenTabFieldParams): Promise<void> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs/${params.path.tabId}/fields/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface MoveScreenTabFieldParams {
    path: { screenId: string; tabId: string; id: string };
    body?: Types.MoveFieldBean;
}

/**
 * Move screen tab field
 * @forge-scopes-asApp read:jira-work
 */
export async function moveScreenTabField(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveScreenTabFieldParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs/${params.path.tabId}/fields/${params.path.id}/move`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface MoveScreenTabParams {
    path: { screenId: string; tabId: string; pos: string };
}

/**
 * Move screen tab
 * @forge-scopes-asApp read:jira-work
 */
export async function moveScreenTab(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveScreenTabParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/screens/${params.path.screenId}/tabs/${params.path.tabId}/move/${params.path.pos}`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetScreenSchemesParams {
    startAt?: number;
    maxResults?: number;
    id?: Array<number>;
    expand?: string;
    queryString?: string;
    orderBy?: "name" | "-name" | "+name" | "id" | "-id" | "+id";
}

/**
 * Get screen schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getScreenSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetScreenSchemesParams): Promise<Types.PageBeanScreenScheme> {
    const path = '/rest/api/2/screenscheme';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      id: params.id,
      expand: params.expand,
      queryString: params.queryString,
      orderBy: params.orderBy,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanScreenScheme>;
}

export interface CreateScreenSchemeParams {
    body?: Types.ScreenSchemeDetails;
}

/**
 * Create screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateScreenSchemeParams): Promise<Types.ScreenSchemeId> {
    const path = '/rest/api/2/screenscheme';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ScreenSchemeId>;
}

export interface UpdateScreenSchemeParams {
    path: { screenSchemeId: string };
    body?: Types.UpdateScreenSchemeDetails;
}

/**
 * Update screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateScreenSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/screenscheme/${params.path.screenSchemeId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteScreenSchemeParams {
    path: { screenSchemeId: string };
}

/**
 * Delete screen scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteScreenScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteScreenSchemeParams): Promise<void> {
    const path = `/rest/api/2/screenscheme/${params.path.screenSchemeId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface SearchForIssuesUsingJqlParams {
    jql?: string;
    startAt?: number;
    maxResults?: number;
    validateQuery?: "strict" | "warn" | "none" | "true" | "false";
    fields?: Array<string>;
    expand?: string;
    properties?: Array<string>;
    fieldsByKeys?: boolean;
    failFast?: boolean;
}

/**
 * Currently being removed. Search for issues using JQL (GET)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function searchForIssuesUsingJql(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchForIssuesUsingJqlParams): Promise<Types.SearchResults> {
    const path = '/rest/api/2/search';
    const queryParams = {
      jql: params.jql,
      startAt: params.startAt,
      maxResults: params.maxResults,
      validateQuery: params.validateQuery,
      fields: params.fields,
      expand: params.expand,
      properties: params.properties,
      fieldsByKeys: params.fieldsByKeys,
      failFast: params.failFast,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchResults>;
}

export interface SearchForIssuesUsingJqlPostParams {
    body?: Types.SearchRequestBean;
}

/**
 * Currently being removed. Search for issues using JQL (POST)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function searchForIssuesUsingJqlPost(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchForIssuesUsingJqlPostParams): Promise<Types.SearchResults> {
    const path = '/rest/api/2/search';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchResults>;
}

export interface CountIssuesParams {
    body?: Types.JQLCountRequestBean;
}

/**
 * Count issues using JQL
 * @forge-scopes-asApp read:jira-work
 */
export async function countIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CountIssuesParams): Promise<Types.JQLCountResultsBean> {
    const path = '/rest/api/2/search/approximate-count';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.JQLCountResultsBean>;
}

export interface SearchAndReconsileIssuesUsingJqlParams {
    jql?: string;
    nextPageToken?: string;
    maxResults?: number;
    fields?: Array<string>;
    expand?: string;
    properties?: Array<string>;
    fieldsByKeys?: boolean;
    failFast?: boolean;
    reconcileIssues?: Array<number>;
}

/**
 * Search for issues using JQL enhanced search (GET)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchAndReconsileIssuesUsingJql(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchAndReconsileIssuesUsingJqlParams): Promise<Types.SearchAndReconcileResults> {
    const path = '/rest/api/2/search/jql';
    const queryParams = {
      jql: params.jql,
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
      fields: params.fields,
      expand: params.expand,
      properties: params.properties,
      fieldsByKeys: params.fieldsByKeys,
      failFast: params.failFast,
      reconcileIssues: params.reconcileIssues,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchAndReconcileResults>;
}

export interface SearchAndReconsileIssuesUsingJqlPostParams {
    body?: Types.SearchAndReconcileRequestBean;
}

/**
 * Search for issues using JQL enhanced search (POST)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchAndReconsileIssuesUsingJqlPost(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchAndReconsileIssuesUsingJqlPostParams): Promise<Types.SearchAndReconcileResults> {
    const path = '/rest/api/2/search/jql';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SearchAndReconcileResults>;
}

export interface GetIssueSecurityLevelParams {
    path: { id: string };
}

/**
 * Get issue security level
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueSecurityLevel(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIssueSecurityLevelParams): Promise<Types.SecurityLevel> {
    const path = `/rest/api/2/securitylevel/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.SecurityLevel>;
}

/**
 * Get Jira instance info
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getServerInfo(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.ServerInformation> {
    const path = '/rest/api/2/serverInfo';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ServerInformation>;
}

/**
 * Get issue navigator default columns
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIssueNavigatorDefaultColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.ColumnItem>> {
    const path = '/rest/api/2/settings/columns';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ColumnItem>>;
}

export interface SetIssueNavigatorDefaultColumnsParams {
    body?: Record<string, unknown>;
}

/**
 * Set issue navigator default columns
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setIssueNavigatorDefaultColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetIssueNavigatorDefaultColumnsParams): Promise<void> {
    const path = '/rest/api/2/settings/columns';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get all statuses
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getStatuses(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.StatusDetails>> {
    const path = '/rest/api/2/status';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.StatusDetails>>;
}

export interface GetStatusParams {
    path: { idOrName: string };
}

/**
 * Get status
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getStatus(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetStatusParams): Promise<Types.StatusDetails> {
    const path = `/rest/api/2/status/${params.path.idOrName}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StatusDetails>;
}

/**
 * Get all status categories
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getStatusCategories(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Array<Types.StatusCategory>> {
    const path = '/rest/api/2/statuscategory';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.StatusCategory>>;
}

export interface GetStatusCategoryParams {
    path: { idOrKey: string };
}

/**
 * Get status category
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getStatusCategory(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetStatusCategoryParams): Promise<Types.StatusCategory> {
    const path = `/rest/api/2/statuscategory/${params.path.idOrKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StatusCategory>;
}

export interface GetStatusesByIdParams {
    id: Array<string>;
}

/**
 * Bulk get statuses
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getStatusesById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetStatusesByIdParams): Promise<Array<Types.JiraStatus>> {
    const path = '/rest/api/2/statuses';
    const queryParams = {
      id: params.id,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.JiraStatus>>;
}

export interface CreateStatusesParams {
    body?: Types.StatusCreateRequest;
}

/**
 * Bulk create statuses
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createStatuses(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateStatusesParams): Promise<Array<Types.JiraStatus>> {
    const path = '/rest/api/2/statuses';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.JiraStatus>>;
}

export interface UpdateStatusesParams {
    body?: Types.StatusUpdateRequest;
}

/**
 * Bulk update statuses
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateStatuses(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateStatusesParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/statuses';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteStatusesByIdParams {
    id: Array<string>;
}

/**
 * Bulk delete Statuses
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteStatusesById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteStatusesByIdParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/statuses';
    const queryParams = {
      id: params.id,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetStatusesByNameParams {
    name: Array<string>;
    projectId?: string;
}

/**
 * Bulk get statuses by name
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getStatusesByName(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetStatusesByNameParams): Promise<Array<Types.JiraStatus>> {
    const path = '/rest/api/2/statuses/byNames';
    const queryParams = {
      name: params.name,
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.JiraStatus>>;
}

export interface SearchParams {
    projectId?: string;
    startAt?: number;
    maxResults?: number;
    searchString?: string;
    statusCategory?: string;
}

/**
 * Search statuses paginated
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function search(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchParams): Promise<Types.PageOfStatuses> {
    const path = '/rest/api/2/statuses/search';
    const queryParams = {
      projectId: params.projectId,
      startAt: params.startAt,
      maxResults: params.maxResults,
      searchString: params.searchString,
      statusCategory: params.statusCategory,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageOfStatuses>;
}

export interface GetProjectIssueTypeUsagesForStatusParams {
    path: { statusId: string; projectId: string };
    nextPageToken?: string;
    maxResults?: number;
}

/**
 * Get issue type usages by status and project
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectIssueTypeUsagesForStatus(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectIssueTypeUsagesForStatusParams): Promise<Types.StatusProjectIssueTypeUsageDTO> {
    const path = `/rest/api/2/statuses/${params.path.statusId}/project/${params.path.projectId}/issueTypeUsages`;
    const queryParams = {
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StatusProjectIssueTypeUsageDTO>;
}

export interface GetProjectUsagesForStatusParams {
    path: { statusId: string };
    nextPageToken?: string;
    maxResults?: number;
}

/**
 * Get project usages by status
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectUsagesForStatus(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectUsagesForStatusParams): Promise<Types.StatusProjectUsageDTO> {
    const path = `/rest/api/2/statuses/${params.path.statusId}/projectUsages`;
    const queryParams = {
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StatusProjectUsageDTO>;
}

export interface GetWorkflowUsagesForStatusParams {
    path: { statusId: string };
    nextPageToken?: string;
    maxResults?: number;
}

/**
 * Get workflow usages by status
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflowUsagesForStatus(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowUsagesForStatusParams): Promise<Types.StatusWorkflowUsageDTO> {
    const path = `/rest/api/2/statuses/${params.path.statusId}/workflowUsages`;
    const queryParams = {
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StatusWorkflowUsageDTO>;
}

export interface GetTaskParams {
    path: { taskId: string };
}

/**
 * Get task
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getTask(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetTaskParams): Promise<Types.TaskProgressBeanObject> {
    const path = `/rest/api/2/task/${params.path.taskId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.TaskProgressBeanObject>;
}

export interface CancelTaskParams {
    path: { taskId: string };
}

/**
 * Cancel task
 * @forge-scopes-asApp read:jira-work
 */
export async function cancelTask(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CancelTaskParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/task/${params.path.taskId}/cancel`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetUiModificationsParams {
    startAt?: number;
    maxResults?: number;
    expand?: string;
}

/**
 * Get UI modifications
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getUiModifications(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUiModificationsParams): Promise<Types.PageBeanUiModificationDetails> {
    const path = '/rest/api/2/uiModifications';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanUiModificationDetails>;
}

export interface CreateUiModificationParams {
    body?: Types.CreateUiModificationDetails;
}

/**
 * Create UI modification
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createUiModification(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateUiModificationParams): Promise<Types.UiModificationIdentifiers> {
    const path = '/rest/api/2/uiModifications';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UiModificationIdentifiers>;
}

export interface UpdateUiModificationParams {
    path: { uiModificationId: string };
    body?: Types.UpdateUiModificationDetails;
}

/**
 * Update UI modification
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateUiModification(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateUiModificationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/uiModifications/${params.path.uiModificationId}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteUiModificationParams {
    path: { uiModificationId: string };
}

/**
 * Delete UI modification
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteUiModification(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteUiModificationParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/uiModifications/${params.path.uiModificationId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetAvatarsParams {
    path: { type: string; entityId: string };
}

/**
 * Get avatars
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvatars(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAvatarsParams): Promise<Types.Avatars> {
    const path = `/rest/api/2/universal_avatar/type/${params.path.type}/owner/${params.path.entityId}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Avatars>;
}

export interface StoreAvatarParams {
    path: { type: string; entityId: string };
    x?: number;
    y?: number;
    size: number;
    body?: Record<string, unknown>;
}

/**
 * Load avatar
 * @forge-scopes-asApp read:jira-work
 */
export async function storeAvatar(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: StoreAvatarParams): Promise<Types.Avatar> {
    const path = `/rest/api/2/universal_avatar/type/${params.path.type}/owner/${params.path.entityId}`;
    const queryParams = {
      x: params.x,
      y: params.y,
      size: params.size,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Avatar>;
}

export interface DeleteAvatarParams {
    path: { type: string; owningObjectId: string; id: string };
}

/**
 * Delete avatar
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteAvatar(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteAvatarParams): Promise<void> {
    const path = `/rest/api/2/universal_avatar/type/${params.path.type}/owner/${params.path.owningObjectId}/avatar/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetAvatarImageByTypeParams {
    path: { type: string };
    size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
    format?: "png" | "svg";
}

/**
 * Get avatar image by type
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvatarImageByType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAvatarImageByTypeParams): Promise<Types.StreamingResponseBody> {
    const path = `/rest/api/2/universal_avatar/view/type/${params.path.type}`;
    const queryParams = {
      size: params.size,
      format: params.format,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StreamingResponseBody>;
}

export interface GetAvatarImageByIDParams {
    path: { type: string; id: string };
    size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
    format?: "png" | "svg";
}

/**
 * Get avatar image by ID
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvatarImageByID(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAvatarImageByIDParams): Promise<Types.StreamingResponseBody> {
    const path = `/rest/api/2/universal_avatar/view/type/${params.path.type}/avatar/${params.path.id}`;
    const queryParams = {
      size: params.size,
      format: params.format,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StreamingResponseBody>;
}

export interface GetAvatarImageByOwnerParams {
    path: { type: string; entityId: string };
    size?: "xsmall" | "small" | "medium" | "large" | "xlarge";
    format?: "png" | "svg";
}

/**
 * Get avatar image by owner
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAvatarImageByOwner(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAvatarImageByOwnerParams): Promise<Types.StreamingResponseBody> {
    const path = `/rest/api/2/universal_avatar/view/type/${params.path.type}/owner/${params.path.entityId}`;
    const queryParams = {
      size: params.size,
      format: params.format,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.StreamingResponseBody>;
}

export interface GetUserParams {
    accountId?: string;
    username?: string;
    key?: string;
    expand?: string;
}

/**
 * Get user
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserParams): Promise<Types.User> {
    const path = '/rest/api/2/user';
    const queryParams = {
      accountId: params.accountId,
      username: params.username,
      key: params.key,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.User>;
}

export interface CreateUserParams {
    body?: Types.NewUserDetails;
}

/**
 * Create user
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function createUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateUserParams): Promise<Types.User> {
    const path = '/rest/api/2/user';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.User>;
}

export interface RemoveUserParams {
    accountId: string;
    username?: string;
    key?: string;
}

/**
 * Delete user
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function removeUser(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveUserParams): Promise<void> {
    const path = '/rest/api/2/user';
    const queryParams = {
      accountId: params.accountId,
      username: params.username,
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface FindBulkAssignableUsersParams {
    query?: string;
    username?: string;
    accountId?: string;
    projectKeys: string;
    startAt?: number;
    maxResults?: number;
}

/**
 * Find users assignable to projects
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findBulkAssignableUsers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindBulkAssignableUsersParams): Promise<Array<Types.User>> {
    const path = '/rest/api/2/user/assignable/multiProjectSearch';
    const queryParams = {
      query: params.query,
      username: params.username,
      accountId: params.accountId,
      projectKeys: params.projectKeys,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.User>>;
}

export interface FindAssignableUsersParams {
    query?: string;
    sessionId?: string;
    username?: string;
    accountId?: string;
    project?: string;
    issueKey?: string;
    issueId?: string;
    startAt?: number;
    maxResults?: number;
    actionDescriptorId?: number;
    recommend?: boolean;
    accountType?: Array<string>;
    appType?: Array<string>;
}

/**
 * Find users assignable to issues
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findAssignableUsers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindAssignableUsersParams): Promise<Array<Types.User>> {
    const path = '/rest/api/2/user/assignable/search';
    const queryParams = {
      query: params.query,
      sessionId: params.sessionId,
      username: params.username,
      accountId: params.accountId,
      project: params.project,
      issueKey: params.issueKey,
      issueId: params.issueId,
      startAt: params.startAt,
      maxResults: params.maxResults,
      actionDescriptorId: params.actionDescriptorId,
      recommend: params.recommend,
      accountType: params.accountType,
      appType: params.appType,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.User>>;
}

export interface BulkGetUsersParams {
    startAt?: number;
    maxResults?: number;
    username?: Array<string>;
    key?: Array<string>;
    accountId: Array<string>;
}

/**
 * Bulk get users
 * @forge-scopes-asApp read:jira-user
 * @forge-scopes-asUser read:jira-user
 */
export async function bulkGetUsers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkGetUsersParams): Promise<Types.PageBeanUser> {
    const path = '/rest/api/2/user/bulk';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      username: params.username,
      key: params.key,
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanUser>;
}

export interface BulkGetUsersMigrationParams {
    startAt?: number;
    maxResults?: number;
    username?: Array<string>;
    key?: Array<string>;
}

/**
 * Get account IDs for users
 * @forge-scopes-asApp read:jira-user
 * @forge-scopes-asUser read:jira-user
 */
export async function bulkGetUsersMigration(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: BulkGetUsersMigrationParams): Promise<Array<Types.UserMigrationBean>> {
    const path = '/rest/api/2/user/bulk/migration';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      username: params.username,
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.UserMigrationBean>>;
}

export interface GetUserDefaultColumnsParams {
    accountId?: string;
    username?: string;
}

/**
 * Get user default columns
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserDefaultColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserDefaultColumnsParams): Promise<Array<Types.ColumnItem>> {
    const path = '/rest/api/2/user/columns';
    const queryParams = {
      accountId: params.accountId,
      username: params.username,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ColumnItem>>;
}

export interface SetUserColumnsParams {
    accountId?: string;
    body?: Record<string, unknown>;
}

/**
 * Set user default columns
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function setUserColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetUserColumnsParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/user/columns';
    const queryParams = {
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface ResetUserColumnsParams {
    accountId?: string;
    username?: string;
}

/**
 * Reset user default columns
 * @forge-scopes-asApp read:jira-user
 * @forge-scopes-asUser read:jira-user
 */
export async function resetUserColumns(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ResetUserColumnsParams): Promise<void> {
    const path = '/rest/api/2/user/columns';
    const queryParams = {
      accountId: params.accountId,
      username: params.username,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetUserEmailParams {
    accountId: string;
}

/**
 * Get user email
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserEmail(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserEmailParams): Promise<Types.UnrestrictedUserEmail> {
    const path = '/rest/api/2/user/email';
    const queryParams = {
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UnrestrictedUserEmail>;
}

export interface GetUserEmailBulkParams {
    accountId: Array<string>;
}

/**
 * Get user email bulk
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserEmailBulk(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserEmailBulkParams): Promise<Types.UnrestrictedUserEmail> {
    const path = '/rest/api/2/user/email/bulk';
    const queryParams = {
      accountId: params.accountId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.UnrestrictedUserEmail>;
}

export interface GetUserGroupsParams {
    accountId: string;
    username?: string;
    key?: string;
}

/**
 * Get user groups
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserGroups(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserGroupsParams): Promise<Array<Types.GroupName>> {
    const path = '/rest/api/2/user/groups';
    const queryParams = {
      accountId: params.accountId,
      username: params.username,
      key: params.key,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.GroupName>>;
}

export interface FindUsersWithAllPermissionsParams {
    query?: string;
    username?: string;
    accountId?: string;
    permissions: string;
    issueKey?: string;
    projectKey?: string;
    startAt?: number;
    maxResults?: number;
}

/**
 * Find users with permissions
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findUsersWithAllPermissions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindUsersWithAllPermissionsParams): Promise<Array<Types.User>> {
    const path = '/rest/api/2/user/permission/search';
    const queryParams = {
      query: params.query,
      username: params.username,
      accountId: params.accountId,
      permissions: params.permissions,
      issueKey: params.issueKey,
      projectKey: params.projectKey,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.User>>;
}

export interface FindUsersForPickerParams {
    query: string;
    maxResults?: number;
    showAvatar?: boolean;
    exclude?: Array<string>;
    excludeAccountIds?: Array<string>;
    avatarSize?: string;
    excludeConnectUsers?: boolean;
}

/**
 * Find users for picker
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findUsersForPicker(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindUsersForPickerParams): Promise<Types.FoundUsers> {
    const path = '/rest/api/2/user/picker';
    const queryParams = {
      query: params.query,
      maxResults: params.maxResults,
      showAvatar: params.showAvatar,
      exclude: params.exclude,
      excludeAccountIds: params.excludeAccountIds,
      avatarSize: params.avatarSize,
      excludeConnectUsers: params.excludeConnectUsers,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FoundUsers>;
}

export interface GetUserPropertyKeysParams {
    accountId?: string;
    userKey?: string;
    username?: string;
}

/**
 * Get user property keys
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserPropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserPropertyKeysParams): Promise<Types.PropertyKeys> {
    const path = '/rest/api/2/user/properties';
    const queryParams = {
      accountId: params.accountId,
      userKey: params.userKey,
      username: params.username,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetUserPropertyParams {
    path: { propertyKey: string };
    accountId?: string;
    userKey?: string;
    username?: string;
}

/**
 * Get user property
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getUserProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetUserPropertyParams): Promise<Types.EntityProperty> {
    const path = `/rest/api/2/user/properties/${params.path.propertyKey}`;
    const queryParams = {
      accountId: params.accountId,
      userKey: params.userKey,
      username: params.username,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface SetUserPropertyParams {
    path: { propertyKey: string };
    accountId?: string;
    userKey?: string;
    username?: string;
    body?: Record<string, unknown>;
}

/**
 * Set user property
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function setUserProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetUserPropertyParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/user/properties/${params.path.propertyKey}`;
    const queryParams = {
      accountId: params.accountId,
      userKey: params.userKey,
      username: params.username,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface DeleteUserPropertyParams {
    path: { propertyKey: string };
    accountId?: string;
    userKey?: string;
    username?: string;
}

/**
 * Delete user property
 * @forge-scopes-asApp write:jira-work, read:jira-user
 * @forge-scopes-asUser write:jira-work, read:jira-user
 */
export async function deleteUserProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteUserPropertyParams): Promise<void> {
    const path = `/rest/api/2/user/properties/${params.path.propertyKey}`;
    const queryParams = {
      accountId: params.accountId,
      userKey: params.userKey,
      username: params.username,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface FindUsersParams {
    query?: string;
    username?: string;
    accountId?: string;
    startAt?: number;
    maxResults?: number;
    property?: string;
}

/**
 * Find users
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findUsers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindUsersParams): Promise<Array<Types.User>> {
    const path = '/rest/api/2/user/search';
    const queryParams = {
      query: params.query,
      username: params.username,
      accountId: params.accountId,
      startAt: params.startAt,
      maxResults: params.maxResults,
      property: params.property,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.User>>;
}

export interface FindUsersByQueryParams {
    query: string;
    startAt?: number;
    maxResults?: number;
}

/**
 * Find users by query
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findUsersByQuery(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindUsersByQueryParams): Promise<Types.PageBeanUser> {
    const path = '/rest/api/2/user/search/query';
    const queryParams = {
      query: params.query,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanUser>;
}

export interface FindUserKeysByQueryParams {
    query: string;
    startAt?: number;
    maxResult?: number;
}

/**
 * Find user keys by query
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findUserKeysByQuery(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindUserKeysByQueryParams): Promise<Types.PageBeanUserKey> {
    const path = '/rest/api/2/user/search/query/key';
    const queryParams = {
      query: params.query,
      startAt: params.startAt,
      maxResult: params.maxResult,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanUserKey>;
}

export interface FindUsersWithBrowsePermissionParams {
    query?: string;
    username?: string;
    accountId?: string;
    issueKey?: string;
    projectKey?: string;
    startAt?: number;
    maxResults?: number;
}

/**
 * Find users with browse permission
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function findUsersWithBrowsePermission(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FindUsersWithBrowsePermissionParams): Promise<Array<Types.User>> {
    const path = '/rest/api/2/user/viewissue/search';
    const queryParams = {
      query: params.query,
      username: params.username,
      accountId: params.accountId,
      issueKey: params.issueKey,
      projectKey: params.projectKey,
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.User>>;
}

export interface GetAllUsersDefaultParams {
    startAt?: number;
    maxResults?: number;
    expand?: string;
}

/**
 * Get all users default
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getAllUsersDefault(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllUsersDefaultParams): Promise<Array<Types.User>> {
    const path = '/rest/api/2/users';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.User>>;
}

export interface GetAllUsersParams {
    startAt?: number;
    maxResults?: number;
    expand?: string;
}

/**
 * Get all users
 * @forge-scopes-asApp read:jira-work, read:jira-user
 * @forge-scopes-asUser read:jira-work, read:jira-user
 */
export async function getAllUsers(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllUsersParams): Promise<Array<Types.User>> {
    const path = '/rest/api/2/users/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.User>>;
}

export interface CreateVersionParams {
    body?: Types.Version;
}

/**
 * Create version
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateVersionParams): Promise<Types.Version> {
    const path = '/rest/api/2/version';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Version>;
}

export interface GetVersionParams {
    path: { id: string };
    expand?: string;
}

/**
 * Get version
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetVersionParams): Promise<Types.Version> {
    const path = `/rest/api/2/version/${params.path.id}`;
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Version>;
}

export interface UpdateVersionParams {
    path: { id: string };
    body?: Types.Version;
}

/**
 * Update version
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateVersionParams): Promise<Types.Version> {
    const path = `/rest/api/2/version/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Version>;
}

export interface DeleteVersionParams {
    path: { id: string };
    moveFixIssuesTo?: string;
    moveAffectedIssuesTo?: string;
}

/**
 * Delete version
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function deleteVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteVersionParams): Promise<void> {
    const path = `/rest/api/2/version/${params.path.id}`;
    const queryParams = {
      moveFixIssuesTo: params.moveFixIssuesTo,
      moveAffectedIssuesTo: params.moveAffectedIssuesTo,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface MergeVersionsParams {
    path: { id: string; moveIssuesTo: string };
}

/**
 * Merge versions
 * @forge-scopes-asApp read:jira-work
 */
export async function mergeVersions(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MergeVersionsParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/version/${params.path.id}/mergeto/${params.path.moveIssuesTo}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface MoveVersionParams {
    path: { id: string };
    body?: Types.VersionMoveBean;
}

/**
 * Move version
 * @forge-scopes-asApp read:jira-work
 */
export async function moveVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: MoveVersionParams): Promise<Types.Version> {
    const path = `/rest/api/2/version/${params.path.id}/move`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.Version>;
}

export interface GetVersionRelatedIssuesParams {
    path: { id: string };
}

/**
 * Get version's related issues count
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getVersionRelatedIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetVersionRelatedIssuesParams): Promise<Types.VersionIssueCounts> {
    const path = `/rest/api/2/version/${params.path.id}/relatedIssueCounts`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.VersionIssueCounts>;
}

export interface GetRelatedWorkParams {
    path: { id: string };
}

/**
 * Get related work
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRelatedWork(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRelatedWorkParams): Promise<Array<Types.VersionRelatedWork>> {
    const path = `/rest/api/2/version/${params.path.id}/relatedwork`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.VersionRelatedWork>>;
}

export interface CreateRelatedWorkParams {
    path: { id: string };
    body?: Types.VersionRelatedWork;
}

/**
 * Create related work
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createRelatedWork(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateRelatedWorkParams): Promise<Types.VersionRelatedWork> {
    const path = `/rest/api/2/version/${params.path.id}/relatedwork`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.VersionRelatedWork>;
}

export interface UpdateRelatedWorkParams {
    path: { id: string };
    body?: Types.VersionRelatedWork;
}

/**
 * Update related work
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateRelatedWork(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateRelatedWorkParams): Promise<Types.VersionRelatedWork> {
    const path = `/rest/api/2/version/${params.path.id}/relatedwork`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.VersionRelatedWork>;
}

export interface DeleteAndReplaceVersionParams {
    path: { id: string };
    body?: Types.DeleteAndReplaceVersionBean;
}

/**
 * Delete and replace version
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteAndReplaceVersion(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteAndReplaceVersionParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/version/${params.path.id}/removeAndSwap`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetVersionUnresolvedIssuesParams {
    path: { id: string };
}

/**
 * Get version's unresolved issues count
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getVersionUnresolvedIssues(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetVersionUnresolvedIssuesParams): Promise<Types.VersionUnresolvedIssuesCount> {
    const path = `/rest/api/2/version/${params.path.id}/unresolvedIssueCount`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.VersionUnresolvedIssuesCount>;
}

export interface DeleteRelatedWorkParams {
    path: { versionId: string; relatedWorkId: string };
}

/**
 * Delete related work
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteRelatedWork(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteRelatedWorkParams): Promise<void> {
    const path = `/rest/api/2/version/${params.path.versionId}/relatedwork/${params.path.relatedWorkId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDynamicWebhooksForAppParams {
    startAt?: number;
    maxResults?: number;
}

/**
 * Get dynamic webhooks for app
 * @forge-scopes-asApp read:jira-work, manage:jira-webhook
 * @forge-scopes-asUser read:jira-work
 */
export async function getDynamicWebhooksForApp(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDynamicWebhooksForAppParams): Promise<Types.PageBeanWebhook> {
    const path = '/rest/api/2/webhook';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanWebhook>;
}

export interface RegisterDynamicWebhooksParams {
    body?: Types.WebhookRegistrationDetails;
}

/**
 * Register dynamic webhooks
 * @forge-scopes-asApp manage:jira-webhook
 */
export async function registerDynamicWebhooks(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RegisterDynamicWebhooksParams): Promise<Types.ContainerForRegisteredWebhooks> {
    const path = '/rest/api/2/webhook';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContainerForRegisteredWebhooks>;
}

export interface DeleteWebhookByIdParams {
    body?: Types.ContainerForWebhookIDs;
}

/**
 * Delete webhooks by ID
 * @forge-scopes-asApp write:jira-work, manage:jira-webhook
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWebhookById(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWebhookByIdParams): Promise<void> {
    const path = '/rest/api/2/webhook';
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetFailedWebhooksParams {
    maxResults?: number;
    after?: number;
}

/**
 * Get failed webhooks
 * @forge-scopes-asApp read:jira-work, manage:jira-webhook
 * @forge-scopes-asUser read:jira-work
 */
export async function getFailedWebhooks(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetFailedWebhooksParams): Promise<Types.FailedWebhooks> {
    const path = '/rest/api/2/webhook/failed';
    const queryParams = {
      maxResults: params.maxResults,
      after: params.after,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.FailedWebhooks>;
}

export interface RefreshWebhooksParams {
    body?: Types.ContainerForWebhookIDs;
}

/**
 * Extend webhook life
 * @forge-scopes-asApp manage:jira-webhook
 */
export async function refreshWebhooks(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RefreshWebhooksParams): Promise<Types.WebhooksExpirationDate> {
    const path = '/rest/api/2/webhook/refresh';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WebhooksExpirationDate>;
}

export interface GetAllWorkflowsParams {
    workflowName?: string;
}

/**
 * Get all workflows
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getAllWorkflows(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllWorkflowsParams): Promise<Array<Types.DeprecatedWorkflow>> {
    const path = '/rest/api/2/workflow';
    const queryParams = {
      workflowName: params.workflowName,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.DeprecatedWorkflow>>;
}

export interface CreateWorkflowParams {
    body?: Types.CreateWorkflowDetails;
}

/**
 * Create workflow
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function createWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateWorkflowParams): Promise<Types.WorkflowIDs> {
    const path = '/rest/api/2/workflow';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowIDs>;
}

export interface ReadWorkflowFromHistoryParams {
    body?: Types.WorkflowHistoryReadRequest;
}

/**
 * Read workflow version from history
 * @forge-scopes-asApp read:jira-work
 */
export async function readWorkflowFromHistory(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReadWorkflowFromHistoryParams): Promise<Types.WorkflowHistoryReadResponseDTO> {
    const path = '/rest/api/2/workflow/history';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowHistoryReadResponseDTO>;
}

export interface ListWorkflowHistoryParams {
    expand?: string;
    body?: Types.WorkflowHistoryListRequest;
}

/**
 * List workflow history entries
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function listWorkflowHistory(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ListWorkflowHistoryParams): Promise<Types.WorkflowHistoryListResponseDTO> {
    const path = '/rest/api/2/workflow/history/list';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowHistoryListResponseDTO>;
}

export interface GetWorkflowTransitionRuleConfigurationsParams {
    startAt?: number;
    maxResults?: number;
    types: Array<"postfunction" | "condition" | "validator">;
    keys?: Array<string>;
    workflowNames?: Array<string>;
    withTags?: Array<string>;
    draft?: boolean;
    expand?: string;
}

/**
 * Get workflow transition rule configurations
 * @forge-scopes-asApp read:jira-work, manage:jira-configuration
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflowTransitionRuleConfigurations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowTransitionRuleConfigurationsParams): Promise<Types.PageBeanWorkflowTransitionRules> {
    const path = '/rest/api/2/workflow/rule/config';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      types: params.types,
      keys: params.keys,
      workflowNames: params.workflowNames,
      withTags: params.withTags,
      draft: params.draft,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanWorkflowTransitionRules>;
}

export interface UpdateWorkflowTransitionRuleConfigurationsParams {
    body?: Types.WorkflowTransitionRulesUpdate;
}

/**
 * Update workflow transition rule configurations
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 */
export async function updateWorkflowTransitionRuleConfigurations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateWorkflowTransitionRuleConfigurationsParams): Promise<Types.WorkflowTransitionRulesUpdateErrors> {
    const path = '/rest/api/2/workflow/rule/config';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowTransitionRulesUpdateErrors>;
}

export interface DeleteWorkflowTransitionRuleConfigurationsParams {
    body?: Types.WorkflowsWithTransitionRulesDetails;
}

/**
 * Delete workflow transition rule configurations
 * @forge-scopes-asApp write:jira-work, manage:jira-configuration
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorkflowTransitionRuleConfigurations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorkflowTransitionRuleConfigurationsParams): Promise<Types.WorkflowTransitionRulesUpdateErrors> {
    const path = '/rest/api/2/workflow/rule/config/delete';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowTransitionRulesUpdateErrors>;
}

export interface GetWorkflowsPaginatedParams {
    startAt?: number;
    maxResults?: number;
    workflowName?: Array<string>;
    expand?: string;
    queryString?: string;
    orderBy?: "name" | "-name" | "+name" | "created" | "-created" | "+created" | "updated" | "+updated" | "-updated";
    isActive?: boolean;
}

/**
 * Get workflows paginated
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getWorkflowsPaginated(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowsPaginatedParams): Promise<Types.PageBeanWorkflow> {
    const path = '/rest/api/2/workflow/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      workflowName: params.workflowName,
      expand: params.expand,
      queryString: params.queryString,
      orderBy: params.orderBy,
      isActive: params.isActive,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanWorkflow>;
}

export interface GetWorkflowTransitionPropertiesParams {
    path: { transitionId: string };
    includeReservedKeys?: boolean;
    key?: string;
    workflowName: string;
    workflowMode?: "live" | "draft";
}

/**
 * Get workflow transition properties
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 * @deprecated
 */
export async function getWorkflowTransitionProperties(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowTransitionPropertiesParams): Promise<Types.WorkflowTransitionProperty> {
    const path = `/rest/api/2/workflow/transitions/${params.path.transitionId}/properties`;
    const queryParams = {
      includeReservedKeys: params.includeReservedKeys,
      key: params.key,
      workflowName: params.workflowName,
      workflowMode: params.workflowMode,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowTransitionProperty>;
}

export interface CreateWorkflowTransitionPropertyParams {
    path: { transitionId: string };
    key: string;
    workflowName: string;
    workflowMode?: "live" | "draft";
    body?: Types.WorkflowTransitionProperty;
}

/**
 * Create workflow transition property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function createWorkflowTransitionProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateWorkflowTransitionPropertyParams): Promise<Types.WorkflowTransitionProperty> {
    const path = `/rest/api/2/workflow/transitions/${params.path.transitionId}/properties`;
    const queryParams = {
      key: params.key,
      workflowName: params.workflowName,
      workflowMode: params.workflowMode,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowTransitionProperty>;
}

export interface UpdateWorkflowTransitionPropertyParams {
    path: { transitionId: string };
    key: string;
    workflowName: string;
    workflowMode?: "live" | "draft";
    body?: Types.WorkflowTransitionProperty;
}

/**
 * Update workflow transition property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function updateWorkflowTransitionProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateWorkflowTransitionPropertyParams): Promise<Types.WorkflowTransitionProperty> {
    const path = `/rest/api/2/workflow/transitions/${params.path.transitionId}/properties`;
    const queryParams = {
      key: params.key,
      workflowName: params.workflowName,
      workflowMode: params.workflowMode,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowTransitionProperty>;
}

export interface DeleteWorkflowTransitionPropertyParams {
    path: { transitionId: string };
    key: string;
    workflowName: string;
    workflowMode?: "live" | "draft";
}

/**
 * Delete workflow transition property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 * @deprecated
 */
export async function deleteWorkflowTransitionProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorkflowTransitionPropertyParams): Promise<void> {
    const path = `/rest/api/2/workflow/transitions/${params.path.transitionId}/properties`;
    const queryParams = {
      key: params.key,
      workflowName: params.workflowName,
      workflowMode: params.workflowMode,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface DeleteInactiveWorkflowParams {
    path: { entityId: string };
}

/**
 * Delete inactive workflow
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteInactiveWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteInactiveWorkflowParams): Promise<void> {
    const path = `/rest/api/2/workflow/${params.path.entityId}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWorkflowProjectIssueTypeUsagesParams {
    path: { workflowId: string; projectId: string };
    nextPageToken?: string;
    maxResults?: number;
}

/**
 * Get issue types in a project that are using a given workflow
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getWorkflowProjectIssueTypeUsages(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowProjectIssueTypeUsagesParams): Promise<Types.WorkflowProjectIssueTypeUsageDTO> {
    const path = `/rest/api/2/workflow/${params.path.workflowId}/project/${params.path.projectId}/issueTypeUsages`;
    const queryParams = {
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowProjectIssueTypeUsageDTO>;
}

export interface GetProjectUsagesForWorkflowParams {
    path: { workflowId: string };
    nextPageToken?: string;
    maxResults?: number;
}

/**
 * Get projects using a given workflow
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectUsagesForWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectUsagesForWorkflowParams): Promise<Types.WorkflowProjectUsageDTO> {
    const path = `/rest/api/2/workflow/${params.path.workflowId}/projectUsages`;
    const queryParams = {
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowProjectUsageDTO>;
}

export interface GetWorkflowSchemeUsagesForWorkflowParams {
    path: { workflowId: string };
    nextPageToken?: string;
    maxResults?: number;
}

/**
 * Get workflow schemes which are using a given workflow
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflowSchemeUsagesForWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowSchemeUsagesForWorkflowParams): Promise<Types.WorkflowSchemeUsageDTO> {
    const path = `/rest/api/2/workflow/${params.path.workflowId}/workflowSchemes`;
    const queryParams = {
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowSchemeUsageDTO>;
}

export interface ReadWorkflowsParams {
    body?: Types.WorkflowReadRequest;
}

/**
 * Bulk get workflows
 * @forge-scopes-asApp read:jira-work
 */
export async function readWorkflows(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReadWorkflowsParams): Promise<Types.WorkflowReadResponse> {
    const path = '/rest/api/2/workflows';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowReadResponse>;
}

export interface WorkflowCapabilitiesParams {
    workflowId?: string;
    projectId?: string;
    issueTypeId?: string;
}

/**
 * Get available workflow capabilities
 * @forge-scopes-asApp read:jira-work
 */
export async function workflowCapabilities(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: WorkflowCapabilitiesParams): Promise<Types.WorkflowCapabilities> {
    const path = '/rest/api/2/workflows/capabilities';
    const queryParams = {
      workflowId: params.workflowId,
      projectId: params.projectId,
      issueTypeId: params.issueTypeId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowCapabilities>;
}

export interface CreateWorkflowsParams {
    body?: Types.WorkflowCreateRequest;
}

/**
 * Bulk create workflows
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createWorkflows(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateWorkflowsParams): Promise<Types.WorkflowCreateResponse> {
    const path = '/rest/api/2/workflows/create';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowCreateResponse>;
}

export interface ValidateCreateWorkflowsParams {
    body?: Types.WorkflowCreateValidateRequest;
}

/**
 * Validate create workflows
 * @forge-scopes-asApp read:jira-work
 */
export async function validateCreateWorkflows(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ValidateCreateWorkflowsParams): Promise<Types.WorkflowValidationErrorList> {
    const path = '/rest/api/2/workflows/create/validation';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowValidationErrorList>;
}

/**
 * Get the user's default workflow editor
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDefaultEditor(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.DefaultWorkflowEditorResponse> {
    const path = '/rest/api/2/workflows/defaultEditor';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DefaultWorkflowEditorResponse>;
}

export interface ReadWorkflowPreviewsParams {
    body?: Types.WorkflowPreviewRequest;
}

/**
 * Preview workflow
 * @forge-scopes-asApp read:jira-work
 */
export async function readWorkflowPreviews(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReadWorkflowPreviewsParams): Promise<Types.WorkflowPreviewResponse> {
    const path = '/rest/api/2/workflows/preview';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowPreviewResponse>;
}

export interface SearchWorkflowsParams {
    startAt?: number;
    maxResults?: number;
    expand?: string;
    queryString?: string;
    orderBy?: string;
    scope?: string;
    isActive?: boolean;
}

/**
 * Search workflows
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function searchWorkflows(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SearchWorkflowsParams): Promise<Types.WorkflowSearchResponse> {
    const path = '/rest/api/2/workflows/search';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
      expand: params.expand,
      queryString: params.queryString,
      orderBy: params.orderBy,
      scope: params.scope,
      isActive: params.isActive,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowSearchResponse>;
}

export interface UpdateWorkflowsParams {
    body?: Types.WorkflowUpdateRequest;
}

/**
 * Bulk update workflows
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateWorkflows(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateWorkflowsParams): Promise<Types.WorkflowUpdateResponse> {
    const path = '/rest/api/2/workflows/update';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowUpdateResponse>;
}

export interface ValidateUpdateWorkflowsParams {
    body?: Types.WorkflowUpdateValidateRequestBean;
}

/**
 * Validate update workflows
 * @forge-scopes-asApp read:jira-work
 */
export async function validateUpdateWorkflows(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ValidateUpdateWorkflowsParams): Promise<Types.WorkflowValidationErrorList> {
    const path = '/rest/api/2/workflows/update/validation';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowValidationErrorList>;
}

export interface GetAllWorkflowSchemesParams {
    startAt?: number;
    maxResults?: number;
}

/**
 * Get all workflow schemes
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getAllWorkflowSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAllWorkflowSchemesParams): Promise<Types.PageBeanWorkflowScheme> {
    const path = '/rest/api/2/workflowscheme';
    const queryParams = {
      startAt: params.startAt,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PageBeanWorkflowScheme>;
}

export interface CreateWorkflowSchemeParams {
    body?: Types.WorkflowScheme;
}

/**
 * Create workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createWorkflowScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateWorkflowSchemeParams): Promise<Types.WorkflowScheme> {
    const path = '/rest/api/2/workflowscheme';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface GetWorkflowSchemeProjectAssociationsParams {
    projectId: Array<number>;
}

/**
 * Get workflow scheme project associations
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getWorkflowSchemeProjectAssociations(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowSchemeProjectAssociationsParams): Promise<Types.ContainerOfWorkflowSchemeAssociations> {
    const path = '/rest/api/2/workflowscheme/project';
    const queryParams = {
      projectId: params.projectId,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ContainerOfWorkflowSchemeAssociations>;
}

export interface AssignSchemeToProjectParams {
    body?: Types.WorkflowSchemeProjectAssociation;
}

/**
 * Assign workflow scheme to project
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function assignSchemeToProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: AssignSchemeToProjectParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/workflowscheme/project';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface SwitchWorkflowSchemeForProjectParams {
    body?: Types.WorkflowSchemeProjectSwitchBean;
}

/**
 * Switch workflow scheme for project
 * @forge-scopes-asApp manage:jira-project
 * @forge-scopes-asUser manage:jira-project
 */
export async function switchWorkflowSchemeForProject(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SwitchWorkflowSchemeForProjectParams): Promise<void> {
    const path = '/rest/api/2/workflowscheme/project/switch';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface ReadWorkflowSchemesParams {
    body?: Types.WorkflowSchemeReadRequest;
}

/**
 * Bulk get workflow schemes
 * @forge-scopes-asApp read:jira-work
 */
export async function readWorkflowSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ReadWorkflowSchemesParams): Promise<Array<Types.WorkflowSchemeReadResponse>> {
    const path = '/rest/api/2/workflowscheme/read';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.WorkflowSchemeReadResponse>>;
}

export interface UpdateSchemesParams {
    body?: Types.WorkflowSchemeUpdateRequest;
}

/**
 * Update workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateSchemes(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateSchemesParams): Promise<Record<string, unknown>> {
    const path = '/rest/api/2/workflowscheme/update';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface GetRequiredWorkflowSchemeMappingsParams {
    body?: Types.WorkflowSchemeUpdateRequiredMappingsRequest;
}

/**
 * Get required status mappings for workflow scheme update
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getRequiredWorkflowSchemeMappings(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetRequiredWorkflowSchemeMappingsParams): Promise<Types.WorkflowSchemeUpdateRequiredMappingsResponse> {
    const path = '/rest/api/2/workflowscheme/update/mappings';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowSchemeUpdateRequiredMappingsResponse>;
}

export interface GetWorkflowSchemeParams {
    path: { id: string };
    returnDraftIfExists?: boolean;
}

/**
 * Get workflow scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflowScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowSchemeParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}`;
    const queryParams = {
      returnDraftIfExists: params.returnDraftIfExists,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface UpdateWorkflowSchemeParams {
    path: { id: string };
    body?: Types.WorkflowScheme;
}

/**
 * Classic update workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateWorkflowScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateWorkflowSchemeParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteWorkflowSchemeParams {
    path: { id: string };
}

/**
 * Delete workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorkflowScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorkflowSchemeParams): Promise<Record<string, unknown>> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface CreateWorkflowSchemeDraftFromParentParams {
    path: { id: string };
}

/**
 * Create draft workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function createWorkflowSchemeDraftFromParent(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: CreateWorkflowSchemeDraftFromParentParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/createdraft`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface GetDefaultWorkflowParams {
    path: { id: string };
    returnDraftIfExists?: boolean;
}

/**
 * Get default workflow
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDefaultWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDefaultWorkflowParams): Promise<Types.DefaultWorkflow> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/default`;
    const queryParams = {
      returnDraftIfExists: params.returnDraftIfExists,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DefaultWorkflow>;
}

export interface UpdateDefaultWorkflowParams {
    path: { id: string };
    body?: Types.DefaultWorkflow;
}

/**
 * Update default workflow
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateDefaultWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateDefaultWorkflowParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/default`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteDefaultWorkflowParams {
    path: { id: string };
    updateDraftIfNeeded?: boolean;
}

/**
 * Delete default workflow
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDefaultWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteDefaultWorkflowParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/default`;
    const queryParams = {
      updateDraftIfNeeded: params.updateDraftIfNeeded,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface GetWorkflowSchemeDraftParams {
    path: { id: string };
}

/**
 * Get draft workflow scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflowSchemeDraft(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowSchemeDraftParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface UpdateWorkflowSchemeDraftParams {
    path: { id: string };
    body?: Types.WorkflowScheme;
}

/**
 * Update draft workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateWorkflowSchemeDraft(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateWorkflowSchemeDraftParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteWorkflowSchemeDraftParams {
    path: { id: string };
}

/**
 * Delete draft workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorkflowSchemeDraft(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorkflowSchemeDraftParams): Promise<void> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDraftDefaultWorkflowParams {
    path: { id: string };
}

/**
 * Get draft default workflow
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDraftDefaultWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDraftDefaultWorkflowParams): Promise<Types.DefaultWorkflow> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/default`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.DefaultWorkflow>;
}

export interface UpdateDraftDefaultWorkflowParams {
    path: { id: string };
    body?: Types.DefaultWorkflow;
}

/**
 * Update draft default workflow
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateDraftDefaultWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateDraftDefaultWorkflowParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/default`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteDraftDefaultWorkflowParams {
    path: { id: string };
}

/**
 * Delete draft default workflow
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDraftDefaultWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteDraftDefaultWorkflowParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/default`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface GetWorkflowSchemeDraftIssueTypeParams {
    path: { id: string; issueType: string };
}

/**
 * Get workflow for issue type in draft workflow scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflowSchemeDraftIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowSchemeDraftIssueTypeParams): Promise<Types.IssueTypeWorkflowMapping> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/issuetype/${params.path.issueType}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypeWorkflowMapping>;
}

export interface SetWorkflowSchemeDraftIssueTypeParams {
    path: { id: string; issueType: string };
    body?: Types.IssueTypeWorkflowMapping;
}

/**
 * Set workflow for issue type in draft workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setWorkflowSchemeDraftIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetWorkflowSchemeDraftIssueTypeParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/issuetype/${params.path.issueType}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteWorkflowSchemeDraftIssueTypeParams {
    path: { id: string; issueType: string };
}

/**
 * Delete workflow for issue type in draft workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorkflowSchemeDraftIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorkflowSchemeDraftIssueTypeParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/issuetype/${params.path.issueType}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface PublishDraftWorkflowSchemeParams {
    path: { id: string };
    validateOnly?: boolean;
    body?: Types.PublishDraftWorkflowScheme;
}

/**
 * Publish draft workflow scheme
 * @forge-scopes-asApp read:jira-work
 */
export async function publishDraftWorkflowScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PublishDraftWorkflowSchemeParams): Promise<void> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/publish`;
    const queryParams = {
      validateOnly: params.validateOnly,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetDraftWorkflowParams {
    path: { id: string };
    workflowName?: string;
}

/**
 * Get issue types for workflows in draft workflow scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getDraftWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetDraftWorkflowParams): Promise<Types.IssueTypesWorkflowMapping> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/workflow`;
    const queryParams = {
      workflowName: params.workflowName,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypesWorkflowMapping>;
}

export interface UpdateDraftWorkflowMappingParams {
    path: { id: string };
    workflowName: string;
    body?: Types.IssueTypesWorkflowMapping;
}

/**
 * Set issue types for workflow in workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateDraftWorkflowMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateDraftWorkflowMappingParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/workflow`;
    const queryParams = {
      workflowName: params.workflowName,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteDraftWorkflowMappingParams {
    path: { id: string };
    workflowName: string;
}

/**
 * Delete issue types for workflow in draft workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteDraftWorkflowMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteDraftWorkflowMappingParams): Promise<void> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/draft/workflow`;
    const queryParams = {
      workflowName: params.workflowName,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWorkflowSchemeIssueTypeParams {
    path: { id: string; issueType: string };
    returnDraftIfExists?: boolean;
}

/**
 * Get workflow for issue type in workflow scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflowSchemeIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowSchemeIssueTypeParams): Promise<Types.IssueTypeWorkflowMapping> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/issuetype/${params.path.issueType}`;
    const queryParams = {
      returnDraftIfExists: params.returnDraftIfExists,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypeWorkflowMapping>;
}

export interface SetWorkflowSchemeIssueTypeParams {
    path: { id: string; issueType: string };
    body?: Types.IssueTypeWorkflowMapping;
}

/**
 * Set workflow for issue type in workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function setWorkflowSchemeIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SetWorkflowSchemeIssueTypeParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/issuetype/${params.path.issueType}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteWorkflowSchemeIssueTypeParams {
    path: { id: string; issueType: string };
    updateDraftIfNeeded?: boolean;
}

/**
 * Delete workflow for issue type in workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorkflowSchemeIssueType(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorkflowSchemeIssueTypeParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/issuetype/${params.path.issueType}`;
    const queryParams = {
      updateDraftIfNeeded: params.updateDraftIfNeeded,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface GetWorkflowParams {
    path: { id: string };
    workflowName?: string;
    returnDraftIfExists?: boolean;
}

/**
 * Get issue types for workflows in workflow scheme
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorkflow(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorkflowParams): Promise<Types.IssueTypesWorkflowMapping> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/workflow`;
    const queryParams = {
      workflowName: params.workflowName,
      returnDraftIfExists: params.returnDraftIfExists,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.IssueTypesWorkflowMapping>;
}

export interface UpdateWorkflowMappingParams {
    path: { id: string };
    workflowName: string;
    body?: Types.IssueTypesWorkflowMapping;
}

/**
 * Set issue types for workflow in workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function updateWorkflowMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateWorkflowMappingParams): Promise<Types.WorkflowScheme> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/workflow`;
    const queryParams = {
      workflowName: params.workflowName,
    };
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowScheme>;
}

export interface DeleteWorkflowMappingParams {
    path: { id: string };
    workflowName: string;
    updateDraftIfNeeded?: boolean;
}

/**
 * Delete issue types for workflow in workflow scheme
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteWorkflowMapping(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteWorkflowMappingParams): Promise<void> {
    const path = `/rest/api/2/workflowscheme/${params.path.id}/workflow`;
    const queryParams = {
      workflowName: params.workflowName,
      updateDraftIfNeeded: params.updateDraftIfNeeded,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetProjectUsagesForWorkflowSchemeParams {
    path: { workflowSchemeId: string };
    nextPageToken?: string;
    maxResults?: number;
}

/**
 * Get projects which are using a given workflow scheme
 * @forge-scopes-asApp read:jira-work, manage:jira-project
 * @forge-scopes-asUser read:jira-work, manage:jira-project
 */
export async function getProjectUsagesForWorkflowScheme(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetProjectUsagesForWorkflowSchemeParams): Promise<Types.WorkflowSchemeProjectUsageDTO> {
    const path = `/rest/api/2/workflowscheme/${params.path.workflowSchemeId}/projectUsages`;
    const queryParams = {
      nextPageToken: params.nextPageToken,
      maxResults: params.maxResults,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowSchemeProjectUsageDTO>;
}

export interface GetIdsOfWorklogsDeletedSinceParams {
    since?: number;
}

/**
 * Get IDs of deleted worklogs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIdsOfWorklogsDeletedSince(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIdsOfWorklogsDeletedSinceParams): Promise<Types.ChangedWorklogs> {
    const path = '/rest/api/2/worklog/deleted';
    const queryParams = {
      since: params.since,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ChangedWorklogs>;
}

export interface GetWorklogsForIdsParams {
    expand?: string;
    body?: Types.WorklogIdsRequestBean;
}

/**
 * Get worklogs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorklogsForIds(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorklogsForIdsParams): Promise<Array<Types.Worklog>> {
    const path = '/rest/api/2/worklog/list';
    const queryParams = {
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'POST',
      path,
      queryParams,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.Worklog>>;
}

export interface GetIdsOfWorklogsModifiedSinceParams {
    since?: number;
    expand?: string;
}

/**
 * Get IDs of updated worklogs
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getIdsOfWorklogsModifiedSince(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetIdsOfWorklogsModifiedSinceParams): Promise<Types.ChangedWorklogs> {
    const path = '/rest/api/2/worklog/updated';
    const queryParams = {
      since: params.since,
      expand: params.expand,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ChangedWorklogs>;
}

export interface GetAddonPropertiesGetParams {
    path: { addonKey: string };
}

/**
 * Get app properties
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function getAddonPropertiesGet(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAddonPropertiesGetParams): Promise<Types.PropertyKeys> {
    const path = `/rest/atlassian-connect/1/addons/${params.path.addonKey}/properties`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.PropertyKeys>;
}

export interface GetAddonPropertyGetParams {
    path: { addonKey: string; propertyKey: string };
}

/**
 * Get app property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function getAddonPropertyGet(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetAddonPropertyGetParams): Promise<Types.EntityProperty> {
    const path = `/rest/atlassian-connect/1/addons/${params.path.addonKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.EntityProperty>;
}

export interface PutAddonPropertyPutParams {
    path: { addonKey: string; propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set app property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function putAddonPropertyPut(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PutAddonPropertyPutParams): Promise<Types.OperationMessage> {
    const path = `/rest/atlassian-connect/1/addons/${params.path.addonKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.OperationMessage>;
}

export interface DeleteAddonPropertyDeleteParams {
    path: { addonKey: string; propertyKey: string };
}

/**
 * Delete app property
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteAddonPropertyDelete(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteAddonPropertyDeleteParams): Promise<void> {
    const path = `/rest/atlassian-connect/1/addons/${params.path.addonKey}/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

/**
 * Get modules
 * @forge-scopes-asApp read:jira-work
 */
export async function getModulesGet(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<Types.ConnectModules> {
    const path = '/rest/atlassian-connect/1/app/module/dynamic';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.ConnectModules>;
}

export interface RegisterModulesPostParams {
    body?: Types.ConnectModules;
}

/**
 * Register modules
 * @forge-scopes-asApp read:jira-work
 */
export async function registerModulesPost(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RegisterModulesPostParams): Promise<void> {
    const path = '/rest/atlassian-connect/1/app/module/dynamic';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface RemoveModulesDeleteParams {
    moduleKey?: Array<string>;
}

/**
 * Remove modules
 * @forge-scopes-asApp read:jira-work
 */
export async function removeModulesDelete(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: RemoveModulesDeleteParams): Promise<void> {
    const path = '/rest/atlassian-connect/1/app/module/dynamic';
    const queryParams = {
      moduleKey: params.moduleKey,
    };
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface UpdateIssueFieldsPutParams {
    body?: Types.ConnectCustomFieldValues;
}

/**
 * Bulk update custom field value
 * @forge-scopes-asApp read:jira-work
 */
export async function updateIssueFieldsPut(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateIssueFieldsPutParams): Promise<Record<string, unknown>> {
    const path = '/rest/atlassian-connect/1/migration/field';
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Record<string, unknown>>;
}

export interface UpdateEntityPropertiesValuePutParams {
    path: { entityType: string };
    body?: Array<Types.EntityPropertyDetails>;
}

/**
 * Bulk update entity properties
 * @forge-scopes-asApp read:jira-work
 */
export async function updateEntityPropertiesValuePut(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: UpdateEntityPropertiesValuePutParams): Promise<void> {
    const path = `/rest/atlassian-connect/1/migration/properties/${params.path.entityType}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface WorkflowRuleSearchPostParams {
    body?: Types.WorkflowRulesSearch;
}

/**
 * Get workflow transition rule configurations
 * @forge-scopes-asApp read:jira-work
 */
export async function workflowRuleSearchPost(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: WorkflowRuleSearchPostParams): Promise<Types.WorkflowRulesSearchDetails> {
    const path = '/rest/atlassian-connect/1/migration/workflow/rule/search';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.WorkflowRulesSearchDetails>;
}

export interface FetchMigrationTaskGetParams {
    path: { connectKey: string; jiraIssueFieldsKey: string };
}

/**
 * Get Connect issue field migration task
 * @forge-scopes-asApp read:jira-work
 */
export async function fetchMigrationTaskGet(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: FetchMigrationTaskGetParams): Promise<Types.TaskProgress> {
    const path = `/rest/atlassian-connect/1/migration/${params.path.connectKey}/${params.path.jiraIssueFieldsKey}/task`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.TaskProgress>;
}

export interface SubmitTaskPostParams {
    path: { connectKey: string; jiraIssueFieldsKey: string };
}

/**
 * Submit Connect issue field migration task
 * @forge-scopes-asApp read:jira-work
 */
export async function submitTaskPost(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: SubmitTaskPostParams): Promise<void> {
    const path = `/rest/atlassian-connect/1/migration/${params.path.connectKey}/${params.path.jiraIssueFieldsKey}/task`;
    const response = await adapter.fetch({
      method: 'POST',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface ServicesGetParams {
    serviceIds: Array<string>;
}

/**
 * Retrieve the attributes of service registries
 * @forge-scopes-asApp read:jira-work
 */
export async function servicesGet(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: ServicesGetParams): Promise<Array<Types.ServiceRegistry>> {
    const path = '/rest/atlassian-connect/1/service-registry';
    const queryParams = {
      serviceIds: params.serviceIds,
    };
    const response = await adapter.fetch({
      method: 'GET',
      path,
      queryParams,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Array<Types.ServiceRegistry>>;
}

/**
 * Get app property keys (Forge)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getForgeAppPropertyKeys(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }): Promise<{ keys?: Array<{ key?: string; self?: string }> }> {
    const path = '/rest/forge/1/app/properties';
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ keys?: Array<{ key?: string; self?: string }> }>;
}

export interface GetForgeAppPropertyParams {
    path: { propertyKey: string };
}

/**
 * Get app property (Forge)
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getForgeAppProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetForgeAppPropertyParams): Promise<{ key?: string; value?: Record<string, unknown> }> {
    const path = `/rest/forge/1/app/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'GET',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<{ key?: string; value?: Record<string, unknown> }>;
}

export interface PutForgeAppPropertyParams {
    path: { propertyKey: string };
    body?: Record<string, unknown>;
}

/**
 * Set app property (Forge)
 * @forge-scopes-asApp read:jira-work
 */
export async function putForgeAppProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: PutForgeAppPropertyParams): Promise<Types.OperationMessage> {
    const path = `/rest/forge/1/app/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'PUT',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.OperationMessage>;
}

export interface DeleteForgeAppPropertyParams {
    path: { propertyKey: string };
}

/**
 * Delete app property (Forge)
 * @forge-scopes-asApp write:jira-work
 * @forge-scopes-asUser write:jira-work
 */
export async function deleteForgeAppProperty(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: DeleteForgeAppPropertyParams): Promise<void> {
    const path = `/rest/forge/1/app/properties/${params.path.propertyKey}`;
    const response = await adapter.fetch({
      method: 'DELETE',
      path,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
}

export interface GetWorklogsByIssueIdAndWorklogIdParams {
    body?: Types.BulkWorklogKeyRequestBean;
}

/**
 * Get worklogs by issue id and worklog id
 * @forge-scopes-asApp read:jira-work
 * @forge-scopes-asUser read:jira-work
 */
export async function getWorklogsByIssueIdAndWorklogId(adapter: ForgeAdapter, authContext: AuthContext = { type: 'asApp' }, params: GetWorklogsByIssueIdAndWorklogIdParams): Promise<Types.BulkWorklogKeyResponseBean> {
    const path = '/rest/internal/api/latest/worklog/bulk';
    const response = await adapter.fetch({
      method: 'POST',
      path,
      body: params.body,
      authContext,
    });
    if (!response.ok) throw await ForgeApiError.fromResponse(response, path);
    return response.json() as Promise<Types.BulkWorklogKeyResponseBean>;
}
