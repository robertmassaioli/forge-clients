/**
 * Jira Cloud REST API v2 client (legacy)
 *
 * v2 is maintained for backwards compatibility.
 * New integrations should use v3 (the default export from @forge-clients/jira).
 *
 * This file and all *.gen.ts files in this directory are generated.
 * Run `pnpm generate` to regenerate from the latest cleaned spec.
 */

// Placeholder export so the package compiles before generation
export const JIRA_V2_PLACEHOLDER = 'Jira v2 client — run pnpm generate to populate';
